# ==========================================================
# Quarto Book のデプロイは quarto publish gh-pages で行います
# ローカルでレンダリング → gh-pages ブランチに直接 push
#
# 手順:
#   1. quarto render --to html     (ローカルでレンダリング)
#   2. quarto publish gh-pages     (gh-pages にデプロイ)
#
# 自動 CI は無効化しています（R パッケージのインストールが
# 複雑で CI 環境では不安定なため）
# ==========================================================

name: Quarto Publish (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This workflow is disabled."
          echo "Deploy locally using: quarto publish gh-pages"
