---
title: "メタ分析"
bibliography: references_metaanalysis.bib
nocite: |
  @harrer2021, @viechtbauer2010, @norris2006, @plonsky2014, @norris2015, @uchihara2019, @nakagawa2021, @cohen1988_ma, @stanley2014, @vevea1995, @page2021, @rosenthal1979, @innami2010, @burkner2017_ma, @cheung2014_metasem, @cheung2015_book, @assink2016, @konstantopoulos2011, @pustejovsky2022, @jak2021_masem, @berkey1998, @vandennoortgate2013, @cheung2019_reflections, @balduzzi2023, @salanti2012, @rucker2015
---

```{r}
#| label: pre-setup
#| include: false
pacman::p_load(tidyverse, gt)
theme_set(theme_bw(base_size = 12, base_family = "HiraginoSans-W3"))
theme_update(legend.position = "bottom")
```

## メタ分析とは何か――なぜ個別の研究では不十分なのか

「明示的な文法指導は英語ライティングの正確性を向上させるか」という研究課題を考える。先行研究を渉猟すると、効果を報告する研究もあれば、そうでない研究もあり、一見して知見が錯綜している。このような状況において、個別の研究結果をどのように統合し、全体的な傾向を把握すればよいのか――これがメタ分析の出発点である。

メタ分析（meta-analysis）とは、同一の研究課題に取り組んだ複数の実証研究の結果を定量的に統合する統計手法である。各研究を独立した「データポイント」として扱い、統計的に合成することで、個々の研究が抱えるサンプルサイズの制約や結果のばらつきを克服し、より信頼性の高い結論を導くことが可能となる。

ナラティブ・レビュー（先行研究を定性的にまとめる従来型の文献検討）と比較した場合、メタ分析には以下の利点がある。

- **定量的な統合**: 「多くの研究で効果が認められた」という質的判断ではなく、「平均効果は *g* = 0.42, 95% CI [0.18, 0.66]」と数値で提示できる
- **異質性の検討**: 効果が研究間でどの程度異なるかを定量的に評価できる（後述する $\tau^2$, $I^2$ などの指標）
- **モデレーター分析**: 「どのような条件下で効果が大きい（あるいは小さい）か」を体系的に探索できる

::: {.callout-note}
### 本章の位置づけ
本章は、メタ分析の実施手順を R の `metafor` パッケージを中心に解説する実践的なガイドである。効果量の基礎概念については「`1_T検定と効果量.qmd`」を参照されたい。本章では、複数の研究から得られた効果量を統合する手続きに焦点を当てる。
:::

::: {.callout-tip}
### Plot labels
文字化け（mojibake）を防ぐため、本章のすべての図は英語ラベルで描画している。各図の読み方は本文中で日本語により解説する。
:::


メタ分析は、効果量の算出から異質性の評価、モデレーター分析、出版バイアスの検討まで、段階的に進める。異質性の大きさによって次のステップが分岐する。

実務手順としては、まず研究を収集して効果量を算出し、ランダム効果モデルでプール推定を行う。次に $I^2$、$Q$、$\tau^2$ で異質性を評価し、異質性が大きい場合はサブグループ分析やメタ回帰でモデレーターを探索する。異質性が小さい場合はプール推定値を中心に報告し、どちらの経路でも最終段階でファンネルプロットや Trim-and-Fill による出版バイアス評価を実施する。最後に PRISMA に準拠して報告する。

::: {.callout-note}
英語教育研究のメタ分析では、介入の種類・学習者レベル・アウトカム指標などが異質性の主な要因となる。異質性が大きい場合（$I^2 > 75\%$）は、必ず**モデレーター分析**で要因を探索する必要がある。
:::


## クイックリファレンス――分析手法の選択

本章は多岐にわたる分析手法を扱うため、まず自身の研究の問いに合った手法を特定してから該当セクションに進むことを推奨する。以下の表は、リサーチ・クエスチョンの類型に対応する効果量・分析モデル・本章の該当箇所を示す。

```{r}
#| label: decision-map
#| echo: false
tibble::tribble(
  ~`研究の問い`, ~`典型例`, ~`効果量`, ~`分析モデル`, ~`該当セクション`,
  "介入の効果（2群比較の統合）", "文法指導 vs. 統制群", "Hedges *g* (SMDH)", "`rma()` ランダム効果", "PART I: Recipes 1--11",
  "変数間の関連（相関の統合）", "動機づけ × 習熟度", "Fisher *z* (ZCOR)", "`rma()` ランダム効果", "Recipe 12",
  "1研究から複数の効果量", "同一論文の語彙・文法・ライティング", "*g* or *z*", "`rma.mv()` 3水準モデル", "Recipe 21",
  "3つ以上の介入を同時比較", "グロス vs. SRS vs. ライティング指導", "SMD or OR", "`netmeta()` NMA", "PART II: Recipes 13--18",
  "ベイズ推定で統合したい", "研究数が少ない場合の安定推定", "*g* or *z*", "`brms` ベイズメタ分析", "Recipe 25",
  "研究内相関への頑健な対処", "語彙・文法の効果量が相関", "*g* or *z*", "CHE + `robust()`", "PART IV: Recipe 28",
  "理論モデルの統合的検証", "動機づけ→方略→習熟度", "相関行列", "MASEM (`lavaan`)", "PART IV: Recipe 29"
) |>
  gt() |>
  tab_header(title = "メタ分析の手法選択マップ") |>
  cols_label(
    `研究の問い` = "研究の問い",
    `典型例` = "典型例（英語教育）",
    `効果量` = "効果量",
    `分析モデル` = "分析モデル",
    `該当セクション` = "本章の該当箇所"
  ) |>
  tab_options(table.font.size = px(13)) |>
  tab_style(
    style = cell_text(weight = "bold"),
    locations = cells_column_labels()
  )
```

::: {.callout-tip}
### 読み方の指針
初めてメタ分析を実施する場合は、PART I（Recipes 1--12）を通読したうえで、自身の研究設計に応じて PART II（NMA）に進むことを推奨する。Recipe 21（多層モデル）は、1 つの論文から複数の効果量を抽出する場合に必須となるため、該当する場合は必ず参照されたい。PART IV は、3 水準モデルの数理的基盤、混合効果モデリングとの等価性、CHE モデル、MASEM など、方法論的に一歩進んだ内容を扱う。
:::


## メタ分析の実施プロセス――系統的レビューの 5 段階

メタ分析は統計的手続きであると同時に、**系統的レビュー**（systematic review）と呼ばれる研究方法論の一部をなす。R コードに着手する前に、以下の 5 段階を踏まえる必要がある[@page2021]。

1. **プロトコル登録**: [PROSPERO](https://www.crd.york.ac.uk/prospero/) などのデータベースに、研究課題・検索戦略・適格基準・分析計画を事前に登録する。事前登録により、分析の透明性と再現性が担保される
2. **系統的検索**: 複数のデータベース（Web of Science, Scopus, ERIC, PsycINFO 等）を用い、事前に定義した検索式で網羅的に文献を収集する[@innami2010]
3. **スクリーニング**: タイトル・抄録の一次スクリーニング → 全文の二次スクリーニングにより、適格基準に合致する研究を選定する
4. **データ抽出**: コーディングシートを用いて、各研究から効果量の算出に必要な統計量とモデレーター変数を抽出する（次節で詳述）
5. **統計的統合**: 本章の主題。抽出されたデータに対してメタ分析を実施する

#### PRISMA 2020 フロー図

PRISMA（Preferred Reporting Items for Systematic Reviews and Meta-Analyses）フロー図は、文献の選定プロセスを可視化する標準的な報告ツールである[@page2021]。以下の R コードで作成できる。

```{r}
#| label: prisma-flowchart
#| fig-width: 8
#| fig-height: 9
library(DiagrammeR)

# 数値は自身の研究に合わせて書き換えてください
grViz("
digraph prisma {
  graph [rankdir=TB, fontname='Helvetica', nodesep=0.4, ranksep=0.5]
  node [shape=box, style='filled', fontsize=11, fontname='Helvetica',
        fillcolor='#e8f4f8', color='#2c3e50', width=3.5, height=0.6]
  edge [color='#2c3e50', arrowsize=0.8]

  id    [label='Records identified\\nDatabases (k = 847)\\nOther sources (k = 23)']
  dup   [label='Duplicates removed\\n(k = 312)']
  scrn  [label='Records screened\\n(k = 558)']
  excl1 [label='Records excluded\\n(k = 478)', fillcolor='#fce4e4']
  ftxt  [label='Full-text articles assessed\\n(k = 80)']
  excl2 [label='Full-text excluded (k = 50)\\n- No control group (18)\\n- Insufficient statistics (15)\\n- Duplicate sample (10)\\n- Off-topic outcome (7)', fillcolor='#fce4e4']
  incl  [label='Studies included in\\nquantitative synthesis\\n(k = 30)', fillcolor='#d4edda']

  id -> dup -> scrn
  scrn -> excl1
  scrn -> ftxt
  ftxt -> excl2
  ftxt -> incl
}
")
```

::: {.callout-tip}
#### カスタマイズ方法
上記コード中の数値（847, 312, 558 など）を自身のレビューの実数に置き換えるだけで、論文投稿用の PRISMA 図が完成する。除外理由のカテゴリも研究に応じて編集されたい。
:::


### コーディングシートの設計とコーダー間信頼性

#### コーディングシートに含めるべき列

メタ分析のデータ抽出では、以下のカテゴリの情報をスプレッドシートに体系的に記録する。本書の `data/meta/uchihara2019_coding_sheet.xlsx` を実例として参照されたい。

| カテゴリ | 推奨列 | 備考 |
|:--|:--|:--|
| **識別情報** | Study ID, 著者, 出版年, 雑誌名 | Study ID は `Author_Year` 形式が便利 |
| **参加者** | N（介入群）, N（統制群）, L1, 習熟度, 教育段階 | モデレーター候補 |
| **介入** | 介入の種類, 期間（週）, 強度（時間/週） | 定義を事前にコードブックで明文化 |
| **効果量の入力値** | M1, SD1, n1, M2, SD2, n2 *または* t / F / r / p | 報告形式は論文ごとに異なる |
| **アウトカム** | テスト種類, 測定時点（直後/遅延） | 1論文から複数行の場合あり |
| **質指標** | ランダム化の有無, 盲検の有無, 脱落率 | Risk of bias の評価に使用 |

::: {.callout-warning}
#### コーディングでよくある間違い

**症状**: 分析後に一部の研究の効果量が不自然に大きい（または符号が逆）。

**原因**: (1) 介入群と統制群の M1/M2 の割り当てが研究間で逆転している、(2) 介入の名称表記が不統一（"CTRL", "Control", "統制群" が混在）。

**修正**: コーディング開始前に**コードブック**（各列の定義と入力規則を記した文書）を作成し、2 名のコーダーで独立にコーディングを行った後、一致率を確認する。
:::

#### コーダー間信頼性の算出

メタ分析の信頼性を確保するために、少なくとも全体の 20% の研究について 2 名のコーダーが独立にコーディングを行い、一致度を報告することが推奨される。カテゴリカル変数には Cohen's $\kappa$（カッパ）、連続変数には級内相関係数（ICC）を用いる。

```{r}
#| label: irr-example
library(irr)

# カテゴリカル変数の一致度（例: 教育段階のコーディング）
set.seed(2024)
coder1_cat <- sample(c("secondary", "tertiary"), 20, replace = TRUE)
# コーダー2は90%の確率で一致、10%で不一致
coder2_cat <- ifelse(runif(20) < 0.9, coder1_cat,
                     sample(c("secondary", "tertiary"), 20, replace = TRUE))
kappa_result <- kappa2(cbind(coder1_cat, coder2_cat))

tibble::tibble(
  指標 = c("Cohen's kappa", "p value"),
  値 = c(round(kappa_result$value, 3), round(kappa_result$p.value, 4))
) |>
  gt() |>
  tab_header(title = "コーダー間信頼性: カテゴリカル変数（教育段階）")
```

```{r}
#| label: icc-example
# 連続変数の一致度（例: 効果量 g のコーディング）
coder1_cont <- rnorm(20, 0.40, 0.15)
coder2_cont <- coder1_cont + rnorm(20, 0, 0.03)  # わずかなずれ
icc_result <- icc(cbind(coder1_cont, coder2_cont),
                  model = "twoway", type = "agreement", unit = "single")

tibble::tibble(
  指標 = c("ICC(2,1)", "95% CI lower", "95% CI upper"),
  値 = c(round(icc_result$value, 3),
         round(icc_result$lbound, 3),
         round(icc_result$ubound, 3))
) |>
  gt() |>
  tab_header(title = "コーダー間信頼性: 連続変数（効果量 g）")
```

Cohen's $\kappa$ の解釈の目安: 0.61--0.80 = substantial, 0.81--1.00 = almost perfect。ICC は 0.75 以上が望ましい。不一致が認められた場合は、2 名のコーダーが協議して最終コーディングを決定する。


## セットアップ

本章で使用するパッケージを一括して準備する。CRAN 上のパッケージに加え、GitHub から提供される可視化・診断用パッケージもインストールする。

```{r}
#| label: setup
#| message: false
#| warning: false
# CRAN パッケージ
pkgs <- c("metafor", "clubSandwich", "ggplot2", "gt", "dplyr", "tidyr",
          "metaviz", "forestploter", "scales", "knitr", "remotes",
          "esc", "effectsize", "irr", "DiagrammeR", "nlme", "lavaan")
for (p in pkgs) {
  if (!requireNamespace(p, quietly = TRUE)) {
    install.packages(p, repos = "https://cloud.r-project.org")
  }
}

# GitHub パッケージ（PART III で使用）
if (!requireNamespace("orchaRd", quietly = TRUE)) {
  remotes::install_github("daniel1noble/orchaRd", upgrade = "never", quiet = TRUE)
}
if (!requireNamespace("dmetar", quietly = TRUE)) {
  remotes::install_github("MathiasHarrer/dmetar", upgrade = "never", quiet = TRUE)
}

# ベイズメタ分析（PART III Recipe 25 で使用）
if (!requireNamespace("brms", quietly = TRUE)) {
  install.packages("brms", repos = "https://cloud.r-project.org")
}

library(metafor)
library(ggplot2)
library(dplyr)
library(tidyr)
library(gt)
library(DT)
set.seed(2024)
theme_set(theme_bw(base_size = 12, base_family = "HiraginoSans-W3"))
theme_update(legend.position = "bottom")

# メタ分析結果をgt表に変換するヘルパー関数
# selmodel 等のオブジェクトでは coef(summary()) の列名が異なる場合があるため、
# 複数の列名パターンに対応する。
rma_to_gt <- function(fit, title = "Meta-Analysis Results") {
  s <- tryCatch(coef(summary(fit)), error = function(e) NULL)
  if (is.null(s) || !is.matrix(s)) {
    # フォールバック: fit オブジェクトから直接抽出
    tbl <- tibble::tibble(
      Term     = "intrcpt",
      Estimate = as.numeric(fit$beta),
      SE       = fit$se,
      z        = fit$zval,
      p        = fit$pval,
      CI_lower = fit$ci.lb,
      CI_upper = fit$ci.ub
    )
  } else {
    # 列名のバリエーションに対応
    col_est <- intersect(colnames(s), c("estimate", "beta"))[1]
    col_se  <- intersect(colnames(s), c("se", "SE"))[1]
    col_z   <- intersect(colnames(s), c("zval", "z"))[1]
    col_p   <- intersect(colnames(s), c("pval", "p"))[1]
    col_lb  <- intersect(colnames(s), c("ci.lb", "CI.L"))[1]
    col_ub  <- intersect(colnames(s), c("ci.ub", "CI.U"))[1]
    tbl <- tibble::tibble(
      Term     = rownames(s),
      Estimate = s[, col_est],
      SE       = s[, col_se],
      z        = s[, col_z],
      p        = s[, col_p],
      CI_lower = s[, col_lb],
      CI_upper = s[, col_ub]
    )
  }
  tbl |>
    gt() |>
    tab_header(title = title) |>
    fmt_number(columns = c(Estimate, SE, z, CI_lower, CI_upper), decimals = 3) |>
    fmt_number(columns = p, decimals = 4)
}
```


## PART I: ペアワイズ・メタ分析

本章の前半では、最も基本的な形式である「2群の比較を複数の研究から統合する」ペアワイズ・メタ分析を扱う。学位論文や学術論文において最も頻繁に用いられる分析手法である。


### Recipe 1: 効果量の選択――統合の単位を決定する

メタ分析の第一段階は、各研究の結果を共通の尺度、すなわち効果量（effect size）に変換することである。

#### どの効果量を選択すべきか

選択の基準は、アウトカム（従属変数）の測定尺度によって決まる。

| アウトカムの種類 | 典型例 | 効果量 | `escalc()` の `measure` |
|:--|:--|:--|:--|
| 連続値（同一テスト） | TOEFL点数の差 | 平均差 (MD) | `"MD"` |
| 連続値（異なるテスト） | ライティング得点 vs. 文法テスト | 標準化平均差 (Hedges *g*) | `"SMDH"`^[`"SMD"` = Cohen's *d*（小標本補正なし）、`"SMDH"` = Hedges' *g*（小標本補正付き）。サンプルサイズが小さい研究を含む場合は `"SMDH"` を使用する。] |
| 二値（成功/失敗） | 合格率 | 対数オッズ比 (log OR) | `"OR"` |
| 相関 | 動機づけと成績の関連 | Fisher *z* | `"ZCOR"` |

英語教育研究では、異なるテストや尺度を用いた研究を統合する場面が多いため、**標準化平均差（Hedges *g*）**が最も広く用いられる。

#### Hedges *g* の定義

Hedges *g* は、2群の平均差をプールされた標準偏差で割った値に、小標本バイアスの補正を施した指標である。

$$
g = J(df) \times \frac{\bar{X}_1 - \bar{X}_2}{S_{\text{pooled}}}
$$

ここで $J(df)$ は小標本補正係数であり、サンプルサイズが小さい場合に効果量がやや過大に推定される傾向を修正する。解釈の目安として、$|g|$ = 0.2（小）、0.5（中）、0.8（大）が広く参照される[@cohen1988_ma]。ただし、この基準は分野や研究文脈によって適切性が異なるため、機械的な適用は避けるべきである。応用言語学においては、@plonsky2014 の分野固有の基準（$d$ = 0.40 で small, 0.70 で medium, 1.00 で large）も参照されたい。

#### 効果量の方向の定義

メタ分析に着手する前に、**正の効果量が何を意味するかを明確に定義する**必要がある。本章では一貫して「正の値 = 介入（明示的文法指導）が統制群に対して有利」と定義する。この定義をコーディングシートの段階で統一しておくことが、分析全体の整合性を確保するうえで不可欠である。


### Recipe 2: データの準備――英語教育研究のシナリオ

#### 分析のシナリオ

以下の仮想的なシナリオに基づき、分析の手順を解説する。

> **リサーチ・クエスチョン**: 明示的な文法指導（Explicit Grammar Instruction）は、EFL/ESLの英語ライティングにおける文法的正確性を向上させるか？

想定する研究の特徴は以下の通りである。

- **介入群**: 明示的文法指導を受けた学習者
- **統制群**: 通常の授業を受けた学習者
- **アウトカム**: ライティングテストの得点（連続値）
- **モデレーター候補**: 教育段階（中等教育 vs. 高等教育）、介入期間（週数）

以下では 30 件の研究をシミュレーションにより生成する。真のパラメータとして、ベースラインの効果量 *g* = 0.35、高等教育で +0.10、介入 1 週あたり +0.01 を設定している。

```{r}
#| label: simulate
n_study <- 30
study_id <- sprintf("Study_%02d", 1:n_study)

# モデレーター
level <- sample(c("secondary", "tertiary"), n_study, replace = TRUE, prob = c(0.6, 0.4))
duration <- sample(4:16, n_study, replace = TRUE)
year <- sample(2010:2024, n_study, replace = TRUE)

# 真の効果（ランダム効果モデル）
# ベース 0.35 + 高等教育ボーナス + 週数効果 + 研究間分散 tau^2 = 0.04
tau <- sqrt(0.04)
theta <- 0.35 + ifelse(level == "tertiary", 0.10, 0) +
  0.01 * (duration - 10) + rnorm(n_study, 0, tau)

# 各研究のサンプルサイズと集計統計量
set.seed(2024)
n1 <- sample(25:60, n_study, replace = TRUE)
n2 <- sample(25:60, n_study, replace = TRUE)
sd_pooled <- runif(n_study, 8, 15)
m2 <- rnorm(n_study, 70, 8)
m1 <- m2 + theta * sd_pooled
sd1 <- sd_pooled * runif(n_study, .9, 1.1)
sd2 <- sd_pooled * runif(n_study, .9, 1.1)

raw <- data.frame(study_id, level, duration, year, m1, sd1, n1, m2, sd2, n2)

# Hedges g と分散を計算
es <- escalc(
  measure = "SMDH",
  m1i = m1, sd1i = sd1, n1i = n1,
  m2i = m2, sd2i = sd2, n2i = n2,
  data = raw
)
dat <- cbind(raw, yi = es$yi, vi = es$vi)
```

`escalc()` は各研究の集計統計量（平均、SD、サンプルサイズ）から効果量 `yi` とその分散 `vi` を算出する。この `yi` と `vi` の対がメタ分析の入力データとなる。

```{r}
#| label: show-data
dat |>
  select(study_id, level, duration, yi, vi) |>
  DT::datatable(caption = "最初の10研究の効果量と分散")
```

::: {.callout-tip}
#### 実データへの適用
実際のメタ分析では、各論文から平均値・標準偏差・サンプルサイズを抽出し、データフレームに格納する。論文が *t* 値や *F* 値のみを報告している場合は、`escalc()` の引数（`ti=`, `fi=` など）や効果量変換パッケージ（`esc`, `effectsize`）、オンラインツール（[Effect Size Calculator](https://www.escal.site/)）を用いて変換が可能である。効果量変換の詳細は Recipe 23 で扱う。
:::


### Recipe 3: ランダム効果モデルによる統合――分析の中核

#### 固定効果モデルとランダム効果モデルの選択

この選択はデータを観察してから決定するものではなく、**研究デザインの性質に基づいて事前に**決定する。

- **固定効果モデル（fixed-effect model）**^[英語表記では fixed-effect（単数形）が慣例的に用いられる。1 つの共通の真の効果を仮定するモデルであるため単数形になる。一方、ランダム効果モデルは効果の分布を仮定するため random-effects（複数形）で表記する。]: すべての研究が同一の真の効果を推定していると仮定する。各研究の重みはサンプルサイズ（精度）のみによって決まる。同一の薬剤を同一条件で検証した治験シリーズなどに適している。
- **ランダム効果モデル（random-effects model）**: 研究ごとに真の効果が異なりうる（真の効果が分布を持つ）と仮定する。英語教育研究では、学習者の年齢・L1 背景・指導者・テスト形式などが研究間で異なるため、**ほぼ常にランダム効果モデルが適切である**。

ランダム効果モデルにおける重み $w_i$ は以下のように定義される。

$$
w_i = \frac{1}{v_i + \hat{\tau}^2}
$$

ここで $v_i$ は各研究の標本分散（サンプリング誤差）、$\hat{\tau}^2$ は研究間分散の推定値である。固定効果モデル（$\hat{\tau}^2 = 0$）と比較すると、小規模な研究に相対的に大きな重みが付与され、信頼区間は広くなる。すなわち、研究間のばらつきに起因する不確実性が推定に反映される。

#### モデルの適合

以下では、シミュレーションデータに対してランダム効果モデルを適合する。`rma()` 関数に効果量 `yi` とその分散 `vi` を渡し、推定法として REML（制限付き最尤法）を指定する。REML は $\tau^2$ の推定において偏りが小さいため、メタ分析では標準的に用いられる。

```{r}
#| label: reml
m <- rma(yi, vi, data = dat, method = "REML")
rma_to_gt(m, title = "ランダム効果モデル（REML）の推定結果")
```

#### 出力の解釈

`rma()` の出力において注目すべきポイントは、大きく 3 点に整理できる。

**1. 平均効果量（estimate）**

- 正の値は介入（明示的文法指導）が統制群に対して有利であることを意味する
- 95% CI が 0 を含まない場合、統計的に有意である
- ただし、**統計的有意性のみならず効果の大きさそのもの**に注目すべきである。*g* = 0.10 であっても大規模なメタ分析では有意となりうるが、教育的に意味のある差であるとは限らない

**2. 異質性の指標**

- **$\tau^2$（タウ二乗）**: 研究間分散の推定値。$\sqrt{\tau^2} = \tau$ は効果量と同じ単位で解釈できる。たとえば $\tau$ = 0.20 であれば、「真の効果量は研究間で *SD* = 0.20 程度のばらつきを持つ」ことを意味する
- **$I^2$**: 観測されたばらつきのうち、真の異質性（サンプリング誤差に帰属しない部分）が占める割合。概ね 25% = 低、50% = 中、75% = 高とされるが、**この基準への過度な依存は避けるべき**である
- **$Q$**: 効果量の同質性を検定する統計量。研究数が多い場合にはほぼ常に有意となるため、$Q$ 検定のみで判断することは適切でない

**3. 予測区間（prediction interval）**

予測区間は「今後同様の研究を実施した場合、効果量がどの範囲に収まるか」を示す区間である。信頼区間が「平均効果の推定精度」を反映するのに対し、予測区間は「研究間のばらつきを含めた効果の実質的な範囲」を表す。教育実践への含意を論じる際には、**予測区間が信頼区間よりも重要な情報を提供する**。

```{r}
#| label: prediction-interval
predict(m)
```

**読み方**: `pred` がプール推定値、`ci.lb` / `ci.ub` が 95% 信頼区間、`pi.lb` / `pi.ub` が 95% 予測区間である。予測区間の下限が負の値を含む場合、一部の研究文脈では介入効果がゼロまたは負になりうることを示唆する。たとえば予測区間が [−0.05, 0.80] であれば、「全体的には正の効果が期待されるが、文脈によってはほぼ効果が見られない可能性もある」と解釈できる。

::: {.callout-warning}
#### 解釈における典型的な誤り

1. ***p* 値のみに基づく結論**: 効果量の大きさ・信頼区間・予測区間を必ず併せて報告する
2. **$I^2$ の過大評価・過小評価**: $I^2$ が低くとも予測区間が広い場合がある。$I^2$ は「ばらつきの大きさ」ではなく「ばらつきに占める真の異質性の割合」である
3. **95% CI が 0 を含む = 効果が存在しない**: 正確には「本分析においては統計的に有意な効果は検出されなかった」ということであり、効果の大きさ自体の報告には依然として意義がある
:::


### Recipe 4: 結果の可視化――フォレストプロットとファンネルプロット

#### フォレストプロット

フォレストプロットは、メタ分析の結果を報告するうえで最も基本的かつ重要な図である。以下のコードでは、各研究の効果量と 95% CI を水平線で、統合効果をダイヤモンドで描画する。`slab` 引数に研究 ID を指定することで各行にラベルが付与される。

```{r}
#| label: forest-plot
#| fig-width: 10
#| fig-height: 10
forest(m, slab = dat$study_id, xlab = "Hedges g", refline = 0,
       header = "Study / Effect Size [95% CI]")
```

**図の読み方:**

- 各行が 1 つの研究に対応する。**四角形（点推定値）**と**水平線（95% CI）**で構成される
- 四角形の大きさは当該研究の重み（精度）を反映する。大きい四角形ほどサンプルサイズが大きく精度が高い
- **垂直参照線（0 の位置）**より右に位置する研究は「介入が有利」、左は「統制群が有利」
- 最下部の**菱形（ダイヤモンド）**が統合された全体効果であり、菱形の幅が 95% CI に対応する

#### ファンネルプロット

ファンネルプロットは、出版バイアスの有無を視覚的に検討するための図である。

```{r}
#| label: funnel-plot
#| fig-width: 8
#| fig-height: 6
funnel(m, xlab = "Hedges g")
```

**図の読み方:**

- 横軸が効果量、縦軸が精度（標準誤差；上方ほど精度が高い）
- バイアスが存在しなければ、大規模研究（上部）は平均効果の近傍に集中し、小規模研究（下部）は広く散布して**逆三角形（漏斗型）**を形成する
- **左右非対称**が認められる場合、出版バイアスの存在が示唆されるが、**非対称性は必ずしも出版バイアスを意味しない**。異質性や方法論的質の差異も非対称の原因となりうる

#### Egger 検定（ファンネルプロットの非対称性の統計的検定）

```{r}
#| label: egger-test
regtest(m, model = "rma")
```

Egger 検定はファンネルプロットの非対称性を統計的に検定する手法である。*p* < .05 であれば非対称性が示唆されるが、研究数が少ない場合（*k* < 10）は検出力が低いため、結果の解釈には留意が必要である。出版バイアスの評価は、**単一の検定に依拠するのではなく、複数の手法を組み合わせて**行うことが推奨される。


### Recipe 5: 出版バイアスの詳細な検討

#### Trim-and-fill 法

Trim-and-fill 法は、ファンネルプロットの非対称性を「欠損研究の存在」に帰属すると仮定し、仮想的に欠損研究を補完したうえで効果量を再推定する感度分析の手法である。以下のコードでは、元のモデル `m` に `trimfill()` を適用し、補完前後のファンネルプロットを並置して比較する。白い点が観測された研究、黒い点が補完された仮想的研究を表す。

```{r}
#| label: trim-fill
#| fig-width: 10
#| fig-height: 5
tf <- trimfill(m)
par(mfrow = c(1, 2))
funnel(m, xlab = "Hedges g", main = "Observed")
funnel(tf, xlab = "Hedges g", main = "Trim-and-fill")
par(mfrow = c(1, 1))

# 補完後の推定値
rma_to_gt(tf, title = "Trim-and-Fill 補完後の推定結果")
```

::: {.callout-warning}
#### Trim-and-fill 法の限界
本手法はあくまで**感度分析**であり、「真の効果量」を推定するものではない。「出版バイアスが存在すると仮定した場合に、結論がどの程度変動するか」を検討するために用いる。補完前後で推定値が大きく変動する場合は、出版バイアスの影響について慎重な議論が求められる。
:::

#### コンター補強ファンネルプロット

有意水準の帯を重ねることで、統計的に有意な結果が偏って分布していないかを視覚的に確認できる。

```{r}
#| label: contour-funnel
#| fig-width: 8
#| fig-height: 6
funnel(m, xlab = "Hedges g", level = c(90, 95, 99),
       shade = c("white", "gray85", "gray70"),
       legend = TRUE)
```


#### Fail-safe N（Rosenthal 法）

Fail-safe N は「統合効果を統計的に非有意にするために、効果量ゼロの未発表研究が何件必要か」を算出する古典的な手法である[@rosenthal1979]。多くの審査委員が依然としてこの指標を要求するため、報告しておくことが実務上有用である。

```{r}
#| label: failsafe-n
fsn(yi, vi, data = dat, type = "Rosenthal")
```

Fail-safe N が研究数 $k$ の 5 倍 + 10（すなわち `5k + 10`）を超えていれば、出版バイアスの影響は限定的と判断される。ただし、この基準は保守的すぎるとの批判もあり、**Fail-safe N のみで出版バイアスの有無を結論づけるべきではない**。

#### PET-PEESE（精度効果検定）

PET-PEESE は、効果量と標準誤差（または分散）の間の関連を回帰モデルで検討する手法である[@stanley2014]。出版バイアスが存在する場合、精度の低い（標準誤差の大きい）研究ほど効果量が大きい傾向が見られ、この関連の強さからバイアスの程度を推定する。

```{r}
#| label: pet-peese
# PET: 標準誤差を予測子とする
dat$sei <- sqrt(dat$vi)
pet <- rma(yi, vi, mods = ~ sei, data = dat, method = "REML")

# PEESE: 分散を予測子とする
peese <- rma(yi, vi, mods = ~ vi, data = dat, method = "REML")

# PET-PEESE の判断ロジック:
# PET の切片が有意 → PEESE の切片をバイアス調整済み推定値として採用
# PET の切片が非有意 → 有意な効果は存在しないと判断
tibble::tribble(
  ~手法, ~切片推定値, ~`95% CI lower`, ~`95% CI upper`, ~`p value`,
  "PET",   pet$b[1],   pet$ci.lb[1],   pet$ci.ub[1],   pet$pval[1],
  "PEESE", peese$b[1], peese$ci.lb[1], peese$ci.ub[1], peese$pval[1]
) |>
  gt() |>
  tab_header(title = "PET-PEESE 結果: バイアス調整済み効果量推定値") |>
  fmt_number(columns = c(切片推定値, `95% CI lower`, `95% CI upper`), decimals = 3) |>
  fmt_number(columns = `p value`, decimals = 4)
```

::: {.callout-note}
#### PET-PEESE の使用手順
1. まず **PET**（`mods = ~sei`）を実行する
2. PET の切片が有意（*p* < .05）であれば、**PEESE**（`mods = ~vi`）の切片をバイアス調整済み推定値として採用する
3. PET の切片が非有意であれば、「統計的に有意な効果は（バイアスを考慮すると）検出されない」と解釈する
:::

#### 選択モデル（Vevea-Hedges 法）

選択モデルは、*p* 値の分布に基づいて「有意な結果が出版されやすい」という選択過程を明示的にモデル化する手法である[@vevea1995]。`metafor` の `selmodel()` 関数で実行できる。

```{r}
#| label: selection-model
# ステップ関数型選択モデル（p < .05 vs. p >= .05）
sel <- selmodel(m, type = "stepfun", steps = c(0.05))
rma_to_gt(sel, title = "選択モデル: バイアス調整済み推定値")
```

選択モデルは Trim-and-fill や PET-PEESE よりも統計的に洗練された手法であるが、研究数が少ない場合（*k* < 20）には推定が不安定になる点に留意が必要である。

#### 出版バイアス評価手法の比較

| 手法 | 強み | 限界 | 推奨場面 |
|:--|:--|:--|:--|
| Egger 検定 | 簡便・広く普及 | *k* < 10 で検出力不足 | 第一段階のスクリーニング |
| Trim-and-fill | 視覚的に理解しやすい | 感度分析にとどまる | ファンネルプロットとの併用 |
| Fail-safe N | 審査者の要求に対応 | 理論的根拠が弱い | 補完的な報告 |
| PET-PEESE | バイアス調整済み推定値を提供 | 異質性が大きい場合に精度低下 | メイン分析の感度分析 |
| 選択モデル | 選択過程を明示的にモデル化 | *k* < 20 で不安定 | *k* ≥ 20 の大規模メタ分析 |

::: {.callout-warning}
#### 出版バイアスの評価は複数手法で
単一の手法に依拠して出版バイアスの有無を結論づけるべきではない。上記の手法を**複数組み合わせ**、結果が一貫しているかを総合的に判断することが推奨される。各手法が異なる結論を示す場合は、その不一致について透明に議論する。
:::


### Recipe 6: 異質性の原因の探索――サブグループ分析とメタ回帰

$I^2$ や $\tau^2$ が大きい値を示す場合、次の段階として「なぜ研究間で効果にばらつきが生じるのか」を検討する。ここではモデレーター（調整変数）を用いた分析を紹介する。

#### サブグループ分析とメタ回帰の使い分け

- **サブグループ分析**: モデレーターが**カテゴリカル変数**（中等教育 vs. 高等教育）の場合に用いる
- **メタ回帰**: モデレーターが**連続変数**（介入期間の週数）の場合に用いる

両者を組み合わせることも可能である。

#### サブグループ分析（教育段階別）

以下では、教育段階（secondary vs. tertiary）をカテゴリカルなモデレーターとして投入する。`mods = ~ factor(level)` は回帰分析のダミーコーディングに相当し、参照カテゴリ（secondary）との差分として tertiary の効果を推定する。

```{r}
#| label: subgroup
m_grp <- rma(yi, vi, mods = ~ factor(level), data = dat, method = "REML")
rma_to_gt(m_grp, title = "サブグループ分析（教育段階別）")
```

`mods = ~ factor(level)` により、教育段階をモデレーターとして投入している。回帰分析の枠組みでは、これは**切片＋ダミー変数の回帰モデル**に相当する。切片が参照カテゴリ（secondary）の平均効果量、回帰係数が tertiary との差分を表す。

各群の平均効果量を直接推定する場合は、切片なしモデルを適合する。

```{r}
#| label: subgroup-means
m_grp0 <- rma(yi, vi, mods = ~ 0 + factor(level), data = dat, method = "REML")
rma_to_gt(m_grp0, title = "サブグループ別平均効果量（切片なしモデル）")
```

#### メタ回帰（介入期間）

```{r}
#| label: meta-regression
m_reg <- rma(yi, vi, mods = ~ duration, data = dat, method = "REML")
rma_to_gt(m_reg, title = "メタ回帰（介入期間）")
```

`duration` の回帰係数が正であれば、介入期間が長いほど効果量が大きい傾向を示唆する。

#### メタ回帰の疑似 $R^2$（異質性の説明割合）

メタ回帰においては、モデレーターの投入によって研究間分散 $\tau^2$ がどの程度減少したかを疑似 $R^2$ として報告できる。これは「モデレーターが異質性の何%を説明するか」を表す指標であり、OLS 回帰の $R^2$ とは異なる概念である点に注意が必要である。

```{r}
#| label: pseudo-r2
# 疑似 R² = (tau2_null - tau2_moderator) / tau2_null
pseudo_r2 <- (m$tau2 - m_reg$tau2) / m$tau2
pseudo_r2 <- max(0, pseudo_r2)  # 負値の場合は 0 に切り上げ

tibble::tibble(
  モデル = c("切片のみ（ベースライン）", "メタ回帰（duration）"),
  `tau²` = c(m$tau2, m_reg$tau2),
  `疑似 R²` = c(NA, pseudo_r2)
) |>
  gt() |>
  tab_header(title = "メタ回帰による異質性の説明割合") |>
  fmt_number(columns = `tau²`, decimals = 4) |>
  fmt_percent(columns = `疑似 R²`, decimals = 1) |>
  sub_missing(missing_text = "---")
```

疑似 $R^2$ が小さい場合、当該モデレーターは異質性の主要な原因ではないことを示唆する。なお、`rma()` の出力中にも `QM`（モデレーター検定）と `QE`（残差異質性検定）が報告される。QM が有意であればモデレーターの寄与が統計的に有意であり、QE が有意であればモデレーター投入後もなお未説明の異質性が残っていることを意味する。

::: {.callout-warning}
#### モデレーター分析における留意事項

- **検出力**: サブグループ分析やメタ回帰で実質的な差を検出するには、各群に最低でも 10 件程度の研究が必要とされる。少数の研究に基づいて「差は認められなかった」と結論するのは早計である
- **生態学的誤謬（ecological fallacy）**: メタ回帰は**研究レベル**の関連を検討している。「介入期間が長い研究ほど効果が大きい」という知見と「個々の学習者が長く学習するほど効果が大きい」という命題は、論理的に等価ではない
- **事前登録**: モデレーター分析は探索的になりやすいため、検討するモデレーターを**事前にプロトコルで登録する**ことが推奨される（PROSPERO 等）
:::

#### バブルプロット（メタ回帰の可視化）

```{r}
#| label: bubble-plot
#| fig-width: 8
#| fig-height: 6
xseq <- seq(min(dat$duration), max(dat$duration), length.out = 50)
pred <- predict(m_reg, newmods = xseq)
df_pred <- data.frame(
  duration = xseq,
  pred = as.numeric(pred$pred),
  ci.lb = as.numeric(pred$ci.lb),
  ci.ub = as.numeric(pred$ci.ub)
)

ggplot(dat, aes(x = duration, y = yi)) +
  geom_point(aes(size = 1 / sqrt(vi)), alpha = .6) +
  geom_line(data = df_pred, aes(x = duration, y = pred),
            color = "#2c3e50", linewidth = 1, inherit.aes = FALSE) +
  geom_ribbon(data = df_pred, aes(x = duration, ymin = ci.lb, ymax = ci.ub),
              fill = "#2c3e50", alpha = .15, inherit.aes = FALSE) +
  scale_size_continuous(name = "Precision (1/SE)") +
  labs(x = "Intervention duration (weeks)", y = "Hedges g",
       title = "Meta-regression: Effect by intervention duration") +
  theme_bw(base_size = 12, base_family = "HiraginoSans-W3") + theme(legend.position = "bottom")
```

**図の読み方**: 各点が 1 つの研究に対応し、点の大きさは精度（サンプルサイズ）を反映する。回帰線が右上がりであれば、介入期間が長い研究ほど効果量が大きい傾向を示す。帯は 95% CI である。


### Recipe 7: 感度分析――結果の頑健性の検証

メタ分析の結果が特定の研究に強く依存していないかを確認する感度分析は、分析過程において不可欠な手続きである。以下の 3 つの手法を組み合わせて結果の頑健性を検証する。

::: {.panel-tabset}

#### Leave-one-out 分析

各研究を 1 つずつ除外してモデルを再推定し、全体推定値の変動幅を確認する。特定の 1 件の研究を除外しただけで推定値が大きく変動する場合、その研究が結果に強い影響を及ぼしていることを意味する。

```{r}
#| label: leave-one-out
#| fig-width: 10
#| fig-height: 8
loo <- leave1out(m)
df_loo <- as.data.frame(loo)
df_loo$study <- dat$study_id
df_loo$idx <- seq_len(nrow(df_loo))

ggplot(df_loo, aes(x = idx, y = estimate)) +
  geom_hline(yintercept = as.numeric(coef(m)),
             linetype = "dashed", color = "gray50") +
  geom_errorbar(aes(ymin = ci.lb, ymax = ci.ub),
                width = 0.2, color = "#2c3e50", alpha = .6) +
  geom_point(color = "#e31a1c", size = 2) +
  scale_x_continuous(breaks = df_loo$idx, labels = df_loo$study) +
  labs(x = "Left-out study", y = "Leave-one-out estimate (Hedges g)",
       title = "Sensitivity: Leave-one-out analysis") +
  theme_bw(base_size = 12, base_family = "HiraginoSans-W3") + theme(legend.position = "bottom") +
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, size = 8))
```

**図の読み方**: 破線が全研究を含めた推定値を示す。特定の研究を除外した際に推定値が破線から大きく乖離する場合、当該研究は全体の結果に対して強い影響力を有している。

#### Baujat・影響度診断

```{r}
#| label: influence-diagnostics
#| fig-width: 10
#| fig-height: 5
par(mfrow = c(1, 2))
baujat(m, xlab = "Contribution to Q statistic",
       ylab = "Influence on overall result")
radial(m)
par(mfrow = c(1, 1))
```

**Baujat プロット**（左）: 横軸が異質性（$Q$ 統計量）への寄与度、縦軸が全体推定値への影響力を表す。右上に位置する研究は異質性と全体結果の双方に強い影響を及ぼしており、精査が求められる。

**Radial プロット**（右）: 回帰線から大きく逸脱する点は外れ値の可能性がある。データの入力誤りや方法論上の問題がないかを確認すべきである。

```{r}
#| label: influence-detail
#| fig-width: 10
#| fig-height: 8
inf <- influence(m)
plot(inf)
```

#### 累積メタ分析

研究が時系列に沿って蓄積される過程で、効果量の推定値がどのように安定していくかを確認する手法である。

```{r}
#| label: cumulative
#| fig-width: 10
#| fig-height: 10
o <- order(dat$year, dat$study_id)
mc <- cumul(m, order = o)
forest(mc, xlab = "Cumulative Hedges g", refline = 0)
```

**図の読み方**: 上から下に向かって研究が時系列順に追加される。推定値が徐々に安定していれば、十分な数の研究が蓄積されていることを示唆する。初期の研究に推定値が過度に引きずられていないかについても確認が可能である。

:::


### Recipe 8: ロバスト分散推定――非独立な効果量への対処

#### 非独立効果量の問題

実際のメタ分析では、1 つの論文が複数のアウトカム（例：語彙テスト、文法テスト、ライティングテスト）について効果量を報告していることが少なくない。これらの効果量は同一の参加者から得られているため統計的に非独立であり、この非独立性を無視すると信頼区間が過度に狭くなり、第一種の過誤率が膨張する。

対処法として主に 2 つのアプローチが存在する。

1. **ロバスト分散推定（RVE）**: `clubSandwich` パッケージの CR2 推定量を用い、クラスタ（= 研究）単位で頑健な標準誤差を算出する方法。研究数が少ない場合にも比較的安定した推定が得られる
2. **多層モデル（`rma.mv()`）**: `metafor` の多変量モデルにより、研究内・研究間の分散構造を明示的にモデル化する方法。より柔軟であるが、仮定が増える（Recipe 21 で詳述）

本節のシミュレーションデータでは各研究から 1 効果量のみを抽出しているため両者の差は小さいが、実務への準備として RVE の適用手順を示す。

```{r}
#| label: robust
library(clubSandwich)
cs <- coef_test(m, vcov = "CR2", cluster = dat$study_id)
cs
```

**出力の読み方**: `coef_test()` は CR2 推定量に基づく頑健な標準誤差を返す。各列の意味は以下の通り: `Estimate` = 効果量の推定値（`rma()` の結果と同じ）、`SE` = CR2 による頑健な標準誤差（通常の `rma()` の SE より大きくなることが多い）、`tstat` = *t* 統計量、`df_Satt` = Satterthwaite 近似による自由度（研究数が少ない場合に重要）、`p_Satt` = Satterthwaite 調整済み *p* 値。

::: {.callout-tip}
#### RVE と通常の推定結果が乖離する場合
RVE を適用すると標準誤差が増大することが多い。これは、非独立性を無視した場合の標準誤差が過度に楽観的であったことを意味する。分析結果の報告においては、RVE による推定値を採用し、通常の推定値との差異について透明性をもって議論することが望ましい。
:::


### Recipe 9: サブグループ別フォレストプロット

```{r}
#| label: forest-subgroup
#| fig-width: 10
#| fig-height: 12
par(mfrow = c(1, 2))
dat_sec <- subset(dat, level == "secondary")
dat_ter <- subset(dat, level == "tertiary")
m_sec <- rma(yi, vi, data = dat_sec, method = "REML")
m_ter <- rma(yi, vi, data = dat_ter, method = "REML")
forest(m_sec, slab = dat_sec$study_id, xlab = "Hedges g", main = "Secondary")
forest(m_ter, slab = dat_ter$study_id, xlab = "Hedges g", main = "Tertiary")
par(mfrow = c(1, 1))
```

サブグループ別のフォレストプロットを並置することで、群間における効果量の大きさと一貫性の差異を視覚的に把握できる。


### Recipe 10: 結果の表形式による要約

```{r}
#| label: summary-table
library(gt)

# モデル要約
summ <- data.frame(
  Estimate = m$b[, 1],
  SE = m$se,
  z = m$zval,
  p = m$pval,
  CI_lower = m$ci.lb,
  CI_upper = m$ci.ub,
  tau2 = m$tau2,
  I2 = m$I2,
  k = m$k
)

gt(summ) |>
  tab_header(title = "ランダム効果モデル（REML）要約") |>
  fmt_number(columns = c(Estimate, SE, z, CI_lower, CI_upper, tau2, I2),
             decimals = 3) |>
  fmt_number(columns = p, decimals = 4) |>
  fmt_integer(columns = k) |>
  tab_options(table.font.size = px(14))
```

```{r}
#| label: effect-table
# 効果量の大きい研究（|g| 上位10件）
top <- dat |>
  arrange(desc(abs(yi))) |>
  head(10) |>
  select(study_id, level, duration, yi, vi)

gt(top) |>
  tab_header(title = "効果量（|g| が大きい順・上位10件）") |>
  fmt_number(columns = c(yi, vi), decimals = 3) |>
  fmt_integer(columns = duration) |>
  tab_options(table.font.size = px(14))
```


### Recipe 11: 公開データによる実践――metafor 付属データ

シミュレーションデータに加え、実データでの分析経験を積むことが重要である。`metafor` パッケージには多数の実データセットが付属している。ここでは BCG ワクチンの有効性に関するデータ（`dat.bcg`）を用い、相対リスク（RR）による統合を示す。

```{r}
#| label: bcg-example
data(dat.bcg, package = "metafor")
dat.bcg <- escalc(measure = "RR",
                  ai = tpos, bi = tneg,
                  ci = cpos, di = cneg,
                  data = dat.bcg)

m_bcg <- rma(yi, vi, data = dat.bcg, method = "REML")
rma_to_gt(m_bcg, title = "BCGワクチン試験: ランダム効果モデル（log RR）")
```

```{r}
#| label: bcg-forest
#| fig-width: 10
#| fig-height: 8
forest(m_bcg, slab = paste(dat.bcg$author, dat.bcg$year),
       xlab = "log(RR)", header = "Study / log(RR) [95% CI]")
```

```{r}
#| label: bcg-funnel
#| fig-width: 8
#| fig-height: 6
funnel(m_bcg, xlab = "log(RR)")
```

二値アウトカムの場合、効果量は log(RR) または log(OR) のスケールで分析し、報告時に `exp()` で RR・OR に変換すると解釈が容易になる。たとえば、log(RR) = −0.71 は RR = `r round(exp(-0.71), 2)` に対応し、「ワクチン接種群は非接種群と比較して結核罹患リスクが `r round((1 - exp(-0.71)) * 100)`% 低い」と解釈される。


### Recipe 12: 相関ベースのメタ分析――Fisher *z* による統合 {#sec-recipe12}

英語教育・SLA 研究では、2 群比較だけでなく**変数間の相関**を統合するメタ分析も頻繁に実施される。動機づけと習熟度、語彙知識と読解力、不安と成績など、連続変数同士の関連を複数の研究から統合する場合がこれに該当する。

#### 分析のシナリオ

> **リサーチ・クエスチョン**: L2 動機づけ（motivation）と英語習熟度（proficiency）の間にはどの程度の相関があるか？

#### なぜ Fisher *z* 変換が必要か

相関係数 *r* は −1 から +1 の範囲に制約されるため、*r* が 0 から離れるほど分布が非対称になり、分散の推定が歪む。Fisher *z* 変換（$z = 0.5 \times \ln\frac{1+r}{1-r}$）を適用することで、分布がほぼ正規となり、分散が $\frac{1}{n-3}$ と安定する。分析は *z* スケールで行い、結果を `tanh()` で *r* スケールに逆変換して報告する。

#### データの準備

```{r}
#| label: cor-simulate
set.seed(2024)
n_cor <- 15
mot_type <- sample(c("integrative", "instrumental"), n_cor, replace = TRUE)
true_r <- ifelse(mot_type == "integrative", 0.38, 0.28) + rnorm(n_cor, 0, 0.08)
true_r <- pmin(pmax(true_r, -0.95), 0.95)  # [-0.95, 0.95] に制約
n_sample <- sample(50:300, n_cor, replace = TRUE)

# 観測 r をシミュレーション（Fisher z 経由）
obs_z <- atanh(true_r) + rnorm(n_cor, 0, 1 / sqrt(n_sample - 3))
obs_r <- tanh(obs_z)

dat_cor <- data.frame(
  study_id = sprintf("Cor_%02d", 1:n_cor),
  r = round(obs_r, 3),
  n = n_sample,
  mot_type = mot_type
)

# Fisher z 変換
es_cor <- escalc(measure = "ZCOR", ri = dat_cor$r, ni = dat_cor$n)
dat_cor$yi <- as.numeric(es_cor$yi)
dat_cor$vi <- as.numeric(es_cor$vi)

DT::datatable(dat_cor, caption = "動機づけ × 習熟度: シミュレーションデータ")
```

#### ランダム効果モデルの適合

```{r}
#| label: cor-rma
m_cor <- rma(yi, vi, data = dat_cor, method = "REML", slab = study_id)
rma_to_gt(m_cor, title = "相関メタ分析: ランダム効果モデル（Fisher z）")
```

```{r}
#| label: cor-backtransform
# Fisher z を r に逆変換して報告
pred_cor <- predict(m_cor, transf = transf.ztor)
tibble::tibble(
  指標 = c("平均相関 (r)", "95% CI lower", "95% CI upper",
           "95% PI lower", "95% PI upper"),
  値 = c(pred_cor$pred, pred_cor$ci.lb, pred_cor$ci.ub,
         pred_cor$pi.lb, pred_cor$pi.ub)
) |>
  gt() |>
  tab_header(title = "相関メタ分析: r スケールでの推定値") |>
  fmt_number(columns = 値, decimals = 3)
```

**解釈**: 平均相関、95% 信頼区間、95% 予測区間を *r* スケールで報告する。予測区間は「今後の同様の研究で得られる相関がどの範囲に収まるか」を示す。

#### フォレストプロット

```{r}
#| label: cor-forest
#| fig-width: 10
#| fig-height: 8
forest(m_cor, atransf = tanh, xlab = "Correlation (r)",
       header = "Study / r [95% CI]", refline = 0,
       order = "obs")
```

**図の読み方**: `atransf = tanh` により、軸ラベルが *r* スケールで表示される。分析自体は Fisher *z* スケールで行われているため、統計的妥当性は損なわれない。

#### サブグループ分析（動機づけの種類別）

```{r}
#| label: cor-subgroup
m_cor_mod <- rma(yi, vi, mods = ~ factor(mot_type), data = dat_cor, method = "REML")
rma_to_gt(m_cor_mod, title = "サブグループ分析: 動機づけの種類別")
```

```{r}
#| label: cor-subgroup-means
m_cor_mod0 <- rma(yi, vi, mods = ~ 0 + factor(mot_type), data = dat_cor, method = "REML")
# r スケールに変換
tibble::tibble(
  `動機づけの種類` = c("Instrumental", "Integrative"),
  `平均 r` = tanh(coef(m_cor_mod0)),
  `95% CI lower` = tanh(m_cor_mod0$ci.lb),
  `95% CI upper` = tanh(m_cor_mod0$ci.ub)
) |>
  gt() |>
  tab_header(title = "動機づけ種類別の平均相関（r スケール）") |>
  fmt_number(columns = c(`平均 r`, `95% CI lower`, `95% CI upper`), decimals = 3)
```

::: {.callout-note}
#### Recipe 21 との接続
1 つの研究から複数の相関（例: 動機づけ × 語彙テスト、動機づけ × ライティングテスト）が抽出される場合は、本 Recipe の `rma()` ではなく、Recipe 21 の `rma.mv()` による多層モデルを用いて非独立性に対処する必要がある。
:::


## PART II: ネットワーク・メタ分析（NMA）

本章の後半では、3 つ以上の介入を同時に比較する**ネットワーク・メタ分析（Network Meta-Analysis; NMA）**を扱う。ペアワイズ・メタ分析が「A vs. B」の 2 群比較を統合するのに対し、NMA は「A vs. B vs. C vs. D」を同時に比較し、直接比較が存在しない介入間の効果も間接推定できる点に特徴がある。


### NMA の基本的な考え方

#### NMA が必要となる状況

たとえば、英語教育研究において以下のような比較研究が存在する場合を考える。

- Study 1: 統制 vs. グロス指導
- Study 2: グロス指導 vs. ライティング指導
- Study 3: 統制 vs. 間隔反復学習 (SRS)
- Study 4: ライティング指導 vs. SRS

ペアワイズ・メタ分析では各比較を個別に統合するにとどまるが、NMA ではこれらのネットワーク全体を統合し、直接比較が存在しない組み合わせ（例：統制 vs. ライティング指導）の効果も**間接推定**することが可能となる。加えて、すべての介入のランキングも得られる。

#### NMA の 2 つの前提

NMA の妥当性は、以下の 2 つの前提に依存する。

1. **整合性（Consistency）**: ある比較について、直接推定と間接推定が統計的に一致すること。たとえば「A vs. B」の直接比較による推定値と、「A vs. C」＋「C vs. B」から間接的に導出された推定値が大きく矛盾しないことを意味する
2. **推移性（Transitivity）**: 異なる比較を行った研究群の参加者特性や研究条件が十分に類似していること。たとえば、グロス vs. 統制の研究が初級学習者のみ、ライティング vs. 統制の研究が上級学習者のみを対象としている場合、推移性の前提が成立しない恐れがある


### Recipe 13: NMA のデータ準備

以下のコードは NMA 分析用のサンプルデータを生成する。実際の分析ではこの部分を自身のデータに置き換える。シミュレーションコードの詳細を理解する必要はない。

```{r}
#| label: nma-setup
#| message: false
if (!requireNamespace("netmeta", quietly = TRUE)) {
  install.packages("netmeta", repos = "https://cloud.r-project.org")
}
library(netmeta)
set.seed(2024)
```

4 つの教授法を比較する 16 件の研究をシミュレーションにより生成する。各研究は 2〜3 群を比較するデザイン（一部が多群試験）である。

```{r}
#| label: nma-simulate
treatments <- c("CTRL", "GLOSS", "WRIT", "SRS")
true_delta <- c(CTRL = 0.00, GLOSS = 0.25, WRIT = 0.45, SRS = 0.60)

make_study <- function(id, arms = sample(2:3, 1)) {
  tr <- sample(treatments, arms, replace = FALSE)
  n  <- sample(30:80, arms, replace = TRUE)
  sd <- runif(arms, 8, 14)
  base <- rnorm(1, 70, 6)
  mu <- sapply(tr, function(t) base + true_delta[t] * mean(sd))
  data.frame(
    study = sprintf("NMA_%02d", id),
    treat = tr, n = n,
    mean = mu + rnorm(arms, 0, 1.0), sd = sd
  )
}

studies <- do.call(rbind, lapply(1:16, make_study))
studies$arm <- ave(studies$treat, studies$study, FUN = seq_along)

wide <- studies |>
  pivot_wider(names_from = arm, values_from = c(treat, n, mean, sd),
              names_sep = "") |>
  arrange(study)
```

::: {.callout-warning}
#### NMA データ整形上の注意

- **多群試験（3 群以上）は 1 行にまとめる**: 各群を別行に分割すると研究内の相関が無視され、誤った推定結果が生じる
- **介入名の表記統一**: 「CTRL」「Control」「統制群」が混在すると、それぞれ異なる介入として処理されてしまう
- **連結ネットワークの確認**: すべての介入が直接的または間接的に比較可能であること（ネットワークが連結していること）が必要条件である
:::

#### ネットワーク連結性のプリフライトチェック

NMA を適合する前に、ネットワークが連結しているかを `netconnection()` で確認する[@balduzzi2023]。非連結ネットワークでは `netmeta()` がエラーを返すため、このチェックは必須である。

```{r}
#| label: nma-connectivity
# wide 形式を pairwise（ペアごとの比較）形式に変換
pw <- pairwise(treat = list(treat1, treat2, treat3),
               mean  = list(mean1,  mean2,  mean3),
               sd    = list(sd1,    sd2,    sd3),
               n     = list(n1,     n2,     n3),
               data = wide, studlab = study, sm = "SMD")

# ネットワーク連結性チェック
nc <- netconnection(treat1, treat2, studlab, data = pw)

# netconnection() の返り値フィールド名はバージョンにより異なるため安全に取得
n_trts    <- nc$n.trts %||% nc$k %||% length(unique(c(pw$treat1, pw$treat2)))
n_subnets <- nc$n.subnets %||% nc$n.components %||% NA
tibble::tibble(
  指標 = c("介入の数", "連結成分の数", "連結か？"),
  結果 = c(as.character(n_trts), as.character(n_subnets),
           ifelse(isTRUE(n_subnets == 1), "はい（1つの連結ネットワーク）",
                  paste0("いいえ（", n_subnets, "個の部分ネットワーク）")))
) |>
  gt() |>
  tab_header(title = "ネットワーク連結性チェック")
```

**読み方**: 連結成分の数が 1 であれば、すべての介入が直接的または間接的に比較可能であり、NMA を実行できる。2 以上の場合は部分ネットワークに分割して分析するか、孤立した介入を除外する必要がある。

::: {.callout-tip}
#### ネットワーク幾何の評価

連結性だけでなく、ネットワークの形状（geometry）も推定精度に影響する:

| 形状 | 特徴 | 推定への影響 |
|:--|:--|:--|
| Star-shaped（星形） | ほぼ全研究が「統制群 vs. 介入 X」 | 介入間の間接比較が統制群を経由し、精度低下 |
| Well-connected | 介入同士の直接比較が多い | 直接エビデンスが豊富で推定が安定 |
| Sparse（疎） | 一部の比較のみ研究が存在 | 特定の比較で CI が極端に広い |

英語教育研究では star-shaped になりやすい（統制群 vs. 各指導法の構造が多い）。Recipe 15 の `netgraph()` でネットワーク構造を可視化すると、これらの形状を直感的に確認できる。
:::


### Recipe 14: NMA の適合と結果の解釈

Recipe 13 で作成したペアワイズ比較データ `pw` をそのまま使用して NMA を適合する。

```{r}
#| label: nma-fit
# Recipe 13 で作成した pw を使用
net <- netmeta(TE, seTE, treat1, treat2, studlab, data = pw,
               sm = "SMD", method.tau = "REML",
               reference.group = "CTRL", random = TRUE)

# NMA結果をgt表で表示（vs. CTRL）
trts <- net$trts[net$trts != net$reference.group]
tibble::tibble(
  Treatment = trts,
  SMD       = net$TE.random[net$reference.group, trts],
  CI_lower  = net$lower.random[net$reference.group, trts],
  CI_upper  = net$upper.random[net$reference.group, trts]
) |>
  gt() |>
  tab_header(title = "NMA: ランダム効果推定値（vs. CTRL）") |>
  fmt_number(columns = c(SMD, CI_lower, CI_upper), decimals = 3)
```


### Recipe 15: NMA の結果の可視化

#### ネットワークグラフ

```{r}
#| label: nma-network
netgraph(net, number.of.studies = TRUE, points = TRUE,
         plastic = FALSE, thickness = "se.random", cex.points = 1.1,
         col.points = "#2c3e50", col = "#2c3e50",
         cex = 1.0, multiarm = TRUE)
```

**図の読み方**: ノード（点）が介入、エッジ（線）が直接比較を表す。線の太さは精度（比較数・サンプルサイズ）を反映し、太い線ほどエビデンスが豊富であることを示す。

#### フォレストプロット（vs. CTRL）

```{r}
#| label: nma-forest
forest(net, ref = "CTRL", xlab = "SMD vs CTRL")
```

**図の読み方**: 統制群（CTRL）を基準とした各介入の相対的効果を示す。0 より右に位置する介入は CTRL に対して有利であることを意味する。

#### リーグテーブル（全組み合わせの相対効果）

```{r}
#| label: nma-league
L <- netleague(net, digits = 2, bracket = "(", separator = " to ")
L
```

```{r}
#| label: nma-league-gt
if (requireNamespace("gt", quietly = TRUE)) {
  Lm <- as.data.frame(L$random)
  Lm <- cbind(comp = rownames(Lm), Lm)
  gt(Lm) |>
    tab_header(title = "League table (SMD): row vs column") |>
    tab_options(table.font.size = px(14))
}
```

**読み方**: 行の介入 vs. 列の介入の相対効果を表す。正の値は行の介入が列の介入よりも効果が大きいことを意味する。

#### P-score ランキング

```{r}
#| label: nma-ranking
# small.values: "good" = 値が小さいほど望ましい（例: エラー数）
#               "bad"  = 値が小さいほど悪い（例: テストスコア）
# SMD が大きいほど介入効果が高い場合は "bad" が適切だが、
# netmeta のデフォルトに従いここでは "good" を使用
rk <- netrank(net, small.values = "good")
rk
```

```{r}
#| label: nma-rank-plot
#| fig-width: 8
#| fig-height: 5
psvec <- if (!is.null(rk$Pscore.random)) rk$Pscore.random else rk$Pscore
df_rank <- data.frame(treatment = names(psvec), Pscore = as.numeric(psvec))

ggplot(df_rank, aes(x = reorder(treatment, Pscore), y = Pscore)) +
  geom_col(fill = "#2c3e50") +
  coord_flip() +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +
  labs(title = "Treatment ranking (P-score)", x = "Treatment", y = "P-score") +
  theme_bw(base_size = 12, base_family = "HiraginoSans-W3") + theme(legend.position = "bottom")
```

**読み方**: P-score は 0〜1（0%〜100%）の指標であり、値が大きいほど有利な介入であることを示す。ただし、P-score の差が小さい場合は実質的な差が存在しない可能性もあるため、効果量と信頼区間を併せて解釈する必要がある。

#### NMA要約テーブル

```{r}
#| label: nma-summary-gt
if (requireNamespace("gt", quietly = TRUE)) {
  TE_mat <- net$TE.random
  ref <- "CTRL"
  tr_all <- rownames(TE_mat)
  keep <- setdiff(tr_all, ref)
  effects <- data.frame(
    treatment = keep,
    TE = as.numeric(TE_mat[keep, ref]),
    stringsAsFactors = FALSE
  )
  if (!is.null(rk$Pscore.random)) {
    ps <- data.frame(treatment = names(rk$Pscore.random),
                     Pscore = as.numeric(rk$Pscore.random))
  } else if (!is.null(rk$Pscore)) {
    ps <- data.frame(treatment = names(rk$Pscore),
                     Pscore = as.numeric(rk$Pscore))
  }
  effects <- merge(effects, ps, by = "treatment", all.x = TRUE)
  effects <- effects[order(-effects$Pscore), ]
  gt(effects) |>
    tab_header(title = "NMA summary: SMD vs CTRL and P-score") |>
    fmt_number(columns = c(TE, Pscore), decimals = 3) |>
    tab_options(table.font.size = px(14))
}
```

#### Rankogram と SUCRA――ランキングの不確実性を可視化する

P-score は各介入が「最良」である確率を要約した点推定値である。しかし、ランキングには不確実性が伴う。**rankogram** は各介入が各順位に位置する確率分布を可視化し、ランキングの安定性を評価するツールである[@rucker2015]。一方、**SUCRA（Surface Under the Cumulative Ranking curve）**は、累積ランキング曲線の下の面積に基づく指標であり、ベイズ NMA で広く用いられてきた。頻度論的 NMA では P-score が SUCRA の解析的近似として機能する。

```{r}
#| label: nma-rankogram
#| fig-width: 10
#| fig-height: 6
# rankogram の算出（リサンプリングに基づく）
rg <- rankogram(net, nsim = 1000)

# ランキング確率のプロット
plot(rg)
```

**読み方**: 各パネルが 1 つの介入に対応し、棒グラフは各順位（1 位、2 位、…）に位置する確率を表す。確率が特定の順位に集中している介入はランキングが安定しており、複数の順位に分散している介入はランキングが不安定（不確実性が高い）である。

```{r}
#| label: nma-sucra
# SUCRA 値の抽出と比較
sucra_vals <- rg$ranking.random
if (is.null(sucra_vals)) sucra_vals <- rg$ranking.common

tibble::tibble(
  Treatment = names(sucra_vals),
  SUCRA = as.numeric(sucra_vals),
  `P-score` = as.numeric(
    if (!is.null(rk$Pscore.random)) rk$Pscore.random[names(sucra_vals)]
    else rk$Pscore[names(sucra_vals)]
  )
) |>
  arrange(desc(SUCRA)) |>
  gt() |>
  tab_header(title = "SUCRA と P-score の比較") |>
  fmt_number(columns = c(SUCRA, `P-score`), decimals = 3)
```

::: {.callout-note}
#### P-score vs. SUCRA

- **P-score**: ネットワーク推定値の点推定から解析的に算出。リサンプリング不要で高速[@rucker2015]
- **SUCRA**: ベイズ NMA の MCMC サンプルまたは頻度論のリサンプリングから算出。ランキングの不確実性を直接反映

実際には両者はほぼ同じ値をとることが多い。頻度論的 NMA（`netmeta`）を使用する場合、**報告には P-score を用い、ランキングの不確実性の検討には rankogram を補足的に使用する**ことを推奨する。
:::

#### NMA 可視化手法の選択ガイド

```{r}
#| label: nma-viz-guide
#| echo: false
tibble::tribble(
  ~目的, ~推奨する可視化, ~関数,
  "ネットワーク構造の概観（研究数・比較構造）", "ネットワークグラフ", "`netgraph()`",
  "各介入の効果量と信頼区間", "フォレストプロット", "`forest()`",
  "全介入ペアの相対効果（論文掲載用）", "リーグテーブル", "`netleague()` + `gt()`",
  "介入の相対的順位", "P-score 棒グラフ", "`netrank()` + `ggplot()`",
  "ランキングの不確実性", "Rankogram", "`rankogram()` + `plot()`",
  "小規模研究バイアス", "ファンネルプロット", "`funnel()`",
  "不整合の検出", "ヒートマップ / ノード分割", "`netheat()` / `netsplit()`"
) |>
  gt() |>
  tab_header(title = "NMA 可視化手法の選択ガイド") |>
  tab_options(table.font.size = px(13)) |>
  tab_style(
    style = cell_text(weight = "bold"),
    locations = cells_column_labels()
  )
```

::: {.callout-tip}
#### 論文に掲載すべき最低限の図表

NMA の結果を報告する際、以下の 4 点は必須とされる[@salanti2012]:

1. **ネットワークグラフ**: ネットワークの構造と各比較の研究数を示す
2. **フォレストプロット**または**リーグテーブル**: 効果量の推定値と信頼区間
3. **ランキング**（P-score / SUCRA）: 介入の相対的順位
4. **整合性検定の結果**: グローバル検定 + ノード分割法の結果
:::


### Recipe 16: NMA の整合性検証

NMA の結果の妥当性を担保するためには、直接推定と間接推定が矛盾しないこと（整合性）を検証する必要がある。

#### グローバル整合性検定

ネットワーク全体で直接推定と間接推定が矛盾していないかを検証する。`decomp.design()` は Q 統計量を 3 成分に分解する。ここでの **design** とは、各研究が比較した介入の組み合わせ（例: A vs. B、A vs. B vs. C）を指す。同じ design の研究は同じ介入群を比較している。3 成分は: (1) 直接比較内（within-design: 同じ design の研究間の異質性）、(2) 比較間（between-design: 異なる design 間の不一致、すなわち整合性の指標）、(3) 不整合（inconsistency: 残差）である。

```{r}
#| label: nma-consistency
dd <- decomp.design(net)
dd
```

**出力の読み方**:

| 成分 | 意味 | $p$ 値の解釈 |
|:--|:--|:--|
| Q (within-design) | 同じデザイン（例: A vs. B）内の研究間の異質性 | $p$ < .05 → 研究間で効果が異なる |
| Q (between-design) | 異なるデザイン間の不整合 | $p$ < .05 → **直接推定と間接推定が矛盾**（重要） |
| Q (total) | 全体の異質性 | 上 2 つの合計 |

**判断基準**: between-design の $p$ 値が .10 未満であれば、整合性に懸念がある。ただし、検出力が低い（研究数が少ない）場合は不整合が見逃される可能性がある点に留意する。

#### ノード分割法（直接 vs. 間接の比較）

ノード分割法（node-splitting）は、各比較について直接エビデンスのみ / 間接エビデンスのみで推定した値を別々に算出し、両者の差を検定する手法である。以下では `netsplit()` の結果を散布図で可視化し、対角線（直接推定 = 間接推定）からの乖離を確認する。

```{r}
#| label: nma-netsplit
ns <- netsplit(net)
ns
```

```{r}
#| label: nma-netsplit-plot
#| fig-width: 8
#| fig-height: 6
dr <- ns$direct.random
ir <- ns$indirect.random
if (!is.null(dr) && !is.null(ir)) {
  d1 <- dr[, c("comparison", "TE")]
  names(d1)[2] <- "TE.direct"
  d2 <- ir[, c("comparison", "TE")]
  names(d2)[2] <- "TE.indirect"
  nsdf <- merge(d1, d2, by = "comparison", all = FALSE)
  ggplot(nsdf, aes(x = TE.direct, y = TE.indirect, label = comparison)) +
    geom_abline(slope = 1, intercept = 0, linetype = "dashed", color = "grey60") +
    geom_point(color = "#2c3e50", size = 3, alpha = 0.7) +
    labs(title = "Node-splitting: Direct vs Indirect estimates",
         x = "Direct estimate", y = "Indirect estimate") +
    theme_bw(base_size = 12, base_family = "HiraginoSans-W3") + theme(legend.position = "bottom")
}
```

**図の読み方**: 各点が対角線（直接推定 = 間接推定）の近傍に位置していれば整合的であり、大きく逸脱する場合は不整合の疑いがある。不整合が認められる比較については、研究間の参加者特性や方法論上の差異を精査する必要がある。

#### ファンネルプロットとヒートマップ

```{r}
#| label: nma-funnel-heat
#| fig-width: 10
#| fig-height: 5
ord <- {
  if (!is.null(rk$Pscore.random)) {
    names(sort(rk$Pscore.random, decreasing = TRUE))
  } else if (!is.null(rk$Pscore)) {
    names(sort(rk$Pscore, decreasing = TRUE))
  } else {
    net$trts
  }
}
par(mfrow = c(1, 2))
funnel(net, order = ord)
netheat(net)
par(mfrow = c(1, 1))
```

**ファンネルプロット**（左）: NMA 全体における小規模研究バイアスを検討する。
**ヒートマップ**（右）: 整合性が低い箇所が赤色で表示される。

#### 直接エビデンスの割合の可視化（dmetar）

`dmetar::direct.evidence.plot()` は、各比較におけるネットワーク推定値のうち直接エビデンスが占める割合、最小並列性（parallelism）、平均パス長を同時に可視化する。これにより、推定値の信頼性を比較ごとに評価できる[@harrer2021]。

```{r}
#| label: nma-direct-evidence
#| fig-width: 10
#| fig-height: 8
if (requireNamespace("dmetar", quietly = TRUE)) {
  library(dmetar)
  dep <- direct.evidence.plot(net)
  plot(dep)
}
```

**3 つのパネルの読み方**:

- **Direct Evidence Proportion（直接エビデンス割合）**: オレンジ色のバーが大きいほど、その比較の推定値が直接比較のデータに基づいている。割合が低い（< 50%）比較は、間接推定に大きく依存しており、推移性の仮定が満たされない場合に結果が不安定になる
- **Minimal Parallelism（最小並列性）**: 推定値に寄与する独立した情報経路の最小数。たとえば A vs. C の間接推定が A→B→C と A→D→C の 2 経路から得られる場合、並列性は 2 である。値が大きいほど推定が頑健。1 の場合、情報が単一の経路に依存しており、その経路上の研究が除外されると推定が大きく変動する可能性がある
- **Mean Path Length（平均パス長）**: 推定値が何段階の間接比較を経由しているかの平均。値が大きいほど間接性が高く、推移性の仮定への依存度が増す

::: {.callout-important}
#### 整合性に問題がある場合の対処フロー

1. **`decomp.design()` の between-design で $p$ < .10** → 整合性に懸念あり
2. **`netsplit()` で不整合の比較を特定** → 直接推定 vs. 間接推定が大きく乖離する比較を確認
3. **`direct.evidence.plot()` で直接エビデンス割合を確認** → 不整合の比較は直接エビデンスが少ないか？
4. **原因を精査**:
   - 当該比較に含まれる研究の参加者特性（年齢、習熟度、L1）に偏りがないか？
   - 介入の定義に不統一がないか（同名の介入で実質的に異なる指導法を含んでいないか）？
5. **報告**: 不整合が認められた比較とその考えられる原因を明記し、**その比較の推定値は慎重に解釈すべきである旨を limitations に記述する**
:::


### Recipe 17: 二値アウトカムの NMA

二値アウトカム（合格/不合格など）の場合は、オッズ比（OR）や相対リスク（RR）を効果量指標として用いる。

```{r}
#| label: nma-binary
set.seed(2024)
treatments_b <- c("CTRL", "GLOSS", "WRIT", "SRS")
logOR_true <- c(CTRL = 0.00, GLOSS = 0.40, WRIT = 0.70, SRS = 0.90)

make_study_bin <- function(id, arms = sample(2:3, 1)) {
  tr <- sample(treatments_b, arms, replace = FALSE)
  n  <- sample(60:140, arms, replace = TRUE)
  p0 <- plogis(rnorm(1, qlogis(0.45), 0.4))
  p  <- sapply(tr, function(t) plogis(qlogis(p0) + logOR_true[t]))
  ev <- rbinom(arms, size = n, prob = p)
  data.frame(study = sprintf("BIN_%02d", id), treat = tr, n = n, event = ev)
}

studies_b <- do.call(rbind, lapply(1:14, make_study_bin))
studies_b$arm <- ave(studies_b$treat, studies_b$study, FUN = seq_along)

wide_b <- studies_b |>
  pivot_wider(names_from = arm, values_from = c(treat, n, event),
              names_sep = "") |>
  arrange(study)

pw_b <- pairwise(treat = list(treat1, treat2, treat3),
                 event = list(event1, event2, event3),
                 n     = list(n1,     n2,     n3),
                 data = wide_b,
                 studlab = study,
                 sm = "OR")

net_b <- netmeta(TE, seTE, treat1, treat2, studlab, data = pw_b,
                 sm = "OR", method.tau = "REML",
                 reference.group = "CTRL", random = TRUE)

# NMA結果をgt表で表示（vs. CTRL, log OR）
trts_b <- net_b$trts[net_b$trts != net_b$reference.group]
tibble::tibble(
  Treatment = trts_b,
  log_OR    = net_b$TE.random[net_b$reference.group, trts_b],
  CI_lower  = net_b$lower.random[net_b$reference.group, trts_b],
  CI_upper  = net_b$upper.random[net_b$reference.group, trts_b]
) |>
  gt() |>
  tab_header(title = "NMA（二値アウトカム）: ランダム効果推定値（vs. CTRL, log OR）") |>
  fmt_number(columns = c(log_OR, CI_lower, CI_upper), decimals = 3)
```

```{r}
#| label: nma-binary-forest
forest(net_b, ref = "CTRL", xlab = "log(OR) vs CTRL")
```

**読み方**: log(OR) > 0 は当該介入が CTRL に対して有利であることを意味する。報告時には `exp()` により OR に変換すると解釈が容易になる。たとえば log(OR) = 0.70 は OR = `r round(exp(0.70), 2)` に対応し、「介入群の成功オッズは統制群の `r round(exp(0.70), 2)` 倍である」と解釈される。

```{r}
#| label: nma-export
dir.create("data/meta", showWarnings = FALSE, recursive = TRUE)
try(utils::write.csv(L$random, "data/meta/nma_league.csv", row.names = TRUE))
if (!is.null(rk$Pscore.random)) {
  rkdf <- data.frame(treatment = names(rk$Pscore.random),
                     Pscore = as.numeric(rk$Pscore.random))
} else if (!is.null(rk$Pscore)) {
  rkdf <- data.frame(treatment = names(rk$Pscore),
                     Pscore = as.numeric(rk$Pscore))
} else {
  rkdf <- NULL
}
if (!is.null(rkdf)) {
  try(utils::write.csv(rkdf, "data/meta/nma_ranking.csv", row.names = FALSE))
}
```


### Recipe 18: NMA の感度分析と実務ガイド

#### Comparison-adjusted ファンネルプロット

NMA における出版バイアスの検討には、通常のファンネルプロットではなく comparison-adjusted ファンネルプロットが推奨される。各比較ごとの残差を標準誤差に対してプロットすることで、特定の比較に偏りが集中していないかを確認できる。

```{r}
#| label: nma-adj-funnel
#| fig-width: 8
#| fig-height: 6
ord_nma <- {
  if (!is.null(rk$Pscore.random)) {
    names(sort(rk$Pscore.random, decreasing = TRUE))
  } else if (!is.null(rk$Pscore)) {
    names(sort(rk$Pscore, decreasing = TRUE))
  } else {
    net$trts
  }
}
funnel(net, order = ord_nma,
       xlab = "Centered SMD", ylab = "Standard Error",
       pch = 16, col = "#2c3e50")
```

**読み方**: 点が漏斗の中心軸の両側にほぼ対称に分布していれば、小規模研究バイアスの懸念は小さい。特定の比較に偏りがある場合、その介入の周辺に非対称なパターンが出現する。

#### NMA の感度分析チェックリスト

::: {.panel-tabset}

##### 整合性

整合性（consistency）は NMA の妥当性の前提条件である。Recipe 16 で紹介したグローバル検定とノード分割法に加え、以下の点を確認する。

- **グローバル整合性検定**: `decomp.design()` の between-design の *p* 値が .10 以上であれば、全体的に整合的と判断できる
- **ノード分割法**: 各比較について直接推定と間接推定が大きく乖離していないかを確認。$p$ < .05 の比較は不整合の疑いがある
- **直接エビデンス割合**: `direct.evidence.plot()` で直接エビデンスが 50% 未満の比較を特定する
- **不整合が認められた場合**: 当該比較に含まれる研究の参加者特性・方法論を精査し、不整合の原因を議論する（Recipe 16 の対処フローを参照）

##### 影響研究の除外

特定の研究が NMA の結果に強い影響を及ぼしていないかを確認するため、影響力の大きい研究を 1 件ずつ除外してランキングの変動を観察する。

```{r}
#| label: nma-sensitivity-loo
#| fig-width: 8
#| fig-height: 5
# 各研究を除外した場合の P-score の変動を確認
studies_nma <- unique(pw$studlab)
if (length(studies_nma) >= 5) {
  loo_ranks <- lapply(studies_nma[1:min(5, length(studies_nma))], function(s) {
    pw_sub <- pw[pw$studlab != s, ]
    tryCatch({
      net_sub <- netmeta(TE, seTE, treat1, treat2, studlab, data = pw_sub,
                         sm = "SMD", method.tau = "REML",
                         reference.group = "CTRL", random = TRUE)
      rk_sub <- netrank(net_sub, small.values = "good")
      ps <- if (!is.null(rk_sub$Pscore.random)) rk_sub$Pscore.random else rk_sub$Pscore
      data.frame(excluded = s, treatment = names(ps), Pscore = as.numeric(ps))
    }, error = function(e) NULL)
  })
  loo_df <- do.call(rbind, Filter(Negate(is.null), loo_ranks))
  if (nrow(loo_df) > 0) {
    ggplot(loo_df, aes(x = treatment, y = Pscore, color = excluded)) +
      geom_point(size = 3, alpha = 0.7, position = position_dodge(0.3)) +
      labs(title = "NMA sensitivity: P-score change by study exclusion",
           x = "Treatment", y = "P-score", color = "Excluded study") +
      theme_bw(base_size = 12, base_family = "HiraginoSans-W3") + theme(legend.position = "bottom")
  }
}
```

**ランキング安定性の評価基準**:

- P-score のシフト幅が **0.10 以上**の介入は、その研究の除外によりランキングが実質的に変動している
- ランキング順位が入れ替わる場合は、当該研究を特定し、サンプルサイズや方法論的特徴を確認する
- 全研究を除外してもランキングが安定している場合は、「ランキングは個別の研究に依存しない頑健な結果である」と報告できる

##### 推移性の検討

推移性（transitivity）の前提を検討するために、各比較に含まれる研究の参加者特性（例: 習熟度、L1）がネットワーク全体で均質であるかを確認する。推移性が満たされない場合、間接比較の妥当性が損なわれる[@salanti2012]。

```{r}
#| label: nma-transitivity
#| echo: false
tibble::tribble(
  ~検討項目, ~方法, ~懸念の兆候,
  "参加者の習熟度", "各比較の研究における習熟度レベルの分布を比較", "特定の比較に初級者のみ、別の比較に上級者のみが偏る",
  "研究の出版年", "各比較の研究における年代分布を確認", "古い研究と新しい研究が異なる比較に集中する",
  "測定指標", "各比較のアウトカム指標（テスト種類）を確認", "比較によって使用されるテストの種類が大きく異なる",
  "介入の実施期間", "各比較の研究における介入期間の分布を確認", "短期介入と長期介入が異なる比較に集中する",
  "教育段階", "各比較の研究における教育段階（中学/高校/大学）の分布", "特定の比較が特定の教育段階に偏る"
) |>
  gt() |>
  tab_header(title = "推移性の検討チェックリスト（英語教育研究向け）") |>
  tab_options(table.font.size = px(13))
```

::: {.callout-warning}
#### 英語教育研究における推移性の脅威

英語教育の NMA では、以下のシナリオで推移性が脅かされやすい:

- **「文法指導 vs. 統制群」の研究が大学生のみ、「文法指導 vs. 語彙指導」の研究が高校生のみ**で構成される場合、「語彙指導 vs. 統制群」の間接推定は年齢効果と混交する
- **即時テストのみ報告する研究と遅延テストのみ報告する研究が異なる比較に偏る**場合、推定値にテストのタイミング効果が混入する

対策として、コーディングシートにこれらのモデレーター変数を含め、比較ごとの分布を表形式で示すことが推奨される。
:::

##### ランキング不確実性と固定効果モデル（common effect model）との比較

ランキングの頑健性を評価するため、ランダム効果モデルと固定効果モデル（`netmeta` では common effect model と呼ばれる）でランキングがどう変わるかを比較する。

```{r}
#| label: nma-ranking-sensitivity
#| fig-width: 8
#| fig-height: 5
# 固定効果モデルで再適合
net_fixed <- netmeta(TE, seTE, treat1, treat2, studlab, data = pw,
                     sm = "SMD", method.tau = "REML",
                     reference.group = "CTRL",
                     random = TRUE, common = TRUE)

# ランダム効果 vs. 固定効果の P-score 比較
rk_random <- netrank(net_fixed, small.values = "good")
ps_random <- if (!is.null(rk_random$Pscore.random)) rk_random$Pscore.random else rk_random$Pscore
ps_common <- if (!is.null(rk_random$Pscore.common)) rk_random$Pscore.common else rk_random$Pscore

rank_comp <- data.frame(
  treatment = names(ps_random),
  Random = as.numeric(ps_random),
  Common = as.numeric(ps_common)
) |>
  tidyr::pivot_longer(cols = c(Random, Common), names_to = "Model", values_to = "Pscore")

ggplot(rank_comp, aes(x = treatment, y = Pscore, fill = Model)) +
  geom_col(position = "dodge") +
  labs(title = "P-score sensitivity: Random vs Common effects model",
       x = "Treatment", y = "P-score", fill = "Model") +
  scale_fill_manual(values = c(Random = "#2c3e50", Common = "#95a5a6")) +
  theme_bw(base_size = 12, base_family = "HiraginoSans-W3") + theme(legend.position = "bottom")
```

**読み方**: ランダム効果モデルと固定効果モデルで P-score やランキング順位が大きく異なる場合、研究間の異質性がランキングに強く影響していることを示す。両モデルで一貫したランキングが得られれば、結果の頑健性が支持される。

:::

#### NMA 実施時の実務的注意点（英語教育研究向け）

::: {.callout-tip}
#### 介入の定義粒度
「明示的文法指導」を一括りにするか、「演繹的指導」「帰納的指導」「意識高揚タスク」に細分化するかは、利用可能な研究数と研究の問いに依存する。細分化するほどネットワークは希薄になり推定の不確実性が増す。目安として、各ノード（介入）に直接比較が少なくとも 2 件存在することが望ましい。
:::

::: {.callout-warning}
#### ネットワーク幾何の偏り
英語教育研究の NMA では、ほとんどの研究が「統制群 vs. 何らかの介入」という比較構造をとるため、star-shaped（星形）のネットワークになりやすい。このようなネットワークでは、介入間の間接比較が統制群を経由することになり、推定の精度が低下する。ネットワークに直接比較のエッジが多いほど（well-connected）、推定はより信頼できる。
:::


## PART III: 公開データによる実践――SLA 語彙学習メタ分析の再分析

PART I・II ではシミュレーションデータを用いて分析手順を解説してきたが、実際の公開データに基づく再分析を行うことで、より実践的な理解が得られる。本セクションでは、OSF（Open Science Framework）で公開されている 2 つの SLA 語彙学習メタ分析のデータセットを用い、再分析と高度な可視化手法を紹介する。

::: {.callout-note}
### データの出典

1. **偶発的語彙学習のメタ分析**（*Language Teaching*, 2022）: OSF [osf.io/92r3t](https://osf.io/92r3t/)
2. **@uchihara2019** "The Effects of Repetition on Incidental Vocabulary Learning": OSF [osf.io/rmnk2](https://osf.io/rmnk2)

いずれも Creative Commons ライセンスのもとで公開されており、教育・研究目的での再利用が認められている。
:::


### 使用パッケージ一覧

本セクションでは、PART I・II で使用した `metafor`, `ggplot2` に加え、以下のパッケージを活用する（すべてセットアップにおいて一括インストール済みである）。

| パッケージ | 用途 | 入手元 |
|:--|:--|:--|
| `orchaRd` | Orchard plot（効果量の分布を直感的に可視化） | GitHub |
| `metaviz` | Rain forest plot, sunset funnel plot | CRAN |
| `dmetar` | 外れ値検出、検出力分析、各種ユーティリティ | GitHub |
| `forestploter` | 出版品質のカスタマイズ可能なフォレストプロット | CRAN |
| `gt` | 出版品質の表 | CRAN |


### Recipe 19: 偶発的語彙学習データの読み込みと前処理

#### データの取得方法

```{r}
#| label: load-vocab-data
# ローカルファイルから読み込み（推奨：再現性が高い）
vocab <- read.csv("data/meta/incidental_vocab_mode_of_input_1st_posttest.csv",
                  stringsAsFactors = FALSE)

# もしローカルにない場合は OSF から直接取得可能
# （元データは Excel 形式: https://osf.io/download/gz9bj/ ）
# CMA 出力の CSV は ZIP に含まれています: https://osf.io/download/3tscg/

# 列名を扱いやすく変更
names(vocab) <- c("study", "mdiff", "sd_common", "n_treat", "n_ctrl",
                   "d", "se_d", "g", "se_g", "mode")

# Hedges g の分散を計算（SE^2）
vocab$vi <- vocab$se_g^2

# データの概要
DT::datatable(vocab, caption = "偶発的語彙学習データ")
```

```{r}
#| label: vocab-summary
vocab |>
  group_by(mode) |>
  summarise(
    k = n(),
    g_mean = round(mean(g), 3),
    g_median = round(median(g), 3),
    n_total = sum(n_treat + n_ctrl)
  ) |>
  gt() |>
  tab_header(title = "偶発的語彙学習: インプットモード別の概要") |>
  tab_options(table.font.size = px(14))
```

::: {.callout-tip}
#### OSF からのデータ取得手順
1. [osf.io](https://osf.io/) にて論文タイトルまたは DOI で検索する
2. プロジェクトの Files タブからデータファイル（CSV, Excel）をダウンロードする
3. Excel 形式の場合は `readxl::read_excel()` で読み込む
4. 列名の整理と欠損値の処理を行い、`yi`（効果量）と `vi`（分散）の列を準備する
:::


### Recipe 20: 公開データに対するランダム効果モデルの適合

```{r}
#| label: vocab-rma
m_vocab <- rma(yi = g, vi = vi, data = vocab, method = "REML",
               slab = study)
rma_to_gt(m_vocab, title = "偶発的語彙学習: ランダム効果モデル（REML）")
```

```{r}
#| label: vocab-predict
# 予測区間を含む推定
predict(m_vocab)
```

#### フォレストプロットとファンネルプロット

```{r}
#| label: vocab-forest
#| fig-width: 12
#| fig-height: 10
forest(m_vocab, xlab = "Hedges g", refline = 0,
       header = "Study / Hedges g [95% CI]",
       order = "obs")
```

```{r}
#| label: vocab-funnel
#| fig-width: 8
#| fig-height: 6
funnel(m_vocab, xlab = "Hedges g")
regtest(m_vocab, model = "rma")
```

#### サブグループ分析（インプットモード別）

```{r}
#| label: vocab-subgroup
# インプットモード（Reading, Listening, Viewing, RWL）によるサブグループ
m_vocab_mode <- rma(yi = g, vi = vi, mods = ~ 0 + factor(mode),
                    data = vocab, method = "REML")
rma_to_gt(m_vocab_mode, title = "サブグループ分析（インプットモード別）")
```

**読み方**: 各モード（Reading, Listening, Viewing, RWL）の平均効果量が直接推定される。係数が大きいモードほど、当該インプットモードによる語彙学習が効果的であることを示唆する。


### Recipe 21: 多層モデル（`rma.mv()`）――非独立な効果量への構造的対処

@uchihara2019 のデータでは、同一の研究から複数の効果量が抽出されている（異なるテスト形式、異なるエンカウンター回数など）。このような場合、通常の `rma()` では効果量間の非独立性を適切に処理できない。

多層（マルチレベル）モデル `rma.mv()` を用いることで、**研究内の相関構造**と**研究間のばらつき**を同時にモデル化することが可能となる。

```{r}
#| label: load-uchihara
uchi <- read.csv("data/meta/uchihara2019_clean.csv", stringsAsFactors = FALSE)

# 列名を確認
names(uchi)
```

```{r}
#| label: uchihara-prep
# 効果量（相関 r）を Fisher z に変換
uchi_clean <- uchi |>
  filter(!is.na(ES1..r.) & ES1..r. != "") |>
  mutate(
    r = as.numeric(ES1..r.),
    n = as.numeric(N),
    study_id = Author,
    es_id = row_number()
  ) |>
  filter(!is.na(r) & !is.na(n))

# Fisher z 変換
es_z <- escalc(measure = "ZCOR", ri = uchi_clean$r, ni = uchi_clean$n)
uchi_clean$yi <- as.numeric(es_z$yi)
uchi_clean$vi <- as.numeric(es_z$vi)
```

#### 3水準モデルの適合

```{r}
#| label: multilevel-model
# 3水準モデル: Level 1 = サンプリング誤差, Level 2 = 研究内, Level 3 = 研究間
m_ml <- rma.mv(yi, vi,
               random = ~ 1 | study_id / es_id,
               data = uchi_clean,
               method = "REML")
rma_to_gt(m_ml, title = "3水準多層モデル（Fisher z）")
```

#### 多層モデルの出力の解釈

- **sigma^2.1**（Level 3: 研究間分散）: 異なる研究間での真の効果のばらつき
- **sigma^2.2**（Level 2: 研究内分散）: 同じ研究内の異なる効果量間のばらつき
- **estimate**（全体平均効果）: Fisher z スケール。相関に戻すには `tanh()` を適用

```{r}
#| label: multilevel-interpret
# 全体の平均相関に変換
cat("全体平均 (Fisher z):", round(coef(m_ml), 3), "\n")
cat("全体平均 (r):", round(tanh(coef(m_ml)), 3), "\n")

# 分散成分の割合
total_var <- sum(m_ml$sigma2)
cat("\n分散の内訳:\n")
cat("  研究間:", round(m_ml$sigma2[1] / total_var * 100, 1), "%\n")
cat("  研究内:", round(m_ml$sigma2[2] / total_var * 100, 1), "%\n")
```

#### モデレーター分析（多層モデル）

```{r}
#| label: multilevel-moderator
# テスト形式（Recognition vs. Recall）をモデレーターとして投入
if ("Test.format" %in% names(uchi_clean)) {
  m_ml_mod <- rma.mv(yi, vi,
                     mods = ~ factor(Test.format),
                     random = ~ 1 | study_id / es_id,
                     data = uchi_clean,
                     method = "REML")
  rma_to_gt(m_ml_mod, title = "多層モデル: モデレーター分析（テスト形式）")
}
```

::: {.callout-tip}
#### `rma()` と `rma.mv()` の使い分け

| 状況 | 使うモデル |
|:--|:--|
| 1研究 = 1効果量 | `rma()` で十分 |
| 1研究から複数の効果量 | `rma.mv()` + `random = ~ 1 | study / es_id` |
| 同一研究グループが複数論文 | `rma.mv()` + `random = ~ 1 | lab / study / es_id` |
| 1研究 = 1効果量だが簡易にRVEしたい | `rma()` + `clubSandwich::coef_test()` |

英語教育研究のメタ分析では、1 つの論文から語彙・文法・ライティングなど複数のアウトカムの効果量を抽出することが多いため、**多層モデルの使用が推奨される**。
:::


### Recipe 22: 高度な可視化手法――orchaRd, metaviz, その他

#### Orchard Plot（orchaRd パッケージ）

Orchard plot は、メタ分析の結果を直感的に可視化する比較的新しい手法である[@nakagawa2021]。各研究の効果量（点）、全体の平均効果（太い線）、95% CI（太い帯）、95% 予測区間（細い線）を 1 つの図に統合して表示する。

```{r}
#| label: orchard-plot
#| fig-width: 10
#| fig-height: 6
if (requireNamespace("orchaRd", quietly = TRUE)) {
  library(orchaRd)

  # orchaRd は rma.mv オブジェクトを推奨
  # Recipe 2 で生成したシミュレーションデータ dat を使用
  m_orch <- rma.mv(yi, vi,
                   random = ~ 1 | study_id,
                   data = dat,
                   method = "REML")

  orchard_plot(m_orch, mod = "1", group = "study_id", xlab = "Hedges g",
               trunk.size = 3, branch.size = 1.5,
               twig.size = 0.5, k = TRUE)
} else {
  message("orchaRd パッケージが利用できません。GitHub からインストールしてください:\n",
          "remotes::install_github('daniel1noble/orchaRd')")
}
```

**図の読み方**:

- **太い横線**: 95% 信頼区間
- **細い横線**: 95% 予測区間（研究間のばらつきを包含）
- **各点**: 個々の研究の効果量（点の大きさは精度を反映）
- **中央の丸**: 全体の平均効果量

予測区間が 0 を含む場合、特定の文脈では効果が存在しないか、あるいは負の方向となる可能性があることを示唆する。

#### モデレーター別の Orchard Plot

```{r}
#| label: orchard-moderator
#| fig-width: 10
#| fig-height: 7
if (requireNamespace("orchaRd", quietly = TRUE)) {
  # サブグループ（教育段階）別
  dat$level_f <- factor(dat$level)
  m_orch_mod <- rma.mv(yi, vi,
                       mods = ~ level_f,
                       random = ~ 1 | study_id,
                       data = dat,
                       method = "REML")

  orchard_plot(m_orch_mod, mod = "level_f", group = "study_id",
               xlab = "Hedges g",
               trunk.size = 3, branch.size = 1.5,
               twig.size = 0.5, k = TRUE)
}
```

#### Rain Forest Plot（metaviz パッケージ）

Rain forest plot は、標準的なフォレストプロットの代替として、各研究の尤度分布を「雨粒」の形状で視覚的に表現する手法である。

```{r}
#| label: rainforest-plot
#| fig-width: 10
#| fig-height: 10
if (requireNamespace("metaviz", quietly = TRUE)) {
  library(metaviz)

  # metaviz は yi と sei の2列データフレーム、または rma オブジェクトを受け取る
  viz_rainforest(x = m,
                 xlab = "Hedges g",
                 method = "REML",
                 text_size = 3)
} else {
  message("metaviz パッケージが利用できません。install.packages('metaviz') を実行してください。")
}
```

**図の読み方**: 各「雨粒」の幅は信頼区間に、高さは尤度に対応する。幅が狭い雨粒ほど精度の高い研究である。下部のダイヤモンドが統合された全体効果を表す。

#### Thick Forest Plot（metaviz）

```{r}
#| label: thick-forest
#| fig-width: 10
#| fig-height: 10
if (requireNamespace("metaviz", quietly = TRUE)) {
  viz_forest(x = m,
             xlab = "Hedges g",
             method = "REML",
             type = "standard",
             variant = "thick",
             text_size = 3)
}
```

#### Sunset Funnel Plot（metaviz）

```{r}
#| label: sunset-funnel
#| fig-width: 8
#| fig-height: 6
if (requireNamespace("metaviz", quietly = TRUE)) {
  viz_sunset(x = m,
             xlab = "Hedges g",
             text_size = 3)
}
```

**図の読み方**: Sunset funnel plot は有意水準の領域を色分けして表示し、出版バイアスの検討をより直感的に行えるようにした図である。

#### GOSH Plot（外れ値と異質性の探索）

GOSH (Graphical Display of Study Heterogeneity) plot は、研究のすべての可能な部分集合についてモデルを適合し、推定値と $I^2$ の分布を可視化する手法である。計算量が大きいため、研究数が 20 件以上の場合はサブセットを用いることが推奨される。

```{r}
#| label: gosh-plot
#| fig-width: 10
#| fig-height: 5
#| cache: true
# 計算量が多いため subsets で制限
g <- gosh(m, subsets = 500)
plot(g, out = 1, breaks = 50)
```

**図の読み方**: ヒストグラムのピークが分析の中心的推定値に対応し、裾野の広がりが異質性の影響を反映する。二峰性が認められる場合、特定の研究が全体の結果を大きく左右している可能性がある。

#### dmetar による外れ値検出とパワー分析

```{r}
#| label: dmetar-tools
if (requireNamespace("dmetar", quietly = TRUE)) {
  library(dmetar)

  # 外れ値の検出
  outliers <- find.outliers(m)
  # find.outliers() returns an rma object with $out containing outlier info
  # Extract outlier studies and display re-estimated model
  out_studies <- outliers$out
  if (!is.null(out_studies)) {
    # out may be study indices or names
    if (is.data.frame(out_studies)) {
      tibble::tibble(
        Study    = out_studies$slab,
        Estimate = out_studies$yi,
        CI_lower = out_studies$ci.lb,
        CI_upper = out_studies$ci.ub
      ) |>
        gt() |>
        tab_header(title = "外れ値と判定された研究") |>
        fmt_number(columns = c(Estimate, CI_lower, CI_upper), decimals = 3)
    } else {
      tibble::tibble(Outlier_Studies = as.character(out_studies)) |>
        gt() |>
        tab_header(title = "外れ値と判定された研究")
    }
  } else {
    tibble::tibble(Result = "外れ値は検出されませんでした") |>
      gt() |>
      tab_header(title = "外れ値検出結果")
  }
}
```

```{r}
#| label: power-analysis
if (requireNamespace("dmetar", quietly = TRUE)) {
  # 検出力分析: 中程度の効果 (d=0.3) を検出する力はあるか？
  pa <- power.analysis(d = 0.3,
                       k = m$k,
                       n1 = mean(dat$n1),
                       n2 = mean(dat$n2),
                       heterogeneity = "moderate")
  tibble::tibble(
    Parameter = c("Effect size (d)", "Number of studies (k)",
                  "Mean n1", "Mean n2", "Heterogeneity", "Power"),
    Value = c(0.3, m$k, round(mean(dat$n1), 1), round(mean(dat$n2), 1),
              "moderate", round(pa$Power, 3))
  ) |>
    gt() |>
    tab_header(title = "検出力分析の結果")
}
```

::: {.callout-tip}
#### 検出力分析の意義
メタ分析においても、研究数や各研究のサンプルサイズが不十分な場合、実際に存在する効果を検出できない可能性がある。**事前に**検出力を算出し、分析が十分な精度を備えていることを確認することが重要である。なお、事後的（post-hoc）な検出力分析には方法論的問題が指摘されているため、避けるべきである。
:::

#### dmetar による包括的な影響度分析

`InfluenceAnalysis()` は Leave-One-Out 分析を実行し、4 つの診断プロットを同時に生成するラッパー関数である。Recipe 7 の個別関数（`leave1out()`, `baujat()`）を統合的に実行できる。

```{r}
#| label: dmetar-influence
#| fig-width: 10
#| fig-height: 10
if (requireNamespace("dmetar", quietly = TRUE)) {
  # InfluenceAnalysis() は meta パッケージの metagen オブジェクトを入力として要求する。
  # metafor::rma() のオブジェクトは直接渡せないため、
  # meta::metagen() で同等のモデルを再構築する。
  if (requireNamespace("meta", quietly = TRUE)) {
    library(meta)
    m_meta <- metagen(TE = dat$yi,                  # 効果量
                      seTE = sqrt(dat$vi),           # 標準誤差
                      studlab = dat$study_id,        # 研究ラベル
                      sm = "SMD",                    # 効果量の種類
                      method.tau = "REML",
                      random = TRUE)

    # 4 パネル影響度分析
    ia <- InfluenceAnalysis(m_meta, random = TRUE)
    plot(ia)
  }
}
```

**4 つの診断プロットの読み方**:

| プロット | 縦軸 | 注目すべきパターン |
|:--|:--|:--|
| **Baujat plot** | 全体推定への寄与 | 右上の研究は影響力が大きく異質性にも寄与 |
| **Leave-one-out pooled effect** | 除外後のプール推定値 | 除外により推定値が大きくシフトする研究 |
| **Leave-one-out I²** | 除外後の I² | 除外により I² が大幅に低下する研究 |
| **Leave-one-out τ²** | 除外後の τ² | 除外により τ² が大幅に低下する研究 |

影響力の大きい研究が特定された場合、それが**外れ値**（コーディングミスや方法論的問題）なのか、**真に異なる効果**（特殊な参加者集団や介入条件）なのかを検討する。前者であれば除外を検討し、後者であればモデレーター分析で理由を探索する。

::: {.callout-note}
#### Recipe 7 との関係
Recipe 7 では `metafor::leave1out()` と `metafor::baujat()` を個別に実行した。`dmetar::InfluenceAnalysis()` はこれらを 1 つの関数呼び出しで統合し、4 つの診断を同時に可視化する。探索的な分析では `InfluenceAnalysis()` が便利であり、レポートで個別の図を掲載する場合は Recipe 7 の関数を使い分けるとよい。
:::


### Recipe 23: 効果量の算出と変換――R パッケージの活用

実際のメタ分析では、論文ごとに報告されている統計量の形式が異なることが一般的である。本節では、効果量の算出・変換に特化した R パッケージ群を紹介したうえで、頻出する変換パターンを解説する。

#### 効果量パッケージの比較

メタ分析における効果量の算出には、主に以下の 3 つのパッケージが利用可能である。

| パッケージ | 主な機能 | 入力形式 | 特徴 |
|:--|:--|:--|:--|
| `metafor::escalc()` | 効果量と分散の同時算出 | 集計統計量（*M*, *SD*, *n*, *t*, *F* 等） | メタ分析パイプラインとの直接連携。最も汎用的 |
| `esc` | 多様な統計量からの効果量変換 | *t*, *F*, *r*, *χ²*, *p*, 比率、平均差など | 入力形式ごとに専用関数（`esc_t()`, `esc_f()` 等）を提供。変換の透明性が高い |
| `effectsize` | 既適合モデルからの効果量抽出 | `t.test()`, `aov()`, `lm()` 等のモデルオブジェクト | `easystats` エコシステムの一部。モデルオブジェクトから直接効果量を算出可能 |

以下に各パッケージの使用例を示す。

#### `esc` パッケージによる効果量変換

`esc` パッケージは、論文中に報告された多様な統計量から標準化効果量（Cohen's *d* / Hedges *g*）を算出するための専用関数群を提供する。

```{r}
#| label: esc-examples
library(esc)

# t 値からの変換
esc_t(t = 2.45, totaln = 62, es.type = "g")

# F 値（1自由度）からの変換
esc_f(f = 6.0, totaln = 62, es.type = "g")

# 相関 r からの変換
esc_rpb(r = 0.30, totaln = 80, es.type = "g")

# 平均値・SD からの変換
esc_mean_sd(grp1m = 75.2, grp1sd = 10.1, grp1n = 30,
            grp2m = 70.8, grp2sd = 11.3, grp2n = 32,
            es.type = "g")
```

`esc` パッケージの利点は、各関数が変換の根拠となる数式を明示的に適用する点にある。出力には効果量、標準誤差、信頼区間、サンプルサイズが含まれ、`metafor::escalc()` への入力としてそのまま利用可能である。

#### `effectsize` パッケージによるモデルベースの効果量算出

`effectsize` パッケージは、R の統計モデルオブジェクトから直接効果量を算出する機能を提供する。一次研究の分析において特に有用であるが、メタ分析のための効果量計算にも応用できる。

```{r}
#| label: effectsize-examples
library(effectsize)

# t検定のオブジェクトから効果量を算出
set.seed(2024)
x <- rnorm(30, mean = 75, sd = 10)
y <- rnorm(30, mean = 70, sd = 11)
tt <- t.test(x, y)
hedges_g(x, y)

# Cohen's d と Hedges' g の変換
d_val <- cohens_d(x, y)
as.data.frame(d_val) |>
  gt() |>
  tab_header(title = "Cohen's d の算出結果") |>
  fmt_number(columns = where(is.numeric), decimals = 3)

# r と d の相互変換
d_to_r(d = 0.50)
r_to_d(r = 0.30)
```

::: {.callout-tip}
#### パッケージの使い分けの指針

- **コーディングシートから効果量を一括算出する場合** → `metafor::escalc()` が最適。`measure` 引数でさまざまな効果量指標に対応する
- **論文中の多様な報告形式から個別に変換する場合** → `esc` パッケージの専用関数群が便利。変換過程が透明で再現性が高い
- **自身の一次データの分析で効果量を算出する場合** → `effectsize` パッケージが R のモデルオブジェクトと直接連携できる
- **いずれのパッケージでも対応できない特殊な変換** → オンラインツール（[Effect Size Calculator](https://www.escal.site/)）を参照する
:::

#### `escalc()` による効果量変換の実例

以下では、`metafor::escalc()` を用いた頻出の変換パターンを示す。

##### 相関 *r* から Fisher *z* への変換

```{r}
#| label: es-conversion-r-to-d
# r = 0.30 の場合
r <- 0.30
d_from_r <- 2 * r / sqrt(1 - r^2)
cat("r =", r, "→ d ≈", round(d_from_r, 3), "\n")

# escalc でも可能（Fisher z から逆変換も含む）
es_example <- escalc(measure = "ZCOR", ri = c(0.20, 0.35, 0.10), ni = c(80, 120, 60))

# Fisher z 変換結果
as.data.frame(es_example[, c("yi", "vi")]) |>
  gt() |>
  tab_header(title = "Fisher z 変換結果") |>
  fmt_number(columns = c(yi, vi), decimals = 4)
cat("\n元の r に戻す: tanh(z)\n")
cat("r =", round(tanh(es_example$yi), 3), "\n")
```

##### *t* 値・*F* 値からの効果量算出

```{r}
#| label: es-from-t-f
# t値から d を計算
t_val <- 2.45
n1 <- 30; n2 <- 32
d_from_t <- t_val * sqrt(1/n1 + 1/n2)
cat("t =", t_val, "→ d ≈", round(d_from_t, 3), "\n")

# F値（1自由度）から d を計算
F_val <- 6.0
d_from_F <- sqrt(F_val) * sqrt(1/n1 + 1/n2)
cat("F =", F_val, "→ d ≈", round(d_from_F, 3), "\n")

# escalc で分散を直接計算する方法
# t値から d を計算した後、Hedges' correction で g を求める
J <- 1 - 3 / (4 * (n1 + n2 - 2) - 1)  # Hedges' correction factor
g_from_t <- d_from_t * J
vi_from_t <- (n1 + n2) / (n1 * n2) + g_from_t^2 / (2 * (n1 + n2))
cat("\nHedges g ≈", round(g_from_t, 3),
    ", vi ≈", round(vi_from_t, 4), "\n")
```

::: {.callout-note}
#### 効果量変換のオンラインツール

手計算による検算やパッケージ外での確認が必要な場合は、以下のオンラインツールが有用である。

- [Effect Size Calculator (escal.site)](https://www.escal.site/) -- 各種統計量間の双方向変換
- [Campbell Collaboration Calculator](https://www.campbellcollaboration.org/calculator/) -- 標準化された効果量計算ツール
- [Psychometrica Effect Size Calculator](https://www.psychometrica.de/effect_size.html) -- 多様な効果量指標に対応
:::


### Recipe 24: 出版品質の表の作成

#### gt パッケージによる結果の整形

```{r}
#| label: gt-publication-table
# シミュレーションデータのフォレスト情報を表にまとめる
report_df <- dat |>
  mutate(
    ci_lb = yi - 1.96 * sqrt(vi),
    ci_ub = yi + 1.96 * sqrt(vi),
    weight = round(1 / vi, 1)
  ) |>
  arrange(desc(yi)) |>
  select(study_id, level, duration, n1, n2, yi, ci_lb, ci_ub, weight)

gt(report_df) |>
  tab_header(
    title = "研究別効果量と信頼区間",
    subtitle = "明示的文法指導の効果（シミュレーション）"
  ) |>
  cols_label(
    study_id = "Study",
    level = "Education",
    duration = "Weeks",
    n1 = "n (Treat)",
    n2 = "n (Ctrl)",
    yi = "Hedges g",
    ci_lb = "95% CI LB",
    ci_ub = "95% CI UB",
    weight = "Weight"
  ) |>
  fmt_number(columns = c(yi, ci_lb, ci_ub), decimals = 3) |>
  fmt_number(columns = weight, decimals = 1) |>
  tab_style(
    style = cell_fill(color = "#e8f4f8"),
    locations = cells_body(
      columns = everything(),
      rows = level == "tertiary"
    )
  ) |>
  tab_options(table.font.size = px(13))
```


### Recipe 25: ベイズメタ分析――brms による推定 {#sec-recipe25}

本書の第 7 章（ベイズ推定）で `brms` パッケージによるベイズ推定の基本を扱った。ここでは、`brms` を用いてメタ分析をベイズ的に実行する方法を示す[@burkner2017_ma]。

#### ベイズメタ分析の利点

ベイズメタ分析は、以下の場面で頻度論的アプローチ（`rma()`）に対する有力な代替手段となる。

1. **研究数が少ない場合（*k* < 10）**: 事前分布の情報を活用することで、頻度論的推定が不安定になる状況でもより安定した推定が得られる
2. **事後確率の直接算出**: 「効果量が 0 を超える確率は 95%」のように、研究上の問いに対する確率的な回答を直接提示できる
3. **予測区間の自然な解釈**: ベイズの事後予測分布は、将来の研究における効果量の不確実性を自然に表現する

#### brms によるランダム効果メタ分析

`brms` では、各研究の標準誤差を既知として扱う `se()` 構文により、メタ分析モデルを直接指定できる。

```{r}
#| label: bayes-meta
#| cache: true
#| message: false
#| warning: false
library(brms)

# 標準誤差の列を準備
dat$sei <- sqrt(dat$vi)

# ベイズランダム効果モデル
# yi | se(sei) はメタ分析の標準的な記法
# (1 | study_id) が研究間のランダム効果
fit_bayes <- brm(
  yi | se(sei) ~ 1 + (1 | study_id),
  data = dat,
  prior = c(
    prior(normal(0, 1), class = "Intercept"),      # 弱情報事前分布
    prior(cauchy(0, 0.5), class = "sd")             # 研究間SDの事前分布（sdは自動的に非負制約）
  ),
  iter = 4000, warmup = 1000, chains = 4, seed = 2024,
  silent = 2, refresh = 0
)
```

::: {.callout-note}
#### 事前分布の選択
- **切片（全体平均効果量）**: `normal(0, 1)` は「効果量がおよそ −2 から +2 の範囲にある」という弱い信念を表現する。SLA 研究で *g* > 2 の効果はきわめて稀であるため、この設定は十分に弱情報的である
- **研究間 SD（$\tau$）**: `cauchy(0, 0.5)` は $\tau$ が 0 に近いことを穏やかに示唆しつつ、大きな値も排除しない。`sd` クラスのパラメータは `brms` が自動的に非負に制約するため、実質的に半コーシー分布となる。MCMC 診断（R-hat, ESS）の詳細は第 7 章を参照
:::

#### 事後分布の要約

```{r}
#| label: bayes-summary
# 事後分布の要約
post_summ <- posterior_summary(fit_bayes, pars = c("b_Intercept", "sd_study_id__Intercept"))
bayes_df <- as.data.frame(post_summ)
bayes_df$Parameter <- c("平均効果量 (g)", "研究間 SD (tau)")
bayes_df <- bayes_df[, c("Parameter", "Estimate", "Est.Error", "Q2.5", "Q97.5")]

gt(bayes_df) |>
  tab_header(title = "ベイズメタ分析: 事後分布の要約") |>
  fmt_number(columns = c(Estimate, Est.Error, Q2.5, Q97.5), decimals = 3) |>
  cols_label(
    Parameter = "パラメータ",
    Estimate = "事後平均",
    Est.Error = "事後SD",
    Q2.5 = "95% CrI lower",
    Q97.5 = "95% CrI upper"
  )
```

#### 「効果が正である確率」の算出

```{r}
#| label: bayes-hypothesis
# hypothesis() で事後確率を算出
h <- hypothesis(fit_bayes, "Intercept > 0")
tibble::tibble(
  仮説 = "効果量 > 0",
  `事後確率` = h$hypothesis$Post.Prob,
  `Evidence Ratio` = h$hypothesis$Evid.Ratio
) |>
  gt() |>
  tab_header(title = "ベイズ仮説検定: 効果が正である確率") |>
  fmt_number(columns = `事後確率`, decimals = 3) |>
  fmt_number(columns = `Evidence Ratio`, decimals = 1)
```

**解釈**: 事後確率が 0.95 を超えていれば、「データと事前分布を総合すると、効果が正である確率は 95% 以上である」と報告できる。Evidence Ratio は事後オッズに対応する。

#### 頻度論 vs. ベイズの結果比較

```{r}
#| label: bayes-comparison
tibble::tibble(
  手法 = c("頻度論 (REML)", "ベイズ (brms)"),
  平均効果量 = c(as.numeric(coef(m)), post_summ["b_Intercept", "Estimate"]),
  `CI/CrI lower` = c(m$ci.lb, post_summ["b_Intercept", "Q2.5"]),
  `CI/CrI upper` = c(m$ci.ub, post_summ["b_Intercept", "Q97.5"]),
  tau = c(sqrt(m$tau2), post_summ["sd_study_id__Intercept", "Estimate"])
) |>
  gt() |>
  tab_header(title = "頻度論 vs. ベイズ: 推定結果の比較") |>
  fmt_number(columns = c(平均効果量, `CI/CrI lower`, `CI/CrI upper`, tau), decimals = 3)
```

研究数が十分に大きい場合、頻度論的推定とベイズ推定は概ね一致する。両者の乖離が大きい場合は、事前分布の影響や研究間分散の推定方法の違いについて議論する。

::: {.callout-tip}
#### ベイズ vs. 頻度論の使い分け

| 状況 | 推奨手法 | 理由 |
|:--|:--|:--|
| *k* ≥ 15, 標準的な分析 | `rma()` (REML) | 十分な研究数があれば頻度論で安定した推定が得られる |
| *k* < 10 | `brms` | 事前分布により推定が安定化。特に $\tau^2$ の推定精度が向上 |
| 事前知識を明示的に組み込みたい | `brms` | 情報事前分布を設定可能 |
| 感度分析として | `brms` | 頻度論の結果と併記し、頑健性を確認 |

学位論文では、**メインの分析を頻度論（`rma()`）で行い、感度分析としてベイズ推定の結果を補足的に報告する**戦略が現実的である。
:::


## PART IV: 発展的手法――3水準モデルの深層・混合効果モデリング・構造方程式アプローチ

Recipe 21（PART III）では `rma.mv()` による 3 水準メタ分析の基本を扱った。本パートでは、3 水準モデルの数理的基礎を掘り下げるとともに、メタ分析と混合効果モデリングの理論的関係、相関する効果量への頑健な対処法（CHE モデル）、および構造方程式モデリングアプローチ（MASEM）を解説する。これらの手法は、英語教育研究におけるメタ分析の方法論的な深化に不可欠である[@cheung2015_book; @pustejovsky2022]。


### Recipe 26: 3水準メタ分析の深層理解――分散成分の分解と尤度比検定 {#sec-recipe26}

Recipe 21 では `rma.mv()` を実践的に使用したが、本 Recipe では背後にある統計モデルの構造を掘り下げる。分散成分の意味を理解することで、モデル選択（2 水準 vs. 3 水準）を統計的根拠に基づいて判断できるようになる[@assink2016; @konstantopoulos2011]。

#### 3水準モデルの数理的基礎

3 水準ランダム効果モデルは以下のように定式化される[@vandennoortgate2013]:

$$
y_{ij} = \mu + u_j + w_{ij} + e_{ij}
$$

ここで:

- $y_{ij}$: 研究 $j$ の効果量 $i$（観測値）
- $\mu$: 全体平均効果量（固定効果）
- $u_j \sim N(0, \sigma^2_{\text{between}})$: 研究間の変動（Level 3）
- $w_{ij} \sim N(0, \sigma^2_{\text{within}})$: 研究内の変動（Level 2）
- $e_{ij} \sim N(0, v_{ij})$: サンプリング誤差（Level 1）。$v_{ij}$ は各効果量の既知の分散

3 つの水準が表す意味を英語教育研究の文脈で整理する:

| 水準 | 分散成分 | 表す変動 | 英語教育研究の具体例 |
|:--|:--|:--|:--|
| Level 1 | $v_{ij}$（既知） | サンプリング誤差 | 各効果量の統計的不確実性（$n$ に依存） |
| Level 2 | $\sigma^2_{\text{within}}$ | 研究内変動 | 同一研究内で語彙・文法・ライティングの効果が異なる |
| Level 3 | $\sigma^2_{\text{between}}$ | 研究間変動 | 異なる研究間で真の効果が異なる（参加者、指導法、文脈等） |

通常の 2 水準ランダム効果モデル（`rma()` のデフォルト）は $w_{ij} = 0$（研究内変動なし）と仮定する特殊ケースに相当する。1 研究から複数の効果量を抽出する場合、この仮定は不適切であり、3 水準モデルが必要となる。

#### 分散成分の分解と ICC

Recipe 21 で適合した 3 水準モデル（`m_ml`）の分散成分を詳細に分解する。以下のコードでは、研究間（Level 3）と研究内（Level 2）の分散がそれぞれ全体の異質性のうちどの程度を説明するかを算出する。

```{r}
#| label: variance-decomposition-detail
# Recipe 21 の m_ml を使用
# sigma2[1] = Level 3（研究間）, sigma2[2] = Level 2（研究内）

sigma2_L3 <- m_ml$sigma2[1]  # 研究間分散
sigma2_L2 <- m_ml$sigma2[2]  # 研究内分散
total_heterogeneity <- sigma2_L3 + sigma2_L2

# ICC: 各水準が全体の異質性に占める割合
ICC_L3 <- sigma2_L3 / total_heterogeneity * 100
ICC_L2 <- sigma2_L2 / total_heterogeneity * 100

tibble::tibble(
  `分散成分` = c("σ²_between (Level 3: 研究間)",
                 "σ²_within (Level 2: 研究内)",
                 "合計（サンプリング誤差を除く）"),
  `推定値` = c(sigma2_L3, sigma2_L2, total_heterogeneity),
  `割合 (%)` = c(ICC_L3, ICC_L2, 100.0)
) |>
  gt() |>
  tab_header(title = "3水準モデルの分散成分の分解") |>
  fmt_number(columns = `推定値`, decimals = 4) |>
  fmt_number(columns = `割合 (%)`, decimals = 1) |>
  tab_footnote(
    footnote = "Level 1（サンプリング誤差）は各効果量の既知の分散 v_ij であり、推定対象ではない",
    locations = cells_column_labels(columns = `分散成分`)
  )
```

**解釈**: Level 3（研究間）の割合が高い場合、効果量のばらつきは主に研究間の違い（参加者集団、指導法の実施方法、文脈）に起因する。Level 2（研究内）の割合が高い場合、同一研究内でもアウトカムによって効果が異なることを意味する。この情報は、モデレーター分析の方向性を示唆する。

#### 尤度比検定によるモデル選択

3 水準モデルが 2 水準モデルよりも統計的に優れているかを検証するため、尤度比検定（LRT）を実施する。LRT ではランダム効果構造の異なるモデルを比較するため、最尤法（ML）で適合する必要がある（REML はランダム効果構造が異なるモデル間の比較に適さない）。

```{r}
#| label: lrt-three-level
# ML による適合（LRT のため）
m_3level_ml <- rma.mv(yi, vi,
                       random = ~ 1 | study_id / es_id,
                       data = uchi_clean,
                       method = "ML")

# 比較モデル 1: 研究間変動のみ（Level 2 を除去）
m_2level_between <- rma.mv(yi, vi,
                            random = ~ 1 | study_id,
                            data = uchi_clean,
                            method = "ML")

# 比較モデル 2: 研究内変動のみ（Level 3 を除去）
m_2level_within <- rma.mv(yi, vi,
                           random = ~ 1 | es_id,
                           data = uchi_clean,
                           method = "ML")

# LRT 1: Level 2（研究内変動）は必要か？
lrt1 <- anova(m_2level_between, m_3level_ml)

# LRT 2: Level 3（研究間変動）は必要か？
lrt2 <- anova(m_2level_within, m_3level_ml)

tibble::tibble(
  検定 = c("3水準 vs. 研究間のみ（Level 2 の必要性）",
           "3水準 vs. 研究内のみ（Level 3 の必要性）"),
  `対数尤度（制約モデル）` = c(logLik(m_2level_between),
                               logLik(m_2level_within)),
  `対数尤度（3水準モデル）` = c(logLik(m_3level_ml),
                               logLik(m_3level_ml)),
  `LRT 統計量` = c(lrt1$LRT, lrt2$LRT),
  `p 値` = c(lrt1$pval, lrt2$pval)
) |>
  gt() |>
  tab_header(title = "尤度比検定: 3水準モデルの各水準の必要性") |>
  fmt_number(columns = c(`対数尤度（制約モデル）`,
                          `対数尤度（3水準モデル）`,
                          `LRT 統計量`), decimals = 2) |>
  fmt_number(columns = `p 値`, decimals = 4)
```

**解釈**: $p$ 値が有意（< .05）であれば、当該水準の分散成分は 0 ではなく、3 水準モデルが統計的に支持される。両方の検定が有意であれば、研究間と研究内の両方の変動が存在し、3 水準モデルの使用が正当化される。

::: {.callout-tip}
#### 報告例

> 3 水準モデルの必要性を尤度比検定で確認した。研究内変動（Level 2; $\sigma^2_{\text{within}}$）の追加は LRT = XX.XX, $p$ < .001 で有意であり、研究間変動（Level 3; $\sigma^2_{\text{between}}$）の追加も LRT = XX.XX, $p$ < .001 で有意であった。したがって、3 水準ランダム効果モデルの使用が統計的に支持された[@assink2016]。
:::

#### プロファイル尤度による分散成分の信頼区間

REML の点推定値だけでは分散成分の不確実性が伝わらない。プロファイル尤度法により、$\sigma^2_{\text{between}}$ と $\sigma^2_{\text{within}}$ それぞれの 95% 信頼区間を構成する。

```{r}
#| label: profile-likelihood
#| fig-width: 10
#| fig-height: 5
#| cache: true
# プロファイル尤度プロット
par(mfrow = c(1, 2))
profile(m_ml, sigma2 = 1, main = "Level 3: Between-study variance")
profile(m_ml, sigma2 = 2, main = "Level 2: Within-study variance")
par(mfrow = c(1, 1))
```

**読み方**: 横軸が $\sigma^2$ の値、縦軸がプロファイル尤度（対数尤度からの差分）である。曲線が破線（95% 閾値）と交わる 2 点が信頼区間の下限・上限にあたる。曲線が鋭い（狭い CI）ほど推定精度が高い。

::: {.callout-note}
#### 3水準モデルを使う場面の判断基準

| 基準 | 2水準で十分 | 3水準が必要 |
|:--|:--|:--|
| 効果量の数 | 各研究から 1 つ | 各研究から複数（平均 > 2） |
| ICC（Level 2） | — | > 10% |
| LRT（Level 2 の追加） | $p$ > .05 | $p$ < .05 |
| 研究数 *k* | — | *k* ≥ 15 が望ましい |

*k* が少ない（< 15）場合でも 3 水準モデルは適用可能だが、分散成分の推定精度は低下する。その場合はプロファイル尤度 CI の幅を確認し、結果の解釈に慎重を期す。
:::


### Recipe 27: メタ分析と混合効果モデリングの理論的関係 {#sec-recipe27}

本 Recipe は実践的な分析手順ではなく、メタ分析の統計的基盤を理解するための概念的な解説である。3 水準モデルや CHE モデルの理論的背景を深く理解したい場合に参照されたい。すぐに分析を行いたい場合は Recipe 28（CHE モデル）に進んで構わない。

メタ分析は本質的に**混合効果モデルの特殊ケース**である。この関係を理解することで、メタ分析の統計的基盤がより明確に把握できる[@viechtbauer2010; @cheung2015_book]。

#### ランダム効果メタ分析 = 既知分散の混合効果モデル

通常のランダム効果メタ分析モデルは以下のように書ける:

$$
y_i = \mu + u_i + e_i, \quad u_i \sim N(0, \tau^2), \quad e_i \sim N(0, v_i)
$$

これは `nlme::lme()` や `lme4::lmer()` で適合する線形混合効果モデルと構造的に同一である。ただし、決定的な違いが 1 つある:

| 特徴 | メタ分析（`rma()`） | 一般的な混合効果モデル（`lme()`/`lmer()`） |
|:--|:--|:--|
| Level 1 分散 $v_i$ | **既知**（各研究の $n$ と効果量から計算済み） | **未知**（データから推定） |
| 重み付け | $1/v_i$（精度重み: $n$ が大きい研究ほど重い） | 残差分散から導出 |
| 推定対象 | $\mu$（全体平均）と $\tau^2$（研究間分散）のみ | $\mu$、$\tau^2$、**および** $\sigma^2$（残差分散） |

::: {.callout-note}
#### 本質的な違い:「分散が既知」とは何を意味するか
通常の回帰分析やマルチレベルモデルでは、残差分散 $\sigma^2$ はデータから推定する。メタ分析では、各効果量の標準誤差 $\sqrt{v_i}$ が一次研究の情報（サンプルサイズ、SD 等）から既に計算されているため、Level 1 の分散は推定する必要がない。この「既知分散」の仮定こそが、メタ分析を混合効果モデリングの特殊ケースにしている。
:::

#### `rma()` と `nlme::lme()` の等価性の実証

以下のコードでは、同一のデータに対して `metafor::rma()` と `nlme::lme()` を適合し、推定値が一致することを確認する。

```{r}
#| label: rma-vs-lme
library(nlme)

# --- metafor::rma() ---
m_metafor <- rma(yi, vi, data = dat, method = "REML")

# --- nlme::lme() で同等のモデルを適合 ---
# ポイント: weights = varFixed(~vi) で Level 1 分散を既知として扱う
m_nlme <- lme(yi ~ 1,
              random = ~ 1 | study_id,
              weights = varFixed(~ vi),
              data = dat,
              method = "REML")

# 結果の比較
tibble::tibble(
  パラメータ = c("全体平均効果量 (μ)", "研究間SD (τ)", "研究間分散 (τ²)"),
  `metafor::rma()` = c(
    as.numeric(coef(m_metafor)),
    sqrt(m_metafor$tau2),
    m_metafor$tau2
  ),
  `nlme::lme()` = c(
    fixef(m_nlme),
    as.numeric(VarCorr(m_nlme)[1, "StdDev"]),
    as.numeric(VarCorr(m_nlme)[1, "Variance"])
  )
) |>
  gt() |>
  tab_header(title = "metafor vs. nlme: 推定値の比較") |>
  fmt_number(columns = c(`metafor::rma()`, `nlme::lme()`), decimals = 4) |>
  tab_footnote(
    footnote = "微小な差異は数値最適化の収束基準の違いによる",
    locations = cells_column_labels(columns = `nlme::lme()`)
  )
```

**読み方**: 両者の推定値はほぼ一致する（微小な差は最適化アルゴリズムの違いによる）。これにより、`rma()` が混合効果モデルの特殊形であることが実証される。

#### 3水準メタ分析と `lme()` の対応

3 水準モデル（Recipe 21, 26）も `lme()` で表現可能である:

```{r}
#| label: rma-mv-vs-lme
# --- metafor::rma.mv() の 3水準モデル ---
# m_mv は Recipe 21 の m_ml と同一（再適合）
m_mv <- rma.mv(yi, vi,
               random = ~ 1 | study_id / es_id,
               data = uchi_clean,
               method = "REML")

# --- nlme::lme() の等価モデル ---
m_lme_3 <- lme(yi ~ 1,
               random = ~ 1 | study_id / es_id,
               weights = varFixed(~ vi),
               data = uchi_clean,
               method = "REML",
               control = lmeControl(opt = "optim"))

# 結果の比較
tibble::tibble(
  パラメータ = c("全体平均効果量 (μ)",
                 "Level 3: 研究間SD",
                 "Level 2: 研究内SD"),
  `rma.mv()` = c(
    as.numeric(coef(m_mv)),
    sqrt(m_mv$sigma2[1]),
    sqrt(m_mv$sigma2[2])
  ),
  `lme()` = {
    vc <- VarCorr(m_lme_3)
    # nlme::VarCorr の行名は "study_id =" や "es_id =" のように
    # 表記される場合があるため、部分一致で安全に取得する
    sd_vals <- as.numeric(vc[, "StdDev"])
    sd_vals <- sd_vals[!is.na(sd_vals) & sd_vals > 0]
    c(fixef(m_lme_3), sd_vals[1], sd_vals[2])
  }
) |>
  gt() |>
  tab_header(title = "rma.mv() vs. lme(): 3水準モデルの推定値比較") |>
  fmt_number(columns = c(`rma.mv()`, `lme()`), decimals = 4)
```

::: {.callout-tip}
#### なぜ `lme()`/`lmer()` ではなく `metafor` を使うのか

理論的に等価であるにもかかわらず、メタ分析には `metafor` パッケージの使用が強く推奨される:

1. **メタ分析専用の機能**: フォレストプロット、ファンネルプロット、Trim-and-Fill、感度分析、出版バイアス検定など、メタ分析に必須のツールが統合されている
2. **効果量の算出**: `escalc()` による標準化された効果量と分散の算出が組み込まれている
3. **分野標準**: 学術論文で `lme()` によるメタ分析を報告すると、レビューアから方法論的な疑問を呈される可能性がある。`metafor` はメタ分析の事実上の標準パッケージである
4. **分散の扱い**: `metafor` は $v_i$ を厳密に「既知」として扱うが、`lme()`/`lmer()` では `varFixed()` の指定を誤るとモデルが変わるリスクがある

ただし、`lme()`/`lmer()` との等価性を理解することには大きな教育的価値がある。メタ分析をマルチレベルモデリングの文脈で捉え直すことで、発展的手法（3 水準モデル、CHE モデル等）の理論的背景がより明瞭になる。
:::


### Recipe 28: CHE モデルと頑健分散推定――相関する効果量への対処 {#sec-recipe28}

3 水準モデル（Recipe 21, 25）は、同一研究内の効果量間を条件付き独立と仮定する。しかし、同一参加者から得られた複数のアウトカム指標（例: 語彙テストと文法テスト）は互いに相関するため、この仮定は現実的でない場合がある。**CHE（Correlated-and-Hierarchical Effects）モデル**[@pustejovsky2022]は、研究内効果量の相関構造を近似的に取り込みつつ、頑健な標準誤差を得るための枠組みを提供する。

#### 問題の背景: 研究内効果量の相関

同一研究から複数の効果量を抽出する場合（例: 語彙テストと文法テストの効果、即時テストと遅延テスト）、それらは共通の参加者サンプルに基づくため相関する。しかし、一次研究がこの相関値 $\rho$ を報告することは稀である。

CHE モデルは以下のアプローチをとる:

1. 研究内効果量間の相関 $\rho$ を**仮定**して近似的な分散共分散行列 $\mathbf{V}$ を構築する
2. `rma.mv()` で $\mathbf{V}$ を指定してモデルを適合する
3. **頑健分散推定**（CR2; `clubSandwich` パッケージ）により、$\rho$ の仮定に対して頑健な標準誤差を得る
4. $\rho$ を変化させて感度分析を行い、結果の安定性を確認する

#### 分散共分散行列の構築

`metafor::vcalc()` 関数を用いて、仮定した $\rho$ のもとで近似的な分散共分散行列を構築する。

```{r}
#| label: che-vcalc
# 研究内効果量間の相関を rho = 0.6 と仮定
V_assumed <- vcalc(vi, cluster = study_id, obs = es_id,
                   data = uchi_clean, rho = 0.6)

# V はブロック対角行列
# 対角要素: vi（各効果量の分散）
# 非対角要素（同一研究内）: rho * sqrt(vi_a * vi_b)
cat("V 行列の次元:", dim(V_assumed), "\n")
cat("非ゼロ要素の割合:",
    round(sum(V_assumed != 0) / prod(dim(V_assumed)) * 100, 1), "%\n")
```

#### CHE モデルの適合

構築した $\mathbf{V}$ を `rma.mv()` の第 2 引数に指定しつつ、ランダム効果構造も保持する。

```{r}
#| label: che-model
# CHE モデル: V を指定しつつ、ランダム効果も保持
m_che <- rma.mv(yi, V_assumed,
                random = ~ 1 | study_id / es_id,
                data = uchi_clean,
                method = "REML")

rma_to_gt(m_che, title = "CHE モデル (ρ = 0.6)")
```

#### 頑健分散推定（CR2）

モデルベースの SE は $\rho$ の仮定に依存する。CR2 推定量（`clubSandwich` パッケージ）による頑健 SE は、$\rho$ が正しくない場合でもおおむね妥当な推論を保証する。

```{r}
#| label: che-robust
library(clubSandwich)

# CR2 推定量による頑健な推論
m_che_robust <- robust(m_che, cluster = uchi_clean$study_id, clubSandwich = TRUE)

tibble::tibble(
  手法 = c("モデルベース SE", "CR2 頑健 SE"),
  推定値 = c(as.numeric(coef(m_che)), as.numeric(coef(m_che))),
  SE = c(m_che$se, m_che_robust$se),
  `95% CI lower` = c(m_che$ci.lb, m_che_robust$ci.lb),
  `95% CI upper` = c(m_che$ci.ub, m_che_robust$ci.ub),
  `p 値` = c(m_che$pval, m_che_robust$pval)
) |>
  gt() |>
  tab_header(title = "CHE モデル: モデルベース vs. 頑健 SE の比較") |>
  fmt_number(columns = c(推定値, SE, `95% CI lower`, `95% CI upper`), decimals = 4) |>
  fmt_number(columns = `p 値`, decimals = 4)
```

#### 相関 $\rho$ に対する感度分析

CHE モデルの最大の利点は、仮定した $\rho$ の値に対する結果の頑健性を系統的に検証できることである。以下のコードでは $\rho$ を 0 から 0.9 まで変化させ、プール推定値と信頼区間の変動を可視化する。

```{r}
#| label: che-sensitivity
#| fig-width: 8
#| fig-height: 5
# rho を 0 から 0.9 まで変化させる
rho_grid <- seq(0, 0.9, by = 0.1)

sens_list <- lapply(rho_grid, function(rho) {
  V <- vcalc(vi, cluster = study_id, obs = es_id,
             data = uchi_clean, rho = rho)
  m <- rma.mv(yi, V,
              random = ~ 1 | study_id / es_id,
              data = uchi_clean, method = "REML")
  r <- robust(m, cluster = uchi_clean$study_id, clubSandwich = TRUE)
  data.frame(rho = rho,
             estimate = as.numeric(r$beta),
             se = as.numeric(r$se),
             ci_lb = as.numeric(r$ci.lb),
             ci_ub = as.numeric(r$ci.ub))
})
sens_df <- do.call(rbind, sens_list)

# 感度分析プロット
ggplot(sens_df, aes(x = rho, y = estimate)) +
  geom_ribbon(aes(ymin = ci_lb, ymax = ci_ub), alpha = 0.2, fill = "steelblue") +
  geom_line(linewidth = 1, color = "steelblue") +
  geom_point(size = 2, color = "steelblue") +
  labs(
    title = "Sensitivity of pooled estimate to assumed within-study correlation",
    x = expression("Assumed " * rho),
    y = "Pooled effect (Fisher z)",
    caption = "Shaded area = 95% CR2 robust CI"
  ) +
  scale_x_continuous(breaks = rho_grid) +
  theme_bw(base_size = 12, base_family = "HiraginoSans-W3")
```

**読み方**: $\rho$ を 0 から 0.9 まで変化させても、プール推定値と信頼区間がほぼ一定であれば、結果は $\rho$ の仮定に対して頑健であるといえる。推定値が大きく変動する場合は、結果の解釈に注意が必要であり、$\rho$ の仮定が実質的な影響を及ぼしている旨を報告すべきである。

::: {.callout-tip}
#### CHE モデル vs. 3水準モデル: どちらを選ぶか

| 基準 | 3水準モデル（Recipe 21, 26） | CHE モデル（本 Recipe） |
|:--|:--|:--|
| 研究内相関の扱い | 条件付き独立を仮定 | $\rho$ を仮定 + 頑健 SE |
| 長所 | シンプル。相関の仮定不要 | $\rho$ への感度分析が可能 |
| 短所 | SE がやや不正確な場合がある | $\rho$ の初期値の設定が必要 |
| 推奨場面 | 研究内効果量が少数（2--3 個） | 研究内効果量が多数（4 個以上） |

実務的には、**3 水準モデルをメイン分析として報告し、CHE モデルによる感度分析を補足的に示す**ことで、レビューアの懸念に対処できる[@pustejovsky2022]。
:::


### Recipe 29: メタ分析的構造方程式モデリング（MASEM）――相関行列の統合 {#sec-recipe29}

これまでの Recipe では、各研究から 1 つまたは複数の効果量（*d* や *r*）を抽出し、それらを統合してきた。**MASEM（Meta-Analytic Structural Equation Modeling）**は、各研究から報告された**相関行列**を統合し、その統合相関行列に対して構造方程式モデル（SEM）を適合するアプローチである[@cheung2015_book; @jak2021_masem]。

#### MASEM の概念と手順

MASEM は、以下の 2 段階で進行する（TSSEM: Two-Stage SEM）[@cheung2014_metasem]:

1. **Stage 1**: 複数の研究から報告された相関行列をメタ分析的に統合し、プール相関行列を得る
2. **Stage 2**: プール相関行列に対して SEM（パス解析、CFA 等）を適合する

英語教育研究における典型的な応用場面:

- **動機づけ → 学習方略 → L2 習熟度** の媒介分析を複数の研究から検証する
- **L2 不安、動機づけ、自己効力感** の因子構造を複数の研究で確認的に検証する
- **語彙知識 → 読解力 → ライティング力** のパスモデルを統合的に検証する

#### シミュレーションデータの生成

MASEM を実演するため、「動機づけ（MOT）、L2 不安（ANX）、L2 習熟度（PROF）」の 3 変数について 12 件の研究から相関行列が報告されたシナリオを設定する。

```{r}
#| label: masem-simulate
set.seed(2024)
k_masem <- 12

# 真の母相関行列
R_true <- matrix(c(
  1.00,  -0.30,  0.45,
 -0.30,   1.00, -0.35,
  0.45,  -0.35,  1.00
), nrow = 3, byrow = TRUE,
dimnames = list(c("MOT", "ANX", "PROF"), c("MOT", "ANX", "PROF")))

# 各研究の相関行列をシミュレーション
# MASS::mvrnorm() で生データを生成し、相関行列を計算
sim_cors <- lapply(1:k_masem, function(i) {
  n <- sample(60:200, 1)
  X <- MASS::mvrnorm(n, mu = rep(0, 3), Sigma = R_true)
  colnames(X) <- colnames(R_true)
  R_obs <- cor(X)
  list(R = R_obs, n = n)
})

# 結果の確認（最初の 2 研究）
cat("Study 1 (n =", sim_cors[[1]]$n, "):\n")
round(sim_cors[[1]]$R, 3)
cat("\nStudy 2 (n =", sim_cors[[2]]$n, "):\n")
round(sim_cors[[2]]$R, 3)
```

**注意**: 実際の MASEM では、各研究の相関行列と $n$ をコーディングシートから読み込む。上記はデモンストレーション用のシミュレーションである。

#### Stage 1: 各ペアワイズ相関のメタ分析

`metafor` で各ペアワイズ相関を個別に Fisher *z* 変換してメタ分析する。これは TSSEM の Stage 1 に相当する簡易的なアプローチである。

```{r}
#| label: masem-pairwise
# 各ペアワイズ相関を Fisher z に変換してメタ分析
pairs <- list(
  c("MOT", "ANX"), c("MOT", "PROF"), c("ANX", "PROF")
)

masem_list <- lapply(pairs, function(p) {
  rs <- sapply(sim_cors, function(s) s$R[p[1], p[2]])
  ns <- sapply(sim_cors, function(s) s$n)
  es <- escalc(measure = "ZCOR", ri = rs, ni = ns)
  m <- rma(yi, vi, data = es, method = "REML")
  data.frame(
    pair = paste(p[1], "---", p[2]),
    r_pooled = round(tanh(coef(m)), 3),
    z_pooled = round(as.numeric(coef(m)), 3),
    ci_lb_r = round(tanh(m$ci.lb), 3),
    ci_ub_r = round(tanh(m$ci.ub), 3),
    I2 = round(m$I2, 1),
    k = m$k
  )
})
masem_results <- do.call(rbind, masem_list)

gt(masem_results) |>
  tab_header(title = "ペアワイズ相関のメタ分析（Stage 1）") |>
  cols_label(
    pair = "変数ペア",
    r_pooled = "統合 r",
    z_pooled = "統合 z",
    ci_lb_r = "95% CI lower (r)",
    ci_ub_r = "95% CI upper (r)",
    I2 = "I\u00B2 (%)",
    k = "k"
  )
```

```{r}
#| label: masem-pooled-matrix
# プール相関行列の構築
R_pooled <- diag(3)
dimnames(R_pooled) <- dimnames(R_true)

for (i in 1:nrow(masem_results)) {
  p <- strsplit(masem_results$pair[i], " --- ")[[1]]
  R_pooled[p[1], p[2]] <- masem_results$r_pooled[i]
  R_pooled[p[2], p[1]] <- masem_results$r_pooled[i]
}

cat("プール相関行列（Stage 1 の結果）:\n")
round(R_pooled, 3)
```

#### Stage 2: プール相関行列への SEM 適合

統合されたプール相関行列に対してパス分析モデルを適合する。ここでは `lavaan` を用い、調和平均 $n$ を標本サイズとして使用する（Stage 1 の不確実性を反映しない近似的アプローチ）。

```{r}
#| label: masem-stage2
#| message: false
#| warning: false
library(lavaan)

# パスモデル: MOT → PROF, ANX → PROF
model_spec <- '
  PROF ~ b1*MOT + b2*ANX
'

# 調和平均 n を算出
ns_masem <- sapply(sim_cors, function(s) s$n)
n_harmonic <- k_masem / sum(1 / ns_masem)

# プール相関行列に対して SEM を適合
fit_sem <- sem(model_spec,
               sample.cov = R_pooled,
               sample.nobs = round(n_harmonic),
               std.lv = TRUE)

# パス係数の抽出
param_df <- parameterEstimates(fit_sem) |>
  filter(op == "~") |>
  select(lhs, rhs, est, se, z, pvalue, ci.lower, ci.upper)

gt(param_df) |>
  tab_header(title = "MASEM Stage 2: パス係数の推定") |>
  fmt_number(columns = c(est, se, z, ci.lower, ci.upper), decimals = 3) |>
  fmt_number(columns = pvalue, decimals = 4) |>
  cols_label(
    lhs = "従属変数", rhs = "独立変数",
    est = "\u03B2", se = "SE", z = "z",
    pvalue = "p", ci.lower = "95% CI lower", ci.upper = "95% CI upper"
  )
```

**解釈**: $\beta$ 係数は、他の変数を統制した上での各予測変数の効果を示す。Recipe 12 のような二変量の相関メタ分析とは異なり、MASEM は**多変量の関係を同時に推定**できる点に利点がある。

::: {.callout-warning}
#### 簡易版 MASEM の限界

上記の `lavaan` によるアプローチは教育目的の簡易版であり、以下の限界がある:

- **Stage 1 の不確実性が Stage 2 に伝播しない**: 各相関の推定誤差が SEM の SE に反映されない
- **調和平均 $n$ は近似**: 正確な SE を得るには `metaSEM::tssem1()` + `tssem2()` による正式な TSSEM が必要[@cheung2014_metasem]
- **異質性の扱い**: 相関の $I^2$ が大きい場合、プール相関行列の代表性に疑問が生じる

正式な MASEM の実施には `metaSEM` パッケージの使用を推奨する。`metaSEM` は `OpenMx` をバックエンドとして Stage 1 の不確実性を Stage 2 に適切に伝播させる統計的枠組みを提供する[@cheung2015_book; @cheung2019_reflections]。ただし、探索的な目的や学位論文の予備分析としては、本 Recipe のアプローチでも十分に有用である。本 Recipe で学んだ概念的手順（Stage 1 → Stage 2）は `metaSEM` でも同一である。
:::

::: {.callout-tip}
#### MASEM を使う場面

| 研究の問い | 適切な手法 |
|:--|:--|
| 「介入の効果はどの程度か？」 | `rma()` / `rma.mv()` |
| 「変数 A と B の関連はどの程度か？」 | Recipe 12（相関メタ分析） |
| 「A → B → C の媒介経路は支持されるか？」 | **MASEM** |
| 「複数構成概念の因子構造は一貫しているか？」 | **MASEM** |

英語教育研究では、「動機づけが学習方略を介して L2 習熟度に影響する」といった理論的な媒介モデルの検証に MASEM が有効である。ただし、十分な数の研究（*k* ≥ 10）が各変数ペアの相関を報告していることが前提条件となる[@jak2021_masem]。
:::

#### PART IV のまとめ

本パートで扱った 4 つの発展的手法を整理する:

```{r}
#| label: part4-summary-table
#| echo: false
tibble::tribble(
  ~手法, ~`主な用途`, ~`R での実装`, ~`前提条件`,
  "3水準モデル深層分析", "分散成分の分解、LRT によるモデル選択", "`rma.mv()` + `anova()`", "1研究 = 複数効果量",
  "混合効果モデリング等価性", "メタ分析の統計的基盤の理解", "`rma()` ≈ `lme()`", "教育・理解目的",
  "CHE モデル + 頑健 SE", "研究内相関への頑健な対処", "`rma.mv()` + `vcalc()` + `robust()`", "ρ の感度分析が必要",
  "MASEM", "理論モデル（パス分析・CFA）の統合的検証", "`metaSEM` or `lavaan`（簡易版）", "各研究が相関行列を報告"
) |>
  gt() |>
  tab_header(title = "PART IV: 発展的手法の一覧") |>
  tab_options(table.font.size = px(13)) |>
  tab_style(
    style = cell_text(weight = "bold"),
    locations = cells_column_labels()
  )
```


## トラブルシューティング（実務で詰まりやすい点）

**ペアワイズ・メタ分析:**

::: {.callout-warning}
### `tau² = 0` と表示される

**症状**: `rma()` の出力で `tau^2 (estimated amount of total heterogeneity): 0.0000` と表示される。

**原因**: データの異質性が非常に小さい、あるいは研究数が少なすぎて $\tau^2$ の推定値が 0 に切り詰められている（REML 推定量は負値をとれないため 0 に制約される）。

**修正**: これは計算エラーではなく、統計的に正当な結果である。$I^2$ も 0% に近くなる。報告では「研究間の異質性は実質的にゼロであった」と記述し、固定効果モデルの結果を併記することを検討する。ただし、*k* < 10 の場合は $\tau^2$ の推定精度自体が低いため、ベイズ推定（Recipe 25）による感度分析が有用である。
:::

::: {.callout-warning}
### フォレストプロットのラベルが重なって読めない

**症状**: `forest(m)` の出力で研究名や CI が重なり合い、判読不能。

**原因**: 研究数（*k* > 30）に対して描画領域が不足している。

**修正**: (1) チャンクオプションで `#| fig-height:` を研究数に応じて増やす（目安: *k* × 0.35 + 2）、(2) `forest()` の `cex` 引数でフォントサイズを縮小（`cex = 0.7`）、(3) 研究数が多い場合は `forestploter` パッケージで出版品質のカスタムプロットを作成する。
:::

::: {.callout-warning}
### 一部の研究の効果量が不自然に大きい（または符号が逆）

**症状**: フォレストプロットで数件の研究が他と大きく乖離する。*g* = −1.5 など、理論的に考えにくい値が出現する。

**原因**: コーディングシートにおいて介入群と統制群の M1/M2 が入れ替わっている。特に、異なるコーダーが担当した研究で生じやすい。

**修正**: (1) 元論文に戻り、どちらの群が介入群かを確認する、(2) 必要に応じて `dat$yi[i] <- -dat$yi[i]` で符号を反転する、(3) コーディングシートの段階で「正の効果量 = 介入が有利」の定義を統一しておく。
:::

::: {.callout-warning}
### `escalc()` が `NA` を返す

**症状**: 一部の研究で `yi` や `vi` が `NA` になる。

**原因**: (1) 入力値（M, SD, n）に欠損がある、(2) 不正な値（SD が 0 や負、n が 0）が含まれている、(3) `measure` と入力引数の組み合わせが不適切。

**修正**: (1) `summary()` や `is.na()` で入力データの欠損を確認する、(2) SD = 0 の場合はデータの入力誤りでないかを検証する、(3) 元論文が *t* 値や *F* 値のみを報告している場合は `esc` パッケージの専用関数（`esc_t()`, `esc_f()`）で代替変換を試みる。
:::

::: {.callout-warning}
### サブグループ分析の一方に *k* < 5

**症状**: あるサブグループの 95% CI が極端に広く、推定が不安定。

**原因**: サブグループの研究数が少なすぎて、推定精度が不十分。

**修正**: (1) カテゴリを統合して研究数を確保する（例: "beginner" と "intermediate" を "lower" にまとめる）、(2) 差の統計的検定は行わず、記述的な報告にとどめる、(3) limitations セクションで検出力不足を明記する。各サブグループに最低 10 件の研究が必要とされるのが一般的な目安である。
:::

::: {.callout-warning}
### Trim-and-fill が多数の研究を補完するが、推定値がほとんど変わらない

**症状**: `trimfill()` が 5 件以上の仮想的研究を補完するが、補完前後の効果量推定値の差が 0.01 未満。

**原因**: 補完された仮想的研究は精度が低い（標準誤差が大きい）ため、ランダム効果モデルにおける重みが小さく、全体推定値への影響が限定的。

**修正**: これは実際にはポジティブな結果であり、「出版バイアスを仮定しても、結論は実質的に変わらない」ことを意味する。報告では「Trim-and-fill 法により *k* = X の研究が補完されたが、調整後の推定値（*g* = Y.YY）は調整前（*g* = Z.ZZ）と実質的に同等であり、結果の頑健性が確認された」と記述する。
:::

**NMA（ネットワーク・メタ分析）:**

::: {.callout-warning}
### NMA のネットワークが非連結

**症状**: `netmeta()` でエラーが発生し、「disconnected network」と表示される。

**原因**: 一部の介入間に直接的にも間接的にも比較経路が存在しない。たとえば、{A, B, C} と {D, E} がそれぞれ内部で比較されているが、2 つのグループ間に橋渡しとなる研究がない。

**修正**: (1) `netgraph()` でネットワーク構造を可視化し、非連結の箇所を特定する、(2) 非連結な部分ネットワークを個別に分析する、(3) 孤立した介入を除外してネットワークを連結にする。
:::

::: {.callout-warning}
### `rankogram()` で特定の介入のランキング分布が二峰性を示す

**症状**: rankogram のバープロットで、ある介入が U 字型の分布を示す（1 位と最下位に棒が高く、中間順位が低い）。

**原因**: その介入に関する直接比較の研究数が極端に少なく、間接比較の経路によって推定値が大きく異なる。結果として、ランキングの不確実性が増大する。

**修正**: (1) `dmetar::direct.evidence.plot()` でその介入の直接エビデンス割合を確認する、(2) 直接エビデンス割合が低い場合、その介入を含む比較の解釈に慎重になる旨を論文に明記する、(3) 感度分析として当該介入を除外した NMA を追加で実施し、残りの介入間のランキングが安定するかを確認する。
:::

::: {.callout-warning}
### `netmeta()` で `singular` または `computationally singular` 警告

**症状**: `netmeta()` の実行時に "computationally singular" や "system is exactly singular" という警告メッセージが表示される。

**原因**: (1) `pairwise()` で多群試験の処理が不適切であり、同一の情報を重複して含む比較対が生じている、(2) 同一研究内で同じ比較ペアが重複して含まれている、(3) ネットワーク内のある比較の自由度が 0 になっている。

**修正**: (1) `pairwise()` に渡すデータを確認し、多群試験が正しく long format に変換されているか検証する（`studlab` が一意に研究を識別しているか）、(2) `pairwise()` で作成した比較ペアデータ `pw` に対し `pw[duplicated(pw[, c("studlab", "treat1", "treat2")]), ]` で重複行を検出する、(3) 3 群以上の研究では `pairwise()` が全ペアを自動生成するため、手動でペアを作成していないか確認する。
:::

**多層モデル・発展的手法:**

::: {.callout-warning}
### `rma.mv()` が収束しない

**症状**: `Warning: Convergence problem` や推定値が極端に大きい/小さい。

**原因**: (1) クラスタ数（研究数）に対してランダム効果の構造が複雑すぎる、(2) 分散成分が境界値（0 付近）にある。

**修正**: (1) 3 水準モデルが収束しない場合は 2 水準に簡素化する（`random = ~ 1 | study_id` のみ）、(2) optimizer を変更する（`control = list(optimizer = "nlminb")`）、(3) `verbose = TRUE` で反復過程を確認し、初期値の問題かどうかを診断する。
:::

::: {.callout-warning}
### CHE モデルの感度分析で推定値が $\rho$ によって大きく変動する

**症状**: CHE モデルの $\rho$ 感度分析プロットで、$\rho$ = 0 と $\rho$ = 0.9 の間でプール推定値が 0.1 以上変動する。

**原因**: 研究内に多数の効果量が含まれる研究があり、それらの重みが $\rho$ の値に強く依存している。

**修正**: (1) 研究ごとの効果量数を確認し、極端に多い研究（10+）がないかチェックする。(2) 頑健 SE（CR2）を使用しているか確認する（CR2 を使わないモデルベース SE は $\rho$ の影響をさらに受けやすい）。(3) 可能であれば、同一研究内のアウトカムの相関に関する先行研究や経験的知見を参照して $\rho$ の範囲を絞る。(4) 結果の報告では $\rho$ の範囲と推定値の変動幅を明示する。
:::


## レポーティング・チェックリスト

メタ分析の結果を学術論文として報告する際に確認すべき事項を以下にまとめる。

### 報告テンプレート

::: {.panel-tabset}

#### ペアワイズ・メタ分析

> A random-effects meta-analysis of *k* = 30 studies yielded a pooled Hedges *g* = 0.42, 95% CI [0.18, 0.66], indicating a moderate positive effect of explicit grammar instruction on EFL writing accuracy. Between-study heterogeneity was moderate ($\tau^2$ = 0.03, $I^2$ = 48%). The 95% prediction interval [−0.05, 0.89] suggests that while the effect is generally positive, it may be negligible or near zero in some contexts. Egger's regression test (*p* = .12) did not indicate significant funnel plot asymmetry. PET-PEESE analysis yielded a bias-adjusted estimate of *g* = 0.38, 95% CI [0.12, 0.64], corroborating the primary finding.

#### 相関メタ分析

> A random-effects meta-analysis synthesizing *k* = 15 correlation coefficients (Fisher *z* transformation) yielded a mean correlation of *r* = .33, 95% CI [.24, .41], indicating a moderate positive association between L2 motivation and English proficiency. Between-study heterogeneity was moderate ($I^2$ = 52%). Subgroup analysis revealed a descriptively stronger association for integrative motivation (*r* = .38) than for instrumental motivation (*r* = .28), though this difference did not reach statistical significance (*p* = .14).

#### NMA

> Network meta-analysis compared four instructional approaches (CTRL, GLOSS, WRIT, SRS). Against CTRL, SRS showed the largest effect (SMD = 0.60, 95% CI [0.35, 0.85]; P-score = 0.92), followed by WRIT (SMD = 0.48, 95% CI [0.22, 0.74]; P-score = 0.82). Rankogram analysis confirmed the robustness of these rankings: SRS had a 78% probability of being ranked first (SUCRA = 0.91), while WRIT had a 65% probability of being ranked second (SUCRA = 0.80).
>
> Global inconsistency testing (*p* = .21) and node-splitting analyses found no significant violations of the consistency assumption. Direct evidence proportions ranged from 0.45 to 1.00, with no comparison relying exclusively on indirect evidence. The comparison-adjusted funnel plot did not reveal evidence of small-study effects.

*NOTE*: 上記は全要素を含む最大版である。論文のスペース制約に応じて、rankogram / SUCRA / 直接エビデンス割合の詳細は Supplementary Material に回してもよい。

#### ベイズメタ分析（感度分析として）

> As a sensitivity analysis, a Bayesian random-effects model was fitted using weakly informative priors (Normal(0, 1) for the overall effect; Half-Cauchy(0, 0.5) for $\tau$). The posterior mean was *g* = 0.41, 95% CrI [0.17, 0.65], consistent with the frequentist estimate. The posterior probability that the effect is positive was 99.5%, providing strong evidence for a beneficial effect of explicit grammar instruction.

#### 多層モデル

> A three-level random-effects model was fitted to account for the non-independence of *m* = 48 effect sizes nested within *k* = 22 studies. The overall mean effect was *z* = 0.35, 95% CI [0.22, 0.48], corresponding to *r* = .34. Variance decomposition revealed that 62% of the total variance was attributable to between-study differences (Level 3) and 38% to within-study differences (Level 2). Likelihood ratio tests confirmed that both variance components were statistically significant (Level 2: LRT = 12.5, *p* < .001; Level 3: LRT = 8.3, *p* = .004).

#### CHE モデル

> A correlated-and-hierarchical effects (CHE) model was fitted with an assumed within-study correlation of $\rho$ = 0.6 and cluster-robust variance estimation (CR2) to account for the non-independence of multiple effect sizes within studies. The pooled effect was *z* = 0.35, 95% CI [0.21, 0.49], *p* < .001. Sensitivity analyses varying $\rho$ from 0 to 0.9 showed that the point estimate (range: 0.34--0.36) and significance (*p* < .001 for all values) were robust to the assumed within-study correlation.

:::

### 最終チェックリスト

::: {.callout-tip}
#### 論文提出前の確認事項

**基本事項:**

- 効果量の定義（種類と向き）を明示したか？ → 例: "Hedges *g*, 正の値 = 介入群が有利"
- ランダム効果モデルを使用し、その理由を述べたか？ → 例: "研究間の異質性を仮定し REML を使用"
- 効果量・95% CI・$\tau^2$・$I^2$・予測区間・*k* を報告したか？
- フォレストプロットとファンネルプロットを掲載したか？

**出版バイアス:**

- Egger 検定の結果を報告したか？
- 追加手法（Trim-and-fill, PET-PEESE, 選択モデルのうち少なくとも 1 つ）を実施したか？
- Fail-safe N を報告したか？（審査者の要求に応じて）

**異質性の探索:**

- $I^2$ > 50% の場合、サブグループ分析やメタ回帰でモデレーターを検討したか？
- モデレーターの選択根拠と疑似 $R^2$ を報告したか？

**NMA（該当する場合）:**

- ネットワーク連結性（`netconnection()`）を事前に確認したか？
- ネットワーク図を掲載し、各比較の研究数を表示したか？
- リーグテーブル・P-score を報告したか？
- rankogram / SUCRA を掲載し、ランキングの不確実性を示したか？
- 直接エビデンス割合（`dmetar::direct.evidence.plot()`）を確認したか？
- 整合性の検証（グローバル検定 + ノード分割）を行ったか？
- Comparison-adjusted ファンネルプロットを掲載したか？

**ベイズ分析（該当する場合）:**

- 事前分布の選択を明記し、その根拠を述べたか？
- MCMC 診断（R-hat < 1.01, ESS > 400）を確認したか？
- 事後確率（「効果 > 0 の確率」）を報告したか？

**3水準・CHE モデル（該当する場合）:**

- 分散成分の分解（$\sigma^2_{\text{between}}$, $\sigma^2_{\text{within}}$, ICC）を報告したか？
- 尤度比検定により 3 水準モデルの必要性を確認したか？
- CHE モデルを使用した場合、仮定した $\rho$ と感度分析の結果を示したか？
- 頑健分散推定（CR2）の結果を報告したか？

**透明性:**

- PRISMA 2020 フロー図を掲載したか？
- プロトコルの事前登録（PROSPERO 等）について言及したか？
- データとコードの共有（OSF, GitHub 等）を検討したか？
- コーダー間信頼性（$\kappa$, ICC）を報告したか？
:::

## 参考文献

::: {#refs}
:::

## 推薦リソース

**公開データ:**

- 偶発的語彙学習メタ分析: [osf.io/92r3t](https://osf.io/92r3t/)
- @uchihara2019 反復と偶発的語彙学習: [osf.io/rmnk2](https://osf.io/rmnk2)

**ガイドライン:**

- PRISMA 2020: [prisma-statement.org](https://www.prisma-statement.org/) -- 系統的レビューとメタ分析の報告ガイドライン
- [Ten simple rules for carrying out and writing meta-analyses](https://pmc.ncbi.nlm.nih.gov/articles/PMC6521986/)
- [Luke Plonsky のメタ分析文献リスト](https://lukeplonsky.wordpress.com/bibliographies/meta-analysis/) -- SLA分野のメタ分析を網羅

**R パッケージ:**

- [metafor プロジェクト公式サイト](https://www.metafor-project.org/) -- 豊富な例題とFAQ
- [orchaRd 2.0](https://daniel1noble.github.io/orchaRd/) -- Orchard plot の公式ドキュメント
- [metaviz](https://cran.r-project.org/web/packages/metaviz/vignettes/metaviz.html) -- Rain forest plot 等の可視化
- [dmetar](https://dmetar.protectlab.org/) -- メタ分析のユーティリティ関数群
- [forestploter](https://cran.r-project.org/web/packages/forestploter/) -- カスタマイズ可能なフォレストプロット
- [metaSEM](https://cran.r-project.org/web/packages/metaSEM/) -- 構造方程式モデリングによるメタ分析（MASEM 等）
- [clubSandwich](https://cran.r-project.org/web/packages/clubSandwich/) -- 頑健分散推定（CHE モデルで使用）

**発展的手法の参考文献:**

- @assink2016 3水準メタ分析のチュートリアル（ステップバイステップ）
- @cheung2015_book メタ分析の構造方程式モデリングアプローチ（教科書）
- @pustejovsky2022 CHE モデルと頑健分散推定の理論と実践

**効果量の算出・変換:**

- `esc` パッケージ: [CRAN](https://cran.r-project.org/web/packages/esc/) -- 多様な統計量からの効果量変換
- `effectsize` パッケージ: [CRAN](https://cran.r-project.org/web/packages/effectsize/) -- モデルオブジェクトからの効果量算出（easystats エコシステム）
- [Effect Size Calculator (escal.site)](https://www.escal.site/) -- オンライン変換ツール
- [Campbell Collaboration Calculator](https://www.campbellcollaboration.org/calculator/) -- 標準化された計算ツール
