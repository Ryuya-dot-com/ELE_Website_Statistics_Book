---
title: "潜在変数モデリングと次元縮約：PCA・因子分析・SEM・LGM・CA・MDS"
bibliography: references_latent_variable.bib
---

```{r}
#| label: required-packages
#| eval: false

required_packages <- c(
  "FactoMineR",
  "GPArotation",
  "broom",
  "dplyr",
  "factoextra",
  "ggrepel",
  "glue",
  "knitr",
  "lavaan",
  "lavaanPlot",
  "MASS",
  "pacman",
  "patchwork",
  "poLCA",
  "psych",
  "purrr",
  "rlang",
  "scales",
  "semPlot",
  "seminr",
  "sjPlot",
  "smacof",
  "tibble",
  "tidyselect",
  "tidyverse"
)

missing_packages <- setdiff(required_packages, rownames(installed.packages()))
if (length(missing_packages) > 0) {
  install.packages(missing_packages, repos = "https://cloud.r-project.org")
}
```


## はじめに

**本章へようこそ**。本章では、英語教育研究で頻出する「動機づけ」「言語適性」「コミュニケーション能力」など**直接観察できない構成概念 (latent construct)** を、観測変数から統計的に推論する **潜在変数モデリング** と **次元縮約** の 8 手法 — **PCA (主成分分析)**・**EFA (探索的因子分析)**・**CFA (確認的因子分析)**・**SEM (構造方程式モデリング)**・**LGM (潜在成長曲線モデル)**・**LCA (潜在クラス分析)**・**CA/MCA (コレスポンデンス分析)**・**MDS (多次元尺度法)** を学びます (CA と MCA は同族手法のため 7 系統と数えることもあります)。

本章は第 11/12 章 IRT との双対性 (CFA ⇔ IRT)、第 7 章ベイズ推定 (`blavaan`)、第 10 章 G theory (分散成分分解との対比) と密接に接続する本書の測定理論パートの中核章です。本章を読み終えると、`psych::fa()`・`lavaan::cfa/sem/growth`・`smacof::mds` などを使って、自分のアンケート / テストデータから尺度妥当性を検証し、構造モデルで理論仮説を検証できるようになります。

英語教育研究では、直接測定できない概念（構成概念: construct / latent construct）を扱う場面が頻繁にあります。たとえば「動機づけ」「言語適性」「コミュニケーション能力」「読解力」はいずれも直接観察できず、複数の質問紙項目やテスト項目を通じて間接的に測定されます。@dornyei2005 は第二言語習得における個人差変数の心理学的基盤を包括的にレビューし、これらの構成概念が複数の下位次元から構成されることを示しました。こうした**観測変数の背後にある構造**を統計的に明らかにするのが、本章で扱う潜在変数モデリングと次元縮約の手法群です[@kline2023; @brown2015]。

::: {.callout-tip}
**「構成概念」とは何か — 動機づけ尺度を例に**

学習者の動機づけを測りたいとき、「あなたは英語学習に動機づけられていますか?」と1問だけ尋ねても、その回答は気分や解釈の幅に左右され、測定誤差が大きくなります。そこで通常は「英語を流暢に使える自分を想像できる」「英語を学ぶことは楽しい」「先生に良い評価をされたい」など 6〜18 項目の Likert 尺度を用います。

ここで重要なのは、**6項目の合計点 ≠ 動機づけそのもの** という点です。合計点はあくまで観測値で、その背後に「動機づけ」という観測できない**構成概念（latent construct）**が存在し、6項目はそれを反映する**観測変数（observed indicators）**だと考えるのが潜在変数モデリングの根本前提です。Cronbach $\alpha$（第1章で習った内的整合性指標）は「6項目が同じ構成概念を測っているか」の一側面を評価する指標ですが、後述する $\omega$ や CFA はこの「構成概念 → 観測変数」の生成過程をより明示的にモデル化します。

本章を通じて、「目に見える数字（観測変数）」と「目に見えない量（潜在変数）」を区別し、両者の関係を統計モデルで結ぶ視点を身につけてください。
:::

本章では 8 つの手法を体系的に解説します。**主成分分析（principal component analysis: PCA）** と **探索的因子分析（exploratory factor analysis: EFA）** で構造の探索と次元縮約を行い、**確認的因子分析（confirmatory factor analysis: CFA）** と **構造方程式モデリング（structural equation modeling: SEM）** で理論に基づく仮説検証を行います。縦断データには **潜在成長曲線モデル（latent growth curve model: LGM）**、潜在クラス分類には **潜在クラス分析（latent class analysis: LCA）** を扱います。さらに、カテゴリカルデータの関係性を可視化する **コレスポンデンス分析（correspondence analysis / multiple correspondence analysis: CA/MCA）** と、距離・類似度データを低次元空間に写像する **多次元尺度法（multidimensional scaling: MDS）** を取り上げます。

すべてのデータ例は英語教育研究の文脈に基づくシミュレーションデータを用いるため、分析結果の解釈がそのまま研究実践に接続する設計になっています。

### 章の読み方

#### Step 1: 問題を1文で捉える

「観測データの背後にある構造（次元・因子・関係パターン）を少数の成分で要約し、理論の検証や尺度の妥当性確認に使う。」

#### Step 2: 理解する区別は1つだけ

- **PCA / CA / MDS** = データを「要約・可視化」する記述的手法
- **EFA / CFA / SEM / LGM** = 潜在構造を「推定・検証」する確率モデル

#### Step 3: この章でやること

1. PCA で次元縮約の基本を理解する
2. EFA で潜在因子を探索し、CFA / SEM で仮説を検証する
3. LGM で個人の成長軌跡をモデリングする（SEM × 縦断データ）
4. CA / MDS でカテゴリカルデータ・距離データを可視化する

#### Step 4: どこまで読めば実務に使えるか

- **尺度開発**: EFA → CFA → 信頼性 で研究に使える
- **構造検証**: 上記 + SEM 節まで
- **縦断的成長**: SEM + LGM 節（語彙力・読解力の成長、視線計測データ）
- **カテゴリデータ**: CA / MCA 節
- **類似度可視化**: MDS 節

#### 節の依存関係

| 範囲 | カバーする内容 |
|------|----------------|
| **基礎** | はじめに → PCA → EFA → 手法比較ガイド → 報告テンプレート |
| **標準** | 上記 + CFA → SEM → PCA vs EFA 比較 |
| **縦断** | 上記 + LGM（語彙成長 → 視線計測 → 条件付きモデル） |
| **発展** | 上記 + CA / MCA → MDS → WLSMV → 測定不変性 → ブートストラップ |

#### 最初に押さえる判断

| 研究上の問い | まず使う手法 | 次に確認すること | よくある誤解 |
|:--|:--|:--|:--|
| 多数の観測変数を少数の軸に要約したい | PCA | 寄与率、スクリープロット、バイプロットの $\cos^2$ | PCA の主成分をそのまま潜在構成概念と呼ぶ |
| 質問紙項目の背後にある因子を探索したい | EFA | 平行分析、斜交回転、交差負荷、共通性 | Kaiser 基準だけで因子数を決める |
| 理論上の測定構造が妥当か確認したい | CFA | CFI/TLI/RMSEA/SRMR、負荷量、残差相関 | EFA と同じデータで CFA も済ませる |
| 潜在変数同士の関係を検証したい | SEM | 測定モデルの妥当性、構造パス、媒介効果 | SEM が因果を自動的に証明すると考える |
| 縦断データの平均成長と個人差を見たい | LGM | 切片・傾きの平均と分散、時点数、欠測処理 | 3時点で複雑な曲線成長を推定する |
| カテゴリ間の対応関係を見たい | CA / MCA | カイ二乗、慣性、寄与率、$\cos^2$ | 行点と列点の距離をそのまま関連の強さと読む |
| 類似度・距離を地図化したい | MDS | Stress-1、Shepard 図、局所残差 | 軸の向きや正負に本質的意味を与える |

: 潜在変数モデリングと次元縮約の初期判断

### 手法選択ガイド

手法選択は、まず研究の問いに対してデータの種類を確認するところから始まります。以下のフローチャートで判断の流れを概観できます。

```{mermaid}
%%| fig-width: 9
%%| label: fig-method-selection
%%| fig-cap: "データの種類と研究目的に基づく手法選択フローチャート"
flowchart TD
    A["データの種類は？"] --> B["連続 / Likert"]
    A --> C["カテゴリカル"]
    A --> D["距離・類似度行列"]

    B --> E{"目的は？"}
    E --> F["次元縮約・可視化 → PCA"]
    E --> G["潜在構造の探索 → EFA"]
    E --> H["測定仮説の検証 → CFA"]
    E --> I["理論パス・媒介の検証 → SEM"]
    E --> J["個人の成長軌跡 → LGM"]

    G --> |"因子構造確定後"| H
    H --> |"構造パス追加"| I
    I --> |"縦断データあり"| J

    C --> K{"入力形式は？"}
    K --> L["クロス集計表 → CA"]
    K --> M["個票データ → MCA"]

    D --> N["MDS"]

    style F fill:#e8f4f8,stroke:#2980b9
    style G fill:#e8f4f8,stroke:#2980b9
    style H fill:#fef9e7,stroke:#f39c12
    style I fill:#fef9e7,stroke:#f39c12
    style J fill:#fef9e7,stroke:#f39c12
    style L fill:#f5eef8,stroke:#8e44ad
    style M fill:#f5eef8,stroke:#8e44ad
    style N fill:#eafaf1,stroke:#27ae60
```

連続変数や Likert データなら、目的に応じて PCA（次元縮約）、EFA（探索）、CFA（確認）、SEM（構造検証）、LGM（成長分析）を選びます。カテゴリカルデータなら、クロス集計表が中心であれば CA、個票データかつ多変数であれば MCA が適しています。距離・類似度行列が与えられている場合は MDS を使います。実務上は EFA で構造を探索してから CFA に進み、さらに構造パスを加えて SEM、縦断データなら LGM へ展開する順序が基本です。

#### 本書全体での位置づけ — 潜在変数の家系図

本章で扱う手法群は、本書の他の章と数学的・概念的に深く接続します。下記の「家系図」は、観測変数と潜在変数の関係をどう仮定するか、どんな推定原理を使うか、という観点から本書全体を俯瞰したものです。

```{mermaid}
%%| fig-width: 10
%%| label: fig-latent-family-tree
%%| fig-cap: "潜在変数モデリング・次元縮約の本書全体での位置づけ"
flowchart TD
    Root["潜在構造を扱う統計モデル"]

    Root --> Cont["連続潜在変数<br/>(continuous latent)"]
    Root --> Cat["カテゴリ潜在変数<br/>(categorical latent)"]
    Root --> Geom["幾何的縮約<br/>(geometric reduction)"]

    Cont --> EFA_CFA["EFA / CFA<br/>第9章"]
    Cont --> SEM["SEM<br/>第9章"]
    Cont --> LGM_node["LGM<br/>第9章"]
    Cont --> IRT["IRT / Rasch<br/>第11/12章"]
    Cont --> GLMM["混合効果モデル<br/>第6章"]

    Cat --> LCA_node["LCA / LPA / GMM<br/>第9章"]
    Cat --> Cluster["クラスタリング<br/>第19章"]

    Geom --> PCA_node["PCA<br/>第9章"]
    Geom --> CA_MCA["CA / MCA<br/>第9章"]
    Geom --> MDS_node["MDS<br/>第9章"]

    EFA_CFA -.->|"双対性<br/>λᵢ = aᵢ/√(1+aᵢ²)"| IRT
    LGM_node -.->|"代数的同値<br/>(Bauer 2003)"| GLMM
    SEM -.->|"ベイズ拡張<br/>blavaan"| Bayes["ベイズ推定<br/>第7章"]
    EFA_CFA -.->|"分散成分の対比"| GTheory["G theory<br/>第10章"]
    SEM -.->|"効果量統合"| Meta["メタ分析<br/>第17章"]
    SEM -.->|"反事実モデル<br/>do-演算子"| Causal["因果推論<br/>第18章"]

    style EFA_CFA fill:#fef9e7,stroke:#f39c12
    style SEM fill:#fef9e7,stroke:#f39c12
    style LGM_node fill:#fef9e7,stroke:#f39c12
    style PCA_node fill:#e8f4f8,stroke:#2980b9
    style CA_MCA fill:#f5eef8,stroke:#8e44ad
    style MDS_node fill:#eafaf1,stroke:#27ae60
    style LCA_node fill:#fdebd0,stroke:#e67e22
    style IRT fill:#d6eaf8,stroke:#1f618d
    style GLMM fill:#d6eaf8,stroke:#1f618d
    style Bayes fill:#d6eaf8,stroke:#1f618d
    style GTheory fill:#d6eaf8,stroke:#1f618d
    style Meta fill:#d6eaf8,stroke:#1f618d
    style Causal fill:#d6eaf8,stroke:#1f618d
    style Cluster fill:#d6eaf8,stroke:#1f618d
```

実線は本章内の手法分類、点線は他章との数学的・概念的橋渡しを表します。特に重要なのが次の双対性です:

- **CFA ↔ IRT**（第 11/12 章）: 順序カテゴリ CFA の **正規 ogive 表現** と IRT 2PL モデルは数学的に同値で、**標準化負荷 $\lambda_j$ かつ潜在因子分散 1** の前提下で $a_j = \lambda_j/\sqrt{1-\lambda_j^2}$ により相互変換可能です（Takane & de Leeuw 1987）。**ロジスティック 2PL** に変換する場合は、scaling constant $D \approx 1.702$ を介して $a_j^{\mathrm{logit}} = D \cdot a_j^{\mathrm{probit}}$ となります。CFA の連続性と IRT の項目特性を同じ枠組みで扱える視点は、テスト開発で強力な武器になります
- **LGM ↔ 混合効果モデル**（第 6 章）: `growth()` と `lmer(... + (time | id))` は同一の固定効果・分散成分を返します。SEM の柔軟性（測定モデル付き縦断分析）と lme4 のシンプルさ（時間変動共変量・交差ランダム効果）を、目的に応じて使い分けます
- **EFA ↔ G theory**（第 10 章）: いずれも観測スコアを「真値 + 誤差」に分解しますが、G theory は分散成分（rater × item × occasion）を ANOVA 的に分解、EFA は共通因子と独自因子を共分散ベースで分離します
- **SEM ↔ ベイズ推定**（第 7 章）: `blavaan` は同じモデル構文でベイズ推定を提供し、small N や複雑な階層構造での収束改善、事前分布による正則化が可能になります
- **SEM ↔ 因果推論**（第 18 章）: パス図を Pearl の DAG として読み替え、do-演算子による介入効果との対応を考えると、SEM の方向性パスが因果として何を主張しているかが明確になります

#### 用語ミニ辞典

**基本概念**

| 用語 | 英語 | 定義 |
|---|---|---|
| 潜在変数 | latent variable | 直接測定できない構成概念（動機づけ、能力等）。複数の観測変数から推定する |
| 観測変数 | observed / indicator variable | 実際に測定できるデータ（質問紙の回答、テスト得点等） |
| 反映指標 | reflective indicator | 潜在変数が観測変数を**生成**するモデル（因子→項目）。尺度開発の標準 |
| 形成指標 | formative indicator | 観測変数が潜在変数を**構成**するモデル（項目→合成変数）。SES 等に適用 |

**EFA / PCA 関連**

| 用語 | 英語 | 定義 |
|---|---|---|
| 因子負荷量 | factor loading | 潜在因子が観測変数に与える影響の大きさ。絶対値 > .40 が実質的 |
| 共通性 | communality ($h^2$) | 因子で説明される観測変数の分散割合。$h^2 < .30$ は除外候補 |
| 独自性 | uniqueness ($u^2$) | 因子で説明されない分散（$1 - h^2$）。測定誤差＋項目固有の分散 |
| 固有値 | eigenvalue | 各成分 / 因子が説明する分散量。平行分析で成分数・因子数の候補判断に用いる |
| 観測相関行列 | observed correlation matrix ($\mathbf{R}$) | データから計算した項目間相関。EFA/PCA の出発点 |
| 観測共分散行列 | observed covariance matrix ($\mathbf{S}$) | データから計算した項目間共分散。CFA/SEM の適合度評価の基礎 |
| 含意相関行列 | model-implied correlation matrix ($\hat{\mathbf{R}}$) | 因子モデルが予測する項目間相関。観測相関との差が残差相関 |
| ガットマン基準 / Kaiser-Guttman 基準 | Guttman criterion / Kaiser-Guttman criterion | 固有値が 1 を超える成分・因子を保持する経験則。単独採用は避ける |
| 回転 | rotation | 因子の解釈可能性を高めるための変換 |
| 斜交回転 | oblique rotation | 因子間相関を許容する回転（promax, oblimin）。心理・教育データの標準 |
| 直交回転 | orthogonal rotation | 因子間を独立に保つ回転（varimax）。因子が無相関の場合のみ |
| 交差負荷 | cross-loading | 1つの項目が複数因子に .30 以上の負荷を持つこと。項目修正の対象 |

**CFA / SEM 関連**

| 用語 | 英語 | 定義 |
|---|---|---|
| 測定モデル | measurement model | 潜在変数と観測変数の対応関係を定義するモデル部分（CFA に相当） |
| 構造モデル | structural model | 潜在変数間の回帰関係を定義するモデル部分 |
| パス係数 | path coefficient | 変数間の回帰関係の強さ。標準化値（$\beta$）で報告するのが一般的 |
| 標準化解 | standardized solution | 全変数を分散1に標準化した推定値。`std.all` 列で確認する |
| 含意共分散行列 | model-implied covariance matrix ($\boldsymbol{\Sigma}(\boldsymbol{\theta})$) | モデルが正しいと仮定した場合に予測される変数間の共分散 |
| 直接効果 | direct effect | ある変数から別の変数への媒介なしの効果 |
| 間接効果 | indirect effect | 媒介変数を経由する効果（パス係数の積で算出） |
| 総効果 | total effect | 直接効果 + 間接効果 |
| 修正指標 | modification indices (MI) | 制約を解放した場合の $\chi^2$ 改善の期待値。大きい MI は候補だが、EPC・理論・再現性と併せて判断する |
| Heywood ケース | Heywood case | 分散の推定値が負、または標準化負荷 > 1 になる異常推定 |

**適合度指標**

| 略称 | 正式名称 | 直感的意味 | 基準値 |
|------|---------|-----------|--------|
| $\chi^2$ | カイ二乗検定 | モデルの完全適合を検定。大標本で有意になりやすいため単独で判断しない | $p > .05$（参考） |
| CFI | Comparative Fit Index | **ベースラインモデル（全変数無相関）からの改善度**。1.0 = 完全改善 | > .90 許容, > .95 良好 |
| TLI | Tucker-Lewis Index | CFI と同様だが**自由度でペナルティ**を与える。複雑モデルに厳しい | > .90 許容, > .95 良好 |
| RMSEA | Root Mean Square Error of Approximation | **自由度あたりの近似誤差**。モデルの複雑さを考慮した「misfit per df」 | < .08 許容, < .05 良好 |
| SRMR | Standardized Root Mean Square Residual | **観測相関と予測相関の平均的なズレ**。残差の大きさを直接反映 | < .08 良好 |

::: {.callout-warning}
**Hu-Bentler (1999) カットオフの文脈と機械的運用への警告**

上表のカットオフ（CFI > .95、RMSEA < .06、SRMR < .08）は @hu1999 のシミュレーション研究に由来します。**ただし**、Hu-Bentler の研究は「最大尤度推定 + 連続データ + 中規模 N + 標準的 CFA モデル」という限定条件下での結果であり、以下の状況では機械的に当てはまりません:

- **順序データ（Likert）+ WLSMV 推定**: 適合度の挙動が異なり、@savalei2024 はカットオフを緩める必要性を示唆
- **小サンプル ($N < 200$)**: 適合度指標自体が不安定
- **単純構造モデル**: McNeish & Wolf (2023) の **dynamic fit index cutoffs** は、特定モデル構造に応じてカットオフを再校正することを推奨。`dynamic` パッケージで実装可能

本章のカットオフは「**目安**」として用い、(a) 複数指標を併用、(b) 残差相関と修正指標も確認、(c) 理論的妥当性とのバランスを判断、という総合的アプローチを取ってください。「.95 を 0.949 で下回ったから棄却」のような硬直的判断は避けます。
:::

**推定量**

| 推定量 | 正式名称 | 使用場面 |
|--------|---------|---------|
| ML | Maximum Likelihood | 連続変数・正規性仮定を満たす場合の標準 |
| MLR | Robust ML | 非正規データ。ロバスト標準誤差とスケール補正 $\chi^2$ を提供 |
| WLSMV | Weighted Least Squares Mean and Variance adjusted | 順序カテゴリ（5件法以下の Likert 尺度）。ポリコリック相関を使用 |
| FIML | Full Information Maximum Likelihood | 欠損データ。MAR 仮定の下で不偏推定を提供 |

**妥当性指標**

| 用語 | 英語 | 定義 | 基準値 |
|------|------|------|--------|
| 収束的妥当性 | convergent validity | 同一因子の指標が十分に高い負荷を持つか | AVE > .50 |
| 弁別的妥当性 | discriminant validity | 異なる因子が十分に区別できるか | 因子間相関 < .85, Fornell-Larcker 基準 |
| AVE | Average Variance Extracted | 因子が指標の分散を平均的にどれだけ説明するか | > .50 |

**LGM / 縦断分析**

| 用語 | 英語 | 定義 |
|---|---|---|
| 潜在成長曲線 | latent growth curve | 個人の変化を切片・傾きの潜在因子でモデリングする手法 |
| 切片因子 | intercept factor | LGM における初期水準の個人差を表す潜在変数 |
| 傾き因子 | slope factor | LGM における成長率の個人差を表す潜在変数 |

**CA / MCA / MDS**

| 用語 | 英語 | 定義 |
|---|---|---|
| 慣性 | inertia | CA における分散に相当する量（$\chi^2 / n$）。大きいほど行列間の関連が強い |
| ストレス | stress | MDS における距離再現の誤差。低次元での「歪みの大きさ」 |
| 注視時間 | gaze duration | 視線計測で関心領域を初回通過する際の総注視時間 |
| 測定不変性 | measurement invariance | 群間で測定モデルが同等に機能するかの検証 |

### この章の到達目標

- PCA の固有値分解を理解し、スクリープロットとバイプロットを解釈できます。
- EFA で因子数を決定し、因子負荷量表を作成・解釈できます。
- CFA のモデル定義と適合度評価ができます（lavaan の基本操作を含みます）。
- SEM で潜在変数間の媒介効果を推定し、ブートストラップ CI を報告できます。
- LGM で個人の成長軌跡をモデリングし、共変量の効果を検証できます。
- LCA でカテゴリカル指標から学習者プロフィールを推定できます。
- CA / MCA でカテゴリカルデータの関係構造を可視化できます。
- MDS で距離データを低次元空間に写像し、ストレスで適合度を評価できます。
- 各手法の結果を APA 形式で報告できます。

### この章の立場
- 本章は「尺度開発・構成概念の検証・縦断的成長・データの要約」に焦点を絞ります。ベイズ SEM やネットワーク分析は別章で扱います。ARIMA 等の集団レベルの時系列分析は第13章を参照してください。
- 目的は「多機能なコード集」ではなく、**研究で使える分析思考**を身につけることです。

### 主なパッケージの役割

| パッケージ | 担当範囲 | 本章での主な使用箇所 | 代替/補完パッケージ |
|------------|----------|---------------------|---------------------|
| `psych` | EFA、信頼性係数（$\alpha$, $\omega$）、平行分析、記述統計 | 全 EFA 節、$\omega$ 計算 | `EFAtools`（高機能 EFA）、`semTools::compRelSEM` |
| `GPArotation` | 因子回転（promax/oblimin/geomin/bifactor 等） | `psych::fa(rotate=...)` の裏で自動呼び出し | — |
| `lavaan` | CFA・SEM・LGM の最尤推定 | CFA/SEM/LGM 節 | `OpenMx`（柔軟）、`brms`（ベイズ） |
| `semPlot` | パス図（静的、出版品質） | CFA/SEM 結果可視化 | `lavaanPlot`（HTML 動的）、`tidySEM` |
| `lavaanPlot` | DiagrammeR ベースの HTML パス図 | （オプション、本章は `semPaths` 主体） | `tidySEM`（ggplot ベース）、`semPlot` |
| `seminr` | PLS-SEM | CB-SEM vs PLS-SEM 比較節 | `cSEM`、SmartPLS（GUI） |
| `poLCA` | LCA（カテゴリカル指標） | LCA 節 | `tidyLPA`（連続指標）、`mclust` |
| `FactoMineR` | PCA・CA・MCA | PCA/CA/MCA 節 | `prcomp`（基本 PCA）、`ca` パッケージ |
| `factoextra` | `FactoMineR` の ggplot 可視化 | バイプロット、寄与プロット | — |
| `smacof` | MDS（SMACOF アルゴリズム、計量・非計量） | MDS 節 | `MASS::isoMDS`、`vegan::monoMDS` |
| `MASS` | `mvrnorm`（多変量正規）, `isoMDS` | データ生成、代替 MDS | — |
| `blavaan` | ベイズ SEM/CFA/LGM (Stan/JAGS バックエンド) | 第 7 章で別途扱う | `brms`、`rstan` 直書き |

`tidyverse` / `patchwork` / `gt` / `broom` / `glue` / `scales` / `ggrepel`: 図表の整形、ラベル衝突の回避、出力の整理、報告文生成を補助します。

::: {.callout-note}
**`blavaan` の位置づけ**: 本章のサンプルコードは頻度論的な `lavaan` を主に使いますが、ベイズ版の `blavaan` [@merkle2018blavaan] は同じモデル構文（`bcfa()` / `bsem()` / `bgrowth()`）でベイズ推定が可能です。Heywood ケース対処、small N、複雑な階層モデルでの収束改善に有効で、本書では第 7 章「ベイズ推定」で詳細を扱います。
:::

```{r}
#| eval: true
#| label: setup
#| message: false
#| warning: false

if (!requireNamespace("pacman", quietly = TRUE)) install.packages("pacman", repos = "https://cloud.r-project.org")

# IMPORTANT: Load MASS BEFORE tidyverse so that dplyr::select() takes precedence
# over MASS::select() for unqualified calls. We still recommend using
# dplyr::select(...) explicitly throughout for clarity.
pacman::p_load(
  MASS,           # mvrnorm, isoMDS — load first to avoid dplyr::select masking
  tidyverse,      # masks MASS::select with dplyr::select
  psych,
  GPArotation,
  sjPlot,
  lavaan,
  semPlot,
  lavaanPlot,
  seminr,
  FactoMineR,
  factoextra,
  smacof,
  poLCA,
  patchwork,
  scales,
  ggrepel,
  broom,
  glue
)
set.seed(2024)
theme_set(theme_bw(base_size = 12, base_family = "HiraginoSans-W3") +
  theme(
    plot.title.position = "plot",
    plot.title = element_text(
      face = "bold",
      size = rel(1.0),
      lineheight = 1.04,
      margin = margin(b = 6)
    ),
    plot.subtitle = element_text(
      size = rel(0.92),
      lineheight = 1.04,
      margin = margin(b = 8)
    ),
    plot.margin = margin(t = 10, r = 14, b = 10, l = 10)
  ))
theme_update(legend.position = "bottom")
```

### 因子分析・SEM の前提条件と前処理

因子分析（EFA/CFA）および SEM を適用する前に、データが分析の前提条件を満たしているかを確認する必要があります。前提の侵害は推定の偏りや不安定な結果を引き起こすため、分析に先立って以下の項目を体系的に検証します。

#### データの種類と相関係数の選択

因子分析の入力となる相関係数は、データの測定水準に応じて使い分ける必要があります。特に Likert 項目は「数字で記録されている」だけで、必ずしも連続変数として扱えるとは限りません。

| データの種類 | 相関係数 | R での指定 | 備考 |
|-------------|---------|-----------|------|
| 連続変数 | Pearson 相関 | `cor(data)` | ML / MLR 推定と相性が良い |
| 順序カテゴリ（Likert 5件法以下） | **ポリコリック相関** | `psych::polychoric(data)$rho` | 背後に連続な正規分布を仮定します[@holgadotello2010] |
| 2値データ（正誤, Yes/No） | **テトラコリック相関** | `psych::tetrachoric(data)$rho` | ポリコリック相関の2値特殊ケースです |
| 6件法以上の Likert | Pearson 相関またはポリコリック相関 | `cor(data)` / `psych::polychoric()` | 分布が歪む場合は MLR または WLSMV を検討します |

ポリコリック相関とテトラコリック相関は、観測される離散的な反応の背後に**連続的な正規分布**（閾値モデル）を仮定します。例えば、5件法の Likert 項目で「4」と回答した場合、背後の連続的な態度が閾値 $\tau_3$ と $\tau_4$ の間にあると解釈します。CFA/SEM で順序カテゴリ指標を使う場合は、`lavaan::cfa(..., ordered = item_names, estimator = "WLSMV")` のように指定すると、lavaan が閾値モデルに基づいて推定します。

::: {.callout-note}
#### 初心者向けの実務判断

5件法以下の Likert 項目をそのまま `cor()` と ML 推定で扱うと、標準誤差や適合度指標が楽観的になることがあります。最初は Pearson/MLR と polychoric/WLSMV の両方を試し、因子数・主負荷・結論が大きく変わるかを確認すると安全です。結果が変わる場合は、カテゴリ分布、閾値の安定性、推定収束、サンプルサイズ、感度分析を踏まえて主分析を決めます。
:::

#### サンプルサイズ

| 手法 | 最低ライン | 推奨 | 根拠 |
|------|-----------|------|------|
| EFA | 項目数 $\times$ 5（$N:p = 5:1$） | 項目数 $\times$ 10 または $N \geq 300$ | @costello2005; @goretzko2021 |
| CFA | $N \geq 200$ | $N \geq 300$ | @kline2023; @brown2015 |
| SEM（媒介モデル等） | $N \geq 200$ | $N \geq 400$（5潜在変数以上） | @kline2023 |
| PLS-SEM | 事前の検出力分析 | 最小効果量・最大入次数・測定品質に基づく | @hair2019 |

サンプルサイズの目安は、絶対的な規則ではなく、項目数、共通性、因子負荷量、因子数、欠測率によって変わります。一般に、共通性が低い項目や交差負荷が多い項目があるほど、より大きなサンプルが必要です。SLA 研究ではサンプルの確保が困難な場合も多いため、$N < 150$ の EFA は「探索的・暫定的」と位置づけ、平行分析、解の安定性、ブートストラップ、別サンプルでの再現性確認を併用してください[@plonsky2015; @goretzko2021]。

#### 多変量正規性

ML（最尤法）推定は多変量正規性を仮定します。心理・教育データ（特に Likert 尺度）では厳密な正規性は満たされないことが多いですが、**MLR（ロバスト ML）**を使用すれば、中程度の非正規性には比較的頑健な推定が得られます。深刻な非正規性（歪度 > 2、尖度 > 7）がある場合や、カテゴリ数が少ない順序尺度の場合は WLSMV を検討します。

```{r}
#| label: mardia-normality-test
#| eval: false

# Multivariate normality check — run on your actual data
mardia_result <- psych::mardia(your_data, plot = FALSE)

# Key outputs:
# mardia_result$b1p    — multivariate skewness
# mardia_result$b2p    — multivariate kurtosis
# mardia_result$p.skew — p-value for skewness
# mardia_result$p.kurt — p-value for kurtosis
```

Mardia 検定が有意（$p < .05$）の場合、多変量正規性の仮定が侵害されている可能性があります。この場合、(a) ML の代わりに **MLR** を使用する、(b) カテゴリ数が少なければ **WLSMV** を使用する、(c) **ブートストラップ**で頑健な標準誤差と信頼区間を算出する、のいずれかを検討します。実データでは EFA/CFA の前に確認してください。本章の EFA セクションでは、シミュレーションデータの正規性を KMO/Bartlett 検定とともに確認します。

#### 線形性

EFA/CFA の因子モデルは、潜在変数と観測変数の間に**線形関係**を仮定します。非線形な関係（天井効果、床効果、U 字型の関係など）がある場合、因子負荷量が過小推定されることがあります。線形性の確認には、項目間の散布図行列や残差プロットを視覚的に検査する方法が実用的です。天井効果や床効果が顕著な項目は、分析前に除外・カテゴリ統合・項目修正を検討します。

#### 外れ値

多変量外れ値は因子構造を歪め、特定の因子が外れ値の影響だけで形成される「アーティファクト因子」を生む危険があります。マハラノビス距離を用いて多変量外れ値を検出し、$p < .001$（カイ二乗分布に基づく）のケースを確認します。外れ値の除外は慎重に行い、統計的基準だけでなく実質的な理由（データ入力ミス、不誠実な回答など）がある場合のみ除外します。

#### 欠損データ

SLA 研究では脱落や無回答により欠損データが生じることが多くあります。欠損のメカニズムには3種類があります。

| メカニズム | 定義 | 対処法 |
|-----------|------|--------|
| **MCAR** (Missing Completely at Random) | 欠損が完全にランダム | リストワイズ削除でも大きな偏りは出にくいが非効率 |
| **MAR** (Missing at Random) | 欠損が他の観測変数に依存 | **FIML** または多重代入（`mice`）が有効 |
| **MNAR** (Missing Not at Random) | 欠損が欠損値自体に依存 | 特別な選択モデルが必要。FIML でも偏る |

lavaan では、連続指標を ML/MLR で推定する場合に `missing = "fiml"` を指定すると FIML を利用できます。FIML は欠測値を埋める方法ではなく、観測されている情報を尤度に直接反映する方法です。MAR が妥当な場合、リストワイズ削除よりも効率的で偏りの少ない推定を期待できます。順序カテゴリ指標を WLSMV で扱う場合は、FIML と同じ扱いにはならないため、多重代入や感度分析も検討してください。

#### 前処理チェックリスト（分析前に確認）
1. サンプルサイズは十分か？（EFA: $N \geq 200$, CFA/SEM: $N \geq 200$--300）
2. KMO > .60、Bartlett 検定有意か？（EFA のみ）
3. データの測定水準に応じた相関係数を選択したか？（Pearson / ポリコリック / テトラコリック）
4. Mardia 検定で多変量正規性を確認したか？→ 侵害されていれば MLR または WLSMV
5. 天井・床効果のある項目はないか？
6. 多変量外れ値（マハラノビス距離）を確認したか？
7. 欠損の割合と FIML の適用を検討したか？

## 回帰分析からの橋渡し

読者がすでに馴染んでいる回帰分析から出発します。@bandalos2018 が測定理論の教科書で整理しているように、社会科学における統計手法は「観測変数間の関係」から「潜在構造の推定」へと段階的に拡張されます。回帰分析では、1つの結果変数を複数の説明変数で予測します。

$$Y = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \cdots + \beta_p X_p + \varepsilon$$

ここで $Y$ は観測される結果変数、$X$ は観測される説明変数です。しかし、英語教育研究では「動機づけ」「不安」「読解力」といった**直接測定できない構成概念**を扱う場面が多くあります。このとき、モデルの方向が逆転します。回帰では「$X$ から $Y$ を予測する」のに対し、因子分析では「潜在変数 $f$ が観測変数 $X$ を生成する」と考えます。

$$\underbrace{X = \Lambda f + u}_{\text{因子分析モデル}}$$

ここで $\Lambda$ は因子負荷行列、$f$ は潜在因子、$u$ は独自性（誤差）です。この「方向の逆転」が、回帰分析と潜在変数モデルの根本的な違いです。

### 橋渡しの直感
- **回帰分析**: 観測変数 → 観測変数（予測）
- **PCA**: 観測変数 → 少数の合成変数（要約）
- **因子分析**: 潜在変数 → 観測変数（生成モデル）

相関行列を計算する時点で、すでに PCA と EFA の入力データは手元にあります。`cor(data)` が出発点です。

以下の小さな例で、相関行列の「ブロック構造」が潜在因子の存在を示唆する様子を確認します。

```{r}
#| eval: true
#| label: bridge-correlation-demo
#| fig-width: 7
#| fig-height: 5

set.seed(2024)
n <- 200
skill_read  <- rnorm(n)
skill_listen <- rnorm(n, mean = 0.3 * skill_read)  # Moderate correlation

bridge_data <- data.frame(
  Read_1  = skill_read  + rnorm(n, sd = 0.6),
  Read_2  = skill_read  + rnorm(n, sd = 0.6),
  Read_3  = skill_read  + rnorm(n, sd = 0.6),
  List_1  = skill_listen + rnorm(n, sd = 0.6),
  List_2  = skill_listen + rnorm(n, sd = 0.6),
  List_3  = skill_listen + rnorm(n, sd = 0.6)
)

# Correlation matrix with block structure
cor_mat <- cor(bridge_data)

# Visualize
cor_long <- cor_mat |>
  as.data.frame() |>
  rownames_to_column("Var1") |>
  pivot_longer(-Var1, names_to = "Var2", values_to = "r") |>
  mutate(
    Var1 = factor(Var1, levels = rev(colnames(cor_mat))),
    Var2 = factor(Var2, levels = colnames(cor_mat))
  )

ggplot(cor_long, aes(Var2, Var1, fill = r)) +
  geom_tile() +
  geom_text(aes(label = round(r, 2)), size = 3.5) +
  scale_fill_gradient2(low = "#2166ac", mid = "white", high = "#b2182b", midpoint = 0) +
  labs(
    title = "Correlation Matrix: 6-Item Vocabulary Test",
    subtitle = "Block structure suggests two latent factors (Reading vs. Listening)",
    fill = "r"
  ) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

相関行列に明確なブロック構造が見えます。Reading 項目同士の相関が高く、Listening 項目同士の相関も高いです。一方、Reading と Listening の間の相関は相対的に低くなっています。この構造が、潜在因子（Reading 能力、Listening 能力）の存在を示唆しています。

**回帰分析・PCA・因子分析の比較**（モデルの方向と目的の違いに注目）

| 観点 | 回帰分析 | PCA | 因子分析 |
|------|---------|-----|---------|
| 目的 | 予測・因果推論 | 次元縮約・可視化 | 潜在構造の推定 |
| モデル方向 | X → Y | X → PC | f → X |
| 推定対象 | 回帰係数 β | 固有ベクトル | 因子負荷 Λ |
| 誤差の扱い | 残差 ε | 含まない | 独自性 u |
| 潜在変数 | なし | なし（合成変数） | あり |
| 典型的な用途（SLA） | テスト得点の予測 | テスト項目の要約 | 尺度開発・妥当性検証 |

### よくある誤解
「PCA は因子分析の一種である」と誤解されることがありますが、両者は目的もモデルも異なります。PCA は全分散の要約であり、因子分析は共通分散の推定です。この違いは後述の「PCA vs EFA の比較」節で詳しく扱います。

## 潜在構造の探索と検証

### 主成分分析（PCA）

::: {.callout-tip}
**1 分プレビュー (PCA)**

「12 個のテスト項目を 2〜3 個の合成スコアに要約してデータの全体像を眺めたい」という時に使う **幾何学的な次元縮約手法** です。相関行列を固有値分解して「最も分散が大きい方向」（主成分）を順に取り出します。バイプロットで個人と項目を同じ平面に布置し、項目グループの分離・個人の特徴を一目で把握できます。**潜在変数モデルではなく**「データを少数の合成変数で要約する道具」と理解してください（潜在構造の探索なら次節 EFA）。
:::

#### コンセプト

主成分分析（PCA）は、多数の変数を少数の**主成分**（principal component）に要約する手法です。データの分散を最大限に保ちながら次元を削減することが目的であり、潜在変数モデルではなく幾何学的な変換手法です[@jolliffe2016]。言語テスティングでは、テスト項目の次元構造を概観する目的で古くから用いられてきました[@bachman1982; @loewen2020]。

#### 概要
1. PCA は相関行列（または共分散行列）を固有値分解します
2. 固有値が大きい成分ほど多くの分散を説明します
3. スクリープロット、累積寄与率、平行分析で保持する成分数を検討します
4. バイプロット、負荷量、寄与率、$\cos^2$ で主成分の意味を解釈します

#### 数学的定義

$p$ 個の観測変数 $X_1, X_2, \ldots, X_p$ に対し、第 $k$ 主成分は次のように定義される。

$$z_k = v_{k1} x_1 + v_{k2} x_2 + \cdots + v_{kp} x_p = \mathbf{v}_k^\top \mathbf{x}$$

ここで $\mathbf{v}_k$ は標準化済データの相関行列 $\mathbf{R}$ の第 $k$ 固有ベクトル（単位ノルム $\|\mathbf{v}_k\|=1$）であり、$z_k$ の分散（より厳密には標準化変数の標本分散）は対応する固有値 $\lambda_k$ に等しくなります。固有値分解は次の形式です。

$$\mathbf{R} = \mathbf{V} \boldsymbol{\Lambda} \mathbf{V}^\top$$

$\lambda_1 \geq \lambda_2 \geq \cdots \geq \lambda_p$ の順に並び、第1主成分が最大の分散を説明します。

**SVD との関係**: 標準化済データ行列を $\mathbf{X}_{n \times p}$ とすると、$\mathbf{X} = \mathbf{U}\mathbf{D}\mathbf{V}^\top$ という特異値分解を持ち、相関行列は $\mathbf{R} = \mathbf{X}^\top \mathbf{X}/(n-1) = \mathbf{V}(\mathbf{D}^2/(n-1))\mathbf{V}^\top$ となります。したがって**特異値の二乗 $d_k^2 = (n-1)\lambda_k$** であり、PCA は SVD の特殊形と位置づけられます。実装上 `prcomp()` は SVD を、`princomp()` は固有値分解を使う違いがあります（前者が数値安定性で推奨）。

固有ベクトルは「分散が最大になる方向」を、固有値は「その方向でどれだけ分散を説明できるか」を表します。主成分分析では、相関行列を固有値分解することで、元の変数空間をより少数の直交する方向（主成分）に要約します。直感的には、固有値が大きい主成分ほど「データの散らばりを多く捉えるカメラアングル」であり、固有値が小さい主成分はノイズに近い方向を表します。

::: {.callout-tip}
**線形代数のミニ復習: 固有値・固有ベクトル**

$\mathbf{R}\mathbf{v} = \lambda \mathbf{v}$ という関係を満たす非ゼロベクトル $\mathbf{v}$ を**固有ベクトル**、そのスカラー $\lambda$ を**固有値**と呼びます。直感的には「行列 $\mathbf{R}$ をかけても方向が変わらず、長さだけ $\lambda$ 倍される特別な方向」です。共分散・相関行列のような対称行列では、固有ベクトルは互いに直交し、固有値はすべて非負です。PCA では「データ雲を眺めるカメラアングル候補」が固有ベクトル、「そのアングルから見たデータの散らばり量」が固有値、と理解できます。
:::

#### 幾何学的直感

PCA は、$p$ 次元のデータ雲を眺める「最良のカメラアングル」を見つける作業に例えられます。第1主成分はデータの分散（散らばり）が最大になる方向に座標軸を回転させ、第2主成分は第1主成分と直交する方向で残りの分散を最大化します。

英語技能テストの例で考えると:

- Reading, Listening, Grammar の12項目は12次元空間に存在します
- PCA はこの12次元空間で「最もデータが散らばっている方向」を見つけます
- 第1主成分は「英語力全般」のように、すべての技能に共通する方向を捉えることが多いです
- 第2主成分以降で「Reading vs. Listening」のような技能間の差異が表現されます

この直感は、後述のバイプロットで矢印の方向と長さを解釈する際の基盤になります。

#### PCA のスケーリング
変数のスケールが異なるとき（例: テスト得点 0〜100 と Likert 1〜6）は、共分散行列ではなく**相関行列**（標準化後）を用いるのが基本です。`FactoMineR::PCA()` はデフォルトで標準化を行うため、変数の単位が異なっても特定の変数だけが分散の大きさで主成分を支配しにくくなります。

| 入力 | R での典型指定 | 向いている場面 | 注意点 |
|---|---|---|---|
| 共分散行列ベースの PCA | `prcomp(x, center = TRUE, scale. = FALSE)` | すべての変数が同じ単位・同じ尺度で、分散の大きさ自体に意味がある | 分散の大きい変数が主成分を支配しやすい |
| 相関行列ベースの PCA | `prcomp(x, center = TRUE, scale. = TRUE)` / `FactoMineR::PCA()` | テスト項目、質問紙項目、異なる単位の指標を同時に扱う | 標準化により元の単位の情報は失われる |

初心者向けには、「単位が違う変数を一緒に入れるなら、まず標準化する」と覚えると安全です。経験者向けには、共分散 PCA と相関 PCA のどちらが研究上の分散概念に合うかを明示して報告します。

#### SLA データ例: 英語技能テスト

言語テスティングにおける PCA の古典的な活用例として、@bachman1982 はコミュニケーション能力の構成要素（言語能力・語用論的能力・社会言語学的能力）を multi-trait multi-method デザインで検討し、PCA で特性効果と方法効果を分離しました。また @dewaele2014 は、1,746名の多言語話者のデータに PCA を適用し、Foreign Language Enjoyment（FLE）尺度の次元構造を解明しています。@plonsky2015 は L2 研究における因子分析的手法のメタ的レビューで、多くの研究が EFA と称しながら実際には PCA を実行していた実態を指摘しており、手法選択の重要性を示しています。

以下では300名の日本人 EFL 学習者に対する12項目のテスト（Reading 4項目、Listening 4項目、Grammar 4項目）をシミュレーションで生成します。

シミュレーションでは、Reading・Listening・Grammar の3技能間に中程度の正の相関（$r = .30$--$.50$）を設定しました。これは Bachman (1982) 等のコミュニケーション能力テスト研究で報告される技能間相関の典型的な範囲を反映しています。因子負荷量は $.65$--$.80$ とし、言語テスト研究で一般的に報告される値域に設定しました。誤差の標準偏差 $.50$ は、共通性がおよそ $.50$--$.70$ になるよう調整したものです。

```{r}
#| eval: true
#| label: pca-data-simulation

set.seed(2024)
n_pca <- 300

# Latent skills with moderate correlations
Sigma_skills <- matrix(c(1, 0.5, 0.4,
                          0.5, 1, 0.3,
                          0.4, 0.3, 1), nrow = 3)
skills <- MASS::mvrnorm(n_pca, mu = c(0, 0, 0), Sigma = Sigma_skills)
colnames(skills) <- c("Reading", "Listening", "Grammar")

pca_data <- data.frame(
  Read_1  = skills[,1] * 0.8 + rnorm(n_pca, sd = 0.5),
  Read_2  = skills[,1] * 0.7 + rnorm(n_pca, sd = 0.5),
  Read_3  = skills[,1] * 0.75 + rnorm(n_pca, sd = 0.5),
  Read_4  = skills[,1] * 0.65 + rnorm(n_pca, sd = 0.5),
  List_1  = skills[,2] * 0.8 + rnorm(n_pca, sd = 0.5),
  List_2  = skills[,2] * 0.7 + rnorm(n_pca, sd = 0.5),
  List_3  = skills[,2] * 0.75 + rnorm(n_pca, sd = 0.5),
  List_4  = skills[,2] * 0.65 + rnorm(n_pca, sd = 0.5),
  Gram_1  = skills[,3] * 0.8 + rnorm(n_pca, sd = 0.5),
  Gram_2  = skills[,3] * 0.7 + rnorm(n_pca, sd = 0.5),
  Gram_3  = skills[,3] * 0.75 + rnorm(n_pca, sd = 0.5),
  Gram_4  = skills[,3] * 0.65 + rnorm(n_pca, sd = 0.5)
)

pca_item_key <- tibble::tibble(
  Variable = names(pca_data),
  Skill = dplyr::case_when(
    stringr::str_detect(Variable, "^Read") ~ "Reading",
    stringr::str_detect(Variable, "^List") ~ "Listening",
    TRUE ~ "Grammar"
  )
)
```

まず、PCA に入れる変数同士の相関構造を確認します。PCA は相関行列または共分散行列を要約する手法なので、入力行列にどのようなまとまりがあるかを先に見ると、後の主成分解釈が安定します。

```{r}
#| eval: true
#| label: pca-correlation-heatmap
#| fig-width: 8
#| fig-height: 7
#| fig-cap: "PCA 前に確認する項目間相関行列（Observed Correlation Matrix before PCA）"

pca_cor_df <- cor(pca_data) |>
  as.data.frame() |>
  tibble::rownames_to_column("Variable1") |>
  tidyr::pivot_longer(-Variable1, names_to = "Variable2", values_to = "r") |>
  dplyr::mutate(
    Variable1 = factor(Variable1, levels = names(pca_data)),
    Variable2 = factor(Variable2, levels = names(pca_data))
  )

ggplot(pca_cor_df, aes(x = Variable2, y = Variable1, fill = r)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(r, 2)), size = 2.7) +
  scale_fill_gradient2(
    low = "#2166ac",
    mid = "white",
    high = "#b2182b",
    midpoint = 0,
    limits = c(-1, 1)
  ) +
  labs(
    title = "Observed Correlation Matrix for PCA",
    subtitle = "Block patterns suggest which components may appear",
    x = NULL,
    y = NULL,
    fill = "r"
  ) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

この例では、Reading、Listening、Grammar の項目がそれぞれブロックを作っています。PCA はこのブロック構造を「少数の直交軸」に回転して要約します。ただし、ブロックが見えるからといって、それを直ちに潜在因子と呼ぶわけではありません。潜在構成概念として解釈したい場合は、後続の EFA/CFA で共通分散と測定誤差を分けて検討します。

次に、尺度化の影響を小さな例で確認します。ここでは `Read_1` だけを10倍し、共分散 PCA と相関 PCA を比較します。共分散 PCA では `Read_1` の分散が人工的に大きくなるため、第1主成分がその変数に強く引っ張られます。一方、相関 PCA では標準化されるため、もとの相関構造に基づいて主成分が推定されます。

```{r}
#| eval: true
#| label: pca-scale-sensitivity
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "共分散 PCA と相関 PCA の尺度感度（Scale Sensitivity in Covariance vs. Correlation PCA）"

pca_scale_demo <- pca_data
pca_scale_demo$Read_1 <- pca_scale_demo$Read_1 * 10

pca_cov_demo <- prcomp(pca_scale_demo, center = TRUE, scale. = FALSE)
pca_cor_demo <- prcomp(pca_scale_demo, center = TRUE, scale. = TRUE)

variance_prop <- function(pca_object) {
  pca_object$sdev^2 / sum(pca_object$sdev^2)
}

scale_sensitivity_df <- dplyr::bind_rows(
  tibble::tibble(
    Analysis = "Covariance PCA",
    Component = paste0("PC", seq_along(variance_prop(pca_cov_demo))),
    Proportion = variance_prop(pca_cov_demo)
  ),
  tibble::tibble(
    Analysis = "Correlation PCA",
    Component = paste0("PC", seq_along(variance_prop(pca_cor_demo))),
    Proportion = variance_prop(pca_cor_demo)
  )
) |>
  dplyr::filter(Component %in% paste0("PC", 1:6))

ggplot(scale_sensitivity_df, aes(x = Component, y = Proportion, fill = Analysis)) +
  geom_col(position = "dodge", width = 0.72) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +
  labs(
    title = "Scale Sensitivity of PCA",
    subtitle = "A single rescaled variable can dominate covariance PCA",
    x = "Component",
    y = "Proportion of variance",
    fill = "Analysis"
  ) +
  theme(legend.position = "bottom")
```

#### 固有値と分散の説明率

ここでは `FactoMineR::PCA()` で主成分分析を実行します。`graph = FALSE` は自動作図を抑制し、後で `factoextra` パッケージで可視化するための設定です。`PCA()` はデフォルトでデータを標準化（`scale.unit = TRUE`）するため、変数のスケールが異なっても自動的に調整されます。

固有値テーブルでは、各行が1つの主成分に対応し、**Eigenvalue 列**が分散量、**Cumulative % 列**が累積寄与率を示します。累積寄与率が 70--80% に達するまでの成分数が、データの大部分を要約するのに必要な次元数の目安になります。ただし、研究目的が可視化であれば2次元に限定することもあり、予測モデルの前処理であれば交差検証で成分数を決めることもあります。

```{r}
#| eval: true
#| label: pca-analysis
#| fig-width: 8
#| fig-height: 5

# Perform PCA
res_pca <- FactoMineR::PCA(pca_data, graph = FALSE)

# Eigenvalue table
pca_eig_df <- as.data.frame(res_pca$eig) |>
  mutate(Dimension = row_number()) |>
  dplyr::select(Dimension, everything())

pca_eig_df |>
  dplyr::rename(
    Dim = Dimension,
    `Eigenvalue` = eigenvalue,
    `% Variance` = `percentage of variance`,
    `Cumulative %` = `cumulative percentage of variance`
  ) |>
  knitr::kable(
    caption = "PCA Eigenvalues and Variance Explained",
    digits = 2,
    booktabs = TRUE
  )

# Scree plot
fviz_eig(res_pca, addlabels = TRUE, ylim = c(0, 50)) +
  labs(
    title = "Scree Plot: English Proficiency Test",
    subtitle = "Look for the 'elbow' where eigenvalue decline levels off"
  )
```

```{r}
#| eval: true
#| label: pca-cumulative-variance
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "PCA の累積寄与率プロット（Cumulative Explained Variance in PCA）"

pca_eig_df |>
  dplyr::transmute(
    Component = Dimension,
    Cumulative = `cumulative percentage of variance` / 100
  ) |>
  ggplot(aes(x = Component, y = Cumulative)) +
  geom_hline(yintercept = c(0.70, 0.80), linetype = "dashed", color = "grey55") +
  geom_line(color = "#2166ac", linewidth = 0.8) +
  geom_point(color = "#2166ac", size = 2.8) +
  scale_x_continuous(breaks = pca_eig_df$Dimension) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1), limits = c(0, 1)) +
  annotate("text", x = max(pca_eig_df$Dimension), y = 0.72, label = "70%", hjust = 1, color = "grey35") +
  annotate("text", x = max(pca_eig_df$Dimension), y = 0.82, label = "80%", hjust = 1, color = "grey35") +
  labs(
    title = "Cumulative Explained Variance",
    subtitle = "Use this with the scree plot and parallel analysis",
    x = "Number of retained components",
    y = "Cumulative explained variance"
  )
```

スクリープロットでは、横軸が成分番号、縦軸が固有値です。「肘」（急激に減少が緩やかになる点）より左側の成分を保持するのが基本方針です。赤い破線は Kaiser 基準（固有値 = 1）を示します。肘の位置は分析者の判断に依存し、特に固有値が連続的に減少する場合に判定が困難になります。Kaiser 基準（固有値 > 1）は変数が少ないときに過大抽出する傾向があるため、平行分析との併用が推奨されます。実際、12変数程度のデータでは Kaiser 基準が4成分を示唆しても、平行分析では2〜3成分が適切と判断されることが少なくありません。

#### スクリープロット（tidyverse 版）

```{r}
#| eval: true
#| label: pca-scree-tidyverse
#| fig-width: 7
#| fig-height: 5

eig_vals <- res_pca$eig[, 1]
scree_tbl <- tibble(Component = seq_along(eig_vals), Eigenvalue = eig_vals)

ggplot(scree_tbl, aes(Component, Eigenvalue)) +
  geom_point(size = 3, color = "darkblue") +
  geom_line(linewidth = 0.8, color = "darkblue") +
  geom_hline(yintercept = 1, linetype = "dashed", color = "red") +
  scale_x_continuous(breaks = scree_tbl$Component) +
  annotate("text", x = 11, y = 1.15, label = "Kaiser criterion (λ = 1)",
           color = "red", size = 3.5, hjust = 1) +
  labs(
    title = "Scree Plot (tidyverse)",
    subtitle = "Components above the red line have eigenvalue > 1",
    y = "Eigenvalue"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

Kaiser 基準（赤い破線）より上の成分は4つありますが、肘の位置はおおむね2〜3成分を示唆します。複数の基準（Kaiser 基準、肘の判断、平行分析）が一致しない場合、理論的な解釈可能性も含めて総合的に判断する必要があります。

#### 成分数の決定基準
- **Kaiser 基準**（固有値 > 1）: 過大評価の傾向があり、単独では推奨しない[@kaiser1960]。
- **スクリープロット**（肘の位置）: 主観的だが視覚的に有用[@cattell1966]。
- **平行分析**（Horn 法）: ランダムデータの固有値と比較する最も推奨される方法[@horn1965]。
- **MAP テスト**: 偏相関行列に基づく客観的基準[@velicer1976]。
- 実務では**複数の基準を総合判断**するのがベストプラクティスです。

#### 平行分析による成分数の検証

平行分析は、実データの固有値をランダムデータ（同じサイズ）の固有値と比較する方法です。実データの固有値がランダムデータの固有値を上回る成分のみを保持します[@horn1965]。

`psych::fa.parallel()` の主要引数:

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `fm` | `"ml"` | 因子分析時の抽出法です。`fa = "pc"` の PCA では本質的な指定は `fa` です |
| `fa` | `"pc"` | **PCA の成分数を決定する場合は `"pc"`**。EFA の因子数なら `"fa"`。混同しやすいので注意 |
| `n.iter` | `100` | ランダムデータの生成回数。多いほど安定するが計算時間が増える |

```{r}
#| eval: true
#| label: pca-parallel-analysis
#| fig-width: 8
#| fig-height: 6

# Parallel analysis for PCA
set.seed(2024)
psych::fa.parallel(pca_data, fm = "ml", fa = "pc", n.iter = 100,
                   main = "Parallel Analysis: English Proficiency Test (PCA)")
```

出力の読み方: 青い三角（実データの固有値）が赤い破線（ランダムデータの固有値）を上回っている成分のみを保持します。ランダムデータの固有値を超えない成分は、偶然のノイズで説明できる程度の分散しか持たないと判断します。

::: {.callout-note}
**Velicer's MAP テスト — 偏相関平均二乗最小化**

@velicer1976 の **MAP** (Minimum Average Partial) テストは、平行分析と並ぶ最も推奨される客観的成分数決定法です。考え方は、$m$ 個の主成分を抽出した後の残差相関行列を計算し、対角を除く偏相関の平均二乗 $f_m$ を最小化する $m$ を選択するというものです:

$$f_m = \frac{1}{p(p-1)} \sum_{i \neq j} r^{*2}_{ij,m}$$

ここで $r^*_{ij,m}$ は最初の $m$ 主成分を取り除いた後の項目 $i, j$ 間の偏相関です。$f_m$ が最小となる $m$ が最適成分数の候補で、$m+1$ 番目以降の成分を抽出すると残差行列が「過度に縮小」される（共通分散ではなく独自分散を取り始める）ことを示唆します。

```r
# psych パッケージで MAP と平行分析を同時実行
psych::vss(pca_data, n = 8, fm = "ml", rotate = "none")
# 出力: Velicer MAP achieves a minimum of 0.04 with 3 factors
```

`psych::vss()` は Very Simple Structure 指標と一緒に MAP も返し、`nFactors::nMAP()` は単独で MAP を実行します。平行分析が「ランダムデータより上か」を判定するのに対し、MAP は「残差の偏相関構造」を直接評価する点で相補的です。論文では両者の結果を併記するのが現代的慣行です（Goretzko et al. 2021）。
:::

複数の基準は、結論だけでなく**判断の役割**も異なります。Kaiser 基準は参考情報、スクリープロットは視覚的確認、平行分析はランダムデータとの比較、累積寄与率は要約率の確認として使います。実務では次のように、基準ごとの結果を1つの表にまとめておくと報告しやすくなります。

```{r}
#| eval: true
#| label: pca-retention-summary

set.seed(2024)
n_random <- 200
random_eigenvalues <- replicate(
  n_random,
  eigen(cor(matrix(rnorm(nrow(pca_data) * ncol(pca_data)), nrow = nrow(pca_data))))$values
)
parallel_mean <- rowMeans(random_eigenvalues)
parallel_95 <- apply(random_eigenvalues, 1, stats::quantile, probs = .95)
cumvar <- pca_eig_df$`cumulative percentage of variance`

pca_retention_summary <- tibble::tibble(
  Criterion = c(
    "Kaiser criterion",
    "Scree plot",
    "Parallel analysis (mean random eigenvalue)",
    "Parallel analysis (95th percentile random eigenvalue)",
    "70% cumulative variance",
    "80% cumulative variance"
  ),
  SuggestedComponents = c(
    as.character(sum(eig_vals > 1)),
    "2-3",
    as.character(sum(eig_vals > parallel_mean)),
    as.character(sum(eig_vals > parallel_95)),
    as.character(min(which(cumvar >= 70))),
    as.character(min(which(cumvar >= 80)))
  ),
  Role = c(
    "参考情報。単独採用は避けます",
    "固有値の落ち込み方を視覚的に確認します",
    "ランダムデータの平均固有値を上回る成分を残します",
    "より保守的な平行分析です",
    "可視化・要約として最低限の情報量を確認します",
    "予測や合成得点利用で候補にします"
  )
)

pca_retention_summary |>
  knitr::kable(
    caption = "PCA Component Retention Decision Summary",
    booktabs = TRUE
  )
```

この例では、可視化目的なら第1・第2主成分の平面を中心に解釈しつつ、技能構造をより丁寧に要約するには第3主成分まで確認する、という判断が自然です。PCA の成分数は「正解を1つ選ぶ」というより、研究目的に応じて**どこまで情報を保持するか**を決める問題です。

#### バイプロットと変数診断

バイプロットは個体（学習者）と変数（テスト項目）を同じ2次元平面上に配置し、両者の関係を一望する図です。バイプロットの読み方には3つの要素があります。第一に、**矢印の方向**は変数と主成分の相関を表し、同じ方向の矢印を持つ変数は正の相関にあります。第二に、**矢印の長さ**は変数の寄与の大きさ（$\cos^2$）に対応し、長い矢印ほどこの2次元空間上でよく表現されています。第三に、**$\cos^2$**（表現の質）は0〜1の値をとり、1に近いほどその変数がこの平面上で正確に表現されていることを意味します。矢印が短い変数は高次の成分に情報が分散しているため、2次元バイプロット上の位置だけで解釈を確定させないようにします。

以下のタブで、FactoMineR 版・tidyverse 版のバイプロット、および各変数の寄与率と $\cos^2$ を確認します。

::: {.panel-tabset}

#### バイプロット（FactoMineR）

```{r}
#| eval: true
#| label: pca-biplot
#| fig-width: 10
#| fig-height: 8

# FactoMineR biplot
fviz_pca_biplot(res_pca, repel = TRUE,
                col.var = "red", col.ind = "grey60",
                title = "PCA Biplot: English Proficiency Test")
```

#### バイプロット（tidyverse）

```{r}
#| eval: true
#| label: pca-biplot-tidyverse
#| fig-width: 10
#| fig-height: 8

# Tidyverse biplot
ind <- as_tibble(res_pca$ind$coord) |> mutate(id = row_number())
var <- as_tibble(res_pca$var$coord, rownames = "Variable")

ggplot() +
  geom_point(data = ind, aes(Dim.1, Dim.2), alpha = 0.4, size = 1.5, color = "grey50") +
  geom_segment(
    data = var,
    aes(x = 0, y = 0, xend = Dim.1 * 3, yend = Dim.2 * 3),
    arrow = arrow(length = unit(0.25, "cm")),
    color = "red", linewidth = 0.8
  ) +
  ggrepel::geom_text_repel(
    data = var,
    aes(Dim.1 * 3.3, Dim.2 * 3.3, label = Variable),
    color = "darkred",
    family = "HiraginoSans-W3",
    size = 3.5,
    box.padding = 0.35,
    point.padding = 0.2,
    min.segment.length = 0,
    seed = 2024
  ) +
  labs(
    title = "PCA Biplot (tidyverse)",
    subtitle = "Arrows: variables; Points: individuals (learners)",
    x = paste0("Dim 1 (", round(res_pca$eig[1,2], 1), "%)"),
    y = paste0("Dim 2 (", round(res_pca$eig[2,2], 1), "%)")
  ) +
  theme(plot.title = element_text(face = "bold"))
```

バイプロットを見ると、Reading 系の変数（Read_1, Read_2, Read_3, Read_4）が同じ方向に密集しているのは、これらが共通の主成分に強く寄与することを示します。同様に Listening 系・Grammar 系の変数もそれぞれクラスタを形成しているのが確認できます。矢印が長い変数ほどその成分上での表現が良好であり、短い変数は2次元空間での近似が不十分であることを意味します。技能グループ間の矢印の角度が鋭角であるのは、シミュレーションで設定した技能間の正の相関（$r = .30$--$.50$）を反映しています。

#### 個体スコア

```{r}
#| eval: true
#| label: pca-individual-score-plot
#| fig-width: 8
#| fig-height: 6
#| fig-cap: "PCA 個体スコアの散布図（Individual Scores on Principal Components）"

pca_ind_scores <- as_tibble(res_pca$ind$coord[, 1:2]) |>
  dplyr::mutate(
    id = dplyr::row_number(),
    DominantSkill = c("Reading", "Listening", "Grammar")[max.col(skills, ties.method = "first")]
  )

ggplot(pca_ind_scores, aes(x = Dim.1, y = Dim.2, color = DominantSkill)) +
  geom_hline(yintercept = 0, color = "grey75") +
  geom_vline(xintercept = 0, color = "grey75") +
  geom_point(alpha = 0.55, size = 1.7) +
  stat_ellipse(linewidth = 0.7, alpha = 0.7) +
  labs(
    title = "Learner Positions in the PCA Space",
    subtitle = "Color uses the simulated dominant latent skill for teaching purposes",
    x = paste0("PC1 (", round(res_pca$eig[1, 2], 1), "%)"),
    y = paste0("PC2 (", round(res_pca$eig[2, 2], 1), "%)"),
    color = "Dominant skill"
  ) +
  theme(legend.position = "bottom")
```

個体スコアの散布図では、各学習者が主成分空間上に配置されます。この例ではシミュレーション時の潜在技能を色分けに使っていますが、実データでは習熟度群、学年、留学経験の有無など、研究上意味のある外部変数で色分けします。PCA 空間で群が分かれて見える場合、その群差がどの変数の組み合わせに対応しているかをバイプロットと照合します。

#### 主成分負荷量のヒートマップ

標準化 PCA では、変数座標（`res_pca$var$coord`）は各変数と主成分の相関に相当します。これを**主成分負荷量**（component loading）として見ると、各主成分がどの変数群によって定義されているかを表形式よりも直感的に確認できます。

```{r}
#| eval: true
#| label: pca-loading-heatmap
#| fig-width: 8
#| fig-height: 6
#| fig-cap: "PCA 主成分負荷量のヒートマップ（Heatmap of PCA Component Loadings）"

pca_loading_df <- as_tibble(res_pca$var$coord[, 1:4], rownames = "Variable") |>
  tidyr::pivot_longer(-Variable, names_to = "Component", values_to = "Loading") |>
  dplyr::mutate(
    Component = stringr::str_replace(Component, "Dim\\.", "PC"),
    Variable = factor(Variable, levels = rev(names(pca_data)))
  ) |>
  dplyr::left_join(pca_item_key, by = "Variable")

ggplot(pca_loading_df, aes(x = Component, y = Variable, fill = Loading)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(Loading, 2)), size = 3) +
  scale_fill_gradient2(low = "#2166ac", mid = "white", high = "#b2182b", midpoint = 0) +
  labs(
    title = "PCA Component Loadings",
    subtitle = "Large absolute values identify variables that define each component",
    x = "Component",
    y = NULL,
    fill = "Loading"
  )
```

#### 寄与率テーブル

各変数が各次元の形成にどれだけ寄与しているかを百分率で示します。寄与率が高い変数が、その次元の「正体」を示します。負荷量は「変数と成分の関連の強さ」、寄与率は「その成分がどの変数に支えられているか」を表すため、両方を見ると解釈が安定します。

```{r}
#| eval: true
#| label: pca-contrib

# Variable contributions (%)
pca_var_contrib <- as_tibble(res_pca$var$contrib[, 1:2], rownames = "Variable") |>
  rename(Contrib_Dim1 = `Dim.1`, Contrib_Dim2 = `Dim.2`) |>
  arrange(desc(Contrib_Dim1))

pca_var_contrib |>
  knitr::kable(
    caption = "PCA Variable Contributions",
    digits = 2,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: pca-contribution-barplot
#| fig-width: 8
#| fig-height: 6
#| fig-cap: "PCA 変数寄与率の棒グラフ（Variable Contributions to PCA Dimensions）"

pca_contrib_long <- pca_var_contrib |>
  tidyr::pivot_longer(
    cols = starts_with("Contrib"),
    names_to = "Dimension",
    values_to = "Contribution"
  ) |>
  dplyr::mutate(
    Dimension = stringr::str_replace(Dimension, "Contrib_Dim", "Dim "),
    Variable = factor(Variable, levels = rev(names(pca_data)))
  ) |>
  dplyr::left_join(pca_item_key, by = "Variable")

average_contribution <- 100 / ncol(pca_data)

ggplot(pca_contrib_long, aes(x = Contribution, y = Variable, fill = Skill)) +
  geom_col(width = 0.65) +
  geom_vline(xintercept = average_contribution, linetype = "dashed", color = "grey45") +
  facet_wrap(~ Dimension, scales = "free_x") +
  labs(
    title = "Variable Contributions to Principal Components",
    subtitle = "Dashed line shows the average contribution expected under equal influence",
    x = "Contribution (%)",
    y = NULL,
    fill = "Skill"
  ) +
  theme(legend.position = "bottom")
```

#### cos² テーブル

$\cos^2$ は各変数がこの2次元平面でどれだけ正確に表現されているかを示します（0〜1）。値が低い変数は、この平面上の位置が信頼できないため解釈を控えます。

```{r}
#| eval: true
#| label: pca-cos2

# Variable cos2 (quality of representation)
pca_var_cos2 <- as_tibble(res_pca$var$cos2[, 1:2], rownames = "Variable") |>
  rename(cos2_Dim1 = `Dim.1`, cos2_Dim2 = `Dim.2`) |>
  arrange(desc(cos2_Dim1))

pca_var_cos2 |>
  knitr::kable(
    caption = "PCA Variable cos²",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: pca-cos2-heatmap
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "PCA 変数 cos² のヒートマップ（Heatmap of Variable cos² in PCA）"

# Variable cos2 heatmap
pca_var_cos2 |>
  pivot_longer(-Variable, names_to = "Dimension", values_to = "cos2") |>
  mutate(Dimension = gsub("cos2_", "", Dimension)) |>
  ggplot(aes(x = Dimension, y = reorder(Variable, cos2), fill = cos2)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(cos2, 2)), size = 3.5) +
  scale_fill_gradient(low = "white", high = "#2b8cbe") +
  labs(title = "Variable cos² Heatmap",
       subtitle = "Quality of representation on each dimension",
       y = "Variable", fill = "cos²")
```

cos² ヒートマップでは、各変数が第1・第2主成分の平面上でどの程度よく表現されているかを色の濃さで示します。cos² が高い変数ほど、バイプロット上の位置を信頼して解釈できます。cos² が低い変数は、この2次元では十分に表現されていないため、解釈を控えるか、第3主成分以降も確認します。目安として、cos² < .50 の変数はバイプロット上の位置だけで結論を出さないようにします。

:::

#### バイプロットの読み方
- **矢印の長さ**: 長いほどこの2次元平面で十分に表現されている（$\cos^2$ が高い）
- **矢印同士の角度**: 小さい = 正の相関、反対向き = 負の相関、直交 = ほぼ無相関
- **個体点の位置**: 矢印方向に遠いほどその変数の値が大きい
- まず**累積寄与率**を確認し、この平面が全体の何%を説明しているかを把握します

#### 寄与率と $\cos^2$ の違い
- **寄与率（contribution）**: その次元が「どの変数に支えられているか」を示します。次元の解釈に使います。
- **$\cos^2$**: その変数が「この平面でどれだけ正確に再現されているか」を示します。低い点の解釈は控えめにします。
- 実務手順: 寄与率で軸の主要因を把握し、$\cos^2$ で信頼できる変数のみ解釈します。

#### 主成分スコアの取り出しと使い方

PCA の個体スコア（component scores）は、各学習者を主成分軸上に配置した値です。予測モデルの前処理、クラスタリング、群比較、外れ値診断などに使えます。ただし、主成分スコアは観測変数の線形結合であり、測定誤差を取り除いた潜在変数得点ではありません。

```{r}
#| eval: true
#| label: pca-component-scores

pca_scores_for_analysis <- as_tibble(res_pca$ind$coord[, 1:3]) |>
  dplyr::rename(PC1 = Dim.1, PC2 = Dim.2, PC3 = Dim.3) |>
  dplyr::mutate(id = dplyr::row_number(), .before = 1)

pca_scores_for_analysis |>
  dplyr::slice_head(n = 8) |>
  knitr::kable(
    caption = "PCA Component Scores for the First Eight Learners",
    digits = 3,
    booktabs = TRUE
  )
```

主成分スコアを後続分析に使う場合は、保持した成分数、標準化の有無、各成分の解釈、累積寄与率を必ず報告します。主成分の符号は任意なので、PC1 が正から負に反転しても統計的意味は同じです。論文や教材では、解釈しやすい方向になるように符号を揃えた場合、その処理を明記します。

#### 2次元表示の情報損失を確認する

PCA のバイプロットは便利ですが、第1・第2主成分だけで全情報を表しているわけではありません。保持する成分数を増やすほど元データの再構成誤差（reconstruction error）は小さくなります。したがって、2次元図を解釈する前に、どの程度の情報を捨てているかを確認します。

```{r}
#| eval: true
#| label: pca-reconstruction-error
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "PCA の再構成誤差（Reconstruction Error by Number of Components）"

pca_prcomp <- prcomp(pca_data, center = TRUE, scale. = TRUE)
pca_scaled_matrix <- scale(pca_data)

reconstruction_rmse <- purrr::map_dbl(seq_len(ncol(pca_data)), \(k) {
  reconstructed <- pca_prcomp$x[, seq_len(k), drop = FALSE] %*%
    t(pca_prcomp$rotation[, seq_len(k), drop = FALSE])
  sqrt(mean((pca_scaled_matrix - reconstructed)^2))
})

reconstruction_df <- tibble::tibble(
  Components = seq_len(ncol(pca_data)),
  RMSE = reconstruction_rmse,
  CumulativeVariance = pca_eig_df$`cumulative percentage of variance` / 100
)

ggplot(reconstruction_df, aes(x = Components, y = RMSE)) +
  geom_line(color = "#2166ac", linewidth = 0.8) +
  geom_point(color = "#2166ac", size = 2.5) +
  scale_x_continuous(breaks = reconstruction_df$Components) +
  labs(
    title = "PCA Reconstruction Error",
    subtitle = "Lower RMSE means the retained components reproduce the standardized data better",
    x = "Number of retained components",
    y = "RMSE of reconstructed standardized variables"
  )
```

再構成誤差は、PCA を**データ圧縮**として使う場合に特に重要です。バイプロットで第1・第2主成分だけを表示する場合でも、第3主成分以降にまとまった情報が残っていれば、2次元図だけで変数間関係や個体差を結論づけないようにします。

#### 外れ値と影響点の診断

PCA は分散が最大になる方向を探すため、極端な個体に引っ張られやすいです。外れ値があると、第1主成分が「主要な能力差」ではなく「数名の特殊な回答パターン」を表してしまうことがあります。ここでは、保持候補となる第1〜第3主成分空間でのスコア距離を使って、極端な個体を確認します。

```{r}
#| eval: true
#| label: pca-score-distance
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "PCA 主成分スコア距離による外れ値診断（Score Distance Diagnostics in PCA）"

pca_score_distance <- as_tibble(res_pca$ind$coord[, 1:3]) |>
  dplyr::mutate(
    id = dplyr::row_number(),
    ScoreDistance = sqrt(Dim.1^2 + Dim.2^2 + Dim.3^2),
    Threshold = stats::quantile(ScoreDistance, probs = .975),
    Flag = ScoreDistance > Threshold
  )

ggplot(pca_score_distance, aes(x = id, y = ScoreDistance, color = Flag)) +
  geom_hline(
    yintercept = unique(pca_score_distance$Threshold),
    linetype = "dashed",
    color = "grey45"
  ) +
  geom_point(alpha = 0.75, size = 1.8) +
  scale_color_manual(values = c("FALSE" = "grey45", "TRUE" = "#b2182b")) +
  labs(
    title = "Score Distance in the First Three Principal Components",
    subtitle = "Flagged cases are above the empirical 97.5th percentile",
    x = "Learner ID",
    y = "Score distance",
    color = "Flagged"
  ) +
  theme(legend.position = "bottom")
```

フラグが立った個体は、即座に除外する対象ではありません。まず、入力ミス、回答パターン、テスト実施条件、背景情報を確認します。正当な観測値であれば分析に残し、外れ値を除いた感度分析も併記すると透明性が高まります。

#### 欠測値と PCA

通常の PCA は完全データを前提とします。欠測がある場合に単純なリストワイズ削除を行うと、サンプルサイズが減り、特に欠測が特定の学習者層に偏っている場合には主成分構造も歪みます。実務では、欠測率と欠測パターンを確認したうえで、欠測が少なければ完全ケース分析、欠測が多い場合は多重代入や PCA 対応の欠測補完法を検討します。

```{r}
#| eval: false
#| label: pca-missing-data-example

# Example workflow for real data with missing values
colMeans(is.na(your_data))

# Option 1: complete cases, only when missingness is minimal and plausibly random
pca_complete <- FactoMineR::PCA(na.omit(your_data), graph = FALSE)

# Option 2: impute first, then run PCA
# imputed <- missMDA::imputePCA(your_data, ncp = 3)
# pca_imputed <- FactoMineR::PCA(imputed$completeObs, graph = FALSE)
```

#### PCA の注意点
- PCA は**全分散**（共通分散 + 独自性）を回収します。したがって、測定誤差を含む合成変数を生成します。これが因子分析との根本的な違いです[@loewen2020]。
- 外れ値に引っ張られやすいです。主成分スコアの散布図や箱ひげ図で、極端な個体が主成分方向を決めていないか確認します。
- 2次元で十分とは限りません。**累積寄与率**と **$\cos^2$** を必ず確認します。
- 主成分の符号は任意です。PC1 の正負が反転しても、解釈上は同じ軸を見ています。
- 尺度開発や構成概念妥当性の検証を目的とする場合、PCA ではなく EFA/CFA を主分析にします。
- 欠測値を含むデータでは、リストワイズ削除が結果を歪めないか確認します。

### 探索的因子分析（EFA）

::: {.callout-tip}
**1 分プレビュー (EFA)**

「18 個の動機づけ質問項目から、**何個の潜在因子**が背後にあり、**どの項目がどの因子を測っている**かを探索したい」という時に使う **確率的な潜在変数モデル** です。観測相関を「共通因子で説明できる部分」と「項目固有の独自性」に分解します。PCA との違いは、EFA は確率モデルとして測定誤差を分離する点です。回転（promax/oblimin）で因子の解釈可能性を高め、$\omega$ 信頼性で内的整合性を確認します。**「データから因子構造を発見」する段階**で使い、「事前に仮定した因子構造を検証」したい場合は次節 CFA に進みます。
:::

#### コンセプトと手順

探索的因子分析（EFA）は、観測変数間の相関を「共通因子 + 独自性」に分解するモデルです。PCA と異なり、EFA は**確率モデル**であり、観測データの背後にある潜在因子を推定します[@thurstone1947; @revelle2024]。

SLA 研究では尺度開発の場面で EFA が最も頻繁に用いられてきました。@gardner1985 は Attitude/Motivation Test Battery（AMTB）を開発する過程で EFA を用い、統合的動機づけ・道具的動機づけ・言語不安・学習環境への態度という4つの上位構成概念を同定しました。AMTB はその後数十年にわたり L2 動機づけ研究の標準的測定ツールとなりました。@horwitz1986 は33項目の Foreign Language Classroom Anxiety Scale（FLCAS）を EFA で分析し、コミュニケーション不安・テスト不安・否定的評価への恐れという3つの下位因子を抽出しました。FLCAS は現在までに世界中で翻訳・追試されている、SLA 研究で最も引用される尺度の一つです。

こうした歴史的な蓄積を踏まえ、@plonsky2015 は L2 研究における EFA の実施慣行を体系的に検証しました。このメタ的レビューでは、因子数決定に Kaiser 基準（固有値 > 1）のみを用いている研究が依然として多いこと、回転法の選択根拠が報告されていないケースが頻繁にあること、そしてサンプルサイズが項目数に対して不十分な研究が散見されることが指摘されています。本章では、これらの問題点を踏まえたベストプラクティスに沿って EFA を解説します。

#### 概要
1. EFA の因子モデル: $\mathbf{X} = \boldsymbol{\Lambda} \mathbf{f} + \mathbf{u}$
2. 因子数は平行分析で決定します（Kaiser 基準は過大評価する傾向があります）
3. 回転は斜交回転（promax）をデフォルトとします
4. 因子負荷量 > 0.40 を実質的に意味のある負荷とみなす

#### 因子モデルの定式化

$$\mathbf{x}_i = \boldsymbol{\Lambda} \mathbf{f}_i + \mathbf{u}_i$$

- $\mathbf{x}_i$: $p$ 個の観測変数ベクトル（第 $i$ 回答者）
- $\boldsymbol{\Lambda}$: $p \times m$ の因子負荷行列
- $\mathbf{f}_i$: $m$ 個の共通因子ベクトル（$m \ll p$）
- $\mathbf{u}_i$: 独自性ベクトル（特殊分散 + 測定誤差）

因子モデルでは、観測変数そのものを直接分解しているのではなく、観測変数間の**共分散行列**または**相関行列**を、少数の共通因子で再現しようとします。識別の前提として、共通因子と独自因子は無相関 $\mathrm{Cov}(\mathbf{f}, \mathbf{u}) = \mathbf{0}$ かつ独自因子間も無相関（$\boldsymbol{\Theta}$ が対角）であると仮定します（Jöreskog 1969）。元の尺度単位を保つ場合、**母集団含意共分散行列** $\boldsymbol{\Sigma}(\boldsymbol{\theta})$ を次のように表します。

$$\boldsymbol{\Sigma}(\boldsymbol{\theta}) = \boldsymbol{\Lambda} \boldsymbol{\Phi} \boldsymbol{\Lambda}^\top + \boldsymbol{\Theta}$$

推定では、標本共分散行列 $\mathbf{S}$ と $\boldsymbol{\Sigma}(\hat{\boldsymbol{\theta}})$ の差を適合関数（ML/GLS/ULS）で最小化します。質問紙項目のようにすべてが同じ尺度であっても、項目の分散が異なる場合、共分散行列を使うと分散の大きい項目が分析を強く支配します。そのため、EFA では各変数を分散1に標準化した**相関行列**を入力にすることが多く、その場合は次の形になります。

$$\boldsymbol{\Sigma}(\boldsymbol{\theta}) = \boldsymbol{\Lambda} \boldsymbol{\Phi} \boldsymbol{\Lambda}^\top + \boldsymbol{\Psi}$$

$\boldsymbol{\Sigma}(\boldsymbol{\theta})$ は標準化を仮定したモデル含意相関行列、$\boldsymbol{\Phi}$ は因子間相関行列（直交回転では単位行列）、$\boldsymbol{\Psi}$ は独自性の対角行列です。つまり、因子分析の目標は、観測された相関行列 $\mathbf{R}$ を、**共通因子で説明できる部分** $\boldsymbol{\Lambda}\boldsymbol{\Phi}\boldsymbol{\Lambda}^\top$ と、**各項目に固有の部分** $\boldsymbol{\Psi}$ に分けることです。

| 入力行列 | 何を表すか | 使う場面 | 注意点 |
|---|---|---|---|
| 共分散行列 $\mathbf{S}$ | 元の単位を保った変数間の共変動 | 同じ単位・同じ測定尺度の連続変数 | 分散の大きい項目が結果を支配しやすい |
| Pearson 相関行列 $\mathbf{R}$ | 標準化後の線形関連 | 連続変数、6件法以上の Likert 項目 | 順序性が強い項目では相関を過小推定することがある |
| ポリコリック相関行列 | 順序カテゴリ背後の連続潜在反応の相関 | 5件法以下の Likert 項目 | 閾値モデルを仮定するため、カテゴリが極端に偏ると不安定 |
| テトラコリック相関行列 | 2値反応背後の連続潜在反応の相関 | 正誤項目、Yes/No 項目 | 正答率が極端な項目では推定が不安定 |

初心者向けには、「因子分析は相関行列の中にあるまとまりを探す方法」と理解するとよいです。経験者向けには、「EFA は $\mathbf{R}$ と $\hat{\mathbf{R}} = \boldsymbol{\Lambda}\boldsymbol{\Phi}\boldsymbol{\Lambda}^\top + \boldsymbol{\Psi}$ の差を小さくする測定モデル」と捉えると、CFA/SEM の含意共分散行列の考え方へ自然につながります。

身近な例で考えてみます。英語テストで「リスニング問題5問」「リーディング問題5問」「文法問題5問」の計15問があるとします。各問題のスコア（観測変数）は、直接は測定できない「聴解力」「読解力」「文法知識」（潜在因子）から生み出されています。因子分析はこの生成過程を逆算し、**観測されたスコアの相関パターンから背後にある少数の能力次元を推定する**手法です。因子負荷行列 $\boldsymbol{\Lambda}$ は「各問題がどの能力にどの程度依存しているか」を数値化したものです。

#### 共通性・独自性の直感

因子モデルでは、各観測変数の分散を**共通性**（communality: $h^2$）と**独自性**（uniqueness: $u^2$）に分解します。

$$\text{Var}(X_j) = h_j^2 + u_j^2 = 1 \quad (\text{標準化後})$$

- **共通性** $h_j^2$: 因子で説明される分散割合です。直交因子・標準化解ではおおまかに因子負荷量の二乗和 $h_j^2 = \sum_{k=1}^{m} \lambda_{jk}^2$ と読めます。斜交回転では因子間相関を含めるため、より正確には $h_j^2 = \boldsymbol{\lambda}_j^\top \boldsymbol{\Phi}\boldsymbol{\lambda}_j$ と考え、実務上は `psych::fa()` の共通性出力を参照します。
- **独自性** $u_j^2 = 1 - h_j^2$: 因子で説明されない分散です。測定誤差と項目固有の分散を含みます。

共通性が低い項目（例: $h^2 < .30$）は、当該の共通因子群では十分に説明されていない候補です。ただし、独自性にはランダムな測定誤差だけでなく、項目固有の意味ある分散も含まれます。そのため、機械的に削除するのではなく、内容妥当性、項目文、信頼性、交差負荷、残差相関を併せて検討します。

#### 因子数の過少・過多のリスク

因子数の決定は EFA における最も重要な判断です。

- **過少抽出**（underfactoring）: 真の因子構造を取りこぼし、項目間の共分散パターンが十分に説明されません。因子負荷に偏りが生じ、因子の解釈が曖昧になります。
- **過多抽出**（overfactoring）: ノイズや特定サンプルに過剰適合し、再現性の低い因子を抽出します。余分な因子は少数の項目のみに負荷し、解釈が困難になります。

このトレードオフを最小化するため、スクリープロット、平行分析、理論的予測を**総合判断**するのが実務的なベストプラクティスです。実務では、理論的に想定される因子数 $k$ に対して $k-1$, $k$, $k+1$ の各モデルを推定し、解釈可能性と適合度を比較することで過少・過多抽出のリスクを軽減できます。

#### 推定法の選択
- **最尤法（ML）**: 正規性を仮定し、適合度検定が可能。中〜大サンプルで推奨[@kline2023]。
- **主軸法（PA）**: 分布の仮定は ML より緩いが、小標本問題を解消するわけではありません。
- **最小残差法（minres）**: 相関残差を小さくする推定法です。探索段階で有用ですが、標本サイズ・共通性・項目数の影響を受けます。
- Likert 尺度には `psych::fa()` の `cor="poly"`（ポリコリック相関）を検討[@holgadotello2010]。

#### SLA データ例: L2 動機づけ質問紙

D&ouml;rnyei の L2 動機づけ自己体系（L2 Motivational Self System）に基づく質問紙をシミュレーションします[@dornyei2009]。この3因子構造は、日本・中国・イランのEFL学習者を対象とした @taguchi2009 の CFA による多国間比較で妥当性が確認され、@you2016 の中国 EFL 学習者調査でも支持されてきました。近年では @alhoorie2024 が弁別的妥当性の問題を指摘しており、適切な EFA・CFA の手続きの重要性が改めて強調されています。

@taguchi2009 は日本・中国・イランの 3 か国で同一の 3 因子構造（理想 L2 自己・義務 L2 自己・L2 学習経験）を確認し、異なる文化圏でも L2MSS モデルの因子構造が再現されることを実証しました。

以下では500名の日本人大学生に対する18項目（6点 Likert）で、3因子を想定したデータを生成します。

- **Ideal L2 Self（理想 L2 自己）**: 自分が英語を流暢に使う将来像への志向
- **Ought-to L2 Self（義務的 L2 自己）**: 周囲からの期待に応えたいという動機
- **L2 Learning Experience（L2 学習経験）**: 授業や教材から得られる楽しさ

因子負荷量は $.60$--$.78$ の範囲で設定しました。これは心理尺度研究で「良好な負荷」とされる値域です。因子間相関は Ideal L2 Self と Ought-to L2 Self の間が $.45$、Ideal L2 Self と L2 Learning Experience の間が $.35$、Ought-to L2 Self と L2 Learning Experience の間が $.25$ とし、@taguchi2009 等の先行研究で報告された L2MSS 因子間相関を参考にしました。また、現実のデータで完全な単純構造が得られることはまれであるため、4箇所に $.08$--$.12$ の微小な交差負荷を意図的に設定しています。

```{r}
#| eval: true
#| label: efa-data-simulation

set.seed(2024)
n_efa <- 500

# Factor correlation matrix (oblique: factors are correlated)
Phi_efa <- matrix(c(1,   0.45, 0.35,
                     0.45, 1,   0.25,
                     0.35, 0.25, 1), nrow = 3)

# Factor scores
f_scores <- MASS::mvrnorm(n_efa, mu = c(0, 0, 0), Sigma = Phi_efa)

# Factor loading matrix (18 items x 3 factors)
Lambda_efa <- matrix(0, nrow = 18, ncol = 3)
# Ideal L2 Self (items 1-6)
Lambda_efa[1:6, 1] <- c(0.78, 0.72, 0.69, 0.75, 0.66, 0.70)
# Ought-to L2 Self (items 7-12)
Lambda_efa[7:12, 2] <- c(0.74, 0.68, 0.71, 0.65, 0.73, 0.60)
# L2 Learning Experience (items 13-18)
Lambda_efa[13:18, 3] <- c(0.76, 0.70, 0.73, 0.67, 0.64, 0.71)
# Small cross-loadings for realism
Lambda_efa[1, 2] <- 0.12; Lambda_efa[3, 3] <- 0.10
Lambda_efa[8, 1] <- 0.08; Lambda_efa[14, 1] <- 0.11

raw_efa <- f_scores %*% t(Lambda_efa) + matrix(rnorm(n_efa * 18, sd = 0.5), nrow = n_efa)
likert_efa <- apply(raw_efa, 2, function(x) {
  pmin(pmax(round(scales::rescale(x, to = c(1, 6))), 1), 6)
})
motivation <- as.data.frame(likert_efa)
names(motivation) <- paste0("M", sprintf("%02d", 1:18))
```

#### 相関行列から因子構造を予想する

EFA を実行する前に、まず項目間の相関行列を可視化します。因子分析は「相関行列をモデル化する手法」なので、相関行列に何のまとまりも見えない場合、きれいな因子構造を期待するのは難しくなります。

```{r}
#| eval: true
#| label: efa-correlation-heatmap
#| fig-width: 8
#| fig-height: 7
#| fig-cap: "EFA 前に確認する観測相関行列（Observed Correlation Matrix before EFA）"

motivation_cor <- cor(motivation)

motivation_cor |>
  as.data.frame() |>
  tibble::rownames_to_column("Item1") |>
  tidyr::pivot_longer(-Item1, names_to = "Item2", values_to = "r") |>
  dplyr::mutate(
    Item1 = factor(Item1, levels = names(motivation)),
    Item2 = factor(Item2, levels = rev(names(motivation)))
  ) |>
  ggplot(aes(Item1, Item2, fill = r)) +
  geom_tile(color = "white", linewidth = 0.2) +
  scale_fill_gradient2(
    low = "#2166ac", mid = "white", high = "#b2182b",
    midpoint = 0, limits = c(-1, 1), name = "r"
  ) +
  coord_equal() +
  labs(x = NULL, y = NULL) +
  theme_minimal(base_size = 11) +
  theme(
    axis.text.x = element_text(angle = 90, hjust = 1, vjust = 0.5),
    panel.grid = element_blank()
  )
```

このヒートマップで M01--M06、M07--M12、M13--M18 の3つのブロックが見えれば、3因子構造の仮説と整合的です。ブロック内相関が高く、ブロック間相関が中程度であれば、斜交回転を用いる EFA に進む根拠になります。逆に、全項目が一様に高く相関している場合は1因子モデルや二次因子モデルを検討し、ほとんど相関がない場合は因子分析そのものの適用を見直します。

共分散行列から相関行列への変換は `cov2cor(cov(motivation))` で確認できます。相関行列は各項目を標準化した共分散行列なので、項目の単位や分散の違いを取り除いた上で「項目同士がどの程度一緒に動くか」を見ています。

#### 因子数の決定

ここでは `fa.parallel()` に `fa = "fa"` を指定し、**因子分析用の平行分析**を実行します。PCA の成分数決定に使った `fa = "pc"` とは異なり、共通分散のみを対象にした基準値が生成される点に注意します。

```{r}
#| eval: true
#| label: efa-parallel-analysis
#| fig-width: 8
#| fig-height: 6

# Parallel analysis (Horn's method)
set.seed(2024)
pa_result <- psych::fa.parallel(motivation, fm = "ml", fa = "fa", n.iter = 100,
                                 main = "Parallel Analysis: L2 Motivation Questionnaire")
```

出力の読み方: 図中の青い三角線（実データ）が赤い破線（ランダムデータ）を上回る因子数が、平行分析による推奨因子数です。コンソールにも `Parallel analysis suggests that the number of factors = X` と表示されます。この値を、理論的な予測（ここでは D&ouml;rnyei の3因子構造）と照合します。

#### EFA の前提検定: KMO と Bartlett 検定

因子分析を実行する前に、データが因子分析に適しているかを確認する2つの事前検定があります。

- **KMO（Kaiser-Meyer-Olkin）指標**: 変数間の偏相関が十分に小さいか（＝共通因子による説明が期待できるか）を測定します。ここで偏相関とは、他のすべての変数を統制した上での2変数間の相関です。値は 0--1 の範囲で、0.60 以上が許容水準、0.80 以上が良好とされます [@kaiser1974]。
- **Bartlett の球面性検定**: 相関行列が単位行列（＝変数間に相関がない）と等しいかどうかを検定します。有意（$p < .05$）であれば、変数間に因子分析で捉えるべき相関構造が存在すると判断できます。

```{r}
#| eval: true
#| label: efa-kmo-bartlett

# KMO measure of sampling adequacy
kmo_result <- psych::KMO(motivation)

# Bartlett's test of sphericity
bartlett_result <- psych::cortest.bartlett(cor(motivation), n = nrow(motivation))

tibble(
  Test = c("KMO (Overall MSA)", "Bartlett χ²", "Bartlett df", "Bartlett p"),
  Value = c(
    round(kmo_result$MSA, 3),
    round(bartlett_result$chisq, 1),
    bartlett_result$df,
    ifelse(bartlett_result$p.value < .001, "< .001",
           round(bartlett_result$p.value, 3))
  )
) |>
  knitr::kable(
    caption = "EFA Prerequisite Tests",
    digits = 3,
    booktabs = TRUE
  )
```

KMO の全体値（Overall MSA）が `r round(kmo_result$MSA, 2)` であり、因子分析に適したデータであることが確認できます。Bartlett 検定も有意であり、相関行列は単位行列ではありません。これは因子分析を検討する最低条件の一つですが、因子構造の妥当性そのものは、因子数、負荷量、残差、共通性、解釈可能性を併せて確認します。

KMO < 0.60 の場合、因子分析の前提が十分に満たされていない可能性があります。対処法として (a) 多重共線性の高い変数の除外、(b) 偏相関が低い変数の除外、(c) サンプルサイズの拡大が考えられます。Bartlett 検定で有意でない場合は、相関行列が単位行列に近く、因子を抽出する十分な根拠がありません。その場合は、因子分析ではなく個別の変数レベルでの分析や尺度項目の再設計を検討します。

以下の固有値テーブルでは、Kaiser 基準（固有値 > 1）を満たす因子数を目安として確認できます。ただし、Kaiser 基準は因子数を多めに見積もりやすいため、単独では採用しません。平行分析と Kaiser 基準の結果が異なる場合は、平行分析を第一候補とし、理論的解釈可能性と解の安定性を合わせて判断します。

```{r}
#| eval: true
#| label: efa-eigenvalue-table

# Eigenvalue table — use distinct name to avoid collision with PCA section's eig_vals
efa_eig_vals <- eigen(cor(motivation))$values
data.frame(
  Factor = 1:length(efa_eig_vals),
  Eigenvalue = efa_eig_vals,
  Proportion = efa_eig_vals / sum(efa_eig_vals),
  Cumulative = cumsum(efa_eig_vals / sum(efa_eig_vals)),
  Guttman_Kaiser = ifelse(efa_eig_vals > 1, "保持候補", "保持しない")
) |>
  slice(1:length(efa_eig_vals)) |>
  knitr::kable(
    caption = "固有値とガットマン基準（Eigenvalues and Kaiser-Guttman Criterion）",
    digits = 3,
    booktabs = TRUE
  )
```

#### ガットマン基準（Kaiser-Guttman criterion）

ガットマン基準、または Kaiser-Guttman 基準は、**相関行列の固有値が 1 を超える成分・因子を保持する**という経験則です[@guttman1954; @kaiser1960]。相関行列を使う場合、各変数は標準化されて分散が 1 になります。したがって、固有値 1 は「標準化された1つの変数と同じだけの分散を説明する」という意味を持ちます。固有値が 1 未満の軸は、元の1変数より少ない情報しか説明しないため、保持しないというのがこの基準の直感です。

ただし、この直感は主に PCA の文脈で理解しやすいものです。EFA では、全分散ではなく**共通分散**をモデル化するため、相関行列の固有値 > 1 をそのまま因子数として採用すると、因子を多く取りすぎることがあります。特に項目数が多い尺度、共通性が低い項目を含む尺度、弱い因子が複数ある尺度では、ガットマン基準は過大抽出しやすくなります。

| 観点 | ガットマン基準の読み方 |
|---|---|
| 数学的な直感 | 固有値 > 1 は「標準化された1変数分より多い分散を説明する」ことを意味します |
| 長所 | 手計算でも理解しやすく、因子数候補を素早く把握できます |
| 限界 | 標本誤差・項目数・共通性の影響を受けやすく、単独では過大抽出しやすいです |
| 実務上の位置づけ | 平行分析、スクリープロット、理論、解の安定性を確認するための参考情報として使います |

本章では、ガットマン基準を「採用基準」ではなく「警告灯」として扱います。例えば、ガットマン基準が5因子、平行分析が3因子を示す場合は、5因子をそのまま採用するのではなく、3因子を主候補としつつ、4因子・5因子解で交差負荷や解釈可能性が改善するかを確認します。

#### 因子数決定のベストプラクティス

因子数は、1つの指標だけで機械的に決めるものではありません。初心者は「Kaiser 基準で固有値が 1 を超える数」と覚えがちですが、これは過大抽出につながりやすい基準です。経験者向けには、複数の候補モデルを推定し、負荷パターン・共通性・因子間相関・解釈可能性を並べて確認する手順が重要です。

| 判断材料 | 役割 | 単独採用を避ける理由 |
|---|---|---|
| 平行分析（parallel analysis） | ランダムデータを基準に、保持すべき因子数を推定します | データの分布やサンプルサイズの影響を受けるため、理論と照合します |
| スクリープロット（scree plot） | 固有値の減少が緩やかになる「肘」を確認します | 肘の位置が曖昧なデータでは判断者間で揺れます |
| ガットマン基準（Kaiser-Guttman criterion） | 固有値 > 1 の数を候補数として確認します | EFA では全分散ベースの基準になりやすく、過大抽出に注意します |
| MAP / VSS などの補助基準 | 残差相関や単純構造から候補数を点検します | パッケージや設定により推奨因子数が異なることがあります |
| 理論的予測 | 先行研究に基づく構成概念数と照合します | 理論だけでデータに合わない構造を固定しないようにします |
| 感度分析 | 因子数を1つ多く / 少なくして頑健性を確認します | 最終判断には解釈可能性と再現性の確認が必要です |

実務では、(1) 平行分析で候補数を決める、(2) スクリープロットで極端な不一致がないか確認する、(3) 因子数を ±1 したモデルも推定する、(4) 交差負荷・低共通性・因子間相関を比較する、という順に進めると判断が安定します。

#### 因子負荷の推定と回転

ここでは `psych::fa()` を用いて EFA を実行します。主要な引数は以下の通りです。

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `nfactors` | `3` | 抽出する因子数です。平行分析の結果に基づいて決定します |
| `fm` | `"ml"` | 推定法です。最尤法（ML）は適合度検定が可能です。`"pa"`（主軸法）は正規性仮定が不要です |
| `rotate` | `"promax"` | 斜交回転です。因子間相関を許容します。`"varimax"` は直交回転です |
| `cor` | （既定値） | 相関行列の種類です。既定は Pearson 相関です。Likert 5件法以下では `cor = "poly"`（ポリコリック相関）を検討します |

以下の因子負荷テーブルの読み方:

| 列 | 意味 | 実務上の判断基準 |
|---|---|---|
| ML1, ML2, ML3 | 各因子への負荷量 | 絶対値 > .40 で「実質的に意味がある」 |
| 高い正の値 | その因子との強い関連 | 1つの因子にのみ高く負荷するのが理想（単純構造） |
| 複数因子に .30 以上 | 交差負荷 | 項目の改訂や除外を検討 |

```{r}
#| eval: true
#| label: efa-estimation

fit_efa <- psych::fa(motivation, nfactors = 3, fm = "ml", rotate = "promax")

unclass(fit_efa$loadings) |>
  as.data.frame() |>
  tibble::rownames_to_column("Item") |>
  dplyr::mutate(dplyr::across(starts_with("ML"), ~ ifelse(abs(.x) < .30, NA_real_, .x))) |>
  knitr::kable(
    caption = "EFA Factor Loadings (cut = 0.30)",
    digits = 3,
    booktabs = TRUE
  )
```

上記のテーブルでは、カットオフ値 .30 以上の負荷量のみが表示されます。結果を見ると、ML1 に M01--M06（Ideal L2 Self 項目）が高く負荷し、ML2 に M07--M12（Ought-to L2 Self 項目）、ML3 に M13--M18（L2 Learning Experience 項目）が対応しています。これは @dornyei2009 の L2MSS 理論の3因子構造と一致しており、交差負荷はいずれも .30 未満で良好な単純構造が得られています[@watkins2018; @goretzko2021]。

複数因子に 0.30 以上の負荷量を持つ項目（交差負荷項目）は因子構造を曖昧にします。対処法には (a) 当該項目の削除、(b) より厳しい基準（0.40 以上）での項目選別、(c) 目標回転（target rotation）の検討があります。ただし、心理学的構成概念には本質的に複数の側面を持つ項目が存在するため、すべての交差負荷を機械的に排除するべきではありません。

#### 観測相関とモデル再現相関の比較

因子負荷量は、因子と項目の対応を読むための表です。一方で、因子分析が統計モデルとして十分かを考えるには、モデルが観測相関行列 $\mathbf{R}$ をどれだけ再現できているかを確認します。斜交 EFA の場合、モデルが予測する相関行列は次のように計算できます。

$$\hat{\mathbf{R}} = \hat{\boldsymbol{\Lambda}}\hat{\boldsymbol{\Phi}}\hat{\boldsymbol{\Lambda}}^\top + \hat{\boldsymbol{\Psi}}$$

観測相関 $\mathbf{R}$ と含意相関 $\hat{\mathbf{R}}$ の差 $\mathbf{R} - \hat{\mathbf{R}}$ が**残差相関**です。残差相関が小さければ、少数の因子で項目間相関をよく説明できていると判断できます。

```{r}
#| eval: true
#| label: efa-model-implied-correlation

L_hat <- as.matrix(unclass(fit_efa$loadings))
Phi_hat <- fit_efa$Phi
if (is.null(Phi_hat)) {
  Phi_hat <- diag(ncol(L_hat))
}
Psi_hat <- diag(fit_efa$uniquenesses)

model_cor_efa <- L_hat %*% Phi_hat %*% t(L_hat) + Psi_hat
dimnames(model_cor_efa) <- dimnames(motivation_cor)

resid_cor_efa <- motivation_cor - model_cor_efa
upper_cells <- upper.tri(resid_cor_efa)

tibble::tibble(
  Index = c(
    "平均絶対観測相関（mean |observed r|）",
    "平均絶対残差相関（mean |residual r|）",
    "最大絶対残差相関（max |residual r|）"
  ),
  Value = c(
    mean(abs(motivation_cor[upper_cells])),
    mean(abs(resid_cor_efa[upper_cells])),
    max(abs(resid_cor_efa[upper_cells]))
  )
) |>
  knitr::kable(
    caption = "観測相関とEFA残差相関の要約（Observed and Residual Correlations）",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: efa-residual-correlation-heatmap
#| fig-width: 8
#| fig-height: 7
#| fig-cap: "EFA モデルの残差相関行列（Residual Correlation Matrix from EFA）"

resid_cor_efa |>
  as.data.frame() |>
  tibble::rownames_to_column("Item1") |>
  tidyr::pivot_longer(-Item1, names_to = "Item2", values_to = "resid_r") |>
  dplyr::filter(Item1 != Item2) |>
  dplyr::mutate(
    Item1 = factor(Item1, levels = names(motivation)),
    Item2 = factor(Item2, levels = rev(names(motivation)))
  ) |>
  ggplot(aes(Item1, Item2, fill = resid_r)) +
  geom_tile(color = "white", linewidth = 0.2) +
  scale_fill_gradient2(
    low = "#2166ac", mid = "white", high = "#b2182b",
    midpoint = 0, limits = c(-0.20, 0.20),
    oob = scales::squish, name = "Residual r"
  ) +
  coord_equal() +
  labs(x = NULL, y = NULL) +
  theme_minimal(base_size = 11) +
  theme(
    axis.text.x = element_text(angle = 90, hjust = 1, vjust = 0.5),
    panel.grid = element_blank()
  )
```

残差相関の絶対値が .10 を超える項目ペアが多い場合は、現在の因子数では相関構造を十分に再現できていない可能性があります。特定の項目ペアだけに大きな残差が残る場合は、項目文が似すぎている、同じ下位内容を共有している、逆転項目の処理に問題がある、といった局所依存を疑います。この考え方は、後の CFA における残差共分散行列の診断と同じです。

#### 回転法の選択
- **斜交回転（promax, oblimin）**: 因子間相関を許容します。心理・教育データではほぼ常に推奨されます。動機づけの下位構成概念は互いに相関するのが自然であるため、斜交回転をデフォルトとします。
- **直交回転（varimax）**: 因子間を独立と仮定。理論的に無相関と考えられる場合のみ。
- 斜交回転の結果、因子間相関が .10 未満であれば、直交回転との感度分析を行い、負荷パターンや解釈がほとんど変わらないか確認できます。

**promax と oblimin の違い**: promax は最初に varimax（直交）回転を行い、その解を目標にして斜交化する**二段階法**です。計算が高速で安定しやすい一方、間接的な方法のため最適解でない可能性があります。oblimin（delta = 0 で quartimin と呼ばれる）は因子パターンの単純構造を**直接最適化**する方法であり、理論的にはより厳密です。実際のデータではどちらを使っても結果が大きく異なることは少ないですが、因子間相関が強い場合（$r > .50$）は oblimin のほうが安定した解を得やすい傾向があります。

::: {.callout-note}
**主要な回転法の整理**

| 回転 | 種別 | 目的関数 | 推奨用途 |
|------|------|----------|----------|
| **varimax** (Kaiser 1958) | 直交 | 列の負荷分散最大化 | 因子間が無相関と仮定できるとき |
| **quartimax** | 直交 | 行の負荷分散最大化 | 一般因子を強調したいとき |
| **promax** (Hendrickson & White 1964) | 斜交（二段階） | varimax → 高次冪 → Procrustean 斜交化 | 高速・実用的なデフォルト |
| **oblimin** (Carroll 1957; quartimin = delta 0) | 斜交（直接） | クロス積最小化 | 厳密な単純構造、強い因子間相関 |
| **geomin** (Yates 1987; @asparouhov2009) | 斜交（直接） | 行ごとの負荷の幾何平均最小化 | **交差負荷を許容する現代的回転**、Mplus の ESEM デフォルト |
| **bifactor (Jennrich-Bentler 2011)** | 直交/斜交 | 一般 + グループ因子構造の探索 | bifactor 構造の探索的検出 |

**geomin の位置づけ**: ESEM や CFA in EFA framework（Mplus 系）で標準採用される現代的な斜交回転で、各項目が複数因子に弱く負荷する状況（多くの心理尺度の現実）に強く、varimax/promax よりも単純構造への過剰最適化を避けます。`psych::fa()` では `rotate = "geominQ"` で利用可能です。
:::

#### Multiverse Analysis: 分析者の選択が結果に与える影響

EFA では、(1) 回転法、(2) 因子数、(3) 推定法の選択が分析者に委ねられており、これらの選択によって結果が変わり得ます。この**分析者の恣意性**（researcher degrees of freedom）を透明化するため、複数の分析仕様を並列で示す **multiverse analysis** が有効です。以下では、まず代表的な回転法（promax / oblimin / varimax）と推定法（ML / PA）の組み合わせを図で確認し、その後に9通り（3回転法 × 3推定法）の仕様で推定値の頑健性を数値的に検証します。

回転法の違いを読む際は、次の点が重要です。回転は「因子空間の軸の向き」を変える操作なので、**因子負荷量の値は変わり得ます**。一方、同じ因子数・同じ推定法であれば、モデルが再現する相関構造や共通性は大きく変わらないはずです。したがって、回転法の比較では、負荷量の大小だけでなく、項目の主因子帰属、交差負荷、残差相関、因子一致係数（factor congruence）を合わせて見ます。

::: {.panel-tabset}

##### Promax × ML（デフォルト）

```{r}
#| eval: true
#| label: multiverse-promax-ml

fit_promax_ml <- psych::fa(motivation, nfactors = 3, fm = "ml", rotate = "promax")
psych::fa.diagram(fit_promax_ml, main = "Promax × ML", cut = 0.30)
```

##### Oblimin × ML

```{r}
#| eval: true
#| label: multiverse-oblimin-ml

fit_oblimin_ml <- psych::fa(motivation, nfactors = 3, fm = "ml", rotate = "oblimin")
psych::fa.diagram(fit_oblimin_ml, main = "Oblimin × ML", cut = 0.30)
```

##### Varimax × ML

```{r}
#| eval: true
#| label: multiverse-varimax-ml

fit_varimax_ml <- psych::fa(motivation, nfactors = 3, fm = "ml", rotate = "varimax")
psych::fa.diagram(fit_varimax_ml, main = "Varimax × ML", cut = 0.30)
```

##### Promax × PA（主軸法）

```{r}
#| eval: true
#| label: multiverse-promax-pa

fit_promax_pa <- psych::fa(motivation, nfactors = 3, fm = "pa", rotate = "promax")
psych::fa.diagram(fit_promax_pa, main = "Promax × PA (Principal Axis)", cut = 0.30)
```

##### Oblimin × PA

```{r}
#| eval: true
#| label: multiverse-oblimin-pa

fit_oblimin_pa <- psych::fa(motivation, nfactors = 3, fm = "pa", rotate = "oblimin")
psych::fa.diagram(fit_oblimin_pa, main = "Oblimin × PA", cut = 0.30)
```

##### 因子負荷量の比較

```{r}
#| eval: true
#| label: multiverse-grid-fit
#| include: false

fit_varimax_pa <- psych::fa(motivation, nfactors = 3, fm = "pa", rotate = "varimax")
fit_promax_minres <- psych::fa(motivation, nfactors = 3, fm = "minres", rotate = "promax")
fit_oblimin_minres <- psych::fa(motivation, nfactors = 3, fm = "minres", rotate = "oblimin")
fit_varimax_minres <- psych::fa(motivation, nfactors = 3, fm = "minres", rotate = "varimax")

multiverse_specs <- tibble::tibble(
  Specification = c(
    "Promax × ML", "Oblimin × ML", "Varimax × ML",
    "Promax × PA", "Oblimin × PA", "Varimax × PA",
    "Promax × minres", "Oblimin × minres", "Varimax × minres"
  ),
  Rotation = c(
    "promax", "oblimin", "varimax",
    "promax", "oblimin", "varimax",
    "promax", "oblimin", "varimax"
  ),
  Extraction = c(
    "ML", "ML", "ML",
    "PA", "PA", "PA",
    "minres", "minres", "minres"
  ),
  Fit = list(
    fit_promax_ml, fit_oblimin_ml, fit_varimax_ml,
    fit_promax_pa, fit_oblimin_pa, fit_varimax_pa,
    fit_promax_minres, fit_oblimin_minres, fit_varimax_minres
  )
)

target_for_item <- function(items) {
  item_number <- as.integer(gsub("[^0-9]", "", items))
  dplyr::case_when(
    item_number <= 6 ~ "Ideal L2 Self",
    item_number <= 12 ~ "Ought-to L2 Self",
    TRUE ~ "L2 Learning Experience"
  )
}

extract_primary_spec <- function(fit, specification, rotation, extraction) {
  loads <- as.matrix(unclass(fit$loadings))
  colnames(loads) <- paste0("F", seq_len(ncol(loads)))

  load_long <- loads |>
    as.data.frame() |>
    tibble::rownames_to_column("Item") |>
    tidyr::pivot_longer(-Item, names_to = "Factor", values_to = "Loading") |>
    dplyr::mutate(TargetConstruct = target_for_item(Item))

  factor_map <- load_long |>
    dplyr::group_by(Factor, TargetConstruct) |>
    dplyr::summarise(MeanAbsLoading = mean(abs(Loading)), .groups = "drop_last") |>
    dplyr::slice_max(MeanAbsLoading, n = 1, with_ties = FALSE) |>
    dplyr::ungroup() |>
    dplyr::select(Factor, PrimaryConstruct = TargetConstruct)

  load_long |>
    dplyr::group_by(Item) |>
    dplyr::arrange(dplyr::desc(abs(Loading)), .by_group = TRUE) |>
    dplyr::summarise(
      Factor = dplyr::first(Factor),
      PrimaryLoading = dplyr::first(Loading),
      PrimaryAbsLoading = abs(dplyr::first(Loading)),
      SecondAbsLoading = abs(Loading[2]),
      .groups = "drop"
    ) |>
    dplyr::left_join(factor_map, by = "Factor") |>
    dplyr::mutate(
      Specification = specification,
      Rotation = rotation,
      Extraction = extraction,
      TargetConstruct = target_for_item(Item),
      MatchesTheory = PrimaryConstruct == TargetConstruct
    )
}

summarise_spec <- function(fit, specification, rotation, extraction, ref_loadings, observed_cor) {
  loads <- as.matrix(unclass(fit$loadings))
  colnames(loads) <- paste0("F", seq_len(ncol(loads)))

  primary <- extract_primary_spec(fit, specification, rotation, extraction)

  phi <- fit$Phi
  if (is.null(phi)) {
    phi <- diag(ncol(loads))
  }
  psi <- diag(fit$uniquenesses)
  model_cor <- loads %*% phi %*% t(loads) + psi
  dimnames(model_cor) <- dimnames(observed_cor)
  resid_cor <- observed_cor - model_cor
  upper <- upper.tri(resid_cor)

  congruence <- psych::factor.congruence(ref_loadings, loads)
  best_congruence <- apply(abs(congruence), 1, max)

  tibble::tibble(
    Specification = specification,
    Rotation = rotation,
    Extraction = extraction,
    MeanPrimaryLoading = mean(primary$PrimaryAbsLoading),
    MinPrimaryLoading = min(primary$PrimaryAbsLoading),
    CrossLoadingItems = sum(primary$SecondAbsLoading >= .30),
    TheoryMismatchItems = sum(!primary$MatchesTheory),
    LowCommunalityItems = sum(fit$communality < .30),
    MeanAbsResidual = mean(abs(resid_cor[upper])),
    MaxAbsResidual = max(abs(resid_cor[upper])),
    MeanBestCongruence = mean(best_congruence),
    MinBestCongruence = min(best_congruence)
  )
}

ref_loadings <- as.matrix(unclass(fit_promax_ml$loadings))
colnames(ref_loadings) <- paste0("F", seq_len(ncol(ref_loadings)))

multiverse_primary <- dplyr::bind_rows(lapply(seq_len(nrow(multiverse_specs)), function(i) {
  extract_primary_spec(
    multiverse_specs$Fit[[i]],
    multiverse_specs$Specification[i],
    multiverse_specs$Rotation[i],
    multiverse_specs$Extraction[i]
  )
}))

multiverse_stats <- dplyr::bind_rows(lapply(seq_len(nrow(multiverse_specs)), function(i) {
  summarise_spec(
    multiverse_specs$Fit[[i]],
    multiverse_specs$Specification[i],
    multiverse_specs$Rotation[i],
    multiverse_specs$Extraction[i],
    ref_loadings,
    motivation_cor
  )
}))
```

```{r}
#| eval: true
#| label: multiverse-stats-table

multiverse_stats |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "EFA Multiverse Analysis: 回転法・推定法による推定値の安定性",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: multiverse-item-stability

item_stability <- multiverse_primary |>
  dplyr::group_by(Item, TargetConstruct) |>
  dplyr::summarise(
    MinPrimaryLoading = min(PrimaryAbsLoading),
    MaxPrimaryLoading = max(PrimaryAbsLoading),
    RangePrimaryLoading = MaxPrimaryLoading - MinPrimaryLoading,
    MaxSecondLoading = max(SecondAbsLoading),
    MismatchSpecs = sum(!MatchesTheory),
    .groups = "drop"
  )

item_stability |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "項目別の頑健性診断: 主負荷量の範囲・最大交差負荷・理論帰属からのズレ",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: multiverse-primary-loading-plot
#| fig-width: 10
#| fig-height: 7

ggplot(
  multiverse_primary,
  aes(x = Item, y = PrimaryAbsLoading, color = Rotation, shape = Extraction)
) +
  geom_point(size = 2.5, alpha = 0.85, position = position_dodge(width = 0.55)) +
  geom_hline(yintercept = 0.40, linetype = "dashed", color = "grey50") +
  facet_wrap(~ TargetConstruct, scales = "free_x") +
  labs(
    title = "EFA Multiverse Analysis: Rotation and Extraction Choices",
    subtitle = "Each point is the absolute primary loading under one analysis specification",
    y = "Absolute primary loading",
    x = NULL
  ) +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    plot.title = element_text(face = "bold")
  )
```

:::

#### Multiverse Analysis の解釈

上記の比較から、以下の点を確認します。

1. **項目の因子帰属が安定しているか**: 全ての仕様で同じ項目が同じ構成概念に帰属していれば、因子構造は頑健です。`MismatchSpecs` が 1 以上の項目は、仕様によって帰属が変わる不安定項目です。
2. **負荷量の大きさが安定しているか**: `RangePrimaryLoading` が小さければ、回転法・推定法を変えても主負荷量は安定しています。大きく異なる場合、その項目は因子の定義に強く依存している可能性があります。
3. **交差負荷が増えないか**: `MaxSecondLoading` が .30 を超える項目は、少なくとも一部の仕様で交差負荷を持ちます。主分析では単純構造に見えても、別仕様で交差負荷が出る項目は慎重に扱います。
4. **モデルが相関構造を同程度に再現しているか**: `MeanAbsResidual` と `MaxAbsResidual` が仕様間で大きく変わらなければ、回転法の違いは主に解釈軸の違いであり、相関構造の再現性は安定しています。
5. **因子一致係数が高いか**: `MeanBestCongruence` と `MinBestCongruence` は、基準解（ここでは Promax × ML）と各仕様の因子構造の類似性を表します。一般に .95 以上なら非常に類似、.85--.94 ならおおむね類似、.85 未満なら意味のある違いがある可能性があります[@lorenzoseva2006]。

結論として、回転法によって**個々の因子負荷量の数値は変わり得ます**。しかし、頑健な因子構造であれば、項目の主因子帰属、主負荷量の相対的な大小、共通性、残差相関、因子一致係数は大きく変わりません。Multiverse analysis の結果は、論文の補遺（supplementary materials）に掲載するか、本文中で「主要な分析仕様は promax × ML とし、oblimin、varimax、PA、minres を含む代替仕様でも因子構造の頑健性を確認した」と報告すると透明性が高まります。

#### 因子負荷の可視化

因子負荷量を複数の角度から確認します。負荷量テーブルでは数値を精査し、ヒートマップとバーチャートでパターンを視覚的に把握し、構造図で因子と項目の対応を確認します。

::: {.panel-tabset}

##### 負荷量テーブル（sjPlot）

`sjPlot::tab_fa()` は因子負荷量・共通性・Cronbach's $\alpha$ を整形されたテーブルで出力します。項目が因子ごとにソートされ、主負荷が一目で分かります。主要な引数は次の通りです。

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `fctr.load.tlrn` | `0.1` | この値未満の負荷量を非表示にする閾値。`.30` にすると微弱な負荷が消え見やすくなるが、交差負荷を見落とす可能性がある |
| `show.cronb` | `TRUE` | 各因子の Cronbach's $\alpha$ を表示。信頼性の簡易確認に便利 |
| `show.comm` | `TRUE` | 各項目の共通性（communality）を表示。共通性が低い（< .20）項目は除外を検討 |
| `sort` | `TRUE` | 項目を主負荷の因子ごとにソートして表示 |

```{r}
#| eval: true
#| label: efa-loading-table

# Formatted factor loading table
sjPlot::tab_fa(
  motivation,
  rotation = "promax",
  method = "ml",
  nmbr.fctr = 3,
  fctr.load.tlrn = 0.1,
  sort = TRUE,
  title = "EFA Results: L2 Motivation Questionnaire",
  show.cronb = TRUE,
  show.comm = TRUE,
  digits = 2,
  encoding = "UTF-8"
)
```

##### 負荷量テーブル（gt）

各項目の主負荷因子・最大負荷量・共通性（$h^2$）・独自性（$u^2$）を一覧します。共通性が .30 未満の項目は因子で十分に説明されておらず、除外候補となります。

#### 斜交回転と共通性の計算
直交回転（varimax）では、共通性は各因子の負荷量の二乗和 $h^2_j = \sum_k \lambda_{jk}^2$ で計算できます。しかし**斜交回転（promax, oblimin）では因子間に相関があるため、この単純な二乗和は正確な共通性になりません**。斜交回転の共通性は因子間相関行列 $\boldsymbol{\Phi}$ を考慮した $h^2_j = \boldsymbol{\lambda}_j^\top \boldsymbol{\Phi} \boldsymbol{\lambda}_j$ で算出されます。以下のコードでは `psych::fa()` が内部で正確に計算した `$communality` を直接参照しています。

```{r}
#| eval: true
#| label: efa-summary-gt

# Summary table with gt
load_tbl <- fit_efa$loadings |>
  unclass() |>
  as.data.frame() |>
  rownames_to_column(var = "Item")

comm_df <- tibble(Item = names(fit_efa$communality),
                  Communality = fit_efa$communality)

efa_summary <- load_tbl |>
  pivot_longer(-Item, names_to = "Factor", values_to = "Loading") |>
  group_by(Item) |>
  mutate(Abs = abs(Loading)) |>
  slice_max(order_by = Abs, n = 1, with_ties = FALSE) |>
  ungroup() |>
  left_join(comm_df, by = "Item") |>
  mutate(Uniqueness = 1 - pmin(Communality, 1)) |>
  dplyr::select(Item, Factor, Loading, Communality, Uniqueness) |>
  arrange(Factor, desc(abs(Loading)))

efa_summary |>
  knitr::kable(
    caption = "EFA Summary Table: Maximum Loading, Communality (h²), and Uniqueness (u²)",
    digits = 3,
    booktabs = TRUE
  )
```

サマリーテーブルから、全18項目の共通性（$h^2$）が .30 以上であることを確認します。共通性が低い項目は因子によって十分に説明されていません。独自性（$u^2 = 1 - h^2$）は各項目の因子で説明されない分散の割合を表します。独自性が高い（例: $u^2 > 0.70$）項目は、測定誤差や特殊因子の影響が大きく、共通因子モデルに適合していない可能性があります。

##### ヒートマップ

ヒートマップでは、行（項目）× 列（因子）のセルの色で負荷量の大きさと方向を直感的に把握します。赤が強い正の負荷、青が負の負荷を示します。理想的な単純構造では、各項目が1つの因子にのみ濃い赤を示します。

```{r}
#| eval: true
#| label: efa-heatmap
#| fig-width: 8
#| fig-height: 10

heat_tbl <- as.data.frame(unclass(fit_efa$loadings)) |>
  rownames_to_column("Item") |>
  pivot_longer(-Item, names_to = "Factor", values_to = "Loading")

ggplot(heat_tbl, aes(Factor, Item, fill = Loading)) +
  geom_tile() +
  scale_fill_gradient2(low = "#2166ac", mid = "white", high = "#b2182b", midpoint = 0) +
  labs(
    title = "EFA Factor Loadings Heatmap",
    subtitle = "L2 Motivation Questionnaire (promax rotation)"
  ) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

ヒートマップの赤色セルは高い因子負荷（当該因子と項目の強い関連）を、青色や白色は低い負荷を示します。理想的な単純構造では、各項目が1つの因子にのみ赤いセルを持ち、他の因子は白色となります。

##### バーチャート

バーチャートでは、各項目の絶対負荷量を因子別に並列比較します。各項目がどの因子に最も強く負荷しているかが一目で分かります。

```{r}
#| eval: true
#| label: efa-loading-barplot
#| fig-width: 10
#| fig-height: 8

# Factor loading bar plot
loadings_df_bar <- as.data.frame.matrix(unclass(fit_efa$loadings))
loadings_df_bar$Item <- rownames(loadings_df_bar)
loadings_long_bar <- pivot_longer(loadings_df_bar, cols = -Item, names_to = "Factor", values_to = "Loading")

ggplot(loadings_long_bar, aes(x = reorder(Item, abs(Loading)), y = abs(Loading), fill = Factor)) +
  geom_col(position = "dodge") +
  coord_flip() +
  labs(
    title = "Factor Loadings by Item",
    subtitle = "L2 Motivation Questionnaire (promax rotation)",
    x = "Item", y = "|Loading|"
  ) +
  scale_fill_brewer(palette = "Set1")
```

##### 因子構造図

`psych::fa.diagram()` は因子と項目の対応をパス図で描画します。矢印の太さが負荷量の大きさに対応し、因子間の相関も曲線で示されます。

```{r}
#| eval: true
#| label: efa-diagram
#| fig-width: 10
#| fig-height: 8

psych::fa.diagram(fit_efa, main = "Factor Structure: L2 Motivation")
```

パス図の矢印の太さは因子負荷量に比例し、太い矢印ほど因子と項目の関連が強いことを示します。因子間の曲線は因子間相関（斜交回転で許容される相関）を表し、0 でなければ因子が独立でないことを示します。

:::

##### 因子間相関マトリックス

斜交回転（promax）を使用した場合、因子間相関行列（$\boldsymbol{\Phi}$）の確認は不可欠です。因子間相関が高すぎる（$r > .85$）場合は弁別的妥当性に疑義が生じ、因子の統合を検討します[@costello2005]。逆に全ての因子間相関が .10 未満であれば、直交回転（varimax）でも同様の解釈になるかを感度分析として確認できます。

```{r}
#| eval: true
#| label: efa-factor-correlation
#| fig-width: 6
#| fig-height: 5

# Factor inter-correlation matrix (Phi)
phi_mat <- fit_efa$Phi
colnames(phi_mat) <- rownames(phi_mat) <- c("ML1:\nIdeal L2\nSelf",
                                              "ML2:\nOught-to\nL2 Self",
                                              "ML3:\nL2 Learning\nExperience")

phi_df <- as.data.frame(phi_mat) |>
  rownames_to_column("Factor1") |>
  pivot_longer(-Factor1, names_to = "Factor2", values_to = "r")

ggplot(phi_df, aes(x = Factor1, y = Factor2, fill = r)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(r, 2)), size = 5) +
  scale_fill_gradient2(low = "#2166ac", mid = "white", high = "#b2182b",
                       midpoint = 0, limits = c(-1, 1)) +
  labs(title = "Factor Inter-Correlation Matrix (Phi)",
       subtitle = "Promax rotation — moderate correlations support oblique rotation",
       fill = "r") +
  theme(axis.title = element_blank())
```

因子間相関はいずれも中程度（$.25$--$.35$ 程度）であり、因子間に相関があることを示しています。これは斜交回転の選択が適切であったことを裏付けます。同時に、.85 を大きく下回っているため、3因子の弁別的妥当性は良好です。因子間相関が .25〜.45 の範囲にあるのは、動機づけの3側面（理想自己・義務自己・学習経験）が独立ではなく緩やかに関連していることを示します。相関が .85 を超える場合は因子の弁別的妥当性が疑われ、2因子を統合すべき根拠になりえます（@brown2015）。

#### 項目選別の基準
- **因子負荷量 < 0.40**: 当該因子との関連が弱い。除外を検討。
- **交差負荷 > 0.30**: 複数因子に負荷します。解釈の曖昧さの原因になります。
- **共通性 $h^2$ < 0.30**: 因子で十分に説明されていない。項目の質を再検討。
- ただし、これらは目安であり、理論的根拠と合わせて判断すること。

これら 3 つの基準（因子負荷 < .40、交差負荷 > .30、共通性 < .30）が矛盾する場合は、(a) まず共通性を確認し測定品質の低い項目を除外、(b) 次に主負荷量が基準を満たすか確認、(c) 最後に交差負荷の有無を検討するという優先順位が実務的です。理論的に重要な項目は、統計基準をわずかに下回っても保持する判断もありえます。

#### 信頼性（$\omega$ 係数・$\alpha$ 係数）

内的整合性の指標として、McDonald の $\omega$（オメガ）が推奨されます[@mcneish2018; @flora2020]。Cronbach の $\alpha$ は因子負荷が等しいという仮定（本質的タウ等価モデル）に敏感であり、因子負荷の不均一性、多次元性、誤差相関によって過小評価にも過大評価にもなりえます。SLA の尺度開発では、因子負荷量が等しくない（tau-equivalent でない）場合が多くあります。$\omega$ はこの不均一性を考慮できるため有用ですが、$\omega$ も推定した因子モデルに依存する点に注意します。

```{r}
#| eval: true
#| label: efa-reliability

# Factor 1 items (Ideal L2 Self): M01-M06
items_f1 <- paste0("M", sprintf("%02d", 1:6))
# Factor 2 items (Ought-to L2 Self): M07-M12
items_f2 <- paste0("M", sprintf("%02d", 7:12))
# Factor 3 items (L2 Learning Experience): M13-M18
items_f3 <- paste0("M", sprintf("%02d", 13:18))

compute_reliability <- function(data, items, factor_name) {
  alpha_res <- psych::alpha(data[, items])
  omega_res <- psych::omega(data[, items], plot = FALSE)
  tibble(
    Factor = factor_name,
    `Cronbach's α` = alpha_res$total$raw_alpha,
    `McDonald's ω` = omega_res$omega.tot,
    `N items` = length(items)
  )
}

efa_rel_tbl <- bind_rows(
  compute_reliability(motivation, items_f1, "Ideal L2 Self"),
  compute_reliability(motivation, items_f2, "Ought-to L2 Self"),
  compute_reliability(motivation, items_f3, "L2 Learning Experience")
)

efa_rel_tbl |>
  knitr::kable(
    caption = "Internal Consistency Reliability",
    digits = 3,
    booktabs = TRUE
  )
```

上記テーブルでは、3因子すべてで McDonald's $\omega$ が .70 を超えており、内的整合性は許容水準を満たしています。$\alpha$ も参考値として併記していますが、$\omega$ のほうが因子構造を正確に反映した信頼性指標であるため[@mcneish2018]、$\omega$ を優先して報告します。

#### 信頼性の報告
- $\omega$（オメガ）を第一指標として報告し、$\alpha$ は比較可能性のため補助的に記載する[@flora2020]。
- $\omega > .70$ が一般的な目安だが、探索段階では .65 程度でも許容されることがある。
- 95% 信頼区間も併記すると情報量が増す。

::: {.callout-note}
**信頼性・収束的/弁別的妥当性の指標一覧**

|  指標 | 定義/数式 | 用途 | カットオフ |
|------|-----------|------|-----------|
| **Cronbach $\alpha$** | $\frac{k}{k-1}\left(1-\frac{\sum \sigma_i^2}{\sigma_{total}^2}\right)$ | 内的整合性 (tau-equivalent 仮定) | $> .70$ |
| **McDonald $\omega$** | $\frac{(\sum \lambda_i)^2}{(\sum \lambda_i)^2 + \sum \theta_i^2}$ | 一般的内的整合性 (congeneric 仮定) | $> .70$ |
| **$\omega_h$** (hierarchical) | bifactor の一般因子 / 全分散 | bifactor の一般因子の信頼性 | $> .70$ で一般因子解釈可 |
| **$\omega_s$** (subscale) | グループ因子 / 全分散 | bifactor の subscale 信頼性 | $> .50$ |
| **Coefficient H** [@hancock2001] | $H = \left(1 + \left(\sum \frac{\lambda_i^2}{1-\lambda_i^2}\right)^{-1}\right)^{-1}$ | 構成概念信頼性（最大可能信頼性） | $> .70$ |
| **AVE** (Average Variance Extracted, Fornell-Larcker 1981) | $\frac{\sum \lambda_i^2}{\sum \lambda_i^2 + \sum \theta_i^2}$ | **収束的妥当性** | $> .50$ |
| **CR** (Composite Reliability) | $\omega$ と等価 | 構成概念信頼性 | $> .70$ |
| **Fornell-Larcker 基準** | 各構成概念の $\sqrt{\text{AVE}} > $ 因子間相関 | **弁別的妥当性** | 不等式が成立 |
| **HTMT** (Heterotrait-Monotrait Ratio, Henseler et al. 2015) | 異特性間相関 / 同特性内相関の幾何平均 | **弁別的妥当性 (近年推奨)** | $< .85$ (厳しめ) または $< .90$ |

実装: `semTools::compRelSEM(fit)` で $\omega$、`semTools::AVE(fit)` で AVE、`semTools::htmt(model, data)` で HTMT が得られます。Bifactor 信頼性 ($\omega_h$, $\omega_s$) は `psych::omega()` または `BifactorIndicesCalculator` パッケージで算出可能です。SEM 論文では AVE と HTMT の両方を報告するのが現代的な慣行です。
:::

::: {.callout-tip}
**因子不定性 (factor indeterminacy) — Maraun (1996), Guttman (1955)**

EFA/CFA の理論的根本問題として、因子得点は観測変数だけからは**一意に定まらない**ことが知られています（@maraun1996）。同じ因子モデルから無限通りの因子得点が導出可能で、Thurstone 法・Bartlett 法・Anderson-Rubin 法・ten Berge 法は「代表者」のひとつに過ぎません。これは数学的不定性であり、サンプルサイズを増やしても解消しません。

実務的含意: (a) 因子得点を「真の潜在変数値」と見なすことには限界がある、(b) 後続分析で因子得点を目的変数として使う際は SEM (`sem()`) で測定モデルを内蔵させたほうが理論的に整合する、(c) 因子得点間の相関は推定法によって変わる（Anderson-Rubin は無相関、Thurstone は相関あり）。Heywood case や empirical underidentification と並ぶ EFA/CFA の構造的限界として理解しておくと、SEM の値打ちがより明確になります。
:::

#### EFA における Heywood ケースの実例

Heywood ケース（推定共通性 $\hat{h}_j^2 \geq 1$ または独自分散 $\hat{u}_j^2 \leq 0$）は EFA 実務で最も頻繁に遭遇する病理の一つです。`psych::fa()` は Heywood 候補を出力に明示します。以下は本章の L2MSS データで Heywood の有無を診断する手順です。

```{r}
#| eval: true
#| label: efa-heywood-diagnostics

# Heywood diagnostic: communalities outside [0, 1]
fit_efa_check <- psych::fa(motivation, nfactors = 3, fm = "ml", rotate = "promax")

heywood_tbl <- tibble::tibble(
  Item = rownames(fit_efa_check$communality |> as.matrix()),
  Communality = as.numeric(fit_efa_check$communality),
  Uniqueness = 1 - as.numeric(fit_efa_check$communality),
  Heywood = Communality >= 1 | Uniqueness <= 0
) |>
  dplyr::arrange(dplyr::desc(Communality))

heywood_tbl |>
  knitr::kable(
    caption = "Communality / Uniqueness Diagnosis (Heywood Detection)",
    digits = 3,
    booktabs = TRUE
  )
```

Heywood が検出された場合の対処法は @chen2001 の枠組みに従って原因別に選択します:

- **サンプル変動** (true $\Theta$ が 0 に近い): サンプル増、または `blavaan` でベイズ推定し弱情報事前分布 $\Theta_{jj} \sim \mathrm{IG}(\alpha, \beta)$ で正則化（第 7 章参照）
- **モデル誤指定** (因子数過大、双子指標、項目内容の重複): 因子数削減、項目統合、`fa(... cor = "poly", residuals = TRUE)` で残差相関の確認
- **空間的非識別** (2 指標因子・極端なサンプル特性): 等値制約を追加、3 指標以上に拡張、または `psych::fa(... SMC = TRUE)` で初期共通性に Squared Multiple Correlation を使用

`psych::fa()` は Heywood 検出時に共通性を $1 - \mathtt{eps}$ に強制クリッピングしますが、これは推定値を歪めます。本格的には ridge factor analysis (`psych::fa(..., correct = 0.5)`) や Bayesian regularization が現代的解決策です。

#### 因子得点と尺度得点の計算: EFA/CFA の結果を後続分析に活かす

EFA で因子構造を確認し、信頼性を検証した後、多くの SLA 研究では**因子ごとの得点**を計算して後続の分析（回帰分析、t 検定、ANOVA 等）に使用します。得点の計算方法には大きく分けて3つのアプローチがあります。

#### 方法1: 尺度得点（Sum Score / Mean Score）

最も簡便な方法は、各因子に属する項目の**平均値**または**合計値**を計算する方法です。SLA 研究で最も広く使われています。

```{r}
#| eval: true
#| label: efa-scale-scores

scale_scores <- tibble(
  id = 1:nrow(motivation),
  Ideal_L2_Self    = rowMeans(motivation[, paste0("M", sprintf("%02d", 1:6))]),
  Ought_to_L2_Self = rowMeans(motivation[, paste0("M", sprintf("%02d", 7:12))]),
  L2_Learning_Exp  = rowMeans(motivation[, paste0("M", sprintf("%02d", 13:18))])
)

scale_scores |>
  pivot_longer(-id, names_to = "Scale", values_to = "Score") |>
  ggplot(aes(x = Score, fill = Scale)) +
  geom_histogram(bins = 30, alpha = 0.7, position = "identity") +
  facet_wrap(~ Scale, scales = "free_y") +
  labs(title = "Distribution of Scale Scores (Mean of Items)",
       subtitle = "Higher scores indicate stronger endorsement of each construct",
       x = "Scale Score (1-6)", y = "Frequency") +
  theme(legend.position = "none")
```

尺度得点は直感的で解釈が容易ですが、**全項目を同じ重みで平均する**ため、因子負荷量の違いを反映しません。また、測定誤差を考慮しないため、因子間の関係を過小推定する傾向があります。

#### 方法2: 因子得点（Factor Scores）

`psych::factor.scores()` は、因子負荷量に基づく**重み付き得点**を計算します。負荷量の高い項目をより強く反映できるため、探索的な個人差の可視化や予備分析に有用です。ただし、因子得点は真値ではなく、後続分析では推定誤差を持つ予測値として扱う必要があります。

```{r}
#| eval: true
#| label: efa-factor-scores

fs <- psych::factor.scores(motivation, fit_efa, method = "Thurstone")

fs_comparison <- tibble(
  id = 1:nrow(motivation),
  # Method 1: Simple mean
  Scale_Mean = scale_scores$Ideal_L2_Self,
  # Method 2: Factor score (Thurstone/regression)
  Factor_Score = fs$scores[, "ML1"]
)

# Correlation between methods
cor_methods <- cor(fs_comparison$Scale_Mean, fs_comparison$Factor_Score)

fs_comparison |>
  ggplot(aes(x = Scale_Mean, y = Factor_Score)) +
  geom_point(alpha = 0.3, size = 1.5) +
  geom_smooth(method = "lm", color = "#e41a1c", se = FALSE) +
  labs(title = "Scale Score vs. Factor Score (Ideal L2 Self)",
       subtitle = glue::glue("r = {round(cor_methods, 3)} — high but not identical"),
       x = "Scale Score (simple mean)", y = "Factor Score (Thurstone method)") +
  theme(plot.title = element_text(face = "bold"))
```

#### 因子得点の推定法

| 方法 | 関数指定 | 特徴 |
|------|---------|------|
| **Thurstone（回帰法）** | `method = "Thurstone"` | 因子負荷量と相関行列から重み付き合成。最も一般的 |
| **Bartlett 法** | `method = "Bartlett"` | 因子得点の推定値が不偏。交差負荷がある場合に頑健 |
| **tenBerge 法** | `method = "tenBerge"` | 因子間相関を正確に保存する。斜交回転で推奨 |

実用上、3つの方法で得られる因子得点は高度に相関するため、結果に大きな違いは生じないことが多いです。ただし、交差負荷や因子間相関が大きい場合には、方法間の差も確認します。

::: {.callout-warning}
#### 因子得点は「真の潜在変数」ではありません

因子得点は、推定された因子モデルから各参加者の潜在変数値を**予測した値**です。真の潜在変数を直接観測した値ではありません。そのため、因子得点を後続の t 検定、ANOVA、回帰分析にそのまま入れると、因子得点の推定誤差を無視し、標準誤差が過小評価されることがあります。

実務上は、探索的な可視化や予備分析では因子得点を使ってよいですが、理論的な構造仮説を検証する場合は、因子得点を作ってから回帰するよりも、CFA/SEM の中で測定モデルと構造モデルを同時に推定する方が望ましいです。
:::

#### 方法3: lavaan による潜在変数得点

CFA/SEM の推定結果から `lavPredict()` で潜在変数得点を予測できます。測定モデルの構造（交差負荷の制約、残差相関等）を反映できる点で有用ですが、これも真の潜在変数そのものではなく、モデルに基づく予測値です。

```{r}
#| label: cfa-lavpredict
#| eval: false

latent_scores <- lavaan::lavPredict(fit_cfa_3f)
head(latent_scores)
# Returns: matrix with columns F1, F2, F3 (one row per respondent)
```

#### どの方法を選ぶべきか？

| 場面 | 推奨アプローチ | 理由 |
|------|---------------|------|
| **尺度開発の報告** | 尺度得点（平均） | 読者が再現しやすい |
| **EFA 後の探索的分析** | 因子得点（Thurstone） | 因子負荷量を反映 |
| **CFA 後の後続分析** | `lavPredict()` | 測定モデルを完全に反映 |
| **因果仮説の検証** | **SEM を使う**（得点を作らない） | 測定誤差を明示的に処理 |

#### 合成変数 vs SEM: いつ SEM を使うべきか

SLA 研究では「EFA/CFA で因子構造を確認 → 尺度得点を計算 → 重回帰分析」というパイプラインが長く主流でしたが、このアプローチには以下の限界があります。

1. **測定誤差の無視**: 尺度得点は測定誤差を含むため、変数間の関係が減衰する（attenuation bias）。Spearman 古典減衰式 $r_{obs} = r_{true}\sqrt{r_{xx} r_{yy}}$ により、信頼性 $\omega_x = \omega_y = .80$ の2尺度の真の相関が $.50$ でも、観測レベルでは $.50 \times \sqrt{.80 \times .80} = .50 \times .80 = .40$ に低下する（信頼性が両尺度で等しい場合、$\sqrt{r_{xx} r_{yy}} = r_{xx}$ となるため、見かけ上「信頼性そのものを掛ける」のと同じ結果になる）。
2. **不確実性の過小評価**: 尺度得点を「確定値」として扱うため、標準誤差が過小推定され、第1種の過誤率が膨張する。
3. **構造全体の整合性が見えない**: 測定モデルと構造モデルを別々に推定すると、全体としてのモデル適合を評価できない。

**SEM を推奨する場面**: 理論的な因果仮説を検証する場合（例: 動機づけ → 努力 → 英語力）。

**合成変数でよい場面**: (a) サンプルサイズが SEM の要件（$N > 200$）を満たさない、(b) 予備的・探索的な分析、(c) 群間比較（t 検定・ANOVA）で使う場合、(d) 他の研究との比較可能性を優先する場合。

実際の SLA 研究では、合成変数による分析と SEM による分析を**併記**し、結果の頑健性を示すことも有効な戦略です。

#### 合成変数を使った後続分析の例

以下のコードは、EFA で確認した3因子の尺度得点を用いて、基本的な群間比較と回帰分析を行う例です。

```{r}
#| eval: true
#| label: efa-downstream-analysis

set.seed(2024)
analysis_data <- scale_scores |>
  mutate(
    Gender = sample(c("Male", "Female"), n(), replace = TRUE),
    Proficiency = 0.35 * Ideal_L2_Self + 0.20 * L2_Learning_Exp -
                  0.15 * Ought_to_L2_Self + rnorm(n(), sd = 0.6)
  )

# --- Group comparison (t-test) ---
t_result <- t.test(Ideal_L2_Self ~ Gender, data = analysis_data)

# --- Multiple regression with scale scores ---
fit_reg <- lm(Proficiency ~ Ideal_L2_Self + Ought_to_L2_Self + L2_Learning_Exp,
              data = analysis_data)

broom::tidy(fit_reg) |>
  knitr::kable(
    caption = "Regression: Motivation Scales Predicting Proficiency",
    digits = 3,
    booktabs = TRUE
  )
```

上記の回帰分析では、Ideal L2 Self と L2 Learning Experience が英語力の有意な正の予測因子であり、Ought-to L2 Self は弱い負の予測因子です。ただし、**この推定値は測定誤差による減衰を受けている**ことに留意する必要があります。同じ仮説を SEM で検証すれば、潜在変数レベルの回帰係数が得られ、測定誤差の影響を補正した推定が可能になります（本章の SEM セクション参照）。

ここまでの EFA で3因子構造が示唆され、信頼性も確認されました。しかし EFA は「データ主導」の探索であり、得られた構造が理論的に妥当かどうかは別の検証が必要です。次に PCA との比較を整理した後、独立サンプルで因子構造を検証する**確認的因子分析（CFA）**に進みます。

#### PCA vs EFA の比較

PCA と EFA は混同されやすいですが、本質的に異なる手法です[@loewen2020]。@plonsky2015 のメタ的レビューでは、L2 研究の論文で「因子分析」と称しつつ実際には PCA を実行しているケースが多数報告されており、手法選択の混乱が SLA 分野全体に影響を与えていることが指摘されています。この節では両者の違いを明確にします。

**PCA と EFA の比較**（目的・モデル・分散の扱いが根本的に異なる）

| 観点 | PCA | EFA |
|------|-----|-----|
| 目的 | 次元縮約・可視化 | 潜在因子の探索 |
| モデルの種類 | 幾何学的変換（非モデル） | 確率モデル |
| 分散の扱い | 全分散を回収 | 共通分散のみ |
| 誤差項 | なし | 独自性（u²） |
| 回転 | 任意（通常なし） | 斜交 / 直交 |
| 因子間相関 | 直交のみ | 許容（斜交時） |
| 適合度検定 | なし | カイ二乗適合度 |
| 推奨場面（SLA） | テスト項目の要約、クラスタリングの前処理 | 尺度開発、構成概念の探索 |

#### PCA は因子分析ではない

PCA の成分は「観測変数の線形結合」であり、誤差を含みます。EFA の因子は「誤差を分離した潜在変数」です。構成概念の妥当性を検討する場合（尺度開発など）は、PCA ではなく EFA / CFA を使うべきです。

#### 使い分けの判断基準
- **PCA を使う場面**: 多変量データの可視化、予測モデルの前処理（多重共線性の解消）、変数のスクリーニング
- **EFA を使う場面**: 尺度開発の初期段階、構成概念の内部構造の探索、因子数の仮説生成
- 迷ったら EFA を選ぶ方が安全です。PCA の結果は EFA とほぼ同じになることもありますが、解釈の枠組みが異なります。

### 確認的因子分析（CFA）

::: {.callout-tip}
**1 分プレビュー (CFA)**

「3 因子構造を仮定した L2MSS モデルが、新しいデータでも適合するか」を**統計的に検証**する手法です。EFA とは違い、各項目がどの因子に負荷するかを事前に固定し、$\chi^2$・CFI・TLI・RMSEA・SRMR で適合度を評価します。Heywood ケース診断、bifactor/高次因子比較、複数群の測定不変性検証もできます。**EFA と同じデータでは絶対に行わない**（データ分割または完全に独立したサンプルが必要）。CFA で測定モデルが妥当と確認できれば、次節 SEM で構造パスを追加して理論検証へと進めます。
:::

#### コンセプト

EFA で「3因子が良い」という仮説が得られたら、次のステップは**別のデータ**（または分割した検証データ）でその構造を**確認**することです。確認的因子分析（CFA）は、事前に指定した因子構造がデータに適合するかどうかを検討する方法です[@brown2015]。

CFA の要点は、「探索で見つけた構造を、確認のデータで試す」という分業にあります。同じデータで EFA → CFA を行うと、EFA がデータ固有のノイズに適合した構造を CFA が追認するだけになりやすく、結論の一般化可能性が損なわれます。

::: {.callout-warning}
#### EFA と CFA は役割を分けます

「同じデータで EFA → CFA をしてはいけない」とだけ覚えるより、**探索と確認を分ける**と理解してください。サンプルサイズが十分にある場合は、探索用サンプルと検証用サンプルに分割します。サンプルサイズが小さく分割できない場合は、CFA を「厳密な確認」ではなく「暫定的なモデル検討」として位置づけ、独立サンプルでの再検証を明記します。
:::

SLA 研究における CFA の代表的な活用例を挙げます。@taguchi2009 は日本・中国・イランの EFL 学習者データで L2MSS の3因子構造（Ideal L2 Self / Ought-to L2 Self / L2 Learning Experience）に CFA を適用し、3か国すべてで同一の因子構造が成立することを確認しました。@kormos2008 は中高生・大学生・成人の3群で同じ動機づけモデルの CFA を実施し、年齢層間で因子構造は維持されるものの因子間相関のパターンが異なることを示しました。@dewaele2014 は FLE（Foreign Language Enjoyment）尺度を開発する過程で、享受と不安が単一次元の両極ではなく独立した構成概念であることを主成分分析で示し、その後の CFA 研究の土台を築きました。@winke2014 は SEM のチュートリアル論文の中で、テストの構成概念妥当性を CFA で検証する手順を step-by-step で解説しています。

近年では、@alhoorie2024 が L2MSS 関連の18尺度を384名の韓国 EFL 学習者に実施し、CFA で弁別的妥当性を検証した結果、多くの構成概念間で重大な弁別性の問題が発見されました。これは L2 動機づけ研究における測定の質を根本的に問い直す知見であり、適切な CFA 手続きの重要性を改めて示しています。

#### アンケート研究と言語テスティングにおける EFA/CFA の役割

EFA と CFA は、SLA 研究における2つの主要な測定場面で中核的な役割を果たします。

**（1）質問紙（アンケート）研究での尺度開発**

尺度開発は、SLA 研究で最も頻繁に EFA/CFA が適用される場面です。典型的なプロセスは次の通りです。

1. **構成概念の定義**: 理論に基づき、測定したい構成概念を明確にする（例: L2 動機づけの3側面）
2. **項目プールの作成**: 構成概念ごとに6--10項目の候補を作成（最終項目数の1.5--2倍）
3. **パイロットデータで EFA**: 探索的に因子構造を確認し、交差負荷や低共通性の項目を除外
4. **独立サンプルで CFA**: 精選された項目構成で因子構造を確認
5. **信頼性の評価**: $\omega$ で内的整合性を報告
6. **妥当性の検証**: 収束的妥当性（AVE > .50）、弁別的妥当性（因子間相関 < .85）、基準関連妥当性（外的変数との相関）

この一連の手続きは @dornyei2009 の動機づけ尺度や @horwitz1986 の言語不安尺度（FLCAS）の開発において実践されてきました。@plonsky2015 のメタレビューは、SLA 研究での EFA 実施慣行に多くの問題（因子数決定に Kaiser 基準のみ使用、直交回転の不適切な選択等）があることを指摘しており、本章で示したベストプラクティスの遵守が重要です。

**（2）言語テスティングにおける構成概念妥当性の検証**

言語テストの妥当性検証においても、CFA/SEM は不可欠な役割を果たす。テストの**構成概念妥当性**（construct validity）とは、テストが意図した能力を測定しているかどうかの根拠であり、Messick [-@messick1989] の統一的妥当性概念やKane [-@kane2013] の解釈・使用論証（Interpretation/Use Argument; IUA）の枠組みで体系化されている。

CFA はこの妥当性検証において以下の役割を担う。

- **内部構造の検証**: テストの下位技能（Reading, Listening, Grammar 等）が理論的に予測される因子構造を持つかを CFA で確認する。@bachman1982 は MTMM（多特性多方法）行列と CFA を組み合わせたコミュニケーション能力テストの妥当性検証の先駆的研究である。
- **次元性の検証**: テストが一次元（1因子）か多次元かを CFA のモデル比較（1因子 vs 多因子）で検証できる。これは IRT（項目応答理論）の前提条件の確認にも直結する（後述）。
- **テスト・バイアスの検出**: 測定不変性（本章で詳述）は、群間（男女、L1背景、習熟度レベル等）で項目が同じように機能しているかを検証する方法であり、IRT における DIF（差異項目機能; Differential Item Functioning）分析と相補的なアプローチである。

#### EFA/CFA と IRT の関係: 2つの潜在変数モデルの統一的理解

EFA/CFA と IRT は、ともに「直接観測できない潜在特性（因子/能力）が観測反応を生成する」という確率モデルに基づいています。実際、両者は数理的に深い対応関係にあります。

| 観点 | EFA/CFA | IRT |
|------|---------|-----|
| データ | 連続変数（Likert 等） | 二値（正誤）/ 順序 |
| 潜在変数 | 因子（factor） | 能力（ability, $\theta$） |
| パラメータ | 因子負荷量 $\lambda$ | 識別力 $a$（$\approx \lambda$） |
| 次元性 | 因子数で表現 | 一次元性の仮定 |
| 推定法 | ML, WLSMV | JML, MML, CMLE |

特に重要なのは、**二値・順序反応を扱うカテゴリカル CFA と IRT は、リンク関数や尺度化の違いを除けば近い表現で結びつく**という点です。Rasch モデルは、項目の識別力を等値制約した一次元項目反応モデルとして理解できます。2パラメータ IRT（2PL）は、項目ごとに識別力が異なるモデルであり、カテゴリカル因子分析では項目ごとに負荷量が異なる1因子モデルと対応します。

ただし、CFA の因子負荷量と IRT の識別力パラメータをそのまま同じ値として読むことはできません。ロジット / プロビットリンク、潜在変数の尺度化、閾値・困難度パラメータの定義、推定法が異なるためです。この対応関係は「同じ考え方を別の記法で表している」と理解すると、尺度開発では CFA、テスト分析では IRT という使い分けの背後にある統一的な枠組みが見えてきます。

テストの**一次元性の検証**は、EFA/CFA ベースのアプローチと IRT ベースのアプローチの両方で行えます。

- **EFA ベース**: 相関行列（二値データの場合はテトラコリック相関）の固有値分析で、第1固有値が第2固有値を大きく上回るかを確認します。比率 > 3--4 は目安ですが、絶対基準ではありません。
- **CFA ベース**: 1因子モデルの適合度を確認します。CFI > .95, RMSEA < .06 はよく使われる目安ですが、項目数やサンプルサイズ、局所依存の有無も合わせて判断します。
- **IRT ベース**: 残差の主成分分析（PCA of residuals）で、モデルが捉えきれていない体系的な次元がないかを確認します（第11章・第12章で詳述）。

本書では第11章（項目応答理論とラッシュモデル）と第12章（拡張ラッシュモデル）で IRT を包括的に扱います。テスティング研究では、CFA による構成概念妥当性の検証と IRT による項目分析・能力推定を**相補的に**活用するのが現代的なアプローチです。

#### 概要
1. CFA は「仮説検証」です。EFA で探索した構造をここで確認します。
2. lavaan パッケージの `cfa()` で推定します[@rosseel2012]。
3. 適合度指標（CFI, TLI, RMSEA, SRMR）だけでなく、負荷量・残差・理論との整合性で評価します。
4. **同じデータで EFA → CFA を完結させない**ことが原則です。別サンプル、分割サンプル、または独立した追試で確認します。

#### CFA の前提条件
- **理論に基づく因子構造の仮説**が必要です。EFA の結果だけでなく、先行研究に基づいて因子構造を定義します。例えば、L2MSS の3因子構造を検証する場合、@dornyei2009 の理論的枠組みと @taguchi2009 の先行 CFA 結果が根拠となります。
- 各因子に最低3つの指標変数があることが望ましいです。2つでも推定は可能ですが、識別性と安定性の点で不利になります[@brown2015]。
- サンプルサイズは最低200、できれば300以上を目安にします[@kline2023]。ただし、項目数、因子負荷量、欠測率、推定法により必要なサンプルサイズは変わります。

#### CFA の識別性と尺度化

CFA で最初につまずきやすいのは、**潜在変数にはもともと単位がない**という点です。観測変数であれば「テスト得点 0--100」「Likert 1--6」のような単位がありますが、潜在変数である「動機づけ」や「不安」には自然な単位がありません。そのため、モデル推定の前に潜在変数の尺度をどこかで固定する必要があります。これを**尺度化**（scaling）と呼びます。

代表的な尺度化は次の2つです。

| 尺度化の方法 | lavaan での指定 | 何を固定するか | 長所 | 注意点 |
|---|---|---|---|---|
| マーカー法（marker variable method） | `std.lv = FALSE`（既定） | 各因子の第1指標の負荷量を 1 に固定 | 古典的で多くの出力と対応しやすい | 第1項目の選び方に非標準化推定値が依存します |
| 潜在分散固定法（fixed factor method） | `std.lv = TRUE` | 潜在変数の分散を 1 に固定 | すべての負荷量を自由推定でき、標準化解に近い直感で読めます | 非標準化負荷量の尺度がマーカー法と変わります |

どちらを使っても、**標準化解（`std.all`）やモデル適合度は原則として同じ**です。変わるのは主に非標準化推定値の尺度です。本章では、初心者が負荷量を比較しやすいように `std.lv = TRUE` を基本にします。

識別性（identification）は「データからパラメータを一意に推定できるか」という条件です。CFA の実務では、次のチェックが重要です。

| チェック項目 | 実務上の目安 | 問題がある場合 |
|---|---|---|
| 因子あたりの指標数 | 3項目以上が望ましい | 2項目因子は制約が必要になりやすく、推定が不安定です |
| 因子負荷量 | 標準化負荷量 .50 以上が望ましい | 低負荷項目が多いと因子の意味が弱くなります |
| 因子間相関 | .85 未満が望ましい | 高すぎる場合は因子の統合、二次因子、bifactor を検討します |
| 残差分散 | 正の値 | 負の分散は Heywood ケースで、モデル誤指定やサンプル不足を疑います |

#### lavaan の記法

`lavaan` パッケージでは以下の演算子を使ってモデルを記述する[@rosseel2012]。

| 演算子 | 意味 | 例 |
|---|---|---|
| `=~` | 「〜により測定される」（因子→指標） | `visual =~ x1 + x2 + x3` |
| `~` | 回帰（構造パス） | `speed ~ visual + textual` |
| `~~` | （共）分散 | `visual ~~ textual` |
| `~1` | 切片 | `x1 ~1` |
| `:=` | 定義式（間接効果など） | `ind := a * b` |

#### カテゴリカル指標の CFA（順序尺度・2値データ）

CFA は連続変数だけでなく、**順序カテゴリ指標**（ordinal indicators）や**2値指標**（binary indicators）にも適用できます。ただし、この場合は通常の Pearson 相関と ML 推定をそのまま使うのではなく、閾値モデル（threshold model）に基づく推定を使います。lavaan では、`ordered = ...` で順序カテゴリとして扱う変数を指定し、`estimator = "WLSMV"` を用いるのが標準的です[@brown2015; @holgadotello2010]。

ここでいう「カテゴリカル指標」は、CA/MCA で扱う名義カテゴリとは異なります。CFA で扱いやすいのは、「1 = 全く当てはまらない」から「5 = 非常に当てはまる」のように**順序があるカテゴリ**、または「正答 / 誤答」のような2値反応です。L1 背景や専攻のような名義カテゴリは、通常の CFA の測定指標としてそのまま入れるのではなく、群変数、多群 CFA、共変量、または CA/MCA の対象として扱います。

| 指標の種類 | 例 | 推奨指定 | 解釈 |
|---|---|---|---|
| 連続指標 | テスト得点、反応時間の平均 | `estimator = "MLR"` | 共分散構造を直接モデル化します |
| 6件法以上の Likert | 1--6 の動機づけ項目 | `MLR` または感度分析として `WLSMV` | 連続近似でもよいことがあります |
| 5件法以下の Likert | 1--5 の態度項目 | `estimator = "WLSMV", ordered = item_names` | ポリコリック相関と閾値を用います |
| 2値項目 | 正答 / 誤答、Yes / No | `estimator = "WLSMV", ordered = item_names` | テトラコリック相関と閾値を用います |
| 名義カテゴリ | L1 背景、専攻、地域 | 通常の CFA 指標にはしない | 群変数・共変量・CA/MCA として扱います |

順序カテゴリ CFA では、観測された 1, 2, 3, ... という値を連続量そのものとはみなしません。その背後に連続的な潜在反応 $y^*$ があり、閾値（threshold）を超えることで回答カテゴリが変わると仮定します。例えば5件法項目では、4つの閾値が推定されます。

この章の実例では、まず連続近似の CFA を示した後、同じモデルを WLSMV でも推定し、順序カテゴリ指標として扱った場合の結果を確認します。

実務上の注意点は次の通りです。

- `ordered = TRUE` よりも、`ordered = item_names` のように変数名ベクトルを明示する方が安全です。連続指標と順序指標が混在するモデルでは特に重要です。
- WLSMV では AIC/BIC が通常の ML と同じようには使えません。モデル比較は適合度指標、理論的妥当性、必要に応じてロバストな差の検定で判断します。
- WLSMV では FIML を連続指標の ML/MLR と同じ形では使えません。欠測が多い場合は、多重代入や感度分析も検討します。
- サンプルサイズが小さく、カテゴリが極端に偏る場合、ポリコリック / テトラコリック相関が不安定になります。カテゴリ統合や項目見直しを検討してください。

#### SLA データ例: 動機づけの CFA

EFA で確認した3因子構造を、**新しい検証サンプル**で確認します。

```{r}
#| eval: true
#| label: cfa-data-simulation

set.seed(4321)  # Different seed for independent sample
n_cfa <- 400

f_scores_cfa <- MASS::mvrnorm(n_cfa, mu = c(0, 0, 0), Sigma = Phi_efa)

raw_cfa <- f_scores_cfa %*% t(Lambda_efa) + matrix(rnorm(n_cfa * 18, sd = 0.5), nrow = n_cfa)
likert_cfa <- apply(raw_cfa, 2, function(x) {
  pmin(pmax(round(scales::rescale(x, to = c(1, 6))), 1), 6)
})
motivation_cfa <- as.data.frame(likert_cfa)
names(motivation_cfa) <- paste0("M", sprintf("%02d", 1:18))
```

ここでは、EFA で得た3因子構造を `lavaan::cfa()` で確認します。比較対象として1因子モデルも推定し、因子構造の必要性を検証します。モデル式は上述の「lavaan の記法」節の演算子（`=~`, `~`, `~~` 等）で記述します。

**`cfa()` の主要引数**:

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `std.lv` | `TRUE` | 潜在変数の分散を 1 に固定する。これにより全ての因子負荷量が自由推定され、負荷量は潜在変数の標準偏差単位で表される。完全標準化解は `standardized = TRUE` の `std.all` 列で確認する |
| `estimator` | （既定 = `"ML"`） | 推定法。正規性仮定が成立する場合は既定の ML でよい |

出力テーブルの主要列:

| 列 | 意味 | 確認ポイント |
|---|---|---|
| `est` | 非標準化推定値 | 因子負荷量（`=~` 行）やパス係数 |
| `std.all` | 完全標準化推定値 | .50 以上なら実質的に意味のある負荷 |
| `pvalue` | 有意確率 | < .05 で統計的に有意 |

標準化解（`std.all`）は、すべての変数を標準偏差 1 に揃えた上でのパラメータ推定値であり、異なるスケールの変数間で効果の大きさを比較するために用いる。因子負荷量の標準化解が .50 を下回る項目は、当該潜在変数との関連が弱い可能性がある。

```{r}
#| eval: true
#| label: cfa-model-estimation

cfa_model_3f <- '
  IdealL2   =~ M01 + M02 + M03 + M04 + M05 + M06
  OughtL2   =~ M07 + M08 + M09 + M10 + M11 + M12
  LearnExp  =~ M13 + M14 + M15 + M16 + M17 + M18
'

cfa_model_1f <- '
  General =~ M01 + M02 + M03 + M04 + M05 + M06 +
             M07 + M08 + M09 + M10 + M11 + M12 +
             M13 + M14 + M15 + M16 + M17 + M18
'

fit_cfa_3f <- cfa(cfa_model_3f, data = motivation_cfa, std.lv = TRUE)
fit_cfa_1f <- cfa(cfa_model_1f, data = motivation_cfa, std.lv = TRUE)

parameterEstimates(fit_cfa_3f, standardized = TRUE) |>
  dplyr::filter(op %in% c("=~", "~", "~~")) |>
  dplyr::select(lhs, op, rhs, est, se, z, pvalue, std.all) |>
  knitr::kable(
    caption = "CFA 3-Factor Model: Parameter Estimates",
    digits = 3,
    booktabs = TRUE
  )
```

パラメータ推定値テーブルでは、`=~` 行が各項目の因子負荷量を示す。`std.all` 列の値が標準化負荷量であり、.50 以上であれば収束的妥当性の目安を満たす。全項目の標準化負荷量を確認し、極端に低い値（< .40）がないか、推定値が1を超える Heywood ケースがないかを確認する。因子間相関（`~~` 行のうち潜在変数間）が .85 を超える場合は弁別的妥当性に疑義がある[@alhoorie2024]。

#### `std.lv = TRUE` とマーカー法の違い

同じモデルをマーカー法（既定）と潜在分散固定法（`std.lv = TRUE`）で推定し、どこが変わり、どこが変わらないかを確認します。

```{r}
#| eval: true
#| label: cfa-scaling-comparison

fit_cfa_marker <- lavaan::cfa(cfa_model_3f, data = motivation_cfa, std.lv = FALSE)

scaling_fit <- tibble::tibble(
  Scaling = c("Marker method", "Fixed latent variance"),
  CFI = c(fitMeasures(fit_cfa_marker, "cfi"), fitMeasures(fit_cfa_3f, "cfi")),
  RMSEA = c(fitMeasures(fit_cfa_marker, "rmsea"), fitMeasures(fit_cfa_3f, "rmsea")),
  SRMR = c(fitMeasures(fit_cfa_marker, "srmr"), fitMeasures(fit_cfa_3f, "srmr"))
)

scaling_loadings <- dplyr::bind_rows(
  parameterEstimates(fit_cfa_marker, standardized = TRUE) |>
    dplyr::filter(op == "=~") |>
    dplyr::mutate(Scaling = "Marker method"),
  parameterEstimates(fit_cfa_3f, standardized = TRUE) |>
    dplyr::filter(op == "=~") |>
    dplyr::mutate(Scaling = "Fixed latent variance")
) |>
  dplyr::filter(rhs %in% c("M01", "M07", "M13")) |>
  dplyr::select(Scaling, lhs, rhs, est, std.all)

scaling_fit |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "CFA Scaling Methods: Fit Indices Are Invariant",
    booktabs = TRUE
  )

scaling_loadings |>
  dplyr::mutate(dplyr::across(c(est, std.all), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "CFA Scaling Methods: Unstandardized vs. Standardized Loadings",
    booktabs = TRUE
  )
```

この比較では、適合度指標は同じですが、非標準化負荷量（`est`）は尺度化の方法で変わります。一方、完全標準化負荷量（`std.all`）は同じです。実務では、尺度化の指定を明記した上で、項目の強さを比較する際には `std.all` を中心に報告すると誤解が少なくなります。

#### 同じ CFA モデルを順序カテゴリ指標として推定する

上の例では、6件法 Likert 項目を連続変数に近似して ML 推定を行いました。次に、同じ3因子モデルを WLSMV で推定し、項目を順序カテゴリ指標として扱った場合の結果を確認します。実データで5件法以下の Likert 項目を扱う場合は、こちらを主分析にすることを検討します。

```{r}
#| eval: true
#| label: cfa-ordinal-wlsmv

ordinal_items <- names(motivation_cfa)

fit_cfa_3f_wlsmv <- lavaan::cfa(
  cfa_model_3f,
  data = motivation_cfa,
  std.lv = TRUE,
  estimator = "WLSMV",
  ordered = ordinal_items
)

fitMeasures(fit_cfa_3f_wlsmv, c("cfi", "tli", "rmsea", "srmr")) |>
  tibble::enframe(name = "FitIndex", value = "Value") |>
  dplyr::mutate(Value = round(Value, 3)) |>
  knitr::kable(
    caption = "Ordinal CFA with WLSMV: Fit Indices",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: cfa-ordinal-thresholds

parameterEstimates(fit_cfa_3f_wlsmv, standardized = TRUE) |>
  dplyr::filter(op %in% c("=~", "|")) |>
  dplyr::mutate(
    Type = dplyr::if_else(op == "=~", "Loading", "Threshold")
  ) |>
  dplyr::select(Type, lhs, op, rhs, est, se, std.all) |>
  dplyr::slice_head(n = 24) |>
  knitr::kable(
    caption = "Ordinal CFA: Loadings and Thresholds (first rows)",
    digits = 3,
    booktabs = TRUE
  )
```

WLSMV の結果では、`=~` 行が因子負荷量、`|` 行が閾値を表します。連続 CFA では切片や残差分散を中心に見ますが、順序カテゴリ CFA ではカテゴリ境界としての閾値も重要です。閾値が極端に偏っている項目は、ほとんどの回答者が同じカテゴリを選んでいる可能性があり、因子負荷量や適合度が不安定になります。

閾値は表だけでは読み取りにくいため、項目ごとの閾値の並びを図でも確認します。閾値が全体的に右上がりに並んでいれば、回答カテゴリの順序が自然に反映されています。一方、特定項目だけ閾値が極端に高い・低い場合は、その項目で天井効果・床効果、またはカテゴリ使用の偏りがある可能性があります。

```{r}
#| eval: true
#| label: cfa-ordinal-threshold-plot
#| fig-width: 9
#| fig-height: 5
#| fig-cap: "順序カテゴリ CFA の閾値プロット（Threshold Plot for Ordinal CFA）"

threshold_df <- parameterEstimates(fit_cfa_3f_wlsmv, standardized = TRUE) |>
  as_tibble() |>
  dplyr::filter(op == "|") |>
  dplyr::mutate(
    Item = lhs,
    Threshold = readr::parse_number(rhs),
    Construct = dplyr::case_when(
      Item %in% paste0("M", sprintf("%02d", 1:6)) ~ "Ideal L2 Self",
      Item %in% paste0("M", sprintf("%02d", 7:12)) ~ "Ought-to L2 Self",
      TRUE ~ "L2 Learning Experience"
    )
  )

ggplot(threshold_df, aes(x = Threshold, y = est, group = Item, color = Construct)) +
  geom_hline(yintercept = 0, linetype = "dashed", color = "grey60") +
  geom_line(alpha = 0.35) +
  geom_point(size = 1.8, alpha = 0.75) +
  scale_x_continuous(breaks = sort(unique(threshold_df$Threshold))) +
  labs(
    title = "Ordinal CFA Thresholds",
    subtitle = "Each line shows how category boundaries shift across one item",
    x = "Threshold number",
    y = "Threshold estimate",
    color = "Construct"
  ) +
  theme(legend.position = "bottom")
```

#### 適合度指標の比較

```{r}
#| eval: true
#| label: cfa-fit-comparison

indices <- c("chisq", "df", "pvalue", "cfi", "tli", "rmsea", "rmsea.ci.lower", "rmsea.ci.upper", "srmr")
fit_3f <- fitMeasures(fit_cfa_3f, indices)
fit_1f <- fitMeasures(fit_cfa_1f, indices)

data.frame(
  Measure = names(fit_3f),
  `3-Factor` = as.numeric(fit_3f),
  `1-Factor` = as.numeric(fit_1f)
) |>
  dplyr::rename(Fit_Index = Measure) |>
  knitr::kable(
    caption = "CFA Model Fit Comparison: 3-Factor (L2MSS) vs. 1-Factor Model",
    digits = 3,
    booktabs = TRUE
  )
```

テーブルの読み方: 3因子モデルが1因子モデルよりも複数の指標で良好であれば、L2MSS の3因子構造と整合的な証拠になります。ただし、$\Delta$CFI の固定的なカットオフだけで競合 CFA モデルの優劣を決めるのではなく、適合度差、因子間相関、残差、パラメータの妥当性、理論的根拠を総合的に判断します。

3因子モデルでは CFI = .95 以上かつ RMSEA < .06、SRMR < .08 を満たしているかを確認します。1因子モデルや2因子モデルと比較して適合度が明確に改善し、因子間相関が過度に高くなく、残差にも体系的な問題がなければ、動機づけデータは**相関する3つの側面**で構成されるという理論的仮説を支持する証拠になります。ΔCFI などの変化量基準は、とくに測定不変性の文脈で有用な目安であり、一般的な競合モデル比較では補助情報として扱います。

#### 相関3因子・二次因子・bifactor モデル

CFA では「3因子モデルがよく適合した」で終わらせず、理論的にあり得る競合モデルも検討します。L2 動機づけのように下位尺度が互いに相関する場合、少なくとも次の3種類のモデルを区別できます。

| モデル | 何を仮定するか | 向いている問い | 注意点 |
|---|---|---|---|
| 相関3因子モデル | Ideal / Ought / Learning Experience が別因子で、互いに相関する | 下位構成概念を区別して扱えるか | 因子間相関が高すぎると弁別性が弱い |
| 二次因子モデル | 3つの一次因子の背後に一般的な動機づけ因子がある | 「全般的な L2 動機づけ」を想定できるか | 一次因子が3つだけの場合、相関3因子モデルと適合が同等になりやすい |
| bifactor モデル | 全項目に負荷する一般因子と、領域固有の特定因子を同時に推定する | 総得点と下位尺度のどちらを重視すべきか | 過剰適合・Heywood ケース・解釈困難が起こりやすい[@reise2012] |

```{r}
#| eval: true
#| label: cfa-second-order-bifactor

cfa_model_second_order <- '
  IdealL2   =~ M01 + M02 + M03 + M04 + M05 + M06
  OughtL2   =~ M07 + M08 + M09 + M10 + M11 + M12
  LearnExp  =~ M13 + M14 + M15 + M16 + M17 + M18

  GeneralMot =~ IdealL2 + OughtL2 + LearnExp
'

cfa_model_bifactor <- '
  GeneralMot =~ M01 + M02 + M03 + M04 + M05 + M06 +
                M07 + M08 + M09 + M10 + M11 + M12 +
                M13 + M14 + M15 + M16 + M17 + M18

  IdealSpecific =~ M01 + M02 + M03 + M04 + M05 + M06
  OughtSpecific =~ M07 + M08 + M09 + M10 + M11 + M12
  LearnSpecific =~ M13 + M14 + M15 + M16 + M17 + M18
'

fit_cfa_second_order <- lavaan::cfa(
  cfa_model_second_order,
  data = motivation_cfa,
  std.lv = TRUE
)

fit_cfa_bifactor <- lavaan::cfa(
  cfa_model_bifactor,
  data = motivation_cfa,
  std.lv = TRUE,
  orthogonal = TRUE
)

fit_row <- function(fit, model_name) {
  tibble::tibble(
    Model = model_name,
    Converged = isTRUE(lavaan::lavInspect(fit, "converged")),
    CFI = fitMeasures(fit, "cfi"),
    RMSEA = fitMeasures(fit, "rmsea"),
    SRMR = fitMeasures(fit, "srmr"),
    AIC = fitMeasures(fit, "aic"),
    BIC = fitMeasures(fit, "bic"),
    Parameters = fitMeasures(fit, "npar")
  )
}

cfa_competing_models <- dplyr::bind_rows(
  fit_row(fit_cfa_1f, "1-factor"),
  fit_row(fit_cfa_3f, "Correlated 3-factor"),
  fit_row(fit_cfa_second_order, "Second-order"),
  fit_row(fit_cfa_bifactor, "Bifactor")
)

cfa_competing_models |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "Competing CFA Models: Correlated Factors, Second-Order, and Bifactor",
    booktabs = TRUE
  )
```

競合モデルの比較は、数値表と併せて図示すると、モデル間の差が直感的に分かります。CFI は高いほどよく、RMSEA と SRMR は低いほどよいため、同じ図を「勝ち負け」だけで読まないことが重要です。破線はよく使われる目安であり、機械的な合否判定ではありません。

```{r}
#| eval: true
#| label: cfa-competing-fit-plot
#| fig-width: 9
#| fig-height: 5
#| fig-cap: "CFA 競合モデルの適合度比較（Fit Indices for Competing CFA Models）"

fit_plot_df <- cfa_competing_models |>
  dplyr::filter(Converged) |>
  dplyr::select(Model, CFI, RMSEA, SRMR) |>
  tidyr::pivot_longer(
    cols = c(CFI, RMSEA, SRMR),
    names_to = "FitIndex",
    values_to = "Value"
  ) |>
  dplyr::mutate(
    Model = factor(
      Model,
      levels = rev(c("1-factor", "Correlated 3-factor", "Second-order", "Bifactor"))
    )
  )

fit_reference <- tibble::tibble(
  FitIndex = c("CFI", "RMSEA", "SRMR"),
  Reference = c(0.95, 0.06, 0.08)
)

ggplot(fit_plot_df, aes(x = Value, y = Model, color = Model)) +
  geom_vline(
    data = fit_reference,
    aes(xintercept = Reference),
    inherit.aes = FALSE,
    linetype = "dashed",
    color = "grey50"
  ) +
  geom_point(size = 3) +
  facet_wrap(~ FitIndex, scales = "free_x") +
  scale_x_continuous(labels = scales::number_format(accuracy = 0.01)) +
  labs(
    title = "Competing CFA Model Fit",
    subtitle = "Dashed lines show common reference values: CFI .95, RMSEA .06, SRMR .08",
    x = "Fit index value",
    y = NULL,
    color = "Model"
  ) +
  theme(legend.position = "none")
```

二次因子モデルは、「3つの下位因子がさらに一般因子によって説明される」という解釈を与えます。ただし、一次因子が3つだけの場合、二次因子部分がほぼ飽和し、相関3因子モデルと同じ適合度になることがあります。その場合、統計的に優劣をつけるというより、理論上「全般的な動機づけ」を置くことに意味があるかで判断します。

bifactor モデルは、一般因子と特定因子を同時に推定する強力なモデルですが、データよりもモデルが複雑になりやすいです。一般因子が強く、特定因子の負荷が弱い場合は、総得点中心の解釈が妥当かもしれません。逆に、特定因子にも十分な負荷が残る場合は、下位尺度を別々に扱う根拠になります。bifactor を使う場合は、適合度だけでなく、一般因子・特定因子の負荷量、Heywood ケース、解釈可能性、$\omega_h$ などの信頼性指標を合わせて確認します[@flora2020; @reise2012]。

CFA/SEM の適合度指標は、モデルが仮定する共分散構造（含意共分散行列 $\boldsymbol{\Sigma}(\boldsymbol{\theta})$）が実データの共分散行列 $\mathbf{S}$ にどれだけ近いかを多角的に評価するものです。各指標は「モデルの悪さ」を異なる角度から測定しており、単一の指標に頼るのではなく複数を総合的に判断します。

- **カイ二乗検定（$\chi^2$）**は、モデルとデータの完全な一致を帰無仮説とする検定である。しかし、サンプルサイズが大きくなると些細なズレでも有意になるため、$N > 200$ の場合は**単独での判断は避ける**。自由度（df）はモデルの複雑さを表す指標で、「既知の情報量（データが提供する共分散・分散の数 $= p(p+1)/2$）からモデルが推定するパラメータ数を差し引いたもの」である。自由度が大きいほどモデルは倹約的であり、自由度が 0 のモデル（飽和モデル）は検定不能だがデータを完全に再現する。
- **CFI（Comparative Fit Index）**は、「全変数が無相関」と仮定するベースライン（独立）モデルから、どれだけ適合が改善されたかを測る。0--1 の範囲で、1.0 に近いほど改善が大きい。直感的には「一番悪いモデルから見て何%改善されたか」を表す。
- **TLI（Tucker-Lewis Index）**は CFI と類似するが、モデルの自由度（パラメータ数）にペナルティを与える。不必要にパラメータを増やしたモデルに対して CFI より厳しく評価するため、「倹約性を重視した改善度」と理解できる。
- **RMSEA（Root Mean Square Error of Approximation）**は、**自由度あたりの近似誤差**を表します。モデルの複雑さを考慮したミスフィットの指標であり、値が小さいほど良好です。90% 信頼区間が併記されるため、推定の不確実性も評価できます。
- **SRMR（Standardized Root Mean Square Residual）**は、観測された相関行列とモデルが予測する相関行列の**残差の平均的な大きさ**である。最も直感的な指標で、「モデルが各変数間の相関をどのくらい再現できているか」を直接反映する。

##### 適合度指標のカットオフ基準（目安）
- **CFI / TLI**: 0.90 以上で許容、0.95 以上で良好[@hu1999]
- **RMSEA**: 0.08 未満で許容、0.05 未満で良好。90% 信頼区間の上限が 0.08 未満なら安心
- **SRMR**: 0.08 未満で良好
- **$\chi^2$ 検定**: 大標本では常に有意になりやすいため、参考値として扱います。なお過去には $\chi^2 / df$ 比（2--3 未満で良好）が使われましたが、@kline2023 や West et al. (2023) は「絶対的基準は存在しない」と明確に否定しており、現代では推奨されません。RMSEA や CFI などの近似適合度指標を併用してください
- 複数の指標を総合的に評価する。「全ての指標が完璧」を求める必要はない[@kline2023]

##### 適合度指標への過度の依存
Hu & Bentler [-@hu1999] のカットオフ値は「目安」であり「絶対基準」ではありません。**この点は早期から強く警告されており**、@marsh2004 は "In search of golden rules" として、Hu & Bentler のカットオフが特定のシミュレーション条件下で得られたものであり、モデル複雑度・標本サイズ・標本特性が異なる現実データに無批判に適用すると過剰排除を生むことを示しました。さらに @mcneish2023 は、固定的なカットオフ値がモデルの複雑さや指標数によって不適切になりうることを示し、データごとに動的なカットオフ（dynamic fit index cutoffs）を算出する方法を提案しています。適合度だけでなく、**理論的整合性**（因子構造に学術的意味があるか）、**パラメータの解釈可能性**（負荷量が合理的か）、**局所適合**（残差行列に系統的パターンがないか）を合わせて評価します。

#### CFA 診断

モデルを報告する前に、パス図・標準化負荷量・残差共分散・因子間相関の4つの側面から結果を点検する。

::: {.panel-tabset}

##### パス図

パス図は測定モデルの構造を視覚的に確認するためのものである。楕円が潜在因子、四角が観測変数、矢印上の数値が標準化因子負荷量を示す。

```{r}
#| eval: true
#| label: cfa-path-diagram
#| fig-width: 10
#| fig-height: 8

# Path diagram (standardized estimates)
semPaths(fit_cfa_3f, whatLabels = "std", layout = "tree", rotation = 2,
         edge.label.cex = 0.8, sizeMan = 6, sizeLat = 10,
         nodeLabels = c(paste0("M", sprintf("%02d", 1:18)),
                        "Ideal\nL2", "Ought\nL2", "Learn\nExp"))
```

パス図の楕円は潜在変数（直接観測できない構成概念）、長方形は観測変数（実際の質問項目）を表す。潜在変数から観測変数への矢印上の数値が因子負荷量であり、.50 以上が望ましい。観測変数の右側の小さい矢印は測定誤差（独自性）を表し、因子では説明できない変動を示す。

##### 標準化因子負荷量

標準化因子負荷量（Std. Loading）は、潜在因子が各観測変数をどの程度説明するかを示す。.50 以上が収束的妥当性の目安である。95% 信頼区間も併記されているため、推定の精度を確認できる。

```{r}
#| eval: true
#| label: cfa-standardized-solution

std_sol <- standardizedSolution(fit_cfa_3f) |>
  as_tibble() |>
  dplyr::filter(op == "=~") |>
  dplyr::select(lhs, rhs, est.std, ci.lower, ci.upper, pvalue)

std_sol |>
  knitr::kable(
    caption = "CFA Standardized Factor Loadings",
    digits = 3,
    booktabs = TRUE
  )
```

表の標準化負荷量は、森林プロット（forest plot）の形で可視化すると、どの項目が強く因子に結びついているか、また信頼区間がどの程度狭いかを把握しやすくなります。破線の .50 は、収束的妥当性を考える際の実務的な目安です。

```{r}
#| eval: true
#| label: cfa-loading-forest
#| fig-width: 9
#| fig-height: 7
#| fig-cap: "CFA 標準化因子負荷量の森林プロット（Forest Plot of Standardized CFA Loadings）"

std_sol |>
  dplyr::mutate(
    rhs = factor(rhs, levels = rev(paste0("M", sprintf("%02d", 1:18)))),
    lhs = factor(lhs, levels = c("IdealL2", "OughtL2", "LearnExp"))
  ) |>
  ggplot(aes(x = est.std, y = rhs, xmin = ci.lower, xmax = ci.upper, color = lhs)) +
  geom_vline(xintercept = 0.50, linetype = "dashed", color = "grey50") +
  geom_pointrange(linewidth = 0.5) +
  facet_grid(lhs ~ ., scales = "free_y", space = "free_y") +
  coord_cartesian(xlim = c(0, 1)) +
  labs(
    title = "Standardized CFA Loadings with 95% CIs",
    subtitle = "The dashed line marks a common practical reference value of .50",
    x = "Standardized loading",
    y = NULL,
    color = "Factor"
  ) +
  theme(legend.position = "none")
```

##### 残差共分散行列

CFA モデルが想定する共分散構造は**含意共分散行列**（model-implied covariance matrix）$\boldsymbol{\Sigma}(\boldsymbol{\theta})$ として表現される。

$$\boldsymbol{\Sigma}(\boldsymbol{\theta}) = \boldsymbol{\Lambda} \boldsymbol{\Phi} \boldsymbol{\Lambda}^\top + \boldsymbol{\Psi}$$

残差行列（観測 $-$ 含意）の要素が全体的に 0 に近いほど局所適合が良好。系統的に大きい残差がある場合は修正指標を参考にする。

```{r}
#| eval: true
#| label: cfa-implied-cov

# Model-implied vs observed covariance (first 6 variables)
implied_cov <- lavInspect(fit_cfa_3f, what = "implied")$cov
observed_cov <- cov(motivation_cfa[, 1:6])

# Residual covariance (discrepancy)
resid_cov <- observed_cov - implied_cov[1:6, 1:6]

as.data.frame(round(resid_cov, 3)) |>
  rownames_to_column("Variable") |>
  knitr::kable(
    caption = "Residual Covariance Matrix (First 6 Items)",
    digits = 3,
    booktabs = TRUE
  )
```

残差の大きさを視覚的に把握するため、全18項目の残差相関行列をヒートマップで可視化する。赤いセルはモデルが過小推定している相関（正の残差）、青いセルは過大推定している相関（負の残差）を示す。系統的なパターン（例: 同一因子内の特定項目ペアに大きな正の残差）がある場合は、残差共分散の追加や項目の修正を検討する。

```{r}
#| eval: true
#| label: cfa-residual-heatmap
#| fig-width: 9
#| fig-height: 8

# Full residual correlation matrix (standardized)
resid_full <- lavaan::lavResiduals(fit_cfa_3f, type = "cor")$cov
resid_df <- as.data.frame(as.table(as.matrix(resid_full))) |>
  rename(Var1 = 1, Var2 = 2, Residual = 3)

ggplot(resid_df, aes(x = Var1, y = Var2, fill = Residual)) +
  geom_tile(color = "white") +
  geom_text(aes(label = ifelse(abs(Residual) > 0.05, round(Residual, 2), "")),
            size = 2.5) +
  scale_fill_gradient2(low = "#2166ac", mid = "white", high = "#b2182b",
                       midpoint = 0, limits = c(-0.15, 0.15)) +
  labs(title = "CFA Residual Correlation Heatmap (18 Items)",
       subtitle = "Red = model underestimates correlation; Blue = overestimates",
       fill = "Residual r") +
  theme(axis.text.x = element_text(angle = 45, hjust = 1),
        axis.title = element_blank())
```

残差相関の絶対値が .10 を超えるペアは局所的な不適合を示唆する。これらのペアは修正指標の結果と照合し、理論的に妥当な場合のみ残差共分散の追加を検討する。

ヒートマップは全体像を把握するのに向いていますが、どの項目ペアを優先的に点検すべきかは分かりにくいことがあります。そこで、絶対値が大きい残差相関を上位から並べ、局所依存（local dependence）の候補を確認します。

```{r}
#| eval: true
#| label: cfa-residual-pair-plot
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "CFA の大きな残差相関ペア（Largest Residual Correlation Pairs in CFA）"

resid_pair_df <- resid_df |>
  dplyr::mutate(
    Var1 = as.character(Var1),
    Var2 = as.character(Var2)
  ) |>
  dplyr::filter(Var1 < Var2) |>
  dplyr::mutate(
    Pair = paste(Var1, Var2, sep = " - "),
    AbsResidual = abs(Residual),
    Direction = dplyr::if_else(Residual >= 0, "Positive residual", "Negative residual")
  ) |>
  dplyr::slice_max(AbsResidual, n = 12, with_ties = FALSE)

ggplot(resid_pair_df, aes(x = AbsResidual, y = forcats::fct_reorder(Pair, AbsResidual), fill = Direction)) +
  geom_col(width = 0.65) +
  geom_vline(xintercept = 0.10, linetype = "dashed", color = "grey45") +
  scale_fill_manual(values = c("Positive residual" = "#b2182b", "Negative residual" = "#2166ac")) +
  labs(
    title = "Largest Absolute Residual Correlations",
    subtitle = "Pairs beyond .10 require theoretical inspection before model modification",
    x = "Absolute residual correlation",
    y = NULL,
    fill = "Direction"
  ) +
  theme(legend.position = "bottom")
```

含意共分散行列（$\Sigma(\hat{\theta})$）はモデルが仮定する理論上の共分散構造であり、データの共分散行列（$S$）との差が残差共分散行列である。残差が大きい項目ペアは、モデルが捉えきれていない関連パターンの存在を示唆する。ただし、残差を小さくするために修正指標に従って相関パスを追加する際は、理論的な根拠がない修正は過適合を招くため慎重に判断する必要がある。

##### 因子間相関

因子間相関が .85 を超える場合は弁別的妥当性に疑義が生じる。その場合、2因子を統合した簡素なモデルとの比較を検討する。

```{r}
#| eval: true
#| label: cfa-factor-correlations

# Factor correlations
fac_cor <- lavInspect(fit_cfa_3f, what = "cor.lv")

as.data.frame(round(fac_cor, 3)) |>
  rownames_to_column("Factor") |>
  knitr::kable(
    caption = "CFA Factor Correlations",
    digits = 3,
    booktabs = TRUE
  )
```

因子間相関は、弁別的妥当性（discriminant validity）を確認するために図でも点検します。濃い色の非対角セルが多い場合、因子名は異なっていても実質的にはかなり近い構成概念を測っている可能性があります。

```{r}
#| eval: true
#| label: cfa-factor-correlation-heatmap
#| fig-width: 6
#| fig-height: 5
#| fig-cap: "CFA 因子間相関のヒートマップ（Heatmap of CFA Factor Correlations）"

fac_cor_df <- as.data.frame(as.table(as.matrix(fac_cor))) |>
  dplyr::rename(Factor1 = 1, Factor2 = 2, Correlation = 3)

ggplot(fac_cor_df, aes(x = Factor1, y = Factor2, fill = Correlation)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(Correlation, 2)), size = 4) +
  scale_fill_gradient2(
    low = "#2166ac",
    mid = "white",
    high = "#b2182b",
    midpoint = 0,
    limits = c(-1, 1)
  ) +
  labs(
    title = "Latent Factor Correlations",
    subtitle = "Very high correlations may weaken discriminant validity",
    x = NULL,
    y = NULL,
    fill = "r"
  )
```

:::

#### 局所依存・方法因子・逆転項目

CFA で残差相関が大きい項目ペアが見つかった場合、その原因は「本来の構成概念」ではなく、項目形式や文言に由来することがあります。これを**局所依存**（local dependence）または**方法効果**（method effect）と呼びます。

典型例は次の通りです。

| 現象 | 例 | 診断の手がかり | 対応 |
|---|---|---|---|
| 文言の重複 | 「英語で話したい」「英語で会話したい」 | 同一因子内の項目ペアに大きな正の残差相関 | 項目統合・項目削除・残差共分散の検討 |
| 逆転項目効果 | 逆転項目だけがまとまる | 逆転項目同士に残差相関、方法因子が出る | 逆転処理の確認、次回調査で文言修正 |
| 同一形式効果 | すべて「将来〜したい」で始まる | 内容より形式が相関を作る | 項目形式を分散させる |
| テストレット効果 | 同じ読解文に基づく複数設問 | 同一パッセージ項目に局所依存 | テストレット因子や多層 IRT を検討 |

修正指標（MI）が大きいからといって、残差共分散を機械的に追加してはいけません。残差共分散は「モデルが説明していない項目ペア固有の関連」を許す処理であり、理論的説明がないまま追加すると過適合になります。

残差共分散と方法因子は、次のように使い分けます。

| 対応 | 使う場面 | 長所 | 注意点 |
|---|---|---|---|
| 残差共分散を1本追加 | 特定の2項目だけが局所依存する | 追加パラメータが少ない | 事後修正になりやすく、再現性が低い |
| 方法因子を追加 | 逆転項目や同一形式項目が複数ある | 方法効果を明示的にモデル化できる | 因子の解釈が複雑になり、識別性が悪化しやすい |
| 項目を削除・修正 | 項目文が重複・曖昧 | 次回以降の測定品質が改善する | 既存データでは尺度内容が変わる |

方法因子を使う場合の lavaan 構文例は次の通りです。これは**例示用**であり、方法因子を入れるには、逆転項目や同一形式項目が方法効果を持つという明確な理論的根拠が必要です。

```{r}
#| eval: false
#| label: cfa-method-factor-example

cfa_model_method <- '
  IdealL2   =~ M01 + M02 + M03 + M04 + M05 + M06
  OughtL2   =~ M07 + M08 + M09 + M10 + M11 + M12
  LearnExp  =~ M13 + M14 + M15 + M16 + M17 + M18

  # Example method factor for reverse-worded items
  ReverseMethod =~ M02 + M05 + M11 + M16

  # Keep the method factor separate from substantive factors
  ReverseMethod ~~ 0*IdealL2
  ReverseMethod ~~ 0*OughtL2
  ReverseMethod ~~ 0*LearnExp
'

fit_method <- lavaan::cfa(cfa_model_method, data = motivation_cfa, std.lv = TRUE)
```

初心者向けには、「残差相関が大きい = 項目同士に、因子では説明できない似方が残っている」と理解してください。経験者向けには、局所依存は測定不変性、IRT の局所独立性、テストレットモデルともつながる重要な診断点です。

#### 項目パーセリングの注意

項目パーセリング（item parceling）は、複数項目を平均して小さな合成指標（parcel）を作り、その parcel を CFA/SEM の観測指標として使う方法です。例えば18項目を6つの parcel にまとめると、モデルが小さくなり、適合度が良く見えることがあります。

しかし、尺度開発や因子構造の検証段階では、原則として item parceling は避けるべきです。理由は、項目レベルの交差負荷、低共通性、局所依存、逆転項目効果が parcel の平均化によって見えにくくなるためです[@bandalos2018; @kline2023]。

| 場面 | parceling の扱い |
|---|---|
| 新規尺度開発 | 原則として使わない。項目レベルで EFA/CFA を行います |
| 既に十分検証された一次元尺度 | 条件付きで使用可能です |
| 多次元性が疑われる尺度 | 使わない。多次元性を隠す危険があります |
| SEM の構造パスが主目的 | 測定モデルが別研究で十分確認済みなら選択肢になります |

報告では、parcel の作成方法（ランダム、バランス法、内容領域別）、項目レベルでの事前検証、parceling を使う理論的理由を明記します。

#### 収束的妥当性と弁別的妥当性
- **収束的妥当性**: 標準化因子負荷量が .50 以上、AVE（平均分散抽出）が .50 以上
- **弁別的妥当性**: 因子間相関の二乗が各因子の AVE を下回る（Fornell-Larcker 基準）
- `lavaan` では `lavInspect(fit, "cor.lv")` で因子間相関を確認できる

CFA では測定モデル（潜在変数と観測変数の関係）の妥当性を検証した。次の SEM では、CFA で確認された潜在変数同士の因果関係・媒介効果を推定する。SEM = 測定モデル（CFA）+ 構造モデル（潜在変数間のパス）という二層構造を持つ。

### 構造方程式モデリング（SEM）

::: {.callout-tip}
**1 分プレビュー (SEM)**

「Ideal L2 Self → Effort → Proficiency という媒介仮説を、**測定誤差を補正したまま**潜在変数レベルで検証したい」という時に使います。CFA の測定モデル（潜在変数 ⇄ 観測項目）に**構造モデル**（潜在変数間の回帰・媒介・相関）を追加します。間接効果は bootstrap CI で評価し、Sobel 検定よりも頑健です。「重回帰の SEM 版」と理解できますが、決定的な違いは (a) 測定誤差の分離、(b) 複数の目的変数を同時推定、(c) 潜在変数同士の関係を直接検証できる点です。**因果は仮定するもの、SEM が証明するわけではない**点に注意（第 18 章因果推論と接続）。
:::

#### コンセプト

構造方程式モデリング（SEM）は、CFA の**測定モデル**（潜在変数と観測変数の対応関係を定義する部分）に、**構造モデル**（潜在変数間の回帰関係を定義する部分）を加えたものである[@kline2023]。測定モデルは「目に見えない構成概念をどの項目で測るか」を記述し、構造モデルは「その構成概念同士がどのように関連するか（因果仮説）」を記述する。SLA 研究では、動機づけ、不安、自律性といった潜在構成概念間の因果仮説を検証するために広く用いられる[@innami2011; @winke2014]。

回帰分析では観測変数をそのまま用いるため測定誤差が無視される。SEM は潜在変数を介して測定誤差を明示的にモデル化するため、より正確な構造パラメータの推定が可能になる。これが SEM を「測定誤差を補正した回帰分析」と呼べる根拠である。

#### 概要
1. SEM = 測定モデル（CFA）+ 構造モデル（潜在回帰）
2. まず CFA で測定モデルの適合を確認してから構造パスを追加する（Two-Step Approach）
3. 媒介効果は `:=` で定義し、ブートストラップで信頼区間を求める
4. Likert 尺度では `estimator = "WLSMV"` を検討する

#### SEM の2部構造

$$\underbrace{\mathbf{x} = \boldsymbol{\Lambda} \boldsymbol{\eta} + \boldsymbol{\varepsilon}}_{\text{測定モデル}} \quad ; \quad \underbrace{\boldsymbol{\eta} = \mathbf{B} \boldsymbol{\eta} + \boldsymbol{\Gamma} \boldsymbol{\xi} + \boldsymbol{\zeta}}_{\text{構造モデル}}$$

- 測定モデル: 観測変数 $\mathbf{x}$ を潜在変数 $\boldsymbol{\eta}$ で説明（$\boldsymbol{\Lambda}$ = 因子負荷行列、$\boldsymbol{\varepsilon}$ = 測定誤差）
- 構造モデル: 潜在変数間の回帰関係を記述（$\mathbf{B}$ = 内生変数間の回帰係数、$\boldsymbol{\Gamma}$ = 外生→内生の回帰係数、$\boldsymbol{\zeta}$ = 残差）

SEM の推定は、モデルのパラメータ $\boldsymbol{\theta}$（負荷量・パス係数・分散等）から**含意共分散行列** $\boldsymbol{\Sigma}(\boldsymbol{\theta})$ を計算し、それが実データの標本共分散行列 $\mathbf{S}$ にどれだけ近いかを最適化する過程である。直感的には、「このモデル（仮説）が正しければ、変数間の共分散はこうなるはず」という予測値と、実際のデータから計算される共分散の差を最小化している。最尤推定（ML）では、対数尤度関数 $F_{\text{ML}} = \log|\boldsymbol{\Sigma}(\boldsymbol{\theta})| + \text{tr}(\mathbf{S}\boldsymbol{\Sigma}(\boldsymbol{\theta})^{-1}) - \log|\mathbf{S}| - p$ を最小化する（$p$ は観測変数の数）。

#### 回帰分析からSEMへ: 統一的な視点

実は、これまでの章で学んできた回帰分析もSEMの特殊ケースとして理解できる。

| モデル | SEM的解釈 | 潜在変数 | 測定モデル |
|--------|----------|---------|-----------|
| 単回帰 $y = \beta x + \varepsilon$ | 観測変数間の直接効果1本 | なし | なし |
| 重回帰 $y = \beta_1 x_1 + \beta_2 x_2 + \varepsilon$ | 複数の観測変数→1つの結果変数 | なし | なし |
| パス解析 | 観測変数のみのSEM（媒介・直接効果） | なし | なし |
| CFA | 測定モデルのみ | あり | あり |
| SEM（フル） | 測定モデル＋構造モデル | あり | あり |

この統一的な視点を持つことで、**回帰分析は「潜在変数がなく、測定誤差を無視した SEM」**と位置づけられる。SEM の利点は、(1) 測定誤差を明示的にモデリングできること、(2) 複数の目的変数を同時に扱えること、(3) 直接効果と間接効果を同時推定できることにある。

#### Two-Step Approach
1. **Step 1**: 測定モデル（CFA）を推定し、適合度を確認する
2. **Step 2**: 測定モデルが許容範囲内であれば、構造パスを追加して SEM を推定する
- この手順を守ることで、測定の問題と構造の問題を切り分けて診断できる[@anderson1988; @kline2023]

#### SLA 研究における SEM の活用

SEM は SLA 研究において理論モデルの検証に広く活用されている。@innami2011 は *Language Assessment Quarterly* で言語テスティング・学習研究における SEM の使用状況をレビューし、報告慣行の問題点と改善策を整理した。@kunnan1998 はそれに先立ち、言語評価研究に SEM を導入するためのチュートリアルを *Language Testing* 誌で発表している。以下は SLA における SEM の代表的な活用例である。

- **動機づけ → 学習行動 → 到達度**: @papi2010 は1,011名のイラン高校生のデータで L2MSS の3要素が不安と意図的努力に与える影響を SEM で検証し、Ideal L2 Self と L2 Learning Experience が不安を低減する一方、Ought-to L2 Self は不安を増加させることを示した。
- **WTC モデル**: @yashima2002 は297名の日本人大学生のデータで「国際的志向性 → 動機づけ → L2自信 → WTC」の因果経路を SEM で明らかにし、L2 WTC 研究に新たな枠組みを提示した。
- **L2 selves と情動**: @teimouri2017 は Ideal L2 Self / Ought-to L2 Self を三分法に拡張し、各自己概念が享受や不安といった情動を介して動機的行動を予測するモデルを SEM で検証した。
- **年齢差と動機**: @kormos2008 は3つの年齢群（中高生・大学生・成人）で L2 動機づけモデルを SEM で比較し、Ideal L2 Self の影響力が年齢層によって異なることを示した。
- **大規模調査**: @you2016 は中国 EFL 学習者の大規模調査で L2MSS の構造関係をジェンダー差に着目して SEM で検証した。
- **テストの構成概念妥当性**: @winke2014 は *Annual Review of Applied Linguistics* のチュートリアル論文で、SEM を用いた言語テストの構成概念妥当性検証を step-by-step で解説し、リスニング・リーディング・文法テスト間の潜在関係モデリングの実例を示した。

重要なのは、SEM は**因果関係を「証明」するものではない**ということである[@kline2023]。SEM が示すのは「データがこの因果仮説と矛盾しない」ということであり、因果の方向性はあくまで理論に基づく仮定である。同じデータに対して、異なる因果方向のモデルが等しく適合することもある（等価モデルの問題）。

#### lavaan モデル構文リファレンス

lavaan [@rosseel2012] のモデル定義では、以下の演算子を使って測定モデルと構造モデルを記述する。

| 演算子 | 意味 | 使用例 | 説明 |
|--------|------|--------|------|
| `=~` | 測定モデル（因子→指標） | `F1 =~ x1 + x2 + x3` | 潜在変数 F1 が x1, x2, x3 を生成する |
| `~` | 回帰パス | `y ~ x1 + x2` | y を x1 と x2 で予測する |
| `~~` | 共分散（残差相関） | `x1 ~~ x2` | x1 と x2 の残差共分散を推定する |
| `~1` | 切片（平均） | `F1 ~ 1` | 潜在変数 F1 の平均を推定する |
| `:=` | 定義済みパラメータ | `ind := a * b` | ラベル付きパスの積で間接効果を定義 |
| `*` | ラベル / 固定値 | `F1 =~ 1*x1` | 負荷量を1に固定（スケーリング） |

#### lavaan の主要関数

| 関数 | 目的 | 返り値の主要列 |
|------|------|--------------|
| `cfa()` / `sem()` / `growth()` | モデル推定 | lavaan オブジェクト |
| `summary(fit, standardized = TRUE)` | 結果の要約 | テキスト出力 |
| `parameterEstimates(fit)` | パラメータテーブル | `lhs`（左辺）, `op`（演算子）, `rhs`（右辺）, `est`（推定値）, `se`（標準誤差）, `z`（z値）, `pvalue`, `ci.lower`, `ci.upper`, `std.all`（完全標準化値） |
| `fitMeasures(fit)` | 適合度指標 | `chisq`, `df`, `pvalue`, `cfi`, `tli`, `rmsea`, `rmsea.ci.lower`, `rmsea.ci.upper`, `srmr` |
| `modindices(fit)` | 修正指標 | `lhs`, `op`, `rhs`, `mi`（修正指標値）, `epc`（期待パラメータ変化量） |
| `standardizedSolution(fit)` | 標準化解 | `est.std`（標準化推定値） |
| `lavInspect(fit, "rsquare")` | 決定係数 | 各内生変数の $R^2$ |

#### 推定法の選択ガイド

| データの特徴 | 推奨推定法 | lavaan 指定 |
|-------------|-----------|------------|
| 連続変数、正規性あり | ML（最尤法） | `estimator = "ML"` |
| 連続変数、正規性に不安 | MLR（ロバストML） | `estimator = "MLR"` |
| 順序カテゴリ（5件法以下） | WLSMV | `estimator = "WLSMV", ordered = names(data)` |
| 間接効果の CI | ブートストラップ | `se = "bootstrap", bootstrap = 5000` |
| 欠損データあり | FIML（完全情報ML） | `missing = "fiml"` |

FIML（Full Information Maximum Likelihood）は、欠損値を含むデータに対して、各ケースで利用可能な情報を最大限に活用してパラメータを推定する方法です。リストワイズ削除と異なり、欠損がランダム（MAR: Missing at Random）であれば偏りの少ない推定が期待できます。lavaan では、ML/MLR 推定時に `missing = "fiml"` を明示して利用します。SLA 研究のように縦断データで脱落が生じやすい場面で特に有用ですが、順序カテゴリ指標の WLSMV では同じ扱いにならない点に注意します。

#### 回帰分析は SEM で再現できる

SEM を学ぶ前に、回帰分析が SEM の特殊ケースであることを確認しましょう。以下のデモでは、`lm()` による重回帰と `lavaan::sem()` で同一の結果が得られることを確認します。

```{r}
#| eval: true
#| label: sem-regression-equivalence

# Demo: lm() and lavaan::sem() produce identical results
set.seed(42)
demo_data <- data.frame(
  motivation = rnorm(200),
  anxiety    = rnorm(200),
  proficiency = rnorm(200)
)
demo_data$proficiency <- 0.4 * demo_data$motivation -
                          0.3 * demo_data$anxiety + rnorm(200, sd = 0.8)

# Method 1: lm()
fit_lm <- lm(proficiency ~ motivation + anxiety, data = demo_data)

# Method 2: lavaan::sem() (path analysis = SEM without latent variables)
path_model <- 'proficiency ~ motivation + anxiety'
fit_path <- lavaan::sem(path_model, data = demo_data)

tibble(
  Predictor = c("motivation", "anxiety"),
  `lm() estimate` = coef(fit_lm)[c("motivation", "anxiety")],
  `sem() estimate` = parameterEstimates(fit_path) |>
    dplyr::filter(op == "~") |> pull(est)
) |>
  knitr::kable(
    caption = "Regression via lm() vs. lavaan::sem()",
    digits = 4,
    booktabs = TRUE
  )
```

結果が一致することから分かるように、**重回帰分析はパス解析（観測変数のみの SEM）と数学的に等価**である。lavaan で `~` のみを使ったモデル（`=~` なし）はパス解析であり、通常の回帰と同じ推定を行う。SEM の強みは、ここに `=~` で測定モデルを追加し、**測定誤差を考慮した潜在変数レベルの関係**を推定できる点にある。

#### パス解析 → SEM への拡張

パス解析では観測変数をそのまま使うため、測定誤差がパス係数を**減衰（attenuation）**させる。例えば、信頼性 .80 の尺度で測定した動機づけと英語力の間の真の相関が .50 であっても、観測レベルの相関は $.50 \times \sqrt{.80 \times .80} = .40$ に低下する。SEM（潜在変数モデル）はこの減衰を補正し、構成概念レベルの関係をより正確に推定する。これが、SLA 研究で尺度データを扱う際にパス解析よりも SEM が推奨される主な理由である[@kline2023]。

#### SLA データ例: 動機づけ → 努力 → 英語力

D&ouml;rnyei の L2 動機づけ自己体系に基づき、「Ideal L2 Self → 学習努力（Intended Effort）→ 英語力（L2 Proficiency）」の媒介モデルを検証する。

5潜在変数の相関構造は @papi2010 のイランEFL学習者データおよび @you2016 の中国EFL学習者データの相関パターンを参考に設定した。Ideal L2 Self と Effort の相関を $.55$ と比較的高く設定しているのは、複数の先行研究で一貫して報告されている強い正の関連を反映している。

```{r}
#| eval: true
#| label: sem-data-simulation

set.seed(2024)
n_sem <- 400

# Latent variables: IdealL2, OughtL2, LearnExp, Effort, Proficiency
Phi_sem <- matrix(c(
  1.0, 0.45, 0.35, 0.55, 0.40,
  0.45, 1.0, 0.25, 0.30, 0.20,
  0.35, 0.25, 1.0, 0.45, 0.35,
  0.55, 0.30, 0.45, 1.0, 0.50,
  0.40, 0.20, 0.35, 0.50, 1.0
), nrow = 5)

f_sem <- MASS::mvrnorm(n_sem, mu = rep(0, 5), Sigma = Phi_sem)

# Motivation items (same structure) + Effort (3 items) + Proficiency (3 items)
Lambda_mot <- Lambda_efa  # 18 x 3

raw_mot <- f_sem[, 1:3] %*% t(Lambda_mot) + matrix(rnorm(n_sem * 18, sd = 0.5), nrow = n_sem)
likert_mot <- apply(raw_mot, 2, function(x) pmin(pmax(round(scales::rescale(x, to = c(1, 6))), 1), 6))

# Effort items (3 items)
effort_raw <- cbind(
  f_sem[, 4] * 0.80 + rnorm(n_sem, sd = 0.5),
  f_sem[, 4] * 0.75 + rnorm(n_sem, sd = 0.5),
  f_sem[, 4] * 0.70 + rnorm(n_sem, sd = 0.5)
)
effort_lik <- apply(effort_raw, 2, function(x) pmin(pmax(round(scales::rescale(x, to = c(1, 6))), 1), 6))

# Proficiency items (3 test scores, continuous)
prof_raw <- cbind(
  f_sem[, 5] * 0.85 + rnorm(n_sem, sd = 0.4),
  f_sem[, 5] * 0.78 + rnorm(n_sem, sd = 0.4),
  f_sem[, 5] * 0.72 + rnorm(n_sem, sd = 0.4)
)

# Combine
sem_data <- data.frame(likert_mot, effort_lik, prof_raw)
names(sem_data) <- c(paste0("M", sprintf("%02d", 1:18)),
                      paste0("E", 1:3),
                      paste0("P", 1:3))
```

以下では、5つの潜在変数（IdealL2, OughtL2, LearnExp, Effort, Profic）を含む SEM を定義する。モデル式では、測定モデル（`=~`）で各潜在変数を観測変数に対応づけ、構造モデル（`~`）で潜在変数間の回帰パスを指定する。パス係数に `a1*IdealL2` のようにラベルを付けると、`:=` で間接効果（積）を定義できる。

`lavaan::sem()` の主要引数:

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `std.lv` | `TRUE` | 潜在変数の分散を 1 に固定（CFA と同様） |
| `estimator` | `"MLR"` | **ロバスト最尤推定量**。ロバスト標準誤差とスケール補正された検定統計量により、正規性の仮定が厳密に成立しない場合にも比較的頑健な推定が得られる。Likert データや歪んだ分布で検討する |

```{r}
#| eval: true
#| label: sem-model-estimation

sem_model <- '
  # Measurement model
  IdealL2   =~ M01 + M02 + M03 + M04 + M05 + M06
  OughtL2   =~ M07 + M08 + M09 + M10 + M11 + M12
  LearnExp  =~ M13 + M14 + M15 + M16 + M17 + M18
  Effort    =~ E1 + E2 + E3
  Profic    =~ P1 + P2 + P3

  # Structural model
  Effort  ~ a1*IdealL2 + a2*OughtL2 + a3*LearnExp
  Profic  ~ b*Effort + c1*IdealL2

  # Indirect effects
  ind_ideal  := a1 * b
  tot_ideal  := c1 + (a1 * b)
'
```

SLA 研究での媒介分析の典型例として、「L2 動機づけ → 学習努力 → 英語力」というモデルがあります。単純回帰では動機づけと英語力の直接関係のみを扱いますが、SEM 媒介モデルでは「動機づけが学習努力を介して英語力と関連する」という間接関連を、測定モデル込みで評価できます。横断データだけでは因果媒介を証明できないため、因果解釈には時間順序、交絡統制、実験・縦断デザインが必要です。

```{r}
#| eval: true
#| label: sem-model-estimation-fit

fit_sem <- lavaan::sem(sem_model, data = sem_data, std.lv = TRUE, estimator = "MLR")

parameterEstimates(fit_sem, standardized = TRUE) |>
  dplyr::filter(op %in% c("=~", "~", "~~")) |>
  dplyr::select(lhs, op, rhs, est, se, z, pvalue, std.all) |>
  knitr::kable(
    caption = "SEM: Parameter Estimates",
    digits = 3,
    booktabs = TRUE
  )
```

SEM のパラメータ推定値テーブルでは、測定モデル部分（`=~`）の標準化負荷量が CFA と同様に十分な値を示すか確認する。構造パス部分（`~`）では、各パスの `std.all` 列が標準化回帰係数に対応する。例えば Ideal L2 Self から Effort への標準化パス係数が正で有意であれば、理想 L2 自己像が高い学習者ほど学習努力が大きいことを意味する。間接効果（`:=`）は、媒介経路を通じた効果の大きさを示す[@hayes2022]。

#### パラメータ推定値

```{r}
#| eval: true
#| label: sem-coefficients

coef_tbl <- parameterEstimates(fit_sem, standardized = TRUE) |>
  as_tibble() |>
  dplyr::filter(op %in% c("~", "=~", ":=")) |>
  transmute(
    Term = paste(lhs, op, rhs),
    Estimate = est, SE = se, z = z, p = pvalue, Std = std.all
  )

coef_tbl |>
  knitr::kable(
    caption = "SEM Parameter Estimates: Standardized and Unstandardized",
    digits = 3,
    booktabs = TRUE
  )
```

パス係数の大きさと有意性を視覚的に比較するため、構造パスと間接効果をフォレストプロットで可視化する。

```{r}
#| eval: true
#| label: sem-path-forest
#| fig-width: 9
#| fig-height: 5

# Forest plot of structural paths
struct_paths <- parameterEstimates(fit_sem, standardized = TRUE) |>
  dplyr::filter(op %in% c("~", ":=")) |>
  mutate(
    Path = paste(lhs, op, rhs),
    Type = ifelse(op == ":=", "Indirect", "Direct"),
    sig = ifelse(pvalue < .05, "p < .05", "n.s.")
  )

ggplot(struct_paths, aes(y = reorder(Path, std.all), x = std.all,
                          xmin = ci.lower, xmax = ci.upper,
                          color = Type, shape = sig)) +
  geom_vline(xintercept = 0, linetype = "dashed", color = "grey50") +
  geom_pointrange(size = 0.6) +
  scale_color_manual(values = c("Direct" = "#2166ac", "Indirect" = "#b2182b")) +
  scale_shape_manual(values = c("p < .05" = 16, "n.s." = 1)) +
  labs(title = "SEM Structural Paths: Standardized Estimates with 95% CI",
       subtitle = "Blue = direct effects; Red = indirect effects",
       x = "Standardized Estimate (β)", y = NULL,
       color = "Effect Type", shape = "Significance") +
  theme(plot.title = element_text(face = "bold"))
```

フォレストプロットでは、0を示す垂直破線と信頼区間が交差しないパスが統計的に有意である。直接効果（青）と間接効果（赤）を区別して表示することで、媒介構造の全体像が一目で把握できる。

#### パス図

```{r}
#| eval: true
#| label: sem-path-diagram
#| fig-width: 12
#| fig-height: 10

semPlot::semPaths(fit_sem, whatLabels = "std", layout = "tree",
                  nCharNodes = 0, edge.label.cex = 0.7,
                  sizeMan = 5, sizeLat = 9)
```

SEM のパス図では、実線矢印は仮説された因果関係（回帰パス）を表す。パス係数には非標準化解（`est`）と標準化解（`std.all`）があり、前者は元のスケールでの効果量、後者は変数間の相対的な効果の比較に使う。論文報告では通常、標準化解を主に報告し、非標準化解を補足する。

#### 直接効果・間接効果・総効果
- **直接効果**: Ideal L2 Self → Proficiency のパス（$c_1$）
- **間接効果**: Ideal L2 Self → Effort → Proficiency（$a_1 \times b$）
- **総効果**: 直接 + 間接（$c_1 + a_1 \times b$）
- `parameterEstimates()` で `:=` 行を確認する[@preacher2008]

#### ブートストラップによる間接効果の信頼区間

間接効果（$a \times b$）の標本分布は正規分布に従わないため、通常の $z$ 検定は不適切である。ブートストラップ法は、データからの再抽出を繰り返して間接効果の経験的分布を構成し、そこからパーセンタイル信頼区間を算出する。**95% CI に 0 が含まれなければ、間接効果は有意**と判断する。

ブートストラップに関する引数:

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `se` | `"bootstrap"` | 標準誤差の計算にブートストラップ法を使用する |
| `bootstrap` | `5000` | 再抽出回数。**論文投稿時は 5,000–10,000 が現代標準**（@hayes2022）。下のデモでは計算時間短縮のため 500 を使用 |
| `estimator` | `"ML"` | `lavaan` の実装上、`se = "bootstrap"` 指定時は ML 推定が必要（MLR との併用は不可） |

```{r}
#| label: sem-bootstrap
#| eval: true

# Bootstrap CI for indirect effect
# NOTE: bootstrap = 500 is for demo only; use bootstrap = 5000+ in publications
set.seed(2024)
fit_sem_boot <- lavaan::sem(
  sem_model, data = sem_data,
  std.lv = TRUE, estimator = "ML",
  se = "bootstrap", bootstrap = 500
)

parameterEstimates(fit_sem_boot, boot.ci.type = "perc") |>
  as_tibble() |>
  dplyr::filter(op == ":=") |>
  dplyr::select(lhs, rhs, est, ci.lower, ci.upper) |>
  knitr::kable(
    caption = "Bootstrap Confidence Intervals for Indirect Effects",
    digits = 3,
    booktabs = TRUE
  )
```

ブートストラップ CI の判定は、信頼区間に 0 が含まれるか否かで行います。CI が 0 を含まなければ、間接効果は統計的に有意であり、Ideal L2 Self が学習努力を介して Proficiency と関連するという媒介仮説と整合的です。

::: {.callout-note}
**Bootstrap CI の選択と Sobel 検定への現代的評価**

間接効果の信頼区間構築には複数の方法があります:

| 方法 | `boot.ci.type` | 特徴 | 現代的推奨度 |
|------|----------------|------|------------|
| **Percentile bootstrap** | `"perc"` | ブートストラップ分布の 2.5/97.5 パーセンタイル。シンプル、Type I error が概ね名義水準 | ★★★ 標準推奨 |
| **Bias-corrected (BC)** | `"bc"` | 中央値バイアス補正 | ★★ 注意: タイプ I error 膨張の懸念 (@hayes2022; Falk & Biesanz 2016) |
| **BCa (bias-corrected accelerated)** | `"bca.simple"` | 加速度パラメータ含む | ★★ 同上 |
| **Monte Carlo CI** | (`semTools::monteCarloCI`) | パラメータ事後分布から $a \times b$ をシミュレート | ★★★ Bootstrap 不可時の代替 |
| **Sobel 検定 / Delta 法** | (古典) | $z = ab/\sqrt{a^2 \sigma_b^2 + b^2 \sigma_a^2}$ | ★ 非推奨（積の正規性仮定に問題） |

近年の知見（Hayes & Scharkow 2013; Falk & Biesanz 2016）では、**percentile bootstrap または Monte Carlo CI が第一選択**で、bias-corrected 系はタイプ I error 膨張の懸念があり推奨度が下がっています。Sobel 検定は積 $a \times b$ の標本分布が正に歪むため正規近似が不適切で、現代の論文ではブートストラップ CI に置き換えるのが標準です。
:::

```{r}
#| eval: true
#| label: sem-indirect-forest
#| fig-width: 8
#| fig-height: 4

# Forest plot of indirect effects with bootstrap CI
ind_effects <- parameterEstimates(fit_sem_boot, boot.ci.type = "perc") |>
  as_tibble() |>
  dplyr::filter(op == ":=") |>
  dplyr::select(lhs, est, ci.lower, ci.upper)

ggplot(ind_effects, aes(y = lhs, x = est, xmin = ci.lower, xmax = ci.upper)) +
  geom_vline(xintercept = 0, linetype = "dashed", color = "grey50") +
  geom_pointrange(size = 0.8, color = "#2166ac") +
  labs(title = "Indirect Effects with 95% Bootstrap CI",
       subtitle = "Percentile method (500 bootstrap samples)",
       x = "Estimate", y = "Effect") +
  theme(plot.title = element_text(face = "bold"))
```

フォレストプロットでは、各間接効果の点推定値（丸）と 95% ブートストラップ信頼区間（横棒）を可視化しています。信頼区間が 0 を示す垂直破線と交差しなければ、その間接関連は統計的に有意と判断します。信頼区間の幅が狭いほど推定の不確実性が小さいことを示します。

#### 順序尺度指標の推定（WLSMV）

Likert 尺度のデータは厳密には連続変数ではない。カテゴリ数が5以下の場合、重み付き最小二乗法（WLSMV）が推奨される。WLSMV に関する引数:

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `estimator` | `"WLSMV"` | 重み付き最小二乗法（平均・分散補正付き）。順序データに対してポリコリック相関を内部的に計算する |
| `ordered` | 変数名のベクトル | **順序カテゴリカルとして扱う変数を明示的に指定する**。指定された変数は閾値モデルで推定される。指定しなければ全て連続変数として扱われる |

```{r}
#| eval: true
#| label: sem-wlsmv

# WLSMV estimation for ordinal indicators
ordinal_vars <- c(paste0("M", sprintf("%02d", 1:18)), paste0("E", 1:3))
fit_wlsmv <- lavaan::sem(
  sem_model, data = sem_data,
  std.lv = TRUE, estimator = "WLSMV",
  ordered = ordinal_vars
)

fit_mlr_idx <- fitMeasures(fit_sem, c("cfi", "tli", "rmsea", "srmr"))
fit_wlsmv_idx <- fitMeasures(fit_wlsmv, c("cfi", "tli", "rmsea", "srmr"))

data.frame(
  Index = names(fit_mlr_idx),
  MLR = as.numeric(fit_mlr_idx),
  WLSMV = as.numeric(fit_wlsmv_idx)
) |>
  knitr::kable(
    caption = "Estimator Comparison: MLR vs WLSMV",
    digits = 3,
    booktabs = TRUE
  )
```

#### 推定法の選択ガイド
- **ML / MLR**: 連続変数またはカテゴリ数 ≥ 6 の場合。MLR は非正規性にやや頑健。
- **WLSMV**: カテゴリ数 ≤ 5 の順序変数。`ordered = ...` で変数を指定。
- **ベイズ推定**: 小サンプルや複雑なモデルで有用（本書第7章参照）。

#### 2値データの CFA: テスト項目（正誤データ）への適用

言語テスト研究では、テスト項目の反応（正答 = 1, 誤答 = 0）が2値データとして得られることが多いです。2値データに対して CFA を適用する場合、通常の Pearson 相関と ML 推定をそのまま使うよりも、**テトラコリック相関**と**WLSMV 推定量**に基づくカテゴリカル CFA が標準的な選択肢です。目的によっては MML/IRT、ベイズ推定、ロバスト推定も候補になります。

#### 閾値モデル（threshold model）: 2値・順序データの背後にある連続変数

WLSMV は、観測される離散的な反応（0/1 や 1--5 の Likert）の背後に**連続的な正規分布する潜在反応変数** $y^*$ が存在すると仮定します。テスト項目の場合、受験者の潜在的な能力 $y^*$ が閾値 $\tau$ を超えれば正答（$y = 1$）、超えなければ誤答（$y = 0$）と解釈します。

$$P(y_j = 1) = P(y^*_j > \tau_j) = \Phi\!\left(\frac{-\tau_j + \lambda_j^{\mathrm{std}} \theta}{\sqrt{1 - (\lambda_j^{\mathrm{std}})^2}}\right)$$

ここで $\lambda_j^{\mathrm{std}}$ は **標準化された因子負荷量**（潜在分散 1 のもとで推定された負荷で、$|\lambda_j^{\mathrm{std}}| < 1$）、$\theta$ は潜在能力（標準正規）、$\tau_j$ は項目 $j$ の閾値（困難度に対応）、$\Phi$ は標準正規累積分布関数です。分母の $\sqrt{1 - (\lambda_j^{\mathrm{std}})^2}$ は残差 $y^*_j - \lambda_j^{\mathrm{std}}\theta$ の標準偏差で、$\lambda_j^{\mathrm{std}}$ が標準化解の場合に成立します。

非標準化負荷 $\lambda_j$ と残差分散 $\theta_{jj}$ で書く場合は、$P(y_j=1) = \Phi\!\left((-\tau_j + \lambda_j \theta) / \sqrt{\theta_{jj}}\right)$ となり、識別力は $a_j = \lambda_j / \sqrt{\theta_{jj}}$ で与えられます。標準化解では $\theta_{jj} = 1 - (\lambda_j^{\mathrm{std}})^2$ なので両者は一致します。

この式は **2パラメータ IRT（2PL）モデルの probit 版**と近い形で対応します。ただし、ロジットリンクを用いる一般的な IRT モデルとは尺度化が $1.7$ 倍程度異なるため、CFA の負荷量や閾値をそのまま IRT の識別力・困難度として解釈しないようにします（変換式: $a_{\mathrm{logit}} \approx 1.7 \times a_{\mathrm{probit}}$）。

例えば、5件法の Likert 項目（1 = 全く当てはまらない 〜 5 = 非常に当てはまる）では 4 つの閾値が推定されます。「全く当てはまらない」と「あまり当てはまらない」の境界が第1閾値であり、背後の連続変数がこの値を超えると回答カテゴリが変わると仮定します。

以下のコードでは、英語読解テスト（12項目、2技能）のシミュレーションデータを使って、2値データの CFA を実装します。

```{r}
#| eval: true
#| label: binary-cfa-simulation

set.seed(2024)
n_test <- 500

# Latent abilities (correlated: r = .60)
theta <- MASS::mvrnorm(n_test, mu = c(0, 0),
                       Sigma = matrix(c(1, 0.6, 0.6, 1), 2))

# Item parameters (difficulty and discrimination)
item_diff <- c(-1.0, -0.5, 0.0, 0.3, 0.7, 1.2,   # Reading items
               -0.8, -0.3, 0.1, 0.5, 0.8, 1.5)     # Grammar items
item_disc <- c(rep(0.8, 6), rep(0.75, 6))            # discrimination ~ loading

binary_data <- matrix(NA, n_test, 12)
for (j in 1:12) {
  skill <- ifelse(j <= 6, 1, 2)
  prob <- pnorm(item_disc[j] * theta[, skill] - item_diff[j])
  binary_data[, j] <- rbinom(n_test, 1, prob)
}
colnames(binary_data) <- paste0("item", sprintf("%02d", 1:12))
binary_data <- as.data.frame(binary_data)
```

```{r}
#| eval: true
#| label: binary-cfa-estimation

binary_cfa_model <- '
  Reading =~ item01 + item02 + item03 + item04 + item05 + item06
  Grammar =~ item07 + item08 + item09 + item10 + item11 + item12
'

fit_binary_cfa <- lavaan::cfa(
  binary_cfa_model, data = binary_data,
  std.lv = TRUE, estimator = "WLSMV",
  ordered = names(binary_data)  # explicit vector is safer than `TRUE` (avoids deprecation in newer lavaan)
)

fitMeasures(fit_binary_cfa, c("chisq.scaled", "df.scaled", "pvalue.scaled",
                               "cfi.scaled", "tli.scaled",
                               "rmsea.scaled", "srmr")) |>
  as.data.frame() |>
  rownames_to_column("Index") |>
  rename(Value = 2) |>
  knitr::kable(
    caption = "CFA for Binary Test Data (WLSMV)",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: binary-cfa-loadings

# Standardized loadings
standardizedSolution(fit_binary_cfa) |>
  dplyr::filter(op == "=~") |>
  dplyr::select(lhs, rhs, est.std, se, pvalue) |>
  knitr::kable(
    caption = "Standardized Factor Loadings (Binary CFA)",
    digits = 3,
    booktabs = TRUE
  )
```

上記の結果では、WLSMV 推定量がテトラコリック相関と閾値モデルに基づいて因子負荷量を推定しています。`est.std` 列の標準化負荷量は、probit IRT・潜在分散1という特定の尺度化のもとで、2PL IRT モデルの識別力パラメータ $a$ と次のように近似的に対応します。

$$a_j = \frac{\lambda_j}{\sqrt{1 - \lambda_j^2}}$$

つまり、probit 尺度では **CFA の標準化負荷量が .70 の項目は、識別力 $a = 0.70 / \sqrt{1 - 0.49} \approx 0.98$ に近い値に対応します**。一般的なロジット 2PL へそのまま代入することはできず、リンク関数の尺度変換が必要です。この変換を知っておくと、CFA と IRT の結果を相互に参照できます（第11章参照）。

#### 2値データの CFA と IRT: いつどちらを使うか

| 目的 | 推奨手法 | 理由 |
|------|---------|------|
| テストの因子構造（何次元か）を検証 | **CFA（WLSMV）** | 多因子モデルの適合度を直接評価できる |
| 項目パラメータ（難易度・識別力）の推定 | **IRT** | 項目・受験者のパラメータ分離が IRT の本領 |
| 受験者の能力推定（$\theta$ スコア） | **IRT** | 条件付き推定が可能 |
| テストの測定不変性（DIF）検出 | **CFA（多群比較）** または **IRT（DIF分析）** | 両方有効。CFA は項目群レベル、IRT は項目レベル |
| 尺度開発の構成概念妥当性 | **CFA → SEM** | 他の構成概念との関係も同時に検証できる |

実務的には、テスト開発の初期段階で CFA（WLSMV）により因子構造を確認し、構造が確認された後に IRT で項目パラメータの精密な推定と能力推定を行う、という**二段階アプローチ**が推奨される。

#### 修正指標（Modification Indices）

```{r}
#| eval: true
#| label: sem-modification-indices

mi_tbl <- modindices(fit_sem) |>
  as_tibble() |>
  arrange(desc(mi)) |>
  dplyr::filter(mi > 10) |>
  dplyr::select(lhs, op, rhs, mi, epc)

mi_tbl |>
  knitr::kable(
    caption = "Modification Indices (MI > 10)",
    digits = 2,
    booktabs = TRUE
  )
```

#### 修正指標と過適合
MI はモデル改善のヒントだが「事後探索」に過ぎない。理論的妥当性がない関連を追加すると過適合に陥る。報告では、どの修正を、どんな根拠で採用 / 不採用にしたかを明記すること。**修正後のモデルは、可能な限り別サンプルで再検証する**ことが望ましく、単一サンプル上での MI 駆動修正は「探索的」とラベルし、その結果を「確認的」と主張しないでください。

#### SEM のモデル比較: ネストモデルと情報量規準

SEM では複数の競合モデルを比較してどのモデルがデータに最も適合するかを判断する場面が頻繁に生じる。例えば、「3因子モデル vs 1因子モデル」「完全媒介モデル vs 部分媒介モデル」「線形成長 vs 二次成長」などである。モデル比較の方法には**ネストモデルの検定**と**情報量規準**の2つのアプローチがある。

#### ネストモデルのカイ二乗差検定（$\Delta \chi^2$）

あるモデル（制約モデル）が別のモデル（自由モデル）にパラメータ制約を加えたものである場合、両モデルは**ネストしている**（nested）という。例えば、1因子 CFA は3因子 CFA に「因子間相関 = 1」の制約を加えたものであり、ネストモデルの関係にある。

ネストモデルの場合、カイ二乗差検定（Likelihood Ratio Test; LRT）で比較できる。

$$\Delta \chi^2 = \chi^2_{\text{制約}} - \chi^2_{\text{自由}}, \quad \Delta df = df_{\text{制約}} - df_{\text{自由}}$$

```{r}
#| eval: true
#| label: sem-model-comparison

comparison_cfa <- anova(fit_cfa_1f, fit_cfa_3f)

comparison_cfa |>
  as_tibble() |>
  knitr::kable(
    caption = "Nested Model Comparison: 1-Factor vs. 3-Factor CFA",
    digits = 3,
    booktabs = TRUE
  )
```

$\Delta \chi^2$ が有意（$p < .05$）であれば、制約の追加がモデル適合を有意に悪化させたことを意味し、**制約の少ない、自由パラメータの多いモデル**が支持されます。モデル自由度で言えば、複雑なモデルほど通常は自由度が小さくなります。上記の例では3因子モデルの適合が1因子モデルより有意に良好であれば、L2 動機づけは単一の構成概念ではなく3因子から成るという仮説と整合的です。

#### MLR 推定量を使用した場合の注意
MLR（ロバスト ML）で推定したモデルのカイ二乗値はスケール補正されているため、**通常のカイ二乗差検定は適用できません**。lavaan の `anova()` は推定量に応じた補正付きの差検定（scaled $\Delta \chi^2$）を計算するため、`anova(model1, model2)` を使えます。手動で計算する場合は `lavaan::lavTestLRT()` を使います。

#### 情報量規準（AIC / BIC）による比較

ネストしていないモデル（例: 3因子モデル vs bifactor モデル）は $\Delta \chi^2$ 検定で比較できない。この場合は**情報量規準**を用いる。

| 規準 | 式 | 特徴 |
|------|-----|------|
| **AIC** | $-2 \log L + 2k$ | パラメータ数 $k$ でペナルティ。予測精度を重視 |
| **BIC** | $-2 \log L + k \log N$ | サンプルサイズ $N$ に依存するペナルティ。大標本で倹約的モデルを選好 |

AIC / BIC とも**値が小さいモデルが望ましい**。

```{r}
#| eval: true
#| label: sem-aic-bic-comparison

aic_bic <- tibble(
  Model = c("1-Factor CFA", "3-Factor CFA"),
  AIC = c(fitMeasures(fit_cfa_1f, "aic"), fitMeasures(fit_cfa_3f, "aic")),
  BIC = c(fitMeasures(fit_cfa_1f, "bic"), fitMeasures(fit_cfa_3f, "bic"))
)

aic_bic |>
  knitr::kable(
    caption = "Information Criteria: AIC and BIC",
    digits = 1,
    booktabs = TRUE
  )
```

#### SEM のモデル比較 vs GLMM のモデル比較: 何が違うか

本書第6章（混合効果モデル）では AIC を用いたモデル比較を扱っているが、SEM のモデル比較にはいくつかの重要な違いがある。

| 観点 | GLMM（第6章） | SEM（本章） |
|------|-------------|------------|
| **主要な比較手段** | AIC / BIC（情報量規準） | $\Delta \chi^2$ 検定（ネストモデル） |
| **AIC の利用可能性** | 常に利用可能 | ML / MLR 推定のみ。**WLSMV では AIC が定義されない** |
| **ネスト関係** | ランダム効果の追加・削除で成立 | パス制約・因子構造の変更で成立 |
| **比較の目的** | 予測精度の最適化（固定効果とランダム効果の選択） | 理論的仮説の検証（因子構造・因果経路の選択） |
| **「最良モデル」の意味** | 予測と倹約のバランス | 理論的に最も妥当な構造 |
| **典型的な比較** | `anova(model1, model2)` で LRT | `anova(model1, model2)` で $\Delta \chi^2$（+ 適合度指標の総合判断） |

**最も重要な違い**は、SEM では AIC/BIC だけでなく**適合度指標（CFI, RMSEA, SRMR）を併用する**点である。GLMM では全体的なモデル適合度指標（CFI 等）が存在しないため AIC/BIC に依存するが、SEM では $\Delta \chi^2$、$\Delta$CFI、AIC/BIC、理論的妥当性を**総合的に判断**する。また、WLSMV 推定量を使用した場合は AIC/BIC が算出されないため、$\Delta \chi^2$（DIFFTEST）と $\Delta$CFI/$\Delta$RMSEA に基づいて比較する。

SLA 研究で多い「測定不変性の段階的比較」も、$\Delta$CFI ≤ .010 という適合度変化量の基準[@chen2007]を用いるものであり、これは AIC に基づく比較とは異なるアプローチである。ΔCFI ≤ .010 という基準は @chen2007 のシミュレーション研究に基づく。サンプルサイズが小さい場合（$n < 200$）、この基準はやや保守的になりうるため、ΔRMSEA（≤ .015）やΔSRMR（≤ .030）との併用が推奨される。

#### 測定不変性（多群比較）

群間（例: 男女、L1背景）で同じ測定モデルが成立するかを検証する。不変性が成立しなければ、群間のスコア比較は意味をなさない[@chen2007; @putnick2016]。

測定不変性の3段階は以下の通りです。**形態的不変性**（configural invariance）は各群で同じ因子構造（どの項目がどの因子に属するか）を仮定します。**計量不変性**（metric invariance）はさらに因子負荷量が群間で等しいことを仮定し、潜在変数の意味が群間で共通であることを保証します。**スカラー不変性**（scalar invariance）は連続指標では切片、順序カテゴリ指標では主に閾値（thresholds）も群間で等しいことを仮定し、潜在平均を群間で比較する前提条件となります。

多群 CFA では、`cfa()` に以下の引数を追加して段階的な制約を課す:

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `group` | `"gender"` | グルーピング変数名。データ内のカテゴリ別に同時推定する |
| `group.equal` | （なし） | **形態的不変性**（Configural）: 同じ因子構造だが全パラメータ自由 |
| `group.equal` | `c("loadings")` | **弱不変性**（Metric）: 因子負荷量を群間で等値制約 |
| `group.equal` | `c("loadings", "intercepts")` | **強不変性**（Scalar）: 連続指標では負荷量＋切片を等値制約。順序カテゴリ指標では `thresholds` の等値制約も検討する |

```{r}
#| eval: true
#| label: sem-measurement-invariance

set.seed(2024)
sem_data$gender <- sample(c("Male", "Female"), n_sem, replace = TRUE)

inv_model <- '
  IdealL2   =~ M01 + M02 + M03 + M04 + M05 + M06
  OughtL2   =~ M07 + M08 + M09 + M10 + M11 + M12
  LearnExp  =~ M13 + M14 + M15 + M16 + M17 + M18
'

# Configural → Metric → Scalar invariance
fit_cfg <- cfa(inv_model, data = sem_data, group = "gender", std.lv = TRUE)
fit_met <- cfa(inv_model, data = sem_data, group = "gender", std.lv = TRUE,
               group.equal = c("loadings"))
fit_scl <- cfa(inv_model, data = sem_data, group = "gender", std.lv = TRUE,
               group.equal = c("loadings", "intercepts"))

# Model comparison
anova(fit_cfg, fit_met, fit_scl) |>
  as_tibble() |>
  knitr::kable(
    caption = "Measurement Invariance Testing",
    digits = 3,
    booktabs = TRUE
  )
```

測定不変性の結果は、Configural → Metric → Scalar の各段階で適合度の変化量（ΔCFI, ΔRMSEA）を確認して判断する[@chen2007; @putnick2016]。ΔCFI ≤ .010 かつ ΔRMSEA ≤ .015 であれば、その段階の不変性が支持される。Metric（弱）不変性が支持されれば因子の意味が群間で同等であり、Scalar（強）不変性まで支持されれば潜在平均の群間比較が可能になる。不変性が棄却された場合は、修正指標を参照して**部分不変性**（partial invariance）の検証を行い、どの項目の等値制約が不適切かを特定する。

#### 実務ヒント: 識別性と健全性チェック

**モデル識別**（identification）は SEM の成否を左右する基本条件である。モデルが識別されるとは、データからパラメータを一意に推定できることを意味する。

- **t ルール**（必要条件）: 推定するパラメータ数 $t$ が、既知の情報量 $p(p+1)/2$（$p$ は観測変数の数）以下でなければなりません。`lavInspect(fit, "npar")` でパラメータ数、`length(lavNames(fit, "ov"))` で観測変数数、`lavInspect(fit, "nobs")` でサンプルサイズを確認できます。
- **各因子に最低3つの指標**が望ましい（3-indicator rule）。**2 indicator rule**（Bollen 1989）によれば、2 指標因子は単独では識別不能ですが、他因子と相関させ等値制約を課すなど追加条件で識別可能になります。ただし empirical underidentification の危険が高く、SE が異常に大きくなる傾向があります。
- **スケーリング**: 潜在変数には固有の尺度がないため、(a) 1つの負荷量を1に固定する（marker variable 法）か、(b) 潜在変数の分散を1に固定する（`std.lv = TRUE`）ことで識別する。
- **Heywood ケース**（分散の負値、標準化負荷 > 1.0）が出たら、指標の見直し・制約追加・サンプル増・推定量変更を検討する。
- `lavaan::lavInspect(fit, "post.check")` で推定後の健全性を確認できる。

#### 測定不変性の段階
1. **形態（Configural）**: 同じ因子構造が両群に成立（パターン同一）
2. **計量（Metric, weak）**: 因子負荷量が等しい $\Lambda^{(1)} = \Lambda^{(2)}$ → 因子の意味が同じ、共分散・回帰係数の群間比較が可能
3. **スカラー（Scalar, strong）**: 連続指標では切片 $\tau^{(1)} = \tau^{(2)}$、順序カテゴリ指標では閾値も等しい → 潜在平均の群間比較が可能
4. **厳密（Strict, residual）**: 残差分散も等しい $\Theta^{(1)} = \Theta^{(2)}$ → 観測値の群間平均差を潜在差として完全に解釈可能
5. **構造的不変性**: 潜在分散・共分散・潜在平均の等値（仮説依存）
- ΔCFI ≤ .010 かつ ΔRMSEA ≤ .015 で不変性が支持される[@chen2007; @putnick2016]
- どの段階まで支持されればよいかは目的依存: 因子構造の解釈には Metric、平均比較には Scalar、観測値の直接比較や ANCOVA 的群間比較には Strict が必要

::: {.callout-note}
**順序カテゴリデータでの測定不変性: Wu-Estabrook (2016) 枠組み**

順序指標（5件法 Likert など）に対して WLSMV 推定で測定不変性を検証する場合、@wu2016 のベースラインが標準です:

- **Configural** (形態): 因子構造のみ同一、閾値・負荷は群間で自由
- **Threshold invariance** (閾値不変): カテゴリ閾値 $\tau_{jc}^{(1)} = \tau_{jc}^{(2)}$ — 連続版の Scalar 不変性に対応
- **Loading invariance** (負荷不変): 閾値に加えて負荷 $\Lambda^{(1)} = \Lambda^{(2)}$ も等値

順序データでは閾値と負荷を**同時に**検討する必要があり、連続データの「Metric → Scalar」順序とは異なります。実装は `lavaan::measEq.syntax()` (推奨) を使い、`semTools::measurementInvariance()` は近年の semTools で **deprecated** となっているため新規コードでは避けてください。検定統計量は Δχ² ではなく Satorra-Bentler scaled difference test または DIFFTEST を用います。**WLSMV のカイ二乗値はモデル間で直接差を取れません** — `lavaan::lavTestLRT()` の DIFFTEST または同等の delta-method 補正が必要です。サンプルサイズが大きく多群（$\geq 10$ 群）の場合は **alignment optimization** [@asparouhov2014alignment]（Mplus 標準）が部分不変性の自動探索に有効です。
:::

::: {.callout-warning}
**Heywood ケースの検出と対処**

Heywood ケースとは、推定された独自分散 $\hat{\Theta}_{jj}$ が**負値**または**ゼロ**、あるいは標準化負荷 $|\hat{\lambda}_j| > 1$ となる識別異常です。原因は概ね 3 系統です:

| 原因 | 診断 | 対処 |
|------|------|------|
| **サンプリング変動** (true $\Theta$ が 0 に近い) | $N$ が小、CI が 0 を含む | サンプル増、ベイズ推定（弱情報事前 $\Theta \sim \mathrm{IG}$） |
| **モデル誤指定** (因子数過大、双子指標) | 修正指標が大、$\Theta$ 推定値が極端 | 因子数を減らす、項目を統合、相関残差を許容 |
| **空間制約による非識別** (2 指標因子) | SE が NA、Hessian 特異 | 等値制約追加、3 指標以上に拡張 |

`lavInspect(fit, "post.check")` または `lavInspect(fit, "gradient")` で Hessian の特異性を診断できます。@chen2001 は「サンプル増 vs モデル修正」の鑑別を勾配ベクトルと事前 simulation で判定する手順を提示。安易に Heywood 項を 0 に固定する（Wothke 1993）と SE が誤推定されるため、ベイズ推定での弱情報正則化が現代的に推奨されます（`blavaan` 参照、第 7 章）。
:::

::: {.callout-note}
**Empirical underidentification の診断**

t-rule（パラメータ数 $\leq p(p+1)/2$）を満たしていても、データ条件によって**実証的に**識別不能となる場合があります（Kenny 1979, Rindskopf 1984）。例えば、因子間相関が真値 0 に近いと相関ベースの識別が崩れます。診断:

- 標準誤差が NA、∞、または異常に大きい
- ヘシアン行列が特異（`lavInspect(fit, "vcov")` の condition number が $> 10^{12}$）
- 推定値が境界値（負荷 = 0、相関 = ±1）に張り付く
- 異なる初期値で異なる解に収束する

対処: 因子間相関が低い場合は等値制約や因子統合を検討、推定が境界に張り付く場合はベイズ事前で正則化、small $N$ なら IV-SEM (`MIIVsem`) や ベイズ推定を選択します。
:::

#### 発展: ESEM と bifactor モデル

CFA/SEM をさらに発展させた手法として、以下の2つが近年注目されている。

- **ESEM（Exploratory Structural Equation Modeling）**: CFA では各項目を1つの因子にのみ負荷させるが、ESEM ではEFA のように**交差負荷（cross-loading）を自由に推定**しつつ、SEM の枠組みで構造パスの検証も行える[@marsh2014]。CFA で適合度が不良だが理論的に妥当な因子構造がある場合に有効であり、心理尺度研究で活用が広がっている。lavaan では `efa()` ブロックを用いた探索的 SEM として実装可能であり、Mplus でも標準的にサポートされている。
- **Bifactor モデル**: 全項目に負荷する**一般因子**（general factor）と、項目のサブセットにのみ負荷する**グループ因子**（group factors）を同時に推定するモデルです[@reise2012]。例えば L2 動機づけ尺度では「全般的な動機づけ」という一般因子と「Ideal L2 Self」「Ought-to L2 Self」等のグループ因子を分離できます。McDonald の $\omega_h$（階層的オメガ）は bifactor モデルに基づく信頼性指標であり、一般因子が全分散のどの程度を説明するかを示します。

::: {.callout-warning}
**Bifactor モデルの識別性と "anomalous results" 問題**

@eid2017 は、標準的な対称 bifactor（symmetric bifactor）が以下の問題を抱えることを警告しています:

- **Anomalous loadings**: 一般因子に対するグループ因子の負荷符号が理論と逆転する、またはグループ因子の負荷が極端に小さくなり「general factor が全分散を吸収」する縮退解
- **Proportionality constraint** (Yung et al. 1999): 各グループ因子内で負荷比が制約され、データに対する fit が見せかけになりうる
- **General factor の解釈不能性**: 強制直交により一般因子が「すべて」を表す抽象指標化

@eid2017 が推奨する **Bifactor-(S-1) モデル**は、1 つのグループ因子（reference domain）の指標が一般因子のみに負荷するよう制約し、他のグループ因子は「reference からの逸脱」として解釈します。これにより一般因子が明確な実質的意味を持ちます。$\omega_h$ の解釈も bifactor-(S-1) では「reference domain に対する一般因子の信頼性」となり、対称版の「全構成概念に対する一般因子の信頼性」とは異なります。論文で bifactor を採用する際は、対称版か S-1 版かを明記し、anomalous loadings の有無を必ず報告してください。
:::

**Higher-order factor model との対比**: Bifactor とは別に、**高次因子モデル**（second-order CFA）は一次因子（Ideal L2/Ought-to L2/Learning Experience）の上に二次因子（L2MSS 全般）を置くネスト構造です。両者は数学的に同値ではなく、bifactor の方が柔軟です（高次因子は bifactor の制約版に対応; @yung1999）。AVE (Average Variance Extracted, @fornell1981) と HTMT (Heterotrait-Monotrait Ratio, @henseler2015) は弁別的妥当性の代表指標で、高次/bifactor 構造の比較で併用します。

::: {.callout-note}
**ESEM (Exploratory SEM) と BSEM (Bayesian SEM) — ICM-CFA 制約からの開放**

CFA は通常「各項目は 1 因子のみに負荷する」(Independent Cluster Model: ICM) を仮定しますが、現実の心理尺度では微小な交差負荷が普遍的に存在します。ICM 制約を緩めるための 2 つの現代的アプローチ:

- **ESEM** [@asparouhov2009; @marsh2014]: EFA の自由な交差負荷推定と SEM の構造パス検証を統合。`lavaan` では **`efa()` ブロック** で実装可能 (lavaan 0.6-12 以降):

  ```r
  esem_model <- '
    efa("block1")*Ideal + efa("block1")*OughtTo + efa("block1")*LearningExp =~
      M01 + M02 + M03 + M04 + M05 + M06 + M07 + M08 + M09 +
      M10 + M11 + M12 + M13 + M14 + M15 + M16 + M17 + M18
    Effort ~ Ideal + OughtTo + LearningExp
  '
  fit_esem <- lavaan::sem(esem_model, data = motivation_data, rotation = "geomin")
  ```

  L2MSS のように交差負荷が現実に存在する尺度では、ICM-CFA より良好な適合度が得られることが多いです。

- **BSEM** (Bayesian SEM with small-variance priors; Muthén & Asparouhov 2012): すべての交差負荷に **平均 0・極小分散の正規事前分布** $\mathcal{N}(0, 0.01)$ を置くことで、「ほぼゼロだが完全にはゼロではない」交差負荷を許容。`blavaan` でハイパーパラメータを指定して実装可能で、第 7 章のベイズ推定と接続します。
:::

::: {.callout-note}
**MIIVsem (Model-Implied Instrumental Variables SEM) — 局所的識別と誤指定診断**

通常の ML-SEM はモデル全体を同時推定するため、一部の誤指定が他のパラメータに伝播 (model-wide misfit propagation) します。@bollen1996 が提案した **MIIV-2SLS** は、各方程式を個別に **モデル含意操作変数** で推定する局所推定法で、以下の利点があります:

- **誤指定の局所化**: グローバル適合度が悪くても、正しく指定された方程式の推定値は影響を受けない
- **方程式単位の Sargan 検定**: どの方程式が誤指定されているかを統計的に特定可能
- **小標本での頑健性**: ML より少ない仮定で動作

R では `MIIVsem::miive()` で実装可能。SLA で「測定モデルの一部だけが誤指定されている」状況や、モデル比較で複数の代替仮説が競合する場合に有用です。
:::

#### 発展: CB-SEM と PLS-SEM — 2つの SEM アプローチ

本章でこれまで扱ってきた SEM は、含意共分散行列 $\boldsymbol{\Sigma}(\boldsymbol{\theta})$ とデータの共分散行列 $\mathbf{S}$ の差を最小化する**共分散ベース SEM（CB-SEM: Covariance-Based SEM）**である。lavaan や Mplus はこの CB-SEM を実装している。一方、**PLS-SEM（Partial Least Squares SEM）**は異なるアプローチで潜在変数モデルを推定する手法であり、近年 SLA 研究でも注目が高まっている。

**CB-SEM と PLS-SEM の比較**

| 観点 | CB-SEM（lavaan 等） | PLS-SEM（seminr, SmartPLS 等） |
|------|---------------------|-------------------------------|
| **推定原理** | 共分散行列の適合最大化 | 潜在変数得点の分散説明最大化 |
| **目的** | 理論モデルの確認・検定 | 予測・探索 |
| **分布の仮定** | 多変量正規性（MLR で緩和可能） | 分布自由 |
| **サンプルサイズ** | モデルの複雑さに応じた検出力と識別性が必要 | 小標本に比較的対応しやすいが、検出力分析が必要 |
| **適合度指標** | CFI, RMSEA, SRMR 等 | $R^2$, $Q^2$, SRMR |
| **潜在変数の扱い** | 不確定（因子得点は事後推定） | 確定的（加重合成得点として計算） |
| **測定モデル** | 反映指標（reflective）が前提 | 反映指標と形成指標（formative）の両方に対応 |
| **因果仮説の検定** | カイ二乗検定、CI、p値 | ブートストラップ CI |

**PLS-SEM が適している場面**:

- 予測重視・探索重視の研究で、理論検証よりも内生変数の説明力や予測力を重視する場合
- **形成指標（formative indicators）**や合成指標（composites）を扱いたい場合。例えば「SES（社会経済的地位）」は収入・学歴・職業が SES を**構成**するのであり、SES が各指標を**生成**するわけではありません。PLS-SEM はこのような composite / formative 指標を扱いやすい一方、CB-SEM でも識別制約や外部基準を適切に設定すれば形成測定を扱えます
- 予測精度の最大化が目的の場合（$R^2$ の最大化）
- 正規性の仮定が著しく侵害される場合

**CB-SEM を使うべき場面**（本章の主要内容）:

- 理論モデルの確認・検定（SLA 動機づけ研究の大半はこちら）
- 適合度指標による包括的なモデル評価が必要な場合
- 測定不変性（多群比較）の検証
- 間接効果の統計的検定

SLA 研究では @hair2019 が PLS-SEM の入門書として広く参照されています。R では `seminr` パッケージが PLS-SEM を実装しており、`SmartPLS`（GUI ベース）も利用者が多いです。ただし、**SLA の動機づけ研究や尺度開発研究の大半は反映指標モデルであり、理論検証が目的であるため、CB-SEM（lavaan）が第一選択肢となる**ことが多いです。PLS-SEM は、形成指標・合成指標を含むモデルや予測重視の探索的研究など、研究目的が CB-SEM と異なる場面で検討します。小標本であっても「PLS なら十分」とはせず、最小効果量、最大入次数、測定品質、検出力分析に基づいてサンプルサイズを設計します。

両アプローチの選択に迷った場合は、以下の判断基準が参考になる。

1. **指標が因子の原因か結果か？** → 原因（形成的）なら PLS-SEM が扱いやすいが、CB-SEM でも識別条件を満たせば扱えます。結果（反映的）なら CB-SEM が標準的です
2. **目的は予測か検定か？** → 予測なら PLS-SEM、検定なら CB-SEM
3. **サンプルサイズは十分か？** → 10-times rule だけに依存せず、検出力分析とモデル複雑性で判断します
4. **適合度検定が必要か？** → 必要なら CB-SEM

#### PLS-SEM の R 実装: seminr パッケージ

以下では、先ほどの CB-SEM（lavaan）と同じ動機づけデータを用いて PLS-SEM を実装し、両手法の結果を比較する。`seminr` パッケージは PLS-SEM の標準的な R 実装であり、測定モデルと構造モデルを直感的な構文で定義できる。

::: {.panel-tabset}

#### seminr の基本構文

`seminr` では、測定モデル（`constructs()`）と構造モデル（`relationships()`）を別々に定義し、`estimate_pls()` で推定する。

```{r}
#| eval: true
#| label: pls-sem-estimation

pls_mm <- constructs(
  reflective("IdealL2",   paste0("M", sprintf("%02d", 1:6))),
  reflective("OughtL2",   paste0("M", sprintf("%02d", 7:12))),
  reflective("LearnExp",  paste0("M", sprintf("%02d", 13:18))),
  reflective("Effort",    paste0("E", 1:3)),
  reflective("Profic",    paste0("P", 1:3))
)

pls_sm <- relationships(
  paths(from = c("IdealL2", "OughtL2", "LearnExp"), to = "Effort"),
  paths(from = c("Effort", "IdealL2"),              to = "Profic")
)

# --- Estimate PLS-SEM ---
fit_pls <- estimate_pls(
  data       = sem_data,
  measurement_model = pls_mm,
  structural_model  = pls_sm
)

# Bootstrap for inference (500 resamples)
boot_pls <- bootstrap_model(fit_pls, nboot = 500, seed = 2024)
```

`seminr` の構文は直感的で、`reflective()` で反映指標、`composite()` で形成指標（合成指標）を定義できる。`multi_items("M", 1:6)` は `M01`--`M06` に自動展開される。

#### 結果の比較: CB-SEM vs PLS-SEM

```{r}
#| eval: true
#| label: pls-vs-cb-comparison

boot_summary <- summary(boot_pls)
pls_paths_mat <- boot_summary$bootstrapped_paths
pls_paths <- as.data.frame(pls_paths_mat) |>
  rownames_to_column("Path") |>
  as_tibble()
pls_cols <- names(pls_paths)
pls_display <- pls_paths |>
  dplyr::select(Path,
                Estimate = all_of(pls_cols[2]),
                CI_lower = all_of(pls_cols[5]),
                CI_upper = all_of(pls_cols[6]))

cb_paths_raw <- parameterEstimates(fit_sem, standardized = TRUE) |>
  dplyr::filter(op == "~") |>
  mutate(Path = paste0(lhs, "  ->  ", rhs)) |>
  dplyr::select(Path, Estimate = std.all, CI_lower = ci.lower, CI_upper = ci.upper)

# --- Side-by-side comparison ---
comparison <- bind_rows(
  pls_display |> mutate(Method = "PLS-SEM"),
  cb_paths_raw |> mutate(Method = "CB-SEM (lavaan)")
)

comparison |>
  dplyr::select(Method, Path, Estimate, CI_lower, CI_upper) |>
  knitr::kable(
    caption = "Path Coefficient Comparison: CB-SEM vs. PLS-SEM",
    digits = 3,
    booktabs = TRUE
  )
```

#### PLS-SEM の診断指標

PLS-SEM では、CB-SEM の CFI/RMSEA に代わり、以下の指標でモデルを評価する。

```{r}
#| eval: true
#| label: pls-sem-diagnostics

pls_summary <- summary(fit_pls)

# Reliability and validity
rel_mat <- pls_summary$reliability
pls_rel_tbl <- as.data.frame(rel_mat) |>
  rownames_to_column("Construct") |>
  as_tibble()

pls_rel_tbl |>
  knitr::kable(
    caption = "PLS-SEM: Construct Reliability and Validity",
    digits = 3,
    booktabs = TRUE
  )
```

:::

#### PLS-SEM の主要な診断指標

| 指標 | 基準 | 意味 |
|------|------|------|
| **$\rho_C$（複合信頼性）** | > .70 | 構成概念の内的整合性（$\omega$ に類似） |
| **AVE（平均分散抽出量）** | > .50 | 因子が指標の分散の50%以上を説明 |
| **$R^2$** | .25 弱, .50 中, .75 強 | 構造モデルの説明力 |
| **$Q^2$（Stone-Geisser）** | > 0 | 予測的妥当性（blindfolding で算出） |
| **VIF（分散膨張係数）** | < 5（理想は < 3） | 構成概念間の多重共線性 |
| **SRMR** | < .08（補助診断） | PLS-SEM の近似的適合度。CB-SEM の全体適合度検定と同等には扱わない |

CB-SEM では全体的適合度（CFI, RMSEA）を重視するが、PLS-SEM では**各内生変数の $R^2$** と**測定品質（$\rho_C$, AVE）**を中心に評価する。

上記の比較から、反映指標のみのモデルでは CB-SEM と PLS-SEM のパス係数は概ね類似することがあります。ただし、PLS-SEM は合成得点ベースの推定であり、測定誤差やモデル適合の扱いが CB-SEM と異なります。**理論検証が目的の場合は CB-SEM を、予測や探索が目的の場合は PLS-SEM を選択する**という原則を保ち、両者を単なる代替推定法として扱わないことが重要です。

## 縦断・カテゴリ・距離データへの拡張

### 潜在成長曲線モデル（LGM）

::: {.callout-tip}
**1 分プレビュー (LGM)**

「300 人の学習者の語彙力を 4 学期にわたり追跡し、**集団の平均成長**と**個人差**を分離してモデル化したい」という時に使います。各個人の成長軌跡を「切片（初期水準）+ 傾き（成長率）」の 2 つの潜在因子で表現し、それぞれの平均と分散を推定します。共変量（動機づけなど）が成長率を予測するかも検証できます。第 6 章の混合効果モデル `lmer(y ~ time + (time|id))` と漸近同値で、SEM の枠組みで縦断分析を行う標準手法です。**3 時点以上が必要**（4 時点以上推奨、二次成長は 5 時点以上）。
:::

#### SEM × 縦断データ: 個人差の変化を捉える

ここまでの SEM は**横断データ**（1時点）を扱ってきました。しかし、英語教育研究や心理言語学研究では**縦断データ**（同一参加者を複数時点で測定）が不可欠です。たとえば「語彙力は学期を通じてどの程度伸びるか」「伸びの速さに個人差はあるか」「動機づけの高さが成長率を予測するか」といった問いは、横断 SEM では回答できません。

SEM の潜在変数の枠組みを時間軸に拡張したのが LGM です。CFA/SEM では潜在変数が「直接観測できない心理特性」を表したのに対し、LGM では潜在変数が「成長の初期値（切片）」と「変化率（傾き）」を表します。

潜在成長曲線モデル（Latent Growth Curve Model; LGM）は、SEM の枠組みで**個人の成長軌跡**をモデリングする手法です[@bollen2006; @duncan2006; @grimm2017; @meredith1990]。直感的には、各個人に線形回帰を当てはめ、その**切片（初期水準）と傾き（成長率）を潜在変数**として扱います。これにより、個人間のばらつき（分散）や予測変数との関係（回帰パス）を同時に推定できます。

#### 概要
1. LGM は「個人ごとの変化」を SEM で捉える手法です。
2. **切片因子**（intercept）= 初期水準の個人差、**傾き因子**（slope）= 成長率の個人差。
3. lavaan の `growth()` 関数で簡潔に推定できます。
4. 第13章（時系列分析 / ARIMA）は**集団レベルの時間パターン**を扱う。LGM は**個人レベルの変化**に焦点を当てる。

#### 時系列データの基礎概念

LGM に入る前に、縦断データの基本的な性質を確認しておく。本書第13章では ARIMA やベクトル自己回帰（VAR）といった**集団レベルの時系列分析**を扱うが、ここでは LGM に必要な最小限の基礎概念に絞る。

#### 横断データと縦断データの違い

```{r}
#| eval: true
#| label: lgm-cross-vs-long
#| fig-width: 9
#| fig-height: 4

# Demonstrate cross-sectional vs longitudinal data
set.seed(2024)
n_lgm <- 20  # Number of learners (for illustration)

lgm_demo <- tibble(
  id = rep(1:n_lgm, each = 4),
  time = rep(0:3, n_lgm),
  intercept = rep(rnorm(n_lgm, mean = 50, sd = 8), each = 4),
  slope = rep(rnorm(n_lgm, mean = 5, sd = 3), each = 4),
  vocab = intercept + slope * time + rnorm(n_lgm * 4, sd = 3)
) |>
  mutate(
    id = factor(id),
    semester = factor(time, labels = c("Sem 1", "Sem 2", "Sem 3", "Sem 4"))
  )

# Panel A: Cross-sectional snapshot (boxplot at each time)
p_cross <- lgm_demo |>
  ggplot(aes(x = semester, y = vocab)) +
  geom_boxplot(fill = "#dee2e6", outlier.alpha = 0.5) +
  labs(title = "A: Cross-sectional View",
       subtitle = "Group means hide individual trajectories",
       x = "Semester", y = "Vocabulary Score")

# Panel B: Longitudinal view (spaghetti plot)
p_long <- lgm_demo |>
  ggplot(aes(x = semester, y = vocab, group = id, color = id)) +
  geom_line(alpha = 0.5) +
  geom_point(size = 1.5, alpha = 0.6) +
  labs(title = "B: Longitudinal View",
       subtitle = "Individual growth trajectories vary",
       x = "Semester", y = "Vocabulary Score") +
  theme(legend.position = "none")

patchwork::wrap_plots(p_cross, p_long, ncol = 2)
```

横断データ（パネル A）ではグループ平均のみが見え、個人の成長軌跡の多様性は隠されています。縦断データ（パネル B）では「全員が同じ速度で成長しているわけではない」ことが可視化されます。教育学的には、この個人差こそが指導法の効果検証や早期介入の根拠となります。

#### 横断データの限界
パネル A のようにグループ平均だけを見ると「語彙力は学期ごとに上昇する」としか分かりません。パネル B のスパゲッティプロットでは、**成長の速い学習者と遅い学習者が混在**していることが明確になります。この「個人差のモデリング」が LGM の核心です。

#### 縦断データに必要な構造

LGM で扱う縦断データには以下の特徴がある[@singer2003; @mcarthur2006]。

| 特徴 | 内容 |
|---|---|
| ワイド形式 | 各時点が別の列（`Y1`, `Y2`, `Y3`, `Y4`）になります。lavaan はワイド形式を前提とします |
| 等間隔 | 時間コーディングは等間隔（0, 1, 2, 3）が基本。不等間隔も可能だがコーディングを調整する |
| 同一測定 | 各時点で同じ測定ツールを使用（測定不変性の前提） |
| サンプルサイズ | 目安: 100名以上、標準的な LGM では時点数は3以上（2時点では成長の個人差を安定して検証しにくい） |

LGM には実質的に 3 時点以上のデータが必要です。2 時点でも強い制約を置けば一部のモデルは推定できますが、標準的な自由残差の LGM では傾きの分散や成長の個人差を安定して検証しにくくなります。4 時点以上あれば二次（曲線的）成長モデルの検討も可能になります。

#### LGM と ARIMA の使い分け（第13章との接続）
| 観点 | LGM（本章） | ARIMA / VAR（第13章） |
|---|---|---|
| 分析単位 | 多人数の少数時点データで個人差をモデル化 | 単一系列または少数系列の多数時点データ |
| 時点数 | 3〜10 程度（少数時点） | 20 以上（多時点） |
| データ構造 | N人 × T時点 | 1系列 × T時点（長い） |
| 関心 | 成長の個人差と予測因子 | 時間的パターン・トレンド・季節性 |
| SLA の例 | 語彙力の学期別成長 | 発話速度の授業ごと変動 |
| R パッケージ | lavaan | forecast, tseries |

語彙テストを学期ごと（4時点）に多人数へ実施する場合は LGM が向いています。授業の音声録音から発話速度を毎回（50時点以上）抽出し、自己相関や季節性を扱う場合は ARIMA / VAR が向いています。

#### LGM の概念的枠組み

#### 線形成長モデルの数式

$T$ 時点で測定された観測変数 $Y_{it}$（個人 $i$、時点 $t$）に対して、線形 LGM は以下の2つのレベルで構成される。

**レベル1（個人内）**: 各個人の変化を記述する

$$Y_{it} = \eta_{0i} + \eta_{1i} \cdot \lambda_t + \varepsilon_{it}$$

ここで $\eta_{0i}$ は個人 $i$ の**切片**（初期水準）、$\eta_{1i}$ は個人 $i$ の**傾き**（成長率）、$\lambda_t$ は時間コーディング（例: 0, 1, 2, 3）、$\varepsilon_{it}$ は時点固有の残差です。

**レベル2（個人間）**: 切片と傾きの個人差を記述する

$$\eta_{0i} = \mu_0 + \zeta_{0i}, \qquad \eta_{1i} = \mu_1 + \zeta_{1i}$$

ここで $\mu_0, \mu_1$ はそれぞれ切片と傾きの**母平均**、$\zeta_{0i}, \zeta_{1i}$ は個人のずれ（偏差）です。潜在偏差は通常二変量正規 $(\zeta_{0i}, \zeta_{1i}) \sim \mathcal{N}(\mathbf{0}, \boldsymbol{\Psi})$ と仮定し、$\mathrm{Cov}(\boldsymbol{\zeta}_i, \boldsymbol{\varepsilon}_i)=\mathbf{0}$ とします。残差については、(a) 時点間独立かつ等分散 $\mathrm{Var}(\varepsilon_{it})=\sigma^2$（homoscedastic LGM、シンプル）、または (b) 時点ごとに自由分散 $\sigma^2_t$（heteroscedastic LGM、より柔軟）の 2 つの選択肢があります。**lavaan の `growth()` はデフォルトで時点ごとの自由分散 $\sigma^2_t$ を推定**します（CFA/SEM と同じ既定）。等分散制約を課したい場合は `vocab_T1 ~~ resvar*vocab_T1` 等のラベル指定で明示します。

$\boldsymbol{\Psi}$ を行列で表すと（線形成長の場合）:

$$\boldsymbol{\Psi} = \begin{pmatrix} \sigma^2_{\eta_0} & \sigma_{\eta_0,\eta_1} \\ \sigma_{\eta_0,\eta_1} & \sigma^2_{\eta_1} \end{pmatrix}, \quad \mathrm{Cov}(\mathbf{y}_i) = \boldsymbol{\Lambda}\boldsymbol{\Psi}\boldsymbol{\Lambda}^\top + \boldsymbol{\Theta}$$

ここで $\boldsymbol{\Lambda}$ は前述の time loading 行列、$\boldsymbol{\Theta}$ は時点別残差分散の対角行列です。これは CFA の含意共分散行列 $\boldsymbol{\Sigma}(\boldsymbol{\theta}) = \boldsymbol{\Lambda}\boldsymbol{\Phi}\boldsymbol{\Lambda}^\top + \boldsymbol{\Theta}$ と同形であり、LGM が CFA の特殊形（負荷を時間で固定）であることが行列レベルで明らかです。

::: {.callout-note}
**時間コーディング $\lambda_t$ と切片の解釈**

`slope =~ 0*T1 + 1*T2 + 2*T3 + 3*T4` のように $\lambda_t$ にゼロを置く時点が、切片 $\mu_0$ の解釈基準点になります。$\lambda_t = 0, 1, 2, 3$ なら $\mu_0$ は **T1（ベースライン）の平均水準**、$\lambda_t = -1.5, -0.5, 0.5, 1.5$ なら **観測期間の中点**、$\lambda_t = -3, -2, -1, 0$ なら **T4（最終時点）の平均水準** を表します。研究目的に合わせて中心化点を選択してください。共変量と切片・傾きの相関を解釈する際は特に注意が必要です（中心化が変わると共変量の効果係数も変わります）。
:::

::: {.callout-tip}
**LGM と混合効果モデル（lme4）の漸近同値性**

LGM `growth(...)` と `lme4::lmer(score ~ time + (time | id), data = long_data)` は、固定効果（$\hat{\mu}_0, \hat{\mu}_1$）・ランダム効果分散共分散行列（$\boldsymbol{\Psi}$）・残差分散（$\sigma^2$）について **ML 推定下で漸近同値**となる推定量を返します（@bauer2003 Theorem 1; @curran2003）。「漸近」と但し書きするのは、有限標本では (a) `lavaan` ML が分母 $N$、`lme4::lmer` の REML が分母 $N-p$ を使う、(b) 数値最適化アルゴリズムが異なる（lavaan: NLM/quasi-Newton、lme4: derivative-free PORT/BOBYQA）、という違いがあるためです。サンプルサイズが小〜中程度（$N < 100$）では推定値が数 % 異なることがあります。両者の主な違いは:

- **データ形式**: LGM は wide フォーマット（1行 = 1人）、lmer は long フォーマット（1行 = 1観測）
- **拡張性**: LGM は時間変動共変量の同時推定や測定不変性検証 (測定モデル付き LGM) が容易、lmer はネスト構造や交差ランダム効果が容易
- **欠測**: LGM は wide で MAR を仮定した FIML、lmer は long で同様に MAR

第 6 章の混合効果モデルは LGM と双対的な関係にあり、研究設計と分析慣行に応じて選びます。
:::

#### SEM の言葉で読む LGM
LGM は以下の測定モデルとして表現できます。

$$\begin{pmatrix} Y_1 \\ Y_2 \\ Y_3 \\ Y_4 \end{pmatrix} = \begin{pmatrix} 1 & 0 \\ 1 & 1 \\ 1 & 2 \\ 1 & 3 \end{pmatrix} \begin{pmatrix} \eta_0 \\ \eta_1 \end{pmatrix} + \begin{pmatrix} \varepsilon_1 \\ \varepsilon_2 \\ \varepsilon_3 \\ \varepsilon_4 \end{pmatrix}$$

切片因子の負荷量はすべて 1（全時点に等しく影響）、傾き因子の負荷量は時間コーディング（0, 1, 2, 3）に固定されます。これは CFA における**因子負荷の固定**と同じ考え方です。

#### パス図

このモデルでは、切片因子（\(\eta_0\)）が全時点 \(Y_1\)〜\(Y_4\) に等しく負荷し、負荷量はすべて 1 に固定されます。傾き因子（\(\eta_1\)）の負荷量は時点に応じて \(0, 1, 2, 3\) と固定され、時間の進行に伴う変化率を表現します。各観測値には対応する残差 \(\varepsilon_1\)〜\(\varepsilon_4\) が割り当てられ、さらに切片因子と傾き因子の共分散を推定することで「初期水準と成長率の関連」を評価します。

#### SLA 応用例: 語彙力の縦断的成長

#### データのシミュレーション

日本人 EFL 学習者 300 名を対象に、4学期にわたって語彙サイズテスト（同一テスト）を実施したという設定でデータを生成します。動機づけ（`motivation`）を時間不変の共変量として含め、成長率への影響を検証します。この種の縦断的語彙成長研究は、@zheng2022 が SSLA 誌で L2 研究における LGM の方法論的チュートリアルとして体系化しており、@hiver2020 は複雑系理論の観点から縦断データの分析法を論じています。

語彙力以外にも、読解力（例: @zheng2022 が報告した長期的な読解ストラテジーの発達）、発音精度、作文の複雑さ（syntactic complexity）など、時間経過に伴う変化を示す SLA の現象はすべて LGM の分析対象となりうる。

切片の平均 $55$ は標準的な語彙サイズテスト（例: Vocabulary Levels Test）のスコア範囲を模擬し、傾き平均 $4.5$（各学期4.5点の伸び）は1学期あたりの典型的な語彙成長量に対応します。切片-傾き相関を $-.30$ としたのは、初期値が低い学習者ほど成長率が大きいという「追いつき効果」を反映するためです。

```{r}
#| eval: true
#| label: lgm-data-simulation

set.seed(2024)
n_lgm_full <- 300

# Time-invariant covariate: motivation (standardized)
motiv <- rnorm(n_lgm_full)

# Individual intercepts and slopes
mu_intercept <- 55    # Mean initial vocabulary score
mu_slope     <- 4.5   # Mean growth per semester
sigma_int    <- 8     # SD of intercepts
sigma_slp    <- 3     # SD of slopes
rho_is       <- -0.3  # Intercept-slope correlation (catch-up effect)

Sigma_lgm <- matrix(c(sigma_int^2, rho_is * sigma_int * sigma_slp,
                       rho_is * sigma_int * sigma_slp, sigma_slp^2), 2, 2)
eta <- MASS::mvrnorm(n_lgm_full, mu = c(0, 0), Sigma = Sigma_lgm)

# Add covariate effects
intercept_i <- mu_intercept + eta[, 1] + 2.0 * motiv   # Motivation → initial level
slope_i     <- mu_slope     + eta[, 2] + 1.5 * motiv   # Motivation → growth rate

lgm_data <- tibble(
  id = 1:n_lgm_full,
  motivation = motiv,
  vocab_T1 = intercept_i + slope_i * 0 + rnorm(n_lgm_full, sd = 4),
  vocab_T2 = intercept_i + slope_i * 1 + rnorm(n_lgm_full, sd = 4),
  vocab_T3 = intercept_i + slope_i * 2 + rnorm(n_lgm_full, sd = 4),
  vocab_T4 = intercept_i + slope_i * 3 + rnorm(n_lgm_full, sd = 4)
)

# Quick descriptives
lgm_data |>
  dplyr::select(starts_with("vocab")) |>
  pivot_longer(everything(), names_to = "Time", values_to = "Score") |>
  group_by(Time) |>
  summarise(
    M  = round(mean(Score), 1),
    SD = round(sd(Score), 1),
    .groups = "drop"
  ) |>
  knitr::kable(
    caption = "Vocabulary Scores across 4 Semesters",
    digits = 1,
    booktabs = TRUE
  )
```

#### スパゲッティプロット: 個人の成長軌跡

```{r}
#| eval: true
#| label: lgm-spaghetti-plot
#| fig-width: 8
#| fig-height: 5

# Spaghetti plot with group mean overlay
lgm_long <- lgm_data |>
  dplyr::select(id, motivation, starts_with("vocab")) |>
  pivot_longer(cols = starts_with("vocab"),
               names_to = "time_label",
               values_to = "score") |>
  mutate(time = as.numeric(gsub("vocab_T", "", time_label)) - 1)

# Randomly sample 50 individuals for visibility
set.seed(2024)
sample_ids <- sample(unique(lgm_long$id), 50)

lgm_long |>
  dplyr::filter(id %in% sample_ids) |>
  ggplot(aes(x = time, y = score, group = id)) +
  geom_line(alpha = 0.25, color = "grey50") +
  geom_point(alpha = 0.3, size = 1, color = "grey50") +
  # Group mean trajectory
  stat_summary(data = lgm_long, aes(group = 1),
               fun = mean, geom = "line",
               color = "#c0392b", linewidth = 1.5) +
  stat_summary(data = lgm_long, aes(group = 1),
               fun = mean, geom = "point",
               color = "#c0392b", size = 3) +
  scale_x_continuous(breaks = 0:3, labels = paste0("T", 1:4)) +
  labs(title = "Individual Vocabulary Growth Trajectories",
       subtitle = "Grey = 50 individual trajectories; Red = group mean",
       x = "Semester", y = "Vocabulary Score")
```

赤い太線は全体の平均軌跡を、灰色の細線は各個人の成長軌跡を表します。個人間の傾きのばらつき（灰色線の扇形の広がり）が大きいほど、傾きの分散が統計的に有意になりやすくなります。この図だけで切片-傾き相関を厳密に判断するのではなく、後続の LGM 推定値で「初期語彙力が低い学習者ほど成長率が大きい」という追いつき効果があるかを確認します。

#### 無条件成長モデル（Unconditional Growth Model）

まず共変量なしの基本モデル（無条件成長モデル）を推定する。このモデルでは、全参加者に共通の「平均的な成長」（固定効果）と、そこからの個人ごとのずれ（ランダム効果）を分離する。

`lavaan::growth()` は LGM 専用のラッパーで、`sem()` と同じ推定エンジンを内部で使用するが、以下の点が自動設定される:

- 潜在変数の平均（`intercept ~ 1`, `slope ~ 1`）を自動的に推定する
- 観測変数の切片をゼロに固定する（成長因子が切片を吸収するため）
- `sem()` でも同じモデルを書けるが、`growth()` を使うとモデル式が簡潔になる

**LGM のモデル式の読み方**: LGM では CFA/SEM と同じ lavaan 構文を使うが、因子負荷を数値で**固定**する点が異なる。

| モデル式 | 意味 |
|----------|------|
| `intercept =~ 1*T1 + 1*T2 + 1*T3 + 1*T4` | 全時点の負荷を `1` に固定 → 切片因子は「ベースラインの水準」を表す |
| `slope =~ 0*T1 + 1*T2 + 2*T3 + 3*T4` | 負荷を `0, 1, 2, 3` に固定 → 傾き因子は「1時点あたりの変化量」を表す |
| `intercept ~ 1` / `slope ~ 1` | 潜在平均の推定を指定 |
| `intercept ~~ slope` | 切片と傾きの共分散（初期値が高い人ほど成長が速い/遅いか？） |

出力テーブルの読み方:

| 行の種類 | op 列 | 確認すべき値 |
|---|---|---|
| 因子負荷（固定） | `=~` | 値が 1, 0, 1, 2, 3 に固定されているか |
| 潜在平均 | `~1` | 切片の平均 $\hat{\mu}_0$、傾きの平均 $\hat{\mu}_1$ |
| 潜在分散・共分散 | `~~` | 切片・傾きの分散が有意か（個人差の存在） |

```{r}
#| eval: true
#| label: lgm-unconditional

# --- Unconditional Linear Growth Model ---
lgm_uncond <- '
  # Intercept and slope latent factors
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ 0*vocab_T1 + 1*vocab_T2 + 2*vocab_T3 + 3*vocab_T4

  # Estimate latent means
  intercept ~ 1
  slope     ~ 1

  # Estimate latent variances and covariance
  intercept ~~ intercept
  slope     ~~ slope
  intercept ~~ slope
'

fit_lgm_uncond <- growth(lgm_uncond, data = lgm_data)

parameterEstimates(fit_lgm_uncond, standardized = TRUE) |>
  dplyr::filter(op %in% c("=~", "~", "~~")) |>
  dplyr::select(lhs, op, rhs, est, se, z, pvalue, std.all) |>
  knitr::kable(
    caption = "Unconditional LGM: Parameter Estimates",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lgm-unconditional-fit

fitMeasures(fit_lgm_uncond, c("chisq", "df", "pvalue",
                               "cfi", "tli", "rmsea",
                               "rmsea.ci.lower", "rmsea.ci.upper",
                               "srmr")) |>
  enframe(name = "Index", value = "Value") |>
  mutate(Value = round(as.numeric(Value), 3)) |>
  knitr::kable(
    caption = "Unconditional LGM: Fit Indices",
    digits = 3,
    booktabs = TRUE
  )
```

#### 結果の読み方（無条件モデル）
- **切片の平均** ($\hat{\mu}_0$): 全参加者の**初期語彙力の平均**。
- **傾きの平均** ($\hat{\mu}_1$): **1学期あたりの平均成長量**。正の値ならば学期を追うごとに語彙力が向上。
- **切片の分散** ($\hat{\sigma}^2_0$): 初期水準の**個人差の大きさ**。有意ならば学習者間で出発点が異なる。
- **傾きの分散** ($\hat{\sigma}^2_1$): 成長率の**個人差の大きさ**。有意ならば「伸びが速い人」と「遅い人」がいる。
- **切片-傾きの共分散**: 正ならば「初期水準が高い人ほど成長も速い」、負ならば「追いつき効果」。

切片の平均（55.08 付近の値）は Time 1 における語彙サイズの推定平均を、傾きの平均（4.52 付近の値）は各時点間の平均的な語彙成長を表します。傾きの分散が有意であるということは、学習者によって成長速度が異なることを意味します。切片-傾き間の共分散が負の場合は「初期値が低い学習者ほど速く成長する」キャッチアップ効果を示します。

無条件 LGM の構造をパス図で可視化すると、切片因子（intercept）と傾き因子（slope）が4時点の観測変数にどのように接続しているかが一目で理解できます。

```{r}
#| eval: true
#| label: lgm-path-diagram
#| fig-width: 10
#| fig-height: 6

semPlot::semPaths(fit_lgm_uncond, whatLabels = "est", layout = "tree2",
                  rotation = 2, edge.label.cex = 0.8,
                  sizeMan = 7, sizeLat = 10, nCharNodes = 0,
                  mar = c(3, 3, 3, 3))
```

パス図では、Intercept から各時点への固定負荷（全て1）と、Slope からの固定負荷（0, 1, 2, 3）が線形成長を表現しています。Intercept と Slope の間の双方向矢印が切片-傾きの共分散を示します。

このパス図は CFA のパス図と構造が似ているが、決定的な違いは因子負荷量が**データから推定される自由パラメータではなく、時間コーディングによる固定値**（0, 1, 2, 3）である点にある。これにより、潜在変数は「成長の初期値」と「成長率」という時間的な意味を持つ。

#### 条件付き成長モデル（Conditional Growth Model）

成長率の個人差が確認されたら、その個人差を予測する共変量を追加する。ここでは**動機づけ**が初期水準と成長率に影響するかを検証する。モデル式は無条件モデルに `intercept ~ motivation` と `slope ~ motivation` を追加すればよい。これにより、動機づけが「初期語彙力」と「成長率」の両方を予測するかを同時に検証できる。

```{r}
#| eval: true
#| label: lgm-conditional

# --- Conditional Growth Model: Motivation as predictor ---
lgm_cond <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ 0*vocab_T1 + 1*vocab_T2 + 2*vocab_T3 + 3*vocab_T4

  # Structural model: motivation predicts intercept and slope
  intercept ~ motivation
  slope     ~ motivation
'

fit_lgm_cond <- growth(lgm_cond, data = lgm_data)

parameterEstimates(fit_lgm_cond, standardized = TRUE) |>
  dplyr::filter(op %in% c("=~", "~", "~~")) |>
  dplyr::select(lhs, op, rhs, est, se, z, pvalue, std.all) |>
  knitr::kable(
    caption = "Conditional LGM: Parameter Estimates",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lgm-conditional-results

parameterEstimates(fit_lgm_cond) |>
  dplyr::filter(op == "~") |>
  dplyr::select(lhs, rhs, est, se, z, pvalue, ci.lower, ci.upper) |>
  mutate(across(c(est, se, z, ci.lower, ci.upper), ~round(.x, 3)),
         pvalue = ifelse(pvalue < .001, "< .001", round(pvalue, 3))) |>
  knitr::kable(
    caption = "Conditional LGM: Structural Paths",
    digits = 3,
    booktabs = TRUE
  )
```

動機づけ → 切片の正の係数は、動機づけが高い学習者ほど初期語彙力も高い傾向があることを意味します。動機づけ → 傾きの正の係数は、動機づけが高い学習者ほど成長が速い傾向があることを示します。観察データでは、これらの結果は動機づけと語彙習得の出発点・成長率との関連を示すものであり、介入効果そのものを証明するには実験・縦断設計や交絡統制が必要です。

#### 条件付きモデルの解釈
- **Motivation → Intercept**: 動機づけが高い学習者は、初期の語彙力も高い傾向がある（正の係数の場合）。
- **Motivation → Slope**: 動機づけが高い学習者は、学期ごとの語彙力の伸びも大きい傾向があります。介入研究や縦断的な追跡デザインと組み合わせることで、教育的介入を検討する根拠になりえます。
- $R^2$ の変化量を確認することで、動機づけが成長率の個人差をどの程度説明するかを定量化できる。

#### モデル比較: 無成長 vs 線形成長 vs 二次成長

モデルの妥当性を検証するため、3つの入れ子モデルを比較する。各タブでモデル定義と比較結果を確認する。

::: {.panel-tabset}

##### 無成長モデル

切片因子のみで傾きを含まないモデルです。「語彙力は変化しない」という帰無仮説に対応します。

```r
lgm_no_growth <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  intercept ~ 1
'
```

##### 線形成長モデル

切片と傾きの2因子モデルです。「語彙力は学期ごとに一定量ずつ変化する」と仮定します。前節で推定済みの無条件モデルがこれに対応します。

```r
lgm_uncond <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ 0*vocab_T1 + 1*vocab_T2 + 2*vocab_T3 + 3*vocab_T4
'
```

##### 二次成長モデル

切片・傾き・二次項の3因子モデルです。「成長速度が時間とともに加速・減速する」パターンを許容します。4時点では自由度が非常に少なくなる点に注意します。

```r
lgm_quad <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ 0*vocab_T1 + 1*vocab_T2 + 2*vocab_T3 + 3*vocab_T4
  quad      =~ 0*vocab_T1 + 1*vocab_T2 + 4*vocab_T3 + 9*vocab_T4
'
```

::: {.callout-warning}
**二次成長モデルの多重共線性問題と時間中心化**

上記の `slope = (0, 1, 2, 3)` と `quad = (0, 1, 4, 9)` は **線形項と二次項の相関が極めて高く** ($r \approx .96$)、推定が不安定になりやすい設定です。実務では時間を**中心化**することで多重共線性を緩和できます:

```r
# 時間中心化版 (T1=-1.5, T2=-0.5, T3=0.5, T4=1.5)
lgm_quad_centered <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ -1.5*vocab_T1 + -0.5*vocab_T2 + 0.5*vocab_T3 + 1.5*vocab_T4
  quad      =~  2.25*vocab_T1 + 0.25*vocab_T2 + 0.25*vocab_T3 + 2.25*vocab_T4
'
```

中心化により slope-quad 相関が大きく低下し、傾きの平均は「観測期間中点での瞬間変化率」、切片は「中点の水準」と解釈し直されます。論文では中心化を採用するのが現代的な慣行です（Biesanz et al. 2004; Mehta & West 2000）。なお 4 時点では二次成長モデルの自由度が極端に少なく ($df = 1$)、適合度の信頼性が低下するため 5 時点以上が望ましいです。
:::

::: {.callout-tip}
**Latent basis (Meredith-Tisak 1990) と piecewise LGM**

二次成長より節約的に非線形成長を表現する 2 つの代替モデルがあります:

```r
# (A) Latent basis model (Meredith & Tisak 1990; @meredith1990)
# 第一・最終時点の負荷を 0/1 に固定、中間負荷を自由推定
# → 「真の成長軌跡の形状」をデータから学習する非線形成長
lgm_latent_basis <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ 0*vocab_T1 + NA*vocab_T2 + NA*vocab_T3 + 1*vocab_T4
  intercept ~ 1
  slope     ~ 1
'

# (B) Piecewise LGM (介入研究の pre/intervention/post に対応)
# 例: T1→T2 が pre 期、T2→T3→T4 が post 期
# slope1 = 介入前の傾き、slope2 = 介入後の傾き
lgm_piecewise <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope1    =~ 0*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope2    =~ 0*vocab_T1 + 0*vocab_T2 + 1*vocab_T3 + 2*vocab_T4
  intercept ~ 1
  slope1    ~ 1
  slope2    ~ 1
'
```

**Latent basis** は SLA の U-shape 発達 (Kellerman 1985) や臨界期効果のように「成長の形状を事前に知らない」場合に有用で、二次モデルより 1 自由度節約できます。**Piecewise** は介入研究 (pre/intervention/post) や留学経験前後の変化など、構造的な変化点が事前にわかっている場合に最適です。knot 位置の選択は Flora (2008) を参照してください。
:::

##### モデル比較結果

尤度比検定（$\chi^2$ 差検定）で、モデルの追加パラメータが有意な改善をもたらすかを判定します。$p < .05$ ならば制約の少ないモデルが適合を改善したと判断できます。ただし、収束、負の分散、標準誤差、自由度を確認したうえで比較します。

```{r}
#| eval: true
#| label: lgm-model-comparison

lgm_no_growth <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  intercept ~ 1
'
fit_no_growth <- growth(lgm_no_growth, data = lgm_data)

lgm_quad <- '
  intercept =~ 1*vocab_T1 + 1*vocab_T2 + 1*vocab_T3 + 1*vocab_T4
  slope     =~ 0*vocab_T1 + 1*vocab_T2 + 2*vocab_T3 + 3*vocab_T4
  quad      =~ 0*vocab_T1 + 1*vocab_T2 + 4*vocab_T3 + 9*vocab_T4
  intercept ~ 1
  slope     ~ 1
  quad      ~ 1
'
fit_quad <- growth(lgm_quad, data = lgm_data)

lgm_quad_check <- tibble(
  Check = c("Converged", "Post-estimation check"),
  Value = c(
    as.character(lavaan::lavInspect(fit_quad, "converged")),
    as.character(lavaan::lavInspect(fit_quad, "post.check"))
  )
)

lgm_quad_check |>
  knitr::kable(
    caption = "Quadratic LGM: Convergence and Post-estimation Checks",
    booktabs = TRUE
  )

anova(fit_no_growth, fit_lgm_uncond, fit_quad) |>
  as_tibble() |>
  knitr::kable(
    caption = "LGM Model Comparison",
    digits = 3,
    booktabs = TRUE
  )
```

モデル比較結果の読み方: `anova()` の出力では、ネストされたモデル間のカイ二乗差検定が行われます。**カイ二乗差が有意（p < .05）ならば、制約の少ないモデルが有意に適合を改善した**と判断します。ただし、二次成長モデルでは負の分散推定や収束警告が出ることがあるため、上の健全性チェックを確認してから採用可否を判断します。一般的な解釈の流れ:

1. 無成長 vs 線形: 有意 → 時間経過による変化が存在する
2. 線形 vs 二次: 有意 → 成長パターンが直線ではなく曲線（加速・減速）を含む
3. 有意でない場合 → より単純なモデル（節約の原則）を採用する

AIC / BIC が表示される場合は、値が**小さいほど良いモデル**である。

:::

#### 二次成長モデルの注意点
- 4時点のデータで二次（曲線）モデルを推定すると、自由度が非常に少なくなります。**5時点以上**が望ましいです。
- 二次成長の係数が有意でない、または負の分散・収束警告・不安定な標準誤差が出る場合は、線形モデルを採用します（節約の原則）。
- 二次項を追加する理論的根拠があるか（例: 初期に急成長し後半でプラトー）を事前に検討する。

#### 心理言語学応用: 視線計測データの成長モデル

LGM は SLA だけでなく、心理言語学の実験研究にも広く応用される。ここでは**視線計測（eye-tracking）データ**を用いた例を示す[@conklin2018; @godfroid2020; @jegerski2014]。

#### 視線計測の主要指標

視線計測研究では、読解中の認知処理を反映する多様な指標が用いられる。LGM で縦断的な変化を追跡する場合、以下の指標が候補となる。

| 指標 | 英語 | 測定内容 | LGM での用途例 |
|---|---|---|---|
| 初回注視時間 | First fixation duration | 最初の注視の長さ | 語彙アクセスの自動化 |
| 注視時間 | Gaze duration | 関心領域の初回通過時の総注視時間 | 初期処理の効率変化 |
| 総注視時間 | Total fixation duration | 全注視の合計時間 | 全体的な処理負荷の変化 |
| 回帰パス時間 | Regression path duration | 回帰を含む初回通過時間 | 統合処理・再分析の変化 |
| 瞳孔径 | Pupil dilation | 瞳孔サイズの変化 | 認知負荷の変化 |

#### データのシミュレーション

L2 学習者 200 名が 4 回のセッション（4週間間隔）で等価な読解実験に参加し、ターゲット領域（関係代名詞構文）の**注視時間**（gaze duration, ms）を測定したという設定で考えます。同じ刺激を繰り返すと記憶や慣れの効果が混入するため、実研究では同程度の難易度を持つ等価刺激を用い、刺激リストをカウンターバランスし、項目レベルのばらつきも考慮します。セッションを重ねるにつれ、処理が自動化し注視時間が短縮されるかを検証します。

```{r}
#| eval: true
#| label: lgm-eyetracking-simulation

set.seed(2024)
n_et <- 200

# Proficiency as time-invariant covariate (standardized)
proficiency <- rnorm(n_et)

# Individual parameters
# Note: slopes are negative (gaze duration decreases with practice)
mu_int_et  <- 350   # Mean initial gaze duration (ms)
mu_slp_et  <- -25   # Mean decrease per session
sigma_int_et <- 50
sigma_slp_et <- 15
rho_et <- -0.2      # Higher initial GD → faster decrease (catch-up)

Sigma_et <- matrix(c(sigma_int_et^2, rho_et * sigma_int_et * sigma_slp_et,
                      rho_et * sigma_int_et * sigma_slp_et, sigma_slp_et^2), 2, 2)
eta_et <- MASS::mvrnorm(n_et, mu = c(0, 0), Sigma = Sigma_et)

intercept_et <- mu_int_et + eta_et[, 1] + (-20) * proficiency  # Higher proficiency → shorter initial GD
slope_et     <- mu_slp_et + eta_et[, 2] + (-8) * proficiency   # Higher proficiency → faster decrease

et_data <- tibble(
  id = 1:n_et,
  proficiency = proficiency,
  GD_S1 = intercept_et + slope_et * 0 + rnorm(n_et, sd = 30),
  GD_S2 = intercept_et + slope_et * 1 + rnorm(n_et, sd = 30),
  GD_S3 = intercept_et + slope_et * 2 + rnorm(n_et, sd = 30),
  GD_S4 = intercept_et + slope_et * 3 + rnorm(n_et, sd = 30)
)

# Descriptives
et_data |>
  dplyr::select(starts_with("GD")) |>
  pivot_longer(everything(), names_to = "Session", values_to = "GD_ms") |>
  group_by(Session) |>
  summarise(M = round(mean(GD_ms), 1),
            SD = round(sd(GD_ms), 1),
            .groups = "drop") |>
  knitr::kable(
    caption = "Gaze Duration across 4 Sessions",
    digits = 1,
    booktabs = TRUE
  )
```

#### 条件付き LGM: 習熟度が処理の自動化を予測するか

```{r}
#| eval: true
#| label: lgm-eyetracking-model

lgm_et <- '
  # Latent growth factors
  intercept =~ 1*GD_S1 + 1*GD_S2 + 1*GD_S3 + 1*GD_S4
  slope     =~ 0*GD_S1 + 1*GD_S2 + 2*GD_S3 + 3*GD_S4

  # Proficiency predicts intercept and slope
  intercept ~ proficiency
  slope     ~ proficiency
'

fit_et <- growth(lgm_et, data = et_data)

parameterEstimates(fit_et, standardized = TRUE) |>
  dplyr::filter(op %in% c("=~", "~", "~~")) |>
  dplyr::select(lhs, op, rhs, est, se, z, pvalue, std.all) |>
  knitr::kable(
    caption = "Eye-tracking LGM: Parameter Estimates",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lgm-eyetracking-results

# Structural paths
parameterEstimates(fit_et) |>
  dplyr::filter(op == "~") |>
  dplyr::select(lhs, rhs, est, se, z, pvalue) |>
  mutate(across(c(est, se, z), ~round(.x, 3)),
         pvalue = ifelse(pvalue < .001, "< .001", round(pvalue, 3))) |>
  knitr::kable(
    caption = "Eye-tracking LGM: Structural Paths",
    digits = 3,
    booktabs = TRUE
  )
```

#### 視線計測 LGM の解釈
- **傾きの平均が負**: セッションを重ねるごとに注視時間が短縮 → 処理の自動化。
- **Proficiency → Intercept（負）**: 習熟度が高い学習者は、初回から注視時間が短い。
- **Proficiency → Slope（負）**: 習熟度が高い学習者は、自動化のペースも速い。
- 教育的示唆: 多読プログラムなどの介入前後で視線計測データを収集し、LGM で処理効率の変化を追跡できる[@godfroid2020]。

#### 可視化: 習熟度別の成長軌跡

```{r}
#| eval: true
#| label: lgm-eyetracking-trajectories
#| fig-width: 8
#| fig-height: 5

# Visualize trajectories by proficiency group
et_long <- et_data |>
  mutate(prof_group = case_when(
    proficiency > 0.5  ~ "High",
    proficiency < -0.5 ~ "Low",
    TRUE ~ "Mid"
  )) |>
  pivot_longer(cols = starts_with("GD"),
               names_to = "session_label",
               values_to = "gaze_duration") |>
  mutate(session = as.numeric(gsub("GD_S", "", session_label)) - 1,
         prof_group = factor(prof_group, levels = c("Low", "Mid", "High")))

et_long |>
  group_by(prof_group, session) |>
  summarise(M = mean(gaze_duration),
            SE = sd(gaze_duration) / sqrt(n()),
            .groups = "drop") |>
  ggplot(aes(x = session, y = M, color = prof_group, fill = prof_group)) +
  geom_ribbon(aes(ymin = M - SE, ymax = M + SE), alpha = 0.15, color = NA) +
  geom_line(linewidth = 1.2) +
  geom_point(size = 3) +
  scale_x_continuous(breaks = 0:3, labels = paste0("S", 1:4)) +
  scale_color_manual(values = c("Low" = "#e74c3c", "Mid" = "#f39c12", "High" = "#27ae60")) +
  scale_fill_manual(values = c("Low" = "#e74c3c", "Mid" = "#f39c12", "High" = "#27ae60")) +
  labs(title = "Gaze Duration Trajectories by Proficiency Group",
       subtitle = "Ribbon = ± 1 SE; L2 readers processing relative clauses",
       x = "Session", y = "Gaze Duration (ms)",
       color = "Proficiency", fill = "Proficiency")
```

この図は、習熟度グループ（高群・低群）ごとの注視時間の縦断的変化を示します。リボン（帯）は標準誤差の範囲を表しますが、帯の重なりだけで有意差を判断するのは粗い近似です。群差や群 × 時間の違いは、モデルベースの予測値と信頼区間、または群を含めた LGM で確認します。高習熟度群は早い段階で注視時間が短くなる傾向、低習熟度群は緩やかに減少する傾向が読み取れます。セッションが進むにつれて両群の軌跡が収束するか乖離するかは、指導効果の持続性を評価する重要な指標となります。

#### LGM の発展と注意点

#### LGM を使う際のチェックリスト
1. **時点数**: 最低3時点（線形）。曲線成長には4時点以上。
2. **測定不変性**: 各時点で同じ測定ツールを使い、前節の測定不変性検証を行うことが理想。
3. **欠損データ**: FIML（完全情報最尤法）を使う場合は、`growth(..., missing = "fiml")` のように `missing = "fiml"` を明示します。外生共変量に欠測がある場合は、`fixed.x = FALSE` の扱いも確認します。
4. **非線形成長**: 二次項の追加、または自由推定（freed loading）で対応可能。
5. **時変共変量**: 各時点で異なる共変量（例: その学期の学習時間）を追加できる。
6. **多変量 LGM**: 複数のアウトカム（例: 語彙力と読解力）の成長を同時にモデリングし、成長率の相関を推定する。

#### 発展モデルへのポインタ
| モデル | 概要 | 参照 |
|---|---|---|
| 区分的 LGM | 成長パターンが途中で変化（例: 介入前後で傾きが異なる） | @bollen2006 |
| 多変量 LGM | 複数アウトカムの並行成長をモデリング | @duncan2006 |
| 二次 LGM | 曲線的成長パターンのモデリング | @curran2010 |
| 潜在クラス成長分析（LCGA） | 異質な成長パターンのサブグループを発見 | @vanlissa2023 |
| ALT モデル（Autoregressive LGM） | 自己回帰効果と成長曲線を統合 | @bollen2006 |
| LGM + 媒介 | 成長率が別のアウトカムを媒介 | @preacher2008lgm |

これらの発展モデルは、基本的な線形 LGM を理解した上で段階的に学ぶとよいです。SLA 応用例は @zheng2022 を参照してください。

#### LGM の報告テンプレート（APA 形式）

```{r}
#| eval: true
#| label: report-lgm
#| results: asis

# LGM の報告テンプレート（無条件モデル + 条件付きモデル）
lgm_fit_uncond <- fitMeasures(fit_lgm_uncond, c("chisq", "df", "pvalue",
                                                  "cfi", "tli", "rmsea", "srmr"))
lgm_fit_cond <- fitMeasures(fit_lgm_cond, c("chisq", "df", "pvalue",
                                              "cfi", "tli", "rmsea", "srmr"))

lgm_paths <- parameterEstimates(fit_lgm_cond) |>
  dplyr::filter(op == "~", rhs == "motivation")

lgm_report <- glue::glue(
  "A latent growth curve model was estimated using lavaan [@rosseel2012] ",
  "to examine vocabulary growth across four semesters (N = {n_lgm_full}; missing data would be handled with FIML if present). ",
  "The unconditional linear growth model showed {ifelse(as.numeric(lgm_fit_uncond['cfi']) > 0.95, 'good', 'adequate')} fit: ",
  "χ²({lgm_fit_uncond['df']}, N = {n_lgm_full}) = {round(as.numeric(lgm_fit_uncond['chisq']), 1)}, ",
  "p {ifelse(as.numeric(lgm_fit_uncond['pvalue']) < .001, '< .001', paste0('= ', round(as.numeric(lgm_fit_uncond['pvalue']), 3)))}, ",
  "CFI = {round(as.numeric(lgm_fit_uncond['cfi']), 3)}, ",
  "TLI = {round(as.numeric(lgm_fit_uncond['tli']), 3)}, ",
  "RMSEA = {round(as.numeric(lgm_fit_uncond['rmsea']), 3)}, ",
  "SRMR = {round(as.numeric(lgm_fit_uncond['srmr']), 3)}. ",
  "Next, a conditional model was estimated by adding motivation as a predictor ",
  "(CFI = {round(as.numeric(lgm_fit_cond['cfi']), 3)}, ",
  "RMSEA = {round(as.numeric(lgm_fit_cond['rmsea']), 3)}). ",
  "Motivation significantly predicted the growth slope ",
  "(B = {round(lgm_paths$est[lgm_paths$lhs == 'slope'], 2)}, ",
  "SE = {round(lgm_paths$se[lgm_paths$lhs == 'slope'], 2)}, ",
  "p {ifelse(lgm_paths$pvalue[lgm_paths$lhs == 'slope'] < .001, '< .001', paste0('= ', round(lgm_paths$pvalue[lgm_paths$lhs == 'slope'], 3)))}), ",
  "indicating that higher motivation was associated with faster vocabulary growth."
)

knitr::asis_output(lgm_report)
```

### 発展: 潜在クラス分析とその他の潜在変数モデル

::: {.callout-tip}
**1 分プレビュー (LCA)**

「学習方略の使用パターンから、**観測されない学習者タイプ**（自律型・文法練習型・全体的高使用型など）を統計的に抽出したい」という時に使います。EFA/CFA が**連続的な潜在変数**を扱うのに対し、LCA は**カテゴリカルな潜在変数**を扱う点が決定的な違いです。各個人がクラスに属する事後確率を推定するため、単純なクラスター分析より分類の不確実性を扱えます。連続指標版は LPA、縦断版は LTA、成長軌跡版は LCGA / GMM。クラス数決定は BIC・LMR-LRT・BLRT・エントロピーを併用、後続分析では Vermunt 3-step による分類誤差補正が論文水準。
:::

ここまでの EFA/CFA/SEM/LGM では、潜在変数を基本的に**連続量**として扱ってきました。例えば、動機づけ、読解力、語彙力、成長率は「低い」から「高い」まで連続的に変化する量として表現しました。一方で、研究上の問いによっては、潜在変数を連続量ではなく**観測されないカテゴリ**として表したい場合があります。この代表例が**潜在クラス分析（latent class analysis: LCA）**です[@collins2010; @linzer2011]。

LCA は、複数のカテゴリカル指標（例: 学習方略の有無、エラータイプ、回答パターン）から、観測されない下位集団を推定するモデルです。たとえば、英語学習者を「自律的方略型」「文法練習型」「全体的高使用型」のようなプロファイルに分類したい場合、単純なクラスター分析ではなく、各学習者が各クラスに属する**事後確率**を推定できる LCA が候補になります。

#### 連続潜在変数とカテゴリカル潜在変数

| 潜在変数の型 | 代表的手法 | 観測指標 | 答える問い |
|---|---|---|---|
| 連続潜在変数 | EFA / CFA / SEM / IRT | 連続、順序、2値 | 背後にある能力・態度・構成概念は何次元か |
| カテゴリカル潜在変数 | LCA | 主にカテゴリカル | 観測されない下位集団はいくつあり、どんな特徴か |
| カテゴリカル潜在変数 | LPA（latent profile analysis） | 主に連続 | 連続得点パターンに基づくプロファイルはいくつか |
| カテゴリカル潜在変数の縦断変化 | LTA（latent transition analysis） | 時点ごとのカテゴリカル指標 | クラス所属が時間とともにどう遷移するか |
| 成長軌跡の異質性 | LCGA / GMM | 縦断データ | 成長パターンが異なる下位集団はいくつあるか |
| 因子 + クラス | Factor mixture model | 連続 / 順序 / 2値 | 連続因子構造と潜在クラスを同時に仮定できるか |

LCA の基本式は次の通りです。個人 $i$ の観測反応を $\mathbf{y}_i = (y_{i1}, \dots, y_{iJ})$、潜在クラスを $C_i = k$ とすると、

$$
P(\mathbf{Y}_i = \mathbf{y}_i)
= \sum_{k=1}^{K} \pi_k
\prod_{j=1}^{J} P(Y_{ij} = y_{ij} \mid C_i = k)
$$

です。$\pi_k$ はクラス $k$ の割合であり、$P(Y_{ij} = y_{ij} \mid C_i = k)$ は、クラス $k$ に属する人が項目 $j$ で特定のカテゴリを選ぶ確率です。この式の要点は、**クラスを条件づけると、指標どうしは独立である**という局所独立（local independence）の仮定です。局所独立が大きく破れている場合、クラス数が過剰に増えたり、実質的には同じクラスが分割されたりします。

#### LCA と MCA / クラスター分析の違い

| 観点 | LCA | MCA | クラスター分析 |
|---|---|---|---|
| 主目的 | 未観測クラスの推定 | カテゴリ関係の可視化 | 似た個体のグループ化 |
| モデル | 確率モデル | 幾何学的分解 | 距離ベースが多い |
| 不確実性 | クラス所属確率を出せます | 基本的には出しません | 方法によります |
| 入力 | カテゴリカル指標 | クロス集計表または個票カテゴリ | 連続・カテゴリなど |
| 注意点 | 局所独立、クラス数、ラベルづけ | 軸解釈、低頻度カテゴリ | 距離尺度、標準化、安定性 |

MCA は「カテゴリがどのように布置されるか」を見る地図です。一方、LCA は「観測されない学習者タイプがいくつあり、各タイプがどのような回答確率を持つか」を推定します。両者は競合というより補完的です。MCA で全体像を見た後、分類的な仮説が強い場合に LCA を行う、という順序が実務的です。

#### LCA の例: 学習方略プロフィール

ここでは、5つの学習方略について、回答を `1 = Low`, `2 = Medium`, `3 = High` の3カテゴリで記録したデータをシミュレーションします。実際の研究では、質問紙の複数項目を単純に合成する前に、項目内容、回答分布、カテゴリの意味を確認します。

```{r}
#| eval: true
#| label: lca-data-simulation

set.seed(2024)
n_lca <- 600

lca_class_names <- c("Balanced", "Form-focused", "Autonomous")
lca_class_prob <- c(0.33, 0.34, 0.33)
true_lca_class <- sample(
  seq_along(lca_class_names),
  size = n_lca,
  replace = TRUE,
  prob = lca_class_prob
)

# Rows = latent classes; columns = response probabilities for Low, Medium, High.
lca_item_prob <- list(
  ExtensiveReading = rbind(
    c(.10, .25, .65),
    c(.70, .20, .10),
    c(.05, .15, .80)
  ),
  GrammarDrill = rbind(
    c(.15, .35, .50),
    c(.05, .15, .80),
    c(.65, .25, .10)
  ),
  SpeakingPractice = rbind(
    c(.20, .35, .45),
    c(.75, .15, .10),
    c(.05, .15, .80)
  ),
  VocabularyReview = rbind(
    c(.10, .30, .60),
    c(.05, .20, .75),
    c(.45, .35, .20)
  ),
  L1Translation = rbind(
    c(.35, .40, .25),
    c(.05, .20, .75),
    c(.70, .20, .10)
  )
)

simulate_lca_item <- function(prob_mat, class_id) {
  purrr::map_int(class_id, \(k) sample.int(3, size = 1, prob = prob_mat[k, ]))
}

lca_data <- purrr::imap_dfc(
  lca_item_prob,
  \(prob_mat, item_name) {
    tibble::tibble(!!item_name := simulate_lca_item(prob_mat, true_lca_class))
  }
) |>
  dplyr::mutate(
    TrueClass = factor(lca_class_names[true_lca_class], levels = lca_class_names)
  )

lca_data |>
  dplyr::select(-TrueClass) |>
  tidyr::pivot_longer(everything(), names_to = "Strategy", values_to = "Response") |>
  dplyr::count(Strategy, Response) |>
  dplyr::group_by(Strategy) |>
  dplyr::mutate(Percent = n / sum(n)) |>
  dplyr::ungroup() |>
  dplyr::mutate(
    Response = factor(Response, levels = 1:3, labels = c("Low", "Medium", "High")),
    Percent = scales::percent(Percent, accuracy = 0.1)
  ) |>
  tidyr::pivot_wider(names_from = Response, values_from = c(n, Percent)) |>
  knitr::kable(
    caption = "Observed Response Distributions for LCA Indicators",
    booktabs = TRUE
  )
```

このシミュレーションでは真のクラスを内部的に保持していますが、推定には使いません。実データでは当然、真のクラスは観測されません。LCA は、観測された複数のカテゴリカル回答から、どのような潜在クラス構造が最も妥当かを推定します。

#### クラス数の比較

LCA ではクラス数 $K$ を事前に固定して推定します。そのため、複数のクラス数を比較し、BIC、AIC、エントロピー、各クラスの解釈可能性、クラスサイズ、局所独立の残差を総合して選びます。@nylund2007 は、LCA や成長混合モデルのクラス数選択では、情報量規準と尤度比検定を含む複数の指標を比較する必要があることを示しています。

```{r}
#| eval: true
#| label: lca-class-enumeration

lca_formula <- cbind(
  ExtensiveReading,
  GrammarDrill,
  SpeakingPractice,
  VocabularyReview,
  L1Translation
) ~ 1

lca_entropy <- function(posterior) {
  k <- ncol(posterior)
  if (k == 1) return(NA_real_)
  # Convention: 0 * log(0) = 0 (Shannon entropy)
  p_log_p <- ifelse(posterior > 0, posterior * log(posterior), 0)
  1 + sum(p_log_p) / (nrow(posterior) * log(k))
}

set.seed(2024)
lca_models <- purrr::map(
  1:4,
  \(k) poLCA::poLCA(
    lca_formula,
    data = lca_data,
    nclass = k,
    nrep = 10,
    maxiter = 2000,
    calc.se = FALSE,
    verbose = FALSE,
    na.rm = FALSE
  )
)

lca_fit_tbl <- tibble::tibble(
  Classes = 1:4,
  LogLik = purrr::map_dbl(lca_models, "llik"),
  AIC = purrr::map_dbl(lca_models, "aic"),
  BIC = purrr::map_dbl(lca_models, "bic"),
  Entropy = purrr::map_dbl(lca_models, \(fit) lca_entropy(fit$posterior))
) |>
  dplyr::mutate(
    BestByBIC = BIC == min(BIC),
    dplyr::across(c(LogLik, AIC, BIC, Entropy), \(x) round(x, 3))
  )

lca_best_k <- lca_fit_tbl$Classes[which.min(lca_fit_tbl$BIC)]
fit_lca_best <- lca_models[[lca_best_k]]

lca_fit_tbl |>
  knitr::kable(
    caption = "Latent Class Enumeration: Fit Indices",
    booktabs = TRUE
  )
```

BIC が最小のモデルを第一候補にします。ただし、BIC が小さいからといって自動的に最良モデルと決めるのではなく、極端に小さいクラスがないか、各クラスが理論的に説明できるか、局所独立の仮定が大きく破れていないかを確認します。混合モデルの尤度には局所最大値が生じやすいため、`nrep` で複数の初期値から推定することも重要です。授業用コードでは計算時間を抑えるため `nrep = 10` としていますが、実研究では `nrep = 50` 以上から始め、同じ最大対数尤度に複数回到達するかを確認します。LCA のクラス名は推定結果から後で付けるラベルであり、モデル自体が「自律型」などの名前を返すわけではありません。

::: {.callout-important}
**クラス数決定: BIC を超えて — LMR-LRT・BLRT・ssBIC**

@nylund2007 によるシミュレーション研究は、LCA / GMM のクラス数決定で **BIC・BLRT** が最も正確であり、AIC は過剰抽出しやすく、Lo-Mendell-Rubin LRT (LMR-LRT) は中庸とされます。実務では以下の指標を併用してください:

- **BIC** (Bayesian Information Criterion): $-2\log L + p \log n$。値が最小のモデルを優先。サンプルサイズに敏感で、$n$ が大きいと小モデルを支持しがち
- **ssBIC** (sample-size adjusted BIC, Sclove 1987): $-2\log L + p \log[(n+2)/24]$。中規模サンプルで BIC より信頼できる
- **AIC** (Akaike Information Criterion): $-2\log L + 2p$。クラス数を過大推定する傾向あり、参考値として
- **LMR-LRT** [@lo2001testing]: $K$ クラスと $K-1$ クラスをネストモデルとして比較する近似 LRT。$p$ 値が有意なら $K$ クラスを採用。`tidyLPA::compare_solutions()` で得られる
- **BLRT** (Bootstrapped LRT, McLachlan 1987): パラメトリックブートストラップで LRT 分布を構築。Nylund らが推奨する gold standard だが計算コスト大。`mclust::mclustBootstrapLRT()` で実装可能
- **エントロピー** $E_K = 1 - \sum_i \sum_k p_{ik}\log p_{ik}/(n \log K)$: 分類の鮮明さ（0–1）。$> .80$ で良好、$< .60$ では群分けが曖昧

`poLCA` は BIC・AIC・エントロピーを返しますが、LMR-LRT と BLRT は標準実装されていません。論文で本格的にクラス数を主張する際は、(a) **連続指標 LPA**: `tidyLPA::estimate_profiles()` + `compare_solutions()` (内部は `mclust` ベースで GMM/LPA に対応)、(b) **カテゴリ LCA**: Mplus + `MplusAutomation` パッケージで TECH11 (LMR) / TECH14 (BLRT) を呼ぶ、(c) `LMest` パッケージ（潜在 Markov モデル）、または独自にパラメトリックブートストラップを実装、のいずれかが必要です。なお `mclust::mclustBootstrapLRT()` は混合正規（GMM/LPA）専用で、カテゴリ LCA には直接適用できません。
:::

```{r}
#| eval: true
#| label: lca-model-class-size

lca_model_class_size <- tibble::tibble(
  Class = seq_len(lca_best_k),
  ModelEstimatedShare = fit_lca_best$P
) |>
  dplyr::mutate(
    ExpectedN = ModelEstimatedShare * nrow(lca_data),
    ModelEstimatedShare = scales::percent(ModelEstimatedShare, accuracy = 0.1),
    ExpectedN = round(ExpectedN, 1)
  )

lca_model_class_size |>
  knitr::kable(
    caption = "Model-estimated Latent Class Shares",
    booktabs = TRUE
  )
```

推定されたクラス割合は、モデル上の母集団比率を表します。後で示す「最尤割当による人数」と一致するとは限りません。LCA では個人が確率的に複数クラスへまたがるため、モデル推定割合、割当人数、平均事後確率を分けて報告します。

#### 条件付き応答確率の解釈

LCA の中心的な出力は、各クラスにおける各指標カテゴリの条件付き応答確率です。これを見て、各クラスに実質的な名前を付けます。

```{r}
#| eval: true
#| label: lca-response-probabilities

lca_response_labels <- c("Low", "Medium", "High")

lca_prob_df <- purrr::imap_dfr(
  fit_lca_best$probs,
  \(prob_mat, item_name) {
    as.data.frame(prob_mat) |>
      tibble::rownames_to_column("Class") |>
      tidyr::pivot_longer(-Class, names_to = "Response", values_to = "Probability") |>
      dplyr::mutate(
        Class = readr::parse_number(Class),
        Response = readr::parse_number(Response),
        ResponseLabel = factor(lca_response_labels[Response], levels = lca_response_labels),
        Strategy = item_name
      )
  }
)

lca_profile_table <- lca_prob_df |>
  dplyr::group_by(Class, Strategy) |>
  dplyr::slice_max(Probability, n = 1, with_ties = FALSE) |>
  dplyr::ungroup() |>
  dplyr::mutate(Probability = round(Probability, 3)) |>
  dplyr::arrange(Class, Strategy) |>
  dplyr::select(Class, Strategy, MostLikelyResponse = ResponseLabel, Probability)

lca_profile_table |>
  knitr::kable(
    caption = "Most Likely Responses by Latent Class",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lca-class-label-guide

lca_class_label_guide <- lca_prob_df |>
  dplyr::group_by(Class) |>
  dplyr::summarise(
    HighProbabilityStrategies = paste(
      Strategy[ResponseLabel == "High" & Probability >= .50],
      collapse = ", "
    ),
    LowProbabilityStrategies = paste(
      Strategy[ResponseLabel == "Low" & Probability >= .50],
      collapse = ", "
    ),
    .groups = "drop"
  ) |>
  dplyr::mutate(
    HighProbabilityStrategies = dplyr::na_if(HighProbabilityStrategies, ""),
    LowProbabilityStrategies = dplyr::na_if(LowProbabilityStrategies, ""),
    SuggestedInterpretation = dplyr::case_when(
      stringr::str_detect(HighProbabilityStrategies, "GrammarDrill") &
        stringr::str_detect(HighProbabilityStrategies, "L1Translation") ~ "Form-focused / translation-oriented",
      stringr::str_detect(HighProbabilityStrategies, "ExtensiveReading") &
        stringr::str_detect(HighProbabilityStrategies, "SpeakingPractice") ~ "Autonomous / communicative",
      TRUE ~ "Balanced / mixed"
    )
  )

lca_class_label_guide |>
  knitr::kable(
    caption = "Substantive Labeling Guide for Latent Classes",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lca-response-probability-plot
#| fig-width: 10
#| fig-height: 7
#| fig-cap: "LCA 条件付き応答確率（Conditional Response Probabilities in Latent Class Analysis）"

ggplot(
  lca_prob_df,
  aes(x = ResponseLabel, y = Probability, fill = factor(Class))
) +
  geom_col(position = position_dodge(width = 0.75), width = 0.65) +
  facet_wrap(~ Strategy, ncol = 2) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1), limits = c(0, 1)) +
  labs(
    title = "Conditional Response Probabilities by Latent Class",
    subtitle = "Class labels are assigned after inspecting these response profiles",
    x = "Response category",
    y = "Probability",
    fill = "Class"
  )
```

この図では、各クラスがどの方略を高く使う傾向にあるかを確認します。例えば、あるクラスで `GrammarDrill` と `L1Translation` の High 確率が高く、`SpeakingPractice` の High 確率が低ければ、そのクラスは「形式重視型」と解釈できます。別のクラスで `ExtensiveReading` と `SpeakingPractice` が高ければ、「自律的・コミュニケーション重視型」と名付けられるかもしれません。ただし、命名は統計モデルではなく研究者の解釈です。項目内容と理論的背景に基づいて慎重に行います。

#### 局所独立の診断

LCA は、潜在クラスを条件づければ指標どうしが独立であると仮定します。しかし、質問項目の文言が似ている、同じ下位技能を測っている、同じ方法効果を共有している場合、クラス内でも項目間関連が残ることがあります。これを**局所依存**と呼びます。局所依存が強いと、モデルはその残差相関を説明するために不要なクラスを増やすことがあります。

以下では、各項目ペアについて、LCA モデルから期待される2変量クロス表と観測クロス表を比較し、ペアごとの残差の大きさを確認します。これは簡易診断であり、ソフトウェアごとの bivariate residual と完全に同じ定義ではありませんが、どの項目ペアが局所独立の仮定を破りやすいかを把握する実務的な手がかりになります。

```{r}
#| eval: true
#| label: lca-local-independence

lca_items <- names(lca_item_prob)
lca_response_levels <- 1:3

compute_lca_pair_residual <- function(item_pair, data, fit, response_levels) {
  item_a <- item_pair[1]
  item_b <- item_pair[2]
  observed <- table(
    factor(data[[item_a]], levels = response_levels),
    factor(data[[item_b]], levels = response_levels)
  )

  expected_prob <- matrix(
    0,
    nrow = length(response_levels),
    ncol = length(response_levels)
  )

  for (k in seq_along(fit$P)) {
    p_a <- as.numeric(fit$probs[[item_a]][k, ])
    p_b <- as.numeric(fit$probs[[item_b]][k, ])
    expected_prob <- expected_prob + fit$P[k] * tcrossprod(p_a, p_b)
  }

  expected <- nrow(data) * expected_prob
  # Standardized Pearson residual; expected is non-zero by construction here
  residual <- (observed - expected) / sqrt(expected)

  tibble::tibble(
    Item1 = item_a,
    Item2 = item_b,
    Pair = paste(item_a, item_b, sep = " × "),
    BivariateResidual = sum(residual^2),
    MaxAbsCellResidual = max(abs(residual))
  )
}

lca_local_independence <- combn(lca_items, 2, simplify = FALSE) |>
  purrr::map_dfr(
    compute_lca_pair_residual,
    data = lca_data,
    fit = fit_lca_best,
    response_levels = lca_response_levels
  ) |>
  dplyr::arrange(dplyr::desc(BivariateResidual))

lca_local_independence |>
  dplyr::mutate(
    BivariateResidual = round(BivariateResidual, 2),
    MaxAbsCellResidual = round(MaxAbsCellResidual, 2)
  ) |>
  knitr::kable(
    caption = "Local Independence Diagnostics for LCA",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lca-local-independence-plot
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "LCA 局所独立診断（Local Independence Diagnostics for LCA）"

ggplot(
  lca_local_independence,
  aes(x = BivariateResidual, y = forcats::fct_reorder(Pair, BivariateResidual))
) +
  geom_col(fill = "#756bb1", width = 0.7) +
  labs(
    title = "Pairwise Residual Dependence after Accounting for Latent Classes",
    subtitle = "Large values suggest item pairs that may violate local independence",
    x = "Bivariate residual summary",
    y = NULL
  )
```

局所依存が大きい項目ペアが見つかった場合、すぐにクラス数を増やすのではなく、まず項目内容を確認します。意味的にほぼ同じ項目なら片方を除外する、同じ下位領域を測る複数項目なら下位尺度化する、理論的に必要なら局所依存を許すモデルや因子混合モデルを検討します。LCA は「クラスを増やせば適合が良くなる」ため、実質的に解釈できない小クラスを作って局所依存を吸収していないかを常に点検します。

#### クラス所属確率と分類の不確実性

LCA は各個人に「最も可能性の高いクラス」を割り当てられますが、分類は確率的です。最大事後確率が .55 の人と .98 の人を同じ確かさで分類してはいけません。

```{r}
#| eval: true
#| label: lca-class-assignment

lca_assignment <- lca_data |>
  dplyr::mutate(
    AssignedClass = fit_lca_best$predclass,
    MaxPosterior = apply(fit_lca_best$posterior, 1, max)
  )

lca_assignment_summary <- lca_assignment |>
  dplyr::group_by(AssignedClass) |>
  dplyr::summarise(
    n = dplyr::n(),
    ClassShare = n / nrow(lca_assignment),
    MeanPosterior = mean(MaxPosterior),
    LowCertainty = mean(MaxPosterior < .70),
    .groups = "drop"
  ) |>
  dplyr::mutate(
    ClassShare = scales::percent(ClassShare, accuracy = 0.1),
    MeanPosterior = round(MeanPosterior, 3),
    LowCertainty = scales::percent(LowCertainty, accuracy = 0.1)
  )

lca_assignment_summary |>
  knitr::kable(
    caption = "Latent Class Size and Classification Uncertainty",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: lca-average-posterior-table

lca_posterior_tbl <- as.data.frame(fit_lca_best$posterior) |>
  tibble::as_tibble(.name_repair = \(x) paste0("PosteriorClass", seq_along(x))) |>
  dplyr::mutate(AssignedClass = fit_lca_best$predclass)

lca_average_posterior <- lca_posterior_tbl |>
  dplyr::group_by(AssignedClass) |>
  dplyr::summarise(
    dplyr::across(
      starts_with("PosteriorClass"),
      \(x) mean(x),
      .names = "{.col}"
    ),
    .groups = "drop"
  ) |>
  dplyr::mutate(
    dplyr::across(starts_with("PosteriorClass"), \(x) round(x, 3))
  )

lca_average_posterior |>
  knitr::kable(
    caption = "Average Posterior Class Probabilities by Assigned Class",
    booktabs = TRUE
  )
```

この表は、割り当てられたクラスごとに平均事後確率をまとめたものです。対角成分（例: AssignedClass 1 の PosteriorClass1）が高く、非対角成分が低いほど、クラス分離が良好です。エントロピーだけでなく、この表を併記すると、どのクラス間で曖昧さが残っているかが分かります。

```{r}
#| eval: true
#| label: lca-posterior-uncertainty-plot
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "LCA 最大事後確率の分布（Distribution of Maximum Posterior Class Probabilities）"

ggplot(lca_assignment, aes(x = MaxPosterior, fill = factor(AssignedClass))) +
  geom_histogram(binwidth = 0.05, color = "white", boundary = 0) +
  geom_vline(xintercept = .70, linetype = "dashed", color = "grey35") +
  scale_x_continuous(labels = scales::percent_format(accuracy = 1), limits = c(0, 1)) +
  labs(
    title = "Classification Uncertainty in LCA",
    subtitle = "Dashed line marks a rough .70 posterior-probability threshold",
    x = "Maximum posterior class probability",
    y = "Number of learners",
    fill = "Assigned class"
  )
```

このヒストグラムでは、右側に多くの学習者が集まるほど、分類の確実性が高いと判断できます。左側に多い場合、クラス間の分離が弱く、クラス数を減らす、指標を見直す、または分類結果を後続分析で硬い群変数として扱わない、といった対応を検討します。

#### 共変量を入れる LCA

学習者背景がクラス所属を予測するかを調べたい場合は、潜在クラス回帰（latent class regression）を使います。`poLCA` では、右辺に共変量を指定します[@linzer2011]。

```{r}
#| eval: false
#| label: lca-with-covariates-example

lca_covariate_model <- cbind(
  ExtensiveReading,
  GrammarDrill,
  SpeakingPractice,
  VocabularyReview,
  L1Translation
) ~ Proficiency + StudyHours + L1Background

fit_lca_covariates <- poLCA::poLCA(
  lca_covariate_model,
  data = learner_strategy_data,
  nclass = 3,
  nrep = 20,
  verbose = FALSE
)
```

このモデルでは、共変量が「各指標への直接効果」ではなく、**潜在クラス所属確率**を予測します。したがって、「高習熟度者は自律的方略型に属する確率が高いか」のような問いに対応します。ただし、クラスを推定した後に `AssignedClass` を通常の従属変数として回帰する単純な2段階分析は、分類誤差を無視するため推定が歪む可能性があります。分類の不確実性を考慮する方法を使うか、少なくとも最大事後確率と分類品質を報告します。

後続アウトカム（例: 期末成績、読解伸長、脱落）をクラス間で比較したい場合も同じ注意が必要です。`AssignedClass` を固定された群として扱うと、分類誤差によりクラス差が過小推定または歪むことがあります。@bray2015 は、分類後に分析する classify-analyze アプローチでは、事後確率を改善する inclusive LCA や擬似クラス割当など、分類誤差を考慮する工夫が必要であることを示しています。入門段階では、まずクラス所属確率を報告し、後続分析は探索的であると明記します。論文の主張に使う場合は、3-step 法、BCH 法、またはクラスとアウトカムを同時に扱うモデルを検討します。

::: {.callout-important}
**Vermunt (2010) 3-step アプローチと BCH 補正**

LCA のクラス所属を共変量・アウトカムと結びつける際、単純な 2 段階分析（推定 → 割当 → 回帰）は分類誤差を無視するためバイアスが生じます。@vermunt2010 が提案した **3-step アプローチ** は次の手順で補正します:

1. **Step 1**: LCA を測定指標のみで推定し、クラスの応答プロファイルを確定する（モデル安定化）
2. **Step 2**: 各個人の事後確率を計算し、modal assignment（最大事後確率クラスへ割当）または probabilistic assignment（事後確率を重みとする割当）を行う。同時に**分類誤差行列** $D = (d_{kk'})$（真クラス $k$ から推定クラス $k'$ への誤分類確率）を保存
3. **Step 3**: $D$ を用いて補正した重み付き回帰でアウトカム or 共変量効果を推定。代表的な補正法は:
   - **BCH** [@bolck2004]: 元来は**カテゴリアウトカム**を主対象とする手法で、inverse of $D$ で重みを構築し加重多項ロジット回帰を行う。連続アウトカムへの拡張は @vermunt2010 が提示
   - **ML 3-step** [@vermunt2010]: Step 3 でクラス所属を潜在変数として再 ML 推定、$D$ を尤度内で固定。連続/カテゴリ両対応
   - **DCAT** [@asparouhov2014]: Mplus が標準実装する補正法で、3-step を auxiliary variable approach として体系化

**R 実装の現状**: `poLCA` 標準では 3-step 法は未対応です。Mplus（`MplusAutomation` 経由）、`LMest`（潜在 Markov モデル）、`tidySEM`、`mclust` 系のいずれかで実装する必要があります。@asparouhov2014 の DCAT は Mplus が事実上の標準実装となっています。本書のサンプルサイズ（数百名）程度なら 2 段階法と 3-step の差は中程度（誤差 5–15% 程度）ですが、クラス分離が弱い（エントロピー $< .80$）場合は深刻なバイアスが生じます。論文で主張する場合は必ず Step 3 補正を実施してください。
:::

#### LCA 実施時のチェックリスト

| チェック項目 | 具体的に確認すること | 問題がある場合 |
|---|---|---|
| クラス数 | BIC/AIC、エントロピー、解釈可能性、小クラスの有無 | クラス数を減らす、指標を見直す |
| 局所最大値 | 複数初期値で同じ最大対数尤度に到達するか | `nrep` を増やす、開始値を増やす |
| 局所独立 | 項目ペア残差が大きくないか | 類似項目の統合、局所依存モデル、因子混合モデル |
| 分類品質 | 平均最大事後確率、平均事後確率表 | 硬い分類を避ける、クラス数・指標を見直す |
| クラス命名 | 条件付き応答確率と理論が一致するか | 恣意的なラベルを避け、記述的名称にする |
| 後続分析 | 分類誤差を無視していないか | 3-step 法、BCH 法、同時モデルを検討する |

#### その他の潜在変数モデル

| モデル | 使う場面 | 本章との関係 | 注意点 |
|---|---|---|---|
| LPA | 連続指標から学習者プロフィールを推定したい | LCA の連続指標版です[@rosenberg2018tidylpa] | 正規混合分布、分散共分散構造の仮定に敏感です |
| LTA | 方略型・不安型などのクラスが時間とともに変化するか見たい | LCA の縦断拡張です[@collins2010] | 時点間の測定不変性と遷移確率を確認します |
| LCGA | 成長軌跡を少数のクラスに分けたい | LGM の混合モデル版です | クラス内の個人差を強く制約するため、解釈は慎重にします |
| GMM | 成長軌跡のクラスとクラス内個人差を同時に扱いたい | LGM + 混合モデルです[@nylund2007] | 局所解、過剰抽出、小さいクラスに注意します |
| Mixture IRT | 項目反応の背後に能力と潜在クラスを同時に仮定したい | IRT と LCA の統合です | DIF や集団差の解釈に有用ですが、識別が難しいです |
| Factor mixture model | 連続因子と潜在クラスを同時に推定したい | CFA と LCA の統合です | 「次元差」と「類型差」を混同しないことが重要です |
| GLLVM | 多変量の2値・カウント・カテゴリ反応を潜在変数で説明したい | GLMM と因子モデルの統合です | モデル指定と推定診断が複雑です |

初心者向けには、まず「連続潜在変数なら EFA/CFA/SEM、カテゴリカル潜在変数なら LCA/LPA、縦断なら LGM/LTA/GMM」と整理するとよいです。専門的には、これらはすべて「観測データの背後に未観測構造を置く」モデルですが、未観測構造を連続量として置くか、カテゴリとして置くか、時間変化させるかによってモデルが分岐します。

::: {.callout-note}
**LCGA と GMM の違い: クラス内分散制約**

LCGA（Nagin 1999）と GMM（Muthén 2004）はいずれも「成長軌跡が異なる下位集団を抽出する」モデルですが、**クラス内の個人差をどう扱うか**が決定的に異なります:

- **LCGA** (Latent Class Growth Analysis; Muthén & Muthén 2000 が命名、起源は @nagin1999 の **GBTM = Group-Based Trajectory Model**): クラス内のランダム効果分散を**ゼロに固定** $\boldsymbol{\Psi}_k = \mathbf{0}$。同じクラス内では全員が同一の成長軌跡（クラス平均）を持つと仮定。クラス数で異質性を表現し、識別が容易。Nagin の Stata 用 `traj` プロシージャ・SAS 用 `proc traj` 系の標準
- **GMM** (Growth Mixture Model; Muthén 2004): クラス内のランダム効果分散 $\boldsymbol{\Psi}_k$ を**自由推定**（または等値制約 $\boldsymbol{\Psi}_k = \boldsymbol{\Psi}$）。同じクラス内でも個人差が許容され、より現実的だが識別が難しく局所解が多発

実務上の選択指針: (a) クラス数が解釈的に明確で、クラス内の個人差は副次的なら LCGA。(b) クラス分類とクラス内個人差の両方を主張したいなら GMM。(c) GMM の収束問題が厳しい場合は、まず LCGA で枠組みを確認 → クラス内分散を等値制約 GMM → 自由推定 GMM の順に複雑化させる戦略が安全。

**R 実装**: GMM/LCGA は `lcmm::hlme()`（連続アウトカム）、`OpenMx`（柔軟）、Mplus（事実上の標準）で推定可能。@nylund2007 のシミュレーションは GMM・LCGA 両方で BLRT がクラス数決定に最適と示しています。実装例:

```r
# LCGA: クラス内分散ゼロ固定 (nwg = FALSE で random effects なし)
fit_lcga <- lcmm::hlme(
  fixed = vocab ~ time,
  mixture = ~ time,        # クラスごとに固定効果を推定
  subject = "id",
  ng = 3,                  # 3 クラス
  data = lgm_long,
  B = lcmm::hlme(fixed = vocab ~ time, subject = "id", data = lgm_long)$best
)

# GMM (等値制約版): クラス内ランダム効果を全クラスで等値
fit_gmm_eq <- lcmm::hlme(
  fixed = vocab ~ time,
  mixture = ~ time,
  random = ~ time,         # ランダム傾き追加
  nwg = FALSE,             # クラス間で random 効果分散を等値
  subject = "id",
  ng = 3,
  data = lgm_long
)

# GMM (自由推定版): クラス間でランダム効果分散を自由
fit_gmm_free <- lcmm::hlme(
  fixed = vocab ~ time,
  mixture = ~ time,
  random = ~ time,
  nwg = TRUE,              # クラス間で異なる random 効果分散
  subject = "id",
  ng = 3,
  data = lgm_long
)

# 事後確率と分類
postprob(fit_gmm_eq)
predictY(fit_gmm_eq, newdata = lgm_long, var.time = "time")
```

`lcmm` の `B` 引数（初期値）には `ng = 1` の単一クラスモデルの推定値を渡すのが標準的な start 戦略です。`hlme()` は内部で複数 random start を試行しないため、`gridsearch()` ヘルパーで初期値を網羅的に試すか、`B` を手動で複数渡して最大対数尤度を比較します。
:::

::: {.callout-tip}
**Latent Change Score モデル（McArdle 2009）への発展**

LGM が「全時点を通した平均的成長」をモデル化するのに対し、**Latent Change Score (LCS) モデル** [@mcardle2009] は隣接時点間の差 $\Delta Y_t = Y_t - Y_{t-1}$ を潜在変数として明示します。これにより、(a) **比例的変化** ($\beta Y_{t-1}$: 直近水準に依存する変化)と(b) **定常的変化** ($\alpha$: 一定の変化)を分離でき、time-series 分析（第 13 章 ARIMA）と LGM の橋渡しになります。Bivariate Dual Change Score モデルでは 2 変数間の動的関連（例: 動機づけ → 語彙力 vs 語彙力 → 動機づけ）も検討できます。`lavaan` で構文化可能ですが、識別と多時点が必要（5 時点以上推奨）です。
:::

#### LCA の報告テンプレート

```{r}
#| eval: true
#| label: report-lca
#| results: asis

lca_best_fit <- lca_fit_tbl |>
  dplyr::filter(BestByBIC)

lca_mean_max_posterior <- mean(lca_assignment$MaxPosterior)
lca_largest_local_dependence <- lca_local_independence |>
  dplyr::slice_max(BivariateResidual, n = 1, with_ties = FALSE) |>
  dplyr::mutate(
    Summary = paste0(Pair, " (BVR = ", round(BivariateResidual, 2), ")")
  ) |>
  dplyr::pull(Summary)

lca_report <- glue::glue(
  "A latent class analysis was conducted using poLCA [@linzer2011] ",
  "to identify learner strategy profiles from five categorical indicators (N = {n_lca}). ",
  "Models with one to four classes were compared. The {lca_best_k}-class solution had the lowest BIC ",
  "(BIC = {round(lca_best_fit$BIC, 1)}, AIC = {round(lca_best_fit$AIC, 1)}, ",
  "entropy = {round(lca_best_fit$Entropy, 2)}). ",
  "Class interpretation was based on conditional response probabilities, and classification uncertainty ",
  "was evaluated using posterior class probabilities (mean maximum posterior probability = ",
  "{round(lca_mean_max_posterior, 2)}). Local independence diagnostics were inspected; the largest ",
  "pairwise residual dependence was {lca_largest_local_dependence}."
)

knitr::asis_output(lca_report)
```

ここまでは連続変数や Likert 尺度データの潜在構造を扱う手法（PCA・EFA・CFA・SEM・LGM）と、カテゴリカル潜在変数を扱う LCA を学んできました。しかし SLA 研究では、エラータイプ・学習方略・L1背景などの**カテゴリカルデータ**の関係を、クラスとしてではなく地図として見たい場面も頻繁にあります。CA（コレスポンデンス分析）と MCA（多重対応分析）は、こうしたカテゴリカルデータに対して PCA と同様の次元縮約を行い、カテゴリ間の関連構造を低次元空間に可視化する手法です。

### コレスポンデンス分析（CA / MCA）

::: {.callout-tip}
**1 分プレビュー (CA / MCA)**

「L1 背景 × エラータイプのクロス集計を可視化し、**どの L1 がどのエラーと結びついているか**を 2 次元地図で読み取りたい」という時に使います。CA はカイ二乗距離に基づく標準化残差行列を SVD し、行カテゴリ（L1）と列カテゴリ（エラー）を同じ平面に配置します。MCA は CA を**個票データ（複数のカテゴリカル変数）**に拡張した手法で、学習者背景アンケートのプロファイル分析に最適です。総慣性 = $\chi^2/n$ の関係が核心。**潜在変数モデルではなく**、カテゴリ間関連の幾何学的可視化と理解してください。
:::

#### コンセプト

コレスポンデンス分析（CA）は、クロス集計表（行カテゴリ × 列カテゴリ）の関係を低次元空間に写像して可視化する手法です[@greenacre2017]。連続変数に対する PCA の役割を、カテゴリカルデータに対して果たします。

ここまでの EFA・CFA・SEM・LGM は連続変数を扱う潜在変数モデルでした。ここからは、カテゴリカルデータ（名義尺度・順序尺度）の関連構造を低次元空間に可視化する手法に転じます。コレスポンデンス分析（CA）はクロス集計表を対象とし、行カテゴリと列カテゴリの関連パターンを2次元平面上に布置します。

言語学・SLA 研究では CA が多様な場面で活用されています。@glynn2014 はコーパス意味論の分野で、多義語や類義語の用法パターンをクロス集計表としてまとめ、CA で視覚的に探索する手法を体系的に解説しました。この方法により、例えば動詞 *run* の多義的用法がどのような文脈変数と結びつくかを2次元平面上で一望できます。@gries2014 は学習者コーパス研究で MuPDAR（Multifactorial Prediction and Deviation Analysis using Regressions）を提案し、ネイティブ話者と学習者の言語使用偏差を回帰と CA の組み合わせで多角的に分析する枠組みを示しました。@levshina2015 は応用言語学者向けの R 統計入門書で、CA と MCA をそれぞれ専用の章で扱い、構文文法やポリセミー研究への適用例を豊富に紹介しており、言語学における CA の標準的な参照文献となっています。

#### 概要
1. CA はクロス集計表を入力します。行 / 列プロファイルのカイ二乗距離を分解します。R では `FactoMineR::CA()`[@le2008] を使います。
2. MCA は個票データ（複数カテゴリ変数）を入力します。ダミー展開して CA を適用します。`FactoMineR::MCA()` が対応しています[@le2008]。
3. 慣性（inertia）は $\chi^2 / n$ です。軸ごとの寄与率で次元数を検討します。
4. まれなカテゴリは結果を不安定にするため、意味的に妥当な併合を検討します。

#### CA を使う場面・使わない場面

| 研究上の問い | CA が向いているか | 理由 |
|---|---|---|
| L1 背景によってエラータイプの分布が異なるかを見たい | 向いています | 行カテゴリと列カテゴリの関連パターンを地図化できます |
| 品詞タグと習熟度レベルの対応を探索したい | 向いています | クロス集計表にまとめられるカテゴリ関係です |
| 3つ以上のカテゴリ変数の回答パターンを個人単位で見たい | MCA が向いています | 個票データの多カテゴリ構造を扱えます |
| 連続得点の背後に潜在因子を仮定したい | 向いていません | EFA/CFA を使います |
| カテゴリが結果をどの程度予測するかを検定したい | 単独では不十分です | ロジスティック回帰や多項ロジットを併用します |

CA は探索的な可視化として非常に有用ですが、因果効果や予測精度を直接評価する手法ではありません。まずカイ二乗検定と標準化残差で関連の有無と偏りを確認し、その後に CA で全体構造を要約する、という順序が実務的です。

#### 数学的背景

$r \times c$ のクロス集計表 $\mathbf{N}$ に対し、対応行列 $\mathbf{P} = \mathbf{N} / n$、行質量 $\mathbf{r} = \mathbf{P} \mathbf{1}$、列質量 $\mathbf{c} = \mathbf{P}^\top \mathbf{1}$ とします。CA は次の標準化残差行列を**特異値分解**（SVD: Singular Value Decomposition）します。SVD とは、任意の行列を3つの行列の積 $\mathbf{U} \mathbf{D} \mathbf{V}^\top$ に分解する線形代数の手法であり、PCA の固有値分解を一般化したものと位置づけられます。

$$\mathbf{D}_r^{-1/2} (\mathbf{P} - \mathbf{r} \mathbf{c}^\top) \mathbf{D}_c^{-1/2} = \mathbf{U} \mathbf{D}_\alpha \mathbf{V}^\top$$

この式の直感的な意味は次の通りです。まず $\mathbf{P} - \mathbf{r} \mathbf{c}^\top$ は「観測比率と期待比率（独立性下の積比率）の差」を表す残差行列で、独立性からの偏差を示します。$\mathbf{D}_r^{-1/2}$ と $\mathbf{D}_c^{-1/2}$ による前後乗算は、各セルの偏差をその周辺確率の幾何平均の平方根で割ることに相当し、これによりカイ二乗距離（chi-square distance）の幾何が定義されます。**注意**: この標準化は「頻度の少ないカテゴリに大きな重みを与える」のではなく、むしろ慣性への寄与は質量（行・列周辺確率 $r_i, c_j$）に比例するため、まれなカテゴリの寄与は通常**小さく**抑えられます（Greenacre 2017, ch. 4）。SVD の結果、$\mathbf{U}$ が行カテゴリ（例: L1背景）の標準座標、$\mathbf{V}$ が列カテゴリ（例: エラータイプ）の標準座標、$\mathbf{D}_\alpha$ の対角要素（特異値 $\alpha_k$）の二乗 $\lambda_k = \alpha_k^2$ が各次元の慣性（principal inertia）を表します。

得られた座標により、行カテゴリと列カテゴリを同一空間に配置できます。**総慣性**（total inertia）は $\chi^2 / n$ に等しいです。慣性は CA における「分散」に相当する概念です。総慣性が大きいほど、行カテゴリと列カテゴリの間に強い関連があります。各次元の慣性の比率（寄与率）は PCA の累積寄与率と同様に解釈でき、第1・第2次元でどの程度の関連構造を捉えられているかを示します。

慣性（inertia）は PCA における分散に相当しますが、計算の基礎は異なります。PCA はピアソン相関（または共分散）行列の固有値分解を行うのに対し、CA は**標準化残差行列** $\mathbf{D}_r^{-1/2}(\mathbf{P}-\mathbf{r}\mathbf{c}^\top)\mathbf{D}_c^{-1/2}$ の特異値分解（SVD）を行い、その特異値の二乗 $\lambda_k$ が各次元の慣性となります。総慣性 $\sum_k \lambda_k = \chi^2/n$ という同値関係が CA の核心です。「プロファイル行列の固有値分解」と説明されることもありますが、厳密にはカイ二乗距離計量下での一般化 SVD と等価で、上記の標準化残差 SVD の方が定式として明確です。

#### CA の例: L1 背景 × エラータイプ

英語学習者のエラーパターンが母語（L1）によってどう異なるかを CA で可視化します。この種の分析は、学習者コーパス研究で広く行われています。@gries2014 は MuPDAR（Multifactorial Prediction and Deviation Analysis using Regressions）を提案し、フランス語/中国語 L1 話者による英語法助動詞の使用偏向を CA と回帰の組み合わせで分析しました。CA は、こうしたクロス集計型の中間言語データにおいてカテゴリ間の関連パターンを直感的に把握するための有力なツールです[@greenacre2017]。

学習者コーパス研究では、CA を用いて L1 背景とエラーパターンの関連を探索することが一般的です。例えば、品詞タグの分布と習熟度レベルのクロス集計に CA を適用し、初級者と上級者で異なる品詞使用パターンを2次元空間で可視化する研究があります。

クロス集計表の値は、L1転移研究の知見を反映して設定しました。日本語には冠詞体系がないため冠詞エラーを多く（45件）、中国語はトピック卓立言語であるため語順エラーを多く（25件）設定しています。スペイン語話者は前置詞・時制の干渉が大きく、アラビア語話者は動詞一致と時制に困難を示す傾向がある、という想定です。

```{r}
#| eval: true
#| label: ca-data-simulation

set.seed(2024)

# Row profiles differ by L1 (realistic patterns)
ca_data <- matrix(c(
  # Article, Preposition, Tense, Agreement, WordOrder
  45, 30, 25, 15, 10,  # Japanese (article difficulty)
  35, 25, 20, 20, 25,  # Chinese (word order difficulty)
  30, 28, 22, 25, 20,  # Korean (similar to Japanese but less article error)
  15, 35, 30, 25, 18,  # Spanish (preposition/tense difficulty)
  20, 20, 35, 30, 15   # Arabic (tense/agreement difficulty)
), nrow = 5, byrow = TRUE)

rownames(ca_data) <- c("Japanese", "Chinese", "Korean", "Spanish", "Arabic")
colnames(ca_data) <- c("Article", "Preposition", "Tense", "Agreement", "WordOrder")

as.data.frame(ca_data) |>
  rownames_to_column("L1 Background") |>
  knitr::kable(
    caption = "Contingency Table: L1 Background by Error Type",
    booktabs = TRUE
  )
```

CA に進む前に、クロス集計表そのものを診断します。CA はカイ二乗検定と同じ「観測度数と期待度数のズレ」を分解する手法なので、まず周辺度数、期待度数、標準化残差を確認します。

```{r}
#| eval: true
#| label: ca-table-diagnostics

ca_chisq <- stats::chisq.test(ca_data)
ca_expected <- ca_chisq$expected
ca_stdres <- ca_chisq$stdres
ca_total <- sum(ca_data)
ca_total_inertia <- unname(ca_chisq$statistic) / ca_total

tibble::tibble(
  Statistic = unname(ca_chisq$statistic),
  df = unname(ca_chisq$parameter),
  p_value = unname(ca_chisq$p.value),
  TotalInertia = ca_total_inertia,
  MinExpected = min(ca_expected),
  CellsExpectedBelow5 = sum(ca_expected < 5)
) |>
  dplyr::mutate(
    dplyr::across(where(is.numeric), \(x) round(x, 3)),
    p_value = dplyr::if_else(p_value < .001, "< .001", as.character(p_value))
  ) |>
  knitr::kable(
    caption = "Chi-square Diagnostics before Correspondence Analysis",
    booktabs = TRUE
  )
```

`p_value` が小さい場合、L1 背景とエラータイプは独立ではなく、何らかの関連パターンがあると判断できます。ただし、カイ二乗検定は「関連があるか」を示すだけで、「どのカテゴリ同士がどの方向にずれているか」は直接見せてくれません。そのため、次に行プロファイルと標準化残差を可視化します。

```{r}
#| eval: true
#| label: ca-row-profile-heatmap
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "L1 背景ごとのエラータイプ行プロファイル（Row Profiles by L1 Background）"

ca_row_profile_df <- as.table(prop.table(ca_data, margin = 1)) |>
  as.data.frame() |>
  dplyr::rename(L1Background = Var1, ErrorType = Var2, Proportion = Freq)

ggplot(ca_row_profile_df, aes(x = ErrorType, y = L1Background, fill = Proportion)) +
  geom_tile(color = "white") +
  geom_text(aes(label = scales::percent(Proportion, accuracy = 1)), size = 3.2) +
  scale_fill_gradient(low = "white", high = "#2b8cbe", labels = scales::percent) +
  labs(
    title = "Row Profiles of Error Types",
    subtitle = "Each row sums to 100%; compare patterns across L1 backgrounds",
    x = "Error type",
    y = "L1 background",
    fill = "Row %"
  ) +
  theme(axis.text.x = element_text(angle = 35, hjust = 1))
```

行プロファイルは、各 L1 グループのエラー分布を割合で示します。例えば Japanese の Article 比率が高ければ、日本語母語話者のエラー分布が全体平均より冠詞エラーに偏っている可能性があります。CA の座標は、このようなプロファイル差を低次元空間に写像したものです。

```{r}
#| eval: true
#| label: ca-standardized-residual-heatmap
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "標準化残差ヒートマップ（Standardized Residual Heatmap before CA）"

ca_resid_df <- as.table(ca_stdres) |>
  as.data.frame() |>
  dplyr::rename(L1Background = Var1, ErrorType = Var2, StdResidual = Freq)

ggplot(ca_resid_df, aes(x = ErrorType, y = L1Background, fill = StdResidual)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(StdResidual, 2)), size = 3.2) +
  scale_fill_gradient2(
    low = "#2166ac",
    mid = "white",
    high = "#b2182b",
    midpoint = 0
  ) +
  labs(
    title = "Standardized Residuals from the Independence Model",
    subtitle = "Red = more frequent than expected; Blue = less frequent than expected",
    x = "Error type",
    y = "L1 background",
    fill = "Std. residual"
  ) +
  theme(axis.text.x = element_text(angle = 35, hjust = 1))
```

標準化残差（standardized residual）は、観測度数が独立モデルの期待度数からどの程度ずれているかを示します。おおまかな目安として、絶対値が2を超えるセルは特徴的な偏りの候補です。CA のバイプロットで近接・同方向に見えるカテゴリは、この残差ヒートマップで本当に正の偏りを示しているか確認します。

標準化残差が「どちらの方向にずれているか」を示すのに対して、セル寄与率（cell contribution）は「そのセルが全体のカイ二乗統計量にどれだけ寄与したか」を示します。標準化残差の絶対値が大きく、かつセル寄与率も高い組み合わせは、CA の地図を形づくる主要な関連です。

```{r}
#| eval: true
#| label: ca-cell-contribution-heatmap
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "CA セル寄与率ヒートマップ（Cell Contributions to Chi-square in CA）"

ca_cell_contribution <- ((ca_data - ca_expected)^2 / ca_expected) /
  unname(ca_chisq$statistic) * 100

ca_cell_contrib_df <- as.table(ca_cell_contribution) |>
  as.data.frame() |>
  dplyr::rename(L1Background = Var1, ErrorType = Var2, Contribution = Freq)

ggplot(ca_cell_contrib_df, aes(x = ErrorType, y = L1Background, fill = Contribution)) +
  geom_tile(color = "white") +
  geom_text(aes(label = paste0(round(Contribution, 1), "%")), size = 3.1) +
  scale_fill_gradient(low = "white", high = "#756bb1") +
  labs(
    title = "Cell Contributions to the Chi-square Association",
    subtitle = "Large cells are the main sources of the CA structure",
    x = "Error type",
    y = "L1 background",
    fill = "Contribution"
  ) +
  theme(axis.text.x = element_text(angle = 35, hjust = 1))
```

`FactoMineR::CA()` でクロス集計表を CA に投入します。入力はクロス集計表（行名 = L1 背景、列 = エラータイプ）であり、個票データではない点に注意してください。`graph = FALSE` で自動プロットを抑制し、`factoextra::fviz_ca_biplot()` で可視化します。バイプロットでは、L1 背景（青三角）とエラータイプ（赤丸）が同じ空間に配置され、関連構造を概観できます。

```{r}
#| eval: true
#| label: ca-analysis
#| fig-width: 9
#| fig-height: 7

# Perform CA
res_ca <- FactoMineR::CA(ca_data, graph = FALSE)

# Biplot
fviz_ca_biplot(res_ca, repel = TRUE,
               title = "CA Biplot: L1 Background × Error Type")
```

バイプロットでは、原点（0, 0）は独立性（行と列が無関係）に近い平均プロファイルを表します。原点から離れたカテゴリほど特徴的な分布パターンを持ちます。同じ方向にある行カテゴリと列カテゴリは、対応するセルが期待度数より多い可能性を示唆します。ただし、行点と列点の近さそのものを関連の強さとして読むのではなく、標準化残差、セル寄与率、$\cos^2$ と照合して解釈します。例えば、Japanese（日本語母語話者）と Article errors（冠詞エラー）が同じ象限にあり、標準化残差とセル寄与率も大きければ、日本語母語話者に冠詞エラーが相対的に多いと解釈できます。

バイプロットの解釈に際しては、以下の点に注目します。

**第1次元（横軸）の解釈**: バイプロットの横軸は最も多くの慣性を説明する次元です。横軸の左端・右端に位置する L1 グループとエラータイプが、この次元を特徴づけています。例えば、Japanese と Article が右側に近接していれば、日本人学習者のエラープロファイルが冠詞エラーに偏っていることを示します。これは日本語に冠詞体系が存在しないという L1 転移の知見と整合します。

**第2次元（縦軸）の解釈**: 第2次元は第1次元では捉えきれない追加的な関連構造を表します。例えば、Spanish と Tense が第2次元の同じ方向にあれば、スペイン語話者は時制エラーにおいて他の L1 グループと異なるプロファイルを持つことを示します。

**原点付近の解釈**: 原点に近い L1 グループやエラータイプは「平均的なプロファイル」を持ち、特徴的な偏りがないことを意味します。Korean が原点付近にあれば、韓国語話者のエラー分布は全体平均に近いことを示します。

#### CA バイプロットの読み方
- **原点付近**: 平均的なプロファイルです。特徴がないことを意味します。
- **外側のポイント**: その軸への寄与（contribution）が大きく、特徴的。
- **行点と列点の近接**: 当該 L1 グループと当該エラータイプの対応を示唆します。ただし行点同士の距離は行プロファイル差、列点同士の距離は列プロファイル差として解釈できますが、**行点と列点間のユークリッド距離そのものはスケーリングに依存するため厳密に解釈しません**。
- **対角位置**: 原点を挟んで反対側に位置する行カテゴリと列カテゴリは、その組み合わせが期待より少ない可能性を示します。必ず標準化残差と照合します。
- **cos²（表現品質）**: 各点の cos² が高いほど、その次元での位置が信頼できます。cos² が低い点は、表示されている2次元平面では十分に表現されていません。

行点と列点を同じ図に重ねるバイプロットは便利ですが、行点と列点の距離を直接読みすぎる危険があります。そこで、行カテゴリだけ、列カテゴリだけを別々に表示し、それぞれの内部構造を確認します。行カテゴリ同士・列カテゴリ同士の距離は、同じ種類の点のプロファイル差として解釈しやすくなります。

```{r}
#| eval: true
#| label: ca-row-column-maps
#| fig-width: 11
#| fig-height: 5
#| fig-cap: "CA の行カテゴリ・列カテゴリ別マップ（Separate Row and Column Maps in CA）"

ca_row_map <- factoextra::fviz_ca_row(
  res_ca,
  col.row = "contrib",
  gradient.cols = c("#deebf7", "#9ecae1", "#08519c"),
  repel = TRUE
) +
  labs(title = "Row Map: L1 Backgrounds")

ca_col_map <- factoextra::fviz_ca_col(
  res_ca,
  col.col = "contrib",
  gradient.cols = c("#fee5d9", "#fcae91", "#a50f15"),
  repel = TRUE
) +
  labs(title = "Column Map: Error Types")

ca_row_map + ca_col_map
```

CA のバイプロットには複数のスケーリングがあります。対称マップ（symmetric map）は全体像を把握するのに便利ですが、行プロファイルを中心に読むなら row-principal、列プロファイルを中心に読むなら col-principal を使うと解釈の焦点が明確になります。

| スケーリング | 主に読む対象 | 実務での使い方 |
|---|---|---|
| Symmetric map | 行と列の全体像 | 探索的な第一図として使います |
| Row-principal map | 行カテゴリ間のプロファイル差 | L1 背景同士の違いを中心に読む場合に使います |
| Column-principal map | 列カテゴリ間のプロファイル差 | エラータイプ同士の違いを中心に読む場合に使います |

```{r}
#| eval: true
#| label: ca-scaling-comparison
#| fig-width: 11
#| fig-height: 5
#| fig-cap: "CA スケーリング比較（Row-principal and Column-principal Maps）"

ca_row_principal <- factoextra::fviz_ca_biplot(
  res_ca,
  map = "rowprincipal",
  repel = TRUE
) +
  labs(title = "Row-principal Map")

ca_col_principal <- factoextra::fviz_ca_biplot(
  res_ca,
  map = "colprincipal",
  repel = TRUE
) +
  labs(title = "Column-principal Map")

ca_row_principal + ca_col_principal
```

```{r}
#| eval: true
#| label: ca-eigenvalues

# Eigenvalues and inertia
ca_eig <- as_tibble(res_ca$eig) |>
  mutate(Dimension = row_number()) |>
  dplyr::select(Dimension, everything())

ca_eig |>
  knitr::kable(
    caption = "CA Eigenvalues and Inertia",
    digits = 3,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: ca-inertia-plot
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "CA の慣性と累積寄与率（Inertia and Cumulative Percentage in CA）"

ca_inertia_plot_df <- ca_eig |>
  dplyr::rename(
    Inertia = eigenvalue,
    Percent = `percentage of variance`,
    Cumulative = `cumulative percentage of variance`
  )

ggplot(ca_inertia_plot_df, aes(x = Dimension, y = Percent)) +
  geom_col(fill = "#2b8cbe", width = 0.65) +
  geom_line(aes(y = Cumulative, group = 1), color = "#b2182b", linewidth = 0.8) +
  geom_point(aes(y = Cumulative), color = "#b2182b", size = 2.5) +
  scale_x_continuous(breaks = ca_inertia_plot_df$Dimension) +
  scale_y_continuous(labels = scales::percent_format(scale = 1), limits = c(0, 100)) +
  labs(
    title = "Inertia Explained by CA Dimensions",
    subtitle = "Bars = percentage by dimension; line = cumulative percentage",
    x = "Dimension",
    y = "Percentage of inertia"
  )
```

固有値テーブルでは、各次元の慣性（eigenvalue）と累積寄与率（cumulative %）を確認します。第1・第2次元の累積寄与率が高いほど、2次元のバイプロットで全体構造を要約しやすくなります。ただし CA の慣性は表の大きさ、カテゴリ数、周辺度数に依存するため、PCA の説明率のように固定的な基準だけで判断しません。寄与率が低い場合でも、寄与率・$\cos^2$・標準化残差に支えられた局所的な対応関係は解釈可能です。

第1次元と第2次元の累積寄与率が 70% 前後以上であれば、2次元図の要約として読みやすいことが多いです。70% 未満の場合は第3次元以降の検討が必要であり、バイプロットだけで結論を出すべきではありません。ただし、この値は経験的な目安であり、絶対基準ではありません。

```{r}
#| eval: true
#| label: ca-cos2-contrib

# Row cos2 (quality of representation)
row_cos2 <- as_tibble(res_ca$row$cos2[, 1:2], rownames = "Category") |>
  rename(cos2_Dim1 = `Dim 1`, cos2_Dim2 = `Dim 2`)

row_cos2 |>
  knitr::kable(
    caption = "Row cos²",
    digits = 3,
    booktabs = TRUE
  )

row_contrib <- as_tibble(res_ca$row$contrib[, 1:2], rownames = "Category") |>
  rename(Contrib_Dim1 = `Dim 1`, Contrib_Dim2 = `Dim 2`)

row_contrib |>
  knitr::kable(
    caption = "Row Contributions",
    digits = 2,
    booktabs = TRUE
  )

# Column contributions
col_contrib <- as_tibble(res_ca$col$contrib[, 1:2], rownames = "Category") |>
  rename(Contrib_Dim1 = `Dim 1`, Contrib_Dim2 = `Dim 2`)

col_contrib |>
  knitr::kable(
    caption = "Column Contributions",
    digits = 2,
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: ca-contribution-barplot
#| fig-width: 9
#| fig-height: 6
#| fig-cap: "CA の行・列カテゴリ寄与率（Row and Column Contributions in CA）"

ca_contrib_long <- dplyr::bind_rows(
  row_contrib |>
    tidyr::pivot_longer(-Category, names_to = "Dimension", values_to = "Contribution") |>
    dplyr::mutate(Type = "L1 background"),
  col_contrib |>
    tidyr::pivot_longer(-Category, names_to = "Dimension", values_to = "Contribution") |>
    dplyr::mutate(Type = "Error type")
) |>
  dplyr::mutate(
    Dimension = stringr::str_replace(Dimension, "Contrib_Dim", "Dim "),
    Category = forcats::fct_reorder(Category, Contribution)
  )

ggplot(ca_contrib_long, aes(x = Contribution, y = Category, fill = Type)) +
  geom_col(width = 0.65) +
  facet_grid(Type ~ Dimension, scales = "free_y", space = "free_y") +
  labs(
    title = "Contributions to CA Dimensions",
    subtitle = "High-contribution categories define the meaning of each dimension",
    x = "Contribution (%)",
    y = NULL,
    fill = "Category type"
  ) +
  theme(legend.position = "bottom")
```

#### 寄与率と $\cos^2$ の違い（CA）
- **寄与率**: 各点が「その次元の形成」にどれだけ寄与したかを示します。行点内で合計 100%、列点内で合計 100% です。
- **$\cos^2$**: 各点が「その平面でどれだけ再現されているか」を示します。1 に近いほど信頼できます。
- 手順: 寄与率で軸の主要因を把握し、$\cos^2$ で信頼できる点のみ解釈します。

#### $\cos^2$ ヒートマップ

```{r}
#| eval: true
#| label: ca-cos2-heatmap
#| fig-width: 7
#| fig-height: 5

row_cos2_long <- as_tibble(res_ca$row$cos2[, 1:2], rownames = "Category") |>
  pivot_longer(cols = c(`Dim 1`, `Dim 2`), names_to = "Dim", values_to = "cos2")

ggplot(row_cos2_long, aes(Dim, Category, fill = cos2)) +
  geom_tile() +
  geom_text(aes(label = round(cos2, 3)), size = 3.5) +
  scale_fill_gradient(low = "white", high = "#2b8cbe") +
  labs(title = "Row cos² Heatmap", subtitle = "Quality of representation by L1 group", fill = "Cos2")
```

行カテゴリの $\cos^2$ ヒートマップは、各 L1 背景が 2 次元空間上でどの程度正確に表現されているかを示します。$\cos^2$ が高い（濃い色の）カテゴリは、バイプロット上の位置を信頼して解釈できます。$\cos^2$ が低いカテゴリは、第 3 次元以降に情報が分散しているため、バイプロットだけでは不十分な表現にとどまっています。

#### 列カテゴリの $\cos^2$ ヒートマップ

```{r}
#| eval: true
#| label: ca-col-cos2-heatmap
#| fig-width: 7
#| fig-height: 5

col_cos2_long <- as_tibble(res_ca$col$cos2[, 1:2], rownames = "ErrorType") |>
  pivot_longer(cols = c(`Dim 1`, `Dim 2`), names_to = "Dim", values_to = "cos2")

ggplot(col_cos2_long, aes(Dim, ErrorType, fill = cos2)) +
  geom_tile() +
  geom_text(aes(label = round(cos2, 3)), size = 3.5) +
  scale_fill_gradient(low = "white", high = "#e6550d") +
  labs(title = "Column cos² Heatmap", subtitle = "Quality of representation by error type", fill = "Cos2")
```

cos² が高い（濃い青の）カテゴリは、この 2 次元空間で十分に表現されており、バイプロット上の位置を信頼して解釈できます。cos² が低い（淡い色の）カテゴリは、表示されていない第3次元以降に情報が分散しているため、バイプロット上の位置だけでは不十分です。

#### CA バイプロット（tidyverse 版）

```{r}
#| eval: true
#| label: ca-biplot-tidyverse
#| fig-width: 9
#| fig-height: 7

row_coords <- as_tibble(res_ca$row$coord, rownames = "Category")
col_coords <- as_tibble(res_ca$col$coord, rownames = "Category")

ggplot() +
  geom_point(data = row_coords, aes(`Dim 1`, `Dim 2`), color = "blue", size = 4, shape = 17) +
  ggrepel::geom_text_repel(
    data = row_coords,
    aes(`Dim 1`, `Dim 2`, label = Category),
    color = "blue",
    family = "HiraginoSans-W3",
    size = 4,
    box.padding = 0.3,
    point.padding = 0.15,
    min.segment.length = 0,
    seed = 2024,
    show.legend = FALSE
  ) +
  geom_point(data = col_coords, aes(`Dim 1`, `Dim 2`), color = "red", size = 4, shape = 16) +
  ggrepel::geom_text_repel(
    data = col_coords,
    aes(`Dim 1`, `Dim 2`, label = Category),
    color = "red",
    family = "HiraginoSans-W3",
    size = 4,
    box.padding = 0.3,
    point.padding = 0.15,
    min.segment.length = 0,
    seed = 2025,
    show.legend = FALSE
  ) +
  geom_hline(yintercept = 0, linetype = "dashed", alpha = 0.5) +
  geom_vline(xintercept = 0, linetype = "dashed", alpha = 0.5) +
  labs(
    title = "CA Biplot (tidyverse)",
    subtitle = "Blue triangles: L1 groups; Red circles: Error types",
    x = paste0("Dim 1 (", round(res_ca$eig[1, 2], 1), "%)"),
    y = paste0("Dim 2 (", round(res_ca$eig[2, 2], 1), "%)")
  ) +
  theme(plot.title = element_text(face = "bold"))
```

ただし、バイプロット上の行点（L1背景）と列点（エラータイプ）の間の距離を直接的な類似度として解釈してはいけません。行点間の距離と列点間の距離はそれぞれ解釈可能ですが、行-列間の距離はカイ二乗距離の近似にすぎません。「近いから関連がある」という解釈は、同じ象限にあるかどうかを主な根拠とします（@greenacre2017）。

#### MCA（多重対応分析）の例

個票データ（複数のカテゴリカル変数）に対しては、MCA（多重対応分析）を用います。MCA は指標行列（ダミー展開: 各カテゴリを 0/1 の列に変換）に CA を適用したものと解釈できます。

MCA は CA を個票データに拡張した手法です。CA がクロス集計表（集約済みデータ）を入力とするのに対し、MCA は個人ごとの回答パターン（指示行列または Burt 行列）を直接分析します。これにより、各回答者を低次元空間に布置し、回答パターンの類似した個人がどのようにクラスタを形成するかを可視化できます。

言語研究における MCA の応用は多岐にわたります。例えば、学習者コーパス研究では**品詞タグ分布×習熟度レベル×L1背景**のような多元カテゴリカルデータを MCA で探索し、習熟度の異なる学習者がどのような言語パターンを示すかを一望できます。構文研究では、**関係節のタイプ（主格・目的格・所有格）×動詞の種類×学習者の L1**をコーディングし、L1 や構文タイプの組み合わせが多次元空間でどのように布置されるかを視覚化できます。@levshina2015 は構文文法やポリセミー研究での MCA の活用を詳しく論じており、応用言語学における標準的な参照文献です。また、言語テスト研究では、**受験者の解答パターン（正答・誤答・無回答）×問題特性（難易度・弁別力カテゴリ）**を MCA で分析することで、テスト項目と受験者集団の関係を同時に可視化できます。

SLA 研究では、学習者の背景変数（L1、学習環境、動機づけタイプ）が複数のカテゴリ変数として収集されることが多いです。MCA はこれらのカテゴリ変数間の関連パターンを探索的に把握するのに適しています。例えば、@levshina2015 は構文選択の規定因子を MCA で探索し、文脈変数と L1 背景が構文使用にどのように関連するかを可視化しました。

カテゴリの出現確率は日本の EFL 環境を反映した設定です。学習コンテキストは教室内が最多（50%）、自習が35%、海外が15%としました。

```{r}
#| eval: true
#| label: mca-data-simulation

set.seed(2024)
n_mca <- 200

context_mca <- sample(
  c("Classroom", "Self-study", "Abroad"),
  n_mca,
  replace = TRUE,
  prob = c(0.5, 0.35, 0.15)
)

df_mca <- tibble(
  Context = context_mca,
  Modality = purrr::map_chr(Context, \(ctx) {
    if (ctx == "Classroom") {
      sample(c("Reading", "Listening", "Speaking", "Writing"), 1,
             prob = c(0.35, 0.30, 0.20, 0.15))
    } else if (ctx == "Self-study") {
      sample(c("Reading", "Listening", "Speaking", "Writing"), 1,
             prob = c(0.45, 0.25, 0.10, 0.20))
    } else {
      sample(c("Reading", "Listening", "Speaking", "Writing"), 1,
             prob = c(0.15, 0.25, 0.45, 0.15))
    }
  }),
  Frequency = purrr::map_chr(Context, \(ctx) {
    if (ctx == "Classroom") {
      sample(c("Daily", "Weekly", "Monthly"), 1, prob = c(0.25, 0.60, 0.15))
    } else if (ctx == "Self-study") {
      sample(c("Daily", "Weekly", "Monthly"), 1, prob = c(0.35, 0.45, 0.20))
    } else {
      sample(c("Daily", "Weekly", "Monthly"), 1, prob = c(0.55, 0.35, 0.10))
    }
  }),
  Efficacy = purrr::map_chr(Context, \(ctx) {
    if (ctx == "Classroom") {
      sample(c("High", "Medium", "Low"), 1, prob = c(0.25, 0.55, 0.20))
    } else if (ctx == "Self-study") {
      sample(c("High", "Medium", "Low"), 1, prob = c(0.20, 0.50, 0.30))
    } else {
      sample(c("High", "Medium", "Low"), 1, prob = c(0.45, 0.40, 0.15))
    }
  })
)

mca_external <- tibble(
  ProficiencyGroup = purrr::map_chr(df_mca$Context, \(ctx) {
    if (ctx == "Abroad") {
      sample(c("Low", "Mid", "High"), 1, prob = c(0.15, 0.40, 0.45))
    } else if (ctx == "Self-study") {
      sample(c("Low", "Mid", "High"), 1, prob = c(0.30, 0.45, 0.25))
    } else {
      sample(c("Low", "Mid", "High"), 1, prob = c(0.35, 0.45, 0.20))
    }
  })
)
```

MCA に進む前に、各カテゴリの頻度を確認します。まれなカテゴリは座標が極端になりやすく、バイプロット全体を歪めることがあります。

```{r}
#| eval: true
#| label: mca-category-frequency

mca_category_frequency <- df_mca |>
  tidyr::pivot_longer(everything(), names_to = "Variable", values_to = "Category") |>
  dplyr::count(Variable, Category, name = "n") |>
  dplyr::group_by(Variable) |>
  dplyr::mutate(
    Proportion = n / sum(n),
    Rare = Proportion < .05
  ) |>
  dplyr::ungroup()

mca_category_frequency |>
  dplyr::mutate(Proportion = scales::percent(Proportion, accuracy = 0.1)) |>
  knitr::kable(
    caption = "MCA Category Frequencies",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: mca-category-frequency-plot
#| fig-width: 9
#| fig-height: 5
#| fig-cap: "MCA に入れるカテゴリ頻度（Category Frequencies before MCA）"

ggplot(mca_category_frequency, aes(x = forcats::fct_reorder(Category, Proportion), y = Proportion, fill = Rare)) +
  geom_col(width = 0.65) +
  geom_hline(yintercept = .05, linetype = "dashed", color = "grey45") +
  facet_wrap(~ Variable, scales = "free_x") +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +
  scale_fill_manual(values = c("FALSE" = "#2b8cbe", "TRUE" = "#b2182b")) +
  labs(
    title = "Category Frequencies for MCA",
    subtitle = "Dashed line marks 5%; rare categories can pull the MCA map",
    x = NULL,
    y = "Proportion",
    fill = "Rare"
  ) +
  theme(axis.text.x = element_text(angle = 35, hjust = 1), legend.position = "bottom")
```

MCA の内部では、各カテゴリが 0/1 のダミー列に展開されます。この完全指示行列（complete disjunctive table）を見ると、MCA が「各人がどのカテゴリを選んだか」という回答パターンを低次元に要約していることが分かります。

```{r}
#| eval: true
#| label: mca-indicator-matrix

mca_indicator <- FactoMineR::tab.disjonctif(df_mca) |>
  as.data.frame() |>
  tibble::as_tibble()

mca_indicator |>
  dplyr::slice_head(n = 8) |>
  knitr::kable(
    caption = "Complete Disjunctive Table for MCA (first rows)",
    booktabs = TRUE
  )
```

指示行列の列同士をクロス集計すると、カテゴリ同士の共起回数を並べた Burt 行列（Burt matrix）になります。MCA は、このようなカテゴリ共起の全体構造を低次元に要約していると理解できます。

```{r}
#| eval: true
#| label: mca-burt-matrix-heatmap
#| fig-width: 9
#| fig-height: 8
#| fig-cap: "MCA の Burt 行列ヒートマップ（Burt Matrix Heatmap for MCA Categories）"

mca_indicator_named <- mca_indicator
names(mca_indicator_named) <- names(mca_indicator_named) |>
  stringr::str_replace("^Context", "Context: ") |>
  stringr::str_replace("^Modality", "Modality: ") |>
  stringr::str_replace("^Frequency", "Frequency: ") |>
  stringr::str_replace("^Efficacy", "Efficacy: ")

mca_burt <- crossprod(as.matrix(mca_indicator_named))
mca_burt_df <- as.table(mca_burt) |>
  as.data.frame() |>
  dplyr::rename(Category1 = Var1, Category2 = Var2, Cooccurrence = Freq)

ggplot(mca_burt_df, aes(x = Category2, y = Category1, fill = Cooccurrence)) +
  geom_tile(color = "white") +
  scale_fill_gradient(low = "white", high = "#2b8cbe") +
  labs(
    title = "Burt Matrix of MCA Categories",
    subtitle = "Diagonal cells are category frequencies; off-diagonal cells are co-occurrences",
    x = NULL,
    y = NULL,
    fill = "Count"
  ) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
        axis.text.y = element_text(size = 8))
```

また、MCA は複数カテゴリ変数間の関連を可視化する手法なので、変数ペアごとの関連の強さも事前に確認しておくと解釈しやすくなります。ここでは Cramer's V を使います。

```{r}
#| eval: true
#| label: mca-cramers-v-heatmap
#| fig-width: 6
#| fig-height: 5
#| fig-cap: "MCA 変数間の Cramer's V（Pairwise Cramer's V before MCA）"

mca_vars <- names(df_mca)
mca_cramer_df <- tidyr::expand_grid(Var1 = mca_vars, Var2 = mca_vars) |>
  dplyr::mutate(
    CramersV = purrr::map2_dbl(Var1, Var2, \(x, y) {
      if (x == y) return(1)
      tbl <- table(df_mca[[x]], df_mca[[y]])
      chi <- suppressWarnings(stats::chisq.test(tbl, correct = FALSE))
      sqrt(unname(chi$statistic) / (sum(tbl) * (min(dim(tbl)) - 1)))
    })
  )

ggplot(mca_cramer_df, aes(x = Var1, y = Var2, fill = CramersV)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(CramersV, 2)), size = 3.5) +
  scale_fill_gradient(low = "white", high = "#2b8cbe", limits = c(0, 1)) +
  labs(
    title = "Pairwise Association among Categorical Variables",
    subtitle = "Cramer's V: 0 = no association, 1 = strong association",
    x = NULL,
    y = NULL,
    fill = "V"
  )
```

`FactoMineR::MCA()` で MCA を実行します。CA との違いは、入力がクロス集計表ではなく**個票データ**（各行が1人の学習者、各列がカテゴリカル変数）である点です。内部的にはダミー変数に展開してから CA を適用します。`graph = FALSE` で自動プロットを抑制します。バイプロットでは、個体（学習者）が点として、変数カテゴリ（例: "Classroom", "Daily", "High"）がラベルとして配置されます。

```{r}
#| eval: true
#| label: mca-analysis
#| fig-width: 10
#| fig-height: 8

# Perform MCA
res_mca <- FactoMineR::MCA(df_mca, graph = FALSE)

# Biplot
fviz_mca_biplot(res_mca, repel = TRUE,
                title = "MCA Biplot: Learner Strategy Questionnaire")
```

MCA バイプロットでは、点（個人）とラベル（カテゴリ）の位置関係が重要です。ある個人がカテゴリラベルの近くに位置する場合、そのカテゴリを含む回答パターンと近い傾向があると読みます。MCA は確率モデルではないため、「そのカテゴリを選択する確率が高い」と断定せず、$\cos^2$、寄与率、補助変数との関係を確認します。また、複数のカテゴリラベルが同じ方向に集まっている場合、それらのカテゴリが同時に現れやすい回答パターンを示唆します。

```{r}
#| eval: true
#| label: mca-eigenvalues

# MCA eigenvalues
mca_eig <- as_tibble(res_mca$eig) |>
  mutate(Dimension = row_number()) |>
  dplyr::select(Dimension, everything()) |>
  slice(1:6)

mca_eig |>
  knitr::kable(
    caption = "MCA Eigenvalues and Inertia",
    digits = 3,
    booktabs = TRUE
  )
```

MCA は CA よりも多くの次元を必要とする傾向があります。これは、指示行列のダミーコーディングにより固有値が分散するためです。@greenacre2017 は、平均以上の固有値のみを解釈対象とする修正基準を提案しています。

MCA の未修正の寄与率は低く見えやすいため、平均固有値（$1/Q$、$Q$ は変数数）を超える次元を確認し、必要に応じて Benzécri / Greenacre 型の修正慣性を併記します。ここでは、平均固有値を超える次元だけに修正を適用し、どの次元を解釈候補とするかを表にまとめます。

```{r}
#| eval: true
#| label: mca-corrected-inertia

q_mca <- ncol(df_mca)
mca_all_eig <- as_tibble(res_mca$eig) |>
  dplyr::mutate(Dimension = dplyr::row_number()) |>
  dplyr::select(Dimension, everything())

mca_corrected_inertia <- mca_all_eig |>
  dplyr::mutate(
    AverageEigenvalue = 1 / q_mca,
    AboveAverage = eigenvalue > AverageEigenvalue,
    CorrectedEigenvalue = dplyr::if_else(
      AboveAverage,
      (q_mca / (q_mca - 1))^2 * (eigenvalue - AverageEigenvalue)^2,
      0
    ),
    CorrectedPercent = 100 * CorrectedEigenvalue / sum(CorrectedEigenvalue),
    CorrectedCumulative = cumsum(CorrectedPercent)
  ) |>
  dplyr::slice_head(n = 8)

mca_corrected_inertia |>
  dplyr::select(
    Dimension,
    eigenvalue,
    `percentage of variance`,
    AverageEigenvalue,
    AboveAverage,
    CorrectedEigenvalue,
    CorrectedPercent,
    CorrectedCumulative
  ) |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "MCA Eigenvalues with Corrected Inertia",
    booktabs = TRUE
  )
```

```{r}
#| eval: true
#| label: mca-corrected-inertia-plot
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "MCA の未修正・修正寄与率（Raw and Corrected Inertia in MCA）"

mca_corrected_inertia |>
  dplyr::select(Dimension, Raw = `percentage of variance`, Corrected = CorrectedPercent) |>
  tidyr::pivot_longer(c(Raw, Corrected), names_to = "InertiaType", values_to = "Percent") |>
  ggplot(aes(x = factor(Dimension), y = Percent, fill = InertiaType)) +
  geom_col(position = "dodge", width = 0.72) +
  labs(
    title = "Raw vs. Corrected MCA Inertia",
    subtitle = "Corrected values emphasize dimensions above the average eigenvalue",
    x = "Dimension",
    y = "Percentage of inertia",
    fill = "Inertia"
  ) +
  theme(legend.position = "bottom")
```

修正慣性は、MCA の「低く見えがちな説明率」を補助的に読むためのものです。未修正の寄与率、修正寄与率、カテゴリの寄与率、$\cos^2$ を合わせて、どの次元を解釈するか判断します。

::: {.callout-note}
**Greenacre 修正慣性の定式化について**

ここで用いた `CorrectedPercent = 100 * CorrectedEigenvalue / sum(CorrectedEigenvalue)` は、平均以上の固有値のみを分母に使う簡便版で、`FactoMineR` や入門教科書で広く採用される表現です。Greenacre [@greenacre2007] のより厳密な「調整慣性」では、分母の総慣性に $\frac{Q}{Q-1}\left(\sum_{s}\lambda_s^2 - \frac{J-Q}{Q^2}\right)$（$J$ は全カテゴリ数、$Q$ は変数数）を用います。さらに厳密には**JCA (Joint Correspondence Analysis)** が Burt 行列の対角ブロック（自己一致セル）を反復的に再推定し、慣性の二重計上を完全に補正します。論文で「Greenacre 修正値」を報告する際は、簡便版か JCA かを明記してください（`ca::mjca(..., lambda = "JCA")` で JCA 版が得られます）。
:::

::: {.callout-note}
**MCA の発展: JCA・Subset MCA・confidence ellipses**

MCA をさらに進めた手法として、SLA で実用性が高いのは以下の 3 つです:

- **JCA (Joint Correspondence Analysis)** [@greenacre2007 Ch. 19]: Burt 行列の対角ブロック（自己一致セル）を反復推定で除外し、MCA 慣性の本質的過小評価問題を完全に補正。`ca::mjca(data, lambda = "JCA")` で実装可能で、論文で慣性%を厳密に報告する際の標準手法

- **Subset MCA / Specific MCA** (Greenacre & Pardo 2006; Le Roux & Rouanet 2010): 「無回答」「Don't know」「該当なし」カテゴリを **passive (消極的)** にして主軸計算から除外し、その他のカテゴリのみで慣性を再分配する手法。SLA の「学習者背景アンケートで未回答が多いカテゴリを抱える」状況で必須。`ca::mjca(data, supcol = ...)` または独自に Burt 行列を編集して実装

- **Confidence ellipses (bootstrap)**: `FactoMineR::plotellipses()` でカテゴリ座標の不確実性を可視化。Lebart (2007) の partial bootstrap が標準で、ベイズ CA と異なり頻度論的な信頼領域を提供します:

  ```r
  res_mca <- FactoMineR::MCA(mca_data, graph = FALSE)
  FactoMineR::plotellipses(res_mca, keepvar = "all")
  ```

論文では `factoextra::fviz_ca_biplot(..., ellipse.type = "confidence")` の出力が「個別観測値の信頼領域」ではなく「**群平均の SE 楕円**」である点に注意してください。両者は意味が異なります。
:::

::: {.callout-tip}
**MFA (Multiple Factor Analysis) — 複数質問群の統合分析**

調査票が複数のブロック（例: 「動機づけ Likert 質問群」+「学習方略カテゴリ群」+「習熟度連続変数」）から構成される場合、各ブロックを別々に PCA/MCA するのは情報量を失います。**MFA** [Pagès, J. (2014). *Multiple Factor Analysis by Example Using R*. Chapman & Hall] は、各ブロックを内部で標準化（最大固有値で正規化）してから統合 PCA/MCA を実行し、ブロック構造を保ったまま全体構造を抽出します。

```r
# 例: 3 ブロックからなる調査データ
res_mfa <- FactoMineR::MFA(
  combined_data,
  group = c(6, 5, 3),  # 各ブロックの変数数
  type = c("s", "n", "c"),  # s=連続標準化, n=カテゴリ, c=連続未標準化
  name.group = c("Motivation", "Strategy", "Proficiency")
)
```

応用言語学で複数の質問群を扱う調査研究では、MFA を一度試す価値があります。
:::

```{r}
#| eval: true
#| label: mca-variable-plot
#| fig-width: 9
#| fig-height: 7

# Variable categories plot colored by contribution
fviz_mca_var(res_mca, col.var = "contrib",
             gradient.cols = c("#00AFBB", "#E7B800", "#FC4E07"),
             repel = TRUE,
             title = "MCA Variable Categories (colored by contribution)")
```

カテゴリラベルの位置は、寄与率（contribution）と $\cos^2$ を合わせて読みます。寄与率が高いカテゴリは次元の意味づけに強く関わり、$\cos^2$ が高いカテゴリは表示されている平面上でよく表現されています。

```{r}
#| eval: true
#| label: mca-category-quality

mca_category_key <- purrr::imap_dfr(
  df_mca,
  \(x, variable_name) tibble::tibble(
    Variable = variable_name,
    Category = sort(unique(as.character(x)))
  )
)

mca_category_quality <- as_tibble(res_mca$var$coord[, 1:2], rownames = "Category") |>
  dplyr::rename(Dim1 = `Dim 1`, Dim2 = `Dim 2`) |>
  dplyr::mutate(
    Cos2_2D = rowSums(res_mca$var$cos2[, 1:2]),
    Contrib_2D = rowSums(res_mca$var$contrib[, 1:2])
  ) |>
  dplyr::left_join(mca_category_key, by = "Category") |>
  dplyr::arrange(dplyr::desc(Contrib_2D))

mca_category_quality |>
  dplyr::select(Variable, Category, Dim1, Dim2, Cos2_2D, Contrib_2D) |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  knitr::kable(
    caption = "MCA Category Coordinates, cos², and Contributions",
    booktabs = TRUE
  )
```

カテゴリ単位の寄与率に加えて、変数単位でどの次元に効いているかも確認します。`FactoMineR::MCA()` の `eta2` は、各カテゴリ変数が各次元をどの程度説明しているかを示す相関比（correlation ratio）です。カテゴリの細部ではなく、「Context という変数全体が第1次元に効いているのか」といった大局的な読み取りに使います。

```{r}
#| eval: true
#| label: mca-variable-eta2
#| fig-width: 8
#| fig-height: 5
#| fig-cap: "MCA 変数単位の eta²（Variable-level eta² in MCA）"

mca_eta2 <- as_tibble(res_mca$var$eta2[, 1:2], rownames = "Variable") |>
  tidyr::pivot_longer(-Variable, names_to = "Dimension", values_to = "Eta2") |>
  dplyr::mutate(Dimension = stringr::str_replace(Dimension, "Dim ", "Dim "))

ggplot(mca_eta2, aes(x = Eta2, y = forcats::fct_reorder(Variable, Eta2), fill = Dimension)) +
  geom_col(width = 0.65) +
  facet_wrap(~ Dimension, scales = "free_x") +
  scale_x_continuous(limits = c(0, 1)) +
  labs(
    title = "Variable-level Contribution to MCA Dimensions",
    subtitle = "eta² shows how strongly each categorical variable structures a dimension",
    x = expression(eta^2),
    y = NULL,
    fill = "Dimension"
  ) +
  theme(legend.position = "none")
```

```{r}
#| eval: true
#| label: mca-category-quality-plot
#| fig-width: 8
#| fig-height: 6
#| fig-cap: "MCA カテゴリの寄与率と表現品質（Category Contribution and cos² in MCA）"

ggplot(mca_category_quality, aes(x = Dim1, y = Dim2, color = Cos2_2D, size = Contrib_2D, label = Category)) +
  geom_hline(yintercept = 0, linetype = "dashed", color = "grey70") +
  geom_vline(xintercept = 0, linetype = "dashed", color = "grey70") +
  geom_point(alpha = 0.8) +
  ggrepel::geom_text_repel(
    family = "HiraginoSans-W3",
    size = 3.2,
    box.padding = 0.3,
    point.padding = 0.15,
    min.segment.length = 0,
    seed = 2024
  ) +
  scale_color_gradient(low = "grey75", high = "#b2182b") +
  scale_size_continuous(range = c(2, 7)) +
  labs(
    title = "MCA Category Diagnostics",
    subtitle = "Size = contribution to Dim 1-2; color = quality of representation",
    x = paste0("Dim 1 (", round(res_mca$eig[1, 2], 1), "%)"),
    y = paste0("Dim 2 (", round(res_mca$eig[2, 2], 1), "%)"),
    color = "cos²\nDim 1-2",
    size = "Contribution\nDim 1-2"
  )
```

#### MCA のポイント
- 個票データの各変数をダミー展開（指標行列）し、CA の枠組みで分解します。
- 次元数は CA より多く必要なことが多いです（カテゴリ数が増えるほど）。
- まれなカテゴリ（度数が全体の 5% 未満）は結果を不安定にするため、隣接カテゴリとの併合を検討します。
- Greenacre の修正慣性を用いると、次元の説明率を補助的に評価できます。

MCA のバイプロットでは、**同じ変数の異なるカテゴリが離れているほど、その変数が学習者のプロファイルを弁別する力が強い**ことを意味します。例えば、Context 変数の "Classroom" と "Abroad" が第1次元上で大きく離れていれば、学習コンテキストが学習者プロファイルの主要な差異化要因であることを示します。一方、同じ変数のカテゴリが近接していれば、その変数は学習者の弁別にあまり寄与していません。

色で表現された寄与率（contribution）は、各カテゴリが次元の慣性にどれだけ貢献しているかを示します。暖色（赤・黄）のカテゴリが次元を定義する主要な要素であり、寒色（青）のカテゴリは次元への寄与が小さいです。原点付近の低寄与カテゴリは、その次元では特徴的でないことを意味します。

なお、MCA は CA と異なり、累積慣性が低くなる傾向があります（変数×カテゴリ数の増加に伴い次元数が増えるためです）。累積寄与率が50%程度でも2次元の解釈は有用な場合が多く、修正慣性を用いると、より読みやすい寄与率の補助情報が得られます。

#### MCA 個体プロット（グループ別）

```{r}
#| eval: true
#| label: mca-individual-plot
#| fig-width: 10
#| fig-height: 8

# Individual plot colored by an external grouping variable
fviz_mca_ind(res_mca,
             col.ind = mca_external$ProficiencyGroup,
             palette = c("#00AFBB", "#E7B800", "#FC4E07"),
             addEllipses = TRUE,
             ellipse.type = "confidence",
             legend.title = "Proficiency Group",
             repel = TRUE,
             title = "MCA: Individuals by Supplementary Proficiency Group")
```

個人プロットでは、各回答者が MCA 空間上に点として布置されます。ここでは MCA 空間を作る能動変数とは別に用意した習熟度グループで色分けしています。楕円は習熟度グループごとの 95% 信頼楕円であり、楕円の分離は、回答パターンと外部属性が関連している可能性を示唆します。ただし、楕円の重なりだけで統計的検定を済ませるのではなく、補助変数としての投影、群別座標、追加のクロス集計やモデルで確認します。

#### 補助変数としての投入

MCA では、地図を作るために使う**能動変数**（active variables）と、地図ができた後に重ねて解釈する**補助変数**（supplementary variables）を区別できます。例えば、学習方略パターンで空間を作り、その後に習熟度群や学年を補助的に投影すれば、習熟度が空間形成そのものを支配することを避けられます。

```{r}
#| eval: false
#| label: mca-supplementary-variable-example

df_mca_sup <- df_mca |>
  dplyr::mutate(
    ProficiencyGroup = sample(c("Low", "Mid", "High"), n(), replace = TRUE)
  )

# ProficiencyGroup is column 5, so it is projected as supplementary.
res_mca_sup <- FactoMineR::MCA(df_mca_sup, quali.sup = 5, graph = FALSE)

factoextra::fviz_mca_biplot(
  res_mca_sup,
  repel = TRUE,
  title = "MCA with Proficiency Group as a Supplementary Variable"
)
```

研究報告では、どの変数を能動変数にしたか、どの変数を補助変数として投影したかを明記します。解釈したい外部属性をすべて能動変数に入れると、その属性が地図そのものを作ってしまうため、探索目的に応じて使い分けます。

#### MCA バイプロット（tidyverse 版）

```{r}
#| eval: true
#| label: mca-biplot-tidyverse
#| fig-width: 10
#| fig-height: 8

ind_coords_mca <- as_tibble(res_mca$ind$coord[, 1:2]) |>
  mutate(ProficiencyGroup = mca_external$ProficiencyGroup)

var_coords_mca <- as_tibble(res_mca$var$coord[, 1:2], rownames = "Category")

ggplot() +
  geom_point(data = ind_coords_mca,
             aes(`Dim 1`, `Dim 2`, color = ProficiencyGroup),
             alpha = 0.4, size = 2) +
  geom_point(data = var_coords_mca,
             aes(`Dim 1`, `Dim 2`),
             color = "black", size = 3, shape = 15) +
  ggrepel::geom_text_repel(
    data = var_coords_mca,
    aes(`Dim 1`, `Dim 2`, label = Category),
    family = "HiraginoSans-W3",
    size = 3,
    box.padding = 0.3,
    point.padding = 0.15,
    min.segment.length = 0,
    seed = 2024,
    show.legend = FALSE
  ) +
  geom_hline(yintercept = 0, linetype = "dashed", alpha = 0.5) +
  geom_vline(xintercept = 0, linetype = "dashed", alpha = 0.5) +
  labs(
    title = "MCA Biplot (tidyverse)",
    subtitle = "Points: individuals; Squares: variable categories",
    x = paste0("Dim 1 (", round(res_mca$eig[1, 2], 1), "%)"),
    y = paste0("Dim 2 (", round(res_mca$eig[2, 2], 1), "%)")
  ) +
  scale_color_manual(values = c("#00AFBB", "#E7B800", "#FC4E07")) +
  theme(plot.title = element_text(face = "bold"))
```

#### CA / MCA の報告テンプレート

CA と MCA の報告では、単に「2次元図を示した」と書くだけでは不十分です。入力データ、カイ二乗検定または関連診断、慣性、主要次元、寄与率の高いカテゴリ、低頻度カテゴリへの対応を明記します。

```{r}
#| eval: true
#| label: report-ca-mca
#| results: asis

ca_p_text <- ifelse(ca_chisq$p.value < .001, "< .001", paste0("= ", round(ca_chisq$p.value, 3)))
ca_dim12 <- round(sum(ca_eig$`percentage of variance`[1:2]), 1)
ca_top_cells <- ca_cell_contrib_df |>
  dplyr::arrange(dplyr::desc(Contribution)) |>
  dplyr::slice_head(n = 3) |>
  dplyr::mutate(Cell = paste0(L1Background, "-", ErrorType, " (", round(Contribution, 1), "%)")) |>
  dplyr::pull(Cell) |>
  paste(collapse = ", ")

mca_dim12_raw <- round(sum(mca_all_eig$`percentage of variance`[1:2]), 1)
mca_dim12_corrected <- round(sum(mca_corrected_inertia$CorrectedPercent[1:2], na.rm = TRUE), 1)
mca_top_variables <- mca_eta2 |>
  dplyr::group_by(Dimension) |>
  dplyr::slice_max(Eta2, n = 1, with_ties = FALSE) |>
  dplyr::ungroup() |>
  dplyr::mutate(Summary = paste0(Dimension, ": ", Variable, " (eta2 = ", round(Eta2, 2), ")")) |>
  dplyr::pull(Summary) |>
  paste(collapse = "; ")

ca_mca_report <- glue::glue(
  "For the correspondence analysis (CA), the contingency table of L1 background by error type showed ",
  "a significant association, χ²({unname(ca_chisq$parameter)}, N = {sum(ca_data)}) = {round(unname(ca_chisq$statistic), 2)}, ",
  "p {ca_p_text}. The total inertia was {round(ca_total_inertia, 3)}, and the first two dimensions accounted for ",
  "{ca_dim12}% of the inertia. The largest cell contributions were {ca_top_cells}. ",
  "For the multiple correspondence analysis (MCA), the first two dimensions accounted for {mca_dim12_raw}% of the raw inertia ",
  "({mca_dim12_corrected}% after corrected inertia). Variable-level eta-squared indicated the strongest structuring variables as follows: ",
  "{mca_top_variables}."
)

knitr::asis_output(ca_mca_report)
```

#### CA / MCA の解釈の注意
- 行点と列点の「ユークリッド距離」を直接解釈してはいけません。スケーリング（対称 / 非対称）に依存します。
- $\cos^2$ が低い点は、この平面上での表現が不十分であり、位置の解釈は控えめにします。
- 外れ的なカテゴリ（非常にまれな回答）が全体の構造を歪めることがある。外れ的なカテゴリ（低頻度カテゴリ）がバイプロットの構造を歪める場合は、低頻度カテゴリを隣接カテゴリに統合するか、補助変数として投影する（分析には含めず可視化のみ行う）方法がある。

#### 発展: Stan によるベイズ対応分析

コレスポンデンス分析を Stan でベイズ推定することは可能です。ただし、ここで重要なのは、`FactoMineR::CA()` が返す SVD に基づく記述的な座標をそのまま Stan で再現する、という意味ではありません。Stan では、クロス集計表のセル度数を確率モデルの観測値として扱い、行カテゴリと列カテゴリの低次元潜在座標を**生成モデル**として推定します。標準的な CA が「カイ二乗距離に基づく可視化」であるのに対し、ベイズ版は「度数がどのような確率過程から生じたか」を明示するモデルになります。

最も単純な発想は、セル度数 $y_{ij}$ をポアソン分布で表す対数線形・対数双線形モデルです。Stan ではポアソン分布の log parameterization（`poisson_log`）が用意されており、対数リンクを使う度数モデルを自然に書けます[@stan2025functions]。

$$
y_{ij} \sim \text{Poisson}(\lambda_{ij})
$$

$$
\log \lambda_{ij}
= \alpha + a_i + b_j + \mathbf{u}_i^\top \mathbf{v}_j
$$

ここで、$\alpha$ は全体平均、$a_i$ は行カテゴリの主効果、$b_j$ は列カテゴリの主効果です。$\mathbf{u}_i^\top \mathbf{v}_j$ が、行カテゴリと列カテゴリの**低次元の交互作用**を表します。$\mathbf{u}_i$ と $\mathbf{v}_j$ を2次元にすれば、通常の CA バイプロットに近い「行カテゴリ・列カテゴリの地図」を、事後分布つきで描けます。

| 目的 | 通常の CA / MCA | Stan によるベイズ版 |
|---|---|---|
| 探索的に全体構造を早く見たい | 適しています | 過剰です |
| 座標の不確実性を見たい | ブートストラップ等が必要です | 事後分布として扱えます |
| ゼロセル・疎な表を安定化したい | 結果が不安定になることがあります | 弱情報事前分布で正則化できます |
| 共変量・階層構造を入れたい | 標準 CA だけでは難しいです | 行・列効果や潜在座標に階層構造を入れられます |
| 予測・モデル比較をしたい | 主目的ではありません | 事後予測チェック、LOO、WAIC などを使えます |

MCA もベイズ的に拡張できます。個票データの複数カテゴリ回答を対象にする場合は、MCA を「カテゴリカル反応の低ランク構造」と見ることができます。@fithian2016mca は、MCA が多項ロジット双線形モデル（multilogit bilinear model）と深く関係することを示しています。したがって、Stan では、各回答者の潜在スコアと各カテゴリの負荷を使う多項ロジット型モデルとして MCA 的な構造を推定できます。ただし、これは標準 MCA よりも IRT や潜在特性モデルに近くなるため、研究目的が「カテゴリ配置の探索」なのか「回答生成過程の推定」なのかを先に決める必要があります。

##### Stan コードの注釈付き抜粋

以下は、CA の発展形として、クロス集計表を2次元のベイズ対数双線形モデルで表す Stan コードの骨格です。実務で使う場合は、識別制約、事後分布の回転・反転の整列、収束診断を必ず追加で確認します。

```stan
data {
  int<lower=1> R;                 // number of row categories
  int<lower=1> C;                 // number of column categories
  int<lower=1> K;                 // latent dimensions, often K = 2
  int<lower=0> N_total;           // total count
  array[R, C] int<lower=0> y;     // contingency table
}

parameters {
  real alpha;                     // grand log rate
  vector[R] row_raw;              // row main effects
  vector[C] col_raw;              // column main effects
  matrix[R, K] U_raw;             // row latent coordinates
  matrix[C, K] V_raw;             // column latent coordinates
  real<lower=0> sigma_row;
  real<lower=0> sigma_col;
  real<lower=0> sigma_latent;
}

transformed parameters {
  vector[R] row_eff = row_raw - mean(row_raw);
  vector[C] col_eff = col_raw - mean(col_raw);
  matrix[R, K] U;
  matrix[C, K] V;

  // Centering removes location arbitrariness, but not rotation/reflection.
  for (k in 1:K) {
    U[, k] = U_raw[, k] - mean(U_raw[, k]);
    V[, k] = V_raw[, k] - mean(V_raw[, k]);
  }
}

model {
  alpha ~ normal(log(1.0 * N_total / (R * C)), 2);
  sigma_row ~ exponential(1);
  sigma_col ~ exponential(1);
  sigma_latent ~ exponential(2);

  row_raw ~ normal(0, sigma_row);
  col_raw ~ normal(0, sigma_col);
  to_vector(U_raw) ~ normal(0, sigma_latent);
  to_vector(V_raw) ~ normal(0, sigma_latent);

  for (i in 1:R) {
    for (j in 1:C) {
      y[i, j] ~ poisson_log(
        alpha + row_eff[i] + col_eff[j] + dot_product(U[i], V[j])
      );
    }
  }
}

generated quantities {
  array[R, C] int y_rep;
  matrix[R, C] log_lambda;

  for (i in 1:R) {
    for (j in 1:C) {
      log_lambda[i, j] = alpha + row_eff[i] + col_eff[j] +
                         dot_product(U[i], V[j]);
      y_rep[i, j] = poisson_log_rng(log_lambda[i, j]);
    }
  }
}
```

このモデルでは、`row_eff` と `col_eff` が周辺度数の違いを吸収し、`U` と `V` の内積が「独立モデルでは説明できない対応関係」を表します。`generated quantities` ブロックで `y_rep` を生成しておくと、事後予測チェック（posterior predictive check）に使えます。Stan の公式リファレンスは、Stan が確率モデルの対数密度、推定アルゴリズム、事後分析を記述するための言語であることを明示しています[@stan2025reference]。

##### R から Stan を実行する例

以下は `cmdstanr` を使う場合の最小例です。`cmdstanr` と CmdStan 本体、C++ toolchain のセットアップが必要なので、通常の本章レンダリングでは実行しません。

```{r}
#| eval: false
#| label: bayes-ca-stan-fit

ca_stan_data <- list(
  R = nrow(ca_data),
  C = ncol(ca_data),
  K = 2,
  N_total = sum(ca_data),
  y = unname(ca_data)
)

bayes_ca_model <- cmdstanr::cmdstan_model("bayes_ca.stan")

bayes_ca_fit <- bayes_ca_model$sample(
  data = ca_stan_data,
  seed = 2024,
  chains = 4,
  parallel_chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000
)

bayes_ca_fit$summary(c("alpha", "sigma_row", "sigma_col", "sigma_latent"))
```

##### Stan 結果の可視化

Stan で推定した結果は、通常の CA 図と同じように点を置くだけでなく、**座標の不確実性**と**度数の再現性**を可視化します。最低限、次の3つを確認します。

| 可視化 | 目的 | 見るポイント |
|---|---|---|
| 事後平均バイプロット | 行・列カテゴリの配置を見る | 通常の CA と大局的に整合するか |
| 信用区間つき座標図 | 座標の不確実性を見る | 区間が広い低頻度カテゴリを過剰解釈していないか |
| 事後予測チェック | 度数表を再現できるかを見る | 観測度数と `y_rep` の分布が大きくずれていないか |

```{r}
#| eval: false
#| label: bayes-ca-posterior-coordinate-plot

row_coord_post <- tidybayes::spread_draws(
  bayes_ca_fit$draws(),
  U[row_id, dim_id]
) |>
  dplyr::filter(dim_id <= 2) |>
  dplyr::mutate(
    RowCategory = rownames(ca_data)[row_id],
    Dimension = paste0("Dim", dim_id)
  ) |>
  dplyr::select(.draw, RowCategory, Dimension, U) |>
  tidyr::pivot_wider(names_from = Dimension, values_from = U) |>
  dplyr::group_by(RowCategory) |>
  dplyr::summarise(
    Dim1_low = stats::quantile(Dim1, 0.025),
    Dim1_high = stats::quantile(Dim1, 0.975),
    Dim2_low = stats::quantile(Dim2, 0.025),
    Dim2_high = stats::quantile(Dim2, 0.975),
    Dim1 = mean(Dim1),
    Dim2 = mean(Dim2),
    .groups = "drop"
  )

ggplot(row_coord_post, aes(x = Dim1, y = Dim2, label = RowCategory)) +
  geom_segment(aes(x = Dim1_low, xend = Dim1_high, y = Dim2, yend = Dim2),
               color = "grey60") +
  geom_segment(aes(x = Dim1, xend = Dim1, y = Dim2_low, yend = Dim2_high),
               color = "grey60") +
  geom_point(size = 3, color = "#2b8cbe") +
  ggrepel::geom_text_repel(seed = 2024) +
  geom_hline(yintercept = 0, linetype = "dashed", color = "grey75") +
  geom_vline(xintercept = 0, linetype = "dashed", color = "grey75") +
  labs(
    title = "Bayesian CA Row Coordinates",
    subtitle = "Posterior means with 95% credible intervals",
    x = "Latent dimension 1",
    y = "Latent dimension 2"
  )
```

```{r}
#| eval: false
#| label: bayes-ca-posterior-predictive-check

y_obs <- as.vector(ca_data)
y_rep <- posterior::as_draws_matrix(bayes_ca_fit$draws("y_rep"))
cell_id <- seq_along(y_obs)
draw_id <- sample(seq_len(nrow(y_rep)), min(100, nrow(y_rep)))

bayesplot::ppc_intervals(
  y = y_obs,
  yrep = y_rep[draw_id, ],
  x = cell_id
) +
  labs(
    title = "Posterior Predictive Check for Bayesian CA",
    x = "Cell ID in the contingency table",
    y = "Observed and replicated counts"
  )
```

ベイズ版 CA の座標は、通常の CA と同様に、回転・反転・スケール変換に対して不定です。そのため、複数チェーンの `U` と `V` をそのまま平均すると、座標が打ち消し合うことがあります。実用上は、1つの基準解に Procrustes 回転で整列する、符号を固定する、または解釈したい距離・内積・再現度数を要約する、といった後処理が必要です。

結論として、CA/MCA はベイズ推定できます。ただし、標準 CA/MCA は探索的可視化として非常に軽量で透明性が高く、Stan 版は不確実性、疎データ、階層構造、予測を扱いたい場合の発展的選択肢です。授業や初期探索では標準 CA/MCA を使い、論文で「カテゴリ地図の不確実性」や「ゼロセルを含む疎な表の推定」を主張したい場合に Stan 版を検討するとよいです。

CA/MCA はカテゴリカルデータのカイ二乗距離に基づく可視化でした。次に学ぶ**多次元尺度法（MDS）**は、任意の距離・類似度行列を入力として低次元空間に配置する、より汎用的な次元縮約手法です。音声知覚の類似度判断や語彙の意味的距離など、SLA/心理言語学で「対象間の距離」がデータとして得られる場面で広く用いられます。

### 多次元尺度法（MDS）

::: {.callout-tip}
**1 分プレビュー (MDS)**

「英語母音 10 個の知覚類似度判断データから、**学習者の知覚空間の地図**を 2 次元に描きたい」という時に使います。入力は **距離行列** または **類似度行列**（PCA・CA とは異なる入力形式）。計量 MDS（cmdscale）は固有値分解で正確な距離を再現、非計量 MDS（smacof）は順位だけを保存し外れ値に頑健です。Stress 値（< 0.10 で良好）と Shepard 図で適合度を診断。SLA では母音知覚（Iverson 1995）、語彙意味距離、教材難易度地図などに広く使われます。**「対象間の距離」がデータとして得られる場面の標準ツール**。
:::

#### コンセプト

CA/MCA がカテゴリカル変数間の関連構造を可視化したのに対し、MDS は「距離」または「類似度」データを入力として、対象間の関係を低次元空間に再現します。入力データの形式が根本的に異なる点が MDS の特徴です。

多次元尺度法（MDS）は、対象間の距離や類似度を入力として、低次元の座標空間に配置する手法です[@borg2005]。PCA が共分散 / 相関構造を入力とするのに対し、MDS は**距離行列**を直接扱う点が特徴的です。心理言語学では、@iverson1995 が MDS を用いて母音知覚における「知覚マグネット効果」を可視化し、カテゴリプロトタイプ付近で知覚空間が圧縮される現象を示しました。また @foxflege1995 は、英語/スペイン語バイリンガルの母音知覚空間を MDS で再構成し、L2 習熟度に応じた知覚カテゴリの再編を実証しました。言語類型論では @croft2008 が MDS を不定代名詞の意味地図や時制・アスペクトの類型分析に適用し、言語普遍性の幾何学的推論に道を開きました。

MDS の座標は、回転・反転・平行移動しても距離関係が同じであれば同等の解です。そのため、軸の正負や原点の位置を過剰に解釈するのではなく、「どの対象が近いか」「どの対象が離れているか」「その配置が外部理論と整合するか」を中心に読み取ります。

#### 概要
1. 計量 MDS: 距離そのものを再現する（固有値ベース、Torgerson 1952; PCoA と同値）
2. 非計量 MDS: 距離の順位のみ保つ（外れ値やスケールに頑健、Shepard-Kruskal 法および SMACOF アルゴリズム）
3. ストレス（stress）で適合度を評価。Kruskal [@kruskal1964] の経験則では < 0.10 で良好とされるが、@sturrock2000 はサンプル数 $n$ や次元数に依存するため固定基準を機械的に当てはめないよう警告している（$n=10$ ではランダム配置でも 0.10 を切りうる）
4. 距離の選択（ユークリッド、マンハッタン、相関距離、Bray-Curtis、Gower など）が結果に大きく影響する
5. 非計量 MDS は局所最適に陥りやすいため、`init = "random"` で複数回起動し最小ストレス解を採用する（`smacof::mds(..., init = "random")` を 50–100 回、`vegan::metaMDS()` は自動で multi-start を実行）

SLA データに対して計量 MDS と非計量 MDS のどちらを選ぶかは入力データの性質によります。音響測定値（F1, F2 のヘルツ値）のように比率尺度のデータには計量 MDS が、類似度評定（Likert スケール）のように順序尺度のデータには非計量 MDS が適しています。

#### 計量 MDS vs 非計量 MDS

- **計量 MDS（classical MDS）**: 距離行列を**二重中心化**（double centering）し、固有値分解で座標を得ます[@torgerson1952]。二重中心化とは、距離の二乗行列 $\mathbf{D}^{(2)} = (d_{ij}^2)$ に対して

$$\mathbf{B} = -\tfrac{1}{2}\mathbf{J}\mathbf{D}^{(2)}\mathbf{J}, \qquad \mathbf{J} = \mathbf{I} - \tfrac{1}{n}\mathbf{1}\mathbf{1}^\top$$

を計算する操作で、$\mathbf{B}$ は中心化された内積行列（Gram 行列）になります。$\mathbf{B}$ を固有値分解 $\mathbf{B} = \mathbf{V}\boldsymbol{\Lambda}\mathbf{V}^\top$ し、上位 $k$ 個の正の固有値を取って座標 $\mathbf{X} = \mathbf{V}_k \boldsymbol{\Lambda}_k^{1/2}$ を得ます。入力がユークリッド距離であれば、計量 MDS は変数を中心化したデータの **PCA と数学的に同値**であり、また @gower1966 が示した **PCoA (Principal Coordinates Analysis)** とも同一です（cmdscale = PCoA = Torgerson scaling）。負の固有値が出現する場合は、入力距離が真にユークリッドでない（例: 順序データ由来）兆候で、Cailliez 補正 (`smacof::stress`) や Lingoes 補正で対処できます。
- **非計量 MDS（ordinal MDS）**: 距離の順位情報のみ保持します。反復最適化でストレスを最小化します[@kruskal1964]。R では `smacof` パッケージが代表的です[@deleeuw2009]。外れ値やスケールの不確かさに頑健です。

@foxflege1995 は、英語母語話者と非母語話者の母音知覚空間を MDS で比較し、L1 の音韻カテゴリが L2 知覚空間を歪めることを示しました。この知見は後続の Speech Learning Model（@flege1995）の実証的根拠となり、L2 音声習得研究における MDS の有用性を確立しました。

#### 距離の基本
- **ユークリッド距離**: $d(x, y) = \sqrt{\sum_j (x_j - y_j)^2}$。スケールの影響大 → 標準化推奨。
- **マンハッタン距離**: $\sum_j |x_j - y_j|$。外れ値にやや頑健。
- **相関距離**: $1 - r_{xy}$。方向の類似性重視。スケール差を無視したいときに有効。
- **マハラノビス距離**: $\sqrt{(x-y)^\top \Sigma^{-1} (x-y)}$。共分散構造を補正した距離。

マハラノビス距離はユークリッド距離と異なり、変数間の共分散構造を考慮します。例えば、F1 と F2 が強く相関する母音空間では、ユークリッド距離は相関の方向に沿った差を過大評価してしまいます。マハラノビス距離はこの歪みを補正し、変数間の相関を考慮した「標準化された」距離を返します。

#### SLA データ例: 英語母音の知覚類似度

日本人英語学習者が10個の英語母音ペアの「聞き取りやすさの類似度」を評定した非類似度行列をシミュレーションします。L2 母音知覚の MDS 研究は、@flege1999 のイタリア語話者による英語母音知覚・産出研究や、@foxflege1995 のスペイン語-英語バイリンガルの知覚空間分析に端を発します。これらの研究は、L1 の母音カテゴリ構造が L2 知覚空間をどのように制約するかを MDS で視覚的に示しました。@iverson1995 の知覚マグネット効果の研究も、MDS が音声知覚研究で果たす役割を象徴的に示しています。

母音の座標は IPA の母音チャートにおける F1（開口度）-F2（前後位置）空間をおおまかに反映した設定です。第1次元は前舌-後舌、第2次元は高さに対応し、/iː/ を高前舌、/ɑː/ を低後舌に配置しています。ノイズの標準偏差 $.30$ は、日本人学習者が L1 に存在しない母音対立（特に /ɪ/-/iː/）の弁別に困難を示すことを模擬するための設定です。

```{r}
#| eval: true
#| label: mds-data-simulation

set.seed(2024)
vowels <- c("/iː/", "/ɪ/", "/eɪ/", "/ɛ/", "/æ/",
            "/ɑː/", "/ɔː/", "/oʊ/", "/ʊ/", "/uː/")

# Front vowels cluster, back vowels cluster
coords_true <- matrix(c(
  2.0, 3.0,  # /iː/ (high front)
  1.8, 2.5,  # /ɪ/  (high front, lax)
  1.5, 2.0,  # /eɪ/ (mid front)
  1.2, 1.5,  # /ɛ/  (mid front, lax)
  0.8, 1.0,  # /æ/  (low front)
  0.5, 0.5,  # /ɑː/ (low back)
  -0.5, 1.0, # /ɔː/ (mid back)
  -1.0, 2.0, # /oʊ/ (mid back)
  -1.5, 2.8, # /ʊ/  (high back, lax)
  -1.8, 3.2  # /uː/ (high back)
), ncol = 2, byrow = TRUE)

# Add noise to true coordinates (preserves metric properties), then compute distances
coords_noisy <- coords_true + matrix(rnorm(20, sd = 0.15), 10, 2)
dist_mat_vowel <- as.matrix(dist(coords_noisy))
rownames(dist_mat_vowel) <- vowels
colnames(dist_mat_vowel) <- vowels

dist_obj <- as.dist(dist_mat_vowel)
```

距離行列は MDS の「生データ」です。MDS の結果を見る前に、どの対象ペアがもともと近く、どの対象ペアが遠いのかを確認しておくと、低次元配置の解釈が安定します。

```{r}
#| eval: true
#| label: mds-distance-heatmap
#| fig-width: 7
#| fig-height: 6

mds_dist_heatmap <- as.data.frame(as.table(dist_mat_vowel)) |>
  as_tibble() |>
  rename(Vowel1 = Var1, Vowel2 = Var2, Dissimilarity = Freq) |>
  mutate(
    Vowel1 = factor(Vowel1, levels = rev(vowels)),
    Vowel2 = factor(Vowel2, levels = vowels)
  )

ggplot(mds_dist_heatmap, aes(Vowel2, Vowel1, fill = Dissimilarity)) +
  geom_tile(color = "white", linewidth = 0.4) +
  geom_text(aes(label = scales::number(Dissimilarity, accuracy = 0.1)), size = 3) +
  scale_fill_gradient(low = "#f7fbff", high = "#08306b", name = "Dissimilarity\n(非類似度)") +
  coord_equal() +
  labs(
    title = "Input Dissimilarity Matrix for MDS",
    subtitle = "Dark cells indicate vowel pairs judged as more dissimilar",
    x = NULL, y = NULL
  ) +
  theme(
    plot.title = element_text(face = "bold"),
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
```

対角線は同一母音どうしの距離なので 0 です。対角線から離れたセルの濃淡を見ることで、/iː/-/ɪ/ のように近いペアと、/iː/-/ɑː/ のように遠いペアを、MDS 配置を見る前に確認できます。

#### 計量 MDS（Classical MDS）

```{r}
#| eval: true
#| label: mds-classical
#| fig-width: 8
#| fig-height: 7

# Classical MDS
fit_cmd <- cmdscale(dist_obj, k = 2, eig = TRUE)

cmd_df <- as_tibble(fit_cmd$points) |>
  setNames(c("Dim1", "Dim2")) |>
  mutate(Vowel = vowels)

# Eigenvalues
cmd_eig_df <- tibble(
  Dimension = seq_along(fit_cmd$eig),
  Eigenvalue = fit_cmd$eig
) |>
  slice(1:10)

cmd_eig_df |>
  knitr::kable(
    caption = "Classical MDS Eigenvalues",
    digits = 3,
    booktabs = TRUE
  )

# Visualization
ggplot(cmd_df, aes(Dim1, Dim2, label = Vowel)) +
  geom_point(size = 4, color = "#1f77b4") +
  ggrepel::geom_text_repel(
    family = "HiraginoSans-W3",
    size = 4,
    box.padding = 0.3,
    point.padding = 0.15,
    min.segment.length = 0,
    seed = 2024
  ) +
  labs(
    title = "Classical MDS: English Vowel Perceptual Space",
    subtitle = "2D representation of dissimilarity judgments by Japanese learners",
    x = "Dimension 1", y = "Dimension 2"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

2次元 MDS 空間の配置は、母音の音響空間（F1-F2 平面）とおおまかに対応する可能性があります。/iː/ と /ɪ/ が近接していれば、日本語母語話者にとって弁別が困難な母音ペアであることと整合します。ただし、MDS の軸そのものに固有の意味があるわけではないため、「第1軸 = 前舌-後舌」と決め打ちするのではなく、外部座標との相関、Procrustes 回転、音声学的知識との照合に基づいて解釈します。

```{r}
#| eval: true
#| label: mds-classical-fit
#| fig-width: 7
#| fig-height: 4.5

positive_eig <- fit_cmd$eig[fit_cmd$eig > 0]
cmd_fit_summary <- tibble(
  Indicator = c(
    "Positive eigenvalues",
    "Negative eigenvalues",
    "Smallest eigenvalue",
    "Variance captured by first 2 positive dimensions"
  ),
  Value = c(
    as.character(sum(fit_cmd$eig > 0)),
    as.character(sum(fit_cmd$eig < 0)),
    scales::number(min(fit_cmd$eig), accuracy = 0.001),
    scales::percent(sum(fit_cmd$eig[1:2]) / sum(positive_eig), accuracy = 0.1)
  )
)

cmd_fit_summary |>
  knitr::kable(
    caption = "Classical MDS fit diagnostics",
    booktabs = TRUE
  )

cmd_eig_df |>
  mutate(Sign = if_else(Eigenvalue >= 0, "Positive", "Negative")) |>
  ggplot(aes(Dimension, Eigenvalue, fill = Sign)) +
  geom_col(width = 0.7, color = "white") +
  geom_hline(yintercept = 0, linewidth = 0.4) +
  scale_fill_manual(values = c(Positive = "#2b8cbe", Negative = "#d95f0e")) +
  scale_x_continuous(breaks = seq_len(nrow(cmd_eig_df))) +
  labs(
    title = "Eigenvalue Diagnostics for Classical MDS",
    subtitle = "Negative eigenvalues indicate that the dissimilarities are not perfectly Euclidean",
    x = "Dimension", y = "Eigenvalue", fill = "Sign"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

Classical MDS では、負の固有値が多いほど、入力距離が完全なユークリッド距離として表現しにくいことを示します。負の固有値が小さく、2次元で正の固有値の大部分を説明できるなら、2次元解は記述的な可視化として扱いやすくなります。

#### Classical MDS の注意
- 負の固有値は非ユークリッド距離の兆候です。前処理のスケーリングや距離の選択を検討します。
- 2次元で分離が不十分なら3次元も候補です。解釈可能性とのトレードオフを考えます。

#### 非計量 MDS（Non-metric MDS）

非計量 MDS は距離の**順位のみ**を保存します。`smacof::mds()` の主要引数は次の通りです。

::: {.callout-note}
**Shepard-Kruskal 法と SMACOF アルゴリズム**

非計量 MDS の歴史的アルゴリズムは @shepard1962 と @kruskal1964 が提案した**Shepard-Kruskal 勾配法**で、観測非類似度 $\delta_{ij}$ から等張回帰で disparities $\hat{d}_{ij}$ を計算し、配置距離 $d_{ij}(\mathbf{X})$ との不一致を最小化します。代表的な Stress 指標は次の 3 つです:

$$\mathrm{Stress\text{-}1} = \sqrt{\frac{\sum_{i<j} (d_{ij}(\mathbf{X}) - \hat{d}_{ij})^2}{\sum_{i<j} d_{ij}(\mathbf{X})^2}}, \quad \mathrm{Stress\text{-}2} = \sqrt{\frac{\sum_{i<j} (d_{ij}(\mathbf{X}) - \hat{d}_{ij})^2}{\sum_{i<j} (d_{ij}(\mathbf{X}) - \bar{d})^2}}, \quad \mathrm{S\text{-}Stress} = \sqrt{\frac{\sum_{i<j} (d_{ij}^2 - \hat{d}_{ij}^2)^2}{\sum_{i<j} d_{ij}^4}}$$

Stress-1 が最も標準で `MASS::isoMDS()` `smacof::mds()` `vegan::monoMDS()` で採用されます。Stress-2 は分母が分散ベースで「無情報モデル」との比較を強調し、S-Stress は ALSCAL (Takane et al. 1977) で使われ大きな距離を強調する性質があります。

@deleeuw1977 が開発した **SMACOF** (Scaling by MAjorizing a COmplicated Function) は、ストレス関数を二次関数で**majorize** して反復的に最小化する方式で、Guttman 変換 $\mathbf{X}^{(t+1)} = \mathbf{V}^+ \mathbf{B}(\mathbf{X}^{(t)}) \mathbf{X}^{(t)}$ を反復します。利点は (a) **ストレスの単調減少が証明済み**（局所最適への収束は保証されるが大域最適は保証されない）、(b) 重み付け（個人差 INDSCAL）や制約 (Procrustes, restricted MDS) への拡張が容易、(c) Kruskal 勾配法より局所最適に陥りにくい、という点です。本節で使う `smacof::mds()` はこの SMACOF アルゴリズムを実装しています。

**主要 R 実装の比較**:

| 関数 | アルゴリズム | Stress 定義 | 特徴 |
|------|-------------|-------------|------|
| `cmdscale` | 二重中心化 + 固有値分解 | (該当なし、計量 MDS) | 高速、PCoA と同値 |
| `MASS::isoMDS` | Kruskal 勾配法 | Stress-1 | クラシック実装、単一 random start |
| `smacof::mds` | SMACOF (majorization) | Stress-1 | 重み付け・制約・INDSCAL 対応、`init = "random"` で multi-start |
| `vegan::monoMDS` | Kruskal 勾配法（改良版） | Stress-1 / Stress-2 | 生態学標準、tie 処理が柔軟 |
| `vegan::metaMDS` | `monoMDS` ラッパー | Stress-1 | **自動 multi-start**（既定 20 回）、Wisconsin double-standardization 自動適用 |

非計量 MDS は局所最適に陥りやすいため、論文用には `smacof::mds(..., init = "random")` を 50–100 回試す、または `vegan::metaMDS()` の自動 multi-start を使うのが標準です。
:::

::: {.callout-note}
**MDS の発展モデル: INDSCAL・Procrustes・Unfolding**

基本的な MDS の他に、SLA で使える 3 つの重要な拡張があります:

- **INDSCAL** (Individual Differences Scaling; Carroll & Chang 1970): **三相データ**（対象 × 対象 × 個人/群）を入力し、共通の対象配置と個人ごとの次元重み（saliences）を同時推定。「日本人 L1 群と中国語 L1 群で母音空間の **どの次元** が重視されるか」を比較できる。`smacof::indscal()` で実装

  ```r
  # 例: 3 群の母音 dissimilarity 行列を list として渡す
  fit_indscal <- smacof::indscal(list(dist_jp, dist_cn, dist_ko), ndim = 2)
  fit_indscal$cweights  # 各群の次元重み (どの次元を強調するか)
  ```

- **Procrustes 回転** (Schönemann 1966): MDS 解は回転・反射・スケーリングに対して不定なため、複数の MDS 解（例: ベイズ推定の事後ドロー、ブートストラップ反復、群比較）を **共通の参照配置に整列** させる必要がある。`vegan::procrustes()` または `smacof::Procrustes()` で実装

  ```r
  proc_result <- vegan::procrustes(target_config, source_config, scale = TRUE)
  plot(proc_result)  # 整列前後の差を可視化
  ```

- **Unfolding** (Coombs 1964; @borg2005 Ch. 14): **選好データ**（学習者 × 項目の好み・難易度ランキング）を「学習者と項目を同一空間に配置」する手法。学習者 $i$ と項目 $j$ の距離が小さいほど $i$ が $j$ を好む / 易しいと評価したと解釈。`smacof::unfolding()` で実装

  ```r
  # rect_data: 行 = 学習者、列 = 項目、値 = 選好/難易度ランク
  fit_unfolding <- smacof::unfolding(rect_data, ndim = 2)
  ```

これらは入門書では省略されがちですが、SLA で「学習者群間の知覚差」「事後不確実性の整列」「教材好み・難易度地図」を扱う際の標準ツールです。
:::

| 引数 | 設定値 | 意味 |
|------|--------|------|
| `type` | `"ordinal"` | **非計量 MDS**。距離の大小順序のみを保存します。知覚データのように距離の絶対値に意味がない場合に適切です |
| `type` | `"ratio"` | 計量 MDS。距離の比率関係を保存します。物理的距離データ向けです |
| `type` | `"interval"` | 距離の間隔関係を保存します。ratio と ordinal の中間です |
| `ndim` | `2`（既定） | 埋め込み次元数です。後述のストレス分析で最適次元を判断します |

```{r}
#| eval: true
#| label: mds-nonmetric
#| fig-width: 8
#| fig-height: 7

# Non-metric MDS using smacof
set.seed(2024)
fit_nmds <- smacof::mds(dist_obj, type = "ordinal")

mds_df <- as_tibble(fit_nmds$conf) |>
  setNames(c("Dim1", "Dim2")) |>
  mutate(Vowel = vowels)

# Visualization
ggplot(mds_df, aes(Dim1, Dim2, label = Vowel)) +
  geom_point(size = 4, color = "#d62728") +
  ggrepel::geom_text_repel(
    family = "HiraginoSans-W3",
    size = 4,
    box.padding = 0.3,
    point.padding = 0.15,
    min.segment.length = 0,
    seed = 2024
  ) +
  labs(
    title = "Non-metric MDS: English Vowel Perceptual Space",
    subtitle = paste("Stress =", round(fit_nmds$stress, 3)),
    x = "Dimension 1", y = "Dimension 2"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

図のサブタイトルにストレス値が表示されます。図中の母音配置を見ると、前舌母音（/iː/, /ɪ/, /eɪ/ 等）と後舌母音（/uː/, /ʊ/, /oʊ/ 等）が左右に分離し、高低（/iː/ vs /æ/）が上下に展開されることが期待されます。日本人学習者の知覚空間では /ɪ/-/iː/ の弁別が困難なため、これらの母音が近接する可能性があります。

計量 MDS と非計量 MDS は、同じ距離行列から似た配置を返すこともありますが、保存しようとしている情報が異なります。計量 MDS は距離の大きさを、非計量 MDS は距離の順位を重視します。次の図では両者を並べ、局所的な近接関係がどの程度一致しているかを確認します。

```{r}
#| eval: true
#| label: mds-method-comparison
#| fig-width: 11
#| fig-height: 5.5

mds_method_compare <- bind_rows(
  cmd_df |>
    mutate(
      Dim1 = as.numeric(scale(Dim1)),
      Dim2 = as.numeric(scale(Dim2)),
      Method = "Classical MDS\n(metric)"
    ),
  mds_df |>
    mutate(
      Dim1 = as.numeric(scale(Dim1)),
      Dim2 = as.numeric(scale(Dim2)),
      Method = "Non-metric MDS\n(ordinal)"
    )
)

ggplot(mds_method_compare, aes(Dim1, Dim2, label = Vowel)) +
  geom_hline(yintercept = 0, color = "grey85", linewidth = 0.3) +
  geom_vline(xintercept = 0, color = "grey85", linewidth = 0.3) +
  geom_point(size = 3, color = "#2b8cbe") +
  ggrepel::geom_text_repel(
    family = "HiraginoSans-W3",
    size = 3.5,
    box.padding = 0.25,
    point.padding = 0.1,
    min.segment.length = 0,
    seed = 2024
  ) +
  facet_wrap(~ Method, scales = "free") +
  labs(
    title = "Metric and Non-metric MDS Compared",
    subtitle = "Signs and rotations are arbitrary; compare neighborhoods rather than axis direction",
    x = "Scaled dimension 1", y = "Scaled dimension 2"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

非計量 MDS は距離の順序関係のみを保存するため、順序尺度の類似度データ（例: 「非常に似ている」〜「全く似ていない」の 7 件法評定）に適しています。計量 MDS が比率尺度の距離を仮定するのに対し、非計量 MDS はより緩い仮定で同様の空間配置を得られます。

#### ストレスの解釈

ストレス（stress）は MDS の適合度を示す中心的な指標であり、「元の距離行列と MDS による再構成距離との乖離度」を測定します。直感的には、**高次元の距離関係を低次元に押し込んだときの歪みの大きさ**です。ストレスが 0 に近いほど、低次元空間での配置が元の距離をよく再現しています。たとえば、10個の英語母音の知覚的類似度を2次元平面上に配置する場合、ストレスが低ければ、元の類似度判断（「/ɪ/ と /iː/ は近い」など）が平面上の距離で忠実に再現されていることを意味します。以下のテーブルでは Stress-1 と距離相関を表示します。

```{r}
#| eval: true
#| label: mds-stress-table

# GOF statistics
mds_dist <- dist(fit_nmds$conf)
dist_cor <- cor(as.vector(dist_obj), as.vector(mds_dist))
dist_spearman <- cor(as.vector(dist_obj), as.vector(mds_dist), method = "spearman")

mds_delta_mat <- as.matrix(fit_nmds$delta)
mds_pair_index <- which(lower.tri(mds_delta_mat), arr.ind = TRUE)
mds_pair_fit <- tibble(
  Vowel1 = rownames(mds_delta_mat)[mds_pair_index[, 1]],
  Vowel2 = colnames(mds_delta_mat)[mds_pair_index[, 2]],
  Observed = as.vector(fit_nmds$delta),
  Disparity = as.vector(fit_nmds$dhat),
  Fitted = as.vector(fit_nmds$confdist)
) |>
  mutate(
    Pair = paste(Vowel1, Vowel2, sep = " - "),
    Residual = Disparity - Fitted,
    AbsResidual = abs(Residual)
  )

tibble(
  Metric = c("Stress-1", "Pearson distance correlation", "Spearman distance correlation", "N objects", "N dimensions"),
  Value = c(fit_nmds$stress, dist_cor, dist_spearman, nrow(fit_nmds$conf), ncol(fit_nmds$conf))
) |>
  knitr::kable(
    caption = "Non-metric MDS Goodness of Fit",
    digits = 3,
    booktabs = TRUE
  )
```

#### Kruskal のストレス基準（Stress-1 の経験的目安）
| ストレス値 | 評価 |
|---|---|
| < 0.05 | Excellent |
| 0.05 -- 0.10 | Good |
| 0.10 -- 0.20 | Fair |
| > 0.20 | Poor |

ストレス値の解釈は対象数、次元数、距離の性質、Stress-1 か raw stress かに依存します。上の表は Kruskal Stress-1 の経験的目安であり、絶対基準ではありません。0.10 未満なら比較的読みやすく、0.10--0.20 では Shepard 図や局所残差をより慎重に確認します。

#### 次元数とストレスの関係

```{r}
#| eval: true
#| label: mds-stress-vs-dim
#| fig-width: 7
#| fig-height: 5

dims <- 2:5  # smacof::mds requires ndim >= 2
stress_tbl <- purrr::map_dfr(dims, function(d) {
  set.seed(2024 + d)
  fit <- smacof::mds(dist_obj, type = "ordinal", ndim = d)
  tibble::tibble(Dim = d, Stress = fit$stress)
})

ggplot(stress_tbl, aes(Dim, Stress)) +
  geom_line(linewidth = 0.8) +
  geom_point(size = 3) +
  geom_hline(yintercept = 0.10, linetype = "dashed", color = "red") +
  annotate("text", x = 4.5, y = 0.12, label = "Reference line", color = "red", size = 3.5) +
  labs(title = "Stress vs. Number of Dimensions", subtitle = "Look for the elbow")
```

ストレス-次元数プロットでは、次元数を増やすごとにストレスが減少します。肘の位置（ストレスの減少が緩やかになる点）が適切な次元数の目安となります。この図では 2 次元でストレスが大きく低下し、3 次元以降は改善が緩やかであることから、2 次元解が適正と判断できます。@borg2005 の基準（ストレス < 0.10 = 良好）と照合して、最終的な次元数を決定します。

#### シェパード図

```{r}
#| eval: true
#| label: mds-shepard
#| fig-width: 7
#| fig-height: 6

# Shepard diagram
plot(fit_nmds, plot.type = "Shepard", main = "Shepard Diagram: Vowel Dissimilarity")
```

シェパード図は、元の距離（横軸）と MDS 空間での距離（縦軸）の関係を示します。非計量 MDS では、元の距離そのものではなく、元の距離を単調変換した **disparity** と、MDS 空間内の再構成距離のズレを見ます。点が単調回帰線から大きく離れている場合、その対象ペアの距離が MDS 空間で十分に再現されていないことを示します。

```{r}
#| eval: true
#| label: mds-tidy-shepard
#| fig-width: 7
#| fig-height: 5.5

mds_shepard_tidy <- mds_pair_fit |>
  arrange(Observed)

ggplot(mds_shepard_tidy, aes(Observed, Fitted)) +
  geom_segment(
    aes(xend = Observed, yend = Disparity),
    color = "grey70",
    alpha = 0.6,
    linewidth = 0.4
  ) +
  geom_line(aes(y = Disparity), color = "#d95f0e", linewidth = 1) +
  geom_point(aes(size = AbsResidual), color = "#2b8cbe", alpha = 0.75) +
  scale_size_continuous(range = c(1.5, 5), name = "|Residual|") +
  labs(
    title = "Tidy Shepard Diagram for Non-metric MDS",
    subtitle = "Orange line = monotone disparities; vertical segments = pairwise residuals",
    x = "Observed dissimilarity",
    y = "Distance in MDS configuration"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

非計量 MDS のシェパード図では、45度線への近さだけを基準にしない点が重要です。順位尺度の類似度評定では、元の距離の絶対値よりも「近い順・遠い順」が安定していればよいからです。上図では、オレンジの単調回帰線に点が沿っているほど、順位関係がよく保存されています。

#### 局所的な再現誤差

全体のストレスが低くても、特定の母音ペアだけが低次元空間で再現しにくいことがあります。そこで、ペアごとの disparity と再構成距離の差を確認します。

```{r}
#| eval: true
#| label: mds-local-misfit
#| fig-width: 8
#| fig-height: 5.5

mds_top_misfit <- mds_pair_fit |>
  slice_max(AbsResidual, n = 10) |>
  arrange(desc(AbsResidual))

mds_top_misfit |>
  transmute(
    Pair,
    `Observed dissimilarity` = round(Observed, 3),
    Disparity = round(Disparity, 3),
    `Fitted distance` = round(Fitted, 3),
    Residual = round(Residual, 3)
  ) |>
  knitr::kable(
    caption = "Vowel pairs with the largest local MDS residuals",
    booktabs = TRUE
  )

mds_top_misfit |>
  mutate(
    Pair = forcats::fct_reorder(Pair, AbsResidual),
    Direction = if_else(Residual > 0, "Fitted too close", "Fitted too far")
  ) |>
  ggplot(aes(AbsResidual, Pair, fill = Direction)) +
  geom_col(width = 0.7) +
  scale_fill_manual(values = c("Fitted too close" = "#d95f0e", "Fitted too far" = "#2b8cbe")) +
  labs(
    title = "Largest Pairwise Residuals in Non-metric MDS",
    subtitle = "Pairs with large bars deserve closer substantive inspection",
    x = "|Disparity - fitted distance|",
    y = NULL,
    fill = "Residual direction"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

この診断は、MDS 図を「きれいに見えるか」だけで判断しないために有用です。大きな残差を示すペアは、評定のばらつき、L1 由来の同化、音響的近接性、または2次元では表現しきれない局所構造を示している可能性があります。

#### シェパード図の読み方
- **点が回帰線に近い**: 距離の再現が良好です（ストレスが低い）
- **外れ点**: 特定のペアの距離が再現困難です。外れ値や測定ノイズの可能性があります。
- **S字カーブ**: 非線形の距離変換が起きています。計量 MDS との比較を推奨します。

#### 距離ごとの埋め込み比較

異なる距離指標を用いると、MDS の結果は大きく変わりえます。以下は英語技能テストデータ（PCA 節で使用）について、3種類の距離で MDS を実行した比較です。

```{r}
#| eval: true
#| label: mds-distance-comparison
#| fig-width: 12
#| fig-height: 5

x_mds <- as.matrix(pca_data)

embeds <- list(
  Euclidean   = cmdscale(dist(x_mds, method = "euclidean"), k = 2),
  Manhattan   = cmdscale(dist(x_mds, method = "manhattan"), k = 2),
  Correlation = cmdscale(as.dist(1 - cor(t(x_mds))), k = 2)
)

emb_df <- purrr::imap_dfr(embeds, ~{
  as_tibble(.x) |>
    setNames(c("Dim1", "Dim2")) |>
    mutate(Method = .y)
})

ggplot(emb_df, aes(Dim1, Dim2)) +
  geom_point(alpha = 0.5, size = 1.5, color = "#1f77b4") +
  facet_wrap(~ Method, scales = "free") +
  labs(title = "MDS Embeddings by Distance Type", subtitle = "Same data, different distance metrics")
```

3 つの距離指標による MDS 配置を比較すると、同じ英語技能テストデータでも学習者間の近さの見え方が変わることが分かります。ユークリッド距離は各項目の差を二乗和で評価し、マンハッタン距離は絶対差の合計で評価するため外れ値の影響がやや緩やかです。相関距離は得点水準よりもプロフィールの形の類似性を重視します。どの距離が「正しい」かは自動的には決まらないため、研究上の問いと測定尺度に合わせて選び、必要に応じて感度分析として複数の距離を比較します。

#### マハラノビス距離による MDS

マハラノビス距離は、変数間の相関構造を考慮した距離指標です。相関の強い方向（実質的に同じ情報）が距離に過剰に効かないよう補正され、スケールの大きい変数も共分散で正規化されます。ただし、通常の標本共分散に基づくマハラノビス距離は外れ値に頑健ではなく、共分散行列が特異または悪条件の場合には不安定になります。必要に応じてロバスト共分散や正則化共分散を検討します。

$$d_M(\mathbf{x}, \mathbf{y}) = \sqrt{(\mathbf{x} - \mathbf{y})^\top \boldsymbol{\Sigma}^{-1} (\mathbf{x} - \mathbf{y})}$$

```{r}
#| eval: true
#| label: mds-mahalanobis
#| fig-width: 8
#| fig-height: 7

# Mahalanobis distance MDS on proficiency test data
x_mah <- as.matrix(pca_data)
mu_mah <- colMeans(x_mah)
S_mah  <- cov(x_mah)
eig_mah <- eigen(S_mah)
S_inv_sqrt <- eig_mah$vectors %*% diag(1 / sqrt(eig_mah$values)) %*% t(eig_mah$vectors)

# 白色化したデータでは Euclidean 距離が Mahalanobis 距離に対応する
x_wh <- scale(x_mah, center = mu_mah, scale = FALSE) %*% S_inv_sqrt
dist_mah <- dist(x_wh)

fit_cmd_mah <- cmdscale(dist_mah, k = 2, eig = TRUE)
cmd_mah_df <- as_tibble(fit_cmd_mah$points) |>
  setNames(c("Dim1", "Dim2")) |>
  mutate(id = row_number())

ggplot(cmd_mah_df, aes(Dim1, Dim2)) +
  geom_point(alpha = 0.5, size = 2, color = "#2ca02c") +
  labs(
    title = "Classical MDS (Mahalanobis Distance)",
    subtitle = "Whitened by covariance: adjusted for scale and correlation differences",
    x = "Dimension 1", y = "Dimension 2"
  ) +
  theme(plot.title = element_text(face = "bold"))
```

#### マハラノビス距離の直感
- 相関の強い方向は「実質的に同じ情報」なので、距離への寄与が抑えられます。
- スケールの大きい変数も共分散で正規化されるため、変数間の「公平な」距離になります。
- 多変量外れ値の検出にも利用できます（中心からのマハラノビス距離が大きい個体が外れ値候補です）。

#### MDS の解釈の注意
- 距離の選択で結果が大きく変わります。複数の距離で感度分析を行い、結果の頑健性を確認します。
- MDS の座標軸には PCA のような「固有ベクトル」の解釈がありません。軸の意味は研究者が外部情報から付与します。
- 外れ値が座標空間全体を引き伸ばすことがあります。事前に外れ値を確認します。

## 実務ガイド

### SEM を中心とした統一的な枠組み

本章で扱った手法のうち、EFA・CFA・SEM・LGM は**構造方程式モデリング（SEM）の一般的枠組みの中で統一的に理解**できます。以下では、これらの手法がどのような入れ子構造になっているかを整理します。

### まず確認する3つの問い

分析手法を選ぶ前に、次の3点を確認します。ここが曖昧なまま分析を始めると、PCA と EFA、EFA と CFA、CA と MDS のように、目的の異なる手法を取り違えやすくなります。

| 確認する問い | 判断のポイント | 典型的な手法 |
|---|---|---|
| データは何を表していますか | 連続得点、Likert 項目、カテゴリ、距離行列を区別します | PCA / EFA / CFA / CA / MDS |
| 目的は探索ですか、検証ですか | 構造を見つけたいのか、事前仮説を検証したいのかを分けます | EFA / CA / MCA は探索、CFA / SEM は検証 |
| 測定誤差を明示的に扱いますか | 合成得点で十分か、潜在変数モデルが必要かを判断します | 回帰 / PCA vs CFA / SEM |

この3つの問いに答えるだけでも、分析の入口での迷いはかなり減ります。特に「次元を少なくしたい」だけなら PCA、「背後の構成概念を推定したい」なら EFA/CFA、「カテゴリの対応関係を見たい」なら CA/MCA、「対象間の近さを地図化したい」なら MDS が基本になります。

### SEM の特殊ケースとしての各手法

```{mermaid}
%%| fig-width: 9
%%| label: fig-sem-hierarchy
%%| fig-cap: "SEM ファミリーの階層構造"
flowchart TD
    SEM["<b>SEM（一般モデル）</b><br/>測定モデル＋構造モデル<br/>lavaan::sem()"]
    CFA["<b>CFA</b><br/>測定モデルのみ<br/>構造パスなし<br/>lavaan::cfa()"]
    PATH["<b>パス解析</b><br/>構造モデルのみ<br/>観測変数のみ（=~ なし）<br/>lavaan::sem()"]
    LGM["<b>LGM</b><br/>時間を因子負荷で表現<br/>成長＝潜在変数<br/>lavaan::growth()"]
    REG["<b>回帰分析</b><br/>パス1本<br/>y ~ x1 + x2<br/>lm()"]
    MIMIC["<b>MIMIC モデル</b><br/>観測変数→潜在変数<br/>+ 潜在変数→指標"]
    MG["<b>多群 SEM</b><br/>group = 'var'<br/>測定不変性"]
    EFA["<b>EFA</b><br/>探索的：因子数・<br/>負荷パターン未指定<br/>psych::fa()"]

    SEM --> CFA
    SEM --> PATH
    SEM --> LGM
    SEM --> MIMIC
    SEM --> MG
    PATH --> REG
    EFA -.->|"構造を仮説化"| CFA
    CFA -.->|"構造パスを追加"| SEM

    style SEM fill:#2166ac,color:#fff,stroke:#333
    style CFA fill:#67a9cf,color:#fff
    style PATH fill:#67a9cf,color:#fff
    style LGM fill:#67a9cf,color:#fff
    style REG fill:#d1e5f0,color:#333
    style EFA fill:#fddbc7,color:#333
    style MIMIC fill:#67a9cf,color:#fff
    style MG fill:#67a9cf,color:#fff
```

上図が示すように、SEM は多様な分析手法を包含する一般的な枠組みです。各手法は SEM に特定の制約を加えたものとして位置づけられます。

| 手法 | SEM との関係 | 制約の内容 |
|------|------------|-----------|
| **CFA** | 構造パスなし | $\mathbf{B} = \mathbf{0}$, $\boldsymbol{\Gamma} = \mathbf{0}$（潜在変数間の回帰なし） |
| **パス解析** | 測定モデルなし | $\boldsymbol{\Lambda} = \mathbf{I}$（因子負荷 = 単位行列、観測変数 = 潜在変数） |
| **回帰分析** | 測定モデルなし + パス1本 | パス解析の最も単純なケース |
| **LGM** | 因子負荷を時間で固定 | $\boldsymbol{\Lambda}$ の値を 0, 1, 2, ... に固定し、成長を表現 |
| **MIMIC モデル** | 共変量が潜在変数を直接予測 | 観測変数→潜在因子の回帰パスを追加 |
| **多群 SEM** | 群間で等値制約を段階的に追加 | 測定不変性の検証（Configural → Metric → Scalar） |
| **EFA** | SEM の外（探索的） | 因子数・負荷パターンを事前に指定しません。CFA の仮説生成に使います |

この統一的理解により、「EFA で構造を探索 → CFA で構造を確認 → SEM で関係構造を検証 → LGM で縦断的変化を追跡」という段階的な分析の流れが、**同一の潜在変数モデリング枠組みの中で段階的に複雑さを増していく過程**であることが分かります。ただし、SEM は因果を自動的に証明する手法ではありません。因果解釈には、研究デザイン、時間順序、交絡の検討が必要です。

### 実践的な含意: 分析手法の選択

この枠組みを理解していると、研究の問いに応じた最適な手法選択が容易になります。

#### 研究の問いと手法選択

- **「この尺度はどんな因子構造を持つか？」** → EFA（仮説なし）→ CFA（仮説あり）
- **「動機づけと英語力の関係は？」**
  - 測定誤差を無視してよい場合 → **パス解析**（合成変数 + 回帰）
  - 測定誤差を考慮したい場合 → **SEM**（潜在変数レベル）
- **「男女でテスト項目の機能は同じか？」** → **多群 CFA**（測定不変性）
- **「語彙力は学期ごとにどう変化するか？」** → **LGM**
- **「学習者の背景変数が潜在能力に直接影響するか？」** → **MIMIC モデル**

いずれも lavaan で `cfa()`, `sem()`, `growth()` のいずれかの関数を呼ぶだけで推定できます。モデル構文（`=~`, `~`, `~~`）を変えるだけで、同一の枠組みの中で異なる問いに答えられるのが SEM の強みです。

### 手法比較ガイド

本章で扱った主な手法を総合的に比較します。

**手法比較ガイド**（入力・目的・性質・用途の比較）

| 観点 | PCA | EFA | CFA | SEM | LGM | LCA | CA/MCA | MDS |
|------|-----|-----|-----|-----|-----|-----|--------|-----|
| 入力データ | 連続変数 | 連続 / 順序 | 連続 / 順序 | 連続 / 順序 | 縦断（連続） | カテゴリカル指標 | カテゴリカル | 距離・類似度行列 |
| 目的 | 次元縮約・可視化 | 潜在因子の探索 | 測定仮説の検証 | 理論パス・媒介の検証 | 個人の成長軌跡 | 潜在クラス推定 | カテゴリ関係の可視化 | 距離の低次元可視化 |
| モデルの性質 | 記述的（変換） | 確率モデル | 確率モデル | 確率モデル | 確率モデル | 確率モデル（混合） | 記述的（SVD） | 記述的（最適化） |
| 適合度検定 | なし | χ² 適合度 | CFI/RMSEA 等 | CFI/RMSEA 等 | CFI/RMSEA 等 | AIC/BIC、エントロピー | なし | ストレス |
| SLA の典型用途 | テスト得点の要約 | 尺度開発・項目分析 | 妥当性検証 | 理論モデルの検証 | 語彙成長・視線計測 | 方略・不安・エラーのプロフィール | エラー分析・方略分析 | 知覚空間の分析 |
| R パッケージ | FactoMineR | psych | lavaan | lavaan | lavaan | poLCA | FactoMineR | smacof |

### 唯一の「最良の手法」はない
手法の選択は研究の問いに依存します[@bandalos2018]。構成概念の妥当性を検証したければ CFA / SEM[@innami2011]、カテゴリカル指標から未観測プロフィールを推定したければ LCA[@collins2010; @linzer2011]、カテゴリカルデータの関係を探索的に地図化したければ CA / MCA[@greenacre2017; @glynn2014]、距離データの可視化なら MDS[@borg2005] を選びます。縦断的な成長の個人差に関心があれば LGM が適切です[@bollen2006]。複数の手法を組み合わせることで、多角的な知見が得られます。

### 手法別の最小報告セット

論文・レポートでは、手法ごとに最低限示すべき図表が異なります。次の表をチェックリストとして使うと、分析結果が「図だけ」「数値だけ」に偏ることを避けられます。

| 手法 | 最低限示す図表 | 必須の診断・数値 | 解釈で避けること |
|---|---|---|---|
| PCA | スクリープロット、寄与率表、負荷量 / contribution 図 | 固有値、累積寄与率、$\cos^2$ | 主成分をそのまま潜在因子と呼ぶ |
| EFA | 平行分析、負荷量表、負荷量ヒートマップ | KMO、Bartlett、共通性、因子間相関、信頼性 | Kaiser 基準だけで因子数を決める |
| CFA | パス図、標準化負荷量表、残差ヒートマップ | CFI/TLI/RMSEA/SRMR、標準化負荷量、残差相関 | 適合度カットオフだけでモデルを採択する |
| SEM | 構造パス図、係数フォレストプロット | 測定モデルの適合、直接効果・間接効果、CI | 横断データだけで因果を断定する |
| LGM | スパゲッティプロット、予測軌跡、モデル比較表 | 切片・傾きの平均と分散、時点数、欠測処理 | 時点数不足で複雑な曲線成長を推定する |
| LCA | クラス数比較表、条件付き応答確率図、分類不確実性 | AIC/BIC、エントロピー、クラスサイズ、最大事後確率 | クラスを実在する固定群として扱いすぎる |
| CA / MCA | プロファイル表、バイプロット、寄与・$\cos^2$ 図 | カイ二乗、慣性、寄与率、カテゴリ頻度 | 行点と列点の距離を直接解釈する |
| MDS | 距離行列、MDS配置、Shepard図、ストレス曲線 | Stress-1、距離相関、局所残差、距離指標 | 軸の正負や回転方向を本質的意味として扱う |

### 手法の組み合わせ例

SLA 研究では、複数の手法を段階的に組み合わせることが多いです。以下に代表的な組み合わせを示します。

**例1: 尺度開発プロジェクト**

1. EFA で因子構造を探索（パイロットデータ）
2. CFA で構造を確認（本データ）
3. 信頼性を $\omega$ で報告
4. SEM で構成概念間の関係を検証

#### 尺度開発で同一データの EFA → CFA を避ける

潜在変数モデリングの標準手順は、データ収集後にサンプルを分割し、探索用データで EFA、検証用データで CFA を行う流れです。EFA で得た因子数と負荷パターンを CFA で確認した後、信頼性（\(\omega\) / \(\alpha\)）を評価します。次に SEM で構造パスを検証し、縦断データがある場合は LGM に拡張します。

同じデータで探索と確認を行うと、CFA が単なる「再現」になり、検定の意味が弱くなります。**サンプルを分割する**（例: 2/3 を EFA、1/3 を CFA）か、**独立した新規サンプル**を使うことが原則です。ただし、サンプルサイズが小さい研究で無理に分割すると、EFA も CFA も不安定になります。その場合は、同一データ内の CFA を「確認」ではなく「暫定モデルの評価」として報告し、独立サンプルでの再検証を今後の課題として明示します。

サンプルサイズが十分にある場合（400名以上）、ランダムに2つのサブサンプルに分割して分析する方法が一般的です。

```{r}
#| label: sample-split-procedure
#| eval: false

# Example: random split into exploration and confirmation samples
set.seed(2024)
n_total <- nrow(full_data)
idx_explore <- sample(n_total, size = floor(n_total * 0.6))
idx_confirm <- setdiff(seq_len(n_total), idx_explore)

data_explore <- full_data[idx_explore, ]
data_confirm <- full_data[idx_confirm, ]

# Step 1: EFA on exploration sample
fit_efa <- psych::fa(data_explore, nfactors = 3, fm = "ml", rotate = "promax")

# Step 2: CFA on confirmation sample based on EFA results
cfa_model <- '
  F1 =~ item1 + item2 + item3 + item4
  F2 =~ item5 + item6 + item7 + item8
  F3 =~ item9 + item10 + item11 + item12
'
fit_cfa <- lavaan::cfa(cfa_model, data = data_confirm, std.lv = TRUE)
```

サンプルサイズが限られている場合は、**異なる時期に収集したデータ**を探索用と確認用に分ける方法も有効です。また、@plonsky2015 が指摘するように、SLA 研究ではサンプルサイズの制約が大きいため、EFA の段階で理論的根拠を十分に吟味し、CFA への移行を慎重に行うことが重要です。

各段階で終了してよい範囲も研究目的によって異なります。尺度開発の初期段階なら EFA で因子構造を提案して終了してよい場合があります。妥当性検証が目的なら CFA + 信頼性まで、理論的パスや媒介仮説の検証が目的なら SEM まで進みます。群間比較を行う場合は、測定不変性の検証が必須です。

**例2: テスト分析プロジェクト**

1. PCA でテスト項目の次元構造を概観
2. EFA で因子構造を探索（必要に応じてポリコリック相関を使用）
3. CFA で測定モデルを検証
4. 測定不変性で群間比較の妥当性を確認

**例3: 縦断的成長プロジェクト**

1. CFA で各時点の測定モデルを確認（測定不変性の検証）
2. LGM で語彙力の成長軌跡をモデリング
3. 条件付き LGM で動機づけ・学習時間が成長率を予測するか検証
4. 第13章の ARIMA と組み合わせ、個人レベル（LGM）とクラスレベル（時系列）の変化を多角的に分析

**例4: 学習者プロファイル分析**

1. LCA で学習方略プロフィールを確率的に推定
2. MCA で学習者の方略パターンを地図として可視化
3. CA で L1 背景とエラータイプの関係を探索
4. MDS で学習者間の類似度を低次元空間に写像

### どの質問にどの手法？
- 「このテストは何を測っているのか？」→ EFA → CFA
- 「動機づけが英語力に影響するか？」→ SEM
- 「語彙力は学期ごとにどう伸びるか？個人差は？」→ LGM
- 「学習者には観測されない方略タイプがあるか？」→ LCA
- 「テスト項目を少なくまとめたい」→ PCA
- 「L1 別のエラーパターンの違いは？」→ CA
- 「学習者の知覚的近さを可視化したい」→ MDS

#### 分析計画に落とし込む例

実際の研究計画では、1つの手法だけを選ぶのではなく、「主分析」と「補助分析」を分けて書くと明確になります。

| 研究目的 | 主分析 | 補助分析 | 報告の焦点 |
|---|---|---|---|
| 動機づけ尺度を開発したい | EFA → CFA | 信頼性、測定不変性 | 因子数、負荷量、適合度、信頼性 |
| 学習者の成長を説明したい | LGM | 条件付き LGM、群別軌跡図 | 平均成長、個人差、共変量効果 |
| エラータイプと L1 背景を探索したい | CA | 標準化残差、カテゴリ併合の感度分析 | どのカテゴリが慣性に寄与したか |
| 方略プロフィールを分類的に見たい | LCA | MCA、分類不確実性診断 | クラス数、条件付き応答確率、事後所属確率 |
| 音声知覚の類似度を地図化したい | 非計量 MDS | 計量 MDS、距離指標の感度分析 | ストレス、Shepard図、局所残差 |

#### 実データリソース
本章ではシミュレーションデータを使用しましたが、実際の SLA 研究データで練習したい場合は以下のリソースが有用です。

- **IRIS** (Instruments for Research into Second Languages; <https://www.iris-database.org>): SLA 研究で使用された質問紙・テスト・コーパスデータが公開されています。EFA/CFA の練習には、動機づけ尺度や言語不安尺度のデータセットが適しています。
- **Open Science Framework (OSF)**: SLA 研究者が分析データを公開するプラットフォームです。「L2 motivation」「language aptitude」等で検索すると、因子分析や SEM に適したデータが見つかります。
- **CHILDES** (<https://childes.talkbank.org>): 言語習得コーパスです。MCA や CA の練習には、発話データのカテゴリカルコーディングが利用できます。

### 報告テンプレート（APA 形式）

研究論文での報告を支援するため、各手法の結果を APA 形式で記述するテンプレートを提供します。L2 量的研究全体の透明性原則は APA 第 7 版と *Language Learning* の Open Methods ガイドライン [@norris2015] (統計手続きの記述、効果量と CI、データ準備、再現に必要な詳細、可能ならデータ/コード公開) でカバーされます。一方、SEM/CFA に固有の **適合度指標の完全セット (CFI / TLI / RMSEA + 90% CI / SRMR / 標準化χ²)、モデル修正の根拠、測定不変性検定の段階別比較**は Norris et al. 2015 では明示的に規定されておらず、SEM 側の報告ガイドライン (例: Schreiber et al. 2006 *Journal of Educational Research* 99(6), 323–338; Boomsma 2000 *SEM* 7(3), 461–483; @kline2023 第 5 章) に従って書きます。以下のテンプレートは両者を統合した最小例です。

### EFA の報告テンプレート

```{r}
#| eval: true
#| label: report-efa
#| results: asis

# EFA reporting template
n_items_efa <- ncol(motivation)
n_factors_efa <- ncol(fit_efa$loadings)  # robust across psych versions ($factors slot is unreliable)
var_explained <- sum(fit_efa$Vaccounted["Proportion Var", ]) * 100

efa_report <- glue::glue(
  "An exploratory factor analysis with promax rotation (extraction: maximum likelihood) ",
  "was conducted on {n_items_efa} items measuring L2 motivation (N = {n_efa}). ",
  "Parallel analysis and scree plot inspection suggested {n_factors_efa} factors. ",
  "The {n_factors_efa}-factor solution explained {round(var_explained, 1)}% of the common variance. ",
  "Factor loadings ranged from {round(min(abs(efa_summary$Loading)), 2)} to ",
  "{round(max(abs(efa_summary$Loading)), 2)}. ",
  "Internal consistency (McDonald's omega) ranged from {round(min(efa_rel_tbl$`McDonald's ω`, na.rm = TRUE), 2)} ",
  "to {round(max(efa_rel_tbl$`McDonald's ω`, na.rm = TRUE), 2)} across the three factors."
)

knitr::asis_output(efa_report)
```

### CFA の報告テンプレート

```{r}
#| eval: true
#| label: report-cfa
#| results: asis

# CFA reporting template
fit_vals <- fitMeasures(fit_cfa_3f, c("chisq", "df", "pvalue", "cfi", "tli", "rmsea",
                                       "rmsea.ci.lower", "rmsea.ci.upper", "srmr"))

cfa_report <- glue::glue(
  "A confirmatory factor analysis was conducted using lavaan (Rosseel, 2012) to evaluate ",
  "the three-factor measurement model of L2 motivation (N = {n_cfa}; estimator = ML, std.lv = TRUE). ",
  "The model showed {ifelse(fit_vals['cfi'] > 0.95, 'good', 'adequate')} fit: ",
  "χ²({fit_vals['df']}, N = {n_cfa}) = {round(fit_vals['chisq'], 1)}, ",
  "p {ifelse(fit_vals['pvalue'] < .001, '< .001', paste0('= ', round(fit_vals['pvalue'], 3)))}, ",
  "CFI = {round(fit_vals['cfi'], 3)}, TLI = {round(fit_vals['tli'], 3)}, ",
  "RMSEA = {round(fit_vals['rmsea'], 3)} ",
  "[90% CI: {round(fit_vals['rmsea.ci.lower'], 3)}, {round(fit_vals['rmsea.ci.upper'], 3)}], ",
  "SRMR = {round(fit_vals['srmr'], 3)}."
)

knitr::asis_output(cfa_report)
```

### PCA の報告テンプレート

```{r}
#| eval: true
#| label: report-pca
#| results: asis

# PCA reporting template
n_comp_kg <- sum(res_pca$eig[, 1] > 1)
cum_var_kg <- round(res_pca$eig[n_comp_kg, 3], 1)

pca_report <- glue::glue(
  "A principal component analysis was conducted on {ncol(pca_data)} English proficiency test items ",
  "(N = {n_pca}). The correlation matrix was used for extraction. ",
  "The Kaiser-Guttman criterion identified {n_comp_kg} components with eigenvalues greater than 1 ",
  "(cumulative variance = {cum_var_kg}%), and this result was interpreted together with scree plot ",
  "inspection and parallel analysis. ",
  "The analysis was performed using FactoMineR (Le et al., 2008)."
)

knitr::asis_output(pca_report)
```

### SEM の報告テンプレート

```{r}
#| eval: true
#| label: report-sem
#| results: asis

# SEM reporting template — using bootstrap CI for indirect effects
sem_fit <- fitMeasures(fit_sem, c("chisq", "df", "pvalue", "cfi", "tli", "rmsea", "srmr"))
ind_boot <- parameterEstimates(fit_sem_boot, boot.ci.type = "perc") |>
  dplyr::filter(op == ":=", lhs == "ind_ideal")

sem_report <- glue::glue(
  "A structural equation model was estimated to test the mediation hypothesis that ",
  "Ideal L2 Self is associated with L2 proficiency both directly and indirectly through intended effort ",
  "(N = {n_sem}; estimator = MLR for the main model; bootstrap CIs estimated with ML). ",
  "The model fit was {ifelse(sem_fit['cfi'] > 0.90, 'acceptable', 'poor')}: ",
  "χ²({sem_fit['df']}, N = {n_sem}) = {round(sem_fit['chisq'], 1)}, ",
  "p {ifelse(sem_fit['pvalue'] < .001, '< .001', paste0('= ', round(sem_fit['pvalue'], 3)))}, ",
  "CFI = {round(sem_fit['cfi'], 3)}, TLI = {round(sem_fit['tli'], 3)}, ",
  "RMSEA = {round(sem_fit['rmsea'], 3)}, SRMR = {round(sem_fit['srmr'], 3)}. ",
  "The indirect effect of Ideal L2 Self on Proficiency through Effort was ",
  "{round(ind_boot$est, 3)}, 95% bootstrap CI [{round(ind_boot$ci.lower, 3)}, ",
  "{round(ind_boot$ci.upper, 3)}] (500 bootstrap samples for demonstration; ",
  "5,000+ recommended for publication, percentile method)."
)

knitr::asis_output(sem_report)
```

### MDS の報告テンプレート

```{r}
#| eval: true
#| label: report-mds
#| results: asis

# MDS reporting template
mds_worst_pair <- mds_top_misfit |>
  slice_max(AbsResidual, n = 1, with_ties = FALSE)

mds_report <- glue::glue(
  "A non-metric multidimensional scaling analysis was conducted using smacof to visualize ",
  "the perceived dissimilarities among 10 English vowels. A two-dimensional ordinal MDS solution ",
  "showed good fit, Stress-1 = {round(fit_nmds$stress, 3)}, with a distance correlation of ",
  "{round(dist_cor, 3)} (Spearman = {round(dist_spearman, 3)}) between the observed dissimilarities and reproduced configuration distances. ",
  "A Shepard diagram and pairwise residual diagnostics were inspected to evaluate local fit. ",
  "The largest local residual was observed for the pair {mds_worst_pair$Pair} ",
  "(|residual| = {round(mds_worst_pair$AbsResidual, 3)})."
)

knitr::asis_output(mds_report)
```

### 論文報告のコピペ用テンプレート

以下は各手法の報告文をそのまま論文に貼り付けられるテンプレートです。`X.XX` の部分を実際の数値に置き換えます。

**EFA（Method 最短版）**:

> An exploratory factor analysis with promax rotation (extraction: maximum likelihood) was conducted on 18 items measuring L2 motivation (*N* = 500). Parallel analysis suggested three factors.

**EFA（固定効果1予測子版）**:

> Factor loadings ranged from X.XX to X.XX. Items M01--M06 loaded on Factor 1 (Ideal L2 Self; loadings .XX--.XX), M07--M12 on Factor 2 (Ought-to L2 Self; .XX--.XX), and M13--M18 on Factor 3 (L2 Learning Experience; .XX--.XX). McDonald's $\omega$ ranged from .XX to .XX across the three factors.

**CFA（Method 最短版）**:

> A confirmatory factor analysis was conducted using lavaan [@rosseel2012] to evaluate the three-factor model (*N* = 400).

**CFA（適合度報告版）**:

> The three-factor model showed good fit: $\chi^2$(X) = X.X, CFI = .XXX, TLI = .XXX, RMSEA = .XXX [90% CI: .XXX, .XXX], SRMR = .XXX. All standardized factor loadings exceeded .50.

**SEM（媒介効果版）**:

> The indirect effect of Ideal L2 Self on Proficiency through Effort was X.XX (*SE* = X.XX, 95% bootstrap CI [X.XX, X.XX]), indicating significant mediation.

**LGM（Method 最短版）**:

> A latent growth curve model was estimated using lavaan to examine vocabulary growth across four semesters (*N* = 300, FIML for missing data).

**LGM（結果版）**:

> The mean intercept was X.XX (*SE* = X.XX, *p* < .001) and the mean slope was X.XX (*SE* = X.XX, *p* < .001), indicating significant growth. Slope variance was significant ($\hat{\sigma}^2$ = X.XX, *p* = .XXX), suggesting individual differences in growth rates. Motivation significantly predicted the slope (*B* = X.XX, *SE* = X.XX, *p* < .001).

**CA（カテゴリ対応版）**:

> A correspondence analysis was conducted on the cross-tabulation of X and Y. The chi-square test indicated a significant association, $\chi^2$(X) = X.XX, *p* = .XXX. The first two dimensions accounted for X.X% of the total inertia. Row and column contributions, standardized residuals, and cos² values were inspected to interpret the map.

**MCA（学習者プロフィール版）**:

> A multiple correspondence analysis was conducted to visualize learner profiles across X categorical variables. The first two dimensions accounted for X.X% of the corrected inertia. Category contributions and cos² values were used to identify the variables and categories most strongly represented in the two-dimensional solution.

**MDS（知覚類似度版）**:

> A non-metric multidimensional scaling analysis was conducted using smacof to visualize perceived dissimilarities among X stimuli. A two-dimensional solution showed Stress-1 = X.XXX. Shepard diagram inspection indicated that the ordinal distance structure was adequately reproduced. Pairwise residual diagnostics were used to identify stimulus pairs that were poorly represented in the two-dimensional configuration.

#### 日本語論文・卒論用テンプレート

**EFA**:

> L2 動機づけ18項目について、最尤法・promax 回転による探索的因子分析を行いました（*N* = 500）。平行分析とスクリープロットの確認から3因子解を採用しました。因子負荷量は .XX から .XX の範囲であり、McDonald の $\omega$ は .XX から .XX でした。

**CFA**:

> 3因子測定モデルの妥当性を確認するため、lavaan による確認的因子分析を行いました（*N* = 400, estimator = ML）。モデル適合は良好でした, $\chi^2$(df, *N* = 400) = X.X, *p* = .XXX, CFI = .XXX, TLI = .XXX, RMSEA = .XXX [90% CI: .XXX, .XXX], SRMR = .XXX。標準化因子負荷量はいずれも .50 を上回りました。

**SEM**:

> Ideal L2 Self が Effort を介して Proficiency と関連するという媒介仮説を、構造方程式モデリングにより検討しました（*N* = XXX, estimator = MLR）。間接効果は X.XX, 95% bootstrap CI [X.XX, X.XX] であり、信頼区間に 0 を含みませんでした。したがって、媒介仮説と整合的な間接関連が示されました。ただし、因果解釈には研究デザイン上の根拠が必要です。

**LGM**:

> 4時点の語彙得点について潜在成長曲線モデルを推定しました（*N* = 300）。切片平均は X.XX（*SE* = X.XX, *p* < .001）、傾き平均は X.XX（*SE* = X.XX, *p* < .001）であり、平均的な成長が確認されました。傾き分散も有意であり、成長率に個人差があることが示されました。

**CA / MCA**:

> L1 背景とエラータイプのクロス集計表に対してコレスポンデンス分析を行いました。カイ二乗検定は有意でした, $\chi^2$(df, *N* = XXX) = X.XX, *p* = .XXX。第1・第2次元は総慣性の X.X% を説明しました。解釈には、バイプロットに加えて寄与率、標準化残差、$\cos^2$ を用いました。

**MDS**:

> X 個の刺激間の非類似度に対して非計量 MDS を行いました。2次元解の Stress-1 は X.XXX であり、Shepard 図と局所残差の確認から、順位構造はおおむね再現されていました。軸の方向ではなく、刺激間の近接関係を中心に解釈しました。

**共通（R パッケージ引用）**:

> All analyses were conducted in R (version X.X.X) using psych (version X.X; Revelle, 2024), lavaan (version X.X; Rosseel, 2012), and FactoMineR (version X.X; L&ecirc; et al., 2008).

### 論文報告の確認事項

**EFA**: サンプルサイズと変数数 / 因子数決定の根拠（平行分析 + スクリー） / 回転法と推定法 / 因子負荷量表（交差負荷含む） / 共通性と独自性 / 信頼性（$\omega$ 推奨）

**CFA / SEM**: 適合度指標を複数（CFI, TLI, RMSEA + CI, SRMR） / 標準化解 / 修正指標の使用有無と根拠 / 媒介効果にはブートストラップ CI / R パッケージのバージョンと引用

**LGM**: 時点数・測定間隔・サンプルサイズ / 無条件モデルの切片・傾きの平均と分散 / モデル比較（無成長 vs 線形 vs 二次） / 条件付きモデルで共変量の効果（B, SE, p） / 適合度指標 / 欠損データの処理法（FIML 等）

**CA / MCA**: クロス集計表または個票データの形式 / カテゴリ頻度 / カイ二乗・慣性 / 寄与率 / $\cos^2$ / まれなカテゴリの扱い / スケーリングの指定

**MDS**: 距離・類似度の作成方法 / 計量・非計量の選択 / 次元数 / Stress-1 / Shepard図 / 距離指標の感度分析 / 局所残差の確認

#### 実務上のアドバイス: 分析ノートの構成

次元削減や潜在変数モデルの分析結果を共有する際は、以下の順でノート化しておくと再現性と可読性が高まります。

1. **前処理**: 標準化・欠損処理・カテゴリ結合の妥当性を記録
2. **推定**: パラメータ設定（推定法、回転法、因子数決定の根拠）
3. **可視化**: 散布図・ヒートマップ・パス図で結果を視覚的に確認
4. **要約表**: `gt` パッケージで因子負荷量表、適合度指標表を作成
5. **解釈メモ**: 各因子 / 成分の意味、理論との整合性を記述
6. **報告テンプレート**: APA 形式の記述文を自動生成

この順序に沿って分析ノートを構成すると、共同研究者へのフィードバックや論文執筆がスムーズになります。

#### 再現性のための最終チェック

分析が終わったら、以下を確認します。統計的な結論そのものだけでなく、後から同じ結果を再現できるかどうかが重要です。

| チェック項目 | 確認内容 |
|---|---|
| データ処理 | 除外基準、欠損処理、標準化、カテゴリ併合を明記しましたか |
| モデル指定 | 因子数、回転法、推定法、距離指標、次元数を明記しましたか |
| 図表 | 主図だけでなく、診断図（残差、$\cos^2$、Shepard図など）を確認しましたか |
| 感度分析 | 因子数、回転法、距離指標、カテゴリ併合などの代替仕様を確認しましたか |
| 報告 | 数値、図、解釈、限界を同じストーリーとして説明できていますか |

#### 解釈上の注意点（総まとめ）
- **PCA**: 2次元で十分とは限りません。累積寄与率と $\cos^2$ を確認します。
- **EFA**: 因子数の過少・過多に注意します。平行分析を第一基準にし、同じデータで CFA に進まないようにします。
- **CFA / SEM**: 適合度指標の「カットオフ」は目安であり絶対基準ではありません。理論との整合性を重視します。
- **LGM**: 時点数が少ないと推定が不安定です。3時点では線形モデルが中心で、曲線成長には5時点以上が望ましいです。
- **CA / MCA**: 行点と列点の距離を直接解釈しません（スケーリング依存）。$\cos^2$ が低い点は控えめに解釈します。
- **MDS**: 距離の選択で結果が大きく変わります。感度分析で妥当性を点検します。
- **すべて**: まれなカテゴリや外れ値が図を歪めます。併合・ロバスト化を検討します。

### トラブルシューティング

分析中に頻出するエラーや診断上の問題を、症状・原因・修正の構造で整理します。まず、代表的な問題を早見表で確認してから、各項目の詳細に進みます。

| 症状 | 主な原因 | 最初に確認すること | 優先する対応 |
|---|---|---|---|
| Heywood ケース | 因子あたりの項目不足、過度に強い負荷、モデル誤特定 | 標準化負荷量、残差分散、因子間相関 | モデル再指定、項目数の確保、因子構造の再検討 |
| 収束失敗 | 複雑すぎるモデル、識別不能、開始値の問題 | 自由パラメータ数、推定警告、`lavInspect()` | モデルを簡素化し、推定設定を確認 |
| EFA の交差負荷 | 因子数不適合、項目内容の重複 | 因子数 ±1、共通性、項目文 | 項目修正・除外、回転法の感度分析 |
| CFA の不良適合 | 測定モデルの誤指定、局所依存 | 残差相関、修正指標、負荷量 | 理論に基づくモデル修正、独立サンプルで再確認 |
| LGM の傾き分散非有意 | 成長率の個人差が小さい、時点数不足 | 傾き分散、測定間隔、信頼性 | 共変量追加を急がず、モデル目的を見直す |
| CA / MCA でカテゴリが極端 | まれなカテゴリ、ゼロセル、カテゴリ定義の細かすぎ | 頻度表、標準化残差、寄与率 | カテゴリ併合、補助カテゴリ化、感度分析 |
| MDS のストレスが高い | 次元数不足、距離指標不適合、外れ対象 | Stress vs 次元数、Shepard図、局所残差 | 次元数増加、距離指標変更、非計量MDS |

#### Heywood ケース（CFA / SEM）

**症状**: `lavaan WARNING: some estimated ov variances are negative` と表示される。または標準化因子負荷量が 1.0 を超える。

**原因**: 指標変数の数が少ない（1因子あたり2項目以下）、サンプルサイズが不十分（< 150）、モデルの誤特定（本来2因子を1因子にまとめている等）が考えられます。

**修正**:

| 対応策 | 具体的操作 |
|---|---|
| 問題の分散に下限制約 | モデル式に `M01 ~~ a*M01; a > 0.001` を追加 |
| 指標を増やす | 1因子あたり最低3項目を確保 |
| サンプルを増やす | 200名以上を目標 |
| モデルを見直す | EFA に戻り、因子構造を再検討 |

#### 収束失敗（SEM / LGM）

**症状**: `WARNING: the model has NOT converged` または `WARNING: the optimizer warns that a solution has NOT been found`.

**原因**: 開始値が不適切、モデルが過度に複雑（推定パラメータ数に対してサンプルが少ない）、識別不能なモデルになっている、などが考えられます。

**修正**:

| 対応策 | 具体的操作 |
|---|---|
| 最適化手法の変更 | `optim.method = "nlminb"` を試す |
| 反復回数の増加 | `control = list(iter.max = 10000)` |
| モデルの簡素化 | 不要なパスや自由パラメータを削除 |
| 開始値の指定 | `start = "simple"` または手動指定 |
| `lavaan::lavInspect(fit, "post.check")` で健全性を確認 | |

#### EFA で交差負荷が多い

**症状**: 多くの項目（4項目以上）が複数の因子に .30 以上の負荷を示します。単純構造が得られません。

**原因**: 因子数が不適切（多すぎるまたは少なすぎる）、項目の弁別性が低い（似た内容を聞いている）、回転法の選択が不適切、などが考えられます。

**修正**:

| 対応策 | 具体的操作 |
|---|---|
| 因子数を変える | 1つ多く / 1つ少なく推定して比較 |
| 回転法を変える | promax → oblimin、または直交回転も試す |
| 問題項目の除外 | 交差負荷が大きく理論的に曖昧な項目を除外して再推定 |
| 項目内容の見直し | 質問文の重複・曖昧さを検討（次回調査への改善） |

#### Kaiser 基準と平行分析の不一致

**症状**: Kaiser 基準では5因子だが、平行分析では3因子を推奨します。

**原因**: Kaiser 基準（固有値 > 1）は因子数を過大評価する傾向があります。特に項目数が多い場合（20項目以上）に顕著です。

**修正**: 平行分析の結果を第一に参照します。理論的予測と照合し、因子数 ± 1 の解も比較して、解釈可能性と統計的基準を総合判断します。Kaiser 基準は参考情報として報告するにとどめます。

#### CFA の適合度が不良（CFI < .90）

**症状**: CFI, TLI がいずれも .90 を下回り、RMSEA が .10 を超える。全ての適合度指標が基準未達。

**原因**: EFA で得た因子構造がこのデータに当てはまらない、重要な交差負荷を無視している、項目の質に問題がある、などが考えられます。

**修正**:

| 対応策 | 具体的操作 |
|---|---|
| EFA に戻る | 検証データで再度 EFA を行い、構造を確認 |
| 修正指標の確認 | `modindices(fit)` で MI > 10 のパスを確認（理論的根拠がある場合のみ追加） |
| 項目の見直し | 残差共分散が大きい項目ペアを検討 |
| 代替モデル | 2因子モデルや階層モデル（二次因子）を検討 |

#### LGM で傾きの分散が非有意

**症状**: 傾き因子の分散推定値の $p > .05$。「成長率に個人差がある」とは言えない。

**原因**: 実際に個人差が小さい、時点数が少なすぎる（3時点は線形モデルの最小限）、測定誤差が大きい、などが考えられます。

**修正**: 傾きの分散が有意でなければ、共変量を追加する意味は薄くなります（予測すべき個人差がないためです）。時点数を増やす（5時点以上が理想）、測定の信頼性を改善する、または「成長率に有意な個人差は見られなかった」と報告します。

#### MDS のストレスが高い（> .20）

**症状**: ストレス値が .20 を超え、シェパード図で点が回帰線から大きく散逸します。

**原因**: 2次元では距離構造を再現できない、外れ値が全体を引き伸ばしている、距離指標の選択が不適切、などが考えられます。

**修正**:

| 対応策 | 具体的操作 |
|---|---|
| 次元数の増加 | 3次元で試します（ストレス vs 次元数プロットの肘を確認） |
| 外れ値の除去 | 極端に大きい距離を持つ対象を一時的に除外 |
| 距離の変更 | ユークリッド → 相関距離やマンハッタン距離を試す |
| 非計量 MDS | 計量 MDS で上手くいかない場合、順位情報のみ保持する非計量 MDS を使う |

#### CA でまれなカテゴリが結果を支配する

**症状**: バイプロットで1つのカテゴリだけが極端に外側に配置され、他のカテゴリが原点付近に圧縮される。

**原因**: 頻度が全体の 5% 未満のまれなカテゴリが、少数の観測値に引き寄せられて極端な座標を取ることがあります。

**修正**: 当該カテゴリを隣接する意味的に近いカテゴリと併合します。例: "Strongly Disagree"（n = 8）と "Disagree"（n = 45）を "Disagree" に統合します。併合前後の慣性と解釈の変化を比較し、妥当性を確認します。

**ここまでお疲れさまでした**。

## 参考文献

::: {#refs}
:::

## セッション情報

```{r}
#| eval: true
#| label: session-info
sessionInfo()
```
