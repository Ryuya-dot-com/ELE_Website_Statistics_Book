{
  "hash": "e89a6f27709950da85fdc5ca5b69433c",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"予測分析：ROC曲線とツリーモデル\"\nprefer-html: true\nalways_allow_html: true\nbibliography: references_tree_roc.bib\nnocite: |\n  @zhang2025, @mau2025, @niu2025, @yagci2022, @kuhn2022, @delong1988, @robin2011, @strumbelj2013\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\nrequired_packages <- c(\n  \"DT\",\n  \"broom\",\n  \"dominanceanalysis\",\n  \"ggparty\",\n  \"gt\",\n  \"pacman\",\n  \"partykit\",\n  \"patchwork\",\n  \"probably\",\n  \"purrr\",\n  \"randomForest\",\n  \"ranger\",\n  \"rpart.plot\",\n  \"scales\",\n  \"shapviz\",\n  \"stringr\",\n  \"tibble\",\n  \"tidymodels\",\n  \"tidyr\",\n  \"tidyverse\",\n  \"vip\",\n  \"workflowsets\",\n  \"xgboost\"\n)\n\nmissing_packages <- setdiff(required_packages, rownames(installed.packages()))\nif (length(missing_packages) > 0) {\n  install.packages(missing_packages, repos = \"https://cloud.r-project.org\")\n}\n```\n:::\n\n\n\n## はじめに\n\n5章・6章で扱ったロジスティック回帰では、「ある説明変数が正答確率にどう影響するか」を1本のグローバルな式でモデル化しました。しかし英語教育データでは、学習者要因と語彙項目要因の**複雑な組み合わせパターン**が頻出します。たとえば、習熟度が高い学習者では retrieval practice 条件の観測で正答確率が高い一方、低習熟者ではその差が小さい、といったパターンです。回帰分析でこうしたパターンを捉えるには交互作用項を手動で指定する必要がありますが、どの変数の組み合わせが重要かを事前に知ることは難しいのが実情です。\n\n**ツリーモデル**（決定木・ランダムフォレスト・XGBoost）は、こうした非線形関係や相互作用の**候補パターン**を、分析者がすべて事前に指定しなくても探索できる手法群です。本章では、GLMを基準線に置き、3つのツリーモデルを同じ教育データで比較し、モデルの性能評価に**ROC曲線とAUC**を用います。\n\n英語教育研究で想定される予測課題の例:\n\n- 語彙テスト項目の正誤（正解/不正解）を予測する\n- 学習者が次の小テストで基準を超えるかを予測する\n- CEFR到達判定の補助指標を構築する\n\n本章を通して、「**どの学習者が、どの語彙項目を、どの条件で正答するか**」を予測し、その予測を教育判断に変換する一連の流れを体系的に学びます。\n\n### 研究場面と適用範囲\n\n#### 分野別の活用例：どんな予測課題で使うか\n\nこの章のモデルは、単に「正答/誤答を当てる」ためだけの道具ではありません。SLA・心理言語学・英語教育学・教育学では、**多数の説明変数の組み合わせから、分類判断を安定して行いたい場面**で広く応用できます。\n\n| 分野 | 典型的な問い | 目的変数 | ツリーモデルが効きやすい理由 | 最初に見る図 |\n|---|---|---|---|---|\n| SLA | どの学習者が次の語彙テストで正答するか | 2値（正誤） | 習熟度 × 学習条件 × 項目特性の非線形な組み合わせを拾いやすい | ROC / PR / 閾値比較 |\n| 心理言語学 | 品詞曖昧性や語義曖昧性の解消で、どのラベルが選ばれるか | 2値または多値 | 文脈手がかり、語頻度、語長、近接語情報の相互作用候補を探索しやすい | 予測確率図、重要度、SHAP |\n| 英語教育学 | どの学習者が支援対象になるか、どの教材群でつまずくか | 2値（到達/未到達） | 明示的な if-then ルールとして現場へ返しやすい | 決定木、混同行列 |\n| 教育学 | 出席・提出・事前成績から成績不振リスクを早期検出できるか | 2値（リスクあり/なし） | 非線形閾値や支援候補のルール化に向く | ROC、キャリブレーション、閾値表 |\n\n心理言語学の例で言えば、**品詞曖昧性（POS ambiguity）** や **語義曖昧性（word-sense ambiguity）** の解消は、まさに「複数の手がかりからどのカテゴリを選ぶか」という予測問題です。語頻度、前後文脈、形態情報、語長、隣接語の分布などを入力し、`noun / verb / adjective` や `sense A / sense B / sense C` を予測する構図は、この章の分類問題と同型です。2値ならロジスティック回帰でも扱えますが、**どの文脈でどの手がかりが急に効き始めるか**を事前に式へ書き込むのは簡単ではありません。\n\n決定木の分岐は、心理言語学でいう **if-then 型処理** の比喩として理解しやすいです。たとえば「もし語頻度が低く、かつ直前語が冠詞なら noun を優先する」のようなルールは、まさに木の分岐です。一方、ランダムフォレストやXGBoostは、単一の厳密ルールというより、**複数の弱いヒューリスティックを束ねて判断する** イメージに近いです。したがって本章では、\n\n- 決定木: `if–then` ルールの可視化\n- ランダムフォレスト: 多数のヒューリスティックの平均化\n- XGBoost: 誤りを見ながらヒューリスティックを逐次改善\n\nという対応で読むと、SLA・心理言語学の研究質問に結びつけやすくなります。\n\nさらに、ツリーモデルの実務上の強みは、**非線形と交互作用候補の探索** にあります。回帰分析なら `proficiency × condition` や `word_freq × test_time` を分析者が明示的に入れる必要がありますが、ツリーモデルでは「どの変数の組み合わせが効いていそうか」をデータ駆動で探索できます。これは、事前理論がまだ粗い探索段階や、説明変数が多く組み合わせが膨大な研究場面で特に有用です。ただし、ツリーが見つけた分岐は「証明された効果」ではなく、独立データ・理論・追加分析で吟味すべき候補です。\n\n#### Yes/No課題では、ツリーモデルとSDT/IRTreeをどう使い分けるか\n\nLexical Decision Task（LDT）、Lexicosemantic Judgement Task、Grammaticality Judgement Task のような **Yes/No 形式** の課題では、「ツリーモデルが使えるか」という問いに対する答えは **研究目的による**、が最も正確です。\n\nまず、**予測** が目的なら、ツリーモデルは十分に有力です。たとえば「どの語が誤答されやすいか」「どの学習者が文法性判断で誤るか」「どの文脈で品詞曖昧性の解消に失敗しやすいか」を、語頻度、語長、文脈長、品詞手がかり、学習者特性の組み合わせから予測したいなら、CART / Random Forest / XGBoost は自然な候補です。とくに、どの変数の非線形効果や交互作用が重要かを事前に理論化しきれていない場面では、探索的な価値があります。\n\nしかし、**信号検出理論（SDT）** の観点で「感度 $d'$ と基準（criterion）」を分けて解釈したいなら、ツリーモデルだけでは足りません。LDT や文法性判断では、`yes` が多い人は「本当に識別能力が高い」のか、それとも「yes と答えやすい反応バイアス」を持っているだけなのかを分離したい場面があります。このときは、受容性判断への SDT 適用を論じた @huang2020sdt や、6章で触れた probit/SDT の枠組みの方が理論的に適切です。\n\nさらに、反応過程を **複数段階の潜在判断** として分けたいなら、**IRTree（Item Response Tree）** が有力です。ここでいう IRTree は、CART のような予測木ではなく、回答過程を「まず signal/noise を弁別する」「次に yes/no に傾く」などの **潜在ノード** に分解する心理測定モデルです[@deboeckpartchev2012; @jeondeboeck2016]。真偽式試験を SDT ベースで IRT 化する例として @decarlo2020tf があり、Yes/No テストの感度と反応バイアスを分けて考える発想に直結します。\n\n要するに、LDT・Lexicosemantic Judgement・GJT のような課題では、次のように使い分けるのが分かりやすいです。\n\n| 研究目的 | 向いている方法 | 何が分かるか |\n|---|---|---|\n| どの観測が正答/誤答になるかを当てたい | ツリーモデル（CART / RF / XGBoost） | 予測性能、非線形、交互作用、重要特徴 |\n| 感度と反応バイアスを分けたい | SDT / probit 系モデル | $d'$、criterion、hit / false alarm の構造 |\n| 反応過程を潜在ノードに分けたい | IRTree / SDT-IRT | 複数段階の潜在判断プロセス |\n\nこの区別は、「木」という言葉が同じでも中身がまったく違うことを意味します。**ツリーモデルは説明変数空間を分割する予測法**です。一方、**IRTree は反応過程を分解する心理測定法**です。前者は「誰がどこで誤るか」の予測に強く、後者は「なぜ yes/no が出たか」の測定モデルに強い、と整理しておくと混乱しにくくなります。\n\n先行研究については、**直接に LDT/GJT の trial-level behavioral response を CART/RF/XGBoost で主分析する研究は、本章で確認した範囲ではまだ主流ではありません**。一方で、隣接領域には十分な接点があります。たとえば、語義曖昧性の計算的解消では decision tree が古典的に使われています[@pedersen2001wsd]。また、L2 読解や語彙処理では random forest を使って lexical features の相対的重要性を評価する研究があります[@nahatameuchida2025]。さらに、語彙カテゴリ化や読みに関する学習研究でも機械学習を用いた予測的分析が広がっています[@gaglgregorova2024]。したがって、**予測章の worked example として LDT/GJT 系課題を扱うこと自体は十分に妥当**ですが、もし論文の主張が「感度」や「反応バイアス」にあるなら、SDT や IRTree に接続した方が理論的に強くなります。\n\n### 読み方と到達目標\n\n#### この章の前提\n\nこの章は、機械学習の実装テクニックを網羅する章ではありません。目的は、**教育研究の読者が「予測モデルを安全に読み、使い、報告できる状態」になること**です。以下の3点だけ確認して進めてください。\n\n1. この章は「2値分類（correct / incorrect）」に絞ります。\n2. いきなり複雑モデルに行かず、GLMを基準に段階的に比較します。\n3. 最後はAUCだけでなく、閾値・混同行列・キャリブレーションまで確認します。\n\nこの3点を守れば、「精度が高いか」だけでなく「現場で使えるか」まで判断できます。\n\n#### この章で最終的に手に入るもの\n\n- **予測結果**: 各観測に対する `P(correct)`（確率）\n- **運用ルール**: 閾値を何にするか（例: CV-Youden、CVコスト最小化、陽性クラスのRecall重視）\n- **説明資料**: どの特徴が予測に効いたか（Dominance / Permutation / SHAP）\n- **報告文**: 論文にそのまま使える報告テンプレート\n\n#### 最初に確認する用語（30秒版）\n\n- **予測確率**: モデルが出す「correct らしさ」（0〜1）\n- **閾値**: 確率を最終判定（correct / incorrect）に変換する境界値\n- **ROC-AUC**: 正答者を誤答者より高確率に並べられる能力\n- **PR-AUC**: 正答クラスの検出性能（不均衡データで特に重要）\n- **キャリブレーション**: 予測確率と実測率の一致度\n- **過学習**: 訓練データに合わせすぎてテストで性能が落ちる状態\n\n::: {.callout-warning}\n#### 陥りやすい3つの誤解\n\n1. **AUCが高ければ実務で使える**  \n   AUCはランキング性能の指標で、閾値運用の品質を保証しません。\n2. **複雑モデルは常に優れている**  \n   解釈可能性や再現性が下がる場合があり、GLMで十分なケースもあります。  \n3. **重要度 = 因果効果**  \n   重要度は予測への寄与を表す指標で、因果関係の証明ではありません。\n:::\n\n#### この章で扱うこと・扱わないこと\n\n- 扱う: 表形式データの2値分類、モデル比較、閾値設計、確率の妥当性確認\n- 扱わない: 深層学習、時系列モデル、因果推論、厳密な公平性監査\n\n範囲を限定することで、実務で頻出する「語彙テスト正誤予測」の判断フローに集中します。本章の `test_time` は「即時テストか遅延テストか」を表す説明変数で、試行順序や時点ごとの自己相関を扱う時系列モデルではありません。反応時間・音声軌跡・縦断測定のように順序そのものが研究対象になる場合は、第13章群の時系列・音声時系列の章に接続します。\n\n#### 概要\n\n- **ツリーモデル** = データを「もし proficiency > 0.4 ならば…」のような条件分岐で繰り返し分割し、局所ごとに予測を出す方法です。回帰分析が1本の式で全データを説明するのに対し、ツリーモデルはデータの「区画」ごとに異なる予測を出します。\n- **ROC-AUC** = ランダムに正答者と誤答者を1人ずつ選んだとき、モデルが正答者に高い確率を正しく与える割合です。1.0が完璧、0.5がランダムです。\n\n**節の依存関係と読み進め方**\n\n| 範囲 | カバーする内容 |\n|------|----------------|\n| **基礎** | はじめに → 学習・評価の分割 → GLM → 決定木 → GLM ベースライン + 3 ツリーモデル比較 → まとめ |\n| **応用** | 上記 + RF + XGBoost + 閾値 + キャリブレーション |\n| **発展** | 上記 + Yes/No 課題の SDT / IRTree 補論 + 回帰→ML の橋渡し + SHAP + ハイパーパラメータ詳細 + 重要度比較 |\n\n#### 図と表の役割マップ\n\nこの章は図が多いため、最初に「どの図を何の判断に使うか」を整理しておきます。1枚の図でモデルの良し悪しを決めるのではなく、**設計 → 学習 → 比較 → 閾値 → 確率 → 解釈**の順に確認します。\n\n| 確認したいこと | 主に見る図・表 | 読み方 | それだけで判断しないこと |\n|---|---|---|---|\n| データ分割は妥当か | Train/Test split, leakage check | 学習者・項目の重なり、正答率の偏りを確認 | 性能の優劣 |\n| モデルが複雑すぎないか | CV schematic, tuning heatmap, pruning profile | CVで安定して高い領域を探す | テスト性能の確定 |\n| どのモデルがよく並べ替えるか | 指標表, ROC曲線, AUC-CI | ROC-AUCとCIで識別性能を見る | 閾値運用の良否 |\n| 陽性クラスをどれだけ拾えるか | PR曲線, threshold trade-off | RecallとPrecisionの両立を見る | 支援対象の検出率。positive classの定義が先に必要 |\n| 確率をそのまま使えるか | calibration plot, calibration summary, Brier | 予測確率と実測率のずれを見る | 因果効果 |\n| 何が予測に寄与したか | Dominance, permutation, Gain, SHAP, PDP | 重要特徴、方向、局所説明を分けて読む | 教材・条件を変えたときの効果 |\n\nこの整理に従うと、最初に「モデル選択」、次に「閾値運用」、最後に「説明・報告」という順番が保てます。\n\n#### この章の到達目標\n\n1. ロジスティック回帰とツリーモデルの違いを、「的のずれ」と「散らばり」（バイアスと分散）の観点から説明できるようになります。\n2. GLMベースラインと、決定木・ランダムフォレスト・XGBoostの3つのツリーモデルを同じデータで比較できるようになります。\n3. ROC-AUCだけでなく、複数の評価指標（PR-AUC・閾値依存指標・予測確率の妥当性）も確認し、報告できるようになります。\n4. 変数の相対的重要性を測る複数の方法（SHAP、dominance analysis、permutation importance）の違いを理解し、使い分けられるようになります。\n5. 「精度が高い」で終わらず、教育実務で使える判断に落とし込めるようになります。\n\n## 分析の流れ\n\n本章では、2値分類問題に対してシンプルなモデルから段階的に複雑なモデルへと進み、最終的に複数モデルの性能を比較します。まずロジスティック回帰をベースラインとして出発し、決定木（CART）で非線形パターンを探索します。予測精度が不十分であればランダムフォレストやXGBoostに拡張し、ROC曲線の重ね描きとAUC比較でモデルを比較します。この「シンプルなモデルで始め、必要に応じて複雑なモデルへ拡張する」アプローチは、解釈可能性と予測精度のバランスを重視する教育データ分析で特に有効です。\n\n```{mermaid}\n%%| label: fig-analysis-flow\n%%| fig-cap: \"本章の分析フロー: シンプルなモデルから段階的に複雑なモデルへ\"\nflowchart TD\n  A[\"2値分類問題を定義<br/>(correct / incorrect)\"] --> B[\"GLMベースライン\"]\n  B --> C{\"精度は十分か?\"}\n  C -->|\"十分\"| D[\"評価・報告<br/>(ROC / PR / 閾値)\"]\n  C -->|\"不十分\"| E[\"決定木で非線形探索\"]\n  E --> F{\"精度改善?\"}\n  F -->|\"十分\"| D\n  F -->|\"不十分\"| G[\"アンサンブル<br/>(RF / XGBoost)\"]\n  G --> D\n  D --> H[\"閾値設計 + キャリブレーション\"]\n  H --> I[\"教育実務への変換\"]\n```\n\n### どのパスが最も関連するか\n\n多くの英語教育研究では、まずロジスティック回帰と決定木で十分な予測精度が得られるかを確認することが出発点になります。アンサンブル手法（RF・XGBoost）は予測精度の向上が必要な場合に検討しますが、解釈可能性が低下する点に注意が必要です。研究報告を目的とする場合は、まず左側のパス（ベースライン → 評価・報告）を試し、精度が不十分な場合に右側のアンサンブルパスへ進む、という順序が説明しやすいです。\n\n\n**このチャンクの目的:** 可視化テーマを章全体で統一するための初期設定です。\n\n\n### 主なパッケージの役割\n\n- `tidymodels` / `workflowsets`: 前処理、学習、評価、ワークフロー比較を統一的に扱います。\n- `ranger` / `xgboost` / `rpart.plot` / `partykit` / `ggparty`: 決定木、ランダムフォレスト、勾配ブースティングとその可視化を担当します。\n- `probably` / `vip` / `shapviz` / `dominanceanalysis`: ROC・確率評価、変数重要度、SHAP による解釈を補助します。\n- `tidyverse` / `gt` / `DT` / `patchwork` / `scales`: データ整形、表、対話表、図の合成、軸ラベル整形を支えます。\n\n### ミニシミュレーション: GJT / LDT の Yes/No trial data\n\nここで、LDT・Lexicosemantic Judgement・GJT に共通する **trial-level の Yes/No データ** を短くシミュレーションしておきます。目的は、この章のツリーモデルがどのようなデータ構造に適用できるかを明確にすることです。\n\n発想は、信号検出理論（SDT）に基づく Yes/No 課題の定式化に沿っています[@huang2020sdt; @decarlo2020tf]。各試行で学習者は、刺激から得られる **evidence** を内部的に形成し、それが自分の **criterion** を上回れば `yes`、下回れば `no` と答える、と考えます。さらに、このような 2 値反応データは、後で IRTree 的に「弁別」と「反応傾向」に分けて見ることもできます[@deboeckpartchev2012; @jeondeboeck2016]。\n\nこの節では、次のような stylized model でデータを作ります。\n\n$$\n\\text{evidence}_{pi} =\nd_p \\cdot s_i\n+ 0.5 \\cdot \\text{cue}_i\n- 0.7 \\cdot \\text{ambiguity}_i\n- 0.6 \\cdot \\text{difficulty}_i\n+ \\tau_{\\text{task}}\n+ \\varepsilon_{pi}\n$$\n\nここで、$d_p$ は参加者 $p$ の感度寄りの能力、$s_i$ は signal/noise を表す符号（signal = +1, noise = -1）、criterion は参加者ごとに持つ応答基準です。最終的な反応は\n\n$$\n\\text{response}_{pi} =\n\\begin{cases}\n\\text{yes} & \\text{if evidence}_{pi} > c_p \\\\\n\\text{no} & \\text{otherwise}\n\\end{cases}\n$$\n\nで決まります。これは厳密な SDT 推定そのものではありませんが、**感度** と **基準** を区別した Yes/No データの生成としては十分に教育的です。\n\n| タスク | `yes` の意味 | `no` の意味 | signal trial の例 | noise trial の例 |\n|---|---|---|---|---|\n| LDT | 単語である | 単語でない | word | nonword |\n| Lexicosemantic judgement | 意味的に適合する | 意味的に適合しない | plausible | implausible |\n| GJT | 文法的である | 文法的でない | grammatical | ungrammatical |\n\nこの同じ trial-level データを、研究目的に応じて異なる方法で読むことができます。\n\n| 研究目的 | 目的変数の置き方 | 主に知りたいこと |\n|---|---|---|\n| ツリーモデル | `response_yes` または `accuracy` | どの観測が yes / no, correct / incorrect になるか |\n| SDT | `signal/noise` と `response_yes` | hit, false alarm, $d'$, criterion |\n| IRTree | 同じ yes/no 反応を潜在ノードへ分解 | 弁別過程と反応傾向をどう切り分けるか |\n\n**このチャンクの目的:** LDT・意味判断・GJT に共通する trial-level Yes/No データを、SDT 的な latent evidence 発想でシミュレーションします。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2026)\n\nn_participants <- 140\nn_items_per_task <- 40\ntask_levels <- c(\"LDT\", \"Lexicosemantic\", \"GJT\")\n\nparticipant_yesno <- tibble(\n  participant = factor(1:n_participants),\n  proficiency = rnorm(n_participants, 0, 1),\n  sensitivity = pmax(0.15, 0.95 + 0.55 * proficiency + rnorm(n_participants, 0, 0.30)),\n  criterion = rnorm(n_participants, 0, 0.45)\n)\n\nitem_yesno <- crossing(\n  task = factor(task_levels, levels = task_levels),\n  item_no = 1:n_items_per_task\n) |>\n  mutate(\n    item = factor(paste(task, item_no, sep = \"_\")),\n    signal_present = rbinom(n(), 1, 0.5),\n    trial_type = case_when(\n      task == \"LDT\" & signal_present == 1 ~ \"word\",\n      task == \"LDT\" & signal_present == 0 ~ \"nonword\",\n      task == \"Lexicosemantic\" & signal_present == 1 ~ \"plausible\",\n      task == \"Lexicosemantic\" & signal_present == 0 ~ \"implausible\",\n      task == \"GJT\" & signal_present == 1 ~ \"grammatical\",\n      task == \"GJT\" & signal_present == 0 ~ \"ungrammatical\"\n    ),\n    cue_strength = rnorm(n(), 0, 0.9),\n    ambiguity = runif(n(), 0, 1),\n    item_difficulty = rnorm(n(), 0, 0.6),\n    task_offset = case_when(\n      task == \"LDT\" ~ 0.15,\n      task == \"Lexicosemantic\" ~ -0.05,\n      task == \"GJT\" ~ -0.15\n    )\n  )\n\nyesno_sdt_dat <- crossing(\n  participant = participant_yesno$participant,\n  item = item_yesno$item\n) |>\n  left_join(participant_yesno, by = \"participant\") |>\n  left_join(item_yesno, by = \"item\") |>\n  mutate(\n    signal_signed = if_else(signal_present == 1, 1, -1),\n    evidence =\n      sensitivity * signal_signed +\n      0.5 * cue_strength -\n      0.7 * ambiguity -\n      0.6 * item_difficulty +\n      task_offset +\n      rnorm(n(), 0, 1),\n    response_yes = factor(\n      if_else(evidence > criterion, \"yes\", \"no\"),\n      levels = c(\"no\", \"yes\")\n    ),\n    accuracy = factor(\n      if_else((response_yes == \"yes\") == (signal_present == 1), \"correct\", \"incorrect\"),\n      levels = c(\"incorrect\", \"correct\")\n    ),\n    proficiency_group = ntile(proficiency, 3) |>\n      factor(labels = c(\"Low\", \"Mid\", \"High\"))\n  ) |>\n  dplyr::select(\n    participant, item, task, trial_type, signal_present, response_yes, accuracy,\n    proficiency, proficiency_group, cue_strength, ambiguity, item_difficulty\n  )\n```\n:::\n\n\nこのデータでは、**signal trial に対して `yes` が多いほど hit rate が高く**、**noise trial に対して `yes` が多いほど false alarm rate が高い**という構造が自然に出ます。`sensitivity` は signal/noise の向きが逆転しないよう正の値に制約しており、ツリーモデルの分類問題としても、SDT の Yes/No 課題としても読める形です。\n\n**このチャンクの目的:** タスク別に hit / false alarm / accuracy を要約し、Yes/No 課題として妥当な構造になっているかを確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nsafe_rate <- function(x) pmin(pmax(x, 0.001), 0.999)\n\nyesno_sdt_dat |>\n  group_by(task) |>\n  summarise(\n    hit_rate = mean(response_yes == \"yes\" & signal_present == 1) /\n      mean(signal_present == 1),\n    false_alarm_rate = mean(response_yes == \"yes\" & signal_present == 0) /\n      mean(signal_present == 0),\n    accuracy_rate = mean(accuracy == \"correct\"),\n    dprime_like = qnorm(safe_rate(hit_rate)) - qnorm(safe_rate(false_alarm_rate)),\n    .groups = \"drop\"\n  ) |>\n  mutate(across(c(hit_rate, false_alarm_rate, accuracy_rate, dprime_like), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Simulated Yes/No Task Summary by Task\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Simulated Yes/No Task Summary by Task\n\n|task           | hit_rate| false_alarm_rate| accuracy_rate| dprime_like|\n|:--------------|--------:|----------------:|-------------:|-----------:|\n|LDT            |    0.701|            0.290|         0.704|       1.079|\n|Lexicosemantic |    0.635|            0.225|         0.702|       1.101|\n|GJT            |    0.596|            0.218|         0.679|       1.021|\n\n\n:::\n:::\n\n\n**このチャンクの目的:** proficiency が上がると signal/noise の分離がどう改善するかを、3課題まとめて可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\nyesno_sdt_dat |>\n  group_by(task, proficiency_group, signal_present) |>\n  summarise(yes_rate = mean(response_yes == \"yes\"), .groups = \"drop\") |>\n  mutate(\n    trial_family = factor(\n      if_else(signal_present == 1, \"Signal trials\", \"Noise trials\"),\n      levels = c(\"Signal trials\", \"Noise trials\")\n    )\n  ) |>\n  ggplot(aes(x = proficiency_group, y = yes_rate, group = trial_family, color = trial_family)) +\n  geom_line(linewidth = 1) +\n  geom_point(size = 2.2) +\n  facet_wrap(~ task) +\n  scale_color_manual(values = c(\"Signal trials\" = \"#2C7FB8\", \"Noise trials\" = \"#D95F02\")) +\n  labs(\n    title = \"Simulated Yes/No Tasks: Hit-Like and False-Alarm-Like Patterns\",\n    subtitle = \"Higher proficiency increases separation between signal and noise\",\n    x = \"Participant proficiency group\",\n    y = \"Observed P(yes)\",\n    color = NULL\n  )\n```\n\n::: {.cell-output-display}\n![Yes-response rates for signal and noise trials across three Yes/No tasks](8_予測分析_ROC曲線とツリーモデル_files/figure-html/yesno-sdt-hit-fa-plot-1.png){width=3000}\n:::\n:::\n\n\nこの図の読み方は単純です。**青線（signal trials）と赤線（noise trials）の差が大きいほど、signal と noise を区別できている**と考えられます。高 proficiency 群でこの差が広がるなら、感度が高い参加者ほど hit は増え、false alarm は減るという、SDT 的に自然なパターンです。\n\n#### 同じ Yes/No データを `probit` で読む\n\nこの同じデータを、今度は **予測** ではなく **感度と基準の分離** を意識して `probit` モデルで見てみます。R では `glm(..., family = binomial(link = \"probit\"))` を使います。`logit` と `probit` はどちらも 2 値応答を扱えますが、SDT と接続したいときは latent normal を前提にする `probit` が自然です。\n\nここでは簡略化のため、`response_yes` を目的変数にし、`signal_present`、`proficiency`、その交互作用、さらに `cue_strength`、`ambiguity`、`item_difficulty`、`task` を入れます。直感的には、\n\n- **切片**: noise trial における `yes` 傾向の強さ\n- **`signal_present`**: signal と noise の分離の大きさ\n- **`proficiency`**: 全般的な応答傾向の変化\n- **`signal_present:proficiency`**: proficiency が高いほど signal/noise の分離がどれだけ強まるか\n\nを見ています。\n\n**このチャンクの目的:** 同じ Yes/No データを probit 回帰で読み、signal/noise の分離と応答基準を近似的に確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nprobit_yesno <- glm(\n  response_yes ~ signal_present * proficiency + cue_strength + ambiguity +\n    item_difficulty + task,\n  data = yesno_sdt_dat,\n  family = binomial(link = \"probit\")\n)\n\nbroom::tidy(probit_yesno, conf.int = TRUE) |>\n  dplyr::filter(term %in% c(\n    \"(Intercept)\", \"signal_present\", \"proficiency\",\n    \"signal_present:proficiency\", \"cue_strength\",\n    \"ambiguity\", \"item_difficulty\"\n  )) |>\n  mutate(across(c(estimate, std.error, statistic, p.value, conf.low, conf.high), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Probit Model for Yes/No Responses (Selected Terms)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Probit Model for Yes/No Responses (Selected Terms)\n\n|term                       | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:--------------------------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept)                |   -0.586|     0.031|   -18.886|       0|   -0.647|    -0.525|\n|signal_present             |    1.456|     0.027|    54.793|       0|    1.404|     1.508|\n|proficiency                |   -0.793|     0.022|   -35.741|       0|   -0.837|    -0.750|\n|cue_strength               |    0.454|     0.015|    31.314|       0|    0.426|     0.483|\n|ambiguity                  |   -0.581|     0.043|   -13.579|       0|   -0.665|    -0.497|\n|item_difficulty            |   -0.536|     0.023|   -23.787|       0|   -0.580|    -0.492|\n|signal_present:proficiency |    1.571|     0.029|    53.906|       0|    1.515|     1.629|\n\n\n:::\n:::\n\n\nこの表の読み方は次のとおりです。\n\n- `signal_present` が大きく正なら、signal trial で `yes` が増え、noise trial とよく分離できています。\n- `proficiency` が正でも、**それだけでは「感度が高い」とは限りません**。noise trial でも `yes` を増やすなら、単に liberal な基準になっている可能性があります。\n- `signal_present:proficiency` が正なら、proficiency が高い参加者ほど **signal と noise の差をより大きく使えている**ことを意味し、SDT 的には感度上昇に近い読み方ができます。\n\n**このチャンクの目的:** probit モデルから得られる `P(yes)` を signal/noise 別に描き、感度と基準の読み方を可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\nprobit_grid <- crossing(\n  task = factor(task_levels, levels = task_levels),\n  proficiency = seq(\n    min(yesno_sdt_dat$proficiency),\n    max(yesno_sdt_dat$proficiency),\n    length.out = 80\n  ),\n  signal_present = c(0, 1),\n  cue_strength = mean(yesno_sdt_dat$cue_strength),\n  ambiguity = mean(yesno_sdt_dat$ambiguity),\n  item_difficulty = mean(yesno_sdt_dat$item_difficulty)\n)\n\nprobit_grid <- probit_grid |>\n  mutate(\n    p_yes = predict(probit_yesno, newdata = probit_grid, type = \"response\"),\n    trial_family = factor(\n      if_else(signal_present == 1, \"Signal trials\", \"Noise trials\"),\n      levels = c(\"Signal trials\", \"Noise trials\")\n    )\n  )\n\nprobit_grid |>\n  ggplot(aes(x = proficiency, y = p_yes, color = trial_family)) +\n  geom_line(linewidth = 1) +\n  facet_wrap(~ task) +\n  scale_color_manual(values = c(\"Signal trials\" = \"#2C7FB8\", \"Noise trials\" = \"#D95F02\")) +\n  labs(\n    title = \"Probit View of the Same Yes/No Data\",\n    subtitle = \"Wider separation implies greater signal-noise discrimination\",\n    x = \"Proficiency\",\n    y = \"Predicted P(yes)\",\n    color = NULL\n  )\n```\n\n::: {.cell-output-display}\n![Probit-based yes-response curves for signal and noise trials](8_予測分析_ROC曲線とツリーモデル_files/figure-html/yesno-probit-pred-1.png){width=3000}\n:::\n:::\n\n\nこの図では、**2本の線の距離**をまず見ます。signal 線と noise 線の差が広いほど、同じ proficiency でも `yes` 応答が刺激の真偽に応じてきちんと切り替わっており、感度が高いと読めます。一方で、両方の線が同時に上に持ち上がるなら、感度というより `yes` と答えやすい基準のシフトかもしれません。\n\n#### 同じ Yes/No データを IRTree で読むと何が変わるか\n\nIRTree は、上の `probit` のように1本の反応関数で `yes/no` をまとめて扱うのではなく、**観測された Yes/No の背後に複数の潜在判断ノードがある**と考えます。ここで重要なのは、IRTree が「正誤を当てる方法」ではなく、**反応過程を分解する測定モデル**だという点です。ただし、1列の `yes/no` 反応だけから反応過程が自動的に分解されるわけではありません。ノード構造、項目設計、制約、反復観測、代替モデルとの比較がそろって初めて、「弁別」と「反応傾向」を分けて読む根拠が生まれます。\n\n| 観測された結果 | SDT での呼び方 | 予測モデルが見るもの | IRTree が分けて考えたいもの |\n|---|---|---|---|\n| signal + yes | hit | 正しく `yes` を出した1観測 | 弁別成功 + yes 側選択 |\n| signal + no | miss | 誤った `no` を出した1観測 | 弁別失敗 or 保守的基準 |\n| noise + yes | false alarm | 誤った `yes` を出した1観測 | 弁別失敗 or liberal 基準 |\n| noise + no | correct rejection | 正しく `no` を出した1観測 | 弁別成功 + no 側選択 |\n\nしたがって、同じ GJT/LDT データでも、\n\n- **ツリーモデル**: どの trial が `yes/no` や `correct/incorrect` になるかを予測する\n- **probit / SDT**: `yes` 応答を signal/noise 分離と基準の観点で読む\n- **IRTree**: hit / miss / false alarm / correct rejection を生む潜在ノードを分ける\n\nという違いがあります。研究目的が「教育現場で誰を支援対象にするか」なら予測モデルが自然です。研究目的が「この参加者は本当に文法性を弁別できているのか、それとも yes バイアスが強いのか」なら、SDT や IRTree の方が中心になります。\n\nこのデータを使うと、同じ `response_yes` / `accuracy` を\n\n- 第8章では **予測の問題**\n- 第6章では **2値 GLMM / probit の問題**\n- IRTree では **潜在反応過程の分解**\n\nとして読み替えられます。したがって、GJT や LDT をツリーモデルで扱うこと自体は十分可能ですが、研究の主張が **予測精度** なのか、**感度と基準** なのか、**反応過程** なのかを先に決める必要があります。\n\n### 回帰分析から機械学習への橋渡し\n\n読者の多くは、5章・6章でロジスティック回帰を学んでいるはずです。ツリーモデルに進む前に、「なぜ回帰だけでは不十分な場面があるのか」「ツリーモデルは何が本質的に異なるのか」を整理します。この節を理解すると、以降の決定木・RF・XGBoostの位置づけがはっきりします。\n\n#### 回帰分析の限界を直感で理解する\n\nロジスティック回帰は、すべてのデータに対して**1本のグローバルな式**をあてはめます。\n\n$$\n\\log\\frac{P(\\text{correct})}{1-P(\\text{correct})} = \\beta_0 + \\beta_1 x_1 + \\beta_2 x_2 + \\cdots\n$$\n\nこの式は、$x_1$（たとえば習熟度）が1単位増えると対数オッズが $\\beta_1$ だけ変化する、という**全データに共通のルール**を仮定しています。しかし教育データでは、「習熟度が高い層では retrieval practice が特に効く」「低頻度語では語長の影響が大きくなる」のように、**ある条件下でだけ効く変数**が少なくありません。\n\n回帰分析でも交互作用項（`proficiency:condition`）を手動で追加すれば対応可能ですが、どの交互作用を入れるかは分析者が事前に決める必要があります。変数が10個あれば交互作用の候補は45通り、3次の交互作用まで含めればさらに膨大になります。\n\nツリーモデルは、この交互作用候補の探索を**データから**行います。\n\n例えば、retrieval practice条件は習熟度が高い学習者で特に効くが、低習熟者ではほとんど差が見えない、という「条件付きパターン」があるとします。回帰分析では `proficiency × condition` という交互作用項を分析者が手動で指定する必要があります。しかし決定木は「proficiency > 0.5 の学習者に対してだけ condition を見る」という分割を候補として拾えます。この探索能力が、予測変数が多い場面でツリーモデルの強みになります。\n\n以下の図は、同じデータに対して「回帰の線形フィット」と「ツリーの矩形分割」がどう異なるかを示しています。コードでは (1) 非線形な真の関係を持つデータを生成し、(2) ロジスティック回帰でフィットし、(3) 決定木でフィットして、3パネルの比較図を作成します。\n\n\n**このチャンクの目的:** 非線形な真の関係に対して、ロジスティック回帰と決定木のフィットがどう異なるかを3パネルで比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2024)\ndemo_dat <- tibble(\n  x = seq(-3, 3, length.out = 200),\n  y_true = plogis(1.5 * sin(2 * x) + 0.3 * x),\n  y_obs = rbinom(200, 1, y_true)\n)\n\n# (A) 真の非線形関係\np1 <- demo_dat |>\n  ggplot(aes(x, y_true)) +\n  geom_line(linewidth = 1.2, color = \"#2C7FB8\") +\n  geom_point(aes(y = y_obs), alpha = 0.15, size = 0.8) +\n  labs(title = \"(A) True relationship\", x = \"x\", y = \"P(correct)\") +\n  ylim(0, 1)\n\n# (B) ロジスティック回帰（線形）\nglm_demo <- glm(y_obs ~ x, data = demo_dat, family = binomial())\ndemo_dat$glm_pred <- predict(glm_demo, type = \"response\")\n\np2 <- demo_dat |>\n  ggplot(aes(x, y_true)) +\n  geom_line(linewidth = 0.6, color = \"gray60\", linetype = \"dashed\") +\n  geom_line(aes(y = glm_pred), linewidth = 1.2, color = \"#D95F02\") +\n  geom_point(aes(y = y_obs), alpha = 0.15, size = 0.8) +\n  labs(title = \"(B) Logistic regression\", x = \"x\", y = \"P(correct)\") +\n  ylim(0, 1)\n\n# (C) ツリー分割\ntree_demo <- rpart::rpart(y_obs ~ x, data = demo_dat, method = \"class\",\n                          control = rpart::rpart.control(maxdepth = 3, cp = 0.01))\ndemo_dat$tree_pred <- predict(tree_demo, type = \"prob\")[, \"1\"]\n\np3 <- demo_dat |>\n  ggplot(aes(x, y_true)) +\n  geom_line(linewidth = 0.6, color = \"gray60\", linetype = \"dashed\") +\n  geom_step(aes(y = tree_pred), linewidth = 1.2, color = \"#1B9E77\") +\n  geom_point(aes(y = y_obs), alpha = 0.15, size = 0.8) +\n  labs(title = \"(C) Decision tree\", x = \"x\", y = \"P(correct)\") +\n  ylim(0, 1)\n\np1 + p2 + p3\n```\n\n::: {.cell-output-display}\n![Regression fits a single global curve; a tree partitions the feature space into local regions](8_予測分析_ROC曲線とツリーモデル_files/figure-html/regression-vs-tree-intuition-1.png){width=3000}\n:::\n:::\n\n\n- **(A)** 真の関係は非線形のS字曲線です。\n- **(B)** ロジスティック回帰は1本の単調なシグモイドで近似するため、波形を捉えきれません。\n- **(C)** 決定木は区間ごとに異なる確率を割り当て、局所的なパターンにフィットします。\n\n#### 回帰とツリーの本質的な違い\n\n- **回帰**: 全データに1本の式（グローバル）を当てはめます。係数を読めば「何が効くか」がわかります。\n- **ツリー**: データを条件分岐で分割し、各領域で別の予測を出します（ローカル）。分岐規則を読めば「どの条件で効くか」がわかります。\n\n#### バイアスと分散のトレードオフ\n\nモデル選択を理解するうえで最も重要な概念が、**バイアス・分散のトレードオフ**です[@hastie2009; @james2021]。\n\n- **バイアス（bias）**: モデルが現実を単純化しすぎることによる系統的な誤差です。ロジスティック回帰が非線形な関係を直線的に近似するとき、これが生じます。\n- **分散（variance）**: データが変わるとモデルの予測が大きく変わる不安定さです。深い決定木はデータの細部まで追いかけるため、別のサンプルでは異なる構造になりやすくなります。\n\n用語は統計学の慣用に合わせ、`variance` を **分散** と呼びます。機械学習文献では *bias-variance tradeoff* と英語のまま書かれることもありますが、本章では **バイアス・分散トレードオフ** に統一します。参考: [e-Stat 統計用語集（分散）](https://www.e-stat.go.jp/classifications/terms/90/00/0605)\n\nより厳密には、**二乗損失（回帰）** での期待予測誤差は次の形で分解されます[@geman1992; @hastie2009; @james2021]。\n\n$$\n\\mathbb{E}\\{(Y-\\hat{f}(X))^2\\} = \\text{Bias}[\\hat{f}(X)]^2 + \\text{Var}[\\hat{f}(X)] + \\sigma^2\n$$\n\n**注意**: この加法分解は二乗損失（回帰）に対する厳密な結果です。0-1 損失（分類）では同様の単純な加法分解は成り立たず、より複雑な定義が必要になります。本章では概念図として読み、分類でも「単純すぎるとバイアス、複雑すぎると分散」という定性的な傾向は同じだと理解してください。\n\n同様の分解は、scikit-learn の公式チュートリアル（bias-variance decomposition）でも確認できます。  \n参考: [scikit-learn: Single estimator versus bagging](https://scikit-learn.org/stable/auto_examples/ensemble/plot_bias_variance.html)\n\nここで \\(\\sigma^2\\) は不可約誤差（データに本質的に含まれるノイズ）です。実務では、この式を次のように読めば十分です。\n\n- モデルを単純にしすぎると、**バイアス** が増えます（重要な関係を取りこぼします）。\n- モデルを複雑にしすぎると、**分散** が増えます（サンプル依存で予測が不安定になります）。\n- 交差検証は、この2つのバランスが最もよい点を探す手続きです。\n\n\n**このチャンクの目的:** バイアス・分散トレードオフを、複雑度に対する訓練/テスト誤差の曲線で可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\nbv_dat <- tibble(\n  complexity = seq(0.5, 10, length.out = 100),\n  train_error = 0.45 * exp(-0.5 * complexity) + 0.02,\n  test_error  = 0.45 * exp(-0.5 * complexity) + 0.02 + 0.005 * complexity^1.8,\n  bias2       = 0.45 * exp(-0.5 * complexity),\n  variance    = 0.005 * complexity^1.8\n)\n\nbv_dat |>\n  ggplot(aes(x = complexity)) +\n  geom_line(aes(y = train_error, color = \"Training error\"), linewidth = 1) +\n  geom_line(aes(y = test_error, color = \"Test error\"), linewidth = 1) +\n  geom_line(aes(y = bias2, color = \"Bias^2\"), linewidth = 0.8, linetype = \"dashed\") +\n  geom_line(aes(y = variance, color = \"Variance\"), linewidth = 0.8, linetype = \"dashed\") +\n  geom_vline(xintercept = 3.5, linetype = \"dotted\", color = \"gray40\") +\n  annotate(\"text\", x = 3.7, y = 0.42, label = \"Optimal complexity\", hjust = 0, size = 3.5) +\n  scale_color_manual(values = c(\n    \"Training error\" = \"#2C7FB8\",\n    \"Test error\"     = \"#D95F02\",\n    \"Bias^2\"         = \"#7570B3\",\n    \"Variance\"       = \"#E7298A\"\n  )) +\n  labs(\n    title = \"Bias-Variance Tradeoff\",\n    subtitle = \"Underfitting (left) vs. Overfitting (right)\",\n    x = \"Model complexity\",\n    y = \"Error\",\n    color = NULL\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![Bias-variance tradeoff: as model complexity increases, training error decreases monotonically, but test error first decreases then increases](8_予測分析_ROC曲線とツリーモデル_files/figure-html/bias-variance-tradeoff-1.png){width=2400}\n:::\n:::\n\n\nこの図の読み方:\n\n- **左側（低複雑度）**: モデルが単純すぎてデータのパターンを拾えません（**未学習、バイアスが大きい**）。\n- **右側（高複雑度）**: モデルがデータのノイズまで拾ってしまいます（**過学習、分散が大きい**）。\n- **最適点**: テスト誤差が最小になる複雑度です。この点を交差検証（後述）で探すのが本章の基本戦略です。\n\n**数値で実感する**: 仮に語彙テストの正答率予測で以下のような結果が出たとします。\n\n| モデル | 訓練データ正答率 | テストデータ正答率 | 症状 |\n|--------|:---:|:---:|------|\n| 浅い決定木（深さ2） | 67% | 65% | バイアス大（両方低い＝パターンを捉えきれていません） |\n| 深い決定木（深さ10） | 95% | 60% | 分散大（訓練とテストの差＝ノイズを学習しています） |\n| ランダムフォレスト | 78% | 73% | バギングで分散を抑制しています |\n| XGBoost | 80% | 75% | ブースティングでバイアスも改善しています |\n\n訓練データとテストデータの正答率が**両方とも低い**ならバイアスの問題、**訓練だけ高くテストが低い**なら分散の問題です。\n\n本章の3つのツリーモデルは、このトレードオフの異なる位置にいます:\n\n- **決定木**: 深くすれば表現力は高いですが、分散が大きくなります（データの揺れに弱いです）。\n- **ランダムフォレスト**: 多数の木の多数決（バギング）で分散を抑制します。\n- **XGBoost**: 前の木の誤差を次の木が修正（ブースティング）し、バイアスを段階的に減らします。\n\n#### 3つのツリーモデルの位置づけ（バイアス・分散の視点）\n\n| モデル | 主な戦略 | 効果 |\n|--------|----------|------|\n| 決定木 | 1本の木で分割します | 解釈しやすいですが不安定です |\n| ランダムフォレスト | 独立な木の多数決（バギング）を使います | 分散を削減します |\n| XGBoost | 前段の残差を逐次修正します（ブースティング） | バイアスを削減します |\n\n#### ツリーモデルの分岐ロジック：再帰分割\n\n決定木がデータを分ける仕組みを、小さな例で追って確認します。\n\n6人の学習者のデータがあるとします。目的変数は正答/誤答、説明変数は `proficiency`（習熟度）です。\n\n\n**このチャンクの目的:** 6人の学習者データで、決定木の分割ポイント選択を具体的に追体験します。\n\n::: {.cell}\n\n```{.r .cell-code}\nmini_split <- tibble(\n  learner = LETTERS[1:6],\n  proficiency = c(-1.2, -0.5, 0.1, 0.4, 0.9, 1.5),\n  correct = factor(c(\"incorrect\", \"incorrect\", \"incorrect\", \"correct\", \"correct\", \"correct\"),\n                   levels = c(\"incorrect\", \"correct\"))\n)\n```\n:::\n\n\n| learner | proficiency | correct |\n|---|---:|---|\n| A | -1.2 | incorrect |\n| B | -0.5 | incorrect |\n| C | 0.1 | incorrect |\n| D | 0.4 | correct |\n| E | 0.9 | correct |\n| F | 1.5 | correct |\n\n: Mini Example: 6 Learners\n\n木は「どこで分けると、分けた後のグループが最も\"純粋\"になるか」を探します。**Gini不純度**で純粋さを測ります:\n\n$$\nG = 1 - \\sum_{k=1}^{K} p_k^2\n$$\n\nここで $p_k$ はノード内のクラス $k$ の割合です。$G = 0$（完全に1クラス）が最も純粋で、$G = 0.5$（2クラスが半々）が最も不純です。\n\n\n**このチャンクの目的:** Gini不純度の計算を手動で実行し、分割前後の純度改善を確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\ngini <- function(labels) {\n  p <- table(labels) / length(labels)\n  1 - sum(p^2)\n}\n\n# 分割前のGini\ngini_before <- gini(mini_split$correct)\n\n# 候補分割点: proficiency = 0.25（CとDの間）\nleft  <- mini_split |> dplyr::filter(proficiency <= 0.25)\nright <- mini_split |> dplyr::filter(proficiency > 0.25)\ngini_after <- (nrow(left) / 6) * gini(left$correct) +\n              (nrow(right) / 6) * gini(right$correct)\n\ntibble(\n  Step = c(\"Before split (all 6)\", \"Left (prof ≤ 0.25)\", \"Right (prof > 0.25)\", \"Weighted Gini after split\"),\n  Gini = c(gini_before, gini(left$correct), gini(right$correct), gini_after)\n) |>\n  mutate(Gini = round(Gini, 3)) |>\n  knitr::kable(caption = \"Gini Impurity Calculation at proficiency = 0.25\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Gini Impurity Calculation at proficiency = 0.25\n\n|Step                      | Gini|\n|:-------------------------|----:|\n|Before split (all 6)      |  0.5|\n|Left (prof ≤ 0.25)        |  0.0|\n|Right (prof > 0.25)       |  0.0|\n|Weighted Gini after split |  0.0|\n\n\n:::\n:::\n\n\n分割前のGiniは 0.500（2クラスが半々）です。`proficiency = 0.25` で分けると、左は全員 incorrect（$G = 0$）、右は全員 correct（$G = 0$）となり、加重Giniは 0.000 です。これが**最も純粋な分割**です。\n\n実際の決定木アルゴリズム（CART）は、すべての変数×すべての分割点でこのGini改善量を計算し、最大の改善をもたらす分割を選びます。これを再帰的に繰り返し、停止条件（最小サンプル数、最大深さなど）に達するまで分岐を続けます[@breiman1984]。\n\n**回帰との対比**:\n- **ロジスティック回帰**: 全データに対して $\\beta$ を最適化します（1つのグローバルな最適化問題です）。\n- **決定木**: 各ノードで局所的に最良の分割を選びます（貪欲な局所最適化の繰り返しです）。\n\n#### 交差検証の直感的理解\n\nツリーモデルのハイパーパラメータ（木の深さ、学習率など）を決めるために、本章では**交差検証（Cross-Validation; CV）**を使います。CVの仕組みを先に確認しておきます。\n\n\n**このチャンクの目的:** 3-fold CVの仕組みを、データ分割のタイル図で可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\ncv_dat <- expand_grid(\n  fold = paste0(\"Iteration \", 1:3),\n  block = 1:3\n) |>\n  mutate(\n    role = case_when(\n      fold == \"Iteration 1\" & block == 1 ~ \"Test\",\n      fold == \"Iteration 2\" & block == 2 ~ \"Test\",\n      fold == \"Iteration 3\" & block == 3 ~ \"Test\",\n      TRUE ~ \"Train\"\n    ),\n    label = paste0(\"Fold \", block)\n  )\n\ncv_dat |>\n  ggplot(aes(x = label, y = fold, fill = role)) +\n  geom_tile(color = \"white\", linewidth = 1.5) +\n  geom_text(aes(label = role), size = 4, fontface = \"bold\") +\n  scale_fill_manual(values = c(\"Train\" = \"#2C7FB8\", \"Test\" = \"#D95F02\")) +\n  labs(\n    title = \"3-Fold Cross-Validation\",\n    subtitle = \"Each fold takes a turn as the test set; results are averaged\",\n    x = NULL, y = NULL, fill = NULL\n  ) +\n  theme(\n    legend.position = \"bottom\",\n    panel.grid = element_blank()\n  )\n```\n\n::: {.cell-output-display}\n![K-fold cross-validation schematic (K=3)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/cv-schematic-1.png){width=2400}\n:::\n:::\n\n\nCVの手順:\n\n1. データをK個に分割します（本章ではK=3）。\n2. 1つ目の分割を検証用に取っておき、残りで学習します。\n3. 2つ目、3つ目でも同様に繰り返します。\n4. K回の結果を平均して、モデルの汎化性能を推定します。\n\nこの方法の利点は、テストデータを「使い切る前に」汎化性能を安定的に推定できる点です。ハイパーパラメータのチューニング（後述の `tune_grid`）はすべてこのCV上で行い、最終評価は別に確保したテストデータで行います。\n\n本章はチュートリアルのため計算時間を短縮する目的で3-foldを採用しています。実際の研究では **5-fold または10-fold** が一般的です[@james2021]。\n\n---\n\n## 事前準備\n\n### パッケージ\n\n\n**このチャンクの目的:** 全パッケージの読み込み、乱数seed固定、tidymodels優先設定を行います。\n\n\n`tidymodels_prefer()` は tidymodels エコシステム内の衝突解決を補助する設定として有用です。ただし、本章ではそれだけに依存せず、`dplyr::select()` や `dplyr::filter()` のように**使用パッケージを明示**して、`MASS::select()` や `stats::filter()` との衝突を避けます。モデル構築フローを「前処理（recipes）→ 仕様（parsnip）→ ワークフロー（workflows）→ 評価（yardstick）」の順で読むと理解が安定します。参考: [tidymodels: Getting started (models)](https://www.tidymodels.org/start/models/), [tidymodels: Getting started (resampling)](https://www.tidymodels.org/start/resampling/)\n\n### 評価設計（最初に決める）\n\n最初に評価設計を固定します。ここを曖昧にすると、後から都合のよい指標だけを見る「後出し」になりがちです。\n\n\n**このチャンクの目的:** 章全体で使う評価指標セット（ROC-AUC, PR-AUC, Brier等）を先に固定します。\n\n::: {.cell}\n\n```{.r .cell-code}\nmetric_tbl <- metric_set(\n  roc_auc, pr_auc, accuracy,\n  sens, spec, brier_class\n)\n\nmetric_tbl\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nA metric set, consisting of:\n- `roc_auc()`, a probability metric     | direction: maximize\n- `pr_auc()`, a probability metric      | direction: maximize\n- `accuracy()`, a class metric          | direction: maximize\n- `sens()`, a class metric              | direction: maximize\n- `spec()`, a class metric              | direction: maximize\n- `brier_class()`, a probability metric | direction: minimize\n```\n\n\n:::\n:::\n\n\nこの章では、次のように役割分担して読みます。\n\n- `ROC_AUC`: モデル全体の識別性能を比較します。\n- `PR_AUC`: 陽性クラス（ここでは `correct`）を重視して比較します。\n- `Sensitivity` / `Specificity`: 閾値を決めた後の実運用評価に使います。\n- `Brier`: 予測確率の総合的な誤差を見ます（識別とキャリブレーションの両方の影響を受けます）。\n\n#### なぜROCだけでなくPRも見るのか\n\n- ROC-AUCは閾値に依存しない便利な比較指標です。\n- ただし陽性が少ないデータではPR曲線の方が差が見えやすい場合があります[@saito2015]。\n- 厳密には、**ROC-AUC自体は陽性率（有病率）に対して不変**です。一方、Precision（適合率）は陽性率に依存するため、陽性が稀な場面ではPR曲線の方が**運用判断（どれだけの陽性予測が真陽性か）と直結しやすい**という性質があります。「ROC-AUCが不均衡データで誤っている」のではなく、「PR曲線の方が運用上の関心と一致する角度から差を見せてくれる」と理解するのが正確です [@saito2015]。\n\n`yardstick` の2値指標は既定で「因子の第1水準」をイベントとして扱います。そのため本章では `levels = c(\"incorrect\", \"correct\")` を固定し、`event_level = \"second\"` を明示しています。`roc_auc()` と `pr_auc()` は閾値非依存の集約指標、`sens()`/`spec()` は閾値依存指標、`brier_class()` は確率予測誤差の二乗平均で、小さいほど良好です。Brier score は便利な確率スコアですが、それだけでキャリブレーションを証明する指標ではありません。確率の妥当性を主張する場合は、後半の信頼度図、calibration slope、ECE などと併せて確認します。参考: [yardstick: roc_auc()](https://yardstick.tidymodels.org/reference/roc_auc.html), [yardstick: pr_auc()](https://yardstick.tidymodels.org/reference/pr_auc.html), [yardstick: brier_class()](https://yardstick.tidymodels.org/reference/brier_class.html)\n\n公式ドキュメントに照らすと、本章の評価設計では次の点を固定しておく必要があります。ここを曖昧にすると、同じ予測確率を使っていても、陽性クラスや評価単位の違いで結果の意味が変わります。\n\n| 項目 | 公式仕様で確認すべき点 | 本章での扱い |\n|---|---|---|\n| 陽性クラス | `yardstick` の2値指標はイベント水準を明示しないと既定動作に依存します | `correct` を第2水準に置き、`event_level = \"second\"` を各指標で明示します |\n| 閾値非依存指標 | `roc_auc()` / `pr_auc()` は確率列から曲線下面積を計算します | モデル選択の主要比較に使いますが、運用閾値の良否とは分けます |\n| 閾値依存指標 | `sens()` / `spec()` / `conf_mat()` は分類ラベルに依存します | CVで決めた閾値をテストデータに適用して評価します |\n| 確率スコア | `brier_class()` は観測クラスと予測確率のずれを要約します | 校正そのものの証明ではなく、キャリブレーション図・校正指標と併読します |\n| 保存済みCV予測 | `collect_predictions()` でCV予測を取り出すには `control_grid(save_pred = TRUE)` が必要です | 閾値選択をテストデータで行わないため、チューニング時にCV予測を保存します |\n\n: Official Specifications That Affect the Interpretation of Classification Metrics\n\n### 混同行列の確認（最小例）\n\nここで一度だけ、ROCの土台になる混同行列を小さな例で確認します。\n\n\n**このチャンクの目的:** 閾値（0.40 vs 0.60）を変えると混同行列がどう変化するかを、6件のミニデータで確認します。`evaluate_threshold()` 関数はこの後の本番分析でも再利用します。\n\n::: {.cell}\n\n```{.r .cell-code}\n# 以降の章でも使える、閾値ごとの基本指標関数\nevaluate_threshold <- function(truth, prob, threshold) {\n  safe_divide <- function(num, den) {\n    if (den > 0) num / den else NA_real_\n  }\n\n  pred <- factor(\n    if_else(prob >= threshold, \"correct\", \"incorrect\"),\n    levels = levels(truth)\n  )\n\n  tp <- sum(pred == \"correct\" & truth == \"correct\")\n  fp <- sum(pred == \"correct\" & truth == \"incorrect\")\n  tn <- sum(pred == \"incorrect\" & truth == \"incorrect\")\n  fn <- sum(pred == \"incorrect\" & truth == \"correct\")\n\n  tibble(\n    threshold = threshold,\n    TP = tp, FP = fp, TN = tn, FN = fn,\n    Sensitivity = safe_divide(tp, tp + fn),\n    Specificity = safe_divide(tn, tn + fp),\n    Precision = safe_divide(tp, tp + fp)\n  )\n}\n\nmini_dat <- tibble(\n  truth = factor(\n    c(\"correct\", \"incorrect\", \"correct\", \"incorrect\", \"correct\", \"incorrect\"),\n    levels = c(\"incorrect\", \"correct\")\n  ),\n  pred_prob = c(0.90, 0.80, 0.63, 0.56, 0.42, 0.10)\n)\n\nbind_rows(\n  evaluate_threshold(mini_dat$truth, mini_dat$pred_prob, 0.40),\n  evaluate_threshold(mini_dat$truth, mini_dat$pred_prob, 0.60)\n) |>\n  mutate(across(c(threshold, Sensitivity, Specificity, Precision), ~ round(.x, 3))) |>\n  mutate(across(c(TP, FP, TN, FN), ~ round(.x, 0))) |>\n  knitr::kable(caption = \"Mini Threshold Example\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Mini Threshold Example\n\n| threshold| TP| FP| TN| FN| Sensitivity| Specificity| Precision|\n|---------:|--:|--:|--:|--:|-----------:|-----------:|---------:|\n|       0.4|  3|  2|  1|  0|       1.000|       0.333|     0.600|\n|       0.6|  2|  1|  2|  1|       0.667|       0.667|     0.667|\n\n\n:::\n:::\n\n\nこの表の読み方:\n\n- 閾値を下げる（0.60 → 0.40）と、拾える正解者（Sensitivity）は上がりやすくなります。\n- その代わり、誤って正解判定する人数（FP）が増えることがあります。\n\n---\n\n## ケース設定：英語語彙学習データ（架空）\n\n::: {.callout-tip}\n### 概要\n6つの学習者特徴（習熟度・学習時間・不安など）と5つの語彙特性（頻度・語長など）で正答/誤答を予測する架空データを作成します。3人の典型的な学習者プロフィール（Case A/B/C）を設定し、全モデルで追跡します。\n:::\n\nこの節の目的は、モデル比較の基盤となるデータ構造を明確にすることです。\n\n### 研究場面の想定\n\n- 対象: 大学生のL2語彙学習\n- 目的変数: 項目正答（`correct` / `incorrect`）\n- 説明変数: 学習者要因 + 語彙項目要因 + 学習条件\n\n回帰分析の知識がある方は、次のように捉えると理解しやすいです。\n\n- ロジスティック回帰: 係数で線形に説明しやすい\n- ツリーモデル: 非線形・相互作用を自然に拾いやすい\n\n::: {.callout-note}\n### 変数を教育文脈で言い換える\n- `proficiency`: 習熟度（高いほど正答しやすい想定）\n- `weekly_study`: 週学習時間（行動指標）\n- `anxiety`: テスト不安（高いほど不利な想定）\n- `word_freq` / `word_length`: 語彙項目の難しさ\n- `condition`: 指導法（glossing / retrieval / sentence）\n:::\n\n### データ生成\n\n\n**このチャンクの目的:** 220人の学習者 × 120語彙項目から3,500観測を抽出し、習熟度・学習条件・語彙特性を含む架空データを生成します。交互作用（`proficiency × condition`）も組み込んでいます。`learner_id` と `item_id` は予測子としては使いませんが、train/test 分割と交差検証でリークを点検するために保持します。\n\n::: {.cell}\n\n```{.r .cell-code}\nn_learners <- 220\nn_items <- 120\n\nlearner_tbl <- tibble(\n  learner_id = factor(1:n_learners),\n  proficiency = rnorm(n_learners, 0, 1),\n  weekly_study = pmax(rnorm(n_learners, 4, 1.5), 0.5),\n  anxiety = rnorm(n_learners, 0, 1),\n  l1_distance = runif(n_learners, 0, 1),\n  u_learner = rnorm(n_learners, 0, 0.55)\n)\n\nitem_tbl <- tibble(\n  item_id = factor(1:n_items),\n  word_freq = rlnorm(n_items, 4.3, 0.8),\n  word_length = sample(3:12, n_items, replace = TRUE),\n  cognate = factor(rbinom(n_items, 1, 0.25), labels = c(\"no\", \"yes\")),\n  pos = factor(sample(c(\"noun\", \"verb\", \"adjective\"), n_items, replace = TRUE,\n                      prob = c(0.45, 0.35, 0.20))),\n  u_item = rnorm(n_items, 0, 0.45)\n)\n\n# learner-item の全組み合わせから一部を観測\nobs <- crossing(learner_id = learner_tbl$learner_id,\n                item_id = item_tbl$item_id) |>\n  slice_sample(n = 3500)\n\ndf <- obs |>\n  left_join(learner_tbl, by = \"learner_id\") |>\n  left_join(item_tbl, by = \"item_id\") |>\n  mutate(\n    condition = factor(sample(c(\"glossing\", \"retrieval\", \"sentence\"), n(), replace = TRUE,\n                              prob = c(0.40, 0.35, 0.25))),\n    test_time = factor(sample(c(\"immediate\", \"delayed\"), n(), replace = TRUE,\n                              prob = c(0.62, 0.38))),\n    # 最小限の式で「それらしい」難易度構造を生成\n    eta = -1.45 +\n      0.95 * proficiency +\n      0.10 * weekly_study -\n      0.35 * anxiety -\n      0.30 * l1_distance +\n      0.38 * log10(word_freq) -\n      0.12 * word_length +\n      0.55 * (cognate == \"yes\") +\n      0.42 * (condition == \"retrieval\") +\n      0.20 * (condition == \"sentence\") -\n      0.33 * (test_time == \"delayed\") +\n      0.25 * proficiency * (condition == \"retrieval\") -\n      0.18 * (condition == \"retrieval\") * (test_time == \"delayed\") +\n      u_learner + u_item,\n    p_correct = plogis(eta),\n    correct = factor(\n      if_else(rbinom(n(), 1, p_correct) == 1, \"correct\", \"incorrect\"),\n      levels = c(\"incorrect\", \"correct\")\n    )\n  ) |>\n  dplyr::select(\n    learner_id, item_id, correct,\n    proficiency, weekly_study, anxiety, l1_distance,\n    word_freq, word_length, cognate, pos, condition, test_time\n  )\n\ndf |>\n  slice_head(n = 10) |>\n  knitr::kable(caption = \"Data Overview (first 10 rows)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Data Overview (first 10 rows)\n\n|learner_id |item_id |correct   | proficiency| weekly_study|    anxiety| l1_distance| word_freq| word_length|cognate |pos       |condition |test_time |\n|:----------|:-------|:---------|-----------:|------------:|----------:|-----------:|---------:|-----------:|:-------|:---------|:---------|:---------|\n|86         |45      |correct   |   0.6859242|     2.976989|  0.2685474|   0.0279090|  58.87584|           7|yes     |noun      |sentence  |immediate |\n|125        |19      |incorrect |   0.1713378|     4.206497| -1.0846678|   0.3185702| 106.44134|           7|no      |verb      |glossing  |immediate |\n|80         |117     |correct   |   0.1998514|     3.270444| -0.1530840|   0.5171248|  53.50617|           8|no      |adjective |sentence  |delayed   |\n|149        |114     |incorrect |   0.0143376|     2.137921|  0.2257182|   0.5970880|  43.22412|          12|no      |verb      |glossing  |immediate |\n|59         |84      |correct   |   0.7112028|     2.790166| -0.1339497|   0.8665114|  83.45225|           3|yes     |verb      |glossing  |delayed   |\n|211        |51      |incorrect |  -0.8565452|     3.602831| -0.5379952|   0.0810203| 188.30095|           4|no      |verb      |sentence  |immediate |\n|220        |82      |incorrect |   0.6354104|     4.757386| -0.5924288|   0.6054017|  80.60368|           8|no      |verb      |sentence  |immediate |\n|116        |86      |incorrect |   0.8152660|     3.744053|  1.1434748|   0.4242004| 134.71138|           8|no      |noun      |sentence  |immediate |\n|121        |116     |incorrect |  -0.7493368|     1.214561|  1.1036247|   0.7664044|  85.15485|          11|no      |verb      |sentence  |immediate |\n|26         |93      |incorrect |   0.7287663|     4.352512| -0.5625738|   0.6469593|  25.37387|           7|no      |noun      |glossing  |delayed   |\n\n\n:::\n:::\n\n\nこのテーブルは「変数型や水準が想定どおりか」を確認するための一次チェックです。ここで `correct` が2水準のfactorになっていること、`condition` や `pos` が文字列ではなくカテゴリとして扱われることを確認しておくと、後の学習工程で不要なトラブルを避けられます。\n\n### データの概観\n\n\n**このチャンクの目的:** 目的変数（correct / incorrect）の比率を確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\ndf |>\n  summarise(\n    n = n(),\n    learners = n_distinct(learner_id),\n    items = n_distinct(item_id),\n    correct_rate = mean(correct == \"correct\"),\n    incorrect_rate = mean(correct == \"incorrect\")\n  ) |>\n  mutate(across(c(correct_rate, incorrect_rate), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Outcome Distribution\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Outcome Distribution\n\n|    n| learners| items| correct_rate| incorrect_rate|\n|----:|--------:|-----:|------------:|--------------:|\n| 3500|      220|   120|        0.321|          0.679|\n\n\n:::\n:::\n\n\nここでは、目的変数の不均衡が極端でないかを確認します。正答率が極端に低い/高い場合は、後半の精度評価でROC-AUCだけでは不十分になり、PR-AUCや閾値設定の重要性がさらに高まります。\n\n\n**このチャンクの目的:** 連続説明変数の記述統計量（平均・SD）を一覧します。\n\n::: {.cell}\n\n```{.r .cell-code}\ndf |>\n  dplyr::select(proficiency, weekly_study, anxiety, l1_distance, word_freq, word_length) |>\n  summarise(across(everything(), list(mean = mean, sd = sd))) |>\n  pivot_longer(everything(), names_to = \"stat\", values_to = \"value\") |>\n  separate(stat, into = c(\"variable\", \"index\"), sep = \"_(?=[^_]+$)\") |>\n  pivot_wider(names_from = index, values_from = value) |>\n  mutate(across(c(mean, sd), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Descriptive Statistics for Continuous Predictors\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Descriptive Statistics for Continuous Predictors\n\n|variable     |   mean|     sd|\n|:------------|------:|------:|\n|proficiency  | -0.068|  0.917|\n|weekly_study |  4.057|  1.385|\n|anxiety      |  0.009|  1.020|\n|l1_distance  |  0.482|  0.279|\n|word_freq    | 96.938| 78.726|\n|word_length  |  7.180|  2.731|\n\n\n:::\n:::\n\n\n連続変数のスケール差（例: `word_freq` と `anxiety`）は、モデルの学習挙動に影響します。本章では `word_freq` に対数変換を入れて分布の歪みを抑え、モデル間比較が公平になるようにしています。\n\n### 具体例: 3人の学習者プロフィール\n\nこの後のモデル比較でイメージしやすいように、典型的な3ケースを先に置いておきます。\n\n\n**このチャンクの目的:** 高習熟/低習熟/中習熟の3ケース（Case A/B/C）を定義します。後のモデル比較で追跡対象となるプロフィールです。\n\n::: {.cell}\n\n```{.r .cell-code}\nprofile_tbl <- tibble(\n  case = c(\n    \"Case A: 高習熟 + retrieval + 即時\",\n    \"Case B: 低習熟 + glossing + 遅延\",\n    \"Case C: 中習熟 + sentence + 遅延\"\n  ),\n  proficiency = c(1.2, -0.8, 0.1),\n  weekly_study = c(6.0, 2.0, 3.5),\n  anxiety = c(-0.5, 0.8, 0.2),\n  word_freq = c(140, 20, 55),\n  word_length = c(5, 9, 7),\n  condition = c(\"retrieval\", \"glossing\", \"sentence\"),\n  test_time = c(\"immediate\", \"delayed\", \"delayed\")\n)\n```\n:::\n\n\n| case | proficiency | weekly_study | anxiety | word_freq | word_length | condition | test_time |\n|---|---:|---:|---:|---:|---:|---|---|\n| Case A: 高習熟 + retrieval + 即時 | 1.2 | 6.0 | -0.5 | 140 | 5 | retrieval | immediate |\n| Case B: 低習熟 + glossing + 遅延 | -0.8 | 2.0 | 0.8 | 20 | 9 | glossing | delayed |\n| Case C: 中習熟 + sentence + 遅延 | 0.1 | 3.5 | 0.2 | 55 | 7 | sentence | delayed |\n\n: Three Concrete Learner Cases\n\nこの3ケースは、単なる例示ではなく「比較の物差し」です。後で4モデルの予測確率を並べるとき、同じ入力に対して各モデルがどれだけ異なる判断をするかを直感的に確認できます。\n\n---\n\n## 学習・評価の分割（リーク防止）\n\n最初に固定する原則は3つです。データを 8:2 に分け、テスト側は最終評価まで触らないこと、前処理（対数化・ダミー化）は訓練データだけで計算すること、ハイパーパラメータのチューニングは訓練データ内の交差検証で行うことです。さらに、学習者×項目データでは「どの単位を未知データとして評価したいか」を先に決めます。\n\nこの節の目的は、「評価の信頼性を担保する実験設計」を先に固定することです。\n\n評価したい汎化先に応じて、分割すべき単位は変わります。\n\n| 評価したい問い | 分割単位 | 解釈 |\n|---|---|---|\n| 未知の学習者に対して予測できるか | `learner_id` | 本章の主ケースです。新しい受講者への予測性能を評価します。 |\n| 未知の項目に対して予測できるか | `item_id` | 新しい語彙・問題への予測性能を評価します。 |\n| 未知の学習者かつ未知の項目に対して予測できるか | `learner_id` と `item_id` の二重ホールドアウト | 最も厳しい設定です。新しい受講者が新しい項目に答える場面を評価します。 |\n| 既知の学習者・既知の項目の未観測行を補間できるか | 行単位 | 反復構造をまたぐため楽観的になりやすいです。主報告には使いにくいです。 |\n\n### データ分割\n\n\n**このチャンクの目的:** `learner_id` 単位で 8:2 に train/test 分割し、未知学習者への汎化性能を評価します。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2024)\ndata_split <- rsample::group_initial_split(df, group = learner_id, prop = 0.8)\ntrain_data <- training(data_split)\ntest_data  <- testing(data_split)\n\nsplit_summary <- bind_rows(\n  tibble(\n    dataset = \"train\",\n    n = nrow(train_data),\n    learners = n_distinct(train_data$learner_id),\n    items = n_distinct(train_data$item_id),\n    correct_rate = mean(train_data$correct == \"correct\")\n  ),\n  tibble(\n    dataset = \"test\",\n    n = nrow(test_data),\n    learners = n_distinct(test_data$learner_id),\n    items = n_distinct(test_data$item_id),\n    correct_rate = mean(test_data$correct == \"correct\")\n  )\n)\n\nsplit_summary |>\n  mutate(correct_rate = percent(correct_rate, accuracy = 0.1)) |>\n  knitr::kable(caption = \"Train/Test Split (grouped by learner_id)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Train/Test Split (grouped by learner_id)\n\n|dataset |    n| learners| items|correct_rate |\n|:-------|----:|--------:|-----:|:------------|\n|train   | 2805|      177|   120|31.8%        |\n|test    |  695|       43|   120|33.5%        |\n\n\n:::\n\n```{.r .cell-code}\nsplit_overlap_tbl <- tibble(\n  Check = c(\"Learners appearing in both train and test\", \"Items appearing in both train and test\"),\n  N = c(\n    length(intersect(train_data$learner_id, test_data$learner_id)),\n    length(intersect(train_data$item_id, test_data$item_id))\n  ),\n  Interpretation = c(\n    \"0なら未知学習者への評価としてリークなし\",\n    \"未知項目への評価ではありません。未知項目を評価したい場合は item_id で分割します\"\n  )\n)\n\nsplit_overlap_tbl |>\n  knitr::kable(caption = \"Leakage Check for Group-Aware Splitting\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Leakage Check for Group-Aware Splitting\n\n|Check                                     |   N|Interpretation                                                                  |\n|:-----------------------------------------|---:|:-------------------------------------------------------------------------------|\n|Learners appearing in both train and test |   0|0なら未知学習者への評価としてリークなし                                         |\n|Items appearing in both train and test    | 120|未知項目への評価ではありません。未知項目を評価したい場合は item_id で分割します |\n\n\n:::\n:::\n\n\nここでは「未知の学習者に対して予測できるか」を主な評価対象にするため、同じ `learner_id` が train と test の両方に入らないようにしています。項目は train/test の両方に出るため、この分割は「未知項目への汎化」を評価するものではありません。未知項目への汎化を評価したい場合は、同じ考え方で `item_id` 単位の分割を使います。未知学習者かつ未知項目への汎化まで主張したい場合は、学習者と項目の両方を分ける二重ホールドアウトが必要です。ただし、データが薄くなりやすいため、標本数とクラス比の確認をより慎重に行います。\n\ngroup split は独立性の確保を優先するため、行単位の層化分割ほど train/test の正答率が揃うとは限りません。そのため、分割後の `correct_rate` を必ず確認し、極端にずれている場合は乱数 seed、分割比率、標本サイズを再点検します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nitem_split <- rsample::group_initial_split(df, group = item_id, prop = 0.8)\n```\n:::\n\n\n**たとえ話で理解する分割**:\n- **train**: これまでの受講者の解答ログ\n- **test**: モデルには見せていない新しい受講者の解答ログ\n\ntrain の点数だけ高くても、test で下がるなら実務では使いにくいモデルです。\n\n`rsample::group_initial_split()` は、同じグループが train/test の両方に入らないように分割する関数です。教育データでは、行単位のランダム分割よりも「学習者単位」「項目単位」「クラス単位」のどれで未知データを作るかを明示する方が、評価の意味が明確になります。参考: [rsample: group_initial_split()](https://rsample.tidymodels.org/reference/group_initial_split.html)\n\n公式ドキュメント上も、`group_initial_split()` や `group_vfold_cv()` は「グループを分割単位にする」ための関数です。ただし、学習者と項目が交差しているデータでは、1回のグループ分割で両方の未知性を同時に保証できるとは限りません。本章では主分析を「未知学習者への汎化」として `learner_id` で分割し、未知項目への汎化は補助的な感度分析として別に `item_id` 分割を確認する設計にしています。これは、予測性能を高く見せるためではなく、**どの母集団へ一般化したいか**を評価設計に対応させるためです。参考: [rsample: group_vfold_cv()](https://rsample.tidymodels.org/reference/group_vfold_cv.html)\n\n### 前処理レシピ\n\n- ツリーモデル用: 最低限の前処理（`word_freq` を対数化）\n- XGBoost用: 追加でダミー化（数値入力が必要）\n\n\n**このチャンクの目的:** 3種類の前処理レシピ（ツリー用・XGBoost用・GLM用）を定義します。共通の `rec_tree` を土台にし、必要に応じてダミー化を追加する設計です。\n\n::: {.cell}\n\n```{.r .cell-code}\nrec_tree <- recipe(\n  correct ~ proficiency + weekly_study + anxiety + l1_distance +\n    word_freq + word_length + cognate + pos + condition + test_time,\n  data = train_data\n) |>\n  step_log(word_freq, base = 10, offset = 1)\n\nrec_xgb <- rec_tree |>\n  step_dummy(all_nominal_predictors())\n\nrec_glm <- rec_tree |>\n  step_dummy(all_nominal_predictors())\n```\n:::\n\n\n`rec_tree` を共通土台にし、`rec_xgb` と `rec_glm` で必要な差分だけ追加する設計にすると、モデル間で「前処理の違いが性能差の原因になる」リスクを最小化できます。\n\n`step_log(..., offset = 1)` はゼロを含むカウント変数を安全に対数変換するための実務定番です。`step_dummy()` は既定で \\(C-1\\) 列化（参照カテゴリあり）で、GLMではこの仕様で係数解釈がしやすくなります。parsnip の `xgboost` エンジンは最終的に数値行列入力を想定するため、カテゴリ変数はダミー化が必要です。参考: [recipes: step_log()](https://recipes.tidymodels.org/reference/step_log.html), [recipes: step_dummy()](https://recipes.tidymodels.org/reference/step_dummy.html), [parsnip: boost_tree() with xgboost](https://parsnip.tidymodels.org/reference/details_boost_tree_xgboost.html)\n\n::: {.callout-warning}\n### 重要: 前処理のfit先\n`recipe`の`prep`は必ず学習データ側で行います。テストデータ情報を前処理に混ぜるとリーク（data leakage）になります。リークとは、テストデータの情報が学習過程に漏れ込むことです。期末テストの問題を事前に見てしまうのと同じで、見かけの成績は上がりますが、本当の実力（汎化性能）は測れなくなります。\n:::\n\n**ハイパーパラメータとは**、モデルがデータから学ぶ前に人間が決める設定値です。たとえば「木の最大深さ」「各分岐に必要な最小サンプル数」「学習率」などがこれにあたります。ロジスティック回帰ではこの概念はほとんど意識しませんが、ツリーモデルでは **ハイパーパラメータの設定が性能を大きく左右します**。最適値は交差検証で探索します（後述の `tune_grid`）。\n\n---\n\n## ベースライン：ロジスティック回帰\n\n読者の既存知識（回帰分析）に接続するため、まずGLMをベースラインに置きます。\n\nここでの位置づけは「比較の出発点」です。勝者を決めるためではなく、ツリーモデルの上積みを判断する基準にします。GLMは1つの式で全データを説明するため、非線形や交互作用を手動で指定しない限り、ツリーモデルに比べて柔軟性が低い側面があります。逆に言えば、GLMで十分な性能が出るなら、あえて複雑なモデルを使う必要はありません。\n\n\n**このチャンクの目的:** ロジスティック回帰をベースラインとして推定し、係数表を出力します。\n\n::: {.cell}\n\n```{.r .cell-code}\nglm_spec <- logistic_reg() |>\n  set_engine(\"glm\")\n\nglm_wf <- workflow() |>\n  add_recipe(rec_glm) |>\n  add_model(glm_spec)\n\nglm_fit <- fit(glm_wf, data = train_data)\n\nglm_fit |>\n  extract_fit_parsnip() |>\n  tidy() |>\n  mutate(across(where(is.numeric), ~ round(., 3))) |>\n  knitr::kable(caption = \"Baseline Logistic Regression: Coefficients on log-odds scale\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Baseline Logistic Regression: Coefficients on log-odds scale\n\n|term                | estimate| std.error| statistic| p.value|\n|:-------------------|--------:|---------:|---------:|-------:|\n|(Intercept)         |   -1.537|     0.364|    -4.220|   0.000|\n|proficiency         |    1.041|     0.063|    16.659|   0.000|\n|weekly_study        |    0.094|     0.035|     2.723|   0.006|\n|anxiety             |   -0.365|     0.046|    -7.883|   0.000|\n|l1_distance         |   -0.483|     0.167|    -2.897|   0.004|\n|word_freq           |    0.326|     0.140|     2.320|   0.020|\n|word_length         |   -0.130|     0.017|    -7.471|   0.000|\n|cognate_yes         |    0.324|     0.107|     3.025|   0.002|\n|pos_noun            |    0.121|     0.130|     0.924|   0.355|\n|pos_verb            |    0.124|     0.132|     0.936|   0.349|\n|condition_retrieval |    0.448|     0.108|     4.155|   0.000|\n|condition_sentence  |    0.310|     0.121|     2.566|   0.010|\n|test_time_immediate |    0.283|     0.098|     2.893|   0.004|\n\n\n:::\n:::\n\n\n係数表は log-odds スケールなので、値そのものを確率差として読むことはできません。方向（正/負）をまず確認し、効果量の比較はこの後のDominance分析や予測確率の比較で補完します。\n\n### 補助解析：Dominance Analysis（GLM）\n\n係数表は「方向と有意性」を示すのに有用ですが、変数間でスケールが異なるため「どの変数が最も重要か」の比較は直接できません。標準化係数を使う方法もありますが、変数間の相関が強い場合は不安定です。Dominance analysisは、すべてのサブモデルの組み合わせを比較することでこの問題を解決します[@budescu1993; @azen2003; @azen2009]。\n\n文献での活用ポイント（具体）:\n\n- @azen2009 は、ロジスティック回帰で dominance analysis を使うために、\n  R2類似指標の選定とブートストラップ推論を組み合わせて、重要度の再現性を検討しました。\n- @mizumoto2022 は、応用言語学データの再解析で dominance analysis を併用し、\n  「標準化βだけでは見えにくい相対的重要性」を示しました。  \n- 本章での具体例としては、`proficiency` と `condition` の relative share を比較し、  \n  「学習者要因と授業デザイン要因のどちらが予測改善に効いているか」を同じ尺度で読む、という使い方になります。  \n\n以下のコードでは、(1) 通常のglmオブジェクトを作成し、(2) `dominanceanalysis`パッケージで全てのサブモデルの組み合わせにおける各変数の平均的な改善量を計算し、(3) Nagelkerke R2ベースの結果を表にします。\n\n\n**このチャンクの目的:** Dominance analysisで、GLMの各説明変数の相対的重要性（Nagelkerke R²ベース）を算出します。\n\n::: {.cell}\n\n```{.r .cell-code}\n# dominance analysis は通常の glm オブジェクトで実行する\nglm_dom <- glm(\n  correct ~ proficiency + weekly_study + anxiety + l1_distance +\n    log10(word_freq + 1) + word_length + cognate + pos + condition + test_time,\n  data = train_data,\n  family = binomial()\n)\n\nda_glm <- dominanceanalysis::dominanceAnalysis(glm_dom)\nda_avg <- dominanceanalysis::averageContribution(da_glm)\n\nda_tbl <- purrr::imap_dfr(da_avg, ~ tibble(\n  Fit_Index = .y,\n  Predictor = names(.x),\n  Average_Contribution = as.numeric(.x)\n)) |>\n  group_by(Fit_Index) |>\n  mutate(\n    Relative = Average_Contribution / sum(Average_Contribution),\n    Rank = dense_rank(desc(Average_Contribution))\n  ) |>\n  ungroup()\n\n# 章内では Nagelkerke R2 ベース（r2.n）を主に表示\nda_tbl |>\n  dplyr::filter(Fit_Index == \"r2.n\") |>\n  arrange(desc(Average_Contribution)) |>\n  mutate(across(c(Average_Contribution, Relative), ~ round(., 4))) |>\n  rename(\n    `Fit Index` = Fit_Index,\n    Predictor = Predictor,\n    `Average Contribution` = Average_Contribution,\n    `Relative Share` = Relative,\n    Rank = Rank\n  ) |>\n  knitr::kable(caption = \"Dominance Analysis (GLM): Relative importance based on Nagelkerke R2 (r2.n)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Dominance Analysis (GLM): Relative importance based on Nagelkerke R2 (r2.n)\n\n|Fit Index |Predictor            | Average Contribution| Relative Share| Rank|\n|:---------|:--------------------|--------------------:|--------------:|----:|\n|r2.n      |proficiency          |               0.1162|         0.6614|    1|\n|r2.n      |anxiety              |               0.0232|         0.1319|    2|\n|r2.n      |word_length          |               0.0194|         0.1104|    3|\n|r2.n      |condition            |               0.0056|         0.0317|    4|\n|r2.n      |l1_distance          |               0.0024|         0.0134|    5|\n|r2.n      |cognate              |               0.0022|         0.0126|    6|\n|r2.n      |log10(word_freq + 1) |               0.0021|         0.0117|    7|\n|r2.n      |test_time            |               0.0021|         0.0117|    8|\n|r2.n      |weekly_study         |               0.0020|         0.0116|    9|\n|r2.n      |pos                  |               0.0006|         0.0035|   10|\n\n\n:::\n:::\n\n\n`Relative` は「説明力への寄与割合」を0〜1で表した指標です。上位変数の合計がどれくらいを占めるかを見ると、少数の主要因で説明できる問題か、複数要因の累積で説明される問題かを判定しやすくなります。\n\n\n**このチャンクの目的:** Dominance重要度の棒グラフを描画します。\n\n::: {.cell}\n\n```{.r .cell-code}\nda_tbl |>\n  dplyr::filter(Fit_Index == \"r2.n\") |>\n  arrange(desc(Average_Contribution)) |>\n  ggplot(aes(x = reorder(Predictor, Average_Contribution),\n             y = Average_Contribution)) +\n  geom_col(fill = \"#4C78A8\") +\n  coord_flip() +\n  labs(\n    title = \"Dominance Analysis for Logistic Baseline\",\n    subtitle = \"Higher values indicate stronger average incremental contribution\",\n    x = \"Predictor\",\n    y = \"Average contribution (r2.n)\"\n  )\n```\n\n::: {.cell-output-display}\n![Dominance ranking of predictors (GLM, r2.n)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/glm-dominance-plot-1.png){width=2700}\n:::\n:::\n\n\n**このチャンクの目的:** サブモデルサイズ別の寄与変化を積み上げ棒グラフで表示します。\n\n::: {.cell}\n\n```{.r .cell-code}\nda_level_tbl <- dominanceanalysis::contributionByLevel(da_glm)$r2.n |>\n  as_tibble() |>\n  pivot_longer(-level, names_to = \"Predictor\", values_to = \"Contribution\")\n\nda_level_tbl |>\n  ggplot(aes(x = factor(level), y = Contribution, fill = Predictor)) +\n  geom_col(position = \"stack\") +\n  labs(\n    title = \"Contribution by Model Size\",\n    subtitle = \"level = number of predictors already in the submodel\",\n    x = \"Model size level\",\n    y = \"Incremental contribution\"\n  ) +\n  guides(fill = \"none\")\n```\n\n::: {.cell-output-display}\n![Dominance contribution by model size (GLM, r2.n)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/glm-dominance-level-1.png){width=2700}\n:::\n:::\n\n\nlevel別プロットは「モデルが複雑になるほど、どの変数の追加価値が維持されるか」を見る図です。初期levelでだけ効く変数は代替可能性が高く、後半levelでも寄与が残る変数は独自情報を持つと解釈できます。\n\n### Dominance と SHAP の役割分担\n\n- Dominance（ここ）: **GLM全体**で、説明変数の相対的重要性を比較します。\n- SHAP（後半）: **XGBoost**で、全体と個別予測の寄与を説明します。\n\n#### Mizumoto (2022) スタイルの最小実装\n\nMizumoto (2022) の流れを本文に沿って最短で再現するなら、`glm()` を直接作って `dominanceAnalysis()` を当てれば十分です。章末に長い付録を置くより、ここで「最小限どこを変えればよいか」を確認した方が再利用しやすくなります。\n\n```r\n# Mizumoto (2022) style: minimal GLM + dominance script\nglm_miz <- glm(\n  correct ~ proficiency + weekly_study + anxiety + l1_distance +\n    log10(word_freq + 1) + word_length + cognate + pos + condition + test_time,\n  data = train_data,\n  family = binomial()\n)\n\nda_miz <- dominanceanalysis::dominanceAnalysis(glm_miz)\n\ndominanceanalysis::averageContribution(da_miz)$r2.n |>\n  tibble::enframe(name = \"Predictor\", value = \"Average_Contribution\") |>\n  dplyr::arrange(dplyr::desc(Average_Contribution))\n```\n\nこのテンプレートでは、変更すべき箇所は `formula` と `data` だけです。`r2.n` を使えば、本章と同じ Nagelkerke R² ベースの dominance を再現できます。\n\n### GLMの限界とツリーモデルへの動機\n\nGLMは全データに1つの式をあてはめるため、交互作用を捉えるには `proficiency:condition` のように手動で指定する必要があります。変数が多い場合、すべての交互作用を事前に列挙するのは現実的ではありません。次節以降のツリーモデルは、この**交互作用候補の探索**をデータ駆動で行う点が根本的に異なります。\n\n---\n\n## 決定木（Decision Tree）\n\n### 概要\n\n決定木は「もし proficiency > 0.4 ならば…」のようなif-thenルールの積み重ねです。1本見れば予測根拠が明確にわかる一方、1本の木はデータの揺れに弱い（高分散）という弱点があります。\n\nこの節の目的は、解釈可能性の高い木モデルを構築し、分岐規則を確認することです。\n\n### 決定木の基本構造\n\n- 決定木は、データを「条件分岐」で繰り返し分けるモデルです。  \n- 分岐点（ノード）では、例: `proficiency > 0.4` のような質問をします。  \n- 最後の葉（leaf）で、正答確率やクラス（correct / incorrect）を出します。  \n- 良い点: ルールが可視化でき、教育的解釈がしやすいです。\n- 注意点: 1本の木はデータの小さな揺れに影響されやすいです。\n\n英語教育の文脈では、「どの条件の学習者が正答しやすいか」をif-thenルールで説明できる点が、決定木の大きな利点です。分岐ロジックの詳細（Gini不純度による分割選択）は、先の「回帰から機械学習への橋渡し」で解説しました。ここではその仕組みを実際の教育データに適用し、ハイパーパラメータのチューニングから可視化までを行います。\n\n### 決定木のハイパーパラメータ\n\n決定木には、木の「形」を制御する3つの主要なハイパーパラメータがあります。\n\n\n| Parameter | `tidymodels` 名 | 意味 | 値が大きいと | 値が小さいと |\n|---|---|---|---|---|\n| コスト複雑度 | `cost_complexity` | 木を刈り込む度合い。分岐1つあたりのペナルティ | 単純な木（高バイアス、低分散） | 複雑な木（低バイアス、高分散） |\n| 最大深さ | `tree_depth` | 根から葉までの最大ステップ数 | 表現力は高いがデータのノイズも拾う | 単純だが大まかなルールしか捉えない |\n| 最小ノードサイズ | `min_n` | 分岐に必要な最小観測数 | 慎重な分岐（過学習を抑制） | 細かい分岐（データの揺れに敏感） |\n\n: Decision Tree Hyperparameters\n\nこれらの最適値は、交差検証（CV）で探索します。`tune_grid` は、指定したグリッドの全組み合わせで3-fold CVを実行し、各設定のROC-AUCを比較して最良の組み合わせを選びます。\n\n### モデルとチューニング\n\nまずは「深すぎる木を避ける」ことを意識して、単純な探索から始めます。\n\n`grid_regular`は各ハイパーパラメータの範囲を等間隔に区切ってグリッドを作ります。`levels = c(3, 2, 2)`は、cost_complexityを3段階、tree_depthを2段階、min_nを2段階に設定し、計3x2x2=12通りの組み合わせを試すという意味です。\n\n\n**このチャンクの目的:** 同じ `learner_id` を複数foldに分けない3-fold CVで決定木のハイパーパラメータを12通り探索し、最良設定を選択します。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2024)\nfolds <- rsample::group_vfold_cv(train_data, group = learner_id, v = 3)\n\ntree_spec <- decision_tree(\n  cost_complexity = tune(),\n  tree_depth = tune(),\n  min_n = tune()\n) |>\n  set_engine(\"rpart\") |>\n  set_mode(\"classification\")\n\ntree_wf <- workflow() |>\n  add_recipe(rec_tree) |>\n  add_model(tree_spec)\n\ntree_grid <- grid_regular(\n  cost_complexity(range = c(-4, -1)),\n  tree_depth(range = c(2L, 8L)),\n  min_n(range = c(10L, 120L)),\n  levels = c(3, 2, 2)\n)\n\nset.seed(2024)\ntree_res <- tune_grid(\n  tree_wf,\n  resamples = folds,\n  grid = tree_grid,\n  metrics = metric_set(roc_auc, pr_auc)\n)\n\ntree_best <- select_best(tree_res, metric = \"roc_auc\")\ntree_best\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 4\n  cost_complexity tree_depth min_n .config         \n            <dbl>      <int> <int> <chr>           \n1          0.0001          8   120 pre0_mod04_post0\n```\n\n\n:::\n:::\n\n\nこの `folds` は訓練データの内部だけで作られます。各foldでは、同じ学習者の行が分析側と評価側に分かれないため、ハイパーパラメータ探索の段階でも「未知学習者への汎化」という最終評価の問いと整合します。なお、CV内の評価foldはチューニング用の検証データで、最終テストデータとは別物です。\n\nこの出力で得られる `cost_complexity`, `tree_depth`, `min_n` は、以降の最終木を再現するための確定値です。論文では「探索範囲」「CV設定」「採用値」の3点をセットで記載すると再現性が上がります。\n\n`tune_grid` の結果を読むときは、最良設定1点だけでなく「次点との差」も確認します。本章のコードはROC-AUC最大の設定を自動採用しますが、上位2〜3設定のROC-AUCがほぼ同じなら、より単純な設定（浅い木・大きい `min_n`・大きい `cost_complexity`）を採用候補にする方が、過学習リスクを下げながら解釈可能性を保ちやすくなります。\n\nparsnip の `decision_tree(engine = \"rpart\")` では、`cost_complexity` が `cp` に対応し、木の刈り込み強度を決めます。`tree_depth` は `maxdepth`、`min_n` は `minsplit` に対応し、木の複雑さと安定性を制御します。参考: [parsnip: decision_tree() with rpart](https://parsnip.tidymodels.org/reference/details_decision_tree_rpart.html), [rpart: rpart.control](https://stat.ethz.ch/R-manual/R-devel/RHOME/library/rpart/html/rpart.control.html)\n\n::: {.panel-tabset}\n\n### チューニング面\n\n最良設定だけを見ると「どこまでが同等性能か」が見えません。`collect_metrics()` で全候補を取り出し、どの領域が安定して高性能かをヒートマップで確認します。  \nこの図は、**単一点最適化ではなく、再現しやすい領域選択**を行うための可視化です。\n\n\n**このチャンクの目的:** 全パラメータ組み合わせのROC-AUCをヒートマップで描き、安定高性能領域を確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\ntree_tune_roc <- tree_res |>\n  collect_metrics() |>\n  dplyr::filter(.metric == \"roc_auc\") |>\n  mutate(\n    tree_depth = factor(tree_depth),\n    min_n = factor(min_n),\n    cp_label = paste0(\"cp = \", scales::number(cost_complexity, accuracy = 0.0001))\n  )\n\ntree_tune_roc |>\n  ggplot(aes(x = min_n, y = tree_depth, fill = mean)) +\n  geom_tile(color = \"white\") +\n  geom_text(aes(label = round(mean, 3)), size = 3) +\n  facet_wrap(~ cp_label) +\n  scale_fill_viridis_c(name = \"Mean ROC-AUC\", option = \"C\") +\n  labs(\n    title = \"Tree Tuning Heatmap\",\n    subtitle = \"Each tile is one hyperparameter combination\",\n    x = \"min_n\",\n    y = \"tree_depth\"\n  )\n```\n\n::: {.cell-output-display}\n![Decision-tree tuning landscape (mean ROC-AUC over CV folds)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/tree-tuning-heatmap-1.png){width=3000}\n:::\n:::\n\n\nこの図では「最高値」だけでなく「近傍の広さ」を見ます。\n- 高性能タイルが複数連続する領域は、再標本化で再現しやすいです。\n- 単発で高いタイルは偶然上振れの可能性があります。\n- 同等性能なら、より単純な設定（浅い木・大きい `min_n`）を優先しやすいです。\n\n`collect_metrics()` は `tune_grid()` の結果をパラメータごと平均化して返すため、可視化の基礎表として使えます。自動可視化として `autoplot(tune_results, type = \"marginals\")` も利用できますが、本章では意思決定しやすい2次元ヒートマップを手動で作成しています。参考: [tune: collect_metrics()](https://tune.tidymodels.org/reference/collect_predictions.html), [tune: Introduction article](https://tune.tidymodels.org/articles/tune.html)\n\n### 最終学習と可視化\n\n\n**このチャンクの目的:** 最良パラメータで決定木を最終学習し、rpart.plotで木構造を描画します。\n\n::: {.cell}\n\n```{.r .cell-code}\ntree_final_wf <- finalize_workflow(tree_wf, tree_best)\ntree_final_fit <- fit(tree_final_wf, data = train_data)\n\ntree_model <- tree_final_fit |>\n  extract_fit_parsnip() |>\n  pluck(\"fit\")\n\nrpart.plot::rpart.plot(tree_model, type = 2, extra = 104,\n                       tweak = 1.0, under = TRUE, fallen.leaves = TRUE)\n```\n\n::: {.cell-output-display}\n![Final decision tree (pruned via cross-validated tuning)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/finalize-tree-1.png){width=3000}\n:::\n:::\n\n\n木図では、上位ノードの分岐ほど全体予測への影響が大きい点に注意します。根に近い分岐が教育的に納得できるか（例: 習熟度や頻度が先に分かれるか）は、モデル妥当性の定性的チェックとして有効です。\n\n`rpart.plot(..., extra = 104)` では、葉ノードに予測クラスと確率情報が表示されます。実務的には「どの葉が高リスク群か」「その葉のサンプル数が十分か」を同時に見て、希少ケースだけで結論を出していないかを確認します。\n\n### ggparty木構造\n\n`rpart.plot` は実務で非常に見やすい一方、`ggplot` 文法でスタイル統一したい場面もあります。`ggparty` を使うと、木構造そのものを `ggplot2` ベースで描けます。\n\n\n**このチャンクの目的:** ggplot文法（ggparty）で木構造を描画し、他図とスタイル統一します。\n\n::: {.cell}\n\n```{.r .cell-code}\n# parsnip経由モデルはas.party()変換で必要な内部情報が不足する場合があるため、\n# 同条件で可視化専用のrpartモデルを再学習します。\ntree_train_vis <- train_data |>\n  mutate(word_freq = log10(word_freq + 1))\n\ntree_model_gg <- rpart::rpart(\n  correct ~ proficiency + weekly_study + anxiety + l1_distance +\n    word_freq + word_length + cognate + pos + condition + test_time,\n  data = tree_train_vis,\n  method = \"class\",\n  control = rpart::rpart.control(\n    cp = tree_best$cost_complexity,\n    maxdepth = as.integer(tree_best$tree_depth),\n    minsplit = as.integer(tree_best$min_n)\n  )\n)\n\ntree_party <- partykit::as.party(tree_model_gg)\n\nggparty::ggparty(tree_party) +\n  ggparty::geom_edge(linewidth = 0.35, color = \"gray50\") +\n  ggparty::geom_node_splitvar(ids = \"inner\", size = 3) +\n  ggparty::geom_node_info(ids = \"terminal\", size = 2.8) +\n  labs(\n    title = \"Decision Tree (ggparty)\",\n    subtitle = \"Inner nodes: split rules; terminal nodes: class summary\"\n  )\n```\n\n::: {.cell-output-display}\n![Decision tree rendered with ggplot grammar (ggparty)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/tree-ggparty-plot-1.png){width=3300}\n:::\n:::\n\n\n`ggparty` 図は見た目の一貫性と拡張性に強みがあります。\n- `theme()` で章全体の図スタイルに合わせやすいです。\n- ノード情報を追加して、用途別に注釈を作りやすいです。\n- ただし、可読性だけを優先するなら `rpart.plot` の方が簡潔な場合もあります。\n\n`partykit::as.party()` は `rpart` オブジェクトを party 構造に変換する標準手順です。`ggparty` は party オブジェクトを `ggplot2` レイヤで描くため、他の可視化とのデザイン統一に適します。参考: [partykit (CRAN)](https://cran.r-project.org/package=partykit), [ggparty (CRAN)](https://cran.r-project.org/package=ggparty)\n\n### 剪定プロファイル\n\n木構造の図だけでは、どれだけ刈り込みが妥当かは判断しづらいです。`rpart` が内部的に計算した `cptable` を使って、分岐数と誤差の関係を可視化します。ただし、この `cptable` は `rpart` 内部の診断表です。本章の主要なハイパーパラメータ選択は、上で実行した `learner_id` 単位の grouped CV に基づきます。\n\n\n**このチャンクの目的:** rpartのcptableから分岐数と内部診断誤差の関係を描画し、刈り込みの妥当性を補助的に確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\ntree_cp_tbl <- tree_model$cptable |>\n  as.data.frame(check.names = FALSE) |>\n  as_tibble() |>\n  rename_with(~ stringr::str_replace_all(.x, \" \", \"_\"))\n\ntree_cp_tbl |>\n  ggplot(aes(x = nsplit, y = xerror)) +\n  geom_line(color = \"#2C7FB8\", linewidth = 1) +\n  geom_point(color = \"#2C7FB8\", size = 2) +\n  geom_errorbar(\n    aes(ymin = pmax(xerror - xstd, 0), ymax = xerror + xstd),\n    width = 0.15, alpha = 0.7\n  ) +\n  labs(\n    title = \"Pruning Profile of the Fitted Tree\",\n    subtitle = \"Diagnostic only; final tuning used learner-grouped CV\",\n    x = \"Number of splits\",\n    y = \"xerror (internal CV error)\"\n  )\n```\n\n::: {.cell-output-display}\n![Diagnostic pruning profile from rpart cptable](8_予測分析_ROC曲線とツリーモデル_files/figure-html/tree-pruning-profile-1.png){width=2400}\n:::\n:::\n\n\nこの図は「枝を増やすほど良いわけではない」ことを確認するために使います。\n- `nsplit` が増えても `xerror` が改善しない領域は過剰複雑化の候補です。\n- `xstd` が大きい点は不確実性が高く、過信しません。\n- 実務では、最小誤差点の近傍でより単純な木を選ぶ戦略が説明しやすいです。\n- 本章のような反復測定データでは、最終判断は `learner_id` 単位のgrouped CVとheld-out testで行います。\n\n`rpart` には `printcp()` / `plotcp()` があり、CPと内部交差検証誤差の確認が標準ワークフローです。本章は `ggplot2` で同等情報を再描画し、他図との見た目を統一しています。参考: [rpart: plotcp and printcp](https://stat.ethz.ch/R-manual/R-devel/library/rpart/html/plotcp.html)\n\n### 解釈ポイント\n\n決定木の最大の強みは**透明性**です。上の図を見れば、「proficiency が高い → さらに word_freq が大きい → 正答確率が高い」のように、予測の根拠をif-thenルールとして直接読み取れます。これは教育現場で「なぜこの学習者が正答/誤答と判定されたのか」を説明する際に非常に有用です。\n\n一方、単木の限界も明確です:\n\n- **高分散**: データの一部を変えるだけで、木の構造が大きく変わることがあります。\n- **貪欲アルゴリズム**: 各分岐で局所最適を選ぶため、全体最適とは限りません。\n- **外挿不可**: 訓練データの範囲外の値に対して、最も近いリーフの予測をそのまま返します。\n\nしたがって、決定木は「予測の仕組みを説明するモデル」として使い、精度を求める場合は次節以降のアンサンブル手法を検討します。\n\n### 分割頻度\n\n\n**このチャンクの目的:** 最終木のフレーム情報から、分岐に使われた変数の頻度を集計します。\n\n::: {.cell}\n\n```{.r .cell-code}\ntibble(split_var = tree_model$frame$var) |>\n  dplyr::filter(split_var != \"<leaf>\") |>\n  count(split_var, sort = TRUE) |>\n  rename(Variable = split_var, Split_Count = n) |>\n  knitr::kable(caption = \"Decision Tree Split Frequency: Variables repeatedly used for branching\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Decision Tree Split Frequency: Variables repeatedly used for branching\n\n|Variable    | Split_Count|\n|:-----------|-----------:|\n|anxiety     |           3|\n|condition   |           2|\n|proficiency |           2|\n|test_time   |           1|\n|word_length |           1|\n\n\n:::\n:::\n\n\nこの表は、木がどの変数を分岐に使ったかを確認するための基礎資料です。分岐回数は説明用の要約で、Permutation importance や SHAP のような予測性能ベースの重要度とは別物として扱います。\n\n### 葉ノードプロファイル\n\n木図は分岐規則の理解に優れますが、各葉の「人数」と「実測正答率」を同時に見るには別図が便利です。バーを実測率、点をモデル予測確率として重ねると、葉ごとの当てはまりを直感的に確認できます。\n\n\n**このチャンクの目的:** 葉ノード別に実測正答率（バー）とモデル予測確率（点）を対比します。\n\n::: {.cell}\n\n```{.r .cell-code}\ntree_leaf_profile <- tibble(\n  node_id = tree_model$where,\n  truth = train_data$correct,\n  pred_prob = predict(tree_model, type = \"prob\")[, \"correct\"]\n) |>\n  group_by(node_id) |>\n  summarise(\n    n = n(),\n    observed_correct_rate = mean(truth == \"correct\"),\n    predicted_correct_rate = mean(pred_prob),\n    .groups = \"drop\"\n  ) |>\n  arrange(observed_correct_rate) |>\n  mutate(node_id = factor(node_id, levels = node_id))\n\ntree_leaf_profile |>\n  ggplot(aes(x = node_id, y = observed_correct_rate)) +\n  geom_col(fill = \"#1B9E77\", alpha = 0.75) +\n  geom_point(aes(y = predicted_correct_rate), color = \"#D95F02\", size = 2.7) +\n  geom_text(aes(label = paste0(\"n=\", n)), hjust = -0.15, size = 3) +\n  coord_flip() +\n  scale_y_continuous(limits = c(0, 1), breaks = seq(0, 1, 0.2)) +\n  labs(\n    title = \"Leaf-level Profile of the Decision Tree\",\n    subtitle = \"Bars: observed correct rate, points: mean predicted probability\",\n    x = \"Leaf node ID\",\n    y = \"Rate\"\n  )\n```\n\n::: {.cell-output-display}\n![Leaf-level profile: observed rate (bars) vs predicted rate (points)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/tree-leaf-profile-1.png){width=3000}\n:::\n:::\n\n\nこの図は「どの葉が信頼できるか」を点検するために使います。ただし、ここでは学習データ上の葉ノードを見ているため、外部性能の証拠ではなく、最終木の構造診断として読みます。\n- `n` が小さい葉は率が極端でも不安定な可能性があります。\n- バーと点の乖離が大きい葉は、局所的な当てはまり不良の候補です。\n- 教育実務で支援候補を定義する際は、`n` と率を必ずセットで確認します。\n\n### 決定マップ\n\n主要説明変数2つに絞ると、決定木が作る「階段状の判断境界」を平面で可視化できます。ここでは `proficiency` と `word_freq` を軸にし、他の変数は代表値（中央値/最頻値）に固定します。\n\n\n**このチャンクの目的:** proficiency × word_freq の格子上で予測確率をラスター表示し、決定木の矩形境界を可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\nmode_level <- function(x) {\n  names(sort(table(x), decreasing = TRUE))[1]\n}\n\ntree_map_grid <- tidyr::expand_grid(\n  proficiency = seq(min(train_data$proficiency), max(train_data$proficiency), length.out = 120),\n  word_freq = seq(\n    quantile(train_data$word_freq, 0.05),\n    quantile(train_data$word_freq, 0.95),\n    length.out = 120\n  )\n) |>\n  mutate(\n    weekly_study = median(train_data$weekly_study),\n    anxiety = median(train_data$anxiety),\n    l1_distance = median(train_data$l1_distance),\n    word_length = median(train_data$word_length),\n    cognate = factor(mode_level(train_data$cognate), levels = levels(train_data$cognate)),\n    pos = factor(mode_level(train_data$pos), levels = levels(train_data$pos)),\n    condition = factor(mode_level(train_data$condition), levels = levels(train_data$condition)),\n    test_time = factor(mode_level(train_data$test_time), levels = levels(train_data$test_time))\n  )\n\ntree_map_grid$.pred_correct <- predict(tree_final_fit, new_data = tree_map_grid, type = \"prob\")$.pred_correct\n\ntree_map_grid |>\n  ggplot(aes(x = proficiency, y = log10(word_freq + 1), fill = .pred_correct)) +\n  geom_raster(interpolate = FALSE) +\n  scale_fill_viridis_c(name = \"P(correct)\", option = \"C\", limits = c(0, 1)) +\n  labs(\n    title = \"Decision Map (Tree Model)\",\n    subtitle = \"Other predictors fixed at typical values\",\n    x = \"Proficiency\",\n    y = \"log10(word frequency + 1)\"\n  )\n```\n\n::: {.cell-output-display}\n![Decision map of the fitted tree over proficiency and word frequency](8_予測分析_ROC曲線とツリーモデル_files/figure-html/tree-decision-map-1.png){width=3000}\n:::\n:::\n\n\n決定マップでは、境界の形からモデルの性質を読めます。\n- 境界が軸平行の階段状になるのは、木が軸方向に分割するためです。\n- 急激な色変化は、分岐閾値をまたいだ判定切替を示します。\n- 2変数図は説明用に有効ですが、他変数固定の条件付き可視化だという点を本文で明示します。\n\n### 単木の安定性確認\n\n決定木は読みやすい一方で、データの小さな変化に敏感です。そこで、学習者単位でブートストラップ標本を作り、同じ設定で木を繰り返し学習したとき、根ノードにどの変数が選ばれやすいかを確認します。この図は「単木のルールをどれくらい頑健に読めるか」を点検するための補助診断です。\n\n\n**このチャンクの目的:** 学習者単位のブートストラップで単木を繰り返し作り、根ノード分岐の安定性を確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2024)\nn_tree_boot <- 40\nlearner_ids_train <- unique(train_data$learner_id)\n\ntree_root_stability <- map_dfr(seq_len(n_tree_boot), \\(b) {\n  sampled_learners <- tibble(\n    learner_id = sample(\n      learner_ids_train,\n      size = length(learner_ids_train),\n      replace = TRUE\n    ),\n    boot_draw = seq_len(length(learner_ids_train))\n  )\n\n  boot_data <- sampled_learners |>\n    left_join(train_data, by = \"learner_id\") |>\n    mutate(word_freq = log10(word_freq + 1))\n\n  boot_tree <- rpart::rpart(\n    correct ~ proficiency + weekly_study + anxiety + l1_distance +\n      word_freq + word_length + cognate + pos + condition + test_time,\n    data = boot_data,\n    method = \"class\",\n    control = rpart::rpart.control(\n      cp = tree_best$cost_complexity,\n      maxdepth = as.integer(tree_best$tree_depth),\n      minsplit = as.integer(tree_best$min_n)\n    )\n  )\n\n  tibble(\n    draw = b,\n    root_split = boot_tree$frame$var[1]\n  )\n})\n\ntree_root_stability |>\n  count(root_split, sort = TRUE) |>\n  mutate(\n    proportion = n / sum(n),\n    root_split = fct_reorder(root_split, proportion)\n  ) |>\n  ggplot(aes(x = root_split, y = proportion)) +\n  geom_col(fill = \"#4C78A8\") +\n  coord_flip() +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    title = \"Root Split Stability Across Bootstrap Trees\",\n    subtitle = \"Single-tree explanations are more stable when the same root variable appears repeatedly\",\n    x = \"Root split variable\",\n    y = \"Bootstrap proportion\"\n  )\n```\n\n::: {.cell-output-display}\n![Root-split stability of single decision trees under learner-level bootstrap resampling](8_予測分析_ROC曲線とツリーモデル_files/figure-html/tree-root-stability-1.png){width=2400}\n:::\n:::\n\n\n根ノードがほぼ同じ変数に集中していれば、単木の最初の大きな分岐は比較的安定していると読めます。逆に、根ノードが複数変数に分散する場合は、1本の木図だけを強く解釈せず、RFやXGBoostの重要度、SHAP、サブグループ別性能と照合して慎重に報告します。\n\n:::\n\n::: {.callout-note}\n### 可視化の使い分け（決定木）\n1. **木図 (`rpart.plot`)**: ルールそのものを説明します。\n2. **木図 (`ggparty`)**: ggplot文法で木構造を描き、スタイル統一しやすくします。\n3. **チューニング面（ヒートマップ）**: 設定選択の妥当性を示します。\n4. **葉ノードプロファイル**: 各葉の安定性と当てはまりを点検します。\n5. **決定マップ**: 主要2変数で局所ルールを直感的に示します。\n6. **単木の安定性確認**: 学習者単位の再標本化で、根ノード分岐がどれくらい再現するかを確認します。\n:::\n\n決定木は「1人の専門家の判断」に似ています。同じデータを少しだけ入れ替えて木を作り直すと、異なる分岐ルールが出てくることがあります（高分散）。では、多数の専門家にそれぞれ少しずつ異なる資料を渡して判断してもらい、多数決を取るとどうなるでしょうか。個々の判断はばらついても、集約すると安定します。これがランダムフォレストの基本発想です。\n\n---\n\n## ランダムフォレスト（Random Forest）\n\n### 概要\n\n不安定な単木をたくさん作って多数決します。各木は別々のデータ（ブートストラップ）と別々の変数（ランダム選択）で育てるので、木どうしが似すぎず、結果として分散が大幅に下がります。\n\nこの節の目的は、単木の弱点（不安定性）をアンサンブルで補うことです[@breiman2001]。\n\n### バギングの原理\n\nランダムフォレストの土台は**バギング（Bootstrap Aggregating）**です。手順は以下の通りです:\n\n1. 元データからブートストラップ標本（復元抽出）を作ります。\n2. その標本で1本の決定木を育てます。\n3. 1-2をB回繰り返します（本章ではB = 300）。\n4. 全B本の木の予測を**多数決**（分類の場合）または**平均**（回帰の場合）で統合します。\n\n比喩で言えば、「多数の専門家に別々の資料を渡し、独立に判断してもらって多数決をとる」イメージです。1人の判断は偏りやすくても、独立した多数の専門家の多数決は安定します。本章では300本の木（`trees = 300`）を使用します。\n\n本章のデータで言えば、木1は「proficiencyが高い学習者を正答と判定するルール」を学び、木2は「cognate（同根語）であれば正答しやすいルール」を拾い、木3は「retrieval条件と即時テストの組み合わせに注目するルール」を学ぶ、というように各木が異なる特徴に着目します。300本の木がそれぞれ異なる「視点」を持つため、多数決をとると1本の木では見逃しがちなパターンも拾えます。\n\nランダムフォレストはバギングに加えて、各分岐で**変数のランダム選択**（mtry個だけ候補にする）を行います。これにより、強い変数（例: proficiency）がすべての木で最初に選ばれる状況を防ぎ、木どうしの**相関を下げて**分散削減効果を高めます[@breiman2001]。\n\n### ランダムフォレストのハイパーパラメータ\n\n\n| Parameter | `tidymodels` 名 | 意味 | 値が大きいと | 値が小さいと |\n|---|---|---|---|---|\n| 候補変数数 | `mtry` | 各分岐で候補にする変数の数 | 木どうしが似やすくなる（相関が高い） | 木が多様になり分散が下がる |\n| 最小ノードサイズ | `min_n` | リーフの最小観測数 | 各木が単純（過学習を抑制） | 各木が複雑（データの揺れに敏感） |\n| 木の本数 | `trees` | アンサンブルに含める木の数 | 予測が安定するが計算時間が増加 | 計算は速いが予測が不安定 |\n\n: Random Forest Hyperparameters\n\n一般的な目安として、分類問題では $\\text{mtry} = \\sqrt{p}$（$p$は変数の数）が推奨されます。本章では $p = 10$ なので $\\sqrt{10} \\approx 3$ が出発点ですが、CVで最適値を探索します。\n\n### モデルとチューニング\n\n`mtry` と `min_n` を中心に調整し、汎化性能を安定化させます。\n\n\n**このチャンクの目的:** max_entropyグリッドで mtry × min_n を6通り探索し、3-fold CVで最良設定を選択します。\n\n::: {.cell}\n\n```{.r .cell-code}\nrf_spec <- rand_forest(\n  mtry = tune(),\n  min_n = tune(),\n  trees = 300\n) |>\n  set_engine(\"ranger\", importance = \"permutation\") |>\n  set_mode(\"classification\")\n\nrf_wf <- workflow() |>\n  add_recipe(rec_tree) |>\n  add_model(rf_spec)\n\nrf_grid <- grid_space_filling(\n  mtry(range = c(2L, 10L)),\n  min_n(range = c(5L, 60L)),\n  size = 6,\n  type = \"max_entropy\"\n)\n\nset.seed(2024)\nrf_res <- tune_grid(\n  rf_wf,\n  resamples = folds,\n  grid = rf_grid,\n  metrics = metric_set(roc_auc, pr_auc)\n)\n\nrf_best <- select_best(rf_res, metric = \"roc_auc\")\nrf_best\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 3\n   mtry min_n .config        \n  <int> <int> <chr>          \n1     2    40 pre0_mod1_post0\n```\n\n\n:::\n:::\n\n\n`rf_best` は最終モデルの設計図です。`mtry` が小さいほど木の多様性が高まり、`min_n` が大きいほど各木は保守的になります。ここで得た値を、後で解釈可能性とのバランス（複雑すぎないか）と一緒に評価します。\n\n`grid_space_filling(type = \"max_entropy\")` は、点どうしが偏らないように探索点を配置します。探索回数が少ない設定でも、極端に狭い領域だけを試してしまうリスクを抑えられます。\n\n### 公式ドキュメント補足（RFチューニング）\n\n- `grid_space_filling()` は `dials` の現行推奨グリッド生成です。以前の `grid_max_entropy()` / `grid_latin_hypercube()` は後方互換のため残っていますが、新規コードでは `grid_space_filling()` が推奨されます。\n- `rand_forest(engine = \"ranger\")` の `importance = \"permutation\"` は、予測性能低下に基づく重要度を返し、解釈で使いやすい設定です。\n\n参考:\n- [dials: grid_space_filling()](https://dials.tidymodels.org/reference/grid_space_filling.html)\n- [parsnip: rand_forest() with ranger](https://parsnip.tidymodels.org/reference/details_rand_forest_ranger.html)\n\n\n\n**このチャンクの目的:** 最良パラメータでRFを最終学習します。\n\n::: {.cell}\n\n```{.r .cell-code}\nrf_final_wf <- finalize_workflow(rf_wf, rf_best)\nrf_final_fit <- fit(rf_final_wf, data = train_data)\n```\n:::\n\n\nこの段階で、CVは「設計選択」に、最終学習は「本番モデル構築」に役割が分かれます。テストデータにはまだ触れないことで、評価の独立性を守ります。\n\n\n**このチャンクの目的:** OOB（Out-of-Bag）誤差を取り出し、ランダムフォレスト内部の参考指標として確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nrf_engine_fit <- rf_final_fit |>\n  extract_fit_parsnip() |>\n  pluck(\"fit\")\n\ntibble(\n  Metric = c(\"OOB Error\", \"OOB Accuracy\"),\n  Value = c(rf_engine_fit$prediction.error, 1 - rf_engine_fit$prediction.error)\n) |>\n  mutate(Value = round(Value, 3)) |>\n  knitr::kable(caption = \"Random Forest OOB Check\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Random Forest OOB Check\n\n|Metric       | Value|\n|:------------|-----:|\n|OOB Error    | 0.175|\n|OOB Accuracy | 0.825|\n\n\n:::\n:::\n\n\nOOB精度が高すぎる一方でテスト精度が低い場合は、データ分割差や特徴量リークを疑います。ただし、本章のOOBはランダムフォレスト内部の行単位ブートストラップに基づくため、`learner_id` 単位の test set や grouped CV の代替にはなりません。OOBは「モデル内部の参考値」、最終判断は held-out learners でのテスト性能に置きます。\n\n### OOB（Out-of-Bag）エラーとは\n\n各木はブートストラップ標本で学習するため、約3分の1のデータが「使われない」まま残ります。この残りデータで予測精度を算出したものがOOBエラーです。1行ずつ独立したデータでは交差検証に近い内部評価として便利ですが、同じ学習者や同じ項目が複数行に現れる教育データでは、OOB側にも学習済みの学習者・項目に近い情報が残りえます。そのため、本章ではOOBを grouped CV/test の代替ではなく、補助的な安定性チェックとして扱います。\n\n### 変数重要度\n\nここは「どの要因が予測に効いていたか」を説明する節です。ランダムフォレストでは**Permutation importance**（並べ替え重要度）を使います。原理は次の通りです:\n\n1. OOBデータまたは検証データで元の予測精度を記録します。\n2. ある変数の値をランダムにシャッフルして、予測精度を再計算します。\n3. 精度の**低下幅**がその変数の重要度になります。\n\n大きく低下する変数は「その変数がないと予測が壊れる」＝重要な変数です。本章の `ranger` 実装では、学習済みRFのOOB予測に基づくPermutation importanceを取り出しています。Gini不純度ベースの重要度（impurity importance）よりカテゴリカル変数のカーディナリティバイアスは小さくなりやすい一方、相関の強い特徴量があると重要度が分散したり、片方だけが高く見えたりする点には注意します [@strobl2007]。**相関の強い特徴量がある場合の根本的な対処**としては、@strobl2008 が提案した **条件付き並べ替え重要度 (conditional permutation importance)** が有効で、`party::cforest` + `permimp::permimp()` で実装できます。重要度の解釈は、点推定だけでなく複数手法 (impurity / permutation / conditional permutation / SHAP) を併記して比較する方が安全です。\n\nカーディナリティとは「カテゴリの水準数」のことです。例えば本章のデータでは `pos`（品詞）は3水準、`condition`（条件）は3水準です。Gini不純度ベースの重要度は分岐改善量の集計で計算されるため、水準数が多い変数や連続変数ほど分割候補が多く、実際の予測力以上に重要度が膨らむことがあります。これがカーディナリティバイアスです。\n\n\n**このチャンクの目的:** Permutation importanceの表と棒グラフを出力します。\n\n::: {.cell}\n\n```{.r .cell-code}\nrf_vi <- tibble(\n  Feature = names(rf_engine_fit$variable.importance),\n  Importance = as.numeric(rf_engine_fit$variable.importance)\n) |>\n  arrange(desc(Importance))\n\nrf_vi |>\n  mutate(Importance = round(Importance, 4)) |>\n  knitr::kable(caption = \"Random Forest Variable Importance (Permutation importance)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Random Forest Variable Importance (Permutation importance)\n\n|Feature      | Importance|\n|:------------|----------:|\n|proficiency  |     0.0501|\n|anxiety      |     0.0100|\n|word_length  |     0.0081|\n|l1_distance  |     0.0045|\n|cognate      |     0.0041|\n|weekly_study |     0.0030|\n|condition    |     0.0025|\n|test_time    |     0.0014|\n|word_freq    |     0.0011|\n|pos          |     0.0010|\n\n\n\nPermutation importance from random forest\n:::\n\n```{.r .cell-code}\nrf_vi |>\n  slice_max(Importance, n = 12) |>\n  ggplot(aes(x = reorder(Feature, Importance), y = Importance)) +\n  geom_col(fill = \"#2C7FB8\") +\n  coord_flip() +\n  labs(title = \"Top Features (Random Forest)\",\n       x = \"Feature\", y = \"Importance\")\n```\n\n::: {.cell-output-display}\n![Permutation importance from random forest](8_予測分析_ROC曲線とツリーモデル_files/figure-html/rf-importance-1.png){width=2700}\n:::\n:::\n\n\nPermutation重要度は「その変数を壊したとき、予測がどれだけ悪化するか」を測るため、実務的な説明力に直結します。ただし相関の強い変数同士では重要度が分散しやすい点は、解釈時に明示しておきます。\n\n重要度を報告するときは、順位だけでなく差の大きさも示します。上位1位と2位が僅差なら「同程度に重要」と記述し、順位差を過剰に強調しない方が実証的です。\n\n#### Mizumoto (2022) スタイルの最小実装\n\nMizumoto (2022) のランダムフォレスト重要度に寄せるなら、`randomForest::randomForest()` で直接モデルを作り、`importance()` を抜き出す最小スクリプトで十分です。\n\n```r\n# Mizumoto (2022) style: minimal RF importance script\nrf_miz <- randomForest::randomForest(\n  correct ~ proficiency + weekly_study + anxiety + l1_distance +\n    word_freq + word_length + cognate + pos + condition + test_time,\n  data = train_data,\n  ntree = 1000,\n  importance = TRUE\n)\n\nrandomForest::importance(rf_miz) |>\n  as.data.frame() |>\n  tibble::rownames_to_column(\"Predictor\")\n```\n\n本文の `ranger` 実装は tidymodels ワークフローに載せるためのものです。一方、この短いスクリプトは「Mizumoto と同じ発想で重要度を確認したい」読者向けの最短経路です。\n\nランダムフォレストは「多数の木が独立に判断して多数決する」方法です。これで分散は下がりますが、木の集合全体に同じ弱点が残ることもあります。例えば多くの木が「anxietyの効果を過小評価」していれば、多数決でもその偏りは残ります。XGBoostは発想を変え、「1本目の木の間違いを分析し、その間違いを集中的に修正する2本目の木を作る」というように、逐次的に弱点を補う戦略を取ります。\n\n---\n\n## XGBoost\n\nXGBoost は「前の木が間違えた部分を、次の木が重点的に修正する」逐次学習法です。ランダムフォレストが木を並列に育てるのに対し、XGBoost は **直列に** 育てて誤差を段階的に減らします。強力ですが、設定項目は多めです。\n\nこの節の目的は、高精度モデルを構築しつつ、重要特徴を解釈可能な形で確認することです[@chen2016]。\n\n### ブースティングの原理：バギングとの対比\n\nランダムフォレスト（バギング）とXGBoost（ブースティング）は、アンサンブルの組み立て方が根本的に異なります。\n\n\n**このチャンクの目的:** バギング（RF）とブースティング（XGBoost）の違いを7つの観点で比較する表です。\n| 観点 | バギング（RF） | ブースティング（XGBoost） |\n|---|---|---|\n| 木の育て方 | 各木を独立に並列で育てる | 前の木の誤差を踏まえて逐次に育てる |\n| 学習データ | ブートストラップ標本（復元抽出） | 全データ（ただし誤差が大きい観測に重点） |\n| 予測の統合 | 多数決 or 平均 | 加重和（学習率で縮小しながら積み上げ） |\n| 主な効果 | 分散の削減 | バイアスの段階的削減 |\n| 各木の深さ | 通常は深い木 | 通常は浅い木（stump〜数段） |\n| 過学習リスク | 低い（木の独立性が高い） | 反復数が多いと過学習しやすい |\n| チューニング難度 | 比較的容易（`mtry` が主要） | やや高い（学習率・反復数・深さなど） |\n\n: Bagging (RF) vs. Boosting (XGBoost)\n\nブースティングの手順を、比喩を使って説明します:\n\n1. **1回目の家庭教師**が全体を見て、「習熟度が低い学習者は苦手」と大まかに判断する（浅い木）。\n2. **2回目の家庭教師**は、1回目が見逃した部分（例: 同根語なのに間違えた高習熟者）を重点的に指導します。\n3. **3回目**はさらに細かい見逃しを拾います。\n4. 最終予測は、全家庭教師の判断を**学習率（learn_rate）で縮小しながら足し合わせた**ものです。\n\n数式で書くと、$m$回目までのモデルの予測は:\n\n$$\n\\hat{f}_m(x) = \\hat{f}_{m-1}(x) + \\eta \\cdot h_m(x)\n$$\n\nここで $h_m(x)$ は $m$回目の木の予測、$\\eta$ は学習率（0〜1）です。学習率を小さくすると各木の寄与が控えめになり、より多くの反復が必要になりますが、過学習しにくくなります[@friedman2001]。\n\n**XGBoostの「勾配」とは**、損失関数をどちらへ動かすと改善するかを示す方向情報です。回帰分析で傾き（slope）を使って最適化するのと同じ発想で、XGBoost でも損失関数の勾配を使って「次の木がどの方向に改善すべきか」を計算します。残差ではなく勾配を使うことで、対数損失（分類）や二乗誤差（回帰）など任意の損失関数に対応できます。\n\n### XGBoostのハイパーパラメータ\n\nXGBoostは設定項目が多いため、各パラメータの意味を整理しておきます。\n\n\n| Parameter | `tidymodels` 名 | `xgboost` 名 | 意味 | 値が大きいと | 値が小さいと |\n|---|---|---|---|---|---|\n| 反復回数 | `trees` | `nrounds` | ブースティングの反復回数 | 表現力が上がるが過学習リスク | 学習不足 |\n| 木の深さ | `tree_depth` | `max_depth` | 各木の最大深さ | 複雑な相互作用を捉える | 浅い木で穏やかに学習 |\n| 学習率 | `learn_rate` | `eta` | 各木の寄与の縮小率 | 速く収束するが過学習しやすい | 慎重だが多くの反復が必要 |\n| 最小ノードサイズ | `min_n` | `min_child_weight` | リーフの最小サンプル重み | 過学習を抑制 | 細かい分岐を許容 |\n| 候補変数割合 | `mtry` | `colsample_bytree` | 各木で使う変数の割合 | 木が似やすくなる | 多様性が上がる |\n| 損失削減量 | `loss_reduction` | `gamma` | 分岐に必要な最小損失改善 | 慎重な分岐（正則化） | 積極的な分岐 |\n| サンプル割合 | `sample_size` | `subsample` | 各木で使うデータの割合 | 全データ使用 | ランダム性を導入 |\n\n: XGBoost Hyperparameters\n\n実務上の指針として、まず `learn_rate` と `trees` のバランスを探り、次に `tree_depth` と正則化パラメータ（`loss_reduction`, `min_n`）を調整するのが効率的です。\n\n### モデルとチューニング\n\nXGBoostは強力ですが、設定が多いため、ここでは小さめの探索空間で実行可能性を優先します。`grid_space_filling(type = \"latin_hypercube\")` は、高次元のパラメータ空間をまんべんなくカバーするようにポイントを配置する方法です。通常のグリッドサーチ（格子点の全組み合わせ）ではパラメータが7つもあると計算量が膨大になるため、少ない試行回数（ここでは5通り）で広い範囲を効率的に探索します。実研究でXGBoostを主分析にする場合は、探索点を増やす、反復回数と学習率の範囲を広げる、早期停止用の検証データを別に用意する、といった追加設計も検討します。\n\n\n**このチャンクの目的:** Latin hypercubeグリッドで7パラメータを5通り探索し、3-fold CVで最良設定を選択します。\n\n::: {.cell}\n\n```{.r .cell-code}\nxgb_spec <- boost_tree(\n  trees = tune(),\n  tree_depth = tune(),\n  min_n = tune(),\n  loss_reduction = tune(),\n  sample_size = tune(),\n  learn_rate = tune(),\n  mtry = tune()\n) |>\n  set_engine(\"xgboost\") |>\n  set_mode(\"classification\")\n\nxgb_wf <- workflow() |>\n  add_recipe(rec_xgb) |>\n  add_model(xgb_spec)\n\nxgb_grid <- grid_space_filling(\n  trees(range = c(100L, 250L)),\n  tree_depth(range = c(2L, 8L)),\n  min_n(range = c(2L, 40L)),\n  loss_reduction(),\n  sample_prop(range = c(0.5, 1.0)),\n  learn_rate(range = c(-3.5, -0.8)),\n  # prep()でレシピを訓練データに適用し、bake()で前処理済みデータを取り出す（mtryの範囲設定用）\n  finalize(mtry(), bake(prep(rec_xgb, training = train_data), new_data = NULL) |> dplyr::select(-correct)),\n  size = 5,\n  type = \"latin_hypercube\"\n)\n\nset.seed(2024)\nxgb_res <- tune_grid(\n  xgb_wf,\n  resamples = folds,\n  grid = xgb_grid,\n  metrics = metric_set(roc_auc, pr_auc),\n  control = control_grid(save_pred = TRUE)\n)\n\nxgb_best <- select_best(xgb_res, metric = \"roc_auc\")\nxgb_best\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 8\n   mtry trees min_n tree_depth learn_rate loss_reduction sample_size .config    \n  <int> <int> <int>      <int>      <dbl>          <dbl>       <dbl> <chr>      \n1     4   168    25          3     0.0136       0.000147       0.531 pre0_mod2_…\n```\n\n\n:::\n:::\n\n\nXGBoostでは、単一パラメータより組み合わせで性能が決まります。特に `learn_rate` が小さい場合は `trees` が多く必要になるため、採用値をセットで読むことが重要です。`tree_depth` が深く、`learn_rate` が高く、`trees` も多い設定は過学習しやすいため、CV性能だけでなくキャリブレーションやサブグループ性能も後で確認します。\n\n`learn_rate(range = c(-3.5, -0.8))` は対数スケール指定で、実際の学習率はおおむね 0.0003〜0.16 の範囲で探索されます。数値の桁を誤読しないよう、「logスケールで探索した」ことを本文に明記すると誤解を防げます。\n\nCVで選ばれた学習率、反復回数、木の深さ等を確定し、訓練データ全体で最終モデルを構築します。\n\n`boost_tree(engine = \"xgboost\")` の主要パラメータ対応は、`learn_rate -> eta`, `trees -> nrounds`, `tree_depth -> max_depth` です。`mtry` は「各木で使う変数割合（`colsample_bytree`）」に対応するため、前処理後の列数に基づいて `finalize()` で範囲確定するのが安全です。`grid_space_filling(type = \"latin_hypercube\")` は、高次元空間で少ない試行回数でも探索の偏りを抑える目的に適しています。参考: [parsnip: boost_tree() with xgboost](https://parsnip.tidymodels.org/reference/details_boost_tree_xgboost.html), [dials: grid_space_filling()](https://dials.tidymodels.org/reference/grid_space_filling.html)\n\n\n**このチャンクの目的:** 最良パラメータでXGBoostを最終学習します。\n\n::: {.cell}\n\n```{.r .cell-code}\nxgb_final_wf <- finalize_workflow(xgb_wf, xgb_best)\nxgb_final_fit <- fit(xgb_final_wf, data = train_data)\n```\n:::\n\n\nここまでで学習器は固定されるため、以降の重要度分析やSHAPは「この最終モデルに対する説明」として一貫して解釈できます。\n\n**XGBoost の学習原理**: 前の木が間違えた観測を次の木が重点的に学習し、誤差を段階的に減らします。最初から完璧な木を作るのではなく、改善を積み重ねる設計です。\n\n\n**このチャンクの目的:** XGBoostのGain-based重要度（上位10特徴）を表で出力します。\n\n::: {.cell}\n\n```{.r .cell-code}\nxgb_engine_fit <- xgb_final_fit |>\n  extract_fit_parsnip() |>\n  pluck(\"fit\")\n\nxgb.importance(model = xgb_engine_fit) |>\n  as_tibble() |>\n  slice_max(Gain, n = 10) |>\n  mutate(across(where(is.numeric), ~ round(., 4))) |>\n  knitr::kable(caption = \"Top Features in XGBoost: Gain-based importance (top 10)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Top Features in XGBoost: Gain-based importance (top 10)\n\n|Feature             |   Gain|  Cover| Frequency|\n|:-------------------|------:|------:|---------:|\n|proficiency         | 0.4636| 0.2404|    0.2112|\n|anxiety             | 0.1099| 0.1221|    0.1370|\n|word_length         | 0.0924| 0.1143|    0.1118|\n|l1_distance         | 0.0827| 0.1248|    0.1381|\n|word_freq           | 0.0726| 0.1367|    0.1435|\n|weekly_study        | 0.0567| 0.0885|    0.1091|\n|cognate_yes         | 0.0360| 0.0553|    0.0365|\n|condition_retrieval | 0.0355| 0.0414|    0.0360|\n|test_time_immediate | 0.0258| 0.0406|    0.0306|\n|pos_verb            | 0.0110| 0.0125|    0.0183|\n\n\n:::\n:::\n\n\nGain重要度は高速で有用ですが、カテゴリ数の多い特徴が有利になりやすい特性があります。最終報告では、後続のSHAPやRFのPermutation重要度と照合して整合性を確認する方針を取ります。\n\n加えて、Gainは「分岐でどれだけ損失を減らしたか」の集計で、寄与の方向（正答側へ押したか、誤答側へ押したか）は示しません。方向解釈にはSHAPを併用する設計が必要です。\n\n### SHAP（全体と個別の解釈）\n\n#### SHAPとは何か\n\nロジスティック回帰では、係数（$\\beta$）が「各変数の寄与」を表していました。たとえば $\\beta_\\text{proficiency} = 1.2$ なら、「習熟度が1単位上がると対数オッズが1.2増える」と読めます。しかしツリーモデルには係数がありません。**SHAP**はツリーモデルでも「この予測に各変数がいくらずつ貢献したか」を分解する手法です[@lundberg2017; @lundberg2020]。\n\n考え方の核は、ゲーム理論のShapley値に基づいています。「チーム（全特徴）の成果（予測）を、各メンバー（特徴）に公平に分配するにはどうすればよいか」を計算します。具体例で説明します:\n\n- XGBoostでは、SHAP値は通常、確率そのものではなくロジット/マージンのスケールで加法分解されます。\n- たとえば平均的なマージンが 0.20、ある学習者（Case A）のマージンが 1.75 なら、差分 1.55 があります。\n- SHAPはこの 1.55 を「proficiency が +0.80、word_freq が +0.35、anxiety が +0.20、…」のように各特徴に分配します。確率として説明したい場合は、最後に逆ロジット変換で `P(correct)` に戻します。\n\nこの分解により、「**なぜ**この学習者は正答と予測されたのか」を特徴ごとに説明できます。\n\n::: {.callout-note}\n### Shapley値の直感\nゲーム理論では「チームの成果を各メンバーに公平に分配する」問題を考えます。SHAPはこれと同じ発想で、「予測という成果を、各特徴量という\"プレイヤー\"に公平に振り分ける」計算です。XGBoostの `predcontrib = TRUE` では、寄与は確率差ではなくロジット/マージン上の寄与として返る点に注意します。全プレイヤーの寄与と `BIAS` を足すとモデル出力に一致し、その値をシグモイド関数に通すと `P(correct)` に戻せます。\n:::\n\nXGBoost公式ドキュメントでも、`predcontrib = TRUE` は各特徴量の寄与と `BIAS` 列を返し、寄与和がモデル出力に対応すると説明されています。したがって、本章のSHAP図は「正答確率そのものを何ポイント上げたか」ではなく、「ロジット/マージン上で正答側・誤答側へどれだけ押したか」として読みます。参考: [xgboost prediction docs](https://xgboost.readthedocs.io/en/stable/prediction.html)\n\n::: {.callout-warning}\n#### path-dependent vs interventional TreeSHAP の違い\n`predcontrib = TRUE` は内部的に **path-dependent TreeSHAP** [@lundberg2020] を計算します。これは木の構造に沿って寄与を分配する高速アルゴリズムで、**訓練データの特徴量分布**に依存します。一方、観測分布で介入を考える **interventional TreeSHAP** は概念的により因果的な解釈に近く、両者は相関の強い特徴量があるときに値が一致しないことが知られています [@janzing2020]。本章では実用優先で path-dependent を使いますが、相関の強い特徴量間で寄与が振り分けられる場合は、Shapley値が「予測モデルの寄与の公平な分配」であって因果効果ではないことを再確認してください。汎用的な代替実装として、`kernelshap` パッケージ [@kernelshappkg] (model-agnostic Kernel SHAP) を `shapviz` と組み合わせる選択肢もあります。\n:::\n\n#### 論文での活用例\n\n先行研究マップの文献を、本章の実装に対応づけます:\n\n- **Dominance**（@budescu1993; @azen2003 系）: GLMベースラインで使用済みです。全サブモデル比較で変数の相対的重要性を算出します。本章の `glm-dominance-analysis` に対応します。\n- **SHAP local → global**（@lundberg2017 系）: 1件の予測をベースライン＋寄与に分解し、多数集約して全体理解へ進みます。@lundberg2020 は医療データで希少だが高リスクな要因を抽出しています。本章の `xgb-shap-local` → `xgb-shap-global` に対応します。\n- **教育データでの読み替え**: たとえばある学習者のlocal SHAPで `test_time_delayed` が負、`proficiency` が正なら、「遅延テスト条件は不利だが、習熟度がそれを一部補っている」と解釈できます。\n\n#### 全体: どの特徴が平均的に効いたか\n\nSHAPの計算は3ステップで進みます: (1) 説明対象データをXGBoostが受け取れる行列形式に変換します、(2) `predcontrib = TRUE`オプションで各観測×各変数のSHAP値を取得します、(3) 全観測にわたる絶対値の平均（Mean |SHAP|）を計算して全体的な重要度を算出します。本章では最終モデルの説明対象としてテストデータを使いますが、SHAPは性能評価指標ではありません。テストデータのSHAPを見た後にモデルや閾値を選び直すと、評価の独立性が崩れます。\n\n\n**このチャンクの目的:** テストデータのSHAP値（predcontrib）を計算し、Mean|SHAP|で全体的な特徴量重要度を算出します。\n\n::: {.cell}\n\n```{.r .cell-code}\n# XGBoost用に前処理したテスト行列を作る\nxgb_prep <- prep(rec_xgb, training = train_data)\nxgb_test_processed <- bake(xgb_prep, new_data = test_data)\nxgb_test_matrix <- xgb_test_processed |>\n  dplyr::select(-correct) |>\n  as.matrix()\n\n# predcontrib = TRUE でSHAP寄与を取得（最後の列はBIAS）\nshap_raw <- predict(xgb_engine_fit, newdata = xgb_test_matrix, predcontrib = TRUE)\nif (!is.matrix(shap_raw)) shap_raw <- as.matrix(shap_raw)\n\nshap_feature_names <- colnames(xgb_test_matrix)\nif (ncol(shap_raw) == length(shap_feature_names) + 1) {\n  colnames(shap_raw) <- c(shap_feature_names, \"BIAS\")\n}\n\nshap_importance <- tibble(\n  Feature = shap_feature_names,\n  MeanAbsSHAP = colMeans(abs(shap_raw[, shap_feature_names, drop = FALSE]))\n) |>\n  arrange(desc(MeanAbsSHAP))\n\nshap_importance |>\n  mutate(MeanAbsSHAP = round(MeanAbsSHAP, 4)) |>\n  knitr::kable(caption = \"Global SHAP Importance (XGBoost): Mean absolute contribution across test observations\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Global SHAP Importance (XGBoost): Mean absolute contribution across test observations\n\n|Feature             | MeanAbsSHAP|\n|:-------------------|-----------:|\n|proficiency         |      0.7821|\n|cognate_yes         |      0.3593|\n|anxiety             |      0.2450|\n|word_length         |      0.2360|\n|condition_retrieval |      0.1749|\n|test_time_immediate |      0.1284|\n|l1_distance         |      0.1082|\n|condition_sentence  |      0.1071|\n|word_freq           |      0.0928|\n|weekly_study        |      0.0674|\n|pos_verb            |      0.0454|\n|pos_noun            |      0.0081|\n\n\n\nGlobal SHAP importance for XGBoost\n:::\n\n```{.r .cell-code}\nshap_importance |>\n  slice_max(MeanAbsSHAP, n = 12) |>\n  ggplot(aes(x = reorder(Feature, MeanAbsSHAP), y = MeanAbsSHAP)) +\n  geom_col(fill = \"#D95F02\") +\n  coord_flip() +\n  labs(\n    title = \"Top 12 Features by SHAP\",\n    x = \"Feature\",\n    y = \"Mean |SHAP|\"\n  )\n```\n\n::: {.cell-output-display}\n![Global SHAP importance for XGBoost](8_予測分析_ROC曲線とツリーモデル_files/figure-html/xgb-shap-global-1.png){width=2700}\n:::\n:::\n\n\nGlobal SHAPは「全体として何が効いたか」を示しますが、方向（正/負）は平均化で消えます。以下のbeeswarmプロットでは、方向と値の大きさを同時に確認できます。\n\n#### Beeswarmプロット: 方向と大きさの同時可視化\n\n棒グラフでは各特徴量の「平均的な効き」しか見えませんが、beeswarmプロットでは個々の観測が点として表示され、特徴量の値（色）とSHAP寄与（横軸）の関係を直接読み取れます。\n\n**このチャンクの目的:** shapvizパッケージでbeeswarmプロットを描画し、各特徴量が予測をどの方向にどれだけ押すかを可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\nsv <- shapviz(xgb_engine_fit, X_pred = xgb_test_matrix)\nsv_importance(sv, kind = \"beeswarm\", max_display = 12)\n```\n\n::: {.cell-output-display}\n![SHAP beeswarm plot: each dot is one test observation, colored by feature value](8_予測分析_ROC曲線とツリーモデル_files/figure-html/xgb-shap-beeswarm-1.png){width=3000}\n:::\n:::\n\n\nbeeswarmプロットの読み方:\n\n- **横軸**: SHAP値（正 = correct側へ押す、負 = incorrect側へ押す）。\n- **色**: 特徴量の元の値（赤 = 高い、青 = 低い）。\n- **読み取り例**: `proficiency` の赤い点（高値）がSHAP正側に集中していれば、「習熟度が高いほど正答確率が上がる」ことを視覚的に確認できます。\n- 棒グラフとの使い分け: 棒グラフで上位変数を特定し、beeswarmで方向性を確認するのが実務的なワークフローです。\n\n**beeswarmで驚くパターンを見つけたら**、それはデータからの重要な信号です。たとえば `word_freq` が高い（赤）にもかかわらず SHAP 値が負側にあるなら、(1) 高頻度語が簡単すぎて天井効果が出ている、(2) 高頻度語が短語に偏り `word_length` と交絡している、(3) 特定の頻度帯にサンプルが偏っている、といった可能性を検討します。beeswarm で仮説を立て、PDP や Local SHAP で検証する流れが実務的です。\n\n`predict(..., predcontrib = TRUE)` は、各特徴量の加法寄与と `BIAS` 列を返します。行ごとの寄与和はモデル出力（マージン）に対応するため、確率との対応を説明する場合は「ロジットスケールの寄与」だという点を明記すると誤読を防げます。ロジットスケールとは、確率 $p$ を $\\log(p / (1-p))$ に変換した尺度です。確率 0.5 はロジット 0、確率 0.8 はロジット約 1.4 に対応します。SHAP 値はこのスケール上で加法的に分解されるため、確率に戻すには逆変換（シグモイド関数）が必要です。参考: [xgboost prediction docs](https://xgboost.readthedocs.io/en/stable/prediction.html), [xgboost in R tutorial](https://xgboost.readthedocs.io/en/stable/R-package/xgboostPresentation.html)\n\n#### 部分依存プロット（PDP）: 効果の形を見る\n\nSHAP beeswarmは個別観測の寄与を示しますが、「変数が増えるとP(correct)はどう変わるか」という関数形は読み取りにくいです。PDP（Partial Dependence Plot）は、他の変数を平均化したときの特定変数の限界効果を描きます。\n\n**このチャンクの目的:** proficiency と word_freq の部分依存プロットを描画し、XGBoostモデルの予測に対する各変数の限界効果の形を確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\ncalc_pdp <- function(var_name, grid_length = 50) {\n  # workflow の predict() は内部で recipe を適用するため、\n  # bake 済みではなく生データ (test_data) を渡す。\n  grid_vals <- seq(\n    min(test_data[[var_name]], na.rm = TRUE),\n    max(test_data[[var_name]], na.rm = TRUE),\n    length.out = grid_length\n  )\n  map_dfr(grid_vals, \\(val) {\n    d_mod <- test_data\n    d_mod[[var_name]] <- val\n    preds <- predict(xgb_final_fit, d_mod, type = \"prob\")$.pred_correct\n    tibble(variable = var_name, value = val, mean_pred = mean(preds))\n  })\n}\n\npdp_data <- bind_rows(\n  calc_pdp(\"proficiency\"),\n  calc_pdp(\"word_freq\")\n)\n\npdp_data |>\n  ggplot(aes(x = value, y = mean_pred)) +\n  geom_line(color = \"#2C7FB8\", linewidth = 1) +\n  facet_wrap(~ variable, scales = \"free_x\") +\n  labs(\n    title = \"Partial Dependence Plots (XGBoost)\",\n    subtitle = \"Average predicted P(correct) as each feature varies\",\n    x = \"Feature value\", y = \"Mean P(correct)\"\n  )\n```\n\n::: {.cell-output-display}\n![Partial dependence plots for top 2 features (XGBoost)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/xgb-pdp-1.png){width=3000}\n:::\n:::\n\n\nPDPの読み方:\n\n- **横軸**: 対象変数の値。**縦軸**: 他の変数を平均化したときの予測確率。\n- **単調増加**（proficiency）: 変数が高いほど一貫してP(correct)が上がるかを確認します。\n- **閾値型**（word_freq）: ある値を超えると予測確率が急に変わるかを確認します。語彙頻度が極端に低い語は正答確率が低く予測されやすい、という読み方になります。\n- **SHAPとの違い**: SHAPは個別観測の寄与分解、PDPは平均的な関数形です。報告ではSHAPで重要変数を特定し、PDPで予測確率の形を補足するのが実務的です。\n\n**PDPの注意点**:\n- 曲線の両端（データが少ない領域）は推定が不安定なので、中央部分を優先して読みます。\n- PDPは、観測データ内ではほとんど出現しない特徴量の組み合わせも平均化に含めることがあります。強い相関や交互作用がある場合は、ICE（Individual Conditional Expectations）プロットや条件付きPDPも検討します。\n- 曲線がジグザグなら、他の変数との相互作用が強い可能性があります。ICEプロットで個別曲線を確認するとよいです。\n- 教育的な読み方の例として、`proficiency` の PDP が「0.0付近で急上昇し、0.5以降は緩やか」なら、「このモデルでは低〜中習熟度帯で予測確率の差が大きい」と読めます。支援対象の候補を立てる材料にはなりますが、指導資源をそこへ配分すれば成果が上がる、という因果的判断には別の研究デザインが必要です。\n\n#### 個別: 1つの予測をどう説明するか\n\n\n**このチャンクの目的:** 正答確率が最も高いテスト観測1件を選び、各特徴のSHAP寄与を方向つきで表示します。\n\n::: {.cell}\n\n```{.r .cell-code}\n# 正答確率が最も高いテスト観測を1件選ぶ\nxgb_prob_test <- predict(xgb_final_fit, test_data, type = \"prob\")$.pred_correct\nfocus_id <- which.max(xgb_prob_test)\nfocus_prob <- xgb_prob_test[focus_id]\n\nlocal_shap <- tibble(\n  Feature = shap_feature_names,\n  SHAP = shap_raw[focus_id, shap_feature_names]\n) |>\n  mutate(Direction = if_else(SHAP >= 0, \"Push to correct\", \"Push to incorrect\")) |>\n  arrange(desc(abs(SHAP))) |>\n  slice_head(n = 12)\n\nlocal_shap |>\n  mutate(across(where(is.numeric), ~ round(., 4))) |>\n  knitr::kable(caption = paste0(\"Local SHAP Explanation: Case ID \", focus_id, \", predicted P(correct) = \", round(focus_prob, 3)))\n```\n\n::: {.cell-output-display}\n\n\nTable: Local SHAP Explanation: Case ID 400, predicted P(correct) = 0.853\n\n|Feature             |    SHAP|Direction         |\n|:-------------------|-------:|:-----------------|\n|proficiency         | -0.9139|Push to incorrect |\n|word_length         | -0.4649|Push to incorrect |\n|anxiety             | -0.4178|Push to incorrect |\n|condition_retrieval | -0.2557|Push to incorrect |\n|l1_distance         | -0.1906|Push to incorrect |\n|cognate_yes         | -0.1815|Push to incorrect |\n|test_time_immediate | -0.1033|Push to incorrect |\n|condition_sentence  | -0.0989|Push to incorrect |\n|word_freq           | -0.0622|Push to incorrect |\n|weekly_study        |  0.0617|Push to correct   |\n|pos_verb            |  0.0129|Push to correct   |\n|pos_noun            |  0.0070|Push to correct   |\n\n\n\nLocal SHAP explanation for one high-probability case\n:::\n\n```{.r .cell-code}\nlocal_shap |>\n  ggplot(aes(x = reorder(Feature, SHAP), y = SHAP, fill = Direction)) +\n  geom_col() +\n  coord_flip() +\n  scale_fill_manual(values = c(\"Push to correct\" = \"#1B9E77\", \"Push to incorrect\" = \"#D95F02\")) +\n  labs(\n    title = \"Local SHAP Contributions (Top 12)\",\n    x = \"Feature\",\n    y = \"SHAP contribution\"\n  )\n```\n\n::: {.cell-output-display}\n![Local SHAP explanation for one high-probability case](8_予測分析_ROC曲線とツリーモデル_files/figure-html/xgb-shap-local-1.png){width=2700}\n:::\n:::\n\n\nLocal SHAPの正値は「correct側へ押す寄与」、負値は「incorrect側へ押す寄与」です。1ケースの説明を本文で示す際は、上位2〜3特徴に絞って因果ではなく寄与として表現すると、過剰解釈を避けられます。\n\n1ケースだけでなく、代表的に「高確率ケース」「境界ケース」「低確率ケース」を並べると、モデルがどの条件で判断を変えるかをより説得的に示せます。\n\n::: {.callout-tip}\n### SHAPの解釈指針\n- **Global SHAP**: どの特徴が「全体として」効いているか。  \n- **Local SHAP**: 1件の予測で、どの特徴が「正答側/誤答側」に押したか。  \n:::\n\n---\n\n## 特徴量重要度の統合比較\n\nここまで、GLMではDominance analysis、RFではPermutation importance、XGBoostではGain importanceとSHAPという4つの異なる重要度指標が登場しました。「proficiencyが最重要」という結論は共通しそうですが、2位以下の順序は手法によって異なるかもしれません。異なる手法が「同じ答え」を出す変数ほど、予測上は頑健に重要だと判断しやすくなります。ここでは4手法を横断的に比較します。\n\n::: {.panel-tabset}\n\n### 比較表\n\n\n**このチャンクの目的:** 4種類の重要度指標（Dominance, Permutation, Gain, SHAP）の測定対象・長所・限界を比較表で整理します。\n| 手法 | 対象モデル | 測定対象 | 長所 | 限界 |\n|---|---|---|---|---|\n| Dominance analysis | GLM | 全サブモデル比較での平均R²改善量 | 理論的に厳密。相対的重要性を公平に比較 | 線形モデル前提。計算コスト高（変数が多いと困難） |\n| Permutation importance | RF | 変数シャッフル後の精度低下幅 | モデル非依存。カーディナリティバイアスが小さい | 相関の強い変数間で重要度が分散する |\n| Gain importance | XGBoost | 分岐に使われた際の損失改善量の合計 | 計算が速い。ツリー構造から直接算出 | カーディナリティの高い変数を過大評価しやすい |\n| SHAP | XGBoost | 各予測のShapley値の絶対値平均 | 局所→全体の統一的枠組みです。理論的に公平です | 計算コストが高いです。相互作用の直接解釈には追加分析が必要です |\n\n: Comparison of Four Feature Importance Methods\n\n### 統合プロット\n\n4手法のランキングを並列で比較します。変数名はモデル間で一致するよう、主要変数に絞っています。\n\n\n**このチャンクの目的:** 4手法の重要度ランキングを正規化して並列棒グラフで比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\n# Dominance（GLM, r2.n）— 既存の da_tbl から取得\ndom_rank <- da_tbl |>\n  dplyr::filter(Fit_Index == \"r2.n\") |>\n  transmute(Feature = Predictor,\n            Importance = Relative,\n            Method = \"Dominance (GLM)\")\n\n# Permutation（RF）— 既存の rf_vi から取得\nperm_rank <- rf_vi |>\n  transmute(Feature,\n            Importance = Importance / max(Importance),\n            Method = \"Permutation (RF)\")\n\n# Gain（XGBoost）— 再計算\ngain_raw <- xgb.importance(model = xgb_engine_fit) |> as_tibble()\ngain_rank <- gain_raw |>\n  transmute(Feature,\n            Importance = Gain / max(Gain),\n            Method = \"Gain (XGBoost)\")\n\n# SHAP（XGBoost）— 既存の shap_importance から取得\nshap_rank <- shap_importance |>\n  transmute(Feature,\n            Importance = MeanAbsSHAP / max(MeanAbsSHAP),\n            Method = \"SHAP (XGBoost)\")\n\n# 統合（上位8変数に絞る）\ntop_features <- shap_rank |> slice_max(Importance, n = 8) |> pull(Feature)\n\nbind_rows(dom_rank, perm_rank, gain_rank, shap_rank) |>\n  dplyr::filter(Feature %in% top_features) |>\n  ggplot(aes(x = reorder(Feature, Importance), y = Importance)) +\n  geom_col(fill = \"#4C78A8\") +\n  coord_flip() +\n  facet_wrap(~ Method, scales = \"free_x\", ncol = 2) +\n  labs(\n    title = \"Feature Importance: 4 Methods Compared\",\n    subtitle = \"Normalized to [0, 1] within each method; top 8 features shown\",\n    x = \"Feature\",\n    y = \"Relative importance\"\n  )\n```\n\n::: {.cell-output-display}\n![Feature importance rankings across four methods](8_予測分析_ROC曲線とツリーモデル_files/figure-html/importance-comparison-plot-1.png){width=3600}\n:::\n:::\n\n\n### 報告の指針\n\n4手法は「異なる質問に答える異なる道具」です。報告時は次のように使い分けます:\n\n- **GLMを主モデルとして使う場合**: Dominance analysisを主報告にし、係数表と組み合わせて相対的重要性を議論します。\n- **ツリーモデルを主モデルとして使う場合**: SHAPを主報告にし、Permutation importanceを補助として併記し、Gain importanceは参考に留めます[@strobl2007]。\n- **複数手法の合意**: 全手法で上位に来る変数（例: proficiency, word_freq）は、予測上頑健に重要だと判断しやすいです。手法間で順位が異なる変数は、「どの手法を信頼するか」の根拠を明記します。\n\n:::\n\n---\n\n## GLMベースライン＋3ツリーモデル比較（ROCはここで効く）\n\n### 概要\n\n4モデル（GLM・決定木・RF・XGBoost）をテストデータで比較します。まず具体ケースで直感を得てから、全体指標（ROC-AUC等）で定量的に比較し、最後にROC/PR曲線で視覚的に確認します。\n\nこの節では、ケース別予測 → 全体指標 → ROC/PR の順で、読み手が迷わないように比較します。\n\n### モデル特性サマリー\n\nコードに入る前に、4モデルの特性を俯瞰します。\n\n\n| Property | Logistic | Decision Tree | Random Forest | XGBoost |\n|---|---|---|---|---|\n| 解釈容易性 | 高（係数表） | 高（分岐規則） | 中（重要度） | 中（SHAP） |\n| 非線形対応 | 手動で指定 | データ駆動で探索 | データ駆動で探索 | データ駆動で探索 |\n| 相互作用発見 | 手動で指定 | 分岐候補として探索 | 分岐候補として探索 | 分岐候補として探索 |\n| 計算速度 | 速い | 速い | 中程度 | やや遅い |\n| チューニング複雑さ | なし〜低 | 低 | 中 | 高 |\n| 過学習リスク | 低 | 高 | 低 | 中 |\n| 推奨場面 | ベースライン | 説明・教育用 | 安定した予測 | 最高精度追求 |\n\n: Model Properties at a Glance\n\n### 予測値をまとめる\n\n4つのモデルの予測を同一フォーマットで集めるためのヘルパー関数を定義します。各モデルについて、正答確率（`.pred_correct`）とクラス予測（`.pred_class`）をテストデータに対して計算し、モデル名を付けて結合します。\n\n\n**このチャンクの目的:** 4モデルのテストデータ予測（確率・クラス）を統一フォーマットで結合します。\n\n::: {.cell}\n\n```{.r .cell-code}\ncollect_binary_preds <- function(fit_obj, data, model_name) {\n  tibble(\n    model = model_name,\n    learner_id = data$learner_id,\n    item_id = data$item_id,\n    proficiency = data$proficiency,\n    condition = data$condition,\n    test_time = data$test_time,\n    truth = data$correct,\n    .pred_correct = predict(fit_obj, data, type = \"prob\")$.pred_correct,\n    .pred_class = predict(fit_obj, data, type = \"class\")$.pred_class\n  )\n}\n\npred_all <- bind_rows(\n  collect_binary_preds(glm_fit, test_data, \"Logistic\"),\n  collect_binary_preds(tree_final_fit, test_data, \"Decision Tree\"),\n  collect_binary_preds(rf_final_fit, test_data, \"Random Forest\"),\n  collect_binary_preds(xgb_final_fit, test_data, \"XGBoost\")\n)\n\npred_all |>\n  slice_head(n = 12) |>\n  knitr::kable(caption = \"Prediction Results Overview (first 12 rows)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Prediction Results Overview (first 12 rows)\n\n|model    |learner_id |item_id | proficiency|condition |test_time |truth     | .pred_correct|.pred_class |\n|:--------|:----------|:-------|-----------:|:---------|:---------|:---------|-------------:|:-----------|\n|Logistic |220        |82      |   0.6354104|sentence  |immediate |incorrect |     0.4485187|incorrect   |\n|Logistic |220        |41      |   0.6354104|sentence  |delayed   |incorrect |     0.3225553|incorrect   |\n|Logistic |47         |87      |   1.0971535|glossing  |delayed   |incorrect |     0.3162753|incorrect   |\n|Logistic |37         |26      |  -0.9354393|retrieval |immediate |incorrect |     0.0915643|incorrect   |\n|Logistic |146        |81      |   0.8210674|glossing  |immediate |incorrect |     0.3113729|incorrect   |\n|Logistic |78         |27      |  -0.6413276|retrieval |immediate |incorrect |     0.0534560|incorrect   |\n|Logistic |94         |43      |  -1.5150228|glossing  |immediate |correct   |     0.0352172|incorrect   |\n|Logistic |153        |105     |   1.4183728|retrieval |delayed   |incorrect |     0.5639888|correct     |\n|Logistic |172        |46      |   0.7325675|retrieval |delayed   |correct   |     0.3614943|incorrect   |\n|Logistic |101        |16      |   0.3274390|glossing  |immediate |incorrect |     0.1871162|incorrect   |\n|Logistic |58         |76      |   0.1365321|sentence  |immediate |correct   |     0.4841992|incorrect   |\n|Logistic |151        |17      |   0.8937135|retrieval |immediate |incorrect |     0.4054849|incorrect   |\n\n\n:::\n:::\n\n\nこの表は、後続の指標計算で使う共通入力です。`truth` の水準順序（`incorrect`, `correct`）と `.pred_correct` の対応が崩れていないことを、ここで一度確認しておくと評価指標の取り違えを防げます。\n\n### 3つの具体ケースで予測値を比べる\n\n全体指標の前に、先ほどの3ケースで「モデルがどう答えるか」を見ます。\n\n\n**このチャンクの目的:** Case A/B/Cの3プロフィールに対する4モデルの予測確率を横並びで比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\ncase_profiles <- tibble(\n  case = c(\n    \"Case A: 高習熟 + retrieval + 即時\",\n    \"Case B: 低習熟 + glossing + 遅延\",\n    \"Case C: 中習熟 + sentence + 遅延\"\n  ),\n  proficiency = c(1.2, -0.8, 0.1),\n  weekly_study = c(6.0, 2.0, 3.5),\n  anxiety = c(-0.5, 0.8, 0.2),\n  l1_distance = c(0.20, 0.75, 0.45),\n  word_freq = c(140, 20, 55),\n  word_length = c(5, 9, 7),\n  cognate = factor(c(\"yes\", \"no\", \"no\"), levels = levels(train_data$cognate)),\n  pos = factor(c(\"noun\", \"verb\", \"adjective\"), levels = levels(train_data$pos)),\n  condition = factor(c(\"retrieval\", \"glossing\", \"sentence\"), levels = levels(train_data$condition)),\n  test_time = factor(c(\"immediate\", \"delayed\", \"delayed\"), levels = levels(train_data$test_time))\n)\n\nmodel_case_pred <- bind_rows(\n  case_profiles |>\n    mutate(model = \"Logistic\",\n           p_correct = predict(glm_fit, case_profiles, type = \"prob\")$.pred_correct),\n  case_profiles |>\n    mutate(model = \"Decision Tree\",\n           p_correct = predict(tree_final_fit, case_profiles, type = \"prob\")$.pred_correct),\n  case_profiles |>\n    mutate(model = \"Random Forest\",\n           p_correct = predict(rf_final_fit, case_profiles, type = \"prob\")$.pred_correct),\n  case_profiles |>\n    mutate(model = \"XGBoost\",\n           p_correct = predict(xgb_final_fit, case_profiles, type = \"prob\")$.pred_correct)\n) |>\n  dplyr::select(case, model, p_correct) |>\n  mutate(p_correct = round(p_correct, 3))\n\nmodel_case_pred |>\n  pivot_wider(names_from = model, values_from = p_correct) |>\n  knitr::kable(caption = \"Case-Based Probability Comparison: Predicted probability of correct response\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case-Based Probability Comparison: Predicted probability of correct response\n\n|case                              | Logistic| Decision Tree| Random Forest| XGBoost|\n|:---------------------------------|--------:|-------------:|-------------:|-------:|\n|Case A: 高習熟 + retrieval + 即時 |    0.830|         0.760|         0.632|   0.821|\n|Case B: 低習熟 + glossing + 遅延  |    0.031|         0.129|         0.090|   0.070|\n|Case C: 中習熟 + sentence + 遅延  |    0.194|         0.270|         0.234|   0.194|\n\n\n:::\n:::\n\n\nケース比較は「平均的にどちらが良いか」ではなく「どんな学習者で判断が割れるか」を見るために使います。教育実務では、この差が支援候補を議論する出発点になります。\n\n::: {.panel-tabset}\n\n### 指標テーブル\n\n\n**このチャンクの目的:** テストデータ上の6指標（ROC-AUC, PR-AUC, Accuracy, Sensitivity, Specificity, Brier）をモデル別に集計します。\n\n::: {.cell}\n\n```{.r .cell-code}\nmetric_summary <- pred_all |>\n  group_by(model) |>\n  summarise(\n    Threshold = 0.50,\n    ROC_AUC = roc_auc_vec(truth, .pred_correct, event_level = \"second\"),\n    PR_AUC = pr_auc_vec(truth, .pred_correct, event_level = \"second\"),\n    Accuracy = accuracy_vec(truth, .pred_class),\n    Sensitivity = sens_vec(truth, .pred_class, event_level = \"second\"),\n    Specificity = spec_vec(truth, .pred_class, event_level = \"second\"),\n    Brier = brier_class_vec(truth, .pred_correct, event_level = \"second\")\n  ) |>\n  arrange(desc(ROC_AUC))\n\nmetric_summary |>\n  mutate(across(where(is.numeric), ~ round(., 3))) |>\n  knitr::kable(caption = \"Model Comparison on Test Set: ROC-AUC is one lens; PR-AUC and Brier are also reported\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Model Comparison on Test Set: ROC-AUC is one lens; PR-AUC and Brier are also reported\n\n|model         | Threshold| ROC_AUC| PR_AUC| Accuracy| Sensitivity| Specificity| Brier|\n|:-------------|---------:|-------:|------:|--------:|-----------:|-----------:|-----:|\n|Random Forest |       0.5|   0.880|  0.796|    0.807|       0.609|       0.907| 0.144|\n|XGBoost       |       0.5|   0.830|  0.707|    0.776|       0.592|       0.868| 0.156|\n|Logistic      |       0.5|   0.787|  0.624|    0.734|       0.455|       0.874| 0.173|\n|Decision Tree |       0.5|   0.765|  0.616|    0.741|       0.472|       0.877| 0.178|\n\n\n:::\n:::\n\n\nEvent level is `correct` (second level).\n\n`metric_summary` は最終順位表ですが、用途に応じて重みづけを変える前提で読みます。ここに出る `Accuracy`、`Sensitivity`、`Specificity` は、各モデルの既定クラス予測（実質的には `P(correct) >= .50`）での値です。運用閾値を決めた後の性能は、後の閾値比較表で別に確認します。例えば「正答を取りこぼさない」ことを重視するなら、ROC-AUCが僅差でもSensitivityやPR-AUCの差で採用モデルが逆転し得ます。\n\n表は正確な値を読むのに向いていますが、モデル間の差を一目で見るには図も有効です。次のスコアカードでは、識別性能、既定閾値での分類性能、確率スコアを同じ画面に並べます。\n\n**このチャンクの目的:** 主要指標をモデル別に可視化し、どの指標で差が出ているかを確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nmodel_order <- metric_summary |>\n  arrange(ROC_AUC) |>\n  pull(model)\n\nmetric_scorecard <- metric_summary |>\n  dplyr::select(\n    model, ROC_AUC, PR_AUC, Accuracy,\n    Sensitivity, Specificity, Brier\n  ) |>\n  pivot_longer(\n    cols = -model,\n    names_to = \"Metric\",\n    values_to = \"Value\"\n  ) |>\n  mutate(\n    model = factor(model, levels = model_order),\n    Metric = factor(\n      Metric,\n      levels = c(\"ROC_AUC\", \"PR_AUC\", \"Accuracy\", \"Sensitivity\", \"Specificity\", \"Brier\")\n    ),\n    Direction = if_else(Metric == \"Brier\", \"Lower is better\", \"Higher is better\")\n  )\n\nmetric_scorecard |>\n  ggplot(aes(x = model, y = Value, fill = Direction)) +\n  geom_col(width = 0.72) +\n  geom_text(\n    aes(label = scales::number(Value, accuracy = 0.001)),\n    hjust = -0.08,\n    size = 3\n  ) +\n  coord_flip() +\n  facet_wrap(~ Metric, ncol = 3) +\n  scale_y_continuous(limits = c(0, 1), expand = expansion(mult = c(0, 0.12))) +\n  scale_fill_manual(values = c(\n    \"Higher is better\" = \"#2C7FB8\",\n    \"Lower is better\" = \"#D95F02\"\n  )) +\n  labs(\n    title = \"Model Comparison Scorecard\",\n    subtitle = \"Brier is the only panel where lower values are better\",\n    x = NULL,\n    y = \"Metric value\",\n    fill = NULL\n  )\n```\n\n::: {.cell-output-display}\n![Model metric scorecard: threshold-free, threshold-dependent, and probabilistic metrics](8_予測分析_ROC曲線とツリーモデル_files/figure-html/model-metric-scorecard-1.png){width=3300}\n:::\n:::\n\n\nこの図では、まずROC-AUCとPR-AUCで識別性能を見ます。次に、Sensitivity / Specificity / Accuracy が既定閾値でどう変わるかを確認します。Brierだけは小さいほど良いので、他のパネルと同じ向きで読まないようにします。本文でモデルを選ぶときは、「XGBoostがROC-AUCで最良だった」だけで終わらせず、「PR-AUC、Brier、閾値依存指標でも同じ判断になるか」を続けて書くと、読者が判断過程を追いやすくなります。\n\nここでの指標は、held-out learners の全項目反応をまとめて評価する **response-level** 指標です。予測単位が「新しい学習者が新しい項目に正答するか」であれば自然ですが、各学習者を同じ重みで評価したい場合は、学習者ごとに指標を計算して平均する **learner-macro** 評価も併記します。ただし、ROC-AUCを学習者ごとに計算するには、その学習者内に `correct` と `incorrect` の両方が必要です。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlearner_macro_summary <- pred_all |>\n  group_by(model, learner_id) |>\n  summarise(\n    Accuracy = accuracy_vec(truth, .pred_class),\n    Brier = brier_class_vec(truth, .pred_correct, event_level = \"second\"),\n    ROC_AUC = if (n_distinct(truth) == 2) {\n      roc_auc_vec(truth, .pred_correct, event_level = \"second\")\n    } else {\n      NA_real_\n    },\n    .groups = \"drop\"\n  ) |>\n  group_by(model) |>\n  summarise(\n    Learners = n(),\n    Learners_with_AUC = sum(!is.na(ROC_AUC)),\n    ROC_AUC_macro = mean(ROC_AUC, na.rm = TRUE),\n    Accuracy_macro = mean(Accuracy),\n    Brier_macro = mean(Brier),\n    .groups = \"drop\"\n  ) |>\n  arrange(desc(ROC_AUC_macro))\n\nlearner_macro_summary |>\n  mutate(across(where(is.numeric), ~ round(., 3))) |>\n  knitr::kable(caption = \"Learner-Macro Metrics: each held-out learner contributes equally\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Learner-Macro Metrics: each held-out learner contributes equally\n\n|model         | Learners| Learners_with_AUC| ROC_AUC_macro| Accuracy_macro| Brier_macro|\n|:-------------|--------:|-----------------:|-------------:|--------------:|-----------:|\n|Random Forest |       43|                38|         0.853|          0.813|       0.140|\n|XGBoost       |       43|                38|         0.711|          0.784|       0.152|\n|Logistic      |       43|                38|         0.610|          0.747|       0.168|\n|Decision Tree |       43|                38|         0.581|          0.754|       0.173|\n\n\n:::\n:::\n\n\n各指標が答える問いは異なります。`Accuracy` は全体の当たりやすさ、`Sensitivity` は正答者を拾えるか、`Specificity` は誤答者を正しく除外できるか、`Brier` は確率予測の総合的な誤差を表します。結果節では「どの問いを優先したか」を先に宣言してから指標値を提示すると、読者が判断軸を追いやすくなります。\n\n### 平均性能だけで見落とすもの（サブグループ別性能）\n\n全体のROC-AUCやAccuracyが高くても、特定の層で性能が大きく落ちることがあります。教育データでは、習熟度帯、テスト時点、教材条件、L1背景などで誤りの出方が変わる場合があるため、モデルを実際の判断に近づけるほど、平均性能だけでなく**どの層で外しやすいか**を確認する必要があります。\n\nここでは例として、`proficiency` を3分位で低・中・高に分け、さらに `test_time`（immediate / delayed）で分けてXGBoostの性能を確認します。この確認は、厳密な公平性監査ではありません。正式な公平性評価では、どの属性を、どの指標で、どの許容差まで確認するかを事前に定義する必要があります。\n\n**このチャンクの目的:** XGBoostの予測性能を習熟度帯 × テスト時点ごとに集計し、平均性能の裏にある弱い領域を確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nmake_proficiency_band <- function(x) {\n  cut(\n    x,\n    breaks = quantile(x, probs = c(0, 1 / 3, 2 / 3, 1), na.rm = TRUE),\n    include.lowest = TRUE,\n    labels = c(\"Low\", \"Mid\", \"High\")\n  )\n}\n\nsafe_auc_group <- function(d) {\n  if (n_distinct(d$truth) < 2) return(NA_real_)\n  roc_auc_vec(d$truth, d$.pred_correct, event_level = \"second\")\n}\n\ncalc_group_metrics <- function(d) {\n  threshold_eval <- evaluate_threshold(d$truth, d$.pred_correct, 0.50)\n\n  tibble(\n    n = nrow(d),\n    correct_rate = mean(d$truth == \"correct\"),\n    ROC_AUC = safe_auc_group(d),\n    Accuracy = (threshold_eval$TP + threshold_eval$TN) / nrow(d),\n    Sensitivity = threshold_eval$Sensitivity,\n    Specificity = threshold_eval$Specificity,\n    Precision = threshold_eval$Precision,\n    Brier = brier_class_vec(d$truth, d$.pred_correct, event_level = \"second\")\n  )\n}\n\nsubgroup_summary <- pred_all |>\n  mutate(proficiency_band = make_proficiency_band(proficiency)) |>\n  group_by(model, proficiency_band, test_time) |>\n  group_modify(~ calc_group_metrics(.x)) |>\n  ungroup()\n\nsubgroup_summary |>\n  dplyr::filter(model == \"XGBoost\") |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  arrange(proficiency_band, test_time) |>\n  knitr::kable(caption = \"Subgroup Performance for XGBoost: proficiency band by test time\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Subgroup Performance for XGBoost: proficiency band by test time\n\n|model   |proficiency_band |test_time |   n| correct_rate| ROC_AUC| Accuracy| Sensitivity| Specificity| Precision| Brier|\n|:-------|:----------------|:---------|---:|------------:|-------:|--------:|-----------:|-----------:|---------:|-----:|\n|XGBoost |Low              |delayed   |  83|        0.060|   0.656|    0.940|       0.000|       1.000|        NA| 0.060|\n|XGBoost |Low              |immediate | 152|        0.118|   0.776|    0.882|       0.000|       1.000|        NA| 0.095|\n|XGBoost |Mid              |delayed   |  93|        0.355|   0.763|    0.731|       0.424|       0.900|     0.700| 0.184|\n|XGBoost |Mid              |immediate | 147|        0.374|   0.790|    0.728|       0.455|       0.891|     0.714| 0.176|\n|XGBoost |High             |delayed   |  83|        0.518|   0.703|    0.687|       0.744|       0.625|     0.681| 0.218|\n|XGBoost |High             |immediate | 137|        0.577|   0.734|    0.693|       0.848|       0.483|     0.691| 0.204|\n\n\n:::\n:::\n\n\n**このチャンクの目的:** サブグループ別性能をヒートマップ化し、弱いセルを視覚的に見つけます。\n\n::: {.cell}\n\n```{.r .cell-code}\nsubgroup_summary |>\n  dplyr::filter(model == \"XGBoost\") |>\n  dplyr::select(proficiency_band, test_time, ROC_AUC, Sensitivity, Specificity, Precision) |>\n  pivot_longer(\n    cols = c(ROC_AUC, Sensitivity, Specificity, Precision),\n    names_to = \"Metric\",\n    values_to = \"Value\"\n  ) |>\n  mutate(\n    Metric = factor(\n      Metric,\n      levels = c(\"ROC_AUC\", \"Sensitivity\", \"Specificity\", \"Precision\")\n    ),\n    Label = if_else(is.na(Value), \"NA\", scales::number(Value, accuracy = 0.01))\n  ) |>\n  ggplot(aes(x = test_time, y = proficiency_band, fill = Value)) +\n  geom_tile(color = \"white\", linewidth = 1) +\n  geom_text(aes(label = Label), size = 3.5) +\n  scale_fill_gradient(low = \"#FEE8C8\", high = \"#2C7FB8\", limits = c(0, 1), na.value = \"gray90\") +\n  facet_wrap(~ Metric, ncol = 4) +\n  labs(\n    title = \"XGBoost Performance by Subgroup\",\n    subtitle = \"Rows = proficiency bands; columns = test time\",\n    x = \"Test time\",\n    y = \"Proficiency band\",\n    fill = \"Metric\"\n  )\n```\n\n::: {.cell-output-display}\n![Subgroup performance heatmap for XGBoost](8_予測分析_ROC曲線とツリーモデル_files/figure-html/subgroup-performance-heatmap-1.png){width=2700}\n:::\n:::\n\n\nこの図で重要なのは、「どのセルが低いか」を見ることです。たとえば delayed × Low proficiency でSensitivityが低いなら、そのモデルは正答する低習熟度学習者を誤って `incorrect` 側に置きやすい可能性があります。Specificityが低いなら、誤答する観測を `correct` 側に置きやすい可能性があります。`NA` は、そのサブグループ内に該当する真値クラスや予測クラスがなく、分母が0になるため指標を計算できないセルです。こうした差は、モデルを採用しない理由になる場合もあれば、閾値をサブグループ別に調整する理由になる場合もあります。ただし、サブグループ別閾値は説明責任や公平性の問題を伴うため、研究目的と倫理的妥当性を明記して扱います。\n\n### 公式ドキュメント補足（event level）\n\n- `yardstick` の2値指標は、イベント水準を明示しないと既定で第1水準を採用します。\n- 本章のように `levels = c(\"incorrect\", \"correct\")` の場合、正答側をイベントにするには `event_level = \"second\"` が必要です。\n\n参考:\n- [yardstick: roc_auc()](https://yardstick.tidymodels.org/reference/roc_auc.html)\n- [yardstick: sens()](https://yardstick.tidymodels.org/reference/sens.html)\n\n解釈の指針:\n\n- まず `ROC_AUC` と `PR_AUC` で上位候補を2つ程度に絞ります。\n- 次に `Sensitivity` / `Specificity` と `Brier` で用途適合性を確認します。\n\n::: {.callout-important}\n本章では `correct` を陽性クラスにしています。したがって、ここでの `Sensitivity` は「正答する観測を正答側として拾えるか」を意味します。支援対象の早期抽出が目的なら、目的変数を `risk` / `incorrect` 側に作り直して陽性クラスにするか、`P(correct)` が低い観測を支援候補にするルールとして別に定義します。`correct` の感度を、そのまま「リスクの検出率」と読まないことが重要です。\n:::\n\n### 指標確認の優先順序\n\n1. 研究で「正答者の取りこぼしが痛い」なら `Sensitivity` と `PR_AUC` を優先します。支援対象の見逃しを扱うなら、陽性クラスを `incorrect` / `risk` 側に再定義します。\n2. 全体性能を比較する場合は `ROC_AUC` を確認します。\n3. 予測確率そのものを使うなら `Brier` とキャリブレーション図を確認します。\n\n### ROC曲線の直感\n\nROC曲線がどうやって作られるかを直感的に確認しておきます。\n\n1. 閾値を **1.0**（全員を incorrect と判定）に設定します → TPR = 0, FPR = 0（左下の点）。\n2. 閾値を少しずつ**下げます** → correct と判定される人が増え、TPR（感度）が上がります。同時にFPR（偽陽性率）も上がります。\n3. 閾値を **0.0**（全員を correct と判定）にします → TPR = 1, FPR = 1（右上の点）。\n4. 各閾値で得られた (FPR, TPR) の点をつないだ曲線がROC曲線です。\n\n**AUCの直感的意味**: ランダムに正答者1人と誤答者1人を選んだとき、モデルが**正答者に高い確率を与える割合**。AUC = 1.0 なら完璧な識別、AUC = 0.5 ならランダムと同等です[@fawcett2006]。\n\n### AUC信頼区間\n\n指標テーブルの点推定だけでは、モデル間の差がどれくらい不確実かを判断できません。ここではブートストラップでROC-AUCの95%信頼区間を計算し、点推定だけに頼らない比較にします。ただし、個別モデルのCIの重なりは有意差検定そのものではありません。モデル差を主張したい場合は、同じ再標本化単位でAUC差の信頼区間を直接計算するか、DeLong検定などの対応のある比較を使います。\n\nここでも、評価対象は「未知学習者への汎化」です。そのため、行単位ではなく `learner_id` 単位で再標本化します。行単位に戻すと、同じ学習者の複数項目反応を独立な観測として扱い、CIが過度に狭く見える可能性があります。\n\n**このチャンクの目的:** 学習者クラスタ単位のブートストラップ500回でROC-AUCの95%CIを計算し、フォレストプロットで比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(42)\nn_boot <- 500\n\nsafe_auc <- function(d) {\n  if (n_distinct(d$truth) < 2) return(NA_real_)\n  roc_auc_vec(d$truth, d$.pred_correct, event_level = \"second\")\n}\n\nsafe_quantile <- function(x, prob) {\n  x <- x[!is.na(x)]\n  if (length(x) == 0) return(NA_real_)\n  unname(quantile(x, prob))\n}\n\nboot_auc <- pred_all |>\n  group_by(model) |>\n  group_map(~ {\n    d <- .x\n    learner_ids <- unique(d$learner_id)\n    aucs <- replicate(n_boot, {\n      sampled_learners <- tibble(\n        learner_id = sample(learner_ids, length(learner_ids), replace = TRUE),\n        boot_draw = seq_along(learner_id)\n      )\n      d_boot <- sampled_learners |>\n        left_join(d, by = \"learner_id\")\n\n      safe_auc(d_boot)\n    })\n    tibble(\n      model = .y$model,\n      auc   = safe_auc(d),\n      lower = safe_quantile(aucs, 0.025),\n      upper = safe_quantile(aucs, 0.975)\n    )\n  }) |>\n  bind_rows()\n\nboot_auc |>\n  ggplot(aes(x = reorder(model, auc), y = auc)) +\n  geom_point(size = 3, color = \"#2C7FB8\") +\n  geom_errorbar(aes(ymin = lower, ymax = upper), width = 0.2, color = \"#2C7FB8\") +\n  coord_flip() +\n  labs(\n    title = \"ROC-AUC with 95% Bootstrap CI\",\n    subtitle = paste0(\"B = \", format(n_boot, big.mark = \",\")),\n    x = NULL, y = \"ROC-AUC\"\n  )\n```\n\n::: {.cell-output-display}\n![ROC-AUC with 95% learner-cluster bootstrap confidence intervals](8_予測分析_ROC曲線とツリーモデル_files/figure-html/auc-bootstrap-ci-1.png){width=2700}\n:::\n:::\n\n\nCIが大きく重なっているモデル同士では、このデータだけで優劣を強く主張しにくいです。教育研究ではAPA 7に沿ってCIを報告することが推奨されており、点推定だけでなくCIを併記することで、結果の不確実性を正直に示せます。\n\n### AUC-CIの読み方\n\n**まずここを見ます**: 各モデルのエラーバー（CI幅）が重なっているかどうかです。\n\n- CIが**重なっていない**モデル同士: 性能差が比較的大きい可能性があります。ただし、正式にはAUC差のCIや対応のある検定で確認します。\n- CIが**大きく重なっている**モデル同士: このデータサイズでは優劣が確定しません。同じ実験を100回繰り返したら、どちらが勝つか変わる可能性があります。\n- CI幅が**狭い**（例: 0.81–0.83）: 推定が安定しています。CI幅が**広い**（例: 0.72–0.88）: サンプルサイズ不足の可能性があります。\n- 指標テーブルの点推定AUCと比べて、CIの**下限**がより現実的な性能見積もりと言えます。\n\n### ROC曲線\n\n\n**このチャンクの目的:** 4モデルのROC曲線を重ね描きし、識別性能を視覚的に比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\npred_all |>\n  group_by(model) |>\n  roc_curve(truth, .pred_correct, event_level = \"second\") |>\n  autoplot() +\n  labs(\n    title = \"ROC Curve Comparison\",\n    subtitle = \"Higher and more leftward is better\",\n    x = \"False Positive Rate\",\n    y = \"True Positive Rate\"\n  )\n```\n\n::: {.cell-output-display}\n![ROC curves for four models](8_予測分析_ROC曲線とツリーモデル_files/figure-html/roc-curves-1.png){width=3000}\n:::\n:::\n\n\n曲線が交差している場合、単純にAUCだけで優劣を断定しない方が安全です。特定の運用領域（低FPR帯など）でどちらが優位かを併記すると、実務判断に接続しやすくなります。\n\n### PR曲線\n\n\n**このチャンクの目的:** 4モデルのPR曲線を重ね描きし、陽性クラスの検出品質を比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\npred_all |>\n  group_by(model) |>\n  pr_curve(truth, .pred_correct, event_level = \"second\") |>\n  autoplot() +\n  labs(\n    title = \"Precision-Recall Curve Comparison\",\n    subtitle = \"Useful when positive class is relatively rare\",\n    x = \"Recall\",\n    y = \"Precision\"\n  )\n```\n\n::: {.cell-output-display}\n![Precision-Recall curves for four models](8_予測分析_ROC曲線とツリーモデル_files/figure-html/pr-curves-1.png){width=3000}\n:::\n:::\n\n\nPR曲線は陽性クラス（ここでは `correct`）の検出品質に敏感です。クラス不均衡が強い設定では、ROCよりPRの差が意思決定に直結するため、両方を並記する意義があります。\n\n特に陽性率が低いデータでは、ROC-AUCが高く見えても実務上のPrecisionが十分でない場合があります。運用判断に近い解釈を重視するなら、PR曲線上で想定Recall帯におけるPrecisionを具体的に読むのが有効です。\n\n### ここでの読み方\n\n- ROC-AUCが近いモデルでも、PR-AUCでは差が広がることがあります。\n- 教育現場で陽性クラスの取りこぼし（FN）を避けたいなら、Recall/PrecisionのバランスをPRで確認する方が実務的です。支援対象を扱う場合は、支援対象が陽性クラスになるよう目的変数を設計します。\n\n:::\n\n### 陽性クラスが極端に少ないとき: 不均衡データの追加対処 {#sec-class-imbalance}\n\n陽性率が 5–10% 未満になると、PR-AUC や閾値調整だけでは安定した運用にならない場合があります。本章のメイン例は概ね均衡しているデータですが、教育研究では「成績不振リスク（ごく一部の学習者）」「稀少エラータイプ」「中退・離脱予測」のように、**陽性クラスが極端に少ないシナリオ**も頻出します。実務上のオプションを 3 系統に整理します。\n\n| 対処 | 実装例 (R) | 性質 | 注意点 |\n|---|---|---|---|\n| **アルゴリズム側の重み付け** | `boost_tree(...) |> set_engine(\"xgboost\", scale_pos_weight = neg/pos)`、`ranger::ranger(..., class.weights = c(...))` | 学習データを変えずにクラス重みを調整。確率予測の性質を比較的保つ | 重みの値は CV 上で調整。極端な重みは過学習の方向に進みやすい |\n| **リサンプリング (recipe ステップ)** | `themis::step_downsample(positive_class)`、`themis::step_smote()`、`themis::step_rose()` [@themispkg] | 訓練データの陽性比率を物理的に変える。tidymodels の recipe に統合可能 | **訓練データのみ**で適用 (CV の `analysis` セットだけ)。`assessment` セットや test set には適用しない。確率を直接使うなら**再校正必須** |\n| **閾値ベースの後処理** | `probably::threshold_perf()` で運用閾値を低めに設定 | データもモデルも変えず、判定境界だけを動かす | 解釈は容易だが、確率自体の分布は変わらない |\n\n::: {.callout-warning}\n#### リサンプリング後の確率は再校正が必須\n`step_downsample` や `step_smote` を使うと訓練データの陽性比率が変わるため、モデルが返す確率は**元の母集団の陽性率を反映しなくなります**。確率を業務判断にそのまま使う場合は、本章後半のキャリブレーション節と `probably::cal_estimate_logistic()` / `cal_estimate_isotonic()` で再校正することが必須です。閾値だけで運用するなら再校正は不要ですが、報告では「リサンプリング後の確率はベースレートを反映しない」ことを明記してください。\n:::\n\n実装の最小例 (recipe に SMOTE ステップを追加):\n\n```r\nlibrary(themis)\n\nrec_smote <- recipe(correct ~ ., data = train_dat) |>\n  step_normalize(all_numeric_predictors()) |>\n  step_dummy(all_nominal_predictors()) |>\n  step_smote(correct, over_ratio = 1.0)  # 陽性:陰性 = 1:1 まで増やす\n\nwf_smote <- workflow() |>\n  add_recipe(rec_smote) |>\n  add_model(boost_tree(...) |> set_engine(\"xgboost\"))\n```\n\nXGBoost で `scale_pos_weight` を渡す場合:\n\n```r\nlibrary(parsnip)\n\nxgb_spec <- boost_tree(...) |>\n  set_engine(\"xgboost\", scale_pos_weight = sum(train_dat$correct == \"incorrect\") /\n                                          sum(train_dat$correct == \"correct\"))\n```\n\n選択の目安は次の通りです。\n\n- **陽性率が 30% 以上**: 通常の閾値調整 + Youden / コストベースで十分。リサンプリング不要。\n- **陽性率が 10–30%**: PR-AUC を主指標にし、必要に応じて `scale_pos_weight` か閾値の側で対処。\n- **陽性率が 10% 未満**: `themis::step_downsample` + 再校正、または `scale_pos_weight` を CV で探索。確率の使い道（判定だけか、確率そのものを伝えるか）を先に決めてから手法を選ぶ。\n\n---\n\n## 閾値をどう決めるか（ROCを運用に接続）\n\n### 概要\n\nAUCは閾値なしの比較指標です。実務では1つの閾値を決めて「支援候補にする/しない」を判断します。閾値の選び方は目的次第で、バランス型（Youden）、取りこぼし回避型（低閾値）、誤警報回避型（高閾値）に分かれます。\n\nAUCは「ランク付けの上手さ」を示しますが、現場では最終的に**閾値**が必要です。ここが「研究結果を実務判断へ変換する」一番重要な節です。閾値は、最終テストデータではなく、訓練データ内のCV予測や別の検証データで決めます。テストデータは、事前に決めたモデルと閾値を最後に評価するために残します。\n\nYouden's J は、Sensitivity と Specificity の和を最大化する古典的な規則です[@youden1950]。ただし、この規則は誤分類コストが対称に近い場合の出発点であり、教育支援のように「見逃し」と「誤警報」の重みが異なる場面では、コスト構造を別に定義する必要があります。したがって、本章では Youden 閾値を「研究報告のバランス型基準」として扱い、運用判断ではコストベース閾値も併記します。\n\n### 閾値選択の3つのアプローチ\n\n閾値の決め方は目的によって異なります。以下の3つが代表的です。\n\n\n| Approach | Formula | Best for | Example |\n|---|---|---|---|\n| Youden's J（バランス型） | Sensitivity + Specificity - 1 を最大化 | 感度と特異度のバランスが重要な場面 | 研究報告の出発点 |\n| コストベース | FNコスト × FN + FPコスト × FP を最小化 | 取りこぼしと誤警報のコストが非対称な場面 | 語彙スクリーニング（支援対象を陽性クラスに置く場合） |\n| 固定Sensitivity | Sensitivity ≥ 0.90 に固定し、対応するSpecificityを確認 | 一定以上の検出率を保証したい場面 | 進級判定の補助ツール |\n\n: Three Approaches to Threshold Selection\n\n### 例: YoudenのJで閾値を選ぶ\n\nここでは例として、最上位モデル（ここでは`XGBoost`を想定）でYoudenのJを使います。重要なのは、YoudenのJを**テストデータではなくCV予測**から計算することです。\n\n\n**このチャンクの目的:** XGBoostのCV予測からYouden's Jを計算し、テスト前に閾値を選択します。\n\n::: {.cell}\n\n```{.r .cell-code}\nxgb_pred <- pred_all |> dplyr::filter(model == \"XGBoost\")\n\nxgb_cv_pred <- collect_predictions(xgb_res, parameters = xgb_best) |>\n  transmute(\n    truth = correct,\n    .pred_correct = .pred_correct\n  )\n\nroc_tbl_xgb <- xgb_cv_pred |>\n  roc_curve(truth, .pred_correct, event_level = \"second\") |>\n  dplyr::filter(is.finite(.threshold)) |>\n  mutate(youden_j = sensitivity + specificity - 1)\n\nbest_threshold <- roc_tbl_xgb |>\n  slice_max(youden_j, n = 1, with_ties = FALSE) |>\n  pull(.threshold)\n\nbest_threshold\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.328\n```\n\n\n:::\n:::\n\n\nここで得た閾値は、訓練データ内のCV予測から選んだ「この学習手順での運用閾値」です。別データにそのまま移植するのではなく、再学習時に同じ手順で再推定する運用ルールを明記しておくと再現性が高まります。テストデータは次の混同行列で初めて使い、閾値選択には使いません。\n\n閾値がどこに置かれているかは、まず予測確率の分布で確認すると分かりやすくなります。正答と誤答の分布がよく分かれていれば、どこに閾値を置いても混同行列は安定しやすくなります。一方、2つの分布が大きく重なっているなら、どの閾値を選んでもFNとFPのトレードオフが残ります。\n\n**このチャンクの目的:** XGBoostのテスト予測確率分布を正誤別に描き、CVで選んだ閾値がどの位置にあるかを確認します。\n\n::: {.cell}\n\n```{.r .cell-code}\nxgb_pred |>\n  ggplot(aes(x = .pred_correct, fill = truth)) +\n  geom_histogram(\n    bins = 30,\n    position = \"identity\",\n    alpha = 0.55,\n    color = \"white\"\n  ) +\n  geom_vline(\n    xintercept = best_threshold,\n    linewidth = 1,\n    linetype = \"dashed\",\n    color = \"#1B1B1B\"\n  ) +\n  scale_fill_manual(values = c(\"incorrect\" = \"#D95F02\", \"correct\" = \"#2C7FB8\")) +\n  labs(\n    title = \"Predicted Score Distribution: XGBoost\",\n    subtitle = \"Dashed line = CV-selected Youden threshold\",\n    x = \"Predicted P(correct)\",\n    y = \"Number of observations\",\n    fill = \"Observed outcome\"\n  )\n```\n\n::: {.cell-output-display}\n![Distribution of predicted P(correct) by observed outcome, with the CV-selected threshold](8_予測分析_ROC曲線とツリーモデル_files/figure-html/xgb-score-distribution-1.png){width=2700}\n:::\n:::\n\n\nこの図では、破線より右側が `correct` 判定、左側が `incorrect` 判定になります。青と橙が大きく重なる領域は、モデルにとって判定が難しい観測です。支援候補を `P(correct)` の低さで定義する場合は、左側の領域にどれだけ観測が集まるかを確認します。\n\n\n**このチャンクの目的:** CVで選んだYouden閾値をテストデータに適用し、混同行列を出力します。\n\n::: {.cell}\n\n```{.r .cell-code}\nxgb_threshold_eval <- xgb_pred |>\n  mutate(\n    .pred_class_j = factor(\n      if_else(.pred_correct >= best_threshold, \"correct\", \"incorrect\"),\n      levels = levels(truth)\n    )\n  )\n\nconf_mat(xgb_threshold_eval, truth = truth, estimate = .pred_class_j)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n           Truth\nPrediction  incorrect correct\n  incorrect       376      79\n  correct         100     146\n```\n\n\n:::\n:::\n\n\n混同行列では、合計精度よりも `FN` と `FP` の内訳を優先して確認します。教育支援の場面では、どちらの誤りが高コストかを先に定義してから閾値を調整するのが実務的です。\n\n数表の混同行列は正確ですが、結果節で説明するときは図にした方が誤りの型を示しやすくなります。\n\n**このチャンクの目的:** CV-Youden閾値でのXGBoost混同行列をヒートマップ化し、各真値クラス内の割合も表示します。\n\n::: {.cell}\n\n```{.r .cell-code}\nconf_heatmap_tbl <- xgb_threshold_eval |>\n  count(truth, predicted = .pred_class_j, name = \"n\") |>\n  group_by(truth) |>\n  mutate(row_prop = n / sum(n)) |>\n  ungroup() |>\n  mutate(\n    label = paste0(n, \"\\n\", scales::percent(row_prop, accuracy = 0.1)),\n    truth = factor(truth, levels = c(\"correct\", \"incorrect\")),\n    predicted = factor(predicted, levels = c(\"incorrect\", \"correct\"))\n  )\n\nconf_heatmap_tbl |>\n  ggplot(aes(x = predicted, y = truth, fill = n)) +\n  geom_tile(color = \"white\", linewidth = 1.2) +\n  geom_text(aes(label = label), size = 4, lineheight = 0.9) +\n  scale_fill_gradient(low = \"#F7FBFF\", high = \"#2C7FB8\") +\n  coord_equal() +\n  labs(\n    title = \"Confusion Matrix at the CV-Selected Threshold\",\n    subtitle = \"Cell labels show count and row percentage\",\n    x = \"Predicted class\",\n    y = \"Observed class\",\n    fill = \"Count\"\n  )\n```\n\n::: {.cell-output-display}\n![Confusion matrix heatmap for XGBoost at the CV-selected threshold](8_予測分析_ROC曲線とツリーモデル_files/figure-html/confusion-heatmap-xgb-1.png){width=2250}\n:::\n:::\n\n\nこの図では、対角セルが正しく分類された観測、対角外セルが誤分類です。`truth = correct` で `predicted = incorrect` のセルは、正答する観測を取りこぼしたケースです。逆に `truth = incorrect` で `predicted = correct` のセルは、誤答する観測を正答側に置いたケースです。どちらを重く見るかは、研究目的と運用コストで決まります。\n\n### 閾値を変えると何が変わるか（実データで比較）\n\n\n**このチャンクの目的:** 3つの閾値候補（0.30, CV-Youden, 0.70）をテストデータに適用し、混同行列指標を横並び比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\nthreshold_cmp <- bind_rows(\n  evaluate_threshold(xgb_pred$truth, xgb_pred$.pred_correct, 0.30),\n  evaluate_threshold(xgb_pred$truth, xgb_pred$.pred_correct, best_threshold),\n  evaluate_threshold(xgb_pred$truth, xgb_pred$.pred_correct, 0.70)\n) |>\n  mutate(Setting = c(\"正答Recall重視\", \"CV-Youden\", \"正答Precision重視\")) |>\n  dplyr::select(Setting, threshold, TP, FP, TN, FN, Sensitivity, Specificity, Precision)\n\nthreshold_cmp |>\n  mutate(across(where(is.numeric), ~ round(., 3))) |>\n  knitr::kable(caption = \"Threshold Trade-off Table (XGBoost): Lower threshold generally increases recall but also false positives\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Threshold Trade-off Table (XGBoost): Lower threshold generally increases recall but also false positives\n\n|Setting           | threshold|  TP|  FP|  TN|  FN| Sensitivity| Specificity| Precision|\n|:-----------------|---------:|---:|---:|---:|---:|-----------:|-----------:|---------:|\n|正答Recall重視    |     0.300| 186| 145| 317|  47|       0.798|       0.686|     0.562|\n|CV-Youden         |     0.328| 182| 129| 333|  51|       0.781|       0.721|     0.585|\n|正答Precision重視 |     0.700|  57|  13| 449| 176|       0.245|       0.972|     0.814|\n\n\n:::\n:::\n\n\n上から順に、`correct` を陽性クラスにした場合の「正答の取りこぼしを減らす設定」→「バランス設定」→「誤って正答扱いする観測を減らす設定」と読めます。支援対象の検出を主目的にする場合は、`incorrect` / `risk` を陽性クラスにして同じ表を作るか、`P(correct)` が低い観測を支援候補とする別ルールを定義します。\n\n表だけでは、閾値を少し動かしたときの変化が見えにくいので、閾値を0.05から0.95まで動かした曲線も描きます。\n\n**このチャンクの目的:** XGBoostで閾値を連続的に変えたとき、Sensitivity・Specificity・Precisionがどう動くかを可視化します。\n\n::: {.cell}\n\n```{.r .cell-code}\nthreshold_path <- map_dfr(\n  seq(0.05, 0.95, by = 0.01),\n  \\(th) evaluate_threshold(xgb_pred$truth, xgb_pred$.pred_correct, th)\n) |>\n  dplyr::select(threshold, Sensitivity, Specificity, Precision) |>\n  pivot_longer(\n    cols = c(Sensitivity, Specificity, Precision),\n    names_to = \"Metric\",\n    values_to = \"Value\"\n  )\n\nthreshold_path |>\n  ggplot(aes(x = threshold, y = Value, color = Metric)) +\n  geom_line(linewidth = 1, na.rm = TRUE) +\n  geom_vline(\n    xintercept = best_threshold,\n    linewidth = 0.9,\n    linetype = \"dashed\",\n    color = \"gray35\"\n  ) +\n  scale_y_continuous(limits = c(0, 1), labels = scales::percent_format(accuracy = 1)) +\n  scale_color_manual(values = c(\n    \"Sensitivity\" = \"#2C7FB8\",\n    \"Specificity\" = \"#D95F02\",\n    \"Precision\" = \"#1B9E77\"\n  )) +\n  labs(\n    title = \"Threshold Trade-off: XGBoost\",\n    subtitle = \"Dashed line = CV-selected Youden threshold\",\n    x = \"Threshold for P(correct)\",\n    y = \"Metric value\",\n    color = NULL\n  )\n```\n\n::: {.cell-output-display}\n![Threshold trade-off curve for XGBoost](8_予測分析_ROC曲線とツリーモデル_files/figure-html/threshold-tradeoff-curve-1.png){width=2700}\n:::\n:::\n\n\nこの図は、閾値設計の説明に向いています。低い閾値では `correct` と判定される観測が増え、Sensitivityは上がりやすい一方、誤って `correct` とするFPも増えやすくなります。高い閾値では逆に、`correct` 判定は慎重になります。最終報告では、採用した閾値だけでなく、その閾値がどのトレードオフ上にあるかを示すと判断過程が透明になります。\n\n閾値を決める際は、次の順序で運用ルールを固定すると実務でぶれにくくなります。\n1. 目的を1つに絞る（例: 支援対象の見逃し最小化）。\n2. その目的に対応する主指標を決める（例: Sensitivity）。\n3. 許容できる副作用の上限を決める（例: FP率の上限）。\n4. その条件を満たす閾値候補を比較して最終採用します。\n\n### 実務ではコストベースで閾値を決める\n\n- 支援対象を `risk` / `incorrect` 側の陽性クラスとして定義する早期支援モデルなら、「支援対象の見逃し（FN）コスト」が高いです。\n- 本章のように `correct` を陽性クラスにする場合は、`P(correct)` が低い観測を支援候補として扱うため、「誤答する観測を正答扱いするFP」のコストを高く見ます。\n\nここで注意したいのは、**FP/FNの意味はpositive classによって変わる**という点です。本章では `correct` を陽性クラスにしているため、\n\n- **FN**: 本当は `correct` なのに `incorrect` と判定した観測（不必要に支援候補へ回す可能性）\n- **FP**: 本当は `incorrect` なのに `correct` と判定した観測（支援が必要な観測を見逃す可能性）\n\nになります。したがって、`P(correct)` を使って支援候補を拾うなら、`FP` のコストを高く置く設定が自然です。もし目的変数を `risk` / `incorrect` 側に作り替えて、それを陽性クラスにするなら、支援対象の見逃しは `FN` として数えられます。\n\n次の例では、`correct` を陽性クラスにしたまま、**誤答する観測を正答扱いするFPをFNの4倍重く見る**という仮定で、CV予測上の期待コストが最小になる閾値を選びます。この4倍という値は分析結果から推定するものではなく、研究目的・教育判断・運用資源に基づいて事前に決める設計値です。\n\n**このチャンクの目的:** CV予測上でFP/FNに異なるコストを置き、期待コストを最小にする閾値を選択します。\n\n::: {.cell}\n\n```{.r .cell-code}\nevaluate_threshold_cost <- function(truth, prob, threshold, cost_fn = 1, cost_fp = 1) {\n  evaluate_threshold(truth, prob, threshold) |>\n    mutate(\n      Cost_FN = cost_fn,\n      Cost_FP = cost_fp,\n      Total_Cost = cost_fn * FN + cost_fp * FP,\n      Cost_per_obs = Total_Cost / length(truth)\n    )\n}\n\ncost_fn <- 1\ncost_fp <- 4\n\ncost_threshold_grid <- map_dfr(\n  seq(0.05, 0.95, by = 0.01),\n  \\(th) evaluate_threshold_cost(\n    xgb_cv_pred$truth,\n    xgb_cv_pred$.pred_correct,\n    threshold = th,\n    cost_fn = cost_fn,\n    cost_fp = cost_fp\n  )\n)\n\ncost_threshold <- cost_threshold_grid |>\n  slice_min(Cost_per_obs, n = 1, with_ties = FALSE) |>\n  pull(threshold)\n\ncost_threshold\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.59\n```\n\n\n:::\n:::\n\n\n**このチャンクの目的:** CV予測上の閾値別コスト曲線を描き、Youden閾値とコスト最小化閾値を比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\ncost_threshold_grid |>\n  ggplot(aes(x = threshold, y = Cost_per_obs)) +\n  geom_line(linewidth = 1, color = \"#1B9E77\") +\n  geom_vline(\n    xintercept = best_threshold,\n    linewidth = 0.9,\n    linetype = \"dashed\",\n    color = \"#2C7FB8\"\n  ) +\n  geom_vline(\n    xintercept = cost_threshold,\n    linewidth = 0.9,\n    linetype = \"dotdash\",\n    color = \"#D95F02\"\n  ) +\n  labs(\n    title = \"Cost-Based Threshold Selection\",\n    subtitle = paste0(\n      \"CV predictions; FN cost = \", cost_fn,\n      \", FP cost = \", cost_fp,\n      \"; dashed = Youden, dot-dash = cost-min\"\n    ),\n    x = \"Threshold for P(correct)\",\n    y = \"Expected cost per observation\"\n  )\n```\n\n::: {.cell-output-display}\n![Cost curve for threshold selection based on cross-validated XGBoost predictions](8_予測分析_ROC曲線とツリーモデル_files/figure-html/cost-threshold-curve-1.png){width=2700}\n:::\n:::\n\n\nこの図では、曲線が低いほど、その閾値での誤分類コストが小さいことを意味します。Youden閾値はSensitivityとSpecificityのバランスを取る規則ですが、コスト最小化閾値は「どちらの誤りを重く見るか」を明示的に反映します。2つの縦線が離れている場合は、研究報告で「統計的にバランスのよい閾値」と「運用上のコストを反映した閾値」が異なることを説明します。\n\n最後に、CVで選んだ2つの閾値をテストデータへ適用し、性能とコストを比較します。ここでも、テストデータで閾値を選び直していない点が重要です。\n\n**このチャンクの目的:** CV-Youden閾値とCVコスト最小化閾値をテストデータに適用し、混同行列指標とコストを比較します。\n\n::: {.cell}\n\n```{.r .cell-code}\nbind_rows(\n  evaluate_threshold_cost(\n    xgb_pred$truth,\n    xgb_pred$.pred_correct,\n    threshold = best_threshold,\n    cost_fn = cost_fn,\n    cost_fp = cost_fp\n  ) |>\n    mutate(Threshold_Rule = \"CV-Youden\"),\n  evaluate_threshold_cost(\n    xgb_pred$truth,\n    xgb_pred$.pred_correct,\n    threshold = cost_threshold,\n    cost_fn = cost_fn,\n    cost_fp = cost_fp\n  ) |>\n    mutate(Threshold_Rule = \"CV cost-min\")\n) |>\n  dplyr::select(\n    Threshold_Rule, threshold, TP, FP, TN, FN,\n    Sensitivity, Specificity, Precision, Cost_per_obs\n  ) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Test-set comparison of CV-selected Youden and cost-minimizing thresholds\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Test-set comparison of CV-selected Youden and cost-minimizing thresholds\n\n|Threshold_Rule | threshold|  TP|  FP|  TN|  FN| Sensitivity| Specificity| Precision| Cost_per_obs|\n|:--------------|---------:|---:|---:|---:|---:|-----------:|-----------:|---------:|------------:|\n|CV-Youden      |     0.328| 182| 129| 333|  51|       0.781|       0.721|     0.585|        0.816|\n|CV cost-min    |     0.590| 106|  38| 424| 127|       0.455|       0.918|     0.736|        0.401|\n\n\n:::\n:::\n\n\n### 教育場面での閾値判断の具体例\n\n- **語彙習得スクリーニング**（学習困難リスクの早期検出）: 支援対象の見逃し（FN）のコストが高いです。支援対象を目的変数の陽性側に置くならSensitivityを優先します。本章の `P(correct)` を使うなら、低い `P(correct)` を支援候補として扱い、追加テストで確認します。\n- **進級判定**（合格基準の設定）: 誤判定（FP）で基準未達の学習者を通過させるコストが高いです。→ **高閾値**でSpecificity/Precisionを優先します。\n- **研究報告**（一般的なモデル比較）: 特定のコスト構造を仮定しません。→ **Youden's J** でバランスをとり、他の閾値候補も感度分析として併記します。\n\n先ほどの3ケースで考えると、Case A（高習熟+有利条件）とCase B（低習熟+不利条件）は閾値の影響を受けにくいです（どちらもほぼ確実に一方に分類されます）。**Case C（中習熟+中間条件）がボーダーライン**で、閾値の選択によって判定が変わる典型的な学習者です。\n\n---\n\n## キャリブレーション確認（確率をそのまま信じてよいか）\n\n### 概要\n\n予測確率が0.80なら、実際に80%の人が正答していてほしいところです。この一致度がキャリブレーションです。AUCが高くてもキャリブレーションが悪いことがあります。\n\nAUCは「順位の正しさ」を測りますが、「確率そのものの正しさ」は保証しません。教育現場で「この生徒は80%の確率で基準を超える」と伝えるなら、その80%の信頼性を確認する必要があります。天気予報の比喩で言えば、「降水確率70%」と言った日のうち実際に雨が降ったのが70%なら、キャリブレーションが良いと言えます。学習支援の優先順位づけに確率を使う場合は、この確認を省略しません。\n\n\n**このチャンクの目的:** XGBoostの予測確率を10分位に区切り、各区間の予測平均と実測正答率を対比するキャリブレーション図を描きます。\n\n::: {.cell}\n\n```{.r .cell-code}\ncalib_all <- pred_all |>\n  group_by(model) |>\n  mutate(bin = ntile(.pred_correct, 10L)) |>\n  group_by(model, bin) |>\n  summarise(\n    pred_mean = mean(.pred_correct),\n    obs_rate  = mean(truth == \"correct\"),\n    n = n(),\n    .groups = \"drop\"\n  )\n\ncalib_all |>\n  ggplot(aes(x = pred_mean, y = obs_rate, size = n)) +\n  geom_point(color = \"#1B9E77\", alpha = 0.85) +\n  geom_abline(intercept = 0, slope = 1, linetype = \"dashed\", color = \"gray40\") +\n  scale_size_continuous(range = c(2, 6)) +\n  coord_equal(xlim = c(0, 1), ylim = c(0, 1)) +\n  facet_wrap(~ model, ncol = 2) +\n  labs(\n    title = \"Calibration Comparison: 4 Models\",\n    subtitle = \"Dashed line = perfect calibration; point size = n per bin\",\n    x = \"Mean predicted probability\",\n    y = \"Observed success rate\"\n  )\n```\n\n::: {.cell-output-display}\n![Calibration plots for four models (test set)](8_予測分析_ROC曲線とツリーモデル_files/figure-html/calibration-plot-1.png){width=3000}\n:::\n:::\n\n\n4モデルを並べると、キャリブレーションの差が一目でわかります。ロジスティック回帰は最尤推定の性質上、キャリブレーションが良好になりやすい一方、ツリー系モデルは葉ノードのサンプル比率やアンサンブルのスコア変換に基づいて確率を出すため、ずれが生じることがあります。点が系統的に対角線から外れるモデルには、再校正（Platt scaling / isotonic regression）を検討します[@platt1999; @niculescu2005]。具体的には、**ランダムフォレストはバギングの平均化により比較的良好な校正を示しやすい**一方、**XGBoost などのブースティングは確率を中央寄せ・極端化させやすく、再校正の必要性が相対的に高い**傾向があります [@niculescu2005]。実装は `probably::cal_estimate_logistic()` (Platt) や `probably::cal_estimate_isotonic()` で、CV 上または別の検証セット上で再校正モデルを当てます。\n\n分位ごとの `n` が小さい場合、点の上下は偶然変動の影響を受けやすくなります。小標本では分位数を5に減らすか、ブートストラップで不確実性を併記すると解釈が安定します。再校正を実際に行う場合は、テストデータではなく、訓練データ内のCV予測または別の検証データで校正モデルを学習します。\n\n### キャリブレーション図の見方\n\n**まずここを見ます**: 4パネルそれぞれで、点が対角線（完璧なキャリブレーション）からどれだけ離れているかを比較します。\n\n- 点が対角線より**上**: 実際の正答率が予測より高いです（モデルが過小評価 = 慎重すぎます）。\n- 点が対角線より**下**: 実際の正答率が予測より低いです（モデルが過大評価 = 自信過剰です）。\n- **点のサイズ**が小さいビン（n が少ない）は偶然変動の影響を受けやすいので、大きな点を優先的に読みます。\n- **GLMは対角線に沿いやすい**です（最尤推定の性質上、キャリブレーションが良好になりやすいです）。ツリー系が対角線から外れている場合は、Platt scalingの検討材料になります。\n\n図に加えて、校正のずれを要約する数値も確認します。ここでは3つだけ使います。ECEは機械学習分野でよく使われる要約指標ですが、ビン数やビンの切り方に依存します[@guo2017]。そのため、ECEだけで校正の良否を断定せず、信頼度図と calibration intercept/slope を合わせて読みます。\n\n- **Calibration intercept**: 0に近いほど、全体として予測が高すぎたり低すぎたりしていません。\n- **Calibration slope**: 1に近いほど、予測確率の広がりが適切です。1より小さい場合は予測が極端すぎる傾向、1より大きい場合は予測が控えめすぎる傾向を示します。\n- **ECE**（Expected Calibration Error）: 予測確率ビンごとの予測平均と実測率のずれを、ビンサイズで重みづけした値です。0に近いほどよいです。\n\n**このチャンクの目的:** キャリブレーション図を補足するため、intercept・slope・ECEをモデル別に計算します。\n\n::: {.cell}\n\n```{.r .cell-code}\ncalc_ece <- function(truth, prob, bins = 10L) {\n  tibble(truth = truth, prob = prob) |>\n    mutate(bin = ntile(prob, bins)) |>\n    group_by(bin) |>\n    summarise(\n      n = n(),\n      pred_mean = mean(prob),\n      obs_rate = mean(truth == \"correct\"),\n      .groups = \"drop\"\n    ) |>\n    summarise(ECE = sum(n / sum(n) * abs(obs_rate - pred_mean))) |>\n    pull(ECE)\n}\n\ncalc_calibration_summary <- function(truth, prob) {\n  eps <- 1e-6\n  p <- pmin(pmax(prob, eps), 1 - eps)\n  y <- as.integer(truth == \"correct\")\n\n  if (length(unique(y)) < 2) {\n    return(tibble(\n      Calibration_Intercept = NA_real_,\n      Calibration_Slope = NA_real_,\n      ECE_10 = calc_ece(truth, prob)\n    ))\n  }\n\n  calib_fit <- glm(\n    y ~ lp,\n    data = tibble(y = y, lp = qlogis(p)),\n    family = binomial()\n  )\n\n  tibble(\n    Calibration_Intercept = unname(coef(calib_fit)[1]),\n    Calibration_Slope = unname(coef(calib_fit)[2]),\n    ECE_10 = calc_ece(truth, prob)\n  )\n}\n\ncalibration_summary <- pred_all |>\n  group_by(model) |>\n  group_modify(~ calc_calibration_summary(.x$truth, .x$.pred_correct)) |>\n  ungroup() |>\n  arrange(ECE_10)\n\ncalibration_summary |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Calibration Summary: intercept, slope, and expected calibration error\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Calibration Summary: intercept, slope, and expected calibration error\n\n|model         | Calibration_Intercept| Calibration_Slope| ECE_10|\n|:-------------|---------------------:|-----------------:|------:|\n|Logistic      |                 0.012|             1.031|  0.030|\n|XGBoost       |                 0.031|             1.221|  0.035|\n|Decision Tree |                -0.073|             1.016|  0.041|\n|Random Forest |                 0.410|             2.083|  0.096|\n\n\n:::\n:::\n\n\nこの表は、キャリブレーション図を読むための補助です。`ECE_10` が小さいモデルは、10分位で見たときの平均的なずれが小さい。`Calibration_Slope` が1から大きく離れる場合は、予測確率のばらつきが強すぎる、または弱すぎる可能性があります。ただし、これらの値も標本サイズとビンの切り方に依存するため、図と合わせて判断します。\n\n### Brier scoreの解釈\n\nBrier scoreは予測確率の精度を定量化する古典的な指標です[@brier1950]:\n\n$$\n\\text{Brier} = \\frac{1}{N}\\sum_{i=1}^{N}(p_i - y_i)^2\n$$\n\nここで $p_i$ は予測確率、$y_i$ は実際の結果（0 or 1）です。**0が完璧**（予測確率と実際が完全一致）で、ベースレートが50%のときの**ランダム予測は0.25**です。先ほどの指標テーブルでBrierが低いモデルほど、確率予測としての総合誤差は小さいと読めます。ただし、Brier score は「順位づけがうまいこと」と「確率がよく校正されていること」の両方に影響されるため、キャリブレーションそのものは図や校正指標で別に確認します。\n\n具体例で考えると、ある学習者に「正答確率0.9」と予測し、実際に正答した場合、その学習者のBrierへの寄与は $(0.9 - 1)^2 = 0.01$ と小さくなります。逆に「正答確率0.9」と予測したのに誤答だった場合、寄与は $(0.9 - 0)^2 = 0.81$ と大きくなります。全学習者のこの値を平均したのがBrier scoreです。\n\n#### キャリブレーション改善の選択肢\n\n本章ではキャリブレーションの**確認**までを行いますが、系統的なずれがある場合の**改善手法**も存在します。再校正はモデル選択後の「後処理」ですが、テストデータで校正モデルを学習すると評価が楽観的になります。そのため、訓練データ内のCV予測、または別に確保した校正用データで学習します。\n\n- **Platt scaling**: 予測確率を入力、実際の結果を出力とするロジスティック回帰を追加で当てはめます。パラメトリックな手法なので少ないデータでも安定しますが、S字カーブの仮定が合わない場合は不適切です。\n- **Isotonic regression**: 「予測が高いほど実測も高い」という単調性だけを仮定し、ノンパラメトリックに校正します。柔軟ですが、データが少ないと過適合しやすくなります。\n\nR では `probably` パッケージで実装可能です。確率値を「この学習者は 80% の確率で正答する」のように直接使う運用では、キャリブレーション改善の検討に値します[@steyerberg2010]。\n\n---\n\n## 英語教育研究としての解釈\n\nこの節は、分析結果を「論文の文章」に変換するためのテンプレートです。\n\n### 分野別にどう読み替えるか\n\nこの章の枠組みは、英語教育の語彙テストだけでなく、SLA・心理言語学・教育学のさまざまな分類課題へそのまま移植できます。重要なのは、**目的変数を何に置くか** と **どの誤分類が高コストか** を先に決めることです。\n\n#### SLA\n\nSLA では、「どの学習者がどの語を学習できたか」「次の小テストで基準に達するか」「どの条件で遅延保持が維持されるか」といった問いが典型です。ツリーモデルは、`proficiency × condition × item difficulty` のような非線形・交互作用候補を拾えるため、**どの層で予測確率の差が大きいか** を探索的に把握したいときに有効です。指導法の効果そのものを主張する場合は、予測モデルだけでなく、介入割付や交絡調整を含む因果推論の設計が必要です。\n\n#### 心理言語学\n\n心理言語学では、**品詞曖昧性（POS ambiguity）** や **語義曖昧性（word-sense ambiguity）** の解消を予測問題として扱えます。たとえば、目的変数を `noun / verb / adjective` や `sense A / sense B / sense C` に置き、説明変数として語頻度、前後文脈、形態手がかり、語長、隣接語情報を入れれば、どの手がかりの組み合わせで判断が変わるかを木の分岐や SHAP で読めます。決定木は「もし直前語が冠詞で、かつ語頻度が低いなら noun を優先する」のような **if–then 型処理** と相性がよく、ランダムフォレストや XGBoost は複数の弱い手がかりを束ねる **ヒューリスティック処理** の比喩として理解しやすいです。\n\n#### 英語教育学・教育学\n\n英語教育学や教育学では、「支援対象の早期抽出」「到達判定の補助」「提出遅延や成績不振の予測」のように、最終的に**介入候補にする / しない** を決める場面が多くなります。このときは AUC の高さだけでなく、`Sensitivity`、`Specificity`、`Precision`、キャリブレーションをセットで確認し、**どのクラスの取りこぼしが痛いのか、どのクラスの誤警報が痛いのか** を先に決める必要があります。\n\n#### 非線形・交互作用候補を探索できる利点\n\nこれらの分野に共通する利点は、ツリーモデルが **非線形** と **交互作用** を事前指定なしに探索できることです。回帰分析なら `proficiency:condition` や `context:frequency` を研究者が明示的に入れなければなりませんが、ツリーモデルでは「どこで分岐するか」「どの変数を組み合わせるか」をデータ駆動で見つけられます。そのため、本章のモデルは「理論が固まった検証」よりも、**どこに複雑なパターンがありそうかを見つける探索** に特に向いています。\n\n### 何を報告すると説得力が出るか\n\n最低限、次をセットで報告します。\n\n1. データ分割（train/test または CV設計）\n2. 主要指標（ROC-AUC, PR-AUC, Sens/Spec, Brier）\n3. 閾値の決定根拠（CV-Youden, あるいはコスト設計）\n4. サブグループ別性能（必要に応じて）\n5. 解釈（重要特徴・教育的意味）\n6. 限界（外的妥当性、バイアス、公平性）\n\n### 予測結果から書ける範囲の例（この章のケース）\n\n- `retrieval`条件の観測では、`glossing`条件より `P(correct)` が高く予測される傾向があります。これはretrieval practiceの文献知見と整合的ですが、この分析だけで条件の因果効果を推定したわけではありません。\n- `condition` と `test_time` の組み合わせで予測確率が変わる場合、遅延テストで保持パターンが変わる可能性を示す探索的結果として報告できます。カリキュラム設計への示唆は、実験デザインや第18章の因果推論と合わせて慎重に書きます。\n- 学習時間だけでなく語彙特性（頻度・長さ・同根語）も予測に寄与する場合、教材選定で注目すべき候補特徴として整理できます。ただし、特徴重要度は「教材を変えれば成果が変わる」ことを直接示すものではありません。\n\n### 報告テンプレート（論文・報告書向け）\n\n予測モデルの報告では、モデル名と AUC だけでは再現性も実務的意味も伝わりにくいです。最低限、**分割方法、比較モデル、評価指標、閾値、キャリブレーション、解釈、限界**をそろえます。L2 研究としての書式は *Language Learning* の Open Methods ガイドライン [@norris2015] に準拠すれば、再現性に必要な情報の漏れを防げます。\n\n| 報告要素 | 本章での確認箇所 | 書くべき内容 |\n|:---|:---|:---|\n| 予測課題 | ケース設定、目的変数 | 何を positive class としたか、評価単位は response / learner / item / trial のどれかを書きます |\n| 分割設計 | 学習・評価の分割 | learner単位・item単位など、汎化先に対応した分割を明示します。CVは訓練データ内で行います |\n| 比較モデル | GLM、決定木、RF、XGBoost | GLMを基準線として含め、複雑モデルの上積みを示します |\n| 評価指標 | ROC、PR、Brier、混同行列 | AUCだけでなく、クラス不均衡、確率スコア、TP/FP/TN/FNも確認します |\n| 不確実性 | AUC信頼区間 | 学習者クラスタ単位など、再標本化単位を明示 |\n| 閾値 | Youden、コストベース閾値 | テストデータではなくCV/検証データで決めたこと、どの誤分類を重く見たかを説明 |\n| キャリブレーション | 信頼度図、Brier | 確率を直接使う場合は校正状態を報告 |\n| サブグループ | 習熟度帯、テスト時点、背景変数 | 平均性能だけでなく、性能が落ちる層がないかを確認 |\n| 解釈 | Dominance、Permutation、SHAP、PDP | 重要度を因果効果として読まないことを明示します。SHAPのスケールも説明します |\n| 限界 | 外的妥当性、公平性、リーク | データ生成過程・測定単位・サブグループ性能の制約を書きます |\n\n#### 日本語テンプレート\n\n「XGBoostは、`learner_id`単位の3-fold CVでハイパーパラメータを調整した後、未知学習者からなるテストデータでROC-AUC = .82、PR-AUC = .78を示しました。評価単位はresponse-levelで、陽性クラスは `correct` としました。CV予測から選んだYouden閾値0.52をテストデータに適用したところ、Sensitivity = .80、Specificity = .72、TP/FP/TN/FN = xx/xx/xx/xxでした。学習者クラスタ単位のブートストラップ95%CIはROC-AUC = [.xx, .xx]でした。サブグループ確認では、低習熟度・遅延テスト条件でSensitivityが相対的に低く、運用前に閾値設定または追加確認が必要な可能性が示唆されました。SHAP分析では、proficiencyとword_freqが予測に大きく寄与しました。これらは正答確率の予測に関する結果で、条件や教材特徴の因果効果を示すものではありません。」\n\nコストベース閾値を採用した場合は、次の一文を加えると判断根拠が明確になります。\n\n「閾値選択では、`correct` を陽性クラスとしたため、誤答観測を正答扱いするFPを、正答観測を誤答扱いするFNの4倍重く設定しました。CV予測上の期待コストを最小化する閾値は0.xxで、この閾値をテストデータに適用したときのCost per observationはx.xxでした。」\n\n上記の数値は仮の例です。ご自身の分析結果で置き換えてください。\n\n#### Results Template (English)\n\n\"An XGBoost classifier was trained on the vocabulary learning data (N = 3,500 observations) with learner-grouped 3-fold cross-validation for hyperparameter tuning. The positive class was `correct`, and performance was evaluated at the response level on held-out learners. On the test set, XGBoost achieved ROC-AUC = .82 and PR-AUC = .78. Applying the Youden threshold selected from cross-validated training predictions (0.52) yielded sensitivity = .80, specificity = .72, and TP/FP/TN/FN = xx/xx/xx/xx. A learner-cluster bootstrap yielded a 95% CI of [.xx, .xx] for ROC-AUC. Subgroup checks suggested lower sensitivity for low-proficiency delayed-test observations, indicating that threshold setting or additional review may be needed before operational use. SHAP values indicated that proficiency and word frequency contributed strongly to predictions. These results describe predictive associations and should not be interpreted as causal effects of instructional conditions.\"\n\nWhen using a cost-based threshold, add: \"Because the positive class was `correct`, false positives corresponded to incorrect observations classified as correct. We therefore assigned FP errors four times the cost of FN errors. The cost-minimizing threshold selected from cross-validated predictions was 0.xx, yielding a test-set cost per observation of x.xx.\"\n\nThe numbers above are illustrative. Replace them with your actual results.\n\n### 限界と今後の課題\n\n- **外的妥当性**: 本章のデータは架空の生成データで、実データでの再現が必要です。\n- **多水準構造の扱い**: 本章では `learner_id` 単位で train/test とCVを分け、未知学習者への汎化を評価しています。ただし `item_id` は train/test の両方に出るため、未知項目への汎化性能は別途 `item_id` 単位の分割で確認する必要があります。\n- **変量効果との違い**: ツリーモデル自体には `learner_id` と `item_id` の変量効果を入れていません。説明・推論が主目的なら、6章で扱ったGLMMとの比較が必要になる場合があります。\n- **公平性**: 学習者の背景（L1, 性別等）によって予測精度が異なる可能性があります。サブグループ別の性能評価を行うことが望ましいです。\n- **因果推論との区別**: ツリーモデルは予測に優れますが、「retrieval conditionが正答率を**因果的に**高める」とは直接言えません。因果推論には別の枠組み（例: 傾向スコア、差の差）が必要です。\n\n---\n\n## 学習曲線: サンプルサイズは十分か\n\n### 概要\n\n学習データを10%から100%まで段階的に使い、検証性能の変化を確認します。曲線が平坦になっていればデータは十分で、まだ上昇中ならデータ追加で改善の余地があります。\n\nここまで4つのモデルを構築・評価してきました。最後に残る問いは「データは十分だったのでしょうか」です。もしデータが倍あれば、RFやXGBoostの精度はさらに上がるのでしょうか。それとも既に飽和しているのでしょうか。章冒頭で理論的に示したバイアス-バリアンストレードオフが、データ量に対してどう現れるかを学習曲線で確認します。最終テストデータを繰り返し見ないよう、ここでは訓練データをさらに内部訓練/検証に分けて診断図を作ります。\n\n**このチャンクの目的:** 訓練データサイズを段階的に増やし、GLMとRFの内部検証ROC-AUCの変化を学習曲線として描画します。\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(123)\nlc_split <- rsample::group_initial_split(train_data, group = learner_id, prop = 0.8)\nlc_train_data <- training(lc_split)\nlc_valid_data <- testing(lc_split)\ntrain_sizes <- seq(0.1, 1.0, by = 0.1)\n\nlc_results <- map_dfr(train_sizes, \\(frac) {\n  learner_sub <- lc_train_data |>\n    distinct(learner_id) |>\n    slice_sample(prop = frac)\n\n  train_sub <- lc_train_data |>\n    semi_join(learner_sub, by = \"learner_id\")\n\n  prep_glm <- prep(rec_glm, training = train_sub)\n  prep_tree <- prep(rec_tree, training = train_sub)\n\n  glm_sub <- logistic_reg() |>\n    set_engine(\"glm\") |>\n    fit(correct ~ ., data = bake(prep_glm, new_data = train_sub))\n\n  rf_sub <- rand_forest(mtry = rf_best$mtry, min_n = rf_best$min_n, trees = 300) |>\n    set_engine(\"ranger\") |>\n    set_mode(\"classification\") |>\n    fit(correct ~ ., data = bake(prep_tree, new_data = train_sub))\n\n  valid_glm <- bake(prep_glm, new_data = lc_valid_data)\n  valid_rf  <- bake(prep_tree, new_data = lc_valid_data)\n\n  bind_rows(\n    tibble(frac = frac, n = nrow(train_sub), model = \"Logistic\",\n           auc = roc_auc_vec(lc_valid_data$correct,\n                             predict(glm_sub, valid_glm, type = \"prob\")$.pred_correct,\n                             event_level = \"second\")),\n    tibble(frac = frac, n = nrow(train_sub), model = \"Random Forest\",\n           auc = roc_auc_vec(lc_valid_data$correct,\n                             predict(rf_sub, valid_rf, type = \"prob\")$.pred_correct,\n                             event_level = \"second\"))\n  )\n})\n\nlc_results |>\n  ggplot(aes(x = n, y = auc, color = model)) +\n  geom_line(linewidth = 1) +\n  geom_point(size = 2) +\n  scale_color_manual(values = c(\"Logistic\" = \"#D95F02\", \"Random Forest\" = \"#1B9E77\")) +\n  labs(\n    title = \"Learning Curves: GLM vs Random Forest\",\n    subtitle = \"Internal validation ROC-AUC as a function of training data size\",\n    x = \"Training set size (n)\",\n    y = \"Validation ROC-AUC\",\n    color = \"Model\"\n  )\n```\n\n::: {.cell-output-display}\n![Learning curves: ROC-AUC by training set size](8_予測分析_ROC曲線とツリーモデル_files/figure-html/learning-curves-1.png){width=3000}\n:::\n:::\n\n\n学習曲線の読み方:\n\n- **GLM**は少量データでも比較的安定します（低バリアンス）が、天井が低いです（高バイアス = 非線形を捉えられません）。\n- **RF**はデータが少ないと不安定です（高バリアンス）が、データ増加とともにGLMを追い越す可能性があります（低バイアス）。\n- 曲線が**平坦**になった領域では、データ追加よりモデル改良や特徴量エンジニアリングの方が効果的です。\n- 2曲線が**収束していない**場合は、さらなるデータ収集で差が開く（または縮まる）可能性があります。\n\n#### 学習曲線の実務的な読み方\n\nまず、曲線が**平坦になる点**を探します。その点がサンプルサイズの十分性の目安です。\n\n- 例: RF の曲線が `n = 800` 以降ほぼ横ばいなら、「800 行あれば十分です。データを倍にしても精度は上がりません」と結論できます。\n- 曲線が**まだ上昇中**で終わっているなら、データ追加で改善の余地があります。査読者への説明にも使いやすく、「学習曲線が飽和していないため、追加データで性能改善が見込まれる」と書けます。\n- 各点のギザギザは訓練データの乱数化の影響です。小さい `N (< 200)` での上下は気にしすぎず、全体のトレンドを見ます。\n\n---\n\n## 方法論上の留意点\n\n1. **AUC単独でモデル選択を行わない**  \n   対応: PR-AUC、閾値依存指標（Sensitivity / Specificity / Precision）も併記します。\n2. **目的変数のイベント水準を明示する**  \n   対応: `event_level = \"second\"` などを明示し、再現可能性を確保します。\n3. **前処理リークを防止する**  \n   対応: recipe の学習は学習データのみに限定し、workflow で一体化します。\n4. **複雑モデルを無条件に採用しない**  \n   対応: 単純モデル（GLM / 単木）を必ず比較対象に含めます。\n5. **カテゴリカル変数の重要度に注意する**  \n   対応: 水準数が多いカテゴリカル変数は、Gini / Gain ベースの重要度で過大評価されやすいです[@gries2020]。Permutation importance や SHAP と併用します。\n6. **Gain重要度のカーディナリティバイアスを意識する**  \n   対応: XGBoost の Gain 重要度は分岐回数に依存するため、連続変数を優遇しやすいです。SHAP と併記して頑健性を確認します[@strobl2007]。\n7. **サンプルサイズとモデル複雑度のバランスを取る**  \n   対応: サンプルサイズが小さい場合（例: `N < 500`）、ツリーモデルより GLM の方が安定することがあります。データ量に見合ったモデル複雑度を常に検討します。\n8. **OOBをgrouped CVの代替にしない**\n   対応: RFのOOBは便利な内部診断ですが、学習者や項目の反復構造をまたぐ評価では、`learner_id` や `item_id` 単位のCV/testを別に設計します。\n9. **説明用図を見てモデルを選び直さない**\n   対応: SHAP、PDP、単木の安定性確認はモデル解釈のための図です。これらを見た後に閾値やモデルを変更する場合は、別の検証データで再評価します。\n\n---\n\n## まとめ\n\n### 概要\n\n回帰→決定木→RF→XGBoostの順にモデルの複雑度を上げ、ROC/PR/閾値/キャリブレーションで評価しました。ツリーモデルは非線形・相互作用に強い一方、ROC-AUC単独ではなく複数指標で判断し、閾値は教育目的に合わせて選ぶ必要があります。\n\n本章の主要な結論は次の通りです。\n\n1. **ツリーモデルは非線形・相互作用候補を探索できます**: ロジスティック回帰では手動で交互作用項を指定する必要がありますが、ツリーモデルはデータ駆動で有用な分割候補を見つけます。英語教育データのように、学習者要因と項目要因が複雑に絡む場面で特に有用です。\n\n2. **ROCは有用ですが、ROCだけでは不十分です**: PR-AUC（陽性クラスが少ないとき特に重要）、Brier score（確率予測の総合誤差）、閾値依存指標（Sensitivity/Specificity）を組み合わせて初めて、モデルの実力を多面的に評価できます。\n\n3. **実務判断には閾値とキャリブレーションが必要です**: AUCは「モデルの潜在能力」を示しますが、教育現場で「この学習者を支援候補にする/しない」を決めるには閾値が必要です。その閾値は教育目的（取りこぼし回避 vs 誤警報回避）に合わせて、訓練データ内のCV予測または検証データで選びます。確率値を直接使う場合はキャリブレーションの確認が不可欠です。\n\n4. **モデル選択は目的に依存します**: 精度だけでなく、解釈可能性、計算コスト、チューニング負荷を考慮します。実務では次の判断基準を用いると整理しやすくなります。\n\n- データが少ない（< 500行）→ **GLM**で十分な場合が多いです。\n- 非線形・相互作用が複雑 → **RF**（安定性重視）or **XGBoost**（精度重視）を検討します。\n- 予測根拠の透明性が最優先 → **決定木**を検討します。\n- 最高精度を追求し、解釈にはSHAPを使う → **XGBoost + SHAP**を検討します。\n\n---\n\n## 実務チェックリスト（提出前に確認）\n\n- [ ] 予測課題は明確ですか（何を当てるか）。\n- [ ] データ分割とCV手順を明記しましたか。\n- [ ] ハイパーパラメータのチューニング手順を記載しましたか。\n- [ ] 交差検証の設計（fold数・グループ化単位）を報告しましたか。\n- [ ] ROC-AUCとPR-AUCの両方を示しましたか。\n- [ ] positive class と評価単位（response-level / learner-macro等）を明示しましたか。\n- [ ] 閾値の根拠を記述しましたか（テストデータではなくCV/検証データで決めましたか）。\n- [ ] コストベース閾値を使う場合、FP/FNのコスト比を事前根拠とともに明記しましたか。\n- [ ] キャリブレーション図を確認しましたか。\n- [ ] サブグループ別に性能が大きく落ちる層がないか確認しましたか。\n- [ ] 複数の重要度指標を比較しましたか。\n- [ ] 比較モデル（GLM含む）の結果を併記しましたか。\n- [ ] 教育的解釈と限界（公平性含む）を書きましたか。\n\n---\n\n## 次に読む章\n\n- **推論・説明を主目的に戻したい場合**: 第5章のGLM/GAM、または第6章のGLMMで係数・ランダム効果・不確実性を明示します。\n- **予測されたリスクを介入判断に接続したい場合**: 第18章で因果 estimand と識別仮定を整理します。高リスク群に介入することと、介入が効くことは別問題です。\n- **学習者タイプを教師なしで探索したい場合**: 第19章のクラスタリングで、予測ラベルを置かない分類を検討します。\n- **時系列・音声・試行順序を扱う場合**: 第13章群で自己相関、時点、within-token trajectory の扱いを確認します。\n\n---\n\n## 先行研究マップ\n\n本章を通読した後の振り返り用として、各文献が本章のどの部分に対応するかを整理します。\n\n\n| Category | Study | Context | Relevance to Chapter |\n|---|---|---|---|\n| 英語教育応用 | Zhang & Lu (2025) | L2テキストのCEFRレベル分類にRFを使用 | 本章のRFセクションと変数重要度の読み方に対応 |\n| 英語教育応用 | Mau & Feng (2025) | L2ライティング自動評価にXGBoost分類器を使用 | 本章のXGBoostセクションとモデル比較の枠組みに対応 |\n| 英語教育応用 | Niu (2025) | 英語教育評価の精度改善にRFを活用（会議報告） | 本章のRFセクションに対応します。会議論文のため再現時は評価設計を明示します |\n| 方法論（ツリー） | Gries (2020) | コーパス言語学でのツリーモデルの落とし穴を指摘 | 本章の方法論上の留意点（カテゴリカル変数の過大評価等）に対応 |\n| 方法論（重要度） | Mizumoto (2022) | 応用言語学でdominance analysisとRFの実践手順を提示 | 本章のGLM dominanceとRF重要度セクションに対応 |\n| 方法論（閾値） | Youden (1950) | Sensitivity + Specificity - 1 に基づく診断閾値規則を提示 | 本章のCV-Youden閾値選択に対応 |\n| 方法論（確率校正） | Brier (1950), Platt (1999), Niculescu-Mizil & Caruana (2005), Guo et al. (2017) | 確率予測スコア、Platt scaling、isotonic regression、ECEの基礎 | 本章のBrier score・キャリブレーション図・再校正の注意点に対応 |\n| 教科書 | James et al. (2021) | ISLR: ツリーモデルの教育的標準テキスト | 本章の概念説明（バイアス・分散・バギング・ブースティング）の理論的基盤 |\n| 教科書 | Kuhn & Silge (2022) | Tidy Modeling with R: tidymodels の実装体系を整理した教科書 | 本章のtidymodels実装パターン全体に対応 |\n\n: Literature Map for This Chapter\n\n## 参考文献\n\n::: {#refs}\n:::\n\n### 関連リソース（実装ドキュメント）\n\n- 基礎フレームワーク:\n  - [tidymodels: Getting started (models)](https://www.tidymodels.org/start/models/)\n  - [tidymodels: Getting started (resampling)](https://www.tidymodels.org/start/resampling/)\n- 分割・前処理:\n  - [rsample: group_initial_split()](https://rsample.tidymodels.org/reference/group_initial_split.html)\n  - [rsample: group_vfold_cv()](https://rsample.tidymodels.org/reference/group_vfold_cv.html)\n  - [recipes: step_log()](https://recipes.tidymodels.org/reference/step_log.html)\n  - [recipes: step_dummy()](https://recipes.tidymodels.org/reference/step_dummy.html)\n- モデル仕様:\n  - [parsnip: decision_tree() with rpart](https://parsnip.tidymodels.org/reference/details_decision_tree_rpart.html)\n  - [parsnip: rand_forest() with ranger](https://parsnip.tidymodels.org/reference/details_rand_forest_ranger.html)\n  - [parsnip: boost_tree() with xgboost](https://parsnip.tidymodels.org/reference/details_boost_tree_xgboost.html)\n  - [rpart::rpart.control](https://stat.ethz.ch/R-manual/R-devel/RHOME/library/rpart/html/rpart.control.html)\n  - [rpart::plotcp / printcp](https://stat.ethz.ch/R-manual/R-devel/library/rpart/html/plotcp.html)\n  - [partykit (CRAN)](https://cran.r-project.org/package=partykit)\n  - [ggparty (CRAN)](https://cran.r-project.org/package=ggparty)\n- チューニング:\n  - [tune: tune_grid()](https://tune.tidymodels.org/reference/tune_grid.html)\n  - [tune: collect_predictions()](https://tune.tidymodels.org/reference/collect_predictions.html)\n  - [tune: control_grid()](https://tune.tidymodels.org/reference/control_grid.html)\n  - [dials: grid_space_filling()](https://dials.tidymodels.org/reference/grid_space_filling.html)\n- 評価・解釈:\n  - [yardstick: roc_auc()](https://yardstick.tidymodels.org/reference/roc_auc.html)\n  - [yardstick: pr_auc()](https://yardstick.tidymodels.org/reference/pr_auc.html)\n  - [yardstick: brier_class()](https://yardstick.tidymodels.org/reference/brier_class.html)\n  - [probably package](https://probably.tidymodels.org/)\n  - [pROC package site](https://xrobin.github.io/pROC/)\n  - [xgboost prediction docs](https://xgboost.readthedocs.io/en/stable/prediction.html)\n  - [xgboost in R tutorial](https://xgboost.readthedocs.io/en/stable/R-package/xgboostPresentation.html)\n- 再現性と注意点:\n  - [scikit-learn: common pitfalls (data leakage)](https://scikit-learn.org/stable/common_pitfalls.html)\n\n### セッション情報\n\n\n**このチャンクの目的:** 再現性のためのセッション情報を記録します。\n\n::: {.cell}\n\n```{.r .cell-code}\nsessionInfo()\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nR version 4.5.1 (2025-06-13 ucrt)\nPlatform: x86_64-w64-mingw32/x64\nRunning under: Windows 11 x64 (build 26200)\n\nMatrix products: default\n  LAPACK version 3.12.1\n\nlocale:\n[1] LC_COLLATE=Japanese_Japan.utf8  LC_CTYPE=Japanese_Japan.utf8   \n[3] LC_MONETARY=Japanese_Japan.utf8 LC_NUMERIC=C                   \n[5] LC_TIME=Japanese_Japan.utf8    \n\ntime zone: Etc/GMT-9\ntzcode source: internal\n\nattached base packages:\n[1] grid      stats     graphics  grDevices utils     datasets  methods  \n[8] base     \n\nother attached packages:\n [1] shapviz_0.10.3          dominanceanalysis_2.1.1 probably_1.2.0         \n [4] ggparty_1.0.0.1         partykit_1.2-27         mvtnorm_1.3-6          \n [7] libcoin_1.0-12          xgboost_3.2.1.1         ranger_0.18.0          \n[10] vip_0.4.5               rpart.plot_3.1.4        rpart_4.1.24           \n[13] yardstick_1.3.2         workflowsets_1.1.1      workflows_1.3.0        \n[16] tune_2.0.1              tailor_0.1.0            rsample_1.3.2          \n[19] recipes_1.3.1           parsnip_1.4.1           modeldata_1.5.1        \n[22] infer_1.1.0             dials_1.4.2             scales_1.4.0           \n[25] broom_1.0.12            tidymodels_1.4.1        patchwork_1.3.2        \n[28] DT_0.34.0               gt_1.3.0                lubridate_1.9.5        \n[31] forcats_1.0.1           stringr_1.6.0           dplyr_1.2.0            \n[34] purrr_1.2.1             readr_2.2.0             tidyr_1.3.2            \n[37] tibble_3.3.1            ggplot2_4.0.2           tidyverse_2.0.0        \n\nloaded via a namespace (and not attached):\n [1] conflicted_1.2.0    rlang_1.1.7         magrittr_2.0.4     \n [4] furrr_0.3.1         otel_0.2.0          compiler_4.5.1     \n [7] systemfonts_1.3.2   vctrs_0.7.2         lhs_1.2.1          \n[10] pkgconfig_2.0.3     fastmap_1.2.0       backports_1.5.0    \n[13] labeling_0.4.3      inum_1.0-5          utf8_1.2.6         \n[16] rmarkdown_2.31      prodlim_2026.03.11  tzdb_0.5.0         \n[19] ragg_1.5.2          xfun_0.57           cachem_1.1.0       \n[22] jsonlite_2.0.0      parallel_4.5.1      R6_2.6.1           \n[25] bslib_0.10.0        stringi_1.8.7       RColorBrewer_1.1-3 \n[28] parallelly_1.46.1   jquerylib_0.1.4     Rcpp_1.1.1         \n[31] iterators_1.0.14    knitr_1.51          future.apply_1.20.2\n[34] pacman_0.5.1        Matrix_1.7-5        splines_4.5.1      \n[37] nnet_7.3-20         timechange_0.4.0    tidyselect_1.2.1   \n[40] rstudioapi_0.18.0   yaml_2.3.12         timeDate_4052.112  \n[43] codetools_0.2-20    listenv_0.10.1      lattice_0.22-9     \n[46] withr_3.0.2         S7_0.2.1            evaluate_1.0.5     \n[49] future_1.70.0       survival_3.8-6      xml2_1.5.2         \n[52] pillar_1.11.1       checkmate_2.3.4     foreach_1.5.2      \n[55] generics_0.1.4      hms_1.1.4           globals_0.19.1     \n[58] class_7.3-23        glue_1.8.0          tools_4.5.1        \n[61] data.table_1.18.2.1 gower_1.0.2         fs_2.0.1           \n[64] crosstalk_1.2.2     ipred_0.9-15        Formula_1.2-5      \n[67] cli_3.6.5           DiceDesign_1.10     textshaping_1.0.5  \n[70] viridisLite_0.4.3   lava_1.8.2          gtable_0.3.6       \n[73] GPfit_1.0-9         sass_0.4.10         digest_0.6.39      \n[76] htmlwidgets_1.6.4   farver_2.1.2        memoise_2.0.1      \n[79] htmltools_0.5.9     lifecycle_1.0.5     hardhat_1.4.2      \n[82] sparsevctrs_0.3.6   MASS_7.3-65        \n```\n\n\n:::\n:::\n\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}