{
  "hash": "9195a35e51f3ed91928e907f87b51805",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"一般化線形モデル・一般化加法モデル\"\nbibliography:\n  - references_glm_gam.bib\n  - references_glmm.bib\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\nrequired_packages <- c(\n  \"betareg\",\n  \"broom\",\n  \"dplyr\",\n  \"emmeans\",\n  \"knitr\",\n  \"ordinal\",\n  \"pacman\",\n  \"performance\",\n  \"pscl\",\n  \"purrr\",\n  \"scales\",\n  \"see\",\n  \"sjPlot\",\n  \"tibble\",\n  \"tidyr\",\n  \"tidyverse\"\n)\n\nmissing_packages <- setdiff(required_packages, rownames(installed.packages()))\nif (length(missing_packages) > 0) {\n  install.packages(missing_packages, repos = \"https://cloud.r-project.org\")\n}\n```\n:::\n\n\n\n## はじめに\n\n英語教育研究では、目的変数が正規分布を仮定しにくい場面が多くあります。典型例は、\n**正誤（0/1）**、**相互作用回数（count）**、**正の連続量**です。これらを\n通常の線形回帰で処理すると、分布仮定と解釈がずれやすくなります。\n\nこの問題に対する標準的な枠組みが\n**一般化線形モデル（GLM）**[@nelder1972] であり、\n非線形効果を柔軟に扱う拡張が\n**一般化加法モデル（GAM）**[@hastie1990; @wood2017; @wood2020] です。\n\n本章は、理論説明を最小限にし、**そのまま再利用できる実務手順**に重点を置きます。\n\n### 第5章と第6章の使い分け\n\n| 状況 | 使う章 |\n|---|---|\n| 観測が独立（反復測定なし） | 本章（GLM / GAM） |\n| 同じ学習者から複数観測がある | 6章（GLMM） |\n| 独立性 + 非線形の両方が問題 | GAMM（6章末尾で概説） |\n\n迷ったら、まず本章で GLM/GAM を試し、章末の「6章への接続」で GLMM との比較を確認してください。\n\n### この章の到達目標\n\n- 分布とリンク関数を、研究目的に応じて選べる。\n- GLM と GAM を同じ研究課題で比較し、採用理由を説明できる。\n- 係数を OR（オッズ比: ある条件で事象が何倍起きやすいか）/ IRR（発生率比: カウントが何倍になるか）/ 倍率（連続値が何倍になるか）に変換し、応答スケール予測で可視化できる。\n- 章内コードをそのままテンプレートとして使える。\n\n### ファストトラック {.unnumbered}\n\n以下の順で読むと 60 分ほどで GLM/GAM の基本ワークフローを習得できます。\n\n| 順序 | セクション | 所要時間 | 内容 |\n|:---:|---|:---:|---|\n| 1 | 理論はここだけ確認する | 10分 | 分布族 + リンク関数 + MLE の3ステップ |\n| 2 | ケース1: 2値（Binary） | 20分 | GLM/GAM推定 → OR → 予測 → 診断 |\n| 3 | モデル比較と OR | 10分 | AIC、sjPlot、worked interpretation |\n| 4 | 報告テンプレート（Case 1） | 10分 | APA 7 形式のMethod/Results |\n| 5 | 第6章への接続 | 10分 | GLM → GLMM への判断基準 |\n\n**2回目以降に読む**: ケース2（カウント）、ケース3（連続正値）、リンク関数の詳細、トラブルシューティング\n\n### OLS から GLM への橋渡し {.unnumbered}\n\nすでに `lm()` を使った線形回帰に馴染みがあれば、GLM はその自然な一般化です：\n\n| | OLS (`lm()`) | GLM (`glm()`) |\n|---|---|---|\n| **分布** | 正規分布（暗黙に仮定） | 任意の指数型分布族（明示的に選択） |\n| **リンク関数** | 恒等（$\\eta = \\mu$） | logit, log, inverse 等（明示的に選択） |\n| **推定法** | 最小二乗法（OLS） | 最尤推定法（MLE） |\n| **R の書き方** | `lm(y ~ x)` | `glm(y ~ x, family = binomial)` |\n\nつまり、**通常の重回帰分析（`lm()`）は GLM の特殊ケース**です。GLM を学ぶとは、この3つの選択肢を適切に選べるようになることです。\n\n- **指数型分布族**とは、正規・二項・ポアソン・ガンマなどの幅広い分布を含むグループです。GLM はこのグループ内の分布を自由に選べます。\n- **リンク関数**とは、予測値（線形予測子）をアウトカムの平均に変換する関数です。例：logit は確率（0〜1）を対数オッズ（−∞〜+∞）に変換します。\n- **MLE（最尤推定法）**とは、「このデータが観測される確率を最大にする」パラメータ値を求める方法です。正規分布では OLS と同じ結果になりますが、2値やカウントデータでは MLE が正しい推定法です。\n\n本章では、GLM/GAM の前提に合わせて **1 行 = 1 つの独立観測単位** となる現実的シミュレーションデータを用います。ケース1・ケース2は独立 dyad、ケース3は独立 learner を単位とします。\n\n以下のコードで、`lm()` と `glm(family = gaussian)` が**全く同じ結果**を出すことを確認できます：\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"OLS と gaussian GLM は同じ推定になる\"}\n# テストデータ\nset.seed(42)\nd_test <- data.frame(y = rnorm(50, 10, 2), x1 = rnorm(50), x2 = rnorm(50))\n\n# 2つの方法で同じモデルを推定\nm_ols <- lm(y ~ x1 + x2, data = d_test)\nm_glm_gauss <- glm(y ~ x1 + x2, data = d_test, family = gaussian)\n\n# 係数は完全に一致する\ntibble(\n  term = names(coef(m_ols)),\n  `lm() coefficient` = coef(m_ols),\n  `glm(gaussian) coefficient` = coef(m_glm_gauss),\n  identical = abs(coef(m_ols) - coef(m_glm_gauss)) < 1e-10\n) |>\n    mutate(across(where(is.numeric), ~ round(.x, 4))) |>\n  knitr::kable(caption = \"lm() and glm(family = gaussian) produce identical results\")\n```\n\n::: {.cell-output-display}\n\n\nTable: lm() and glm(family = gaussian) produce identical results\n\n|term        | lm() coefficient| glm(gaussian) coefficient|identical |\n|:-----------|----------------:|-------------------------:|:---------|\n|(Intercept) |          10.0272|                   10.0272|TRUE      |\n|x1          |          -0.5939|                   -0.5939|TRUE      |\n|x2          |           0.2563|                    0.2563|TRUE      |\n\n\n:::\n:::\n\n\n#### 理解しておくべきこと\n\n`lm(y ~ x1 + x2)` は `glm(y ~ x1 + x2, family = gaussian(link = \"identity\"))` と**数学的に等価**です。GLM は新しい手法ではなく、**既に馴染みのある回帰分析の枠組みを拡張したもの**です。\n\n| 馴染みのある方法 | GLM としての正式名称 |\n|---|---|\n| 単回帰 `lm(y ~ x)` | Gaussian GLM（恒等リンク、予測子1つ） |\n| 重回帰 `lm(y ~ x1 + x2)` | Gaussian GLM（恒等リンク、予測子複数） |\n| ロジスティック回帰 | Binomial GLM（logit リンク） |\n| ポアソン回帰 | Poisson GLM（log リンク） |\n\nつまり、この章で新しく学ぶのは「分布とリンク関数の選び方」だけです。モデルの構造（切片 + 傾き × 予測子）は全く同じです。\n#### なぜ `lm()` では不適切なのか：目で見る問題\n\n以下のシミュレーションで、2値データに `lm()` を使ったときの問題を確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2024)\nn_demo <- 200\nscore_demo <- rnorm(n_demo, 0, 1.5)\nprob_demo  <- plogis(1.5 * score_demo)  # 真の確率（ロジスティック曲線）\ncorrect_demo <- rbinom(n_demo, 1, prob_demo)\ndat_demo <- data.frame(correct = correct_demo, score = score_demo)\n\nm_lm_demo  <- lm(correct ~ score, data = dat_demo)\nm_glm_demo <- glm(correct ~ score, data = dat_demo, family = binomial)\n\nnd_demo <- data.frame(score = seq(-4, 4, length.out = 200))\nnd_demo$pred_lm  <- predict(m_lm_demo, newdata = nd_demo)\nnd_demo$pred_glm <- predict(m_glm_demo, newdata = nd_demo, type = \"response\")\n\nggplot(nd_demo, aes(x = score)) +\n  geom_point(data = dat_demo, aes(y = correct), alpha = 0.3, size = 1) +\n  geom_line(aes(y = pred_lm, color = \"lm() — linear\"), linewidth = 1) +\n  geom_line(aes(y = pred_glm, color = \"glm() — logistic\"), linewidth = 1) +\n  geom_hline(yintercept = c(0, 1), linetype = \"dashed\", alpha = 0.4) +\n  annotate(\"rect\", xmin = -4, xmax = -2.2, ymin = -0.35, ymax = 0,\n           fill = \"red\", alpha = 0.15) +\n  annotate(\"text\", x = -3.1, y = -0.17, label = \"Impossible\\n(< 0)\", size = 3) +\n  annotate(\"rect\", xmin = 2.5, xmax = 4, ymin = 1, ymax = 1.45,\n           fill = \"red\", alpha = 0.15) +\n  annotate(\"text\", x = 3.25, y = 1.22, label = \"Impossible\\n(> 1)\", size = 3) +\n  scale_color_manual(values = c(\"lm() — linear\" = \"#E69F00\", \"glm() — logistic\" = \"#0072B2\")) +\n  labs(x = \"Predictor (score)\", y = \"Predicted probability\",\n       title = \"Why lm() fails for binary data\",\n       subtitle = \"lm() predicts impossible values outside [0, 1]; glm() stays within bounds\",\n       color = NULL) +\n  coord_cartesian(ylim = c(-0.4, 1.5))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/lm-failure-demo-1.png){width=2100}\n:::\n:::\n\n\n::: {.callout-important}\n#### この図の読み方\n\n- **オレンジの直線（lm）**: 予測が 0 未満や 1 超に飛び出しています（赤い領域）。確率は 0〜1 の範囲しかとれないため、これは**論理的に不可能な予測**です。\n- **青のS字曲線（glm）**: logit リンク関数のおかげで、予測は必ず 0〜1 に収まります。\n- **結論**: 2値データには `glm(family = binomial)` を使うべきです。`lm()` は確率の制約を無視するため、特に予測子が極端な値をとるとき、無意味な予測を生成します。\n\n同様の問題はカウントデータ（`lm()` が負のカウントを予測する）や正値連続データ（`lm()` が負の値を予測する）でも発生します。GLM はリンク関数によってこれらの制約を自然に尊重します。\n:::\n\n## 分析の流れ\n\nGLM/GAM の分析では、**目的変数の型（2値・カウント・ゼロ過剰カウント・連続正値・(0,1)割合・名義カテゴリ・順序カテゴリ）に応じて分布族とリンク関数を選ぶ**ことが出発点になります。分布の選択を誤ると、推定値の解釈がデータの生成過程と乖離し、予測精度だけでなく実質的な結論にも影響を及ぼします。本章では、まず目的変数の型を確認し、2値なら Binomial GLM、カウントなら Poisson GLM（過分散があれば Negative Binomial GLM）、ゼロ過剰なら zero-inflated / hurdle、連続正値なら Gamma GLM、(0,1) の連続割合なら beta regression を選択します。名義カテゴリが3水準以上なら multinomial regression、順序カテゴリなら ordinal regression を選びます。次に非線形性を診断し、必要なら GAM に拡張、不要なら GLM を維持します。最後に AIC 比較と残差診断でモデルを確定し、OR / IRR / 倍率 / RRR / 予測確率を中心に報告します。\n\n英語教育研究では、テスト項目の正誤データ（2値）や発話中のエラー回数（カウント）を扱うことが多いため、**Binomial GLM** または **Poisson / NB GLM** のパスを辿るケースが最も一般的です。一方、SLA・心理言語学・教育学では、**ゼロが異常に多い構文使用回数**、**L2使用率のような割合データ**、**未知語への対処方略の選択**、**自己効力感・確信度・ルーブリック得点** も頻出します。まずはアウトカムの型を正しく決め、その後で GLM / ZI-Hurdle / Beta / multinomial / ordinal / GAM のどこへ進むかを選んでください。\n\n### 分析判断の早見表\n\n| Research Question | Outcome Type | Start Here | Escalate to GAM When... | Main Output |\n|:--|:--|:--|:--|:--|\n| 助言関係の有無を予測 | `0/1` | Binomial GLM | 連続予測子に閾値・飽和が疑われる | OR + 予測確率 |\n| 協働回数の差を比較 | count | Poisson GLM | 非線形が疑われる / 残差構造が複雑 | IRR + 予測件数 |\n| ゼロが極端に多い回数を説明 | zero-heavy count | NB / ZINB / Hurdle | まずゼロ生成過程を分けて考える。非線形化はその後 | `Pr(any use)` + 期待件数 |\n| 協働強度の差を説明 | positive continuous | Gamma GLM | 連続予測子の形が直線でない | 倍率 + 予測平均 |\n| L2使用率や完成率を説明 | continuous proportion in `(0,1)` | Beta Regression | 割合の形が直線でない / 分散も説明したい | 予測割合 + 必要なら precision |\n| 方略選択を予測 | nominal (3+ categories) | Multinomial Logit | まずカテゴリ回帰を正しく選ぶ。非線形化は別途検討 | RRR + 各カテゴリの予測確率 |\n| 5件法評定を説明 | ordered categories | Ordinal Logit | まず順序情報を保つ。必要なら `clm()` の拡張や Bayesian ordinal | 共通 OR + 各カテゴリの予測確率 |\n\n: GLM/GAM Decision Map (Applied Linguistics Data)\n\n### 3つの解釈スケール\n\nGLM/GAM では、同じモデルでも**どの尺度で読むか**によって意味が変わります。\n\n| Family | `summary()` の係数 | 指数変換後の解釈 | いちばん直感的な最終報告 |\n|---|---|---|---|\n| Binomial (logit) | log-odds | OR | predicted probability |\n| Poisson / NB (log) | log expected count | IRR | expected count |\n| Gamma (log) | log expected mean | multiplier | expected mean |\n| Beta regression | logit of expected proportion | usually stay on response scale | predicted proportion |\n| Multinomial logit | log relative risk vs. reference category | RRR | category probability |\n| Ordinal logit | cumulative log-odds | cumulative OR | category / cumulative probability |\n\n- **係数表**は、モデルが内部で何を推定したかを示します。\n- **OR / IRR / multiplier** は、1単位変化で何倍になるかを示します。\n- **応答スケール予測**は、実際にどれくらい違うのかを示します。\n\n実務では  \n`係数表で方向を確認 -> exp(beta) で倍率を確認 -> 予測図で大きさを伝える`  \nという順に読むと迷いにくくなります。\n\n## 理論はここだけ確認する\n\nGLM を一言でまとめると、**「3ステップのレシピ」**です[@nelder1972; @winter2019stats]。\n\n1. **アウトカムの形を決める**（2値なら binomial、カウントなら Poisson、正の連続量なら Gamma）\n2. **リンク関数という橋を選ぶ**（アウトカムの期待値と線形式をつなぐ変換）\n3. **最尤法で推定する**（最小二乗法の代わりに、データの「起こりやすさ」を最大化するパラメータを探す）\n\n### 分布は「取りうる値」と「ばらつき方」を決める {.unnumbered}\n\nGLM で分布を選ぶというのは、「見た目が似ている分布を当てはめる」ことではありません。分布は少なくとも次の2つを決めます。\n\n1. **どんな値をとってよいか**\n   0/1 だけなのか、0以上の整数なのか、0より大きい連続値なのか。\n2. **平均が大きくなったとき、分散がどう増えるか**\n   OLS のように一定なのか、平均と一緒に増えるのか、平均より速く増えるのか。\n\nこの2つがデータとずれていると、予測値の範囲が不自然になるだけでなく、標準誤差や信頼区間の解釈まで崩れます。したがって `family` は、単なる R の引数ではなく、**測定されたアウトカムの性質そのもの**だと考えるのが大切です。\n\n| Family / model | Allowed values | Typical variance pattern | Typical use in SLA / psycholinguistics / education | Why Gaussian is awkward |\n|---|---|---|---|---|\n| Bernoulli / Binomial | 0/1 または 0〜n 成功数 | $\\mu(1-\\mu)$ | 項目正誤、正答数、受講完了の有無 | 予測が 0 未満や 1 超になりうる |\n| Poisson | 0, 1, 2, ... | $\\mathrm{Var}(Y)=\\mu$ | エラー回数、発話回数、協働回数 | 負の件数を予測しうる。不等分散を無視 |\n| Negative Binomial | 0, 1, 2, ... | $\\mathrm{Var}(Y)>\\mu$ | ばらつきの大きいエラー回数、ゼロと多発が混在する行動数 | Poisson より柔軟だが Gaussian ではさらに不自然 |\n| Gamma | $Y>0$ | $\\mathrm{Var}(Y)\\propto\\mu^2$ | RT、発話速度、継続時間、正の強度指標 | 負の予測を出しうる。右歪みを捉えにくい |\n| Multinomial | いずれか1カテゴリ | 各カテゴリ確率の和が1 | 方略選択、反応タイプ分類 | カテゴリ間の排他性を反映できない |\n| Ordinal | 順序つきカテゴリ | latent continuum + thresholds | 自己効力感、confidence rating、ルーブリック | 順序情報を捨てるか、等間隔を仮定してしまう |\n\n#### 離散カテゴリ分布の直感図 {.unnumbered}\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndiscrete_family_df <- tibble(\n  family = c(\n    rep(\"Bernoulli / Binomial\", 2),\n    rep(\"Multinomial\", 3),\n    rep(\"Ordinal\", 5)\n  ),\n  category = c(\n    \"0\", \"1\",\n    \"Skip\", \"Infer\", \"AI\",\n    \"1\", \"2\", \"3\", \"4\", \"5\"\n  ),\n  probability = c(\n    0.35, 0.65,\n    0.30, 0.45, 0.25,\n    0.10, 0.18, 0.27, 0.25, 0.20\n  )\n)\n\nggplot(discrete_family_df, aes(x = category, y = probability, fill = family)) +\n  geom_col(width = 0.7, show.legend = FALSE) +\n  geom_text(\n    aes(label = scales::percent(probability, accuracy = 1)),\n    vjust = -0.3,\n    size = 3\n  ) +\n  facet_wrap(~ family, scales = \"free_x\") +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1),\n                     limits = c(0, 0.55)) +\n  labs(\n    x = NULL,\n    y = \"Probability\",\n    title = \"Representative discrete outcome structures\",\n    subtitle = \"Bernoulli = 2 outcomes, multinomial = unordered categories, ordinal = ordered categories\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-discrete-families-1.png){#fig-discrete-families width=2100}\n:::\n:::\n\n\nこの図では、**multinomial と ordinal の違いは「棒の本数」ではなく「左から右への順序が理論的に意味を持つかどうか」**です。`Skip / Infer / AI` は順番を入れ替えても意味は変わりませんが、`1 < 2 < 3 < 4 < 5` は順序自体が情報です。\n\n#### 平均と分散の関係を見る {.unnumbered}\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmu_dense <- seq(0.01, 10, length.out = 400)\nmu_binary <- seq(0.01, 0.99, length.out = 200)\n\nmean_var_df <- bind_rows(\n  tibble(mean = mu_dense, variance = 1, family = \"Gaussian\"),\n  tibble(mean = mu_binary, variance = mu_binary * (1 - mu_binary), family = \"Bernoulli\"),\n  tibble(mean = mu_dense, variance = mu_dense, family = \"Poisson\"),\n  tibble(mean = mu_dense, variance = mu_dense + (mu_dense^2) / 2, family = \"Neg. Binomial\"),\n  tibble(mean = mu_dense, variance = 0.35 * mu_dense^2, family = \"Gamma\")\n)\n\nggplot(mean_var_df, aes(x = mean, y = variance, color = family)) +\n  geom_line(linewidth = 1) +\n  labs(\n    x = \"Mean (mu)\",\n    y = \"Variance\",\n    color = \"Family\",\n    title = \"Different families imply different mean-variance relationships\",\n    subtitle = \"This is why family choice affects SEs and uncertainty, not just fitted values\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-mean-variance-families-1.png){#fig-mean-variance-families width=2100}\n:::\n:::\n\n\nこの図の要点は単純です。\n\n- **Gaussian** は分散が一定だと考える。\n- **Poisson** は平均が大きいほど分散も同じ比率で増える。\n- **Negative Binomial** は、Poisson より速く分散が増える。\n- **Gamma** は、平均が大きくなるほど分散がさらに速く増える。\n- **Bernoulli** は、平均が 0.5 付近で最も分散が大きく、0 や 1 に近づくと分散が小さくなる。\n\nしたがって、`family` の選択は「予測値の形」だけでなく、**不確実性をどう計算するか** そのものに直結しています。Poisson で過分散を無視すると SE が小さく出やすい、Gamma を Gaussian で近似すると右裾の不確実性をうまく拾いにくい、というのはこの図の違いが実務に現れたものです。\n\nケース別の実データ形状は後ろの Figure @fig-outcome-shapes でも確認できます。ここではまず、**どの family がどの測定スケールに自然か** を確認してください。\n\nこの3要素を数式で書くと以下のようになります[@nelder1972]。\n\n- **線形予測子**: $\\eta_i = \\mathbf{x}_i^\\top\\beta$（つまり、おなじみの「切片 + 傾き × 予測子」。$\\eta_i = \\beta_0 + \\beta_1 x_1 + \\beta_2 x_2 + \\ldots$ と同じ意味を行列で簡潔に書いたもの）\n- **リンク関数**: $g(\\mu_i) = \\eta_i$（アウトカムの平均 $\\mu$ を変換して、線形予測子 $\\eta$ と等しくする。例：logit リンクなら $\\log\\frac{\\mu}{1-\\mu} = \\eta$、つまり「確率をオッズに変換してから対数をとる」）\n- **分布**: $Y_i$ の分布（binomial, Poisson, Gamma など。アウトカムのばらつき方を指定する）\n\nGAM は GLM の線形項を平滑関数 $f(\\cdot)$ に置き換え、\n平滑化パラメータを REML 等で推定します[@wood2011; @wood2017]。言語学・心理言語学での GAM チュートリアルとしては @soskuthy2017 と @wieling2018 が包括的です。また、反応時間データに GLM（Gamma 分布）を適用する利点については @lo2015 が詳しく論じています。\n\n$$\ng(\\mu_i)=\\beta_0 + f_1(x_{i1}) + \\cdots + f_p(x_{ip})\n$$\n\n#### GAM の仕組みを理解する\n\n**GLM との違い**: GLM は「$y = \\beta_0 + \\beta_1 x$」のように、予測子 $x$ と応答 $y$ の関係を**直線**で近似します。GAM は「$y = \\beta_0 + f(x)$」のように、この直線を**なめらかな曲線** $f(x)$ に置き換えます。\n\n**曲線の作り方（スプライン）**: GAM は予測子の範囲を小さな区間に分割し、各区間で低次の多項式（通常は3次）を当てはめ、区間の境界でなめらかにつなぎます。これが**スプライン**です。つなぎ目の位置を**ノット（knot）**と呼びます。\n\n**3つの重要なパラメータ**:\n\n| パラメータ | R での指定 | 意味 | 実務での判断 |\n|---|---|---|---|\n| **k**（基底次元） | `s(x, k = 10)` | 曲線の最大の複雑さ。ノットの数に相当 | `gam.check()` で k-index < 0.8 なら k を増やす |\n| **edf**（推定自由度） | 出力の `summary()` に表示 | 実際に使われた複雑さ。1 = 直線、2 ≈ 放物線 | edf ≈ 1 なら GLM で十分。edf >> 1 なら非線形 |\n| **ペナルティ λ** | 自動推定（REML） | 曲線のくねくね度を制御。大きいほど滑らか | 研究者が直接触ることはほぼない |\n\n**なぜ REML が必要か**: OLS では「残差の二乗和を最小にする」だけで回帰直線が決まりますが、GAM では曲線の形（くねくね度）も決めなければなりません。もしペナルティなしで当てはめると、データの全てのノイズを拾ったギザギザの曲線になります（過適合）。REML（制限付き最尤法）は「データへの適合度」と「曲線の滑らかさ」を**自動的にバランス**させる推定法です。研究者がペナルティの強さを手動で調整する必要がないのが大きな利点です[@wood2011]。\n\n**アナロジー**: GAM のスプラインは「定規を使わずにフリーハンドで曲線を描く」ようなもの。ただし、ペナルティが「あまり複雑にしすぎないで」とブレーキをかけるため、過適合が防がれます[@wood2017]。\n\n**SLA での具体例**: 語彙サイズと読解速度の関係は、低語彙では大きく改善するが高語彙では頭打ちになる（天井効果）。GLM は直線しか引けないため頭打ちを捉えられませんが、GAM の `s(vocab_size)` は自動的にこの非線形パターンを検出します。\n\n実装仕様は `stats::glm` / `stats::family` / `mgcv` の公式ドキュメントを参照してください\n[@rstatsglm; @rstatsfamily; @mgcvpkg]。\n\n::: {.callout-note}\n#### なぜ GLM だけでなく GAM が必要なのか — SLA・心理言語学の先行研究から\n\nGAM が GLM/LMM より有利な場面は「非線形関係が理論的に予想される場合」です。以下は実際に GAM を使った先行研究の例です。\n\n| 研究 | 分野 | GAM で何を発見したか | なぜ GLM では不十分だったか |\n|---|---|---|---|\n| @baayen2017cave | 心理言語学 | 語彙判断 RT が実験中に非線形に変化（学習→疲労→注意の波） | LMM は試行番号を線形としか扱えず、疲労効果を見逃す |\n| @winter2016 | 言語変化 | 言語変化の軌跡が多項式では捉えきれない非線形パターンを示す | Growth Curve Analysis の多項式は端部でアーティファクトを生む |\n| @pedersen2019 | 生態学→SLA応用可 | 個人ごとに異なる非線形発達軌跡（階層的 GAM） | ランダム傾きは直線のみ。個人別の曲線は GAM の factor smooth が必要 |\n| @wirtz2025 | SLA | 英語習熟度の発達が開始年齢の非線形関数 | 早期開始の利点が一定ではなく、加速→頭打ちのパターン |\n\n**判断の目安**: `gam.check()` で edf が k-1 に張り付く、または残差に構造がある場合、GAM への拡張を検討してください。差が小さければ解釈しやすい GLM を維持するのが実務上の安全策です。\n:::\n\n::: {.callout-tip}\n#### SLA・心理言語学・教育学でよく使う GLM の組み合わせ\n\n| 研究場面 | アウトカム | 分布族 | リンク | R コード |\n|---|---|---|---|---|\n| 語彙テストの正誤 | 正答=1, 誤答=0 | Binomial | logit | `glm(correct ~ proficiency, family = binomial)` |\n| ライティングの文法エラー数 | 0, 1, 2, ... | Poisson / NB | log | `glm(errors ~ task, family = poisson)` |\n| 特定構文の使用回数（ゼロ過多） | 0 が極端に多いカウント | ZINB / Hurdle | log + logit | `pscl::zeroinfl(count ~ x | z, dist = \"negbin\")` |\n| 発話の流暢性（語数/分） | 正の連続値 | Gamma | log | `glm(wpm ~ condition, family = Gamma(link=\"log\"))` |\n| 語彙テストの反応時間 | 正の連続値 | Gamma / IG | log | `glm(rt ~ frequency, family = Gamma(link=\"log\"))` |\n| L2 使用率・完成率・滞在率 | (0,1) の連続割合 | Beta regression | logit | `betareg::betareg(prop ~ treatment + motivation)` |\n| 読解中の未知語への対処方略 | 名義カテゴリ（skip / infer / lookup） | Multinomial | baseline-category logit | `nnet::multinom(strategy ~ proficiency + anxiety)` |\n| フィードバック後の行動選択 | 名義カテゴリ（revise / ask / ignore） | Multinomial | baseline-category logit | `nnet::multinom(action ~ feedback + self_efficacy)` |\n| 文法性判断後の確信度 | 順序カテゴリ（1-5） | Ordinal | cumulative logit | `ordinal::clm(confidence ~ condition + proficiency)` |\n| 自己効力感・WTC・授業評価 | 順序カテゴリ（Likert 1-5/1-7） | Ordinal | cumulative logit | `ordinal::clm(rating ~ treatment + proficiency)` |\n| ルーブリックや CEFR レベル | 順序カテゴリ | Ordinal | cumulative logit | `ordinal::clm(level ~ instruction + background)` |\n\nこの表を見て「自分のデータはどの行か？」を判断するのが GLM の第一歩です。\n:::\n\n### モデル比較のための尤度（必須）\n\n::: {.callout-note}\n#### なぜ ANOVA の残差平方和ではなく尤度を使うのか\n\nOLS（線形回帰）では、モデルの良さを「残差平方和（RSS）」で測り、F 検定でモデルを比較しました。しかし GLM では残差平方和が使えません。理由は：\n\n- **2値データ**: 予測値は確率（0〜1）であり、「残差の二乗」は分布の形を無視した不適切な指標になる\n- **カウントデータ**: 分散が平均に依存するため、全観測に同じ重みをかける RSS は不公平\n- **Gamma データ**: 分散が平均の二乗に比例するため、同じ問題が発生\n\n代わりに、**尤度**（データが観測される確率）をモデルの良さの指標にします。尤度が高い = そのモデルがデータをよく説明している、という意味です。AIC, BIC, 尤度比検定（LRT）は全て尤度に基づいています。\n:::\n\nモデル比較を説明するには、尤度を明示しておく必要があります。\n\n$$\nL(\\theta \\mid y) = \\prod_{i=1}^{n} f(y_i \\mid \\theta), \\quad\n\\ell(\\theta) = \\log L(\\theta \\mid y)\n$$\n\n- 尤度 $L$: 「このモデルが観測データをどれだけ起こしやすいか」を表す数値。コインを10回投げて7回表が出たとき、$p = 0.7$ の尤度は $p = 0.5$ より高い（7/10 表は $p = 0.7$ の方が起こりやすいため）\n- 対数尤度 $\\ell$: 尤度の対数をとったもの。掛け算を足し算に変換できるため、コンピュータでの計算が安定する\n- $\\prod$（パイ記号）: 「全部かけ合わせる」の意味。各観測の確率を全て掛けた値が尤度\n\n実務でよく使う比較指標は以下です[@burnham2002]。\n\n$$\n\\mathrm{AIC} = -2\\ell + 2k,\\quad\n\\mathrm{BIC} = -2\\ell + k\\log n\n$$\n\n- $k$: 推定パラメータ数（`y ~ x1 + x2` なら $k = 3$：切片 + 傾き2つ）\n- 小さい AIC/BIC は、適合と複雑さのバランスがよいことを示す\n\n::: {.callout-note}\n#### 逸脱度（deviance）とは\n\nGLM の出力に表示される**逸脱度（deviance）**は、OLS での残差平方和に相当する指標です。\n\n- **Null deviance（帰無逸脱度）**: 切片だけのモデル（予測子なし）の適合悪さ\n- **Residual deviance（残差逸脱度）**: 予測子を入れたモデルの適合悪さ\n\nNull deviance から Residual deviance への**減少幅が大きいほど、予測子がデータをよく説明**しています。OLS の $R^2$ が「説明された分散の割合」であるように、逸脱度の減少率が GLM での適合の良さの目安になります。\n:::\n\n\n::: {.callout-tip}\n#### 実務での AIC/BIC の読み方\n\n2つのモデルの AIC を比較するとき、差（ΔAIC）の大きさが判断材料になります[@burnham2002]。\n\n| ΔAIC | 解釈 |\n|---|---|\n| 0〜2 | ほぼ同等。どちらを選んでも大きな差はない |\n| 4〜7 | 中程度の証拠。AIC が小さいモデルがやや優勢 |\n| 10 以上 | 強い証拠。AIC が大きいモデルは支持されにくい |\n\nBIC はサンプルサイズが大きいとき AIC よりも複雑なモデルにペナルティを課すため、\nAIC と BIC が同じ方向を向いていれば判断は明快です。\n:::\n\n入れ子モデルなら尤度比検定（LRT）も使えます。\n\n$$\n2\\{\\ell(\\text{full})-\\ell(\\text{reduced})\\}\n\\sim \\chi^2_{\\Delta df}\n$$\n\nこの検定は「大きいモデルは小さいモデルより有意に良いか？」を問います。ANOVA で入れ子モデルを F 検定で比較するのと同じ発想ですが、GLM ではカイ二乗分布を使います。$\\Delta df$ は「2つのモデルのパラメータ数の差」です。\n\n::: {.callout-note}\n#### LRT を使うときの条件\n\n- 同じ分布族・同じリンク関数・同じデータで比較する。\n- **入れ子**とは、小さいモデルが大きいモデルの「予測子を減らしたバージョン」であること（例：`y ~ x1 + x2` は `y ~ x1 + x2 + x3` の入れ子）。\n- GAM では平滑化推定が入るため、GLMのLRTと同じ感覚で乱用しない。\n  本章では GAM 比較は主に AIC / `compare_performance()` / `gam.check()` で判断する。\n:::\n\n::: {.callout-note}\n#### LRT と AIC/BIC の使い分け\n\n- **LRT**: 入れ子モデル（一方が他方の特殊ケース）でのみ使用可能。p 値を返す。\n- **AIC/BIC**: 入れ子でなくても使用可能。**ただし、異なる分布族（例: OLS vs Gamma GLM）の AIC を R の `AIC()` で直接比較するのは危険です**。R の `AIC()` は各分布族で異なるスケールの対数尤度を使うため、Gaussian と Gamma の AIC 値は同一基準ではありません。同じ分布族内でのモデル比較（例: Poisson GLM vs Poisson GAM）には安全に使えます。\n- 異なる分布族を比較する場合は、`performance::compare_performance()` の結果を参考値として示しつつ、**残差診断（DHARMa、pp_check）と理論的整合性**を主な判断基準にしてください。\n- 両方が同じ結論を指すなら、判断は明快です。矛盾する場合は、理論的な整合性と診断結果を加味して最終判断してください。\n:::\n\n### モデル前提チェックリスト（GLM/GAM）\n\n::: {.callout-important}\n#### 最もよくある間違い\n\n「アウトカムの正規性」を Shapiro-Wilk 検定で確認する方が多いですが、\nGLM では**アウトカムの正規性は仮定していません**。\n分布は `family` 引数で指定するものであり、2値データが正規分布に従わないのは当然です。\n確認すべきは「`family` がアウトカムの型と整合しているか」と「残差に大きな問題がないか」です。\n:::\n\nGLM/GAM の前提は「正規性」だけではありません。実務では次を最低限確認します。\n\n1. `outcome` と `family/link` が整合しているか（binary/count/positive）。\n2. 観測の独立性が成り立つか（反復・階層があるなら GLMM/GAMM へ）。\n3. GLM の線形項がリンクスケールで妥当か（不自然なら GAM を検討）。\n4. count で過分散がないか（Poisson 過信を避ける）。\n5. GAM の平滑項が過不足ないか（`gam.check()` の k-index / edf）。\n6. 共線性が強すぎないか（推定不安定の回避）。\n\n::: {.callout-tip}\n#### 前提確認の実装対応（本章）\n\n- 形状と平滑: `gam.check()`, `concurvity()`\n- 分布妥当性: `check_overdispersion()`, `check_model()`\n- 共線性: `check_collinearity()`\n- 階層性: 章末の「6章への接続」で GLMM 比較を実演\n:::\n\n### 2値データのリンク関数ガイド（詳細）\n\n2値アウトカムでは、同じ説明変数でもリンク関数の選び方で\n「係数の意味」「尾部の当てはまり」「予測確率の形」が変わります。\nR の公式ドキュメント `?family`（`stats::binomial()`）では、利用可能なリンク関数として `logit`, `probit`, `cloglog`, `cauchit`, `log`, `identity` が定義されています[@rstatsfamily; @rmakelink]。各分布族にはデフォルトリンクが設定されており（binomial → logit, poisson → log, Gamma → inverse）、`?make.link` でリンク関数の数学的定義を確認できます。\n\n::: {.callout-tip}\n#### R で確認する方法\n\n```r\n# 各分布族で使えるリンク関数とデフォルトリンクを確認\n?family           # 分布族の一覧とデフォルトリンク\n?binomial         # binomial で使える全リンク\n?make.link        # 各リンク関数の数学的定義\nbinomial()$link   # → \"logit\"（デフォルト）\npoisson()$link    # → \"log\"（デフォルト）\nGamma()$link      # → \"inverse\"（デフォルト。本章では log を明示指定）\n```\n:::\n\n| Link | $g(p)$ | $g^{-1}(\\eta)$ | 係数の主な解釈 | 注意点 |\n|---|---|---|---|---|\n| logit | $\\log\\{p/(1-p)\\}$ | $\\mathrm{logit}^{-1}(\\eta)$ | $\\exp(\\beta)$ はオッズ比（OR） | 最も標準的。解釈がしやすい |\n| probit | $\\Phi^{-1}(p)$ | $\\Phi(\\eta)$ | 潜在正規尺度での変化量 | logit と予測は近いが係数尺度は異なる |\n| cloglog | $\\log\\{-\\log(1-p)\\}$ | $1-\\exp\\{-\\exp(\\eta)\\}$ | 非対称リンク | 稀少イベント/離散時間ハザードで有用[@prentice1978] |\n| log-log | $-\\log\\{-\\log(p)\\}$ | $\\exp\\{-\\exp(-\\eta)\\}$ | cloglog の鏡像 | 事象定義を反転したい場合に有用[@vgamcloglog] |\n| cauchit | $\\tan\\{\\pi(p-1/2)\\}$ | $\\arctan(\\eta)/\\pi + 1/2$ | 重尾リンク | 外れ値的な線形予測子に比較的頑健 |\n| log | $\\log(p)$ | $\\exp(\\eta)$ | $\\exp(\\beta)$ はリスク比に近い解釈 | 確率>1の制約に注意 |\n| identity | $p$ | $\\eta$ | リスク差に近い | [0,1] 制約を満たしにくく通常は非推奨 |\n\n::: {.panel-tabset}\n\n#### logit vs probit（よくある疑問）\n\n- 予測確率の形はしばしば似るが、係数尺度は同一ではない。\n- 係数比較では「どちらが有意か」より、**応答スケール予測**で比較する方が安全。\n- `exp(beta)` を OR として解釈できるのは logit のときのみ。\n- **SLA での実務**: 語彙テストの正答率、文法性判断の正誤など、ほとんどのケースで logit が標準。probit を選ぶのは SDT（信号検出理論）に基づく分析の場合のみ。\n\n#### cloglog / log-log（稀少イベント向け）\n\n- **cloglog**: 成功確率が極端に低い（< 5%）場合に logit より適切。離散時間ハザードモデル（学習者の脱落分析など）で使用[@prentice1978]。\n- **log-log**: `cloglog` の反転関係。`VGAM` 文書では「0/1の定義を反転すると実質的に loglog が適用される」ことが示されている[@vgamcloglog]。\n- **SLA での場面**: 稀少な語用論的ストラテジーの使用有無、極低頻度のエラータイプなど。\n\n#### 選択の目安（実務フローチャート）\n\n1. **特別な理由がなければ logit**（OR 解釈が可能、最も広く使われる）\n2. SDT 分析なら probit（$d'$ の直接推定に対応）\n3. 稀少イベント（ベースレート < 5%）なら cloglog\n4. 理論的根拠がない限り、cauchit / log / identity は避ける\n\n:::\n\n### リンク関数の形状を見る\n\nリンク関数の選択が予測確率の形にどう影響するかを視覚的に確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\neta <- seq(-4, 4, length.out = 200)\nlinks_df <- data.frame(\n  eta = rep(eta, 3),\n  prob = c(plogis(eta), pnorm(eta), 1 - exp(-exp(eta))),\n  link = factor(rep(c(\"logit\", \"probit\", \"cloglog\"), each = 200),\n                levels = c(\"logit\", \"probit\", \"cloglog\"))\n)\n\nggplot(links_df, aes(x = eta, y = prob, color = link)) +\n  geom_line(linewidth = 1.2) +\n  geom_hline(yintercept = 0.5, linetype = \"dashed\", alpha = 0.3) +\n  geom_vline(xintercept = 0, linetype = \"dotted\", alpha = 0.3) +\n  scale_color_manual(values = c(\"#E69F00\", \"#56B4E9\", \"#009E73\")) +\n  labs(\n    x = \"Linear predictor (η = β₀ + β₁x₁ + ...)\",\n    y = \"P(Y = 1)\",\n    title = \"Inverse Link Functions\",\n    subtitle = \"logit and probit are nearly identical in the center; cloglog is asymmetric\"\n  ) +\n  annotate(\"text\", x = 2.5, y = 0.55, label = \"logit ≈ probit\\n(center region)\", size = 3, alpha = 0.6) +\n  annotate(\"text\", x = -2.5, y = 0.25, label = \"cloglog rises\\nfaster on left\", size = 3, alpha = 0.6, color = \"#009E73\")\n```\n\n::: {.cell-output-display}\n![Inverse link functions: how the linear predictor maps to probability](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-link-functions-1.png){#fig-link-functions width=2100}\n:::\n:::\n\n\n::: {.callout-note}\n**図の読み方**: 横軸は線形予測子 $\\eta$（係数と予測子の積の和）、縦軸は予測確率です。logit と probit は中央付近でほぼ同じ形ですが、cloglog は左右非対称です。**実務では logit を使い、OR で報告するのが最も一般的**です。\n:::\n\n### R の formula 記法ガイド（基本的な理解）\n\n::: {.callout-tip collapse=\"true\"}\n#### R の formula で使う記号の意味\n\n| 記号 | 意味 | 例 | 読み方 |\n|:---:|---|---|---|\n| `~` | 「〜を予測する」 | `y ~ x` | 「y を x で予測する」 |\n| `+` | 予測子を追加 | `y ~ x1 + x2` | 「y を x1 と x2 で予測する」 |\n| `*` | 主効果 + 交互作用 | `y ~ x1 * x2` | `x1 + x2 + x1:x2` と同じ |\n| `:` | 交互作用のみ | `y ~ x1:x2` | 主効果なしの交互作用 |\n| `s()` | GAM の平滑項 | `y ~ s(x, k=10)` | 「x の非線形効果を推定」 |\n| `(1\\|g)` | ランダム切片 | `y ~ x + (1\\|id)` | 「id ごとに切片が異なる」（Ch6） |\n\n**GLM の例**: `glm(correct ~ proficiency + score, family = binomial, data = dat)`\n\n→ 「正誤（correct）を、習熟度（proficiency）とスコア（score）で予測する。分布は二項分布。」\n:::\n\n## 実行前ガイド\n\n1. 研究質問を「確率・件数・平均」のどれかに落とし込む。\n2. まず GLM を当てる（最小構成）。\n3. 過分散・残差・予測妥当性を確認する。\n4. 連続予測子の形が怪しい場合だけ GAM に拡張する。\n5. 係数はリンクスケール、図は応答スケールで報告する。\n\n## セットアップ\n\n### 主なパッケージの役割\n\n- `MASS` / `nnet` / `ordinal` / `pscl` / `betareg`: 負の二項、名義、多項、ゼロ過剰、ベータ回帰など、GLM の拡張ファミリーを扱う。\n- `mgcv`: 一般化加法モデルを使って非線形効果を柔軟に表現する。\n- `tidyverse`: データ整形、可視化、結果表作成の共通基盤。\n- `performance` / `see` / `emmeans` / `sjPlot` / `broom`: 診断、診断図の描画、推定周辺平均、モデル図示、出力整形を担当する。\n- `gt`: 論文向けの比較表を整える。\n\n\n::: {.cell}\n\n```{.r .cell-code}\npacman::p_load(\n  MASS,\n  nnet,\n  ordinal,\n  pscl,\n  betareg,\n  tidyverse,\n  mgcv,\n  performance,\n  see,\n  emmeans,\n  sjPlot,\n  broom,\n  gt\n)\n\n# プロットは全て英語のため、フォント指定不要（システムデフォルトを使用）\ntheme_set(theme_bw(base_size = 12, base_family = \"HiraginoSans-W3\") +\n  theme(\n    plot.title.position = \"plot\",\n    plot.title = element_text(\n      face = \"bold\",\n      size = rel(1.0),\n      lineheight = 1.04,\n      margin = margin(b = 6)\n    ),\n    plot.subtitle = element_text(\n      size = rel(0.92),\n      lineheight = 1.04,\n      margin = margin(b = 8)\n    ),\n    plot.margin = margin(t = 10, r = 14, b = 10, l = 10)\n  ))\ntheme_update(legend.position = \"bottom\")\nset.seed(2024)\n\n# ── ヘルパー関数 ──\n# make_ic_table(): 複数モデルの AIC/BIC/deviance を1つの比較表にまとめる\n# plot_check_model_separate(): 明示的に指定した check を描画する\n\nmake_ic_table <- function(...) {\n  models <- list(...)\n  names(models) |>\n    purrr::map_dfr(function(nm) {\n      m <- models[[nm]]\n      tibble(\n        model = nm,\n        AIC = AIC(m),\n        BIC = BIC(m),\n        deviance = deviance(m)\n      )\n    }) |>\n    arrange(AIC)\n}\n\nplot_check_model_separate <- function(\n  model,\n  model_name = deparse(substitute(model)),\n  checks = c(\"pp_check\", \"linearity\", \"binned_residuals\", \"outliers\", \"vif\")\n) {\n  diagnostic_bundle <- performance::check_model(\n    model,\n    panel = FALSE,\n    check = checks\n  )\n  diagnostic_plots <- plot(diagnostic_bundle)\n\n  if (length(diagnostic_plots) == 0) {\n    return(invisible(NULL))\n  }\n\n  purrr::iwalk(diagnostic_plots, function(p, nm) {\n    p_out <- p +\n      theme_bw(base_size = 12, base_family = \"HiraginoSans-W3\") +\n  theme(\n    plot.title.position = \"plot\",\n    plot.title = element_text(\n      face = \"bold\",\n      size = rel(1.0),\n      lineheight = 1.04,\n      margin = margin(b = 6)\n    ),\n    plot.subtitle = element_text(\n      size = rel(0.92),\n      lineheight = 1.04,\n      margin = margin(b = 8)\n    ),\n    plot.margin = margin(t = 10, r = 14, b = 10, l = 10)\n  ) +\n      theme(\n        legend.position = \"bottom\",\n        plot.title = element_text(face = \"bold\", size = 12),\n        axis.title = element_text(size = 11),\n        axis.text = element_text(size = 10)\n      ) +\n      labs(caption = paste(\"Model:\", model_name, \"| Check:\", nm))\n    print(p_out)\n  })\n\n  invisible(diagnostic_plots)\n}\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprof_levels <- c(\"A2\", \"B1\", \"B2\")\nprof_map <- c(A2 = 1, B1 = 2, B2 = 3)\nclass_levels <- c(\"A\", \"B\", \"C\")\n\nsimulate_independent_dyads <- function(n, directed = TRUE) {\n  class_from <- sample(class_levels, n, replace = TRUE, prob = c(0.35, 0.35, 0.30))\n  prof_from <- sample(prof_levels, n, replace = TRUE, prob = c(0.30, 0.45, 0.25))\n  prof_num_from <- unname(prof_map[prof_from])\n  score_from_raw <- rnorm(n, mean = c(A2 = -0.8, B1 = 0.0, B2 = 0.8)[prof_from], sd = 0.85)\n\n  same_class <- rbinom(n, 1, plogis(-0.2 + 0.25 * (prof_num_from - 2)))\n  class_to <- ifelse(\n    same_class == 1,\n    class_from,\n    purrr::map_chr(class_from, ~ sample(setdiff(class_levels, .x), 1))\n  )\n\n  prof_to <- sample(prof_levels, n, replace = TRUE, prob = c(0.32, 0.43, 0.25))\n  prof_num_to <- unname(prof_map[prof_to])\n  score_to_raw <- rnorm(n, mean = c(A2 = -0.8, B1 = 0.0, B2 = 0.8)[prof_to], sd = 0.85)\n\n  tibble(\n    observation_id = seq_len(n),\n    from = if (directed) sprintf(\"sender_%03d\", seq_len(n)) else sprintf(\"member_a_%03d\", seq_len(n)),\n    to = if (directed) sprintf(\"receiver_%03d\", seq_len(n)) else sprintf(\"member_b_%03d\", seq_len(n)),\n    class_from = factor(class_from, levels = class_levels),\n    class_to = factor(class_to, levels = class_levels),\n    prof_from = factor(prof_from, levels = prof_levels),\n    prof_to = factor(prof_to, levels = prof_levels),\n    prof_num_from = prof_num_from,\n    prof_num_to = prof_num_to,\n    score_from_raw = score_from_raw,\n    score_to_raw = score_to_raw\n  ) |>\n    mutate(\n      score_from_sc = as.numeric(scale(score_from_raw)),\n      score_to_sc = as.numeric(scale(score_to_raw)),\n      same_class = same_class,\n      prof_gap = abs(prof_num_from - prof_num_to),\n      score_gap = abs(score_from_sc - score_to_sc),\n      baseline_mean_sc = (score_from_sc + score_to_sc) / 2\n    )\n}\n\ndat_bin <- simulate_independent_dyads(420, directed = TRUE) |>\n  mutate(\n    eta = -1.55 +\n      0.75 * same_class +\n      0.22 * prof_gap +\n      0.40 * score_from_sc -\n      0.18 * score_to_sc +\n      0.10 * score_gap +\n      0.20 * (prof_num_from == 3) -\n      0.16 * (prof_num_to == 3) +\n      0.30 * sin(score_from_sc) -\n      0.18 * score_to_sc^2,\n    tie = rbinom(n(), 1, plogis(eta))\n  ) |>\n  dplyr::select(-eta, -score_from_raw, -score_to_raw)\n\ndat_count <- simulate_independent_dyads(320, directed = FALSE) |>\n  mutate(\n    eta = 0.15 +\n      0.65 * same_class -\n      0.20 * prof_gap +\n      0.38 * baseline_mean_sc -\n      0.16 * baseline_mean_sc^2,\n    mu = exp(eta),\n    weight = rnbinom(n(), mu = mu, size = 1.45)\n  ) |>\n  dplyr::select(-eta, -mu, -score_from_raw, -score_to_raw)\n\ndat_pos <- tibble(\n  learner_id = sprintf(\"L%03d\", 1:260),\n  class = factor(sample(class_levels, 260, replace = TRUE, prob = c(0.35, 0.33, 0.32)), levels = class_levels),\n  proficiency = factor(sample(prof_levels, 260, replace = TRUE, prob = c(0.30, 0.46, 0.24)), levels = prof_levels)\n) |>\n  mutate(\n    prof_num = unname(prof_map[as.character(proficiency)]),\n    baseline_score = rnorm(n(), mean = c(A2 = -0.7, B1 = 0.0, B2 = 0.9)[as.character(proficiency)], sd = 0.9),\n    baseline_score_sc = as.numeric(scale(baseline_score)),\n    mu = exp(\n      1.45 +\n        0.27 * baseline_score_sc +\n        c(A = 0.00, B = 0.12, C = -0.10)[as.character(class)] +\n        c(A2 = -0.10, B1 = 0.00, B2 = 0.16)[as.character(proficiency)] +\n        0.14 * sin(1.4 * baseline_score_sc)\n    ),\n    collab_strength = rgamma(n(), shape = 5.2, scale = mu / 5.2)\n  ) |>\n  dplyr::select(-mu)\n\ntibble(\n  dataset = c(\"Binary dyads\", \"Count dyads\", \"Positive learners\"),\n  n = c(nrow(dat_bin), nrow(dat_count), nrow(dat_pos)),\n  independent_unit = c(\"one directed dyad\", \"one undirected dyad\", \"one learner\")\n) |>\n  invisible()\n```\n:::\n\n\n| Dataset | n | Independent unit |\n|:--|--:|:--|\n| Binary dyads | 420 | one directed dyad |\n| Count dyads | 320 | one undirected dyad |\n| Positive learners | 260 | one learner |\n\n: Independent observation units used in this chapter\n\nこの章では、GLM/GAM の前提に合わせるため、3 ケースすべてで **独立観測** を使います。ケース1・2の単位は独立に抽出された dyad、ケース3は独立に抽出された learner です。\n\n\n::: {.cell}\n\n```{.r .cell-code}\noutcome_shapes <- bind_rows(\n  tibble(case = \"Case 1: Binary\", value = dat_bin$tie),\n  tibble(case = \"Case 2: Count\", value = dat_count$weight),\n  tibble(case = \"Case 3: Positive\", value = dat_pos$collab_strength)\n)\n\nggplot(outcome_shapes, aes(x = value)) +\n  geom_histogram(bins = 24, fill = \"#4C78A8\", color = \"white\") +\n  facet_wrap(~ case, scales = \"free_x\") +\n  labs(\n    x = \"Observed outcome value\",\n    y = \"Frequency\",\n    title = \"Why the three cases need different families\"\n  )\n```\n\n::: {.cell-output-display}\n![Outcome shapes across the three cases](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-outcome-shapes-1.png){#fig-outcome-shapes width=3000}\n:::\n:::\n\n\n**図の読み方**:\n\n- **Case 1** は 0/1 の二点にしか値が出ません。したがって Gaussian ではなく binomial を使います。\n- **Case 2** は 0 が多く、右裾が長い整数分布です。まず Poisson から始め、過分散があれば NB に切り替えます。\n- **Case 3** は正の連続値で右に歪んでいます。負の予測を避けたいので Gamma + log link が自然です。\n\n------------------------------------------------------------------------\n\n## ケース1: 助言関係の有無（Binomial GLM/GAM）\n\n**目的**: 2人組の特徴（同クラスか、習熟度差、ベースライン差）で、\n助言関係（有/無）を予測します。\n\n### モデル用データの作成（独立 dyad のシミュレーション）\n\nここでは、**独立に抽出された sender -> receiver dyad** を 420 件作成します。\n各行は 1 つの独立観測であり、同じ `from` / `to` は再利用しません。\nアウトカム `tie` は、その dyad で助言関係が観測されたら 1、そうでなければ 0 です。\n予測子には、dyad レベルの特徴量を含めます。\n\n- `same_class`: 同じクラスに所属していれば 1\n- `prof_gap`: 習熟度の絶対差（0, 1, 2）\n- `score_from_sc` / `score_to_sc`: 送信者・受信者のベースライン得点（標準化済み）\n- `score_gap`: 得点の絶対差\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncount(\n  dat_bin,\n  same_class,\n  prof_gap,\n  tie\n) |>\n  arrange(same_class, prof_gap, tie) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Binary case: observed counts by dyad characteristics\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Binary case: observed counts by dyad characteristics\n\n| same_class| prof_gap| tie|   n|\n|----------:|--------:|---:|---:|\n|          0|        0|   0|  66|\n|          0|        0|   1|  20|\n|          0|        1|   0| 100|\n|          0|        1|   1|  22|\n|          0|        2|   0|  30|\n|          0|        2|   1|   9|\n|          1|        0|   0|  34|\n|          1|        0|   1|  17|\n|          1|        1|   0|  56|\n|          1|        1|   1|  39|\n|          1|        2|   0|  18|\n|          1|        2|   1|   9|\n\n\n:::\n:::\n\n\nこの表は、`same_class` と `prof_gap` ごとに 0/1 がどれくらい出ているかを示します。\n`tie = 1` の割合が低いほど、モデルは少数の 1 を多数の 0 から区別する必要があります。\nこれは 2 値データの典型的な特性であり、通常の線形回帰（OLS）ではなく GLM を使う理由の一つです。\n\n#### ベースレートが極端に低い場合の注意\n\nベースレートが 5% 未満になると、**完全分離**（complete separation）や**準完全分離**（quasi-complete separation）が起きやすくなります。\nこれが生じると、一部の係数が極端に大きくなり、標準誤差が無限大に近づきます。\nモデル当てはめ時に警告が出た場合は、この問題を疑ってください。\n\n### GLM と GAM\n\nここでは同じ研究課題に対し、**4種類のリンク関数による GLM** と **1つの GAM** を当てはめます。\n複数のリンク関数を試す目的は、有意な結果を「探す」ためではなく、\n**実質的な結論がリンク関数の選択に依存しないか**を確認するためです。\n\n各モデルの設定を確認しておきます。\n\n- `family = binomial(link = \"logit\")`: アウトカムが 0/1 であることを R に伝え、logit リンクを使う\n- `family = binomial(link = \"probit\")`: 同じ2値アウトカムだが、リンク関数を正規分布の累積分布関数に変更\n- `family = binomial(link = \"cloglog\")`: 非対称リンク。稀少イベントや離散時間ハザードモデルで有用\n- `family = binomial(link = \"cauchit\")`: 重い裾をもつリンク。外れ値的な線形予測子に頑健\n\nGAM では、連続予測子 `score_from_sc` と `score_to_sc` を平滑関数 `s()` で扱い、\n非線形関係を自動的に検出させます。\n\n- `method = \"REML\"`: 平滑化パラメータの推定に制限付き最尤法を使用（@wood2011 が推奨する標準設定）\n- `select = TRUE`: 不要な平滑項をゼロに縮退させることを許可（自動変数選択に近い効果。edf ≈ 0 なら「その予測子の非線形効果は不要」と判断される）\n- `k = 8`: 平滑項の基底次元の上限。実際に使われる自由度（edf）はデータから推定される。`k` は「曲線の最大の複雑さ」を制御するため、小さすぎると真の非線形パターンを見逃す。目安: 予測子のユニーク値数の半分程度から開始し、`gam.check()` で k-index < 0.8 なら増やす\n\n\n::: {.cell}\n\n```{.r .cell-code}\nf_bin <- tie ~ same_class + prof_gap + score_from_sc + score_to_sc + score_gap +\n  prof_from + prof_to\n\nm_bin_glm <- glm(\n  f_bin,\n  family = binomial(link = \"logit\"),\n  data = dat_bin\n)\n\nm_bin_probit <- glm(\n  f_bin,\n  family = binomial(link = \"probit\"),\n  data = dat_bin\n)\n\nm_bin_cloglog <- glm(\n  f_bin,\n  family = binomial(link = \"cloglog\"),\n  data = dat_bin\n)\n\nm_bin_cauchit <- glm(\n  f_bin,\n  family = binomial(link = \"cauchit\"),\n  data = dat_bin\n)\n\nm_bin_gam <- gam(\n  tie ~ same_class + prof_gap +\n    s(score_from_sc, k = 8) +\n    s(score_to_sc, k = 8) +\n    score_gap +\n    prof_from + prof_to,\n  family = binomial(link = \"logit\"),\n  data = dat_bin,\n  method = \"REML\",\n  select = TRUE\n)\n\nbroom::tidy(m_bin_glm, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Binomial GLM: Logistic Regression\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Binomial GLM: Logistic Regression\n\n|term          | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:-------------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept)   |   -1.706|     0.417|    -4.092|   0.000|   -2.556|    -0.915|\n|same_class    |    0.762|     0.238|     3.200|   0.001|    0.297|     1.233|\n|prof_gap      |   -0.058|     0.195|    -0.299|   0.765|   -0.441|     0.326|\n|score_from_sc |    0.466|     0.149|     3.125|   0.002|    0.178|     0.764|\n|score_to_sc   |   -0.192|     0.151|    -1.269|   0.205|   -0.493|     0.102|\n|score_gap     |   -0.070|     0.161|    -0.433|   0.665|   -0.391|     0.241|\n|prof_fromB1   |    0.391|     0.319|     1.228|   0.219|   -0.224|     1.030|\n|prof_fromB2   |    1.067|     0.374|     2.852|   0.004|    0.342|     1.813|\n|prof_toB1     |    0.078|     0.307|     0.255|   0.799|   -0.521|     0.684|\n|prof_toB2     |   -0.291|     0.388|    -0.749|   0.454|   -1.062|     0.463|\n\n\n:::\n\n```{.r .cell-code}\nbroom::tidy(m_bin_gam, parametric = TRUE, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Binomial GAM: Parametric Terms\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Binomial GAM: Parametric Terms\n\n|term        | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:-----------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept) |   -1.682|     0.411|    -4.089|   0.000|   -2.488|    -0.876|\n|same_class  |    0.750|     0.237|     3.164|   0.002|    0.285|     1.214|\n|prof_gap    |   -0.064|     0.195|    -0.331|   0.741|   -0.446|     0.317|\n|score_gap   |   -0.036|     0.154|    -0.230|   0.818|   -0.338|     0.267|\n|prof_fromB1 |    0.435|     0.317|     1.374|   0.169|   -0.186|     1.057|\n|prof_fromB2 |    1.150|     0.369|     3.120|   0.002|    0.428|     1.873|\n|prof_toB1   |   -0.008|     0.294|    -0.027|   0.979|   -0.583|     0.567|\n|prof_toB2   |   -0.456|     0.348|    -1.308|   0.191|   -1.139|     0.227|\n\n\n:::\n:::\n\n\n::: {.callout-warning}\n#### 重要: Estimate 列はロジットスケール\n\n上の表の `Estimate` 列は**確率でも確率の変化でもありません**。対数オッズ（ロジットスケール）の値です。例えば Estimate = 1.0 は「その予測子が 1 単位増えると、対数オッズが 1.0 増加する」ことを意味し、これは確率に直すと状況によって +5% にも +20% にもなり得ます。確率スケールでの効果を知りたい場合は、後述の OR（オッズ比）変換または応答スケール予測プロットを参照してください。\n:::\n\n#### `summary(m_bin_glm)` の読み方（ロジスティック GLM）\n\n出力の **Coefficients テーブル** を以下の手順で読みます。\n\n| 列 | 意味 | 実務上の読み方 |\n|---|---|---|\n| `Estimate` | **ロジットスケール**の係数（確率ではない） | 正なら対数オッズが増加＝助言関係の可能性が高まる |\n| `Std. Error` | 推定の不確実さ | 小さいほど精度が高い |\n| `z value` | Wald 検定統計量（Estimate / SE） | 絶対値が約 2 以上なら有意に近い |\n| `Pr(>|z|)` | p 値 | < .05 で「偶然では説明しにくい」と判断 |\n\n- **Null deviance vs Residual deviance**: Null deviance は切片のみモデルの逸脱度、Residual deviance は当てはめ後の逸脱度です。差が大きいほど、予測子全体がモデルの適合を改善しています。\n- **AIC**: モデル比較に使います（値が小さいほど、適合と複雑さのバランスがよい）。\n\n::: {.callout-tip}\n#### 係数の符号を読む（ケース1）\n\n- `same_class` の推定値が正 → 同クラスのペアは助言関係をもちやすい\n- `prof_gap` の推定値が正 → 習熟度差が大きいペアほど助言関係をもちやすい\n- `score_from_sc` の推定値が正 → ベースライン得点が高い送信者ほど助言を求めやすい\n\nただし、これらはすべて**ロジットスケール**です。実質的な意味を解釈するには、後述の OR（オッズ比）への変換が必要です。\n:::\n\n#### `summary(m_bin_gam)` の読み方（GAM 固有の出力）\n\nGAM の出力には GLM にはない **Approximate significance of smooth terms** セクションがあります。\n\n| 項目 | 意味 | 判断基準 |\n|---|---|---|\n| `edf`（有効自由度） | 平滑項が実質的に使っている自由度 | edf ≈ 1 なら線形と同等（GAM の利点なし）。edf > 2 なら意味のある非線形 |\n| `Ref.df` | 検定に使用される参照自由度 | 技術的な値（通常は解釈不要） |\n| `Chi.sq` / `p-value` | 平滑項全体の有意性検定 | p < .05 なら「直線では捉えられない関係がある」 |\n\n- edf が **0 に近い**場合（`select = TRUE` 使用時）：その平滑項はペナルティで完全に縮退しており、予測に寄与していません。\n- **R-sq.(adj)**: 逸脱度ベースの近似決定係数。GLM の値と比較して GAM の追加利得を評価できます。\n\n\n::: {#tbl-binary-gam-smooths .cell}\n::: {.cell-output-display}\n\n\nTable: Case 1: Smooth-term summary from the binomial GAM\n\n|Smooth term      |   edf| Ref.df| Chi-square|     p|How to read it                       |\n|:----------------|-----:|------:|----------:|-----:|:------------------------------------|\n|s(score_from_sc) | 0.902|      7|      8.732| 0.002|Almost linear; GLM may be sufficient |\n|s(score_to_sc)   | 0.393|      7|      0.616| 0.210|Almost linear; GLM may be sufficient |\n\n\n:::\n:::\n\n\nこの表は、GAM の平滑項を最短で読むための要約です。まず `edf` を見て「直線で足りるか」を判断し、次に `p` 値で「その非線形全体がゼロと言えるか」を確認します。ただし、**本当にどんな形をしているか** は表だけでは分からないので、次の smooth plot を必ず見ます。\n\n#### smooth plot の読み方\n\nGAM の解釈で注意が必要なのは、`plot.gam()` や smooth plot の **y 軸が確率ではない** ことです。ここでは `predict(..., type = \"terms\")` を使って、各平滑項が **リンクスケール上でどれだけ寄与しているか** を可視化します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsmooth_grid <- tidyr::crossing(\n  score_from_sc = seq(min(dat_bin$score_from_sc), max(dat_bin$score_from_sc), length.out = 180),\n  score_to_sc = seq(min(dat_bin$score_to_sc), max(dat_bin$score_to_sc), length.out = 180)\n)\n\nsmooth_from_grid <- tibble(\n  same_class = 0L,\n  prof_gap = 1,\n  score_from_sc = seq(min(dat_bin$score_from_sc), max(dat_bin$score_from_sc), length.out = 180),\n  score_to_sc = mean(dat_bin$score_to_sc),\n  score_gap = abs(score_from_sc - score_to_sc),\n  prof_from = factor(\"B1\", levels = levels(dat_bin$prof_from)),\n  prof_to = factor(\"B1\", levels = levels(dat_bin$prof_to))\n)\n\nsmooth_to_grid <- tibble(\n  same_class = 0L,\n  prof_gap = 1,\n  score_from_sc = mean(dat_bin$score_from_sc),\n  score_to_sc = seq(min(dat_bin$score_to_sc), max(dat_bin$score_to_sc), length.out = 180),\n  score_gap = abs(score_from_sc - score_to_sc),\n  prof_from = factor(\"B1\", levels = levels(dat_bin$prof_from)),\n  prof_to = factor(\"B1\", levels = levels(dat_bin$prof_to))\n)\n\nsmooth_from_pred <- predict(m_bin_gam, newdata = smooth_from_grid, type = \"terms\", se.fit = TRUE)\nsmooth_to_pred <- predict(m_bin_gam, newdata = smooth_to_grid, type = \"terms\", se.fit = TRUE)\n\nsmooth_plot_tbl <- bind_rows(\n  tibble(\n    predictor_value = smooth_from_grid$score_from_sc,\n    effect = smooth_from_pred$fit[, \"s(score_from_sc)\"],\n    se = smooth_from_pred$se.fit[, \"s(score_from_sc)\"],\n    smooth = \"s(score_from_sc)\"\n  ),\n  tibble(\n    predictor_value = smooth_to_grid$score_to_sc,\n    effect = smooth_to_pred$fit[, \"s(score_to_sc)\"],\n    se = smooth_to_pred$se.fit[, \"s(score_to_sc)\"],\n    smooth = \"s(score_to_sc)\"\n  )\n) |>\n  mutate(\n    lower = effect - 1.96 * se,\n    upper = effect + 1.96 * se,\n    smooth = factor(\n      smooth,\n      levels = c(\"s(score_from_sc)\", \"s(score_to_sc)\"),\n      labels = c(\"Sender score smooth\", \"Receiver score smooth\")\n    )\n  )\n\nggplot(smooth_plot_tbl, aes(x = predictor_value, y = effect)) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", color = \"gray55\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), fill = \"#9ecae1\", alpha = 0.35) +\n  geom_line(color = \"#08519c\", linewidth = 1) +\n  facet_wrap(~ smooth, scales = \"free_x\") +\n  labs(\n    x = \"Predictor value (z)\",\n    y = \"Centered smooth effect on the link scale\",\n    title = \"How to read GAM smooths\",\n    subtitle = \"These are partial effects on the logit scale, not probabilities\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-binary-gam-smooths-1.png){#fig-binary-gam-smooths width=2100}\n:::\n:::\n\n\nこの図の読み方は次の通りです。\n\n- **y 軸は確率ではなく、リンクスケール上の部分効果**です。0 線は「その予測子が平均的な条件に比べて追加的な寄与をしていない」基準です。\n- 曲線が **上にある** 領域では、その値域で助言関係のロジットが押し上げられます。**下にある** 領域では押し下げられます。\n- 傾きが急な場所は、「その範囲で予測子の変化が結果に強く効く」区間です。平らなら、その範囲では実質的な変化が小さいと読めます。\n- リボンが広い端部は、データが少なく不確実性が大きい領域です。端のうねりを過信しないでください。\n- smooth plot は「形」を読む図です。**最終的な確率の大きさ**を説明したいときは、この直後の応答スケール予測図を使います。\n\n要するに、GAM では\n\n1. `edf` で「直線か、曲線か」を確認する  \n2. smooth plot で「どの範囲で曲がっているか」を確認する  \n3. 応答スケール予測で「確率がどれだけ変わるか」を説明する\n\nという順序で読むと迷いにくくなります。\n\n### リンク関数の比較（Case 1: 2値）\n\n4つのリンク関数（logit, probit, cloglog, cauchit）が同じデータでどの程度異なる予測を生むかを確認します。\nここでの核心的な問いは「リンク関数の選択で結論が変わるか？」です。\n\nまず `compare_performance()` で適合度を数値比較し、\nその後、予測確率の曲線を視覚的に比較します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::compare_performance(\n  m_bin_glm,\n  m_bin_probit,\n  m_bin_cloglog,\n  m_bin_cauchit\n) |>\n  knitr::kable(caption = \"Case 1: Link Function Comparison (GLM)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: Link Function Comparison (GLM)\n\n|Name          |Model |      AIC|    AIC_wt|     AICc|   AICc_wt|      BIC|    BIC_wt|      RMSE| Sigma|  Log_loss| Score_log| Score_spherical|       PCP| R2_Nagelkerke|   R2_Tjur|\n|:-------------|:-----|--------:|---------:|--------:|---------:|--------:|---------:|---------:|-----:|---------:|---------:|---------------:|---------:|-------------:|---------:|\n|m_bin_glm     |glm   | 453.0596| 0.2722955| 453.5975| 0.2722955| 493.4621| 0.2722955| 0.4128840|     1| 0.5155471| -40.51843|       0.0076263| 0.6589908|            NA| 0.1470901|\n|m_bin_probit  |glm   | 453.0545| 0.2729876| 453.5924| 0.2729876| 493.4570| 0.2729876| 0.4131028|     1| 0.5155411| -40.43744|       0.0070829| 0.6585045|     0.1981594|        NA|\n|m_bin_cloglog |glm   | 453.2363| 0.2492708| 453.7742| 0.2492708| 493.6388| 0.2492708| 0.4127628|     1| 0.5157575| -40.63611|       0.0077250| 0.6591385|     0.1976199|        NA|\n|m_bin_cauchit |glm   | 453.6230| 0.2054460| 454.1609| 0.2054460| 494.0255| 0.2054460| 0.4123004|     1| 0.5162178| -41.18027|       0.0108504| 0.6597301|     0.1964713|        NA|\n\n\n:::\n:::\n\n\n上の表で **AIC 列**を比較します。4つの AIC が 2〜3 以内に収まっていれば、\nこのデータではリンク関数間に実質的な差はないと判断できます。\nその場合は、**解釈のしやすさ（OR として報告可能）** から logit を採用するのが実務上の標準です。\n\n逆に、1つのリンク関数だけ AIC が 4 以上小さければ、そのリンクがデータの裾の挙動をよりよく捉えている可能性があります。\n\n次に、各リンク関数の予測確率を可視化して比較します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnew_bin_link <- tidyr::expand_grid(\n  same_class = c(0L, 1L),\n  prof_gap = 1,\n  score_from_sc = seq(-2, 2, length.out = 120),\n  score_to_sc = 0,\n  prof_from = factor(\"B1\", levels = levels(dat_bin$prof_from)),\n  prof_to = factor(\"B1\", levels = levels(dat_bin$prof_to))\n)\n\nnew_bin_link$score_gap <- abs(new_bin_link$score_from_sc - new_bin_link$score_to_sc)\nnew_bin_link$p_logit <- predict(m_bin_glm, newdata = new_bin_link, type = \"response\")\nnew_bin_link$p_probit <- predict(m_bin_probit, newdata = new_bin_link, type = \"response\")\nnew_bin_link$p_cloglog <- predict(m_bin_cloglog, newdata = new_bin_link, type = \"response\")\nnew_bin_link$p_cauchit <- predict(m_bin_cauchit, newdata = new_bin_link, type = \"response\")\n\nplot_link <- new_bin_link |>\n  pivot_longer(\n    cols = c(p_logit, p_probit, p_cloglog, p_cauchit),\n    names_to = \"link\",\n    values_to = \"prob\"\n  ) |>\n  mutate(\n    link = dplyr::case_match(\n      link,\n      \"p_logit\" ~ \"logit\",\n      \"p_probit\" ~ \"probit\",\n      \"p_cloglog\" ~ \"cloglog\",\n      \"p_cauchit\" ~ \"cauchit\"\n    ),\n    same_class = factor(\n      same_class,\n      levels = c(0, 1),\n      labels = c(\"Different class\", \"Same class\")\n    )\n  )\n\nggplot(plot_link, aes(x = score_from_sc, y = prob, color = link, linetype = same_class)) +\n  geom_line(linewidth = 0.9) +\n  labs(\n    x = \"Sender Baseline Score (z)\",\n    y = \"Predicted Probability of Advice Tie\",\n    title = \"Case 1: Predicted Probabilities by Link Function\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/pred-link-binary-1.png){width=2100}\n:::\n:::\n\n\nこの図は、送信者のベースライン得点（横軸）を変化させたときの助言関係の予測確率（縦軸）を、\n4つのリンク関数ごとに描いています。実線と破線は同クラス / 異クラスの比較です。\n\n**読み方のポイント**:\n\n- 4本の曲線が中央付近でほぼ重なっている場合、リンク関数の選択は実質的に結果に影響しません。\n- 差が生じるのは主に**裾（極端な得点域）**です。cloglog は上裾の立ち上がりが速く、cauchit は裾が重い（ゆっくり 0/1 に近づく）傾向があります。\n- 曲線が大きく重なっていれば、**logit を採用して OR で報告する**のが最も通りがよい選択です。\n\n### モデル比較と OR\n\nGLM と GAM のどちらを最終モデルにするかを判断します。\n判断基準は「GAM にすることで、GLM では捉えられなかった非線形パターンが改善されるか？」です。\n\n#### SLA での判断例\n語彙テストで「語彙サイズと正答率の関係」を調べた場合、GLM は直線的な関係を仮定しますが、GAM は天井効果（高語彙サイズで正答率が頭打ちになる）を自動的に捉えます。AIC が 4 以上改善し、edf > 2 であれば、非線形モデルを採用する根拠になります。\n\n以下の3ステップで比較します:\n\n::: {.panel-tabset}\n\n#### AIC/BIC\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmake_ic_table(\n  \"Binomial GLM\" = m_bin_glm,\n  \"Binomial GAM\" = m_bin_gam\n) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: Model Comparison (Binary)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: Model Comparison (Binary)\n\n|model        |     AIC|     BIC| deviance|\n|:------------|-------:|-------:|--------:|\n|Binomial GAM | 453.017| 491.893|  433.773|\n|Binomial GLM | 453.060| 493.462|  433.060|\n\n\n:::\n:::\n\n\nAIC / BIC / deviance を比較します。GAM の AIC が GLM より 4 以上小さければ非線形の追加利得がある可能性があります。\n差が小さければ、より解釈しやすい GLM を採用するのが実務上の安全策です。\n\n#### LRT（尤度比検定）\n\nGLM 同士の入れ子比較（尤度比検定）を実演します。\nここでは `score_gap` を除いた reduced モデルと full モデルを比較し、\n「この予測子を追加することで適合が統計的に改善するか」を検定します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 尤度ベース比較（入れ子 GLM）\ndat_bin_glm_complete <- model.frame(m_bin_glm)\nm_bin_glm_reduced <- update(m_bin_glm, . ~ . - score_gap, data = dat_bin_glm_complete)\n\ntibble(\n  model = c(\"Reduced GLM\", \"Full GLM\"),\n  logLik = c(as.numeric(logLik(m_bin_glm_reduced)), as.numeric(logLik(m_bin_glm))),\n  AIC = c(AIC(m_bin_glm_reduced), AIC(m_bin_glm)),\n  BIC = c(BIC(m_bin_glm_reduced), BIC(m_bin_glm))\n) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: Likelihood-based Comparison (Nested GLM)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: Likelihood-based Comparison (Nested GLM)\n\n|model       |   logLik|     AIC|     BIC|\n|:-----------|--------:|-------:|-------:|\n|Reduced GLM | -216.624| 451.248| 487.611|\n|Full GLM    | -216.530| 453.060| 493.462|\n\n\n:::\n\n```{.r .cell-code}\nanova(m_bin_glm_reduced, m_bin_glm, test = \"Chisq\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nAnalysis of Deviance Table\n\nModel 1: tie ~ same_class + prof_gap + score_from_sc + score_to_sc + prof_from + \n    prof_to\nModel 2: tie ~ same_class + prof_gap + score_from_sc + score_to_sc + score_gap + \n    prof_from + prof_to\n  Resid. Df Resid. Dev Df Deviance Pr(>Chi)\n1       411     433.25                     \n2       410     433.06  1   0.1888   0.6639\n```\n\n\n:::\n:::\n\n\n`anova()` の出力で `Pr(>Chi)` が有意（< .05）であれば、`score_gap` の追加が適合を有意に改善したと判断できます。\n非有意であれば、`score_gap` を除いたシンプルなモデルで十分です。\n\n#### compare_performance\n\n`compare_performance()` は AIC / BIC に加え、Tjur's R²（2値モデル専用の擬似決定係数）なども一括で比較でき、\n単一指標への依存を避けたいときに便利です。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::compare_performance(m_bin_glm, m_bin_gam) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: compare_performance()\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: compare_performance()\n\n|Name      |Model |     AIC| AIC_wt|    AICc| AICc_wt|     BIC| BIC_wt|  RMSE| Sigma| Log_loss| Score_log| Score_spherical|   PCP| R2_Tjur|    R2|\n|:---------|:-----|-------:|------:|-------:|-------:|-------:|------:|-----:|-----:|--------:|---------:|---------------:|-----:|-------:|-----:|\n|m_bin_glm |glm   | 453.060|  0.495| 453.597|    0.49| 493.462|  0.313| 0.413|     1|    0.516|   -40.518|           0.008| 0.659|   0.147|    NA|\n|m_bin_gam |gam   | 453.017|  0.505| 453.516|    0.51| 491.893|  0.687| 0.413|     1|    0.516|   -40.278|           0.008| 0.657|      NA| 0.128|\n\n\n:::\n:::\n\n\n**`compare_performance()` テーブルの読み方**:\n\n| 列名 | 意味 | 判断基準 |\n|---|---|---|\n| **AIC / BIC** | 情報量規準（低いほど良い） | 差が 4 以上あればモデル選択の根拠になる |\n| **R2_Tjur** | Tjur の判別 R²（2値モデル専用）。予測確率の平均差 | 0 に近いと予測力なし、1 に近いと完全判別 |\n| **RMSE** | 残差の標準偏差。予測誤差の大きさ | 低いほど良い。2値モデルでは 0〜1 の範囲 |\n| **Sigma** | 残差の分散推定 | GLM では 1 に固定（binomial） |\n\n**読み方のポイント**: AIC が同等（差 < 2）なら、より解釈しやすい GLM を採用します。R2_Tjur は効果量の指標として報告に使えます。\n\n:::\n\n### sjPlot による報告用テーブル\n\n`sjPlot::tab_model()` は、論文に直接貼れる形式の回帰結果テーブルを HTML で生成します。\nOR（オッズ比）、信頼区間、p 値が自動計算されるため、報告用テーブルの作成に便利です。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsjPlot::tab_model(\n  m_bin_glm,\n  show.aic = TRUE,\n  show.se = TRUE,\n  dv.labels = \"Case 1 Binomial GLM\"\n)\n```\n\n::: {.cell-output-display}\n`````{=html}\n<table style=\"border-collapse:collapse; border:none;\">\n<tr>\n<th style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm;  text-align:left; \">&nbsp;</th>\n<th colspan=\"4\" style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm; \">Case 1 Binomial GLM</th>\n</tr>\n<tr>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  text-align:left; \">Predictors</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">Odds Ratios</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">std. Error</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">CI</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">p</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">(Intercept)</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.18</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.08</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.08&nbsp;&ndash;&nbsp;0.40</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>&lt;0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">same class</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">2.14</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.51</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.35&nbsp;&ndash;&nbsp;3.43</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">prof gap</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.94</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.18</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.64&nbsp;&ndash;&nbsp;1.38</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.765</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">score from sc</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.59</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.24</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.19&nbsp;&ndash;&nbsp;2.15</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>0.002</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">score to sc</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.83</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.12</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.61&nbsp;&ndash;&nbsp;1.11</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.205</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">score gap</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.93</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.15</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.68&nbsp;&ndash;&nbsp;1.27</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.665</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">prof from [B1]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.48</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.47</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.80&nbsp;&ndash;&nbsp;2.80</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.219</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">prof from [B2]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">2.91</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.09</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.41&nbsp;&ndash;&nbsp;6.13</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>0.004</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">prof to [B1]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.08</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.33</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.59&nbsp;&ndash;&nbsp;1.98</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.799</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">prof to [B2]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.75</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.29</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.35&nbsp;&ndash;&nbsp;1.59</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.454</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm; border-top:1px solid;\">Observations</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left; border-top:1px solid;\" colspan=\"4\">420</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm;\">R<sup>2</sup> Tjur</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">0.147</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm;\">AIC</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">453.060</td>\n</tr>\n\n</table>\n\n`````\n:::\n:::\n\n\n**`tab_model()` テーブルの読み方**:\n\n- **Predictors**: 予測変数名。参照カテゴリ（例: A2）は表に現れません。\n- **Odds Ratios**: OR（オッズ比）。1 を基準に「何倍か」を読みます。\n- **CI**: 95% 信頼区間。1 をまたげば統計的に非有意。\n- **p**: p 値。太字 or *** は有意を示します。\n- **Observations**: 観測数。**AIC**: モデルの情報量規準。\n\n`sjPlot` は**報告用の整形レイヤー**として非常に有用です。ただし、どのモデルを採用するかは `sjPlot` ではなく、AIC/BIC、診断、理論的整合性で判断します。\n\n`tab_model()` は自動的に OR に変換するため、`summary()` のログオッズ係数を自分で `exp()` する必要がありません。**論文のテーブルにはこの出力をそのまま使えます。**\n\nロジスティック回帰の係数をそのまま読んでも直感的ではありません（ロジットスケールのため）。\nそこで、係数を**指数変換**して**オッズ比（OR: Odds Ratio）**に変換します。\n\n- OR = 1: 効果なし\n- OR > 1: その予測子が増えると、アウトカム（助言関係）のオッズが増加\n- OR < 1: オッズが減少\n\n`broom::tidy(..., exponentiate = TRUE)` がこの変換を自動で行います。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbroom::tidy(m_bin_glm, conf.int = TRUE, exponentiate = TRUE) |>\n  rename(\n    Term = term,\n    OR = estimate,\n    `2.5%` = conf.low,\n    `97.5%` = conf.high,\n    `p` = p.value\n  ) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: Odds Ratios (Binomial GLM)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: Odds Ratios (Binomial GLM)\n\n|Term          |    OR| std.error| statistic|     p|  2.5%| 97.5%|\n|:-------------|-----:|---------:|---------:|-----:|-----:|-----:|\n|(Intercept)   | 0.182|     0.417|    -4.092| 0.000| 0.078| 0.400|\n|same_class    | 2.144|     0.238|     3.200| 0.001| 1.346| 3.431|\n|prof_gap      | 0.943|     0.195|    -0.299| 0.765| 0.643| 1.385|\n|score_from_sc | 1.594|     0.149|     3.125| 0.002| 1.195| 2.148|\n|score_to_sc   | 0.825|     0.151|    -1.269| 0.205| 0.611| 1.107|\n|score_gap     | 0.933|     0.161|    -0.433| 0.665| 0.676| 1.273|\n|prof_fromB1   | 1.479|     0.319|     1.228| 0.219| 0.799| 2.800|\n|prof_fromB2   | 2.906|     0.374|     2.852| 0.004| 1.408| 6.130|\n|prof_toB1     | 1.081|     0.307|     0.255| 0.799| 0.594| 1.983|\n|prof_toB2     | 0.748|     0.388|    -0.749| 0.454| 0.346| 1.590|\n\n\n:::\n:::\n\n\n#### OR テーブルの読み方（worked interpretation）\n\n各予測子の OR を以下のように解釈します。\n\n- **same_class**: OR が 1 より大きければ、同クラスのペアは異クラスに比べてその倍数だけ助言関係のオッズが高い。たとえば OR = 2.5 なら「同クラスは異クラスの 2.5 倍のオッズで助言関係をもつ」ことを意味します。\n- **prof_gap**: OR が 1 より大きければ、習熟度差が 1 段階大きくなるごとにオッズがその倍率で増加します。\n- **score_from_sc**: 送信者のベースライン得点が 1SD 高くなるごとのオッズの変化を示します。\n- **score_to_sc**: 受信者のベースライン得点が 1SD 高くなるごとのオッズの変化です。\n- **score_gap**: 送信者と受信者の得点差が 1SD 大きくなるごとのオッズの変化です。\n- **prof_from / prof_to**: カテゴリカル変数のため、参照水準（A2）と比較した各水準の OR です。\n\n::: {.callout-tip}\n#### 論文報告で OR を使うときの4点セット\n\nOR を報告する際は、以下の4つを必ずセットで提示してください。\n\n1. **効果の方向**（正 or 負）\n2. **OR と 95% 信頼区間**（CI が 1 をまたがなければ有意）\n3. **p 値**\n4. **応答スケールでの予測確率**（下のセクションで算出）\n\nOR だけでは「実際にどれくらい確率が変わるか」が伝わりにくいため、\n予測確率の差を併記することで読者の理解が格段に向上します。\n\n:::\n\n### 応答スケール予測（予測確率）\n\nOR はオッズの比であり、確率そのものではありません。\n同じ OR = 2 でも、ベースラインの確率が 5% のときと 50% のときでは、確率の変化幅が大きく異なります。\nそのため、**応答スケール（確率スケール）での予測**を可視化し、\nモデルの実質的な含意を直感的に伝えることが重要です。\n\nここでは、送信者のベースライン得点（`score_from_sc`）を −2 から +2 まで動かし、\n同クラス / 異クラスの条件ごとに予測確率を算出します。\nGLM と GAM の予測を重ねることで、「非線形を許すとどれだけ予測が変わるか」も確認できます。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnew_bin <- tidyr::expand_grid(\n  same_class = c(0L, 1L),\n  prof_gap = 1,\n  score_from_sc = seq(-2, 2, length.out = 120),\n  score_to_sc = 0,\n  prof_from = factor(\"B1\", levels = levels(dat_bin$prof_from)),\n  prof_to = factor(\"B1\", levels = levels(dat_bin$prof_to))\n) |>\n  mutate(score_gap = abs(score_from_sc - score_to_sc))\n\n# GLM: link スケールで予測 + SE → 逆変換で CI を算出\npred_glm <- predict(m_bin_glm, newdata = new_bin, type = \"link\", se.fit = TRUE)\nnew_bin$prob_glm <- plogis(pred_glm$fit)\nnew_bin$lwr_glm  <- plogis(pred_glm$fit - 1.96 * pred_glm$se.fit)\nnew_bin$upr_glm  <- plogis(pred_glm$fit + 1.96 * pred_glm$se.fit)\n\n# GAM: 同様\npred_gam <- predict(m_bin_gam, newdata = new_bin, type = \"link\", se.fit = TRUE)\nnew_bin$prob_gam <- plogis(pred_gam$fit)\nnew_bin$lwr_gam  <- plogis(pred_gam$fit - 1.96 * pred_gam$se.fit)\nnew_bin$upr_gam  <- plogis(pred_gam$fit + 1.96 * pred_gam$se.fit)\n\nplot_bin <- new_bin |>\n  pivot_longer(\n    cols = c(prob_glm, prob_gam),\n    names_to = \"model\",\n    values_to = \"prob\"\n  ) |>\n  mutate(\n    lwr = ifelse(model == \"prob_glm\", lwr_glm, lwr_gam),\n    upr = ifelse(model == \"prob_glm\", upr_glm, upr_gam),\n    model = dplyr::case_match(model, \"prob_glm\" ~ \"GLM\", \"prob_gam\" ~ \"GAM\"),\n    same_class = factor(same_class, levels = c(0, 1), labels = c(\"Different class\", \"Same class\"))\n  )\n\nggplot(plot_bin, aes(x = score_from_sc, y = prob, color = model, fill = model, linetype = same_class)) +\n  geom_ribbon(aes(ymin = lwr, ymax = upr), alpha = 0.15, linetype = \"solid\") +\n  geom_line(linewidth = 1) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Sender Baseline Score (z)\",\n    y = \"Predicted Probability of Advice Tie\",\n    title = \"Case 1: Response-Scale Prediction (with 95% CI)\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/pred-binary-1.png){width=2100}\n:::\n:::\n\n\n**図の読み方**:\n\n- **y 軸**: 助言関係の予測確率（0〜1）。読者にとって最も直感的なスケールです。\n- **GLM と GAM の曲線がほぼ一致**: ロジットスケールでの線形仮定が妥当であり、より単純な GLM で十分。\n- **GAM 曲線に明確な屈曲がある**: 非線形関係があり、GAM がより適切。\n- **同クラスと異クラスの曲線の間隔**: `same_class` 効果の実質的な大きさを確率スケールで示します。間隔が大きいほど、クラスの同一性が助言関係に大きく影響しています。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbin_prob_diff <- new_bin |>\n  mutate(class_condition = if_else(same_class == 1L, \"same\", \"different\")) |>\n  dplyr::select(score_from_sc, class_condition, prob_glm, prob_gam) |>\n  pivot_wider(\n    names_from = class_condition,\n    values_from = c(prob_glm, prob_gam)\n  ) |>\n  transmute(\n    score_from_sc,\n    risk_diff_glm = prob_glm_same - prob_glm_different,\n    risk_diff_gam = prob_gam_same - prob_gam_different\n  ) |>\n  pivot_longer(\n    cols = c(risk_diff_glm, risk_diff_gam),\n    names_to = \"model\",\n    values_to = \"risk_difference\"\n  ) |>\n  mutate(model = dplyr::case_match(\n    model,\n    \"risk_diff_glm\" ~ \"GLM\",\n    \"risk_diff_gam\" ~ \"GAM\"\n  ))\n\nggplot(bin_prob_diff, aes(x = score_from_sc, y = risk_difference, color = model)) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", color = \"gray55\") +\n  geom_line(linewidth = 1) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Sender Baseline Score (z)\",\n    y = \"Absolute Probability Difference\\n(Same class - Different class)\",\n    color = \"Model\",\n    title = \"Case 1: How much does same-class membership change the probability?\",\n    subtitle = \"Probability-point differences are often easier to interpret than odds ratios\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-binary-risk-diff-1.png){#fig-binary-risk-diff width=2100}\n:::\n:::\n\n\nこの差分図は、`same_class` を **何倍のオッズ**ではなく、**何ポイント確率が上がるか** で読ませるための図です。例えば y 軸が `0.18` なら、「同クラスのペアは異クラスのペアより助言関係をもつ確率が 18 ポイント高い」と読めます。教育学・SLA の文脈では、この **probability-point difference** の方が実質的な含意を説明しやすいことが多いです。\n\n\n::: {#tbl-binary-prob-examples .cell}\n::: {.cell-output-display}\n\n\nTable: Case 1: Selected predicted probabilities for representative sender profiles\n\n|sender_level   |same_class      |GLM probability |GAM probability |\n|:--------------|:---------------|:---------------|:---------------|\n|Low (-1.5 SD)  |Different class |7%              |8%              |\n|Low (-1.5 SD)  |Different class |8%              |8%              |\n|Low (-1.5 SD)  |Different class |5%              |5%              |\n|Low (-1.5 SD)  |Different class |10%             |12%             |\n|Low (-1.5 SD)  |Different class |11%             |12%             |\n|Low (-1.5 SD)  |Different class |8%              |8%              |\n|Low (-1.5 SD)  |Different class |18%             |22%             |\n|Low (-1.5 SD)  |Different class |19%             |22%             |\n|Low (-1.5 SD)  |Different class |14%             |15%             |\n|Low (-1.5 SD)  |Same class      |14%             |16%             |\n|Low (-1.5 SD)  |Same class      |15%             |16%             |\n|Low (-1.5 SD)  |Same class      |11%             |11%             |\n|Low (-1.5 SD)  |Same class      |20%             |23%             |\n|Low (-1.5 SD)  |Same class      |21%             |22%             |\n|Low (-1.5 SD)  |Same class      |15%             |16%             |\n|Low (-1.5 SD)  |Same class      |32%             |37%             |\n|Low (-1.5 SD)  |Same class      |34%             |37%             |\n|Low (-1.5 SD)  |Same class      |26%             |27%             |\n|Mean           |Different class |15%             |15%             |\n|Mean           |Different class |16%             |15%             |\n|Mean           |Different class |11%             |10%             |\n|Mean           |Different class |20%             |21%             |\n|Mean           |Different class |22%             |21%             |\n|Mean           |Different class |16%             |15%             |\n|Mean           |Different class |33%             |36%             |\n|Mean           |Different class |35%             |35%             |\n|Mean           |Different class |27%             |26%             |\n|Mean           |Same class      |27%             |27%             |\n|Mean           |Same class      |28%             |27%             |\n|Mean           |Same class      |22%             |19%             |\n|Mean           |Same class      |35%             |36%             |\n|Mean           |Same class      |37%             |36%             |\n|Mean           |Same class      |29%             |27%             |\n|Mean           |Same class      |52%             |54%             |\n|Mean           |Same class      |54%             |54%             |\n|Mean           |Same class      |44%             |43%             |\n|High (+1.5 SD) |Different class |24%             |23%             |\n|High (+1.5 SD) |Different class |25%             |23%             |\n|High (+1.5 SD) |Different class |19%             |16%             |\n|High (+1.5 SD) |Different class |31%             |32%             |\n|High (+1.5 SD) |Different class |33%             |32%             |\n|High (+1.5 SD) |Different class |26%             |23%             |\n|High (+1.5 SD) |Different class |47%             |49%             |\n|High (+1.5 SD) |Different class |49%             |49%             |\n|High (+1.5 SD) |Different class |40%             |38%             |\n|High (+1.5 SD) |Same class      |40%             |39%             |\n|High (+1.5 SD) |Same class      |42%             |39%             |\n|High (+1.5 SD) |Same class      |33%             |29%             |\n|High (+1.5 SD) |Same class      |50%             |50%             |\n|High (+1.5 SD) |Same class      |52%             |50%             |\n|High (+1.5 SD) |Same class      |42%             |39%             |\n|High (+1.5 SD) |Same class      |66%             |67%             |\n|High (+1.5 SD) |Same class      |68%             |67%             |\n|High (+1.5 SD) |Same class      |59%             |57%             |\n\n\n:::\n:::\n\n\nこの表は、図を本文用の数字に落としたものです。論文では「平均的な送信者では同クラスで約 X%、異クラスで約 Y%」のように、**代表値での予測確率** を1〜2行示すと、OR だけを出すよりはるかに伝わりやすくなります。\n\n### 予測性能の確認（Calibration / Brier score）\n\n2値モデルでは、**予測確率そのものがどれだけ信用できるか** も見ておくと解釈が安定します。ここで確認したいのは、(a) 予測確率 40% のケースが実際にも 40% 前後で起きているかという **calibration** と、(b) 確率予測の平均二乗誤差である **Brier score** です[@brier1950]。\n\nこれは AIC や OR の代わりではなく、**予測確率を教育的な意思決定に使ってよいか** をみる追加チェックです。SLA の介入研究なら、「誰が tie を持ちやすいか」だけでなく、「予測確率 0.70 をどこまで信頼してよいか」を確認する、という位置づけです。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbin_eval_tbl <- bind_rows(\n  tibble(\n    model = \"GLM\",\n    observed = model.response(model.frame(m_bin_glm)),\n    predicted = predict(m_bin_glm, type = \"response\")\n  ),\n  tibble(\n    model = \"GAM\",\n    observed = model.response(model.frame(m_bin_gam)),\n    predicted = predict(m_bin_gam, type = \"response\")\n  )\n)\n\nbin_brier_tbl <- bin_eval_tbl |>\n  group_by(model) |>\n  summarise(\n    Brier = mean((observed - predicted)^2),\n    Mean_predicted_probability = mean(predicted),\n    Observed_base_rate = mean(observed),\n    .groups = \"drop\"\n  )\n\nbin_calibration_tbl <- bin_eval_tbl |>\n  group_by(model) |>\n  mutate(decile = dplyr::ntile(predicted, 10L)) |>\n  group_by(model, decile) |>\n  summarise(\n    mean_predicted = mean(predicted),\n    observed_rate = mean(observed),\n    n = n(),\n    .groups = \"drop\"\n  )\n```\n:::\n\n\n\n::: {#tbl-binary-brier .cell}\n::: {.cell-output-display}\n\n\nTable: Case 1: Brier score and average predicted probability\n\n|model | Brier| Mean_predicted_probability| Observed_base_rate|\n|:-----|-----:|--------------------------:|------------------:|\n|GAM   | 0.171|                      0.276|              0.276|\n|GLM   | 0.170|                      0.276|              0.276|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  bin_calibration_tbl,\n  aes(x = mean_predicted, y = observed_rate, color = model, size = n)\n) +\n  geom_abline(intercept = 0, slope = 1, linetype = \"dashed\", color = \"gray55\") +\n  geom_line(linewidth = 0.8, alpha = 0.8) +\n  geom_point(alpha = 0.9) +\n  scale_x_continuous(labels = scales::percent_format(accuracy = 1)) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Mean predicted probability (by decile)\",\n    y = \"Observed tie rate\",\n    color = \"Model\",\n    size = \"n in bin\",\n    title = \"Calibration plot for the binary models\",\n    subtitle = \"Points close to the 45-degree line indicate well-calibrated predicted probabilities\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-binary-calibration-1.png){#fig-binary-calibration width=2100}\n:::\n:::\n\n\n**読み方のポイント**:\n\n- **45度線に近い**: 予測確率と実測率がよく一致している  \n- **線より下**: 確率を高く出しすぎている（over-prediction）  \n- **線より上**: 確率を低く出しすぎている（under-prediction）  \n- **Brier score は低いほど良い**: 0 に近いほど、確率予測のズレが小さい\n\nGLM と GAM で Brier と calibration がほぼ同じなら、**非線形を入れても実務上の確率予測はほとんど変わっていない** と判断できます。その場合は、より単純で報告しやすい GLM を選ぶ理由が強くなります。\n\n`sjPlot::plot_model()` は、特定の予測子の効果を可視化する簡便な関数です。\n`type = \"pred\"` で応答スケールの予測を出力し、`terms` で表示する予測子を選択します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsjPlot::plot_model(\n  m_bin_glm,\n  type = \"pred\",\n  terms = c(\"score_from_sc\", \"prof_from\"),\n  show.data = FALSE\n) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Sender baseline score (z)\",\n    y = \"Predicted probability of advice tie\",\n    color = \"Sender proficiency\",\n    fill = \"Sender proficiency\"\n  ) +\n  theme_bw(base_size = 12, base_family = \"HiraginoSans-W3\") +\n  theme(\n    plot.title.position = \"plot\",\n    plot.title = element_text(\n      face = \"bold\",\n      size = rel(1.0),\n      lineheight = 1.04,\n      margin = margin(b = 6)\n    ),\n    plot.subtitle = element_text(\n      size = rel(0.92),\n      lineheight = 1.04,\n      margin = margin(b = 8)\n    ),\n    plot.margin = margin(t = 10, r = 14, b = 10, l = 10)\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/sjplot-binary-pred-1.png){width=2100}\n:::\n:::\n\n\n### 下位検定（Case 1: 送信者/受信者習熟度）\n\nGLM の係数テーブルは「習熟度が有意か」を示しますが、\n「具体的にどの習熟度レベル間で差があるのか」は教えてくれません（3水準以上のカテゴリ変数の場合）。\n\n`emmeans`（estimated marginal means）を使うと、各水準における予測確率を算出し、\nさらにペアごとの差を検定できます。\n`type = \"response\"` を指定することで、ロジットスケールではなく**確率スケール**で結果が得られます。\nHolm 法による多重比較補正を適用しています。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nemm_from <- emmeans(m_bin_glm, ~ prof_from, type = \"response\")\nemm_to <- emmeans(m_bin_glm, ~ prof_to, type = \"response\")\n\nas.data.frame(emm_from) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: EMMs for Sender Proficiency (response scale)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: EMMs for Sender Proficiency (response scale)\n\n|prof_from |  prob|    SE|  df| asymp.LCL| asymp.UCL|\n|:---------|-----:|-----:|---:|---------:|---------:|\n|A2        | 0.179| 0.038| Inf|     0.116|     0.265|\n|B1        | 0.244| 0.035| Inf|     0.181|     0.319|\n|B2        | 0.388| 0.058| Inf|     0.281|     0.506|\n\n\n:::\n\n```{.r .cell-code}\npairs(emm_from, adjust = \"holm\") |>\n  as.data.frame() |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: Pairwise (Sender Proficiency, Holm)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: Pairwise (Sender Proficiency, Holm)\n\n|contrast | odds.ratio|    SE|  df| null| z.ratio| p.value|\n|:--------|----------:|-----:|---:|----:|-------:|-------:|\n|A2 / B1  |      0.676| 0.215| Inf|    1|  -1.228|   0.219|\n|A2 / B2  |      0.344| 0.129| Inf|    1|  -2.852|   0.013|\n|B1 / B2  |      0.509| 0.156| Inf|    1|  -2.205|   0.055|\n\n\n:::\n\n```{.r .cell-code}\nas.data.frame(emm_to) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: EMMs for Receiver Proficiency (response scale)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: EMMs for Receiver Proficiency (response scale)\n\n|prof_to |  prob|    SE|  df| asymp.LCL| asymp.UCL|\n|:-------|-----:|-----:|---:|---------:|---------:|\n|A2      | 0.275| 0.047| Inf|     0.193|     0.376|\n|B1      | 0.291| 0.041| Inf|     0.219|     0.376|\n|B2      | 0.221| 0.047| Inf|     0.143|     0.327|\n\n\n:::\n\n```{.r .cell-code}\npairs(emm_to, adjust = \"holm\") |>\n  as.data.frame() |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 1: Pairwise (Receiver Proficiency, Holm)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 1: Pairwise (Receiver Proficiency, Holm)\n\n|contrast | odds.ratio|    SE|  df| null| z.ratio| p.value|\n|:--------|----------:|-----:|---:|----:|-------:|-------:|\n|A2 / B1  |      0.925| 0.284| Inf|    1|  -0.255|   0.908|\n|A2 / B2  |      1.337| 0.519| Inf|    1|   0.749|   0.908|\n|B1 / B2  |      1.446| 0.478| Inf|    1|   1.116|   0.794|\n\n\n:::\n:::\n\n\n#### EMMs テーブルの読み方\n\n- **EMMs テーブル**: 各習熟度水準（A2, B1, B2）の予測確率を示します。他の予測子は平均値に固定されています。「B2 の送信者は平均的に X% の確率で助言関係をもつ」のように読みます。\n- **Pairwise テーブル**: 各ペア（A2-B1, A2-B2, B1-B2）の対数オッズ差と p 値を示します。`p.value` が Holm 補正後に .05 未満であれば、そのペア間に有意な差があります。\n- 報告では、EMMs の確率値をペア間の差と合わせて提示すると、読者に伝わりやすくなります（例：「B2 送信者の予測確率は X% であり、A2 送信者の Y% と比べて有意に高かった, p = .ZZZ」）。\n\n### 診断\n\nモデルを報告する前に、主要な仮定が破綻していないかを確認します。\n以下のタブで、それぞれ異なる側面を点検します。\n\n::: {.panel-tabset}\n\n#### GAM 診断（gam.check）\n\n**なぜ必要か**: GAM は平滑項の柔軟さに依存するため、(1) 分布の仮定が妥当か、(2) 基底次元 `k` が十分か、の2点を事後的に確認する必要があります[@wood2017]。`gam.check()` は4つの診断プロットと基底次元の検定を同時に出力します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngam.check(m_bin_gam)\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-gamcheck-1.png){width=2100}\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nMethod: REML   Optimizer: outer newton\nfull convergence after 10 iterations.\nGradient range [-8.066061e-05,-1.450062e-05]\n(score 223.8431 & scale 1).\nHessian positive definite, eigenvalue range [3.287562e-05,0.4038427].\nModel rank =  22 / 22 \n\nBasis dimension (k) checking results. Low p-value (k-index<1) may\nindicate that k is too low, especially if edf is close to k'.\n\n                    k'   edf k-index p-value\ns(score_from_sc) 7.000 0.902    0.96    0.30\ns(score_to_sc)   7.000 0.393    0.94    0.16\n```\n\n\n:::\n:::\n\n\n**4つのプロットの読み方**:\n\n- **左上（QQ プロット）**: 残差が対角線に沿っていれば分布仮定は妥当。裾で大きくずれていれば分布族の不適合を示唆（例：Poisson → Negative Binomial への変更を検討）。\n- **右上（残差 vs 線形予測子）**: 系統的なパターン（U字型、漏斗型）がなければ OK。パターンがあれば予測子の非線形性や分散構造の問題を示唆。\n- **左下（ヒストグラム）**: 残差の分布。極端な歪みがなければ問題なし。\n- **右下（残差 vs 応答値）**: 応答値の大きさに依存した残差パターンがないか確認。Gamma や Poisson では軽い扇状が許容される。\n\n**k-index テーブルの読み方**:\n\n- k-index ≈ 1.0: 現在の `k` で十分\n- k-index < 0.8 かつ p < .05: `k` を倍にして再推定（例: `k = 8` → `k = 16`）\n- edf が k-1 に張り付く: `k` が明らかに不足。増やすべき\n\n#### k の判断ルール\n\n`gam.check()` で k-index < 0.8 かつ p < .05 の場合、GAM を `k` を倍にして再当てはめしてください（例：`k = 8` → `k = 16`）。\nk-index ≈ 1.0 であれば現在の設定で問題ありません。\n\n#### Concurvity（平滑項間の冗長性）\n\n`concurvity()` は、平滑項同士が冗長でないか（多重共線性の平滑版）を確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nconcurvity(m_bin_gam, full = FALSE)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$worst\n                         para s(score_from_sc) s(score_to_sc)\npara             1.000000e+00     1.736509e-26   6.114138e-27\ns(score_from_sc) 1.678725e-26     1.000000e+00   4.044390e-02\ns(score_to_sc)   5.797809e-27     4.044390e-02   1.000000e+00\n\n$observed\n                         para s(score_from_sc) s(score_to_sc)\npara             1.000000e+00     1.068044e-33   4.369194e-35\ns(score_from_sc) 1.678725e-26     1.000000e+00   1.175292e-02\ns(score_to_sc)   5.797809e-27     1.158307e-02   1.000000e+00\n\n$estimate\n                         para s(score_from_sc) s(score_to_sc)\npara             1.000000e+00     6.352736e-29   3.751667e-29\ns(score_from_sc) 1.678725e-26     1.000000e+00   1.502561e-02\ns(score_to_sc)   5.797809e-27     1.237727e-02   1.000000e+00\n```\n\n\n:::\n:::\n\n\n**concurvity とは**: 回帰の「多重共線性（VIF）」の GAM 版です。平滑項同士が似たパターンを捉えようとしていないかを測定します。`full = FALSE` を指定すると、各平滑項がペアワイズでどの程度冗長かを示す行列が得られます。\n\n**判断基準**（`worst` 列を見る）:\n\n- 値が 0 に近い: 冗長性なし（問題なし）\n- 0.3〜0.8: 中程度の冗長性（注意が必要だが致命的ではない）\n- 0.8 以上: 強い冗長性。平滑項の統合または一方の削除を検討\n\n**読み方の例**: もし `s(score_from_sc)` の `worst` 列が 0.45 であれば、「sender のスコアの平滑効果は他の平滑項と中程度に冗長」→ 注意は必要だが、即座に削除する必要はありません。0.9 であれば、その平滑項は他の項とほぼ同じ情報を捉えているため、一方を削除するか線形項（`score_from_sc` を `s()` なしで入れる）に簡素化すべきです。\n\n#### 過分散・共線性\n\nGLM 側の診断として、過分散と共線性を確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::check_overdispersion(m_bin_glm)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Overdispersion test\n\n dispersion ratio = 1.004\n          p-value = 0.984\n```\n\n\n:::\n\n```{.r .cell-code}\nperformance::check_collinearity(m_bin_glm)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Check for Multicollinearity\n\nLow Correlation\n\n          Term  VIF   VIF 95% CI adj. VIF Tolerance Tolerance 95% CI\n    same_class 1.02 [1.00, 7.34]     1.01      0.98     [0.14, 1.00]\n      prof_gap 1.19 [1.10, 1.38]     1.09      0.84     [0.73, 0.91]\n score_from_sc 1.43 [1.29, 1.64]     1.19      0.70     [0.61, 0.78]\n   score_to_sc 1.56 [1.39, 1.79]     1.25      0.64     [0.56, 0.72]\n     score_gap 1.18 [1.09, 1.37]     1.09      0.85     [0.73, 0.92]\n     prof_from 1.50 [1.34, 1.72]     1.11      0.67     [0.58, 0.74]\n       prof_to 1.68 [1.49, 1.93]     1.14      0.60     [0.52, 0.67]\n```\n\n\n:::\n:::\n\n\n**過分散の読み方**:\n\n- **2値データの重要な注意**: 個別レベルの 0/1 データ（各行が1回の試行）では、分散パラメータは定義上 1 に固定されるため、**過分散は理論的に発生しません**。`check_overdispersion()` の結果はここでは参考情報にすぎません。過分散が問題になるのは、集約された二項データ（n > 1 の試行数がある場合）やカウントデータ（ケース2）です。\n- ケース2（Poisson/NB）では、このチェックが**モデル選択の決定的な根拠**になります。\n\n**共線性（VIF）の読み方**:\n\n- VIF < 3: 問題なし\n- VIF 3〜5: 中程度の共線性。推定は可能だが注意\n- VIF > 5: 高い共線性。予測子の削除・統合を検討\n- カテゴリカル変数の VIF は構造的にやや高くなりやすい点に留意\n\n#### 総合診断（check_model）\n\n`check_model()` はモデル仮定の視覚的な一括チェックを提供します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot_check_model_separate(\n  m_bin_glm,\n  model_name = \"m_bin_glm\",\n  checks = c(\"pp_check\", \"binned_residuals\", \"outliers\", \"vif\")\n)\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-1.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-2.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-3.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-4.png){width=3000}\n:::\n\n```{.r .cell-code}\nplot_check_model_separate(\n  m_bin_gam,\n  model_name = \"m_bin_gam\",\n  checks = c(\"pp_check\", \"binned_residuals\", \"outliers\", \"vif\")\n)\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-5.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-6.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-binary-checkmodel-7.png){width=3000}\n:::\n:::\n\n\n**出力の読み方**:\n\n- **Posterior predictive check**: モデルから生成したデータが実データに似ていれば、モデルの仮定は妥当。\n- **Binned residuals**: 2値データでは通常の残差プロットが使えないため、残差をビンに分けて平均残差を確認します。ほとんどのビンがゼロ近辺にあれば OK。\n- **Influential observations**: クック距離が大きい観測値がないかを確認します。\n\n:::\n\n#### 注意点\n\n- GAM の平滑項が縮退（edf がほぼ 0 または 1）した場合、\n  「非線形の追加利得は小さい」と判断できる。\n- 最終報告は OR と予測確率図を併記すると読み手に伝わりやすい。\n- 診断で深刻な問題がなければ、論文には「主要な仮定違反は認められなかった」と1文で記述し、必要に応じて図を supplementary materials に掲載します。\n\n------------------------------------------------------------------------\n\nケース1では**2値アウトカム**（助言関係の有無）を Binomial GLM/GAM で分析しました。次のケース2では、独立 dyad の**カウントデータ**（協働回数: 0, 1, 2, ...）を Poisson / 負の二項分布でモデル化します。分布族が変わっても、分析の流れ（推定→比較→解釈→診断）は同じです。\n\n## ケース2: 協働回数（Count: Poisson/NB/GAM）\n\n**目的**: 学習者ペアの協働回数（0を含む）を、\n同クラスかどうか、習熟度差、ベースライン平均で説明します。\n\nここでの観測単位は、授業やタスクが異なるため相互に独立とみなせる **undirected dyad** です。\n\n#### なぜ Poisson から始めるのか\n\nカウントデータ（0, 1, 2, ...）には Poisson 分布が最も自然です。Poisson は「単位時間あたりに何回起きるか」を記述する分布で、SLA ではエラー回数、発話回数、協働回数などに対応します。\n\nただし、Poisson は**平均 = 分散**を仮定します。例えば平均3回なら分散も3。しかし実データではばらつきがもっと大きいこと（**過分散**）がよくあります。例：平均3回なのに、0回と15回の学習者が混在していれば分散は3よりはるかに大きくなります。\n\n**過分散を無視すると何が起きるか**: Poisson モデルは「ばらつきは平均と同じくらい」と信じています。実際のばらつきがその2倍あるのに Poisson を使うと、**標準誤差が本来の半分程度に過小推定**されます。結果として：\n\n- p 値が実際より小さくなる（偽陽性のリスクが上がる）\n- 信頼区間が実際より狭くなる（精度を過信する）\n- 「有意」と判断した効果が、実はノイズだった可能性が高まる\n\nつまり、過分散を放置すると**結論が信頼できなくなる**のです。\n\nこの場合、Negative Binomial（NB）に切り替えます。NB は分散が平均より大きくてもよい分布で、追加の分散パラメータ（theta）でばらつきの超過分を吸収します。**まず Poisson で始め、過分散を確認し、必要なら NB に切り替える**のが GLM の標準的な手順です[@burnham2002]。\n\n### モデル用データの作成（独立 dyad のシミュレーション）\n\nここでは、独立に抽出された undirected dyad を 320 件使います。\nアウトカム `weight` は、各 dyad の協働回数（0 を含む整数値）です。\n0 が多いのは count data の典型的な特徴です。\n\n予測子は以下のとおりです。\n\n- `same_class`: 同クラスなら 1\n- `prof_gap`: 習熟度の絶対差\n- `baseline_mean_sc`: ペア両者のベースライン得点の平均（標準化済み）\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndat_count |>\n  summarise(\n    n = n(),\n    zero_rate = mean(weight == 0),\n    mean_weight = mean(weight),\n    var_weight = var(weight)\n  ) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Count case: basic distribution summary\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Count case: basic distribution summary\n\n|   n| zero_rate| mean_weight| var_weight|\n|---:|---------:|-----------:|----------:|\n| 320|     0.422|       1.384|      3.084|\n\n\n:::\n:::\n\n\n0 が多く、しかも `var_weight` が `mean_weight` を上回る場合、Poisson の「平均 = 分散」仮定が怪しくなります。これが Poisson から始めて、直後に過分散を確認する理由です。\n\n### Poisson から開始し、過分散を確認\n\nカウントデータの自然な出発点は **Poisson GLM** です。\nPoisson 分布は「平均 = 分散」を仮定しますが、\n実データではしばしば分散が平均を上回ります（**過分散**）。\nまず Poisson を当て、直後に過分散を検定します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nm_count_pois <- glm(\n  weight ~ same_class + prof_gap + baseline_mean_sc,\n  family = poisson(link = \"log\"),\n  data = dat_count\n)\n\nperformance::check_overdispersion(m_count_pois)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Overdispersion test\n\n       dispersion ratio =   1.871\n  Pearson's Chi-Squared = 591.091\n                p-value = < 0.001\n```\n\n\n:::\n:::\n\n\n**過分散テストの読み方**:\n\n- **dispersion ratio**: この値が 1 に近ければ Poisson の仮定は妥当。1 より大きく、かつ p 値が有意であれば過分散が存在します。\n- 過分散がある場合、Poisson の標準誤差は**過小評価**されます。その結果、本来有意でない効果が有意と判定されるリスク（偽陽性の膨張）が生じます。\n\n::: {.callout-warning}\n#### カウントデータでは過分散チェックを絶対に飛ばさない\n\nPoisson 回帰で過分散を無視すると、標準誤差が実際より小さく推定され、Type I エラー率が膨張します。\n過分散が検出された場合は、次のステップで負の二項（NB）モデルに切り替えます。\n:::\n\n### NB-GLM と NB-GAM\n\n過分散が検出されたため、**負の二項（Negative Binomial, NB）分布**に切り替えます。\nNB は Poisson に**分散パラメータ（theta）**を追加したモデルで、平均と分散を独立に扱えます。\n\n- `MASS::glm.nb()`: NB-GLM を当てはめます。内部で theta を推定します。\n- `mgcv::gam(..., family = nb())`: NB-GAM。連続予測子の非線形を許容します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nm_count_nb_glm <- MASS::glm.nb(\n  weight ~ same_class + prof_gap + baseline_mean_sc,\n  data = dat_count\n)\n\nm_count_nb_gam <- gam(\n  weight ~ same_class + prof_gap + s(baseline_mean_sc, k = 10),\n  family = nb(),\n  data = dat_count,\n  method = \"REML\",\n  select = TRUE\n)\n\nbroom::tidy(m_count_nb_glm, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Negative Binomial GLM\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Negative Binomial GLM\n\n|term             | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:----------------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept)      |    0.054|     0.132|     0.411|   0.681|   -0.206|     0.312|\n|same_class       |    0.695|     0.137|     5.062|   0.000|    0.428|     0.964|\n|prof_gap         |   -0.153|     0.108|    -1.415|   0.157|   -0.367|     0.060|\n|baseline_mean_sc |    0.333|     0.095|     3.490|   0.000|    0.140|     0.529|\n\n\n:::\n\n```{.r .cell-code}\nbroom::tidy(m_count_nb_gam, parametric = TRUE, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Negative Binomial GAM: Parametric Terms\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Negative Binomial GAM: Parametric Terms\n\n|term        | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:-----------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept) |    0.041|     0.133|     0.307|   0.759|   -0.220|     0.302|\n|same_class  |    0.708|     0.138|     5.130|   0.000|    0.438|     0.979|\n|prof_gap    |   -0.149|     0.108|    -1.378|   0.168|   -0.360|     0.063|\n\n\n:::\n:::\n\n\n#### NB-GLM 出力の読み方\n\nNB-GLM の `summary()` 出力は Poisson GLM と構造が似ていますが、以下の点が異なります。\n\n- **係数は log スケール**: Poisson と同様に、係数を指数変換すると **IRR（Incidence Rate Ratio）** になります。\n- **Theta**: 出力の末尾に表示される分散パラメータです。theta が小さいほど過分散が強いことを意味します。theta が十分大きい場合（例：数百以上）、NB は Poisson とほぼ同じ結果を返します。\n- **AIC**: Poisson と NB の AIC を比較することで、NB への切り替えが正当化されるかを確認できます（NB の AIC が小さければ、過分散を考慮する利得がある）。\n\n#### NB-GAM 出力の読み方\n\nGAM 固有の出力（edf, k-index）の読み方はケース1と同じです。\nedf ≈ 1 であれば、`baseline_mean_sc` の非線形効果は小さく、NB-GLM で十分です。\n\n### モデル比較と IRR\n\nPoisson, NB-GLM, NB-GAM の3モデルを情報量規準で比較します。\n通常、過分散がある場合は Poisson の AIC が最も大きく（最も悪い適合）なります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmake_ic_table(\n  \"Poisson GLM\" = m_count_pois,\n  \"NB GLM\" = m_count_nb_glm,\n  \"NB GAM\" = m_count_nb_gam\n) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 2: Model Comparison (Count)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 2: Model Comparison (Count)\n\n|model       |      AIC|      BIC| deviance|\n|:-----------|--------:|--------:|--------:|\n|NB GAM      | 1006.940| 1031.181|  334.303|\n|NB GLM      | 1007.430| 1026.272|  339.436|\n|Poisson GLM | 1079.225| 1094.299|  588.274|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::compare_performance(m_count_pois, m_count_nb_glm, m_count_nb_gam) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 2: compare_performance()\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 2: compare_performance()\n\n|Name           |Model  |      AIC| AIC_wt|     AICc| AICc_wt|      BIC| BIC_wt|  RMSE| Sigma| Score_log| Score_spherical| R2_Nagelkerke|    R2|\n|:--------------|:------|--------:|------:|--------:|-------:|--------:|------:|-----:|-----:|---------:|---------------:|-------------:|-----:|\n|m_count_pois   |glm    | 1079.225|  0.000| 1079.352|   0.000| 1094.299|  0.000| 1.657|     1|    -1.674|           0.049|         0.255|    NA|\n|m_count_nb_glm |negbin | 1007.430|  0.439| 1007.621|   0.453| 1026.272|  0.921| 1.661|     1|    -1.570|           0.049|         0.183|    NA|\n|m_count_nb_gam |gam    | 1006.940|  0.561| 1007.246|   0.547| 1031.181|  0.079| 1.651|     1|    -1.562|           0.048|            NA| 0.103|\n\n\n:::\n:::\n\n\n上の表で AIC が最も小さいモデルを確認します。\nPoisson → NB への改善が大きければ、過分散の扱いが重要であったことが裏付けられます。\nNB-GLM → NB-GAM の改善が小さければ、非線形の追加利得は限定的です。\n\n`sjPlot::tab_model()` で Poisson と NB-GLM の回帰テーブルを並べ、\n係数と標準誤差の違いを確認します。過分散を無視した Poisson の SE が NB より小さいことに注目してください。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsjPlot::tab_model(\n  m_count_pois,\n  m_count_nb_glm,\n  show.aic = TRUE,\n  show.se = TRUE,\n  dv.labels = c(\"Poisson GLM\", \"NB GLM\")\n)\n```\n\n::: {.cell-output-display}\n`````{=html}\n<table style=\"border-collapse:collapse; border:none;\">\n<tr>\n<th style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm;  text-align:left; \">&nbsp;</th>\n<th colspan=\"4\" style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm; \">Poisson GLM</th>\n<th colspan=\"4\" style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm; \">NB GLM</th>\n</tr>\n<tr>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  text-align:left; \">Predictors</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">Incidence Rate Ratios</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">std. Error</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">CI</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">p</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">Incidence Rate Ratios</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  col7\">std. Error</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  col8\">CI</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  col9\">p</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">(Intercept)</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.06</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.10</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.87&nbsp;&ndash;&nbsp;1.28</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.567</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.06</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.14</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">0.81&nbsp;&ndash;&nbsp;1.37</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\">0.681</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">same class</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.97</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.20</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.63&nbsp;&ndash;&nbsp;2.40</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>&lt;0.001</strong></td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">2.00</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.27</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">1.53&nbsp;&ndash;&nbsp;2.62</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>&lt;0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">prof gap</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.87</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.07</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.75&nbsp;&ndash;&nbsp;1.01</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.070</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.86</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.09</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">0.69&nbsp;&ndash;&nbsp;1.06</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\">0.157</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">baseline mean sc</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.33</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.09</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.17&nbsp;&ndash;&nbsp;1.52</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>&lt;0.001</strong></td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.39</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.13</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">1.15&nbsp;&ndash;&nbsp;1.70</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>&lt;0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm; border-top:1px solid;\">Observations</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left; border-top:1px solid;\" colspan=\"4\">320</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left; border-top:1px solid;\" colspan=\"4\">320</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm;\">R<sup>2</sup> Nagelkerke</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">0.255</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">0.183</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm;\">AIC</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">1079.225</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">1007.430</td>\n</tr>\n\n</table>\n\n`````\n:::\n:::\n\n\nカウントデータでは、係数を指数変換して **IRR（Incidence Rate Ratio: 発生率比）** として解釈します。\n\n- IRR = 1: 効果なし\n- IRR > 1: その予測子が増えると期待カウント（協働回数）が増加\n- IRR < 1: 期待カウントが減少\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbroom::tidy(m_count_nb_glm, conf.int = TRUE, exponentiate = TRUE) |>\n  rename(\n    Term = term,\n    IRR = estimate,\n    `2.5%` = conf.low,\n    `97.5%` = conf.high,\n    `p` = p.value\n  ) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 2: IRR (NB GLM)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 2: IRR (NB GLM)\n\n|Term             |   IRR| std.error| statistic|     p|  2.5%| 97.5%|\n|:----------------|-----:|---------:|---------:|-----:|-----:|-----:|\n|(Intercept)      | 1.056|     0.132|     0.411| 0.681| 0.814| 1.367|\n|same_class       | 2.003|     0.137|     5.062| 0.000| 1.534| 2.621|\n|prof_gap         | 0.858|     0.108|    -1.415| 0.157| 0.693| 1.062|\n|baseline_mean_sc | 1.395|     0.095|     3.490| 0.000| 1.150| 1.697|\n\n\n:::\n:::\n\n\n#### IRR テーブルの読み方（worked interpretation）\n\n- **same_class**: IRR が 1 より大きければ、同クラスのペアは異クラスに比べてその倍率だけ期待協働回数が多い。たとえば IRR = 3.0 なら「同クラスペアの期待協働回数は異クラスの 3 倍」です。\n- **prof_gap**: 習熟度差が 1 段階大きくなるごとに期待協働回数がその倍率で変化します。\n- **baseline_mean_sc**: ペアのベースライン平均得点が 1SD 高くなるごとの変化率です。\n\n#### IRR を論文で報告するときの書き方\n\n「同クラスペアは異クラスに比べ、期待協働回数が有意に高かった, β = [値], SE = [値], z = [値], p = [値], IRR = [値], 95% CI [[下限], [上限]]。」\n\nIRR の 95% CI が 1 をまたがなければ有意です。\n\n### 応答スケール予測（期待協働回数）\n\nIRR はカウントの比であり、「実際に何回の協働が期待されるか」は直接伝えません。\n応答スケール（期待カウント）での予測を可視化し、\nNB-GLM と NB-GAM の予測を比較します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnew_count <- tidyr::expand_grid(\n  same_class = c(0L, 1L),\n  prof_gap = 1,\n  baseline_mean_sc = seq(-2, 2, length.out = 120)\n)\n\n# NB-GLM: link スケールで SE を取得し、逆変換で CI を構築\npred_nb_link <- predict(m_count_nb_glm, newdata = new_count, type = \"link\", se.fit = TRUE)\nnew_count$mu_nb_glm <- exp(pred_nb_link$fit)\nnew_count$lo_nb_glm <- exp(pred_nb_link$fit - 1.96 * pred_nb_link$se.fit)\nnew_count$hi_nb_glm <- exp(pred_nb_link$fit + 1.96 * pred_nb_link$se.fit)\n\nnew_count$mu_nb_gam <- predict(m_count_nb_gam, newdata = new_count, type = \"response\")\n\nplot_count <- new_count |>\n  pivot_longer(\n    cols = c(mu_nb_glm, mu_nb_gam),\n    names_to = \"model\",\n    values_to = \"mu\"\n  ) |>\n  mutate(\n    model = dplyr::case_match(model, \"mu_nb_glm\" ~ \"NB-GLM\", \"mu_nb_gam\" ~ \"NB-GAM\"),\n    same_class = factor(same_class, levels = c(0, 1), labels = c(\"Different class\", \"Same class\"))\n  )\n\n# NB-GLM の CI リボン用データ\nribbon_count <- new_count |>\n  mutate(same_class_f = factor(same_class, levels = c(0, 1), labels = c(\"Different class\", \"Same class\")))\n\nggplot(plot_count, aes(x = baseline_mean_sc, y = mu, color = model, linetype = same_class)) +\n  geom_ribbon(data = ribbon_count,\n              aes(x = baseline_mean_sc, ymin = lo_nb_glm, ymax = hi_nb_glm,\n                  linetype = same_class_f),\n              fill = \"grey80\", alpha = 0.3, color = NA, inherit.aes = FALSE) +\n  geom_line(linewidth = 1) +\n  labs(\n    x = \"Mean Baseline Score of Dyad (z)\",\n    y = \"Expected Collaboration Count\",\n    title = \"Case 2: Response-Scale Prediction\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/pred-count-1.png){width=2100}\n:::\n:::\n\n\n**図の読み方**:\n\n- **y 軸**: 期待協働回数（0以上の連続値）。ベースライン平均得点が変化するにつれて、期待カウントがどう変わるかを示します。\n- **NB-GLM と NB-GAM の曲線が近い**: log スケールでの線形仮定が妥当。GLM で十分。\n- **NB-GAM に屈曲がある**: 非線形が存在し、GAM が有用。\n- **同クラス vs 異クラスの間隔**: `same_class` 効果の実質的な大きさを件数スケールで示します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsjPlot::plot_model(\n  m_count_nb_glm,\n  type = \"pred\",\n  terms = \"baseline_mean_sc\",\n  show.data = FALSE\n) +\n  labs(\n    x = \"Mean baseline score of dyad (z)\",\n    y = \"Expected collaboration count\"\n  ) +\n  theme_bw(base_size = 12, base_family = \"HiraginoSans-W3\") +\n  theme(\n    plot.title.position = \"plot\",\n    plot.title = element_text(\n      face = \"bold\",\n      size = rel(1.0),\n      lineheight = 1.04,\n      margin = margin(b = 6)\n    ),\n    plot.subtitle = element_text(\n      size = rel(0.92),\n      lineheight = 1.04,\n      margin = margin(b = 8)\n    ),\n    plot.margin = margin(t = 10, r = 14, b = 10, l = 10)\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/sjplot-count-pred-1.png){width=2100}\n:::\n:::\n\n\n### 下位検定（Case 2: same_class）\n\n`same_class` は 0/1 の二水準変数ですが、`emmeans` で応答スケール（期待カウント）を算出し、\n差の検定を明示的に行うと報告が明確になります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nemm_count_same <- emmeans(\n  m_count_nb_glm,\n  ~ same_class,\n  at = list(same_class = c(0, 1)),\n  type = \"response\"\n)\n\nas.data.frame(emm_count_same) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 2: EMMs for same_class (response scale)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 2: EMMs for same_class (response scale)\n\n| same_class| response|    SE|  df| asymp.LCL| asymp.UCL|\n|----------:|--------:|-----:|---:|---------:|---------:|\n|          0|    0.938| 0.092| Inf|     0.774|     1.136|\n|          1|    1.878| 0.180| Inf|     1.556|     2.267|\n\n\n:::\n\n```{.r .cell-code}\npairs(emm_count_same) |>\n  as.data.frame() |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 2: Pairwise Contrast for same_class\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 2: Pairwise Contrast for same_class\n\n|contrast                  | ratio|    SE|  df| null| z.ratio| p.value|\n|:-------------------------|-----:|-----:|---:|----:|-------:|-------:|\n|same_class0 / same_class1 | 0.499| 0.069| Inf|    1|  -5.062|       0|\n\n\n:::\n:::\n\n\nEMMs テーブルは、同クラス / 異クラスのそれぞれの期待協働回数を示します。\nPairwise テーブルの ratio は比率スケールでの比較（IRR に相当）です。\np 値が有意であれば、同クラスと異クラスの期待カウントに統計的な差があります。\n\n### 診断\n\nケース1と同じ構造で診断しますが、**カウントデータでは過分散のチェックが特に重要**です。Poisson モデルは平均 = 分散を仮定するため、実データの分散が平均を大きく超える場合（過分散）、標準誤差が過小推定され、偽陽性のリスクが高まります。\n\n::: {.panel-tabset}\n\n#### GAM 診断\n\n**なぜ必要か**: Case 1 と同様、基底次元 `k` の妥当性と残差パターンを確認します。カウントデータの場合、QQ プロットが離散的（階段状）になるのは正常です。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngam.check(m_count_nb_gam)\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-gamcheck-1.png){width=2100}\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nMethod: REML   Optimizer: outer newton\nfull convergence after 3 iterations.\nGradient range [-0.0004759757,7.044024e-05]\n(score 504.0342 & scale 1).\nHessian positive definite, eigenvalue range [0.0001222163,25.61117].\nModel rank =  12 / 12 \n\nBasis dimension (k) checking results. Low p-value (k-index<1) may\nindicate that k is too low, especially if edf is close to k'.\n\n                      k'  edf k-index p-value\ns(baseline_mean_sc) 9.00 1.88    0.89    0.46\n```\n\n\n:::\n\n```{.r .cell-code}\nconcurvity(m_count_nb_gam, full = FALSE)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$worst\n                            para s(baseline_mean_sc)\npara                1.000000e+00        3.624096e-25\ns(baseline_mean_sc) 3.608787e-25        1.000000e+00\n\n$observed\n                            para s(baseline_mean_sc)\npara                1.000000e+00        8.464499e-30\ns(baseline_mean_sc) 3.608787e-25        1.000000e+00\n\n$estimate\n                            para s(baseline_mean_sc)\npara                1.000000e+00        1.698941e-27\ns(baseline_mean_sc) 3.608787e-25        1.000000e+00\n```\n\n\n:::\n:::\n\n\n**読み方**: Case 1 の4プロットと同じ基準です。k-index ≈ 1.0 なら十分、concurvity の worst > 0.8 の組み合わせがあれば平滑項の統合を検討してください。\n\n#### 過分散（Poisson vs NB）\n\n**なぜ必要か**: 過分散を無視すると p 値が楽観的になり、効果がないのに有意と判断するリスクが高まります。NB に切り替えた後にも確認することで、NB が過分散を吸収できたかを検証します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::check_overdispersion(m_count_pois)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Overdispersion test\n\n       dispersion ratio =   1.871\n  Pearson's Chi-Squared = 591.091\n                p-value = < 0.001\n```\n\n\n:::\n\n```{.r .cell-code}\nperformance::check_overdispersion(m_count_nb_glm)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Overdispersion test\n\n dispersion ratio = 0.927\n          p-value = 0.728\n```\n\n\n:::\n:::\n\n\nPoisson の dispersion ratio が大きく有意である一方、\nNB の dispersion ratio が 1 に近ければ、NB への切り替えが過分散を適切に吸収したことを意味します。\n\n#### 共線性・総合診断\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::check_collinearity(m_count_nb_glm)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Check for Multicollinearity\n\nLow Correlation\n\n             Term  VIF    VIF 95% CI adj. VIF Tolerance Tolerance 95% CI\n       same_class 1.03 [1.00,  2.55]     1.01      0.97     [0.39, 1.00]\n         prof_gap 1.03 [1.00,  2.22]     1.02      0.97     [0.45, 1.00]\n baseline_mean_sc 1.01 [1.00, 55.28]     1.01      0.99     [0.02, 1.00]\n```\n\n\n:::\n\n```{.r .cell-code}\nplot_check_model_separate(m_count_pois, model_name = \"m_count_pois\", checks = c(\"pp_check\", \"outliers\", \"vif\"))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-1.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-2.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-3.png){width=3000}\n:::\n\n```{.r .cell-code}\nplot_check_model_separate(m_count_nb_glm, model_name = \"m_count_nb_glm\", checks = c(\"pp_check\", \"outliers\", \"vif\"))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-4.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-5.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-6.png){width=3000}\n:::\n\n```{.r .cell-code}\nplot_check_model_separate(m_count_nb_gam, model_name = \"m_count_nb_gam\", checks = c(\"pp_check\", \"outliers\", \"vif\"))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-count-checkmodel-7.png){width=3000}\n:::\n:::\n\n\nPoisson, NB-GLM, NB-GAM の `check_model()` を並べることで、\n分布の選択による診断の改善を視覚的に確認できます。\n特に Posterior predictive check で、NB モデルが Poisson より実データに近い生成データを作っていれば、\nNB の選択は妥当です。\n\n:::\n\n::: {.callout-note}\n#### 注意点\n\n- 過分散がある場合、Poisson をそのまま使うと標準誤差が過小評価されやすい。\n- NB を基準にし、非線形が必要なときだけ NB-GAM へ拡張する。\n- 0 が極端に多い場合（ゼロ過剰）は `zeroinfl()` や `hurdle()` も検討候補。\n:::\n\n------------------------------------------------------------------------\n\nケース2では**カウントデータ**を Poisson → 負の二項分布で分析しました。最後のケース3では**正の連続値**（協働強度）を扱います。正規分布（OLS）では右に歪んだ正値データをうまくモデル化できないため、**Gamma 分布** + log リンクを使います。「なぜ対数変換して OLS では不適切なのか」も含めて解説します。\n\n## ケース3: 協働強度（Positive Continuous: Gamma GLM/GAM）\n\n**目的**: 学習者ごとの協働強度を、\nベースライン得点・クラス・習熟度で説明します。\n\nここでの観測単位は **1 learner = 1 row** です。\n\n#### SLA でこのケースに当てはまるデータ\n\n正の連続値データは SLA で頻出します：\n\n- **発話速度**（語数/分）: 0 にはならず、右に歪む（多くは中程度だが、一部の流暢な学習者が非常に速い）\n- **反応時間**（ミリ秒）: 必ず正で、右裾が長い（ほとんどは速いが、まれに非常に遅い反応がある）[@lo2015]\n- **ライティングの語数**: 正の連続値（0語は通常ありえない）で、課題により大きくばらつく\n\nいずれも OLS（正規分布仮定）では**負の予測値**が生じたり、**分散の不等性**に対処できなかったりする問題があります。Gamma GLM はこれらを自然に扱えます。\n\n#### なぜ対数変換 + OLS ではなく Gamma GLM を使うのか\n\n正に歪んだ連続データに対して、伝統的には `log(y)` に変換してから `lm()` を使う方法がとられてきました。しかし、この方法には重要な問題があります：\n\n1. **対数変換は「log(y) の平均」をモデル化する**のに対し、Gamma GLM は「**y の平均の対数**」をモデル化します。この2つは異なります（イェンセンの不等式）。つまり、対数変換 + OLS の予測値を元のスケールに戻すと、**体系的に偏った推定値**になります。\n2. **ゼロ値が含まれると対数変換できません**（log(0) = −∞）。Gamma GLM もゼロは扱えませんが、この問題が明示的になるため対処しやすくなります。\n3. **信頼区間の計算**が Gamma GLM の方が適切です。対数変換 + OLS の CI を元に戻すと非対称性が過小評価されます。\n\n### GLM/GAM の当てはめ\n\n正の連続量（協働強度）には **Gamma 分布 + log リンク** が自然な選択です。\n\n- **Gamma 分布**: 正の値のみをとり、右裾が長い分布に適合します。分散が平均に比例して大きくなるデータ（分散が一定でない場合）に向いています。\n- **log リンク**: 予測値が常に正になることを保証し、係数を**乗法的**（何倍か）に解釈できます。\n\n比較のために、通常の線形回帰（OLS: 正規分布仮定）も当てはめます。\nOLS は「加法的効果」（何単位増えるか）、Gamma GLM は「乗法的効果」（何倍になるか）を推定する点が根本的に異なります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nm_pos_lm <- lm(\n  collab_strength ~ baseline_score_sc + class + proficiency,\n  data = dat_pos\n)\n\nm_pos_gamma_glm <- glm(\n  collab_strength ~ baseline_score_sc + class + proficiency,\n  family = Gamma(link = \"log\"),\n  data = dat_pos\n)\n\nm_pos_gamma_gam <- gam(\n  collab_strength ~ s(baseline_score_sc, k = 8) + class + proficiency,\n  family = Gamma(link = \"log\"),\n  data = dat_pos,\n  method = \"REML\",\n  select = TRUE\n)\n\nbroom::tidy(m_pos_lm, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"OLS Linear Model\")\n```\n\n::: {.cell-output-display}\n\n\nTable: OLS Linear Model\n\n|term              | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:-----------------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept)       |    3.978|     0.352|    11.303|   0.000|    3.284|     4.671|\n|baseline_score_sc |    1.118|     0.172|     6.512|   0.000|    0.780|     1.456|\n|classB            |    0.564|     0.353|     1.598|   0.111|   -0.131|     1.260|\n|classC            |   -0.879|     0.346|    -2.539|   0.012|   -1.561|    -0.197|\n|proficiencyB1     |    0.475|     0.361|     1.316|   0.189|   -0.236|     1.187|\n|proficiencyB2     |    2.366|     0.483|     4.901|   0.000|    1.415|     3.317|\n\n\n:::\n\n```{.r .cell-code}\nbroom::tidy(m_pos_gamma_glm, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Gamma GLM (log link)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Gamma GLM (log link)\n\n|term              | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:-----------------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept)       |    1.310|     0.070|    18.635|   0.000|    1.172|     1.451|\n|baseline_score_sc |    0.277|     0.034|     8.091|   0.000|    0.207|     0.348|\n|classB            |    0.114|     0.071|     1.612|   0.108|   -0.024|     0.253|\n|classC            |   -0.223|     0.069|    -3.220|   0.001|   -0.357|    -0.088|\n|proficiencyB1     |    0.156|     0.072|     2.158|   0.032|    0.013|     0.297|\n|proficiencyB2     |    0.445|     0.096|     4.617|   0.000|    0.256|     0.636|\n\n\n:::\n\n```{.r .cell-code}\nbroom::tidy(m_pos_gamma_gam, parametric = TRUE, conf.int = TRUE) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Gamma GAM: Parametric Terms\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Gamma GAM: Parametric Terms\n\n|term          | estimate| std.error| statistic| p.value| conf.low| conf.high|\n|:-------------|--------:|---------:|---------:|-------:|--------:|---------:|\n|(Intercept)   |    1.323|     0.069|    19.098|   0.000|    1.188|     1.459|\n|classB        |    0.109|     0.070|     1.569|   0.118|   -0.027|     0.246|\n|classC        |   -0.231|     0.068|    -3.418|   0.001|   -0.364|    -0.099|\n|proficiencyB1 |    0.132|     0.071|     1.851|   0.065|   -0.008|     0.272|\n|proficiencyB2 |    0.441|     0.095|     4.651|   0.000|    0.255|     0.626|\n\n\n:::\n:::\n\n\n#### OLS vs Gamma GLM の出力比較\n\nOLS（`m_pos_lm`）と Gamma GLM（`m_pos_gamma_glm`）の係数が異なるのは当然です。\n\n- **OLS の係数**: 「ベースライン得点が 1SD 上がると、協働強度が β 単位増える」（加法）\n- **Gamma GLM の係数（log スケール）**: 「ベースライン得点が 1SD 上がると、協働強度が exp(β) 倍になる」（乗法）\n\nどちらが適切かは、データの性質に依存します。\n分散が平均に比例する（得点が高い人ほどばらつきも大きい）場合は Gamma が適しています。\nモデル比較（AIC/BIC）と `check_model()` の診断で判断します。\n\n::: {.callout-note}\n#### Gamma GLM の注意点\n\nGamma 分布は**厳密に正の値**のみをモデル化できます。\n`collab_strength` に 0 が含まれる場合、Gamma GLM はエラーを返すか、不安定な推定になります。\n0 を含むデータでは、小さな定数（例: 0.001）を加えるか、Tweedie 分布を検討してください。\n:::\n\n### モデル比較と倍率解釈\n\nOLS, Gamma GLM, Gamma GAM の3モデルを比較します。\n\n::: {.callout-important}\n**AIC の直接比較に注意**: R の `AIC()` は Gaussian（OLS）と Gamma GLM で異なる尤度スケールを使うため、**数値の直接比較は厳密には不適切**です。以下の表は参考値として扱い、**残差診断**（Q-Q プロット、DHARMa）と**理論的整合性**（データが非負で右に歪んでいるなら Gamma が自然）を主な判断基準としてください。\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmake_ic_table(\n  \"LM (Gaussian)\" = m_pos_lm,\n  \"Gamma GLM\" = m_pos_gamma_glm,\n  \"Gamma GAM\" = m_pos_gamma_gam\n) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: Model Comparison (Positive Continuous)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: Model Comparison (Positive Continuous)\n\n|model         |      AIC|      BIC| deviance|\n|:-------------|--------:|--------:|--------:|\n|Gamma GAM     | 1042.723| 1077.426|   51.219|\n|Gamma GLM     | 1047.265| 1072.189|   53.165|\n|LM (Gaussian) | 1178.541| 1203.466| 1341.857|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::compare_performance(m_pos_lm, m_pos_gamma_glm, m_pos_gamma_gam) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: compare_performance()\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: compare_performance()\n\n|Name            |Model |      AIC| AIC_wt|     AICc| AICc_wt|      BIC| BIC_wt|  RMSE| Sigma|    R2| R2_adjusted| R2_Nagelkerke|\n|:---------------|:-----|--------:|------:|--------:|-------:|--------:|------:|-----:|-----:|-----:|-----------:|-------------:|\n|m_pos_lm        |lm    | 1178.541|  0.000| 1178.986|   0.000| 1203.466|  0.000| 2.272| 2.298| 0.399|       0.387|            NA|\n|m_pos_gamma_glm |glm   | 1047.265|  0.094| 1047.709|   0.113| 1072.189|  0.933| 2.289| 0.459|    NA|          NA|         0.492|\n|m_pos_gamma_gam |gam   | 1042.740|  0.906| 1043.580|   0.887| 1077.443|  0.067| 2.228| 0.449| 0.406|          NA|            NA|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsjPlot::tab_model(\n  m_pos_lm,\n  m_pos_gamma_glm,\n  show.aic = TRUE,\n  show.se = TRUE,\n  dv.labels = c(\"LM\", \"Gamma GLM\")\n)\n```\n\n::: {.cell-output-display}\n`````{=html}\n<table style=\"border-collapse:collapse; border:none;\">\n<tr>\n<th style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm;  text-align:left; \">&nbsp;</th>\n<th colspan=\"4\" style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm; \">LM</th>\n<th colspan=\"4\" style=\"border-top: double; text-align:center; font-style:normal; font-weight:bold; padding:0.2cm; \">Gamma GLM</th>\n</tr>\n<tr>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  text-align:left; \">Predictors</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">Estimates</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">std. Error</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">CI</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">p</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  \">Estimates</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  col7\">std. Error</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  col8\">CI</td>\n<td style=\" text-align:center; border-bottom:1px solid; font-style:italic; font-weight:normal;  col9\">p</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">(Intercept)</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">3.98</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.35</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">3.28&nbsp;&ndash;&nbsp;4.67</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>&lt;0.001</strong></td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">3.71</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.26</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">3.23&nbsp;&ndash;&nbsp;4.27</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>&lt;0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">baseline score sc</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.12</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.17</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.78&nbsp;&ndash;&nbsp;1.46</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>&lt;0.001</strong></td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.32</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.05</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">1.23&nbsp;&ndash;&nbsp;1.42</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>&lt;0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">class [B]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.56</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.35</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">&#45;0.13&nbsp;&ndash;&nbsp;1.26</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.111</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.12</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.08</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">0.98&nbsp;&ndash;&nbsp;1.29</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\">0.107</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">class [C]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">&#45;0.88</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.35</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">&#45;1.56&nbsp;&ndash;&nbsp;-0.20</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>0.012</strong></td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.80</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.06</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">0.70&nbsp;&ndash;&nbsp;0.92</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">proficiency [B1]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.48</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.36</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">&#45;0.24&nbsp;&ndash;&nbsp;1.19</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.189</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.17</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.08</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">1.01&nbsp;&ndash;&nbsp;1.35</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>0.031</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; \">proficiency [B2]</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">2.37</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">0.48</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.42&nbsp;&ndash;&nbsp;3.32</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \"><strong>&lt;0.001</strong></td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  \">1.56</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col7\">0.15</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col8\">1.29&nbsp;&ndash;&nbsp;1.89</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:center;  col9\"><strong>&lt;0.001</strong></td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm; border-top:1px solid;\">Observations</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left; border-top:1px solid;\" colspan=\"4\">260</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left; border-top:1px solid;\" colspan=\"4\">260</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm;\">R<sup>2</sup> / R<sup>2</sup> adjusted</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">0.399 / 0.387</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">0.492</td>\n</tr>\n<tr>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; text-align:left; padding-top:0.1cm; padding-bottom:0.1cm;\">AIC</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">1178.541</td>\n<td style=\" padding:0.2cm; text-align:left; vertical-align:top; padding-top:0.1cm; padding-bottom:0.1cm; text-align:left;\" colspan=\"4\">1047.265</td>\n</tr>\n\n</table>\n\n`````\n:::\n:::\n\n\nGamma GLM の係数を指数変換して**倍率（Multiplier）**として解釈します。\n\n- Multiplier = 1: 効果なし\n- Multiplier > 1: その予測子が増えると期待協働強度がその倍率で増加\n- Multiplier < 1: 期待協働強度が減少（例: 0.8 なら 20% 減少）\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbroom::tidy(m_pos_gamma_glm, conf.int = TRUE, exponentiate = TRUE) |>\n  rename(\n    Term = term,\n    Multiplier = estimate,\n    `2.5%` = conf.low,\n    `97.5%` = conf.high,\n    `p` = p.value\n  ) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: Multiplicative Effects (Gamma GLM)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: Multiplicative Effects (Gamma GLM)\n\n|Term              | Multiplier| std.error| statistic|     p|  2.5%| 97.5%|\n|:-----------------|----------:|---------:|---------:|-----:|-----:|-----:|\n|(Intercept)       |      3.706|     0.070|    18.635| 0.000| 3.229| 4.269|\n|baseline_score_sc |      1.320|     0.034|     8.091| 0.000| 1.230| 1.416|\n|classB            |      1.120|     0.071|     1.612| 0.108| 0.976| 1.287|\n|classC            |      0.800|     0.069|    -3.220| 0.001| 0.700| 0.916|\n|proficiencyB1     |      1.168|     0.072|     2.158| 0.032| 1.013| 1.346|\n|proficiencyB2     |      1.561|     0.096|     4.617| 0.000| 1.291| 1.888|\n\n\n:::\n:::\n\n\n#### 倍率テーブルの読み方（worked interpretation）\n\n- **baseline_score_sc**: ベースライン得点が 1SD 高くなるごとに協働強度がその倍率で変化します。たとえば Multiplier = 1.25 なら「1SD の得点上昇で協働強度は 25% 増加する」と解釈します。\n- **class**: 参照カテゴリ（例: class A）と比較した各クラスの倍率です。Multiplier = 0.7 なら「そのクラスは class A の 70%（30% 低い）」。\n- **proficiency**: 参照水準と比較した各習熟度レベルの倍率です。\n\n::: {.callout-tip}\n#### 倍率を論文で報告するときの書き方\n\n「Gamma GLM（log リンク）において、ベースライン得点の 1SD 増加は協働強度の有意な増加と関連していた, β = [値], SE = [値], z = [値], p = [値], exp(β) = [値], 95% CI [[下限], [上限]]。これは [値]% の増加に相当する。」\n:::\n\n### 応答スケール予測（期待協働強度）\n\n倍率は相対的な指標であり、「実際にどの程度の協働強度が期待されるか」は直接読めません。\n応答スケール（期待値）で予測を可視化し、クラス間・モデル間の違いを確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnew_pos <- tidyr::expand_grid(\n  class = factor(c(\"A\", \"B\", \"C\"), levels = levels(dat_pos$class)),\n  proficiency = factor(\"B1\", levels = levels(dat_pos$proficiency)),\n  baseline_score_sc = seq(min(dat_pos$baseline_score_sc), max(dat_pos$baseline_score_sc), length.out = 120)\n)\n\n# Gamma GLM: link スケールで SE → 逆変換で CI\npred_gamma_link <- predict(m_pos_gamma_glm, newdata = new_pos, type = \"link\", se.fit = TRUE)\nnew_pos$mu_gamma_glm <- exp(pred_gamma_link$fit)\nnew_pos$lo_gamma_glm <- exp(pred_gamma_link$fit - 1.96 * pred_gamma_link$se.fit)\nnew_pos$hi_gamma_glm <- exp(pred_gamma_link$fit + 1.96 * pred_gamma_link$se.fit)\n\nnew_pos$mu_gamma_gam <- predict(m_pos_gamma_gam, newdata = new_pos, type = \"response\")\n\nplot_pos <- new_pos |>\n  pivot_longer(\n    cols = c(mu_gamma_glm, mu_gamma_gam),\n    names_to = \"model\",\n    values_to = \"mu\"\n  ) |>\n  mutate(model = dplyr::case_match(model, \"mu_gamma_glm\" ~ \"Gamma-GLM\", \"mu_gamma_gam\" ~ \"Gamma-GAM\"))\n\nggplot(plot_pos, aes(x = baseline_score_sc, y = mu, color = model, linetype = class)) +\n  geom_ribbon(data = new_pos,\n              aes(x = baseline_score_sc, ymin = lo_gamma_glm, ymax = hi_gamma_glm,\n                  linetype = class),\n              fill = \"grey80\", alpha = 0.3, color = NA, inherit.aes = FALSE) +\n  geom_line(linewidth = 1) +\n  labs(\n    x = \"Baseline Score (z)\",\n    y = \"Expected Collaboration Strength\",\n    title = \"Case 3: Response-Scale Prediction with 95% CI (Gamma GLM)\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/pred-positive-1.png){width=2100}\n:::\n:::\n\n\n**図の読み方**:\n\n- **y 軸**: 期待協働強度（正の連続値）。\n- **Gamma-GLM と Gamma-GAM の曲線が近い**: log スケールでの線形仮定が妥当。GAM は不要。\n- **クラス間の曲線間隔**: クラスの違いが協働強度にどの程度影響しているかを示します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsjPlot::plot_model(\n  m_pos_gamma_glm,\n  type = \"pred\",\n  terms = c(\"baseline_score_sc\", \"class\"),\n  show.data = FALSE\n) +\n  labs(\n    x = \"Baseline score (z)\",\n    y = \"Predicted collaboration strength\",\n    color = \"Class\",\n    fill = \"Class\"\n  ) +\n  theme_bw(base_size = 12, base_family = \"HiraginoSans-W3\") +\n  theme(\n    plot.title.position = \"plot\",\n    plot.title = element_text(\n      face = \"bold\",\n      size = rel(1.0),\n      lineheight = 1.04,\n      margin = margin(b = 6)\n    ),\n    plot.subtitle = element_text(\n      size = rel(0.92),\n      lineheight = 1.04,\n      margin = margin(b = 8)\n    ),\n    plot.margin = margin(t = 10, r = 14, b = 10, l = 10)\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/sjplot-positive-pred-1.png){width=2100}\n:::\n:::\n\n\n### 下位検定（Case 3: class / proficiency）\n\nclass（3水準）と proficiency（3水準）について、\n各水準の期待協働強度（応答スケール）とペア間の差を `emmeans` で算出します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nemm_class_pos <- emmeans(m_pos_gamma_glm, ~ class, type = \"response\")\nemm_prof_pos <- emmeans(m_pos_gamma_glm, ~ proficiency, type = \"response\")\n\nas.data.frame(emm_class_pos) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: EMMs for Class (response scale)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: EMMs for Class (response scale)\n\n|class | response|    SE|  df| lower.CL| upper.CL|\n|:-----|--------:|-----:|---:|--------:|--------:|\n|A     |    4.528| 0.215| 254|    4.124|    4.972|\n|B     |    5.074| 0.275| 254|    4.561|    5.645|\n|C     |    3.624| 0.187| 254|    3.275|    4.011|\n\n\n:::\n\n```{.r .cell-code}\npairs(emm_class_pos, adjust = \"holm\") |>\n  as.data.frame() |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: Pairwise (Class, Holm)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: Pairwise (Class, Holm)\n\n|contrast | ratio|    SE|  df| null| t.ratio| p.value|\n|:--------|-----:|-----:|---:|----:|-------:|-------:|\n|A / B    | 0.892| 0.063| 254|    1|  -1.612|   0.108|\n|A / C    | 1.249| 0.086| 254|    1|   3.220|   0.003|\n|B / C    | 1.400| 0.103| 254|    1|   4.582|   0.000|\n\n\n:::\n\n```{.r .cell-code}\nas.data.frame(emm_prof_pos) |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: EMMs for Proficiency (response scale)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: EMMs for Proficiency (response scale)\n\n|proficiency | response|    SE|  df| lower.CL| upper.CL|\n|:-----------|--------:|-----:|---:|--------:|--------:|\n|A2          |    3.574| 0.212| 254|    3.180|    4.017|\n|B1          |    4.176| 0.169| 254|    3.857|    4.522|\n|B2          |    5.579| 0.373| 254|    4.891|    6.365|\n\n\n:::\n\n```{.r .cell-code}\npairs(emm_prof_pos, adjust = \"holm\") |>\n  as.data.frame() |>\n    mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Case 3: Pairwise (Proficiency, Holm)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Case 3: Pairwise (Proficiency, Holm)\n\n|contrast | ratio|    SE|  df| null| t.ratio| p.value|\n|:--------|-----:|-----:|---:|----:|-------:|-------:|\n|A2 / B1  | 0.856| 0.062| 254|    1|  -2.158|   0.032|\n|A2 / B2  | 0.641| 0.062| 254|    1|  -4.617|   0.000|\n|B1 / B2  | 0.749| 0.058| 254|    1|  -3.744|   0.000|\n\n\n:::\n:::\n\n\nEMMs テーブルの ratio 列が 1 からどれだけ離れているかが、群間差の実質的な大きさを示します。\nHolm 補正後の p 値で有意性を判断してください。\n\n### 診断\n\n連続正値データでは、**分布の選択が妥当か**（OLS の正規分布仮定 vs Gamma の右裾分布）と **リンク関数上での線形性** を特に確認します。\n\n::: {.panel-tabset}\n\n#### GAM 診断\n\n**なぜ必要か**: Gamma 分布は右裾が長いデータに適しますが、平滑項の基底次元が不足していると非線形パターンを見逃します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngam.check(m_pos_gamma_gam)\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-gamcheck-1.png){width=2100}\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nMethod: REML   Optimizer: outer newton\nfull convergence after 7 iterations.\nGradient range [-1.404692e-08,4.60108e-09]\n(score 526.7195 & scale 0.2015375).\nHessian positive definite, eigenvalue range [0.251788,135.965].\nModel rank =  12 / 12 \n\nBasis dimension (k) checking results. Low p-value (k-index<1) may\nindicate that k is too low, especially if edf is close to k'.\n\n                       k'  edf k-index p-value\ns(baseline_score_sc) 7.00 2.91    1.05    0.84\n```\n\n\n:::\n\n```{.r .cell-code}\nconcurvity(m_pos_gamma_gam, full = FALSE)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$worst\n                             para s(baseline_score_sc)\npara                 1.000000e+00         3.143911e-27\ns(baseline_score_sc) 3.351726e-27         1.000000e+00\n\n$observed\n                             para s(baseline_score_sc)\npara                 1.000000e+00         1.628586e-31\ns(baseline_score_sc) 3.351726e-27         1.000000e+00\n\n$estimate\n                             para s(baseline_score_sc)\npara                 1.000000e+00         2.887169e-29\ns(baseline_score_sc) 3.351726e-27         1.000000e+00\n```\n\n\n:::\n:::\n\n\n**読み方**: 4プロットの基準は Case 1 と同じ。Gamma モデルでは QQ プロットの右裾に軽い逸脱があっても許容されます（Gamma 分布自体が右裾を持つため）。k-index ≈ 1.0 なら十分です。\n\n#### 共線性\n\n**なぜ必要か**: 複数の予測子（class, proficiency, baseline_score_sc）が互いに相関している場合、係数の推定が不安定になります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nperformance::check_collinearity(m_pos_gamma_glm)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# Check for Multicollinearity\n\nLow Correlation\n\n              Term  VIF   VIF 95% CI adj. VIF Tolerance Tolerance 95% CI\n baseline_score_sc 1.44 [1.27, 1.73]     1.20      0.69     [0.58, 0.79]\n             class 1.05 [1.00, 1.69]     1.01      0.95     [0.59, 1.00]\n       proficiency 1.41 [1.24, 1.69]     1.09      0.71     [0.59, 0.81]\n```\n\n\n:::\n:::\n\n\n**読み方**: VIF < 3（安全）、3-5（注意）、> 5（懸念）。class と proficiency が相関している場合でも VIF < 5 であれば実用上の問題はありません。\n\n#### 総合診断（OLS vs Gamma）\n\n**なぜ必要か**: OLS（正規分布）と Gamma GLM のどちらの分布仮定がデータに適合しているかを視覚的に比較します。`check_model()` の Posterior Predictive Check パネルで、生成データが実データに近いモデルが優れています。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot_check_model_separate(m_pos_lm, model_name = \"m_pos_lm\", checks = c(\"qq\", \"linearity\", \"homogeneity\", \"outliers\", \"vif\"))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-1.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-2.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-3.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-4.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-5.png){width=3000}\n:::\n\n```{.r .cell-code}\nplot_check_model_separate(m_pos_gamma_glm, model_name = \"m_pos_gamma_glm\", checks = c(\"pp_check\", \"outliers\", \"vif\"))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-6.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-7.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-8.png){width=3000}\n:::\n\n```{.r .cell-code}\nplot_check_model_separate(m_pos_gamma_gam, model_name = \"m_pos_gamma_gam\", checks = c(\"pp_check\", \"outliers\", \"vif\"))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-9.png){width=3000}\n:::\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/diag-positive-checkmodel-10.png){width=3000}\n:::\n:::\n\n\n特に注目すべき点:\n\n- **OLS の残差プロット**: 残差が漏斗状に広がっていれば、分散が一定でない（不等分散）ことを示し、Gamma への切り替えを支持します。\n- **Gamma の Posterior predictive check**: 生成データが実データの右裾の分布を再現できていれば、Gamma の仮定は妥当です。\n\n:::\n\n::: {.callout-note}\n#### 注意点\n\n- Gamma(link = log) では、係数の指数変換が「何倍か」を意味する。\n- 非線形が弱い場合は GLM で十分。GAM は必要最小限で使う。\n:::\n\n## 第6章への接続: モデル比較とランダム効果選択（最短実装）\n\n::: {.callout-important}\n### この節は分析パイプラインで最も重要な判断を含む\n\n第5章で GLM/GAM の適合と分布選択が決まった後、**データ構造が独立性を崩しているか**を判断します。\nたとえば、同じ学習者が複数作文を書いたり、同じ項目に複数回答したり、同じ話者が複数試行を出したりすると、観測は独立ではなくなります。\nこの依存構造を無視したまま GLM の結果を報告すると、標準誤差が過小になり偽陽性が増えます。\n:::\n\n5章で GLM/GAM の判断ができたら、次に確認すべきは\n「観測の独立性が崩れていないか」です。学習者・項目の反復がある場合は、\nGLMM を候補に含めて比較します[@jaeger2008; @baayen2008; @barr2013; @meteyard2020]。\n\n実務では次の順序が安定です。\n\n1. まず GLM（固定効果のみ）を作る。\n2. ランダム切片（例: `from` / `to`）を追加した GLMM を作る。\n3. 必要ならランダム傾きを試す。\n4. `compare_performance()`、`anova(..., test=\"Chisq\")`、`isSingular()` を併用して採否を決める。\n\n以下の表は、GLM から GLMM への拡張を検討する際の判断ガイドです。具体的な実装は第6章で詳しく扱います。\n\n| ステップ | モデル | R コード（第6章で実行） | 判断基準 |\n|---|---|---|---|\n| 0 | GLM（固定効果のみ） | `glm(y ~ x, family = binomial)` | ベースライン |\n| 1 | + 送信者ランダム切片 | `glmer(y ~ x + (1\\|from), ...)` | AIC が GLM より 4+ 低ければ有意義 |\n| 2 | + 受信者ランダム切片 | `glmer(y ~ x + (1\\|from) + (1\\|to), ...)` | LRT で Step 1 と比較 |\n| 3 | + ランダム傾き | `glmer(y ~ x + (1+x1\\|from) + (1\\|to), ...)` | `isSingular() = FALSE` を確認 |\n\n**判断の3点セット**:\n\n1. **AIC/BIC**: GLM より GLMM の AIC が 4 以上小さければ、ランダム効果が重要\n2. **LRT（`anova()`）**: 入れ子モデル間で有意（p < .05）なら、追加したランダム効果に意味がある\n3. **`isSingular()`**: `TRUE` ならランダム効果が過剰。シンプルなモデルに戻す\n\n具体的なコードと出力の解釈は第6章で詳しく扱います。\n\n### 第5章での判断を第6章へつなぐポイント\n\n- 非線形が主課題なら GAM、依存構造が主課題なら GLMM。\n- どちらも疑われる場合は、まず GLMM で依存構造を処理してから GAMM を検討する。\n- モデル選択は単一指標ではなく、適合指標・特異適合・理論整合性の3点で判断する。\n\n## 3ケースをどう報告するか（実践テンプレート）\n\n### APA 7 の表記ルール（共通）\n\n- 統計記号はイタリック: *M*, *SD*, *β*, *SE*, *z*, *t*, *F*, *p*, *R*^2^\n- *p* 値は `p = .032`、非常に小さい場合は `p < .001`\n- 区間推定は `95% CI [LL, UL]` で表記\n- 効果量（OR / IRR / 倍率）を必ず併記\n- リンクスケールの係数と応答スケールの図をセットで示す\n\n### ケース別テンプレート（worked example）\n\n::: {.panel-tabset}\n\n#### Case 1: Binary（logit）\n\n**Method:**\n\n```text\nWe modeled advice ties (0/1) for 420 independent directed dyads\nusing a binomial GLM with a logit link as the baseline model.\nA binomial GAM with REML estimation and penalized smooth terms\n(select = TRUE) was also fitted to assess potential nonlinear\neffects of continuous predictors. Model comparison was based on\nAIC and BIC, with diagnostics including binned residual checks,\ncollinearity (VIF), and gam.check() for basis-dimension adequacy.\nFour link functions (logit, probit, cloglog, cauchit) were compared\nto verify robustness of conclusions to link specification.\n```\n\n**Results:**\n\n```text\nA binomial GLM and GAM were fitted to 420 independent directed\ndyads. The GAM did not provide a\nsubstantial improvement over the GLM (ΔAIC < 2), indicating\nlimited nonlinear gain for baseline-score effects. We therefore\nreport odds ratios from the GLM.\n\nSame-class membership was positively associated with advice ties,\nβ = [値], SE = [値], z = [値], p [= 値 or < .001],\nOR = [値], 95% CI [[下限], [上限]]. On the response scale,\nthe predicted probability of an advice tie was [X]% for\ndifferent-class dyads and [Y]% for same-class dyads.\n\nProficiency gap was [positively/not significantly] associated\nwith advice ties, β = [値], SE = [値], z = [値], p = [値],\nOR = [値], 95% CI [[下限], [上限]].\n```\n\n**査読者チェックリスト:**\n\n1. 分布族（binomial）とリンク関数（logit）の選択根拠を明記したか？\n2. GLM と GAM を比較し、GAM の採否理由を説明したか？\n3. リンクスケール（β, OR）と応答スケール（予測確率）の両方を報告したか？\n4. 信頼区間を効果量に併記したか？\n5. 診断結果（binned residuals・共線性・gam.check）を要約したか？\n\n#### Case 2: Count（NB）\n\n**Method:**\n\n```text\nCollaboration counts for 320 independent dyads were modeled using\ngeneralized linear models with a log link. A Poisson GLM was fitted\nfirst, and overdispersion was tested using the performance package.\nBecause significant overdispersion was detected (dispersion ratio = [値],\np < .05), a negative binomial GLM was adopted. A negative binomial\nGAM with REML estimation was also fitted to assess nonlinear effects.\n```\n\n**Results:**\n\n```text\nThe Poisson model showed overdispersion (dispersion ratio = [値],\np [< .001]). A negative binomial model was therefore adopted\n(AIC improvement: ΔAIC = [値]).\n\nSame-class pairs had significantly higher expected collaboration\ncounts than cross-class pairs, β = [値], SE = [値], z = [値],\np [= 値 or < .001], IRR = [値], 95% CI [[下限], [上限]].\nOn the response scale, expected counts were [X] for cross-class\nand [Y] for same-class pairs.\n```\n\n**査読者チェックリスト:**\n\n1. Poisson からの出発と過分散テスト結果を報告したか？\n2. NB への切り替え理由（過分散 + AIC改善）を明記したか？\n3. IRR と期待カウントの両方を報告したか？\n4. NB-GLM と NB-GAM の比較結果を説明したか？\n\n#### Case 3: Positive continuous（Gamma）\n\n**Method:**\n\n```text\nCollaboration strength for independent learners was modeled using\na Gamma GLM with a log link, which ensures positive predictions\nand allows multiplicative interpretation of effects. An OLS\nregression (Gaussian family) was fitted as a comparison. A Gamma\nGAM with REML estimation was used to assess nonlinear effects of\nbaseline score.\n```\n\n**Results:**\n\n```text\nThe Gamma GLM provided better fit than the OLS model (ΔAIC = [値]),\nconsistent with the right-skewed distribution and mean-variance\nrelationship observed in collaboration strength.\n\nA 1-SD increase in baseline score was associated with a\nmultiplicative change in expected collaboration strength,\nβ = [値], SE = [値], z = [値], p [= 値 or < .001],\nexp(β) = [値], 95% CI [[下限], [上限]], indicating a [値]%\n[increase/decrease] in expected strength.\n```\n\n**査読者チェックリスト:**\n\n1. Gamma 分布と log リンクの選択根拠を説明したか？\n2. OLS との比較で Gamma の優位性を示したか？\n3. 倍率（exp(β)）と期待値の両方を報告したか？\n4. 0 を含むデータの処理について言及したか？\n\n:::\n\n#### コピペ用1文テンプレート\n\n**Binary**: 「Same-class membership was associated with higher odds of an advice tie, *β* = [値], *SE* = [値], *z* = [値], *p* [= 値], OR = [値], 95% CI [[下限], [上限]].」\n\n**Count**: 「Same-class pairs had [値] times the expected collaboration count of cross-class pairs, *β* = [値], *SE* = [値], *z* = [値], *p* [= 値], IRR = [値], 95% CI [[下限], [上限]].」\n\n**Positive**: 「A 1-*SD* increase in baseline score was associated with a [値]-fold change in expected collaboration strength, *β* = [値], *SE* = [値], *z* = [値], *p* [= 値], exp(*β*) = [値], 95% CI [[下限], [上限]].」\n\n#### DHARMa について\n\n本章（5章）は GLM/GAM が中心のため、残差診断は\n`check_model()`, `gam.check()`, `concurvity()` を主に用いています。\n`DHARMa` は階層構造を含む GLMM の残差診断で特に有効なため、\n6章で本格的に使用しています。\n\n## トラブルシューティング（実務で詰まりやすい点）\n\n### `family` の不一致\n\n**症状**: モデルは収束するが、診断プロットが全面的に破綻。予測値が理論上ありえない範囲に出る。\n\n**原因**: outcome の型（0/1, count, positive continuous）と `family` が合っていない。\n\n**修正**: 以下の対応表を確認してください。\n\n| Outcome | 正しい family | よくある間違い |\n|---|---|---|\n| 0/1 | `binomial` | `gaussian`（OLS） |\n| 0以上の整数 | `poisson` or `nb()` | `gaussian` |\n| 正の連続量 | `Gamma(link=\"log\")` | `gaussian` |\n\n### 過分散の見落とし\n\n**症状**: Poisson GLM の係数が有意だが、NB に切り替えると非有意になる。\n\n**原因**: Poisson は「平均 = 分散」を仮定するため、実際の分散が大きい場合に SE が過小評価される。\n\n**修正**: count データでは必ず `check_overdispersion()` を最初に実行。dispersion ratio > 1 かつ有意なら NB に切り替え。\n\n### GAM の `k` が不足\n\n**症状**: `gam.check()` で k-index が 0.8 未満、p 値が有意。\n\n**原因**: 基底次元が小さすぎて、データの非線形パターンを捉えきれていない。\n\n**修正**: `k` を倍に増やして再当てはめ（例: `k = 8` → `k = 16`）。k-index ≈ 1 になるまで調整。ただし k をデータ数以上にはしない。\n\n### 係数の解釈を混同する\n\n**症状**: 「OR = 2 だから確率が2倍」と誤解する。\n\n**原因**: OR はオッズの比であり、確率の比ではない。ベースラインの確率によって、同じ OR でも確率の変化幅は大きく異なる。\n\n**修正**: 必ず応答スケール（予測確率 / 期待カウント / 期待値）を併記する。OR / IRR / 倍率だけで報告しない。\n\n### 完全分離・準完全分離（binary GLM）\n\n**症状**: 一部の係数が極端に大きい（例: 15〜20以上）。SE が非常に大きいか `Inf`。収束警告が出る。\n\n**原因**: ある予測子の値でアウトカムが完全に 0 または 1 に分かれてしまう。\n\n**修正**: (1) 該当する予測子のカテゴリを統合、(2) Firth のペナルティ付き尤度（`brglm2` パッケージ）を検討、(3) サンプルサイズを増やす。\n\n### Gamma GLM でゼロ値エラー\n\n**症状**: `Error in eval(family$initialize): y values must be > 0`\n\n**原因**: Gamma 分布は厳密に正の値のみをモデル化するため、0 を含むデータでは使えない。\n\n**修正**: (1) 小さな定数を加える（例: `collab_strength + 0.001`）、(2) Tweedie 分布（`tweedie()` in mgcv）を検討、(3) 0 を含む場合はハードルモデルを検討。\n\n### GAM の edf が k-1 に張り付く\n\n**症状**: `gam.check()` で edf がほぼ k-1 と一致。\n\n**原因**: 基底次元の上限まで平滑が使われており、これ以上の柔軟性が必要だが k が不足している。\n\n**修正**: `k` を大きくして再当てはめ。edf が k-1 に張り付かなくなるまで増やす。\n\n## 発展トピック（GAM の拡張と GLM の応用）\n\n本章の3ケースをマスターしたら、以下のトピックに進むことで分析の幅が大きく広がります。\n\n### 発展部の読み方\n\nこの後半は、1本の直線的な流れというより、**自分のアウトカムに合う枝を選ぶ辞書**として使うと効率的です。\n\n- まず `offset` で、露出量が違うカウントをどう補正するかを確認する\n- カテゴリカルな応答なら、`multinomial / ordinal / partial proportional odds` の枝へ進む\n- 0 が多すぎる回数や 0-1 の割合なら、`zero-inflated / hurdle / beta regression` を見る\n- 非線形の形そのものが研究質問なら、最後の `tensor product / varying coefficient` を読む\n\n### カテゴリカル・順序アウトカムの拡張\n\nここでは、2値を超えるカテゴリカル応答を扱います。ポイントは、**順序があるかないか** と、**その順序仮定がどこまで妥当か** です。\n\n#### offset（露出変数）: テキスト長の補正 {.unnumbered}\n\nコーパス分析やライティング研究では、テキストの長さが異なるため、エラー**数**をそのまま比較できません。500語のエッセイは200語のエッセイより多くのエラーを含むのが当然です。**offset** はこの問題を解決します。\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"offset で曝露量を調整する\"}\n# シミュレーションデータ: エッセイごとにエラー数と総語数がある\nset.seed(42)\ndat_offset <- data.frame(\n  errors      = rpois(100, lambda = 3),\n  total_words = sample(200:500, 100, replace = TRUE),\n  treatment   = factor(rep(c(\"control\", \"feedback\"), each = 50))\n)\n\n# offset なし: エラー「数」を直接モデル化（テキスト長を無視）\nm_no_offset <- glm(errors ~ treatment, family = poisson, data = dat_offset)\n\n# offset あり: エラー「率」をモデル化（テキスト長で補正）\nm_with_offset <- glm(errors ~ treatment + offset(log(total_words)),\n                      family = poisson, data = dat_offset)\n\ntibble::tibble(\n  Model = c(\"Without offset (count)\", \"With offset (rate)\"),\n  `Intercept` = c(coef(m_no_offset)[1], coef(m_with_offset)[1]),\n  `treatment effect` = c(coef(m_no_offset)[2], coef(m_with_offset)[2]),\n  `IRR` = exp(c(coef(m_no_offset)[2], coef(m_with_offset)[2]))\n) |>\n  mutate(across(where(is.numeric), ~ round(.x, 3))) |>\n  knitr::kable(caption = \"Effect of offset on coefficient estimates\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Effect of offset on coefficient estimates\n\n|Model                  | Intercept| treatment effect|   IRR|\n|:----------------------|---------:|----------------:|-----:|\n|Without offset (count) |     1.292|           -0.314| 0.731|\n|With offset (rate)     |    -4.626|           -0.184| 0.832|\n\n\n:::\n:::\n\n\n::: {.callout-important}\n#### offset の仕組み\n\n`offset(log(total_words))` は「log(語数)を係数1で固定した予測子」です。これにより、Poisson モデルは**語あたりのエラー率**をモデル化します。R コードでは `glm(errors ~ treatment + offset(log(total_words)), family = poisson)` と書くだけです。\n\n**使うべき場面**: アウトカムがカウントで、観測ごとに「暴露量」（テキスト長、録音時間、試行数）が異なるとき。SLA では**ほぼ常に**該当します。\n:::\n\n#### 名義カテゴリと順序カテゴリの違い {.unnumbered}\n\n2値 GLM は「はい / いいえ」のアウトカムに対応しますが、実際の研究では **3カテゴリ以上** の応答も頻繁に出てきます。ここで重要なのは、**カテゴリに順序があるかどうか**です。\n\n| Outcome type | Example | Keep the order? | Recommended model | Main interpretation |\n|---|---|---|---|---|\n| Nominal | 未知語への対処方略: `skip / infer / lookup` | No | Multinomial logit | reference category に対する RRR |\n| Ordinal | 自己効力感・確信度: `1 < 2 < 3 < 4 < 5` | Yes | Ordinal logit (`clm`) | 共通の cumulative OR |\n\n順序があるデータを multinomial regression に落とすと、カテゴリ順序の情報を捨てることになります。逆に、順序がないデータに ordinal regression を使うと、存在しない序列を仮定してしまいます。したがって、**順序があるなら ordinal、ないなら multinomial** が基本です[@agresti2013cda; @mccullagh1980]。\n\n| 分野 | 名義カテゴリの具体例 | 順序カテゴリの具体例 | 注意点 |\n|---|---|---|---|\n| SLA | 読解中の未知語処理方略（skip / infer / lookup） | WTC, 自己効力感, 満足度の 5 件法 | 同一学習者の反復回答なら第6章へ |\n| 心理言語学 | 反応タイプ（correct / omission / other error）や方略分類 | 語彙判断・文法性判断の confidence rating | 試行反復が多い場合は mixed ordinal/logit へ |\n| 教育学 | フィードバック後の行動選択（revise / ask / ignore） | ルーブリック得点, CEFR band, 授業評価 | 評価者反復があるなら階層構造を考慮 |\n\nこの章では独立観測を守るため、**1 学習者 1 回の方略選択** と **1 学習者 1 回の自己効力感評定** という形で例を示します。もし同じ学習者が複数回回答していたり、同じ項目を複数評定者が採点していたりするなら、第6章の GLMM/ordinal mixed model へ進んでください。\n\n#### 名義カテゴリ3水準以上: Multinomial Regression {.unnumbered}\n\nアウトカムが3水準以上で、しかも自然な順序を持たないなら、**multinomial logit** を使います。`nnet::multinom()` は、最初のカテゴリを参照カテゴリにして、残りのカテゴリについて `K - 1` 個のロジットを推定します[@venables2002modern; @nnetpkg]。\n\nここでは、SLA の読解課題を想定します。学習者が未知語に出会ったときに取りうる選択肢を\n\n- `Skip`（読み飛ばす）\n- `Inferencing`（文脈から推測する）\n- `AI lookup`（AI や辞書に頼る）\n\nの3つとします。これは**順序ではなく選択肢の違い**なので、ordinal ではなく multinomial が自然です。\n\n- `exp(beta)` は **odds ratio** ではなく **relative risk ratio (RRR)** と読むのが一般的です。\n- 参照カテゴリを変えると係数の値と符号は変わりますが、**各カテゴリの予測確率自体は変わりません**。\n- 研究報告では、係数表だけでなく **各カテゴリの予測確率の図** を必ず併記した方が読みやすくなります。\n- `RRR > 1` は「その予測子が増えると、参照カテゴリよりそのカテゴリが選ばれやすくなる」ことを意味します。ただし、**確率そのもの**がどれだけ動くかは、他カテゴリとの兼ね合いもあるため、最終判断は予測確率図で行います。\n\nmultinomial logit の係数は、式で書くと次のようになります。\n\n$$\n\\log\\frac{P(\\text{Inferencing})}{P(\\text{Skip})}\n= \\beta_{0,\\text{inf}} + \\beta_{1,\\text{inf}}x_1 + \\cdots\n$$\n\n$$\n\\log\\frac{P(\\text{AI lookup})}{P(\\text{Skip})}\n= \\beta_{0,\\text{ai}} + \\beta_{1,\\text{ai}}x_1 + \\cdots\n$$\n\nつまり係数は、**あるカテゴリそのもの**ではなく、**参照カテゴリに対する相対的な選ばれやすさ**を表しています。ここで `Skip` を参照カテゴリにしているので、`Inferencing vs Skip` の係数は「推測する方略が、読み飛ばす方略よりどれだけ選ばれやすくなるか」を意味します。\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"多項ロジット回帰の最小例\"}\nset.seed(315)\nn_multi <- 360\n\ndat_multi <- tibble(\n  proficiency_sc = rnorm(n_multi),\n  anxiety_sc = rnorm(n_multi),\n  support_condition = factor(\n    sample(c(\"control\", \"strategy_training\", \"ai_support\"), n_multi, replace = TRUE,\n           prob = c(0.35, 0.35, 0.30)),\n    levels = c(\"control\", \"strategy_training\", \"ai_support\")\n  )\n)\n\neta_skip <- 0.30 - 0.80 * dat_multi$proficiency_sc + 0.55 * dat_multi$anxiety_sc +\n  0.20 * (dat_multi$support_condition == \"control\")\neta_inf <- -0.10 + 0.85 * dat_multi$proficiency_sc - 0.40 * dat_multi$anxiety_sc +\n  0.50 * (dat_multi$support_condition == \"strategy_training\")\neta_ai <- -0.20 + 0.10 * dat_multi$proficiency_sc + 0.15 * dat_multi$anxiety_sc +\n  0.95 * (dat_multi$support_condition == \"ai_support\")\n\neta_multi <- cbind(\n  skip = eta_skip,\n  inferencing = eta_inf,\n  ai_lookup = eta_ai\n)\n\nprob_multi <- exp(eta_multi) / rowSums(exp(eta_multi))\n\ndat_multi$response_strategy <- factor(\n  apply(prob_multi, 1, function(p) sample(colnames(prob_multi), 1, prob = p)),\n  levels = c(\"skip\", \"inferencing\", \"ai_lookup\")\n)\n\nm_multi <- nnet::multinom(\n  response_strategy ~ support_condition + proficiency_sc + anxiety_sc,\n  data = dat_multi,\n  Hess = TRUE,\n  trace = FALSE\n)\n\nmulti_coef_tbl <- broom::tidy(m_multi, conf.int = TRUE, exponentiate = TRUE) |>\n  dplyr::filter(term != \"(Intercept)\") |>\n  mutate(\n    outcome = paste0(y.level, \" vs skip\"),\n    predictor = dplyr::case_when(\n      term == \"support_conditionstrategy_training\" ~ \"Support: strategy training\",\n      term == \"support_conditionai_support\" ~ \"Support: AI support\",\n      term == \"proficiency_sc\" ~ \"Proficiency (z)\",\n      term == \"anxiety_sc\" ~ \"Anxiety (z)\",\n      TRUE ~ term\n    )\n  ) |>\n  dplyr::select(outcome, predictor, estimate, conf.low, conf.high, p.value)\n\nmulti_coef_story_tbl <- multi_coef_tbl |>\n  dplyr::filter(predictor == \"Proficiency (z)\") |>\n  mutate(\n    interpretation = dplyr::case_when(\n      outcome == \"inferencing vs skip\" ~ paste0(\n        \"1 SD higher proficiency multiplies the relative risk of choosing Inferencing over Skip by \",\n        sprintf(\"%.2f\", estimate), \".\"\n      ),\n      outcome == \"ai_lookup vs skip\" ~ paste0(\n        \"1 SD higher proficiency multiplies the relative risk of choosing AI lookup over Skip by \",\n        sprintf(\"%.2f\", estimate), \".\"\n      ),\n      TRUE ~ \"\"\n    )\n  ) |>\n  dplyr::select(outcome, estimate, interpretation)\n\nmulti_prob_examples <- tidyr::crossing(\n  proficiency_sc = c(-1, 0, 1),\n  anxiety_sc = 0,\n  support_condition = c(\"control\", \"strategy_training\", \"ai_support\")\n) |>\n  bind_cols(\n    as_tibble(predict(\n      m_multi,\n      newdata = tidyr::crossing(\n        proficiency_sc = c(-1, 0, 1),\n        anxiety_sc = 0,\n        support_condition = c(\"control\", \"strategy_training\", \"ai_support\")\n      ),\n      type = \"probs\"\n    ))\n  ) |>\n  mutate(\n    proficiency_level = dplyr::case_when(\n      proficiency_sc == -1 ~ \"Low (-1 SD)\",\n      proficiency_sc == 0 ~ \"Mean\",\n      proficiency_sc == 1 ~ \"High (+1 SD)\"\n    ),\n    support_condition = dplyr::case_when(\n      support_condition == \"control\" ~ \"Control\",\n      support_condition == \"strategy_training\" ~ \"Strategy training\",\n      support_condition == \"ai_support\" ~ \"AI support\"\n    )\n  ) |>\n  dplyr::select(proficiency_level, support_condition, skip, inferencing, ai_lookup)\n\nmulti_coef_plot_tbl <- multi_coef_tbl |>\n  mutate(\n    predictor = factor(\n      predictor,\n      levels = rev(c(\n        \"Support: strategy training\",\n        \"Support: AI support\",\n        \"Proficiency (z)\",\n        \"Anxiety (z)\"\n      ))\n    ),\n    outcome = factor(\n      outcome,\n      levels = c(\"inferencing vs skip\", \"ai_lookup vs skip\"),\n      labels = c(\"Inferencing vs Skip\", \"AI lookup vs Skip\")\n    )\n  )\n```\n:::\n\n\n\n::: {#tbl-multinom .cell}\n::: {.cell-output-display}\n\n\nTable: Multinomial logit coefficients as relative risk ratios (reference = skip)\n\n|Contrast            |Predictor                  |   RRR| CI low| CI high|     p|\n|:-------------------|:--------------------------|-----:|------:|-------:|-----:|\n|inferencing vs skip |Support: strategy training | 2.109|  1.059|   4.200| 0.034|\n|inferencing vs skip |Support: AI support        | 1.247|  0.576|   2.697| 0.575|\n|inferencing vs skip |Proficiency (z)            | 4.622|  3.180|   6.717| 0.000|\n|inferencing vs skip |Anxiety (z)                | 0.466|  0.337|   0.646| 0.000|\n|ai_lookup vs skip   |Support: strategy training | 1.324|  0.683|   2.564| 0.406|\n|ai_lookup vs skip   |Support: AI support        | 2.879|  1.509|   5.495| 0.001|\n|ai_lookup vs skip   |Proficiency (z)            | 1.859|  1.374|   2.517| 0.000|\n|ai_lookup vs skip   |Anxiety (z)                | 0.710|  0.534|   0.944| 0.018|\n\n\n:::\n:::\n\n\n\n::: {#tbl-multinom-story .cell}\n::: {.cell-output-display}\n\n\nTable: How to interpret the proficiency coefficient in multinomial logit\n\n|Contrast            |  RRR|How to read it                                                                                  |\n|:-------------------|----:|:-----------------------------------------------------------------------------------------------|\n|inferencing vs skip | 4.62|1 SD higher proficiency multiplies the relative risk of choosing Inferencing over Skip by 4.62. |\n|ai_lookup vs skip   | 1.86|1 SD higher proficiency multiplies the relative risk of choosing AI lookup over Skip by 1.86.   |\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  multi_coef_plot_tbl,\n  aes(x = estimate, y = predictor, xmin = conf.low, xmax = conf.high, color = outcome)\n) +\n  geom_vline(xintercept = 1, linetype = \"dashed\", color = \"gray50\") +\n  geom_pointrange(position = position_dodge(width = 0.55), linewidth = 0.6) +\n  scale_x_log10(\n    breaks = c(0.25, 0.5, 1, 2, 4, 8),\n    labels = c(\"0.25\", \"0.5\", \"1\", \"2\", \"4\", \"8\")\n  ) +\n  labs(\n    x = \"Relative risk ratio (log scale)\",\n    y = NULL,\n    color = \"Contrast\",\n    title = \"Which predictors matter most in the multinomial model?\",\n    subtitle = \"Dashed line = no relative-risk difference (RRR = 1)\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-multinom-coef-1.png){#fig-multinom-coef width=2100}\n:::\n:::\n\n\nこの係数図では、破線 `RRR = 1` が「参照カテゴリ `Skip` と違いがない」境界です。右に大きく離れるほど、その予測子は `Skip` より `Inferencing` や `AI lookup` を選ばせやすくします。とくに `Proficiency` が `Inferencing vs Skip` で強く右へ出ていれば、「習熟度が上がるほど読み飛ばしより推測が選ばれやすい」と解釈できます。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnd_multi <- tidyr::crossing(\n  proficiency_sc = seq(-2.2, 2.2, length.out = 120),\n  anxiety_sc = 0,\n  support_condition = levels(dat_multi$support_condition)\n)\n\npred_multi <- predict(m_multi, newdata = nd_multi, type = \"probs\") |>\n  as_tibble()\n\npred_multi <- bind_cols(nd_multi, pred_multi) |>\n  pivot_longer(\n    cols = c(skip, inferencing, ai_lookup),\n    names_to = \"strategy\",\n    values_to = \"probability\"\n  ) |>\n  mutate(\n    strategy = factor(\n      strategy,\n      levels = c(\"skip\", \"inferencing\", \"ai_lookup\"),\n      labels = c(\"Skip\", \"Inferencing\", \"AI lookup\")\n    )\n  )\n\nggplot(pred_multi, aes(x = proficiency_sc, y = probability, color = strategy)) +\n  geom_line(linewidth = 1) +\n  facet_wrap(\n    ~ support_condition,\n    labeller = as_labeller(c(\n      control = \"Control\",\n      strategy_training = \"Strategy training\",\n      ai_support = \"AI support\"\n    ))\n  ) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Predicted probability\",\n    color = \"Strategy\",\n    title = \"Predicted probabilities from multinomial logit\",\n    subtitle = \"Unknown-word strategy choice in an L2 reading task\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-multinom-pred-1.png){#fig-multinom-pred width=2100}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(pred_multi, aes(x = proficiency_sc, y = probability, fill = strategy)) +\n  geom_area(color = \"white\", linewidth = 0.25, alpha = 0.9) +\n  facet_wrap(\n    ~ support_condition,\n    labeller = as_labeller(c(\n      control = \"Control\",\n      strategy_training = \"Strategy training\",\n      ai_support = \"AI support\"\n    ))\n  ) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Probability composition\",\n    fill = \"Strategy\",\n    title = \"How the three strategy probabilities reallocate across proficiency\",\n    subtitle = \"Each vertical slice sums to 100%\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-multinom-stack-1.png){#fig-multinom-stack width=2100}\n:::\n:::\n\n\n\n::: {#tbl-multinom-prob-examples .cell}\n::: {.cell-output-display}\n\n\nTable: Selected predicted probabilities from the multinomial model\n\n|Proficiency  |Condition         |  Skip| Inferencing| AI lookup|\n|:------------|:-----------------|-----:|-----------:|---------:|\n|Low (-1 SD)  |AI support        | 0.524|       0.058|     0.418|\n|Low (-1 SD)  |Control           | 0.732|       0.065|     0.203|\n|Low (-1 SD)  |Strategy training | 0.643|       0.120|     0.236|\n|Mean         |AI support        | 0.334|       0.171|     0.495|\n|Mean         |Control           | 0.519|       0.213|     0.268|\n|Mean         |Strategy training | 0.393|       0.340|     0.268|\n|High (+1 SD) |AI support        | 0.163|       0.386|     0.451|\n|High (+1 SD) |Control           | 0.259|       0.492|     0.249|\n|High (+1 SD) |Strategy training | 0.160|       0.638|     0.202|\n\n\n:::\n:::\n\n\nこの図は、**支援条件ごとに、習熟度が上がるほど未知語処理方略がどう変わるか** を示しています。読み方は次の通りです。\n\n- 各 facet は support condition 条件です。\n- 各 x 値で 3 本の線の合計は 100% です。\n- 線が交差する位置は、「優勢なカテゴリが入れ替わる proficiency 水準」を意味します。\n- 3 本の線が近い領域は、「どの方略もそれなりに起こりうる」不確実性が高い領域です。\n\n積み上げ面グラフは、同じ情報を **配分の変化** として読ませるための補助図です。1 本の縦切片を見ると、その proficiency 水準で `Skip / Inferencing / AI lookup` が何対何対何で起こりそうかが分かります。multinomial regression では、**カテゴリごとの確率は互いに連動して合計 100% になる**ので、この配分図は line plot より直感的なことがあります。\n\nこの例では、低 proficiency・高 anxiety では `Skip` が優勢ですが、習熟度が上がると `Inferencing` が優勢になります。また `AI support` 条件では `AI lookup` の確率が全体に押し上げられます。教育的には、「どの支援条件が、どの層の学習者にどの方略を促すか」を見るためのモデルだと理解すると分かりやすいです。ここで大事なのは、**RRR 表は参照カテゴリとの比較であり、研究質問としては予測確率図の方がそのまま読める**という点です。\n\n心理言語学でも発想は同じです。例えば、語彙処理課題で反応タイプが `correct / omission / other error` のような複数カテゴリに分かれるなら、カテゴリに順序がない限り multinomial regression が自然です。\n\n#### 順序カテゴリ: Ordinal Regression {.unnumbered}\n\n5件法・7件法の評定や CEFR のように、カテゴリに明確な順序があるなら、**ordinal regression** を使います。`ordinal::clm()` は cumulative link model を当てはめ、全てのカットポイントで**共通の傾き**を仮定します[@mccullagh1980; @ordinalpkg]。\n\nここでは、教育学・SLA でよくある **介入後の L2 speaking self-efficacy を 1-5 で答える場面** を想定します。`1 < 2 < 3 < 4 < 5` という順序をそのままモデルに残せるのが ordinal regression の利点です。\n\n- `exp(beta)` は **cumulative odds ratio** です。\n- 正の係数は、**より高いカテゴリに入る cumulative odds が増える** ことを意味します。\n- 重要な前提は **proportional odds assumption** です。`ordinal::nominal_test()` を使うと、予測子ごとの逸脱を簡便に点検できます。\n- ここでいう OR は「1点上がる」という意味ではなく、**ある cutpoint 以上に入る odds が全体としてどれだけ増えるか** を表します。\n\n`clm()` の出力には、2種類の係数が出ます[@ordinalpkg]。\n\n- `1|2`, `2|3`, `3|4`, `4|5`: **threshold / cutpoint**。カテゴリ境界の位置を表すもので、通常は理論解釈の中心ではありません。\n- `treatment`, `proficiency_sc`, `anxiety_sc`: **location coefficients**。こちらが「どの予測子が高いカテゴリへの移動を促すか」を表す中心的な係数です。\n\nしたがって、ordinal regression の結果表では、**threshold を眺めるより location 部分を読む**のが基本です。threshold はモデル内部でカテゴリを区切る役割、location coefficient は研究上の説明変数の役割、と分けて理解すると混乱しにくくなります。\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"順序ロジット回帰の最小例\"}\nset.seed(316)\nn_ord <- 420\n\ndat_ord <- tibble(\n  proficiency_sc = rnorm(n_ord),\n  anxiety_sc = rnorm(n_ord),\n  treatment = factor(\n    sample(c(\"control\", \"strategy_feedback\"), n_ord, replace = TRUE),\n    levels = c(\"control\", \"strategy_feedback\")\n  )\n)\n\nlatent_ord <- 0.60 * dat_ord$proficiency_sc +\n  0.50 * (dat_ord$treatment == \"strategy_feedback\") -\n  0.70 * dat_ord$anxiety_sc +\n  rlogis(n_ord)\n\ncuts_ord <- c(-1.4, -0.4, 0.5, 1.3)\nrating_num <- cut(latent_ord, breaks = c(-Inf, cuts_ord, Inf), labels = FALSE)\n\ndat_ord$self_efficacy <- ordered(rating_num, levels = 1:5)\n\nm_ord <- ordinal::clm(\n  self_efficacy ~ treatment + proficiency_sc + anxiety_sc,\n  data = dat_ord,\n  link = \"logit\"\n)\n\nord_coef_tbl <- broom::tidy(m_ord, conf.int = TRUE, exponentiate = TRUE) |>\n  dplyr::filter(coef.type == \"location\") |>\n  mutate(\n    predictor = dplyr::case_when(\n      term == \"treatmentstrategy_feedback\" ~ \"Treatment: strategy feedback\",\n      term == \"proficiency_sc\" ~ \"Proficiency (z)\",\n      term == \"anxiety_sc\" ~ \"Anxiety (z)\",\n      TRUE ~ term\n    )\n  ) |>\n  dplyr::select(predictor, estimate, conf.low, conf.high, p.value)\n\nord_po_tbl <- ordinal::nominal_test(m_ord) |>\n  as.data.frame() |>\n  tibble::rownames_to_column(\"predictor\") |>\n  dplyr::filter(predictor != \"<none>\")\n\nord_coef_story_tbl <- ord_coef_tbl |>\n  mutate(\n    interpretation = dplyr::case_when(\n      predictor == \"Treatment: strategy feedback\" ~ paste0(\n        \"Learners in the strategy-feedback condition have \",\n        sprintf(\"%.2f\", estimate),\n        \" times the cumulative odds of reporting a higher self-efficacy category.\"\n      ),\n      predictor == \"Proficiency (z)\" ~ paste0(\n        \"A 1 SD increase in proficiency multiplies the cumulative odds of being in a higher self-efficacy category by \",\n        sprintf(\"%.2f\", estimate), \".\"\n      ),\n      predictor == \"Anxiety (z)\" ~ paste0(\n        \"A 1 SD increase in anxiety multiplies the cumulative odds by \",\n        sprintf(\"%.2f\", estimate),\n        \", so higher anxiety is associated with lower ratings.\"\n      ),\n      TRUE ~ \"\"\n    )\n  ) |>\n  dplyr::select(predictor, estimate, interpretation)\n\nord_prob_examples <- tidyr::crossing(\n  proficiency_sc = c(-1, 0, 1),\n  anxiety_sc = 0,\n  treatment = c(\"control\", \"strategy_feedback\")\n)\n\nord_prob_matrix <- predict(m_ord, newdata = ord_prob_examples, type = \"prob\")$fit |>\n  as_tibble() |>\n  setNames(paste0(\"Rating \", 1:5))\n\nord_prob_examples <- bind_cols(ord_prob_examples, ord_prob_matrix) |>\n  mutate(\n    proficiency_level = dplyr::case_when(\n      proficiency_sc == -1 ~ \"Low (-1 SD)\",\n      proficiency_sc == 0 ~ \"Mean\",\n      proficiency_sc == 1 ~ \"High (+1 SD)\"\n    ),\n    treatment = dplyr::case_when(\n      treatment == \"control\" ~ \"Control\",\n      treatment == \"strategy_feedback\" ~ \"Strategy feedback\"\n    ),\n    `Pr(4 or 5)` = `Rating 4` + `Rating 5`\n  ) |>\n  dplyr::select(proficiency_level, treatment, `Rating 1`, `Rating 2`, `Rating 3`, `Rating 4`, `Rating 5`, `Pr(4 or 5)`)\n\nord_high_grid <- tidyr::crossing(\n  proficiency_sc = seq(-2.2, 2.2, length.out = 120),\n  anxiety_sc = 0,\n  treatment = levels(dat_ord$treatment)\n)\n\nord_high_prob <- predict(m_ord, newdata = ord_high_grid, type = \"prob\")$fit |>\n  as_tibble() |>\n  setNames(paste0(\"Rating \", 1:5))\n\nord_high_prob <- bind_cols(ord_high_grid, ord_high_prob) |>\n  transmute(\n    proficiency_sc,\n    treatment,\n    pr_high = `Rating 4` + `Rating 5`\n  )\n\nord_coef_plot_tbl <- ord_coef_tbl |>\n  mutate(\n    predictor = factor(\n      predictor,\n      levels = rev(c(\n        \"Treatment: strategy feedback\",\n        \"Proficiency (z)\",\n        \"Anxiety (z)\"\n      ))\n    )\n  )\n\nord_threshold_tbl <- tibble(\n  threshold = names(stats::coef(m_ord))[str_detect(names(stats::coef(m_ord)), \"\\\\|\")],\n  value = unname(stats::coef(m_ord)[str_detect(names(stats::coef(m_ord)), \"\\\\|\")]),\n  label_y = 0.34\n)\n\nbeta_ord <- stats::coef(m_ord)\n\nord_latent_demo <- tidyr::crossing(\n  x = seq(-4.5, 4.5, length.out = 500),\n  profile = c(\"Low proficiency / Control\", \"High proficiency / Strategy feedback\")\n) |>\n  mutate(\n    eta = dplyr::case_when(\n      profile == \"Low proficiency / Control\" ~ -1 * beta_ord[\"proficiency_sc\"],\n      profile == \"High proficiency / Strategy feedback\" ~\n        beta_ord[\"treatmentstrategy_feedback\"] + beta_ord[\"proficiency_sc\"]\n    ),\n    density = dlogis(x, location = eta, scale = 1)\n  )\n```\n:::\n\n\n\n::: {#tbl-ordinal-location .cell}\n::: {.cell-output-display}\n\n\nTable: Ordinal logit coefficients for L2 speaking self-efficacy\n\n|Predictor                    | Cumulative OR| CI low| CI high|  p|\n|:----------------------------|-------------:|------:|-------:|--:|\n|Treatment: strategy feedback |         1.925|  1.351|   2.751|  0|\n|Proficiency (z)              |         1.471|  1.224|   1.774|  0|\n|Anxiety (z)                  |         0.468|  0.386|   0.564|  0|\n\n\n:::\n:::\n\n\n\n::: {#tbl-ordinal-story .cell}\n::: {.cell-output-display}\n\n\nTable: How to interpret the location coefficients in ordinal logit\n\n|Predictor                    | Cumulative OR|How to read it                                                                                                                |\n|:----------------------------|-------------:|:-----------------------------------------------------------------------------------------------------------------------------|\n|Treatment: strategy feedback |          1.92|Learners in the strategy-feedback condition have 1.92 times the cumulative odds of reporting a higher self-efficacy category. |\n|Proficiency (z)              |          1.47|A 1 SD increase in proficiency multiplies the cumulative odds of being in a higher self-efficacy category by 1.47.            |\n|Anxiety (z)                  |          0.47|A 1 SD increase in anxiety multiplies the cumulative odds by 0.47, so higher anxiety is associated with lower ratings.        |\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  ord_coef_plot_tbl,\n  aes(x = estimate, y = predictor, xmin = conf.low, xmax = conf.high)\n) +\n  geom_vline(xintercept = 1, linetype = \"dashed\", color = \"gray50\") +\n  geom_pointrange(color = \"#1b9e77\", linewidth = 0.6) +\n  scale_x_log10(\n    breaks = c(0.25, 0.5, 1, 2, 4),\n    labels = c(\"0.25\", \"0.5\", \"1\", \"2\", \"4\")\n  ) +\n  labs(\n    x = \"Cumulative odds ratio (log scale)\",\n    y = NULL,\n    title = \"Location effects in the ordinal model\",\n    subtitle = \"Dashed line = no shift toward higher self-efficacy categories\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-ordinal-coef-1.png){#fig-ordinal-coef width=2100}\n:::\n:::\n\n\nこの図では、破線 `OR = 1` が「高い自己効力感カテゴリへ移動する傾向がない」境界です。右側にある係数は高いカテゴリへの移動を促し、左側にある係数はそれを抑えます。`Anxiety` が 1 より十分小さいなら、不安の高い学習者ほど高い自己効力感を報告しにくい、という解釈になります。\n\n\n::: {#tbl-ordinal-po .cell}\n::: {.cell-output-display}\n\n\nTable: Quick check of the proportional odds assumption using nominal_test()\n\n|Predictor      | Df|   logLik|      AIC|   LRT|     p|\n|:--------------|--:|--------:|--------:|-----:|-----:|\n|treatment      |  3| -618.233| 1256.466| 3.089| 0.378|\n|proficiency_sc |  3| -619.088| 1258.176| 1.378| 0.711|\n|anxiety_sc     |  3| -618.227| 1256.453| 3.101| 0.376|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(ord_latent_demo, aes(x = x, y = density, color = profile)) +\n  geom_line(linewidth = 1) +\n  geom_vline(\n    data = ord_threshold_tbl,\n    aes(xintercept = value),\n    linetype = \"dashed\",\n    color = \"gray45\",\n    inherit.aes = FALSE\n  ) +\n  geom_text(\n    data = ord_threshold_tbl,\n    aes(x = value, y = label_y, label = threshold),\n    angle = 90,\n    vjust = -0.4,\n    size = 3,\n    color = \"gray35\",\n    inherit.aes = FALSE\n  ) +\n  labs(\n    x = \"Latent self-efficacy continuum\",\n    y = \"Density\",\n    color = \"Profile\",\n    title = \"How thresholds partition the latent continuum\",\n    subtitle = \"A rightward shift means higher probabilities for higher observed categories\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-ordinal-threshold-1.png){#fig-ordinal-threshold width=2100}\n:::\n:::\n\n\nこの threshold 図は、ordinal regression を「潜在連続体をいくつかの境界で区切って観測カテゴリを作るモデル」として見るための図です。縦の破線が `1|2`, `2|3`, `3|4`, `4|5` の cutpoint で、密度曲線が右へずれるほど高いカテゴリに入る確率が増えます。つまり `location coefficient` は、この潜在分布全体を左右に動かす量だと考えると理解しやすくなります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnd_ord <- tidyr::crossing(\n  proficiency_sc = seq(-2.2, 2.2, length.out = 120),\n  anxiety_sc = 0,\n  treatment = levels(dat_ord$treatment)\n)\n\npred_ord <- predict(m_ord, newdata = nd_ord, type = \"prob\")$fit |>\n  as_tibble() |>\n  setNames(paste0(\"Rating \", 1:5))\n\npred_ord <- bind_cols(nd_ord, pred_ord) |>\n  pivot_longer(\n    cols = starts_with(\"Rating \"),\n    names_to = \"rating\",\n    values_to = \"probability\"\n  )\n\nggplot(pred_ord, aes(x = proficiency_sc, y = probability, color = rating)) +\n  geom_line(linewidth = 1) +\n  facet_wrap(\n    ~ treatment,\n    labeller = as_labeller(c(\n      control = \"Control\",\n      strategy_feedback = \"Strategy feedback\"\n    ))\n  ) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Predicted probability\",\n    color = \"Self-efficacy\",\n    title = \"Predicted category probabilities from ordinal logit\",\n    subtitle = \"L2 speaking self-efficacy after feedback\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-ordinal-pred-1.png){#fig-ordinal-pred width=2100}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  ord_high_prob,\n  aes(\n    x = proficiency_sc,\n    y = pr_high,\n    color = treatment\n  )\n) +\n  geom_line(linewidth = 1) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  scale_color_manual(\n    values = c(\"control\" = \"#1b9e77\", \"strategy_feedback\" = \"#d95f02\"),\n    labels = c(\"Control\", \"Strategy feedback\")\n  ) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Pr(Self-efficacy >= 4)\",\n    color = \"Condition\",\n    title = \"A more direct ordinal interpretation\",\n    subtitle = \"Probability of landing in the top two self-efficacy categories\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-ordinal-high-1.png){#fig-ordinal-high width=2100}\n:::\n:::\n\n\n\n::: {#tbl-ordinal-prob-examples .cell}\n::: {.cell-output-display}\n\n\nTable: Selected predicted probabilities from the ordinal model\n\n|Proficiency  |Condition         | Rating 1| Rating 2| Rating 3| Rating 4| Rating 5| Pr(4 or 5)|\n|:------------|:-----------------|--------:|--------:|--------:|--------:|--------:|----------:|\n|Low (-1 SD)  |Control           |    0.296|    0.216|    0.202|    0.136|    0.150|      0.286|\n|Low (-1 SD)  |Strategy feedback |    0.179|    0.173|    0.212|    0.181|    0.254|      0.435|\n|Mean         |Control           |    0.222|    0.194|    0.213|    0.164|    0.207|      0.371|\n|Mean         |Strategy feedback |    0.129|    0.141|    0.199|    0.198|    0.334|      0.531|\n|High (+1 SD) |Control           |    0.163|    0.164|    0.209|    0.187|    0.277|      0.464|\n|High (+1 SD) |Strategy feedback |    0.092|    0.109|    0.174|    0.201|    0.424|      0.625|\n\n\n:::\n:::\n\n\nこの図では、proficiency が上がるにつれて確率の山が `Rating 1-2` から `Rating 4-5` に移動しています。`strategy_feedback` 条件ではその移動が全体に少し右へ寄り、より高い自己効力感カテゴリに入りやすくなっています。ordinal regression のよい点は、**「高い方へ寄る」という順序情報を1本の傾きで効率よく表現できる**ことです。\n\n特に `Pr(Self-efficacy >= 4)` の図は、cumulative OR の意味を直感的に読む助けになります。例えば、control から strategy feedback に変わったときに曲線全体が上へ持ち上がっていれば、それは「高い評定カテゴリに入る odds が一貫して上がっている」ということです。\n\n教育学では、同じ考え方で **ルーブリックの 1-5 評定** や **授業満足度 1-7** を扱えます。心理言語学では、語彙判断や文法性判断の **confidence rating** にそのまま使えます。ただし、その場合に同じ参加者が多数試行をこなしているなら、第6章か第7章で扱う mixed / Bayesian ordinal model が自然です。\n\n実務的には、次の順で判断すると整理しやすくなります。\n\n1. 順序があるかないかを先に決める。\n2. 順序があるなら `clm()` を第一候補にする。\n3. `nominal_test()` で proportional odds の大きな破れがないか確認する。\n4. 前提が強く破れるなら、`clm(..., nominal = ~ predictor)` のような拡張、multinomial regression、または第7章の Bayesian ordinal model を検討する。\n\n#### proportional odds assumption が怪しいとき {.unnumbered}\n\n`ordinal::clm()` の強みは、**「高いカテゴリへ寄る」傾向を1本の傾きで要約できる**ことです。しかしそのためには、予測子の効果が各 cutpoint で大きく変わらないという **proportional odds assumption** が必要です[@mccullagh1980; @ordinalpkg]。`nominal_test()` は、この前提が実務上問題になるほど破れていないかを点検する入口として有用です。\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"比例オッズ仮定を緩める\"}\nm_ord_nominal_treatment <- ordinal::clm(\n  self_efficacy ~ treatment + proficiency_sc + anxiety_sc,\n  nominal = ~ treatment,\n  data = dat_ord,\n  link = \"logit\"\n)\n\nord_po_decision_tbl <- tibble::tribble(\n  ~Scenario, ~Recommended_Model, ~How_to_Read_It,\n  \"nominal_test() で大きな逸脱なし\", \"Standard ordinal logit\", \"Keep the simpler cumulative-odds interpretation\",\n  \"1つの予測子だけ逸脱\", \"Partial proportional odds (`nominal = ~ predictor`)\", \"Relax only the offending predictor\",\n  \"複数予測子で強い逸脱 / 順序の理論が弱い\", \"Multinomial logit\", \"Abandon the common-odds assumption and read category probabilities directly\"\n)\n\nord_po_compare_tbl <- tibble(\n  model = c(\"Standard proportional-odds model\", \"Relaxed thresholds for treatment\"),\n  AIC = c(AIC(m_ord), AIC(m_ord_nominal_treatment)),\n  BIC = c(BIC(m_ord), BIC(m_ord_nominal_treatment))\n)\n\nord_relaxed_grid <- tidyr::crossing(\n  proficiency_sc = seq(-2.2, 2.2, length.out = 160),\n  anxiety_sc = 0,\n  treatment = levels(dat_ord$treatment)\n)\n\nord_std_prob <- predict(m_ord, newdata = ord_relaxed_grid, type = \"prob\")$fit\nord_relaxed_prob <- predict(m_ord_nominal_treatment, newdata = ord_relaxed_grid, type = \"prob\")$fit\n\nord_relaxed_plot_tbl <- bind_rows(\n  tibble(\n    ord_relaxed_grid,\n    pr_high = rowSums(ord_std_prob[, 4:5]),\n    model = \"Proportional odds\"\n  ),\n  tibble(\n    ord_relaxed_grid,\n    pr_high = rowSums(ord_relaxed_prob[, 4:5]),\n    model = \"Relaxed thresholds\"\n  )\n) |>\n  mutate(\n    treatment = factor(\n      treatment,\n      levels = c(\"control\", \"strategy_feedback\"),\n      labels = c(\"Control\", \"Strategy feedback\")\n    )\n  )\n```\n:::\n\n\n| Scenario | Recommended Model | How to Read It |\n|:--|:--|:--|\n| `nominal_test()` で大きな逸脱なし | Standard ordinal logit | Keep the simpler cumulative-odds interpretation |\n| 1つの予測子だけ逸脱 | Partial proportional odds (`nominal = ~ predictor`) | Relax only the offending predictor |\n| 複数予測子で強い逸脱 / 順序の理論が弱い | Multinomial logit | Abandon the common-odds assumption and read category probabilities directly |\n\n: What to do when the proportional-odds assumption is questionable\n\n\n::: {#tbl-ordinal-po-compare .cell}\n::: {.cell-output-display}\n\n\nTable: AIC comparison: standard ordinal model vs. relaxed-threshold model\n\n|model                            |      AIC|      BIC|\n|:--------------------------------|--------:|--------:|\n|Standard proportional-odds model | 1253.554| 1281.836|\n|Relaxed thresholds for treatment | 1256.466| 1296.868|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  ord_relaxed_plot_tbl,\n  aes(x = proficiency_sc, y = pr_high, color = model)\n) +\n  geom_line(linewidth = 1) +\n  facet_wrap(~ treatment) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Pr(Self-efficacy >= 4)\",\n    color = \"Model\",\n    title = \"Does relaxing the proportional-odds assumption change the conclusion?\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-ordinal-po-relaxed-1.png){#fig-ordinal-po-relaxed width=2100}\n:::\n:::\n\n\nここで大事なのは、**前提違反の有無そのもの**よりも、**結論がどれだけ変わるか** です。`nominal_test()` がわずかに有意でも、予測確率曲線がほぼ重なるなら、標準の ordinal model を維持してよいことがあります。逆に、緩めたモデルで `Pr(Self-efficacy >= 4)` の形が大きく変わるなら、その予測子については `nominal = ~ predictor` を考える価値があります。\n\n### ゼロ過剰カウントと割合データ\n\nこのセクションは、「平均だけでは足りない形のアウトカム」を扱います。  \nゼロが多すぎる回数データでは **ゼロ生成過程** を、0-1 の連続割合では **平均とばらつき** の両方を意識する必要があります。\n\n#### Zero-inflated / Hurdle モデル（ゼロ過剰カウント） {.unnumbered}\n\nSLA・教育データでは、「0 が多い」だけでなく、**なぜ 0 なのかが1通りではない**ことがあります。例えば、短いスピーキング課題で target clause の使用回数を数えると、\n\n- 当該構文を**回避した**ために 0\n- 使うつもりはあったが、その短い課題では**たまたま 0**\n\nの2つが混ざりえます。後者だけなら NB-GLM でもある程度扱えますが、前者のような **構造的ゼロ** が多いときは `zeroinfl()` や `hurdle()` を検討します[@lambert1992; @zeileis2008count; @psclpkg]。\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"ゼロ過剰カウントの最小例\"}\nset.seed(617)\nn_zero <- 420\n\ndat_zero <- tibble(\n  proficiency_sc = rnorm(n_zero),\n  anxiety_sc = rnorm(n_zero),\n  planning_support = factor(\n    sample(c(\"control\", \"planning_support\"), n_zero, replace = TRUE),\n    levels = c(\"control\", \"planning_support\")\n  )\n) |>\n  mutate(\n    logit_struct_zero = 1.10 -\n      1.00 * proficiency_sc -\n      0.85 * (planning_support == \"planning_support\") +\n      0.65 * anxiety_sc,\n    structural_zero = rbinom(n(), 1, plogis(logit_struct_zero)),\n    mu_count = exp(\n      -0.25 +\n        0.75 * proficiency_sc +\n        0.55 * (planning_support == \"planning_support\") -\n        0.35 * anxiety_sc\n    ),\n    count_draw = rnbinom(n(), size = 1.35, mu = mu_count),\n    target_clause_count = if_else(structural_zero == 1L, 0L, count_draw)\n  )\n\nm_zero_nb <- MASS::glm.nb(\n  target_clause_count ~ planning_support + proficiency_sc + anxiety_sc,\n  data = dat_zero\n)\n\nm_zero_zinb <- pscl::zeroinfl(\n  target_clause_count ~ planning_support + proficiency_sc + anxiety_sc |\n    planning_support + proficiency_sc + anxiety_sc,\n  data = dat_zero,\n  dist = \"negbin\"\n)\n\nm_zero_hurdle <- pscl::hurdle(\n  target_clause_count ~ planning_support + proficiency_sc + anxiety_sc |\n    planning_support + proficiency_sc + anxiety_sc,\n  data = dat_zero,\n  dist = \"negbin\"\n)\n\nzero_compare_tbl <- tibble(\n  model = c(\"NB-GLM\", \"Zero-inflated NB\", \"Hurdle NB\"),\n  AIC = c(AIC(m_zero_nb), AIC(m_zero_zinb), AIC(m_zero_hurdle)),\n  BIC = c(BIC(m_zero_nb), BIC(m_zero_zinb), BIC(m_zero_hurdle)),\n  `Two-process interpretation` = c(\"No\", \"Yes\", \"Yes\")\n) |>\n  arrange(AIC)\n\nzero_grid <- tidyr::crossing(\n  proficiency_sc = seq(-2.2, 2.2, length.out = 180),\n  anxiety_sc = 0,\n  planning_support = levels(dat_zero$planning_support)\n)\n\nzero_pred_tbl <- bind_rows(\n  tibble(\n    zero_grid,\n    model = \"NB-GLM\",\n    prob_any = 1 - dnbinom(\n      0,\n      mu = predict(m_zero_nb, newdata = zero_grid, type = \"response\"),\n      size = m_zero_nb$theta\n    ),\n    expected_count = predict(m_zero_nb, newdata = zero_grid, type = \"response\")\n  ),\n  tibble(\n    zero_grid,\n    model = \"Zero-inflated NB\",\n    prob_any = 1 - as.numeric(predict(m_zero_zinb, newdata = zero_grid, type = \"prob\", at = 0:1)[, 1]),\n    expected_count = as.numeric(predict(m_zero_zinb, newdata = zero_grid, type = \"response\"))\n  ),\n  tibble(\n    zero_grid,\n    model = \"Hurdle NB\",\n    prob_any = 1 - as.numeric(predict(m_zero_hurdle, newdata = zero_grid, type = \"prob\", at = 0:1)[, 1]),\n    expected_count = as.numeric(predict(m_zero_hurdle, newdata = zero_grid, type = \"response\"))\n  )\n) |>\n  mutate(\n    planning_support = factor(\n      planning_support,\n      levels = c(\"control\", \"planning_support\"),\n      labels = c(\"Control\", \"Planning support\")\n    )\n  )\n\nzero_example_grid <- tidyr::crossing(\n  proficiency_sc = c(-1, 0, 1),\n  anxiety_sc = 0,\n  planning_support = levels(dat_zero$planning_support)\n)\n\nzero_examples_tbl <- bind_rows(\n  tibble(\n    zero_example_grid,\n    model = \"NB-GLM\",\n    prob_any = 1 - dnbinom(\n      0,\n      mu = predict(m_zero_nb, newdata = zero_example_grid, type = \"response\"),\n      size = m_zero_nb$theta\n    ),\n    expected_count = predict(m_zero_nb, newdata = zero_example_grid, type = \"response\")\n  ),\n  tibble(\n    zero_example_grid,\n    model = \"Zero-inflated NB\",\n    prob_any = 1 - as.numeric(predict(m_zero_zinb, newdata = zero_example_grid, type = \"prob\", at = 0:1)[, 1]),\n    expected_count = as.numeric(predict(m_zero_zinb, newdata = zero_example_grid, type = \"response\"))\n  ),\n  tibble(\n    zero_example_grid,\n    model = \"Hurdle NB\",\n    prob_any = 1 - as.numeric(predict(m_zero_hurdle, newdata = zero_example_grid, type = \"prob\", at = 0:1)[, 1]),\n    expected_count = as.numeric(predict(m_zero_hurdle, newdata = zero_example_grid, type = \"response\"))\n  )\n) |>\n  mutate(\n    proficiency = dplyr::case_when(\n      proficiency_sc == -1 ~ \"Low (-1 SD)\",\n      proficiency_sc == 0 ~ \"Mean\",\n      proficiency_sc == 1 ~ \"High (+1 SD)\"\n    ),\n    planning_support = factor(\n      planning_support,\n      levels = c(\"control\", \"planning_support\"),\n      labels = c(\"Control\", \"Planning support\")\n    ),\n    `Pr(any use)` = scales::percent(prob_any, accuracy = 1),\n    `Expected count` = round(expected_count, 2)\n  ) |>\n  dplyr::select(model, proficiency, planning_support, `Pr(any use)`, `Expected count`)\n\nzero_diag_tbl <- tibble(\n  model = c(\"Observed data\", \"NB-GLM\", \"Zero-inflated NB\", \"Hurdle NB\"),\n  zero_rate = c(\n    mean(dat_zero$target_clause_count == 0),\n    mean(dnbinom(0, mu = predict(m_zero_nb, type = \"response\"), size = m_zero_nb$theta)),\n    mean(as.numeric(predict(m_zero_zinb, type = \"prob\", at = 0:1)[, 1])),\n    mean(as.numeric(predict(m_zero_hurdle, type = \"prob\", at = 0:1)[, 1]))\n  ),\n  mean_count = c(\n    mean(dat_zero$target_clause_count),\n    mean(predict(m_zero_nb, type = \"response\")),\n    mean(as.numeric(predict(m_zero_zinb, type = \"response\"))),\n    mean(as.numeric(predict(m_zero_hurdle, type = \"response\")))\n  ),\n  mean_positive_count = c(\n    mean(dat_zero$target_clause_count[dat_zero$target_clause_count > 0]),\n    mean(\n      predict(m_zero_nb, type = \"response\") /\n        pmax(1 - dnbinom(0, mu = predict(m_zero_nb, type = \"response\"), size = m_zero_nb$theta), 1e-8)\n    ),\n    mean(\n      as.numeric(predict(m_zero_zinb, type = \"response\")) /\n        pmax(1 - as.numeric(predict(m_zero_zinb, type = \"prob\", at = 0:1)[, 1]), 1e-8)\n    ),\n    mean(\n      as.numeric(predict(m_zero_hurdle, type = \"response\")) /\n        pmax(1 - as.numeric(predict(m_zero_hurdle, type = \"prob\", at = 0:1)[, 1]), 1e-8)\n    )\n  )\n)\n\nzero_diag_plot_tbl <- zero_diag_tbl |>\n  mutate(\n    model = factor(\n      model,\n      levels = c(\"Observed data\", \"NB-GLM\", \"Zero-inflated NB\", \"Hurdle NB\")\n    )\n  ) |>\n  tidyr::pivot_longer(\n    cols = c(zero_rate, mean_count, mean_positive_count),\n    names_to = \"metric\",\n    values_to = \"value\"\n  ) |>\n  mutate(\n    metric = factor(\n      metric,\n      levels = c(\"zero_rate\", \"mean_count\", \"mean_positive_count\"),\n      labels = c(\"Zero rate\", \"Overall mean count\", \"Mean count among positives\")\n    )\n  )\n```\n:::\n\n\n\n::: {#tbl-zero-compare .cell}\n::: {.cell-output-display}\n\n\nTable: Comparing NB, zero-inflated NB, and hurdle NB\n\n|model            |     AIC|     BIC|Two-process interpretation |\n|:----------------|-------:|-------:|:--------------------------|\n|Zero-inflated NB | 857.413| 893.775|Yes                        |\n|Hurdle NB        | 858.909| 895.271|Yes                        |\n|NB-GLM           | 870.084| 890.285|No                         |\n\n\n:::\n:::\n\n\n| Model part | Positive coefficient means | Practical interpretation |\n|---|---|---|\n| NB count model | larger expected count | more target-structure uses overall |\n| ZINB count part | larger expected count within the count process | once the learner is in the “can produce” process, they use the structure more often |\n| ZINB zero part | **more structural zeros** | the learner is more likely to belong to the avoidance / never-use state |\n| Hurdle count part | larger positive counts after crossing the hurdle | once the learner starts using the structure, they produce more tokens |\n| Hurdle zero part | **higher probability of a non-zero count** | the learner is more likely to attempt the structure at all |\n\nこの表で重要なのは、**zero part の符号の意味が ZINB と hurdle で同じではない**ことです。ZINB の zero part は「構造的ゼロ側」なので正の係数はゼロを増やします。hurdle の zero part は「0 を超える側」なので、正の係数はむしろ使用開始を促す方向に読みます。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  zero_pred_tbl,\n  aes(x = proficiency_sc, y = prob_any, color = model, linetype = planning_support)\n) +\n  geom_line(linewidth = 1) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Pr(at least one target-clause use)\",\n    color = \"Model\",\n    linetype = \"Planning condition\",\n    title = \"Zero-heavy counts: who uses the target structure at all?\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-zero-any-1.png){#fig-zero-any width=2100}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  zero_pred_tbl,\n  aes(x = proficiency_sc, y = expected_count, color = model, linetype = planning_support)\n) +\n  geom_line(linewidth = 1) +\n  labs(\n    x = \"Proficiency (z)\",\n    y = \"Expected target-clause count\",\n    color = \"Model\",\n    linetype = \"Planning condition\",\n    title = \"Zero-heavy counts: expected number of uses\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-zero-count-1.png){#fig-zero-count width=2100}\n:::\n:::\n\n\n\n::: {#tbl-zero-examples .cell}\n::: {.cell-output-display}\n\n\nTable: Selected predictions from the zero-heavy count models\n\n|model            |proficiency  |planning_support |Pr(any use) | Expected count|\n|:----------------|:------------|:----------------|:-----------|--------------:|\n|NB-GLM           |Low (-1 SD)  |Control          |8%          |           0.09|\n|NB-GLM           |Low (-1 SD)  |Planning support |20%         |           0.30|\n|NB-GLM           |Mean         |Control          |17%         |           0.25|\n|NB-GLM           |Mean         |Planning support |35%         |           0.80|\n|NB-GLM           |High (+1 SD) |Control          |32%         |           0.66|\n|NB-GLM           |High (+1 SD) |Planning support |51%         |           2.14|\n|Zero-inflated NB |Low (-1 SD)  |Control          |6%          |           0.08|\n|Zero-inflated NB |Low (-1 SD)  |Planning support |16%         |           0.28|\n|Zero-inflated NB |Mean         |Control          |14%         |           0.24|\n|Zero-inflated NB |Mean         |Planning support |32%         |           0.83|\n|Zero-inflated NB |High (+1 SD) |Control          |29%         |           0.72|\n|Zero-inflated NB |High (+1 SD) |Planning support |50%         |           2.26|\n|Hurdle NB        |Low (-1 SD)  |Control          |6%          |           0.09|\n|Hurdle NB        |Low (-1 SD)  |Planning support |16%         |           0.28|\n|Hurdle NB        |Mean         |Control          |14%         |           0.24|\n|Hurdle NB        |Mean         |Planning support |31%         |           0.80|\n|Hurdle NB        |High (+1 SD) |Control          |27%         |           0.70|\n|Hurdle NB        |High (+1 SD) |Planning support |51%         |           2.24|\n\n\n:::\n:::\n\n\n#### ゼロ過剰モデルの診断 {.unnumbered}\n\nzero-heavy モデルでは、AIC だけでなく **「ゼロ率」と「平均件数」をどこまで再現できているか** を見ると理解しやすくなります。理論的に二過程を仮定しても、観測データのゼロ率をほとんど再現できていなければ、その説明は弱いです。\n\n\n::: {#tbl-zero-diagnostics .cell}\n::: {.cell-output-display}\n\n\nTable: Observed vs model-implied summaries for the zero-heavy counts\n\n|model            | zero_rate| mean_count| mean_positive_count|\n|:----------------|---------:|----------:|-------------------:|\n|Observed data    |     0.733|      0.995|               3.732|\n|NB-GLM           |     0.720|      1.027|               2.547|\n|Zero-inflated NB |     0.734|      0.999|               2.642|\n|Hurdle NB        |     0.733|      0.998|               2.642|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  dplyr::filter(zero_diag_plot_tbl, model != \"Observed data\"),\n  aes(x = model, y = value, color = model)\n) +\n  geom_hline(\n    data = dplyr::filter(zero_diag_plot_tbl, model == \"Observed data\"),\n    aes(yintercept = value),\n    inherit.aes = FALSE,\n    linetype = \"dashed\",\n    color = \"gray45\"\n  ) +\n  geom_point(size = 3) +\n  facet_wrap(~ metric, scales = \"free_y\") +\n  labs(\n    x = NULL,\n    y = \"Model-implied value\",\n    color = \"Model\",\n    title = \"How well do the models recover the observed zero-heavy distribution?\",\n    subtitle = \"Dashed line = observed data\"\n  ) +\n  theme(axis.text.x = element_text(angle = 15, hjust = 1))\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-zero-diagnostics-1.png){#fig-zero-diagnostics width=2100}\n:::\n:::\n\n\n**読み方のポイント**:\n\n- **Zero rate** が観測値に近い: ゼロの多さをうまく再現できている  \n- **Overall mean count** が近い: 全体の件数レベルを捉えている  \n- **Mean count among positives** が近い: 一度使い始めた後の件数も妥当\n\nたとえば NB が平均件数は合わせられても zero rate を外しているなら、「件数のばらつき」だけでは説明しきれず、**ゼロ生成過程を別に考える意義** があると読めます。逆に ZINB / hurdle を使っても dashed line に近づかないなら、二過程モデルの物語自体が弱い可能性があります。\n\n読み方は2段階です。まず `Pr(any use)` の図で、**使用するか否か** を見ます。次に `Expected count` の図で、**使うとしたら何回くらい出るか** を見ます。教育的には、planning support が「使い始めるハードル」を下げているのか、それとも「一度使い始めた後の件数」を増やしているのかを区別できるのが、この種のモデルの利点です。\n\n実務ルールとしては、\n\n- 0 が多いだけで理論上 1 つの過程に見えるなら、まず NB を試す\n- 「回避群」と「使用群」が理論的に分かれそうなら ZINB / hurdle を検討する\n- **全員がまず使用するかどうかを決め、その後で正の件数が出る** と考えたいなら hurdle が分かりやすい\n\nと理解すると整理しやすいです。\n\n#### Beta Regression（0-1 の連続割合） {.unnumbered}\n\n割合データを見ると、すぐに `glm(..., family = binomial)` を使いたくなります。しかし **binomial は「成功回数 / 試行回数」があるデータ** のモデルです。もし目的変数が「L2で話していた時間の割合」「課題完成率」「学習ログイン日の比率」のような **0 と 1 の間の連続量** なら、beta regression の方が自然です[@ferrari2004beta; @cribarineto2010betareg; @betaregpkg]。\n\n| データの形 | 第一候補 | 典型例 |\n|---|---|---|\n| `8/10` 正答のように分母が既知 | Binomial GLM | テスト10問中の正答数 |\n| `0.63` のような連続割合 | Beta regression | 授業中のL2使用割合、タスク完成率 |\n| 0 や 1 がそのまま頻出 | Extended-support beta / zero-one inflated beta | 完全達成率、完全不使用率 |\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"ベータ回帰の最小例\"}\nset.seed(619)\nn_beta <- 280\n\ndat_beta <- tibble(\n  motivation_sc = rnorm(n_beta),\n  anxiety_sc = rnorm(n_beta),\n  feedback_condition = factor(\n    sample(c(\"control\", \"strategy_training\"), n_beta, replace = TRUE),\n    levels = c(\"control\", \"strategy_training\")\n  )\n) |>\n  mutate(\n    mu = plogis(\n      -0.10 +\n        0.45 * (feedback_condition == \"strategy_training\") +\n        0.75 * motivation_sc -\n        0.30 * anxiety_sc\n    ),\n    phi = exp(1.80),\n    l2_use_ratio = rbeta(n(), mu * phi, (1 - mu) * phi)\n  )\n\nm_beta <- betareg::betareg(\n  l2_use_ratio ~ feedback_condition + motivation_sc + anxiety_sc,\n  data = dat_beta,\n  link = \"logit\"\n)\n\nbeta_coef_tbl <- broom::tidy(m_beta, conf.int = TRUE) |>\n  mutate(\n    component = dplyr::case_match(\n      component,\n      \"mean\" ~ \"Mean model\",\n      \"precision\" ~ \"Precision model\"\n    )\n  )\n\nbeta_grid <- tidyr::crossing(\n  motivation_sc = seq(-2.2, 2.2, length.out = 180),\n  anxiety_sc = 0,\n  feedback_condition = levels(dat_beta$feedback_condition)\n)\n\nbeta_pred_tbl <- beta_grid |>\n  bind_cols(\n    tibble(predicted_ratio = predict(m_beta, newdata = beta_grid, type = \"response\"))\n  ) |>\n  mutate(\n    feedback_condition = factor(\n      feedback_condition,\n      levels = c(\"control\", \"strategy_training\"),\n      labels = c(\"Control\", \"Strategy training\")\n    )\n  )\n\nbeta_examples_tbl <- tidyr::crossing(\n  motivation_sc = c(-1, 0, 1),\n  anxiety_sc = 0,\n  feedback_condition = levels(dat_beta$feedback_condition)\n) |>\n  bind_cols(\n    tibble(predicted_ratio = predict(\n      m_beta,\n      newdata = tidyr::crossing(\n        motivation_sc = c(-1, 0, 1),\n        anxiety_sc = 0,\n        feedback_condition = levels(dat_beta$feedback_condition)\n      ),\n      type = \"response\"\n    ))\n  ) |>\n  mutate(\n    motivation = dplyr::case_when(\n      motivation_sc == -1 ~ \"Low (-1 SD)\",\n      motivation_sc == 0 ~ \"Mean\",\n      motivation_sc == 1 ~ \"High (+1 SD)\"\n    ),\n    feedback_condition = factor(\n      feedback_condition,\n      levels = c(\"control\", \"strategy_training\"),\n      labels = c(\"Control\", \"Strategy training\")\n    ),\n    `Predicted proportion` = scales::percent(predicted_ratio, accuracy = 1)\n  ) |>\n  dplyr::select(motivation, feedback_condition, `Predicted proportion`)\n```\n:::\n\n\n\n::: {#tbl-beta-coefs .cell}\n::: {.cell-output-display}\n\n\nTable: Beta regression coefficients\n\n|Component       |Term                                | Estimate|    SE|      z|     p| CI low| CI high|\n|:---------------|:-----------------------------------|--------:|-----:|------:|-----:|------:|-------:|\n|Mean model      |(Intercept)                         |   -0.033| 0.064| -0.517| 0.605| -0.159|   0.092|\n|Mean model      |feedback_conditionstrategy_training |    0.512| 0.092|  5.543| 0.000|  0.331|   0.693|\n|Mean model      |motivation_sc                       |    0.742| 0.047| 15.827| 0.000|  0.651|   0.834|\n|Mean model      |anxiety_sc                          |   -0.267| 0.044| -6.078| 0.000| -0.353|  -0.181|\n|Precision model |(phi)                               |    6.365| 0.511| 12.449| 0.000|  5.363|   7.367|\n\n\n:::\n:::\n\n\nこの表では、`Mean model` の係数が研究上の主役です。これは **期待割合のロジット** に対する係数なので、最終解釈は予測割合に戻して行います。`Precision model` の `(phi)` は、観測値が平均のまわりにどれくらい tightly clustered しているかを示します。`phi` が大きいほど、同じ平均でもばらつきが小さくなります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  beta_pred_tbl,\n  aes(x = motivation_sc, y = predicted_ratio, color = feedback_condition)\n) +\n  geom_line(linewidth = 1) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Motivation (z)\",\n    y = \"Predicted proportion of L2 use\",\n    color = \"Condition\",\n    title = \"Beta regression: response-scale predictions\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-beta-pred-1.png){#fig-beta-pred width=2100}\n:::\n:::\n\n\n\n::: {#tbl-beta-examples .cell}\n::: {.cell-output-display}\n\n\nTable: Selected predicted proportions from the beta regression\n\n|motivation   |feedback_condition |Predicted proportion |\n|:------------|:------------------|:--------------------|\n|Low (-1 SD)  |Control            |32%                  |\n|Low (-1 SD)  |Strategy training  |43%                  |\n|Mean         |Control            |49%                  |\n|Mean         |Strategy training  |62%                  |\n|High (+1 SD) |Control            |67%                  |\n|High (+1 SD) |Strategy training  |77%                  |\n\n\n:::\n:::\n\n\nこの図では、motivation が高いほど **L2使用割合そのもの** が上がり、strategy training 条件では曲線全体が上へ持ち上がっています。ここでの解釈単位は OR ではなく、**割合そのものの変化** です。例えば予測値が `42% -> 58%` なら、「平均的な学習者で L2使用割合が 42% から 58% へ上がる」と読むのが自然です。\n\nbeta regression の実務ポイントは次の3つです。\n\n1. 比率が **試行数つきの成功率** なら binomial、**連続的な割合** なら beta を疑う  \n2. 係数は logit スケールでも、**本文では予測割合** で報告する  \n3. 0 や 1 がそのまま多いときは、`betareg` の extended-support beta (`dist = \"xbetax\"`) や zero/one-inflated beta を検討する\n\n#### 0 と 1 を含む割合への対処 {.unnumbered}\n\n古典的な beta regression は、応答が **厳密に `(0, 1)` の内部** にあることを前提にしています。したがって、授業中の L2 使用率が `0%` や `100%` として記録されると、そのままでは通常の beta 分布に入りません。`betareg` の公式ドキュメントでは、この場合に **extended-support beta (`dist = \"xbetax\"`)** が使えること、また境界値があるときはそれが自動選択されることが説明されています[@betaregpkg; @kosmidis2025xbetax]。\n\n| Observed data shape | First choice | Why |\n|---|---|---|\n| 0〜1 の連続割合で、0/1 を含まない | classical beta regression | 標準 beta の支持がデータと一致する |\n| 0 や 1 が少数だけ含まれる | extended-support beta (`xbetax`) | 境界値を無理なく扱える |\n| 0 と 1 が理論的に別過程で頻出する | zero/one-inflated beta | 「完全不使用」「完全達成」を別過程として扱う |\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"0 と 1 を含む比率データの調整\"}\nbeta_low_idx <- order(dat_beta$l2_use_ratio)[1:4]\nbeta_high_idx <- order(dat_beta$l2_use_ratio, decreasing = TRUE)[1:4]\n\ndat_beta_boundary <- dat_beta |>\n  mutate(l2_use_ratio_boundary = l2_use_ratio)\n\ndat_beta_boundary$l2_use_ratio_boundary[beta_low_idx] <- 0\ndat_beta_boundary$l2_use_ratio_boundary[beta_high_idx] <- 1\n\nm_beta_boundary <- betareg::betareg(\n  l2_use_ratio_boundary ~ feedback_condition + motivation_sc + anxiety_sc,\n  data = dat_beta_boundary,\n  link = \"logit\",\n  dist = \"xbetax\"\n)\n\nbeta_boundary_tbl <- tibble(\n  `Exact 0` = sum(dat_beta_boundary$l2_use_ratio_boundary == 0),\n  `Exact 1` = sum(dat_beta_boundary$l2_use_ratio_boundary == 1),\n  `Distribution used` = \"xbetax\",\n  AIC = AIC(m_beta_boundary)\n)\n```\n:::\n\n\n\n::: {#tbl-beta-boundary .cell}\n::: {.cell-output-display}\n\n\nTable: Boundary-value example for beta regression\n\n| Exact 0| Exact 1|Distribution used |      AIC|\n|-------:|-------:|:-----------------|--------:|\n|       4|       4|xbetax            | -160.123|\n\n\n:::\n:::\n\n\nここでの実務的なポイントは、**境界値があるから即 binomial に戻るわけではない** ということです。分母つきの成功回数ではなく、あくまで連続割合として観測されているなら、まずは beta 系のモデルの中で境界値を扱える拡張を検討する方が自然です。\n\n### 高度な GAM とロバストな推定\n\nここから先は、GLM の family 選択を越えて、**曲面・群別非線形・推定上の難しさ** を扱います。  \n本章の本線を理解した後に読むと、`mgcv` の発展的な使い方がつながりやすくなります。\n\n#### GAM の tensor product 交互作用（`te()` / `ti()`） {.unnumbered}\n\nGLM の交互作用 `x1:x2` は「2つの予測子の積」ですが、GAM では**2つの連続予測子の非線形交互作用**を `te()` でモデル化できます。\n\n- `te(x1, x2)`: 2次元の滑らかな曲面（主効果 + 交互作用を一体で推定）\n- `ti(x1, x2)`: 交互作用のみ（`s(x1) + s(x2) + ti(x1, x2)` で ANOVA 的分解が可能）\n- 実務では、**主効果と交互作用を分けて読みたいなら `ti()`**、1枚の滑らかな面としてまとめて捉えたいなら `te()` が便利です。\n\n```r\n# 例: 語彙サイズ × 読解速度の非線形交互作用\ngam(accuracy ~ s(vocab_size) + s(reading_speed) + ti(vocab_size, reading_speed),\n    family = binomial, data = dat, method = \"REML\")\n```\n\n`ti()` を使うと、`summary()` に主効果の smooth と交互作用 smooth が別行で出るので、「それぞれ単独の非線形はあるか」「さらに非加法的な交互作用があるか」を分けて判断しやすくなります。これは ANOVA の「主効果 + 交互作用」にかなり近い読み方です。\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\" code-summary=\"tensor product smooth の最小例\"}\nset.seed(531)\nn_tensor <- 480\n\ndat_tensor <- tibble(\n  vocab_size_sc = rnorm(n_tensor),\n  reading_speed_sc = rnorm(n_tensor)\n) |>\n  mutate(\n    eta = -0.35 +\n      0.75 * vocab_size_sc +\n      0.45 * reading_speed_sc +\n      0.90 * exp(-((vocab_size_sc - 0.7)^2 + (reading_speed_sc - 0.6)^2) / 0.85) -\n      0.80 * exp(-((vocab_size_sc + 0.9)^2 + (reading_speed_sc - 0.9)^2) / 0.60),\n    accuracy = rbinom(n(), 1, plogis(eta))\n  )\n\nm_tensor <- mgcv::gam(\n  accuracy ~\n    s(vocab_size_sc, k = 8) +\n    s(reading_speed_sc, k = 8) +\n    ti(vocab_size_sc, reading_speed_sc, k = c(6, 6)),\n  family = binomial(link = \"logit\"),\n  data = dat_tensor,\n  method = \"REML\",\n  select = TRUE\n)\n\ntensor_smooth_tbl <- tibble::as_tibble(\n  summary(m_tensor)$s.table,\n  rownames = \"smooth_term\"\n) |>\n  dplyr::rename(\n    edf = edf,\n    ref_df = `Ref.df`,\n    chi_sq = `Chi.sq`,\n    p_value = `p-value`\n  ) |>\n  dplyr::mutate(\n    interpretation = dplyr::case_when(\n      smooth_term == \"s(vocab_size_sc)\" ~ \"Nonlinear main effect of vocabulary size\",\n      smooth_term == \"s(reading_speed_sc)\" ~ \"Nonlinear main effect of reading speed\",\n      str_detect(smooth_term, \"^ti\\\\(\") ~ \"Non-additive interaction surface beyond the two main smooths\",\n      TRUE ~ \"\"\n    )\n  )\n```\n:::\n\n\n\n::: {#tbl-tensor-smooths .cell}\n::: {.cell-output-display}\n\n\nTable: Tensor-product GAM: smooth-term summary\n\n|Smooth term                        |   edf| Ref.df| Chi-square|     p|How to read it                                               |\n|:----------------------------------|-----:|------:|----------:|-----:|:------------------------------------------------------------|\n|s(vocab_size_sc)                   | 1.764|      7|     71.790| 0.000|Nonlinear main effect of vocabulary size                     |\n|s(reading_speed_sc)                | 2.314|      7|     28.041| 0.000|Nonlinear main effect of reading speed                       |\n|ti(vocab_size_sc,reading_speed_sc) | 1.253|     25|      2.856| 0.088|Non-additive interaction surface beyond the two main smooths |\n\n\n:::\n:::\n\n\nこの表では、最後の `ti(vocab_size_sc, reading_speed_sc)` がいわゆる **2次元交互作用** です。ここがほぼ 0 なら、「語彙サイズと読解速度はそれぞれ非線形でも、足し算で十分」と読めます。ここが大きく有意なら、「語彙サイズの効果は読解速度によって形そのものが変わる」と考えます。\n\n#### `vis.gam()` の2次元図はどう読むか\n\n`vis.gam()` は、2つの連続予測子にまたがる **予測面** をそのまま見せる関数です。ここでは `type = \"response\"` を指定し、ロジットではなく**正答確率そのもの**を描いています。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmgcv::vis.gam(\n  m_tensor,\n  view = c(\"vocab_size_sc\", \"reading_speed_sc\"),\n  type = \"response\",\n  plot.type = \"contour\",\n  color = \"heat\",\n  too.far = 0.08,\n  n.grid = 70,\n  main = \"Predicted probability surface from a tensor-product GAM\",\n  xlab = \"Vocabulary size (z)\",\n  ylab = \"Reading speed (z)\"\n)\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-tensor-visgam-1.png){#fig-tensor-visgam width=2160}\n:::\n:::\n\n\nこの図の読み方は次の通りです。\n\n- **色**は予測確率の高さです。暖色ほど正答確率が高く、寒色ほど低いと読みます。\n- **等高線**は「同じ確率」の場所を結んだ線です。線が密な場所ほど、少しの変化で確率が大きく動きます。\n- 線が **まっすぐで平行** なら、2変数の効果はほぼ足し算です。線が**曲がる・ねじれる・斜めに傾く**なら、片方の効果がもう片方の値に依存している、つまり交互作用があると読めます。\n- 広い赤い高原があれば、その領域では天井効果に近い状態です。逆に広い青い谷は床効果を示します。\n- 端の不自然な模様はデータが少ない範囲の外挿であることがあるので、`too.far` で切り落としたとしても過信しないでください。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntensor_slice_grid <- tidyr::crossing(\n  vocab_size_sc = seq(-2.2, 2.2, length.out = 180),\n  reading_speed_sc = c(-1, 0, 1)\n)\n\ntensor_slice_tbl <- tensor_slice_grid |>\n  bind_cols(\n    tibble(probability = predict(m_tensor, newdata = tensor_slice_grid, type = \"response\"))\n  ) |>\n  mutate(\n    reading_speed_level = factor(\n      reading_speed_sc,\n      levels = c(-1, 0, 1),\n      labels = c(\"Slow reading speed (-1 SD)\", \"Average reading speed\", \"Fast reading speed (+1 SD)\")\n    )\n  )\n\nggplot(tensor_slice_tbl, aes(x = vocab_size_sc, y = probability, color = reading_speed_level)) +\n  geom_line(linewidth = 1) +\n  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +\n  labs(\n    x = \"Vocabulary size (z)\",\n    y = \"Predicted probability of accurate response\",\n    color = \"Reading speed\",\n    title = \"Reading the same surface as 1D slices\",\n    subtitle = \"The effect of vocabulary size changes across reading-speed levels\"\n  )\n```\n\n::: {.cell-output-display}\n![](5_一般化線形モデル_一般化加法モデル_files/figure-html/fig-tensor-slices-1.png){#fig-tensor-slices width=2100}\n:::\n:::\n\n\n断面図は、等高線図を「3本の線」に言い換えたものです。こちらの方が読むのに慣れている人も多いです。\n\n- 3 本の線が **平行に近い** なら、読解速度によって語彙サイズの効果の形はあまり変わっていません。\n- 線が **交差したり、離れ方が変わったり** するなら、語彙サイズの効果は読解速度に依存しています。\n- たとえば低速読解では語彙サイズが増えても改善が小さいのに、高速読解ではある閾値を超えたあたりから確率が急に上がるなら、それは「十分な処理速度がある学習者にだけ語彙知識が強く効く」という解釈につながります。\n\n要するに、2次元平滑面は次の順で読むと分かりやすいです。\n\n1. `summary()` の `ti(...)` 行で、非加法的な交互作用があるか確認する  \n2. `vis.gam()` で、どの領域が高確率・低確率かを俯瞰する  \n3. 断面図で、どの predictor の効果がどの条件で強まるかを具体的に言語化する\n\n::: {.callout-tip}\n#### いつ tensor product を使うか\n\n- 2つの連続予測子の**スケールが異なる**とき（例: 語彙サイズ 0-5000 と反応時間 200-2000ms）\n- 主効果と交互作用を**分離**して解釈したいとき → `s() + s() + ti()` の分解を使う\n- 2次元の可視化には `vis.gam(model, view = c(\"x1\", \"x2\"))` が便利[@wood2017]\n:::\n\n#### GAM の varying coefficient モデル（`by=` 引数） {.unnumbered}\n\n`s(x, by = group)` を指定すると、**群ごとに異なる非線形効果**を推定できます。例えば「L1 日本語話者と L1 中国語話者で、習熟度と正確さの関係が異なる形をしている」かを検証できます。\n\n```r\n# 群別の非線形効果\ngam(accuracy ~ L1_group + s(proficiency, by = L1_group),\n    family = binomial, data = dat, method = \"REML\")\n\n# ordered factor にすると「群間の差」の平滑項が直接得られる\ndat$L1_ordered <- ordered(dat$L1_group)\ngam(accuracy ~ L1_ordered + s(proficiency, by = L1_ordered),\n    family = binomial, data = dat, method = \"REML\")\n```\n\n::: {.callout-tip}\n#### `by=` のポイント\n\n- `by = factor`: 各群に独立した平滑項を当てはめる。群間で曲線の形が全く異なってもよい\n- `by = ordered(factor)`: 参照群からの**差分**として推定する。差分の平滑項が有意なら「群間で非線形パターンが異なる」と結論できる[@pedersen2019]\n- `by = numeric`: varying coefficient モデル。平滑項の強さが連続変数に比例する\n:::\n\n#### quasi-likelihood（過分散の最も簡単な対処） {.unnumbered}\n\nNB に切り替える前に、**quasi-Poisson** で標準誤差だけを補正する方法もあります。\n\n| 方法 | 分散パラメータ | AIC | 用途 |\n|---|---|---|---|\n| Poisson | 固定（φ = 1） | ✅ 使える | 過分散がないとき |\n| quasi-Poisson | 推定（φ > 1 が可能） | ❌ 使えない（尤度がない） | 軽い過分散の簡易補正 |\n| NB | 推定（theta で制御） | ✅ 使える | 強い過分散、正式なモデル比較 |\n\n```r\n# quasi-Poisson: SE だけ膨張させる\nm_quasi <- glm(errors ~ treatment, family = quasipoisson, data = dat)\nsummary(m_quasi)$dispersion  # φ が 1 に近ければ Poisson で十分\n```\n\n#### 完全分離への対処（Firth のペナルティ付き推定） {.unnumbered}\n\n2値 GLM で「ある条件の全員が正解」のようなデータでは、係数が無限大に発散します（**完全分離**）。`brglm2` パッケージの Firth 法で対処できます。\n\n```r\n# 完全分離の検出\nbrglm2::detect_separation(m_bin_glm)\n\n# Firth のペナルティ付き推定\nm_firth <- glm(y ~ x, family = binomial, data = dat,\n               method = brglm2::brglmFit)\n```\n\n::: {.callout-note}\n#### SLA で完全分離が起きやすい場面\n\n- 易しいテスト項目で全員正解\n- 稀少な文法構造を特定の条件でのみ産出\n- サンプルサイズが小さい実験で極端な結果\n\n警告メッセージ「fitted probabilities numerically 0 or 1 occurred」が出たら、この問題を疑ってください。\n:::\n\n#### その他の発展トピック（概要） {.unnumbered}\n\n::: {.callout-note collapse=\"true\"}\n#### さらに先へ進むためのトピック一覧\n\n| トピック | 内容 | R パッケージ | SLA での用途 |\n|---|---|---|---|\n| **Zero-inflated / Hurdle** | 構造的ゼロを含むカウントデータ | `pscl`, `glmmTMB` | 回避行動によるゼロ過多（例: 特定構文の不使用） |\n| **Tweedie 分布** | ゼロ + 正の連続値が混在 | `mgcv::tw()` | 協働強度（ゼロを含む正値）に Gamma の代替 |\n| **GAMLSS** | 平均だけでなく分散もモデル化 | `gamlss`, `mgcv::gaulss()` | 習熟度で分散が変わるデータ |\n| **Cross-validation** | AIC を補完する予測精度の評価 | `boot::cv.glm()` | モデル選択の頑健性確認 |\n| **Model averaging** | 複数モデルの加重平均 | `MuMIn` | 予測子選択の不確実性を反映（注意: 探索的使用は非推奨） |\n\nこれらのトピックは第7章（ベイズ推定）でも `brms` を通じて扱われます。\n:::\n\n## 発展学習（読書順）\n\n- GLM の原点: @nelder1972\n- **GLM チュートリアル（言語学向け）**: @winter2019stats（ロジスティック回帰・ポアソン回帰を言語データで解説した教科書。本章の内容を体系的に学ぶのに最適）\n- 多カテゴリカテゴリカル回帰: @agresti2013cda; @mccullagh1980; @venables2002modern; @nnetpkg; @ordinalpkg\n- GAM の基礎と実装: @hastie1990; @wood2017; @wood2020\n- 平滑化推定（REML）の理論: @wood2011\n- 言語データへの応用: @winter2016; @pedersen2019; @wirtz2025\n- **GAM チュートリアル（言語学向け）**: @soskuthy2017（基底関数・ペナルティ・ランダム平滑の実践入門）; @wieling2018（L1/L2 調音データでの GAMM チュートリアル）\n- **GLM で反応時間を分析する利点**: @lo2015（対数変換なしで Gamma/逆ガウス GLM を使う方法）\n- **心理言語学での GAMM**: @baayen2017cave（実験中の疲労・学習効果の非線形モデリング）\n- リンク関数の一次資料: @rstatsfamily; @rmakelink; @vgamcloglog; @prentice1978\n- 実装リファレンス: @rstatsglm; @mgcvpkg\n- ハンズオン補助: @ucla_logit; @ucla_probit; @ucla_poisson\n\n## セッション情報\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsessionInfo()\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nR version 4.5.1 (2025-06-13 ucrt)\nPlatform: x86_64-w64-mingw32/x64\nRunning under: Windows 11 x64 (build 26200)\n\nMatrix products: default\n  LAPACK version 3.12.1\n\nlocale:\n[1] LC_COLLATE=Japanese_Japan.utf8  LC_CTYPE=Japanese_Japan.utf8   \n[3] LC_MONETARY=Japanese_Japan.utf8 LC_NUMERIC=C                   \n[5] LC_TIME=Japanese_Japan.utf8    \n\ntime zone: Etc/GMT-9\ntzcode source: internal\n\nattached base packages:\n[1] stats     graphics  grDevices utils     datasets  methods   base     \n\nother attached packages:\n [1] see_0.13.0         betareg_3.2-4      pscl_1.5.9         ordinal_2025.12-29\n [5] nnet_7.3-20        MASS_7.3-65        systemfonts_1.3.2  broom_1.0.12      \n [9] sjPlot_2.9.0       emmeans_2.0.2      performance_0.16.0 mgcv_1.9-4        \n[13] nlme_3.1-168       lubridate_1.9.5    forcats_1.0.1      stringr_1.6.0     \n[17] dplyr_1.2.0        purrr_1.2.1        readr_2.2.0        tidyr_1.3.2       \n[21] tibble_3.3.1       ggplot2_4.0.2      tidyverse_2.0.0    gt_1.3.0          \n\nloaded via a namespace (and not attached):\n [1] Rdpack_2.6.6        sandwich_3.1-1      rlang_1.1.7        \n [4] magrittr_2.0.4      multcomp_1.4-30     snakecase_0.11.1   \n [7] otel_0.2.0          compiler_4.5.1      flexmix_2.3-20     \n[10] vctrs_0.7.2         pkgconfig_2.0.3     fastmap_1.2.0      \n[13] backports_1.5.0     labeling_0.4.3      effectsize_1.0.2   \n[16] rmarkdown_2.31      tzdb_0.5.0          nloptr_2.2.1       \n[19] haven_2.5.5         ragg_1.5.2          xfun_0.57          \n[22] modeltools_0.2-24   jsonlite_2.0.0      sjmisc_2.8.11      \n[25] ggeffects_2.3.2     R6_2.6.1            stringi_1.8.7      \n[28] RColorBrewer_1.1-3  boot_1.3-32         lmtest_0.9-40      \n[31] numDeriv_2016.8-1.1 estimability_1.5.1  Rcpp_1.1.1         \n[34] knitr_1.51          zoo_1.8-15          DHARMa_0.4.7       \n[37] parameters_0.28.3   pacman_0.5.1        Matrix_1.7-5       \n[40] splines_4.5.1       timechange_0.4.0    tidyselect_1.2.1   \n[43] yaml_2.3.12         codetools_0.2-20    sjlabelled_1.2.0   \n[46] lattice_0.22-9      withr_3.0.2         bayestestR_0.17.0  \n[49] S7_0.2.1            coda_0.19-4.1       evaluate_1.0.5     \n[52] survival_3.8-6      xml2_1.5.2          pillar_1.11.1      \n[55] stats4_4.5.1        reformulas_0.4.4    insight_1.4.6      \n[58] generics_0.1.4      hms_1.1.4           scales_1.4.0       \n[61] minqa_1.2.8         xtable_1.8-8        glue_1.8.0         \n[64] tools_4.5.1         lme4_2.0-1          fs_2.0.1           \n[67] mvtnorm_1.3-6       grid_4.5.1          rbibutils_2.4.1    \n[70] datawizard_1.3.0    Formula_1.2-5       cli_3.6.5          \n[73] textshaping_1.0.5   gtable_0.3.6        sass_0.4.10        \n[76] digest_0.6.39       ucminf_1.2.2        ggrepel_0.9.8      \n[79] TH.data_1.1-5       htmlwidgets_1.6.4   farver_2.1.2       \n[82] htmltools_0.5.9     lifecycle_1.0.5     statmod_1.5.1      \n```\n\n\n:::\n:::\n\n\n## 参考文献\n\n::: {#refs}\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}