---
title: "混合効果モデル：2値ロジスティック回帰分析"
bibliography:
  - references_glm_gam.bib
  - references_glmm.bib
  - references_gtheory.bib
  - references_irt_rasch.bib
---

```{r}
#| label: required-packages
#| eval: false

required_packages <- c(
  "DHARMa",
  "broom",
  "broom.mixed",
  "dplyr",
  "emmeans",
  "forcats",
  "ggeffects",
  "ggplot2",
  "gt",
  "knitr",
  "lme4",
  "mclogit",
  "ordinal",
  "pROC",
  "pacman",
  "patchwork",
  "performance",
  "purrr",
  "readr",
  "scales",
  "simr",
  "sjPlot",
  "tibble",
  "tidyr",
  "tidyverse"
)

missing_packages <- setdiff(required_packages, rownames(installed.packages()))
if (length(missing_packages) > 0) {
  install.packages(missing_packages, repos = "https://cloud.r-project.org")
}
```


## はじめに

5章では GLM/GAM の使い分けを扱いました。次の実務課題は、
**データの独立性が崩れる場面**（ある観測の値が他の観測の値に影響しないという仮定が成り立たない場面）です。

英語教育データでは「同じ学習者が複数のペアに登場する」「同じテスト項目に複数の学習者が回答する」
といった**繰り返し構造**がほぼ必ず存在します。
このとき GLM を使うと、実際には独立でない観測を独立と見なすことになり、
標準誤差が過小評価され、p 値が実際より小さく出てしまいます（本当は有意でない効果が有意に見えてしまいます）[@baayen2008; @meteyard2020]。

この問題に対する標準解が
**一般化線形混合効果モデル（GLMM）**です。
GLMM は、報告したい効果（**固定効果**: 研究者が検証したい予測子の平均的な効果）と個人差・項目差（**ランダム効果**: 個人や項目ごとのばらつき）を
同時にモデル化することで、標準誤差を**正直に**推定します[@bates2015]。
SLA（第二言語習得）分野でも GLMM は標準ツールとなっており、
@cunnings2012 と @linck2015 が分野に特化したチュートリアルを提供しています。
2値言語データに対するロジット混合モデルの有効性も、
複数の方法論研究で一貫して示されてきました[@jaeger2008; @quene2008]。

本章では、実データを用いて 2値ロジスティック GLMM を
最小モデルの構築から診断・報告まで一貫した手順で実装します。

### この章の到達目標

- GLM と GLMM を同一データで比較し、ランダム効果の必要性を説明できる。
- 交差分類ランダム効果（sender / receiver）を実装できる。
- ICC（級内相関係数: グループ内の類似度）とデザインエフェクト（実効サンプルサイズの縮小率）を用いて、独立性仮定の破れを説明できる。
- OR と予測確率を併記して、読み手に伝わる結果提示ができる。
- 特異適合・過分散・残差の点検を、最小限の手順で実行できる。

### ファストトラック（最短ルート）

本章は 3,500 行以上の包括的なチュートリアルです。**以下のセクションを読めば、GLMM の実行・報告に必要な知識が得られます**：

| 順序 | セクション | 所要時間目安 |
|:---:|:---|:---:|
| 1 | GLMM が必要になる理由 | 5 分 |
| 2 | データ準備 → 分析データ作成 | 10 分 |
| 3 | 階層性の確認: ICC | 5 分 |
| 4 | ステップ 1-3: GLM → GLMM-RI → モデル比較 | 20 分 |
| 5 | 固定効果の解釈（OR）+ 応答スケール予測 | 15 分 |
| 6 | 診断（DHARMa + VIF） | 10 分 |
| 7 | 報告テンプレート | 5 分 |

**以下は 2 回目以降または必要に応じて参照**：コントラストコーディング、ランダム効果構造の理論、リンク関数の詳細、交互作用、検出力分析、技術的補足。

## GLMM が必要になる理由（最短版）

### GLM と GLMM の核心的な違い

GLM は**推定値そのもの**（β）を大きく変えるわけではありません。GLMM の本質は**標準誤差を正直にする**ことにあります。

- **GLM**: 全ペアが独立と仮定 → SE が過小 → p 値が楽観的
- **GLMM**: sender/receiver の繰り返しを考慮 → SE が適切 → p 値が正直

つまり、GLMM は「効果を見つける」ためではなく、「効果の不確かさを正しく評価する」ために使います。

### GLMM の数学的表現

2値 GLMM は次で表されます[@nelder1972; @bates2015]。

$$
g(p_{ij}) = \mathbf{x}_{ij}^\top\beta + \mathbf{z}_{ij}^\top b,
\quad b \sim \mathcal{N}(0, \Sigma)
$$

この式を日本語で読み下すと：

> **ペア $ij$ の助言確率 $p_{ij}$ をリンク関数 $g()$ で変換した値は、「固定効果（全員に共通の平均的効果 $\beta$）」と「ランダム効果（個人ごとの偏り $b$）」の和で表される。ランダム効果 $b$ は平均ゼロの正規分布から生成される。**

もう少し直感的に言えば：

- **固定効果 $\beta$**（報告したい効果）: 「同クラスだと助言しやすい」「スコアが高いと助言しやすい」など、研究で検証したい平均的な効果
- **ランダム効果 $b$**（統制したいばらつき）: 「田中さんは全般に助言をよくする」「鈴木さんは助言をあまり受けない」など、個人ごとの偏り

#### この式の理解が難しい場合

数式よりもコードから理解を深めたい場合は、以下の入門チュートリアルを先に参照することを推奨します。

1. @winter2013 — 言語学データの例で、線形モデルから混合モデルへの拡張を段階的に解説。
2. @brown2021 — 心理学データを使い、`lme4` での実装を一から追える R チュートリアル。
3. @debruine2021 — 「自分でデータを生成して、ランダム効果が何をしているか体感する」という独自のアプローチ。数式の意味が腑に落ちます。

いずれも本章の前提知識として有用ですが、**本章だけでも完結する設計**になっています。

#### 交差分類 vs 入れ子

**入れ子（nested）** と **交差分類（crossed）** は、ランダム効果の構造を正しく指定するための最重要概念です。

**入れ子（nested）**: あるグルーピング変数が、別のグルーピング変数の中にしか存在しない構造。

- 例：クラスA の「生徒1」とクラスB の「生徒1」は**別人**。ID番号がたまたま同じだけ。
- R実装：`(1 | class/student)` = `(1 | class) + (1 | class:student)`
- `(1 | class) + (1 | student)` と書くと、クラスAの生徒1とクラスBの生徒1を**同一人物**と見なしてしまうため、**誤り**。

**交差分類（crossed）**: どのグルーピング変数の水準も、他のグルーピング変数のどの水準とも組み合わさりうる構造。

- 例：本データでは sender と receiver が同じ 30 人のプールから出ている。sender 1 は receiver 2, 3, ... 29 と全員のペアを作る。
- R実装：`(1 | sender) + (1 | receiver)`
- 英語教育で最も多いパターン：「学習者 × テスト項目」「学習者 × 評価者」

**判別法**: 「グループ A の ID=1 とグループ B の ID=1 は同じ対象か？」→ Yes なら交差、No なら入れ子[@baayen2008; @barr2013; @quene2008]。

#### GLMM のもう一つの利点: 欠測データと不均衡デザインへの頑健性

GLMM は**ロング形式**（1 行 = 1 観測）でデータを扱います。ある学習者が 6 項目中 5 項目しか回答していなくても、回答した 5 項目はそのまま分析に使われます。欠測した 1 項目の行だけが除外されます。

反復測定 ANOVA では**ワイド形式**（1 行 = 1 参加者）を必要とし、1 項目でも欠測があればその参加者の**全データが除外**されます。GLMM はこの無駄を回避できるため、SLA 研究で頻出する脱落や無回答に対して頑健です。

また、GLMM は**不均衡デザイン**（参加者ごとの観測数が異なる）を自然に扱えます。これは反復測定 ANOVA では困難です[@baayen2008]。

### リンク関数の選択: logit / probit / cloglog

GLMM で 2 値アウトカムを扱う際、リンク関数（確率を線形予測子に変換する関数）を選択できます。実装上は `glmer(..., family = binomial(link = "..."))` を変えるだけですが、**係数の解釈がまったく異なる**ため、選択の理論的根拠を理解することが重要です。

#### 3 つのリンク関数の比較

```{r}
#| eval: true
#| label: link-function-comparison
#| fig-height: 4.5
#| fig-width: 9
#| echo: false

# このチャンクは setup より前に実行されるため、明示的にパッケージを読み込む
if (!requireNamespace("pacman", quietly = TRUE)) install.packages("pacman", repos = "https://cloud.r-project.org")

pacman::p_load(ggplot2)
base_font <- "HiraginoSans-W3"
theme_set(theme_bw(base_size = 12, base_family = base_font))
theme_update(legend.position = "bottom")

# 3 つのリンク関数の逆関数（確率曲線）を比較
eta <- seq(-4, 4, length.out = 200)

link_df <- data.frame(
  eta = rep(eta, 3),
  prob = c(
    plogis(eta),                    # logit: logistic CDF
    pnorm(eta),                     # probit: normal CDF
    1 - exp(-exp(eta))              # cloglog: complementary log-log
  ),
  Link = rep(c("Logit (logistic CDF)", "Probit (normal CDF)", "Cloglog"), each = length(eta))
)
link_df$Link <- factor(link_df$Link, levels = c("Logit (logistic CDF)", "Probit (normal CDF)", "Cloglog"))

ggplot(link_df, aes(x = eta, y = prob, color = Link, linetype = Link)) +
  geom_line(linewidth = 1.1) +
  geom_hline(yintercept = 0.5, linetype = "dotted", color = "gray50") +
  scale_color_manual(values = c("#E69F00", "#56B4E9", "#CC79A7")) +
  labs(title = "Inverse Link Functions: Probability as a Function of Linear Predictor",
       subtitle = "Logit and probit are nearly identical; cloglog is asymmetric",
       x = expression(paste("Linear predictor (", eta, " = X", beta, ")")),
       y = "P(Y = 1)") +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "top")
```

**この図の読み方**:

- **Logit（黄）と Probit（青）**: ほぼ重なる S 字カーブ。中央（η ≈ 0）付近で予測確率はほぼ同一。尾部で logit がわずかに重い裾を持つ（極端な確率でやや異なる）。
- **Cloglog（紫）**: 非対称。P(Y=1) が低い領域（左側）ではゆっくり上昇し、高い領域（右側）では急速に 1 に近づく。

#### 係数の解釈の違い {.tabset}

##### Logit → オッズ比（OR）

$$
\text{logit}(p) = \log\frac{p}{1-p} = \beta_0 + \beta_1 X
$$

- $\exp(\beta_1)$ = **オッズ比**（odds ratio）: X が 1 単位増えたときのオッズの倍率
- 例: $\beta_1 = 0.7$ → $\exp(0.7) = 2.01$ → 「X が 1 増えるとオッズが 2.01 倍」
- **最大の利点**: OR は確率の水準に依存しない一定の効果量指標

##### Probit → 潜在正規尺度の z スコア

$$
\Phi^{-1}(p) = \beta_0 + \beta_1 X
$$

- $\beta_1$ = X が 1 単位増えたときの、潜在正規変数の **z スコア**の変化量
- **OR に変換できない**: probit 係数を指数化しても意味のある量にならない
- **信号検出理論（SDT）との接続**: probit モデルで刺激条件（signal/noise）の係数は、そのまま $d'$（感度指標）として解釈可能[@decarlo1998]
- **項目反応理論との接続**: 正規オジブモデル（normal ogive model）は probit そのもの。IRT の 2PL は $P = \Phi(a(\theta - b))$ で、probit 回帰の特殊ケース

##### Cloglog → ハザード比

$$
\log(-\log(1-p)) = \beta_0 + \beta_1 X
$$

- $\exp(\beta_1)$ = **ハザード比**: 連続時間生存分析の Cox 比例ハザードモデルと等価[@prentice1978]
- 離散時間（例：学期ごとのドロップアウト）を扱う場合に理論的に適切[@singer2003]
- 応答確率が非対称な場合（低確率側はなだらか、高確率側は急激）に適合しやすい

#### Logit と Probit のスケーリング関係

logit 係数と probit 係数の間には近似的な換算関係があります：

$$
\beta_{\text{probit}} \approx \frac{\beta_{\text{logit}}}{1.7}
$$

これはロジスティック分布の標準偏差が $\pi/\sqrt{3} \approx 1.814$ であることに由来します（実用的には 1.7 で十分な近似）。つまり logit で $\beta = 0.85$ なら、probit では $\beta \approx 0.50$ になります。**効果の方向と有意性は同じ**ですが、数値の大きさが異なるため、混同に注意が必要です。

#### どのリンク関数を選ぶか

```{r}
#| eval: true
#| label: link-choice-guide
#| echo: false

# setup より前のチャンクのため明示的にパッケージを読み込む
pacman::p_load(gt)

tibble::tribble(
  ~場面, ~推奨リンク, ~理由, ~`係数の解釈`,
  "一般的な 2 値データ", "logit（デフォルト）", "OR 解釈が可能。最も広く使われる", "exp(β) = OR",
  "信号検出理論（SDT）分析", "probit", "係数 = d'（感度指標）[@decarlo1998]", "β = Δz on latent normal",
  "IRT 正規オジブモデル", "probit", "IRT の理論的基盤", "β = 識別力 × 能力差",
  "離散時間生存分析", "cloglog", "Cox PH の離散時間版[@prentice1978; @singer2003]", "exp(β) = hazard ratio",
  "反応確率の非対称性", "cloglog", "低確率→高確率の移行が急速な場合", "exp(β) = hazard ratio",
  "判断に迷った場合", "logit", "probit とほぼ同一の予測。OR の解釈性で優位", "exp(β) = OR"
) |>
  knitr::kable(
    caption = "リンク関数の選択ガイド: 理論的根拠がなければ logit がデフォルト"
  )
```

### 実務での判断（最短版）

1. **理論がリンクを指定する場合**: SDT → probit、離散時間ハザード → cloglog
2. **理論が中立なら**: logit（OR 解釈の利便性）
3. **「どちらが当てはまりが良いか」で選ばない**: logit と probit は AIC がほぼ同じになることが多く、当てはまりの差は実質的ではない。選択の根拠は理論的解釈性であるべき[@mccullagh1989]

本章の後半（ステップ②）で実際に 3 リンクを AIC/BIC で比較するコードを示しますが、上記の通り理論的根拠が第一です。

リンクの候補の詳細は `stats::family` / `stats::make.link` を参照してください
[@rstatsfamily; @rmakelink]。

## 実行前ガイド（実務フロー）

本章の分析手順は、**研究設計に繰り返し構造や交差分類がある時点で GLMM を本命**と考え、そのうえで GLM を比較用の基準線として当てる、という順序です。ICC / DEFF は「GLMM を使うかどうか」を機械的に決めるゲートではなく、**依存構造がどこから来て、どの程度の情報の目減りがありそうか**を説明するための記述指標です。そこで `isSingular()` を確認し、問題なければランダム傾きモデルを試行、特異適合が出るなら構造を簡素化してランダム切片モデルに戻します。最終的には AIC / LRT で候補モデルを比較し、OR 表と応答スケール予測、DHARMa と過分散テストによる最終診断を経て結果を報告します。

### モデル前提チェックリスト（GLMM）

GLMM の前提は「正規残差」ではなく、次の**構造的条件**を確認することです
[@bolker2009; @meteyard2020; @glmmfaq]。

1. `outcome` と `family/link` が整合しているか（本章は 2値 + logit）。
2. 群化構造（sender/receiver）が研究設計に一致しているか。
3. ランダム効果構造が過剰でないか（`isSingular()`, `rePCA()`）。
4. 過分散・残差の大きな破綻がないか（`DHARMa`, `check_overdispersion()`）。
5. 固定効果の推定が不安定でないか（`check_collinearity()`）。

#### よくある誤解：「残差が正規分布でないから問題がある」

GLM/GLMM では **アウトカムの正規性は仮定していません**。family（本章では `binomial`）がアウトカムの型と整合していれば、残差の正規性は本来問題になりません。上の 5 項目が GLMM の本当のチェックポイントです。

なお、混合モデルは分布仮定からの軽度な逸脱に対して比較的頑健であることが、大規模シミュレーション研究で確認されています[@schielzeth2020]。

#### 本章での対応関数

- 構造比較: `compare_performance()`, `anova(..., test="Chisq")`
- 特異適合: `isSingular()`, `performance::check_singularity()`, `rePCA()`
- 残差診断: `DHARMa::simulateResiduals()`, `testDispersion()`, `testUniformity()`
- 補助診断: `check_overdispersion()`, `check_collinearity()`, `check_model()`

## セットアップ

GLMM 分析に必要なパッケージを読み込みます。主要パッケージの役割：

| パッケージ | 役割 |
|-----------|------|
| `lme4` | GLMM の推定（`glmer()`）|
| `performance` | モデル比較、ICC、R²、診断（`compare_performance()`, `icc()`, `r2_nakagawa()`）|
| `emmeans` | 推定周辺平均と事後比較 |
| `ggeffects` | `ggpredict()` による調整済み予測値のデータフレーム化 |
| `DHARMa` | シミュレーションベースの残差診断 |
| `simr` | `powerSim()` / `powerCurve()` による混合モデルの検出力確認 |
| `sjPlot` | モデル比較表と予測プロット |

```{r}
#| eval: true
#| label: setup
#| message: false
#| warning: false

# --- パッケージ読み込み ---
# pacman::p_load() は未インストールのパッケージを自動でインストールしてから読み込む
pacman::p_load(
  tidyverse,
  lme4,
  mclogit,
  performance,
  emmeans,
  ggeffects,
  DHARMa,
  simr,
  sjPlot,
  patchwork,
  broom.mixed,
  pROC,
  gt
)

# --- 共通設定 ---
# ggplot2 のデフォルトテーマ
base_font <- "HiraginoSans-W3"
theme_set(theme_bw(base_size = 12, base_family = base_font))
theme_update(legend.position = "bottom")
set.seed(2024)      # 乱数の再現性を確保

# --- 自作ヘルパー関数 ---
# compare_models_gt: compare_performance() の結果を gt で整形表示
# AIC/BIC の最小値をハイライトし、ΔAIC を追加
compare_models_gt <- function(..., title = "Model Comparison") {
  comp <- performance::compare_performance(...)
  comp_df <- as.data.frame(comp)

  # ΔAIC / ΔBIC を計算（最小値からの差）
  if ("AIC" %in% names(comp_df)) {
    comp_df$delta_AIC <- comp_df$AIC - min(comp_df$AIC, na.rm = TRUE)
  }
  if ("BIC" %in% names(comp_df)) {
    comp_df$delta_BIC <- comp_df$BIC - min(comp_df$BIC, na.rm = TRUE)
  }

  tbl <- comp_df |>
    gt::gt() |>
    gt::tab_header(title = title)

  # AIC 列が存在すればフォーマット
  numeric_cols <- intersect(
    c("AIC", "BIC", "delta_AIC", "delta_BIC", "R2_conditional", "R2_marginal", "ICC", "RMSE", "Sigma"),
    names(comp_df)
  )
  if (length(numeric_cols) > 0) {
    tbl <- tbl |> gt::fmt_number(columns = dplyr::all_of(numeric_cols), decimals = 3)
  }

  # ΔAIC = 0（最良モデル）の行をハイライト
  if ("delta_AIC" %in% names(comp_df)) {
    best_row <- which(comp_df$delta_AIC == 0)
    if (length(best_row) > 0) {
      tbl <- tbl |>
        gt::tab_style(
          style = gt::cell_fill(color = "#E8F5E9"),
          locations = gt::cells_body(rows = best_row)
        )
    }
  }

  tbl
}

plot_check_model_separate <- function(
  model,
  model_name = deparse(substitute(model)),
  checks = c("binned_residuals", "homogeneity", "outliers", "vif"),
  font_family = base_font  # グローバル変数に依存せず引数で受け取る
) {
  # check_model を直接呼び出す（エラーを隠さない）
  # 2値 GLMM では "qq"/"normality"/"linearity" は不適切
  # 代わりに "binned_residuals" を使用（2値データ用の残差プロット）
  diagnostic_bundle <- performance::check_model(model, panel = FALSE, check = checks)
  diagnostic_plots  <- plot(diagnostic_bundle)

  purrr::iwalk(diagnostic_plots, function(p, nm) {
    p_out <- p +
      theme_bw(base_size = 12, base_family = font_family) +
      theme(
        legend.position = "bottom",
        plot.title = element_text(face = "bold", size = 12),
        axis.title = element_text(size = 11),
        axis.text = element_text(size = 10)
      ) +
      labs(caption = paste("Model:", model_name, "| Check:", nm))
    print(p_out)
  })

  invisible(diagnostic_plots)
}
```

### `compare_models_gt()` とは？（クリックで展開）

`compare_models_gt()` は本章で使うヘルパー関数です。`performance::compare_performance()` の結果を `gt` テーブルに整形し、ΔAIC（最良モデルからの AIC の差）を追加、最良モデルの行を緑でハイライトします。

## データ準備（実データ）

本章は `data/network` の実データを使います。

- `nodes.csv`: 助言ネットワークの学習者属性（`id`, `class`, `proficiency`）
- `edges_directed_advice.csv`: 有向助言関係（from → to）
- `nodes_collaboration.csv`: ベースライン得点（共変量に使用）

以下のコードでは、ノード属性と助言エッジを読み込み、`proficiency` を順序つきファクター（A2 < B1 < B2）に変換し、ベースラインスコアを標準化（z スコア）します。`factor()` への変換は、後に `glmer()` がグループ変数を正しく認識するために必要です。

```{r}
#| eval: true
#| label: data-load

# ── CSV ファイルの読み込み ──
nodes_advice <- readr::read_csv("data/network/nodes.csv",
                                show_col_types = FALSE)

nodes_collab <- readr::read_csv("data/network/nodes_collaboration.csv",
                                show_col_types = FALSE) |>
  dplyr::select(id, baseline_score)  # 必要な列だけ選択

edges_advice <- readr::read_csv("data/network/edges_directed_advice.csv",
                                show_col_types = FALSE)

# ── 変数の前処理 ──
prof_levels <- c("A2", "B1", "B2")          # 順序を定義
prof_map    <- c(A2 = 1, B1 = 2, B2 = 3)   # 数値に変換するマップ

nodes <- nodes_advice |>
  left_join(nodes_collab, by = "id") |>     # ベースラインスコアを結合
  mutate(
    class = factor(class),                                        # ファクター化
    proficiency = factor(proficiency, levels = prof_levels),       # 順序付きファクター
    prof_num = unname(prof_map[as.character(proficiency)]),        # 数値化（差の計算用）
    baseline_score_sc = as.numeric(scale(baseline_score))          # z 標準化
  )

head(nodes, 8) |>
  knitr::kable(caption = "Node Data Overview: First 8 Rows")
```

`summary()` の出力で確認すべき点：

- **class**: 各クラスの人数バランス。極端な偏りがあるとランダム効果の推定が不安定になる。
- **proficiency**: A2/B1/B2 の分布。3水準が揃っているか。
- **baseline_score_sc**: 平均≈0、SD≈1 になっていれば標準化は正常。

### なぜ連続変数を中心化（標準化）するのか

上のコードで `baseline_score` を `scale()` で z 標準化しました（平均 = 0, SD = 1 に変換）。これは GLMM で連続予測子を扱う際の**標準的な前処理**です。

```{r}
#| eval: true
#| label: centering-demo
#| fig-height: 5
#| fig-width: 10

# ── 中心化の効果を可視化 ──
# 生のスコアと z 標準化後のスコアを比較
raw_score  <- nodes$baseline_score
z_score    <- nodes$baseline_score_sc

centering_df <- data.frame(
  Score = c(raw_score, z_score),
  Type  = rep(c("Raw (original scale)", "Z-standardized (centered)"),
              each = length(raw_score))
)

p_hist <- ggplot(centering_df, aes(x = Score, fill = Type)) +
  geom_histogram(bins = 12, alpha = 0.7, position = "identity") +
  geom_vline(data = data.frame(
    Type = c("Raw (original scale)", "Z-standardized (centered)"),
    xint = c(mean(raw_score), 0)
  ), aes(xintercept = xint), linetype = "dashed", linewidth = 0.8) +
  facet_wrap(~Type, scales = "free_x") +
  scale_fill_manual(values = c("#E69F00", "#56B4E9")) +
  labs(title = "Centering: Before and After Z-standardization",
       subtitle = "Dashed line = mean. After centering, mean = 0 and SD = 1.",
       x = "Score", y = "Count") +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "none")

print(p_hist)
```

**中心化が重要な 3 つの理由**:

```{r}
#| eval: true
#| label: centering-reasons
#| echo: false

tibble::tribble(
  ~理由, ~中心化なし, ~中心化あり,
  "切片の解釈", "切片 = スコアが 0 のときの予測値（0 点の学習者は存在しないため無意味）", "切片 = スコアが平均的な学習者の予測値（解釈しやすい）",
  "収束性", "大きな値（例: 80点）がログオッズに直接入り、数値的に不安定になりやすい", "値が -2〜+2 の範囲に収まり、収束が安定する",
  "ランダム傾きとの関係", "切片とランダム傾きの相関が人工的に高くなる（スケーリング由来の偽相関）", "偽相関が解消され、ランダム効果の推定が正確になる"
) |>
  knitr::kable(
    caption = "中心化（z標準化）の3つの利点: GLMM では連続予測子の中心化はほぼ必須"
  )
```

#### 中心化の実務ルール

- **連続変数**: 必ず `scale()` で z 標準化する。これにより全連続予測子が同じスケール（SD 単位）になり、効果量の比較も容易になる。
- **2 値変数**（0/1）: 中心化は任意。sum coding（-0.5/+0.5）にすると切片が全体平均になる（コントラストコーディング節参照）。
- **カウント変数**: `log()` 変換 + `scale()` が一般的。
- **注意**: `scale()` は元のスケールを変えるため、係数の解釈が「1 SD あたりの変化」になる。元のスケール（例: 1 点あたり）で解釈したい場合は、平均を引くだけの中心化（`x - mean(x)`）を使う。

## 分析データ作成（有向ペア単位）

`sender != receiver` の全有向ペアを作り、
各ペアが実際に助言エッジとして観測されたか（`tie`）を 0/1 で定義します。

**変数名のポイント**: `sender`/`receiver` という命名にしておくと、後の `glmer()` 式で `(1 | sender) + (1 | receiver)` と書くだけで交差分類ランダム効果が実装でき、コードの可読性が上がります。また、`factor()` への変換は `lme4` がランダム効果のグループ変数を正しく認識するために必須です。

```{r}
#| eval: true
#| label: make-analysis-data

# ── 全有向ペアを生成（自分自身は除外）──
dat_bin <- tidyr::crossing(
  sender   = nodes$id,
  receiver = nodes$id
) |>
  dplyr::filter(sender != receiver) |>

  # sender 側の属性を結合
  left_join(
    nodes |>
      dplyr::select(sender = id, class_sender = class,
                    prof_sender = proficiency, prof_sender_num = prof_num,
                    score_sender_sc = baseline_score_sc),
    by = "sender"
  ) |>

  # receiver 側の属性を結合
  left_join(
    nodes |>
      dplyr::select(receiver = id, class_receiver = class,
                    prof_receiver = proficiency, prof_receiver_num = prof_num,
                    score_receiver_sc = baseline_score_sc),
    by = "receiver"
  ) |>

  mutate(
    # ── 予測子の作成 ──
    same_class = as.integer(class_sender == class_receiver),  # 同クラスなら 1
    prof_gap   = abs(prof_sender_num - prof_receiver_num),    # 熟達度差（0, 1, 2）
    score_gap  = abs(score_sender_sc - score_receiver_sc),    # スコア差（連続量）

    # ── アウトカム：助言エッジがあるか ──
    tie = as.integer(
      paste(sender, receiver) %in% paste(edges_advice$from, edges_advice$to)
    ),

    # ── glmer のランダム効果に必要なファクター化 ──
    sender   = factor(sender),
    receiver = factor(receiver)
  )

# ── データの概要を確認 ──
tibble(
  `全ペア数`     = nrow(dat_bin),
  `助言あり`     = sum(dat_bin$tie),
  `ベースレート` = mean(dat_bin$tie) |> round(3)
)
```

出力の読み方：

| 指標 | 確認ポイント |
|------|------------|
| `全ペア数` | 全有向ペア数 = $n \times (n-1)$（n は学習者数）。30人なら 870 行 |
| `助言あり` | 実際に助言関係があったペア数 |
| `ベースレート` | 助言関係の割合。**極端に低い**（< 0.05）場合は完全分離のリスクあり |

### ベースレートと依存構造

`tie_rate` が低いということは、大半のペアで `tie = 0` であることを意味します。このようなスパースなデータでは、各学習者が複数のペアに繰り返し出現するという依存構造（sender が 29 回、receiver が 29 回出現）が推定に大きく影響します。GLM はこの依存を無視するため、標準誤差が過小評価される典型的な場面です。

## カテゴリカル変数のコーディング（コントラスト）

分析データが準備できたので、次にモデルに投入するカテゴリ変数の扱い方を決めます。コーディングの選択は係数の解釈に直結するため、**モデル推定の前に**理解しておくことが重要です。

### なぜコーディングが重要か

**同じデータ・同じモデルでも、コーディング方式が異なれば係数の解釈がまったく変わります**。特に交互作用を含むモデルでは、コーディングの選択を誤ると「主効果がある」という誤った結論を導くことがあります[@schad2020; @brehm2022]。@brehm2022 が心理言語学の 3,384 本の論文を調査したところ、**6割以上がコーディング方式を明記していなかった**ことが判明しています。再現性の観点からも、コーディングの明示的な報告は必須です。

### コーディングとは何か

回帰モデルにカテゴリカル変数を投入する際、R は内部でそのカテゴリを**数値に変換**します。この変換規則を**コントラスト（対比）**と呼びます。異なるコントラストは、同じモデルの当てはまり（AIC, 対数尤度）を変えませんが、**各係数が何を意味するか**を変えます[@winter2019; @vasishth2022]。

### 主要な 4 つのコーディング方式

```{r}
#| eval: true
#| label: contrast-overview
#| echo: false

tibble::tribble(
  ~方式, ~`R関数`, ~`切片の意味`, ~`係数が検定するもの`, ~直交,
  "Treatment (dummy)", "contr.treatment()", "参照水準の平均", "各水準 vs 参照水準", "No",
  "Sum (effect/deviation)", "contr.sum()", "全体平均（grand mean）", "各水準 vs 全体平均", "No",
  "Helmert", "contr.helmert()", "全体平均", "各水準 vs それ以前の水準の平均", "Yes",
  "Polynomial", "contr.poly()", "全体平均", "線形・二次・三次…のトレンド", "Yes"
) |>
  knitr::kable(
    caption = "主要なコントラスト方式の比較（R のデフォルト: 非順序因子 = treatment, 順序因子 = polynomial）"
  )
```

### Treatment（ダミー）コーディング — R のデフォルト

R で `factor()` を使うと、自動的に **treatment coding**（ダミーコーディング）が適用されます。これは「参照水準を 0、それ以外を 1」にする最も馴染みのあるコーディングです。

```{r}
#| eval: true
#| label: contrast-treatment-demo

# 3水準の因子: A, B, C（A が参照水準）
demo_factor <- factor(c("A", "B", "C"))
contr.treatment(3)  # 3 水準の treatment coding 行列

# R のデフォルトで factor を作ると treatment coding が適用される
contrasts(demo_factor)
```

**解釈**: 切片 = A（参照水準）の平均。`B` の係数 = B と A の差。`C` の係数 = C と A の差。

#### Treatment coding の危険性：交互作用の解釈

Treatment coding では、「主効果」の係数は実際には **単純効果**（simple effect）です。つまり、「もう一方の因子が参照水準のときだけの効果」を表しています[@schad2020]。交互作用が存在する場合、これを「主効果」と報告するのは誤りです。

### Sum（効果）コーディング — 交互作用があるときの推奨

Sum coding では、各水準を **+1 / -1** でコーディングし、切片が**全体平均（grand mean）**を表すようにします。これにより、係数は ANOVA と同様の「主効果」として解釈できます。

```{r}
#| eval: true
#| label: contrast-sum-demo

# Sum coding: 最後の水準が -1 になる
contr.sum(3)

# 因子に適用
demo_factor_sum <- demo_factor
contrasts(demo_factor_sum) <- contr.sum(3)
contrasts(demo_factor_sum)
```

**解釈**: 切片 = 全体平均。`[,1]` の係数 = A の平均と全体平均の差。`[,2]` の係数 = B の平均と全体平均の差。C の効果は -(A の効果 + B の効果) で暗黙的に決まります。

### シミュレーションで理解する：コーディングが結論を変える {.tabset}

以下のシミュレーションは、**交差交互作用**（crossover interaction）がある場合に、treatment coding と sum coding で「主効果」の解釈がまったく異なることを示します[@brehm2022; @vasishth2022]。

#### Step 1: データ生成

```{r}
#| eval: true
#| label: contrast-sim-data
#| fig-height: 4.5
#| fig-width: 7

set.seed(2024)

# ── シミュレーション: 食事速度 ──
# 2x2 要因計画: Utensil (fork/spoon) × Food (salad/soup)
# 真の構造: 交差交互作用のみ（主効果なし）
n_per_cell <- 50
sim_data <- data.frame(
  utensil = rep(c("fork", "spoon"), each = 2 * n_per_cell),
  food    = rep(rep(c("salad", "soup"), each = n_per_cell), 2)
)
mu <- c(fork_salad = 4, fork_soup = 2, spoon_salad = 2, spoon_soup = 4)
sim_data$speed <- rnorm(nrow(sim_data),
                        mean = mu[paste(sim_data$utensil, sim_data$food, sep = "_")],
                        sd = 1)

# ── セル平均の可視化（真の構造を確認）──
cell_means <- aggregate(speed ~ utensil + food, data = sim_data, FUN = mean)

ggplot(cell_means, aes(x = food, y = speed, color = utensil, group = utensil)) +
  geom_point(size = 4) +
  geom_line(linewidth = 1.2) +
  geom_hline(yintercept = mean(sim_data$speed), linetype = "dashed", color = "gray50") +
  annotate("text", x = 0.6, y = mean(sim_data$speed) + 0.15,
           label = "Grand Mean", color = "gray50", size = 3.5) +
  scale_color_manual(values = c(fork = "#E69F00", spoon = "#56B4E9")) +
  labs(title = "Crossover Interaction: No Main Effects",
       subtitle = "Lines cross → interaction only; grand mean ≈ cell means average",
       x = "Food", y = "Eating Speed", color = "Utensil") +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "top")
```

**この図の読み方**: 2 本の線が**交差**しています。fork は salad で速く soup で遅い。spoon は逆。線が交差するということは、utensil の効果が food によって反転する = **交互作用のみで主効果はない**構造です。点線は全体平均（grand mean）を示しています。

#### Step 2: コントラスト行列の可視化

```{r}
#| eval: true
#| label: contrast-matrix-heatmap
#| fig-height: 3.5
#| fig-width: 8

# コントラスト行列をヒートマップで可視化
# tidyr は tidyverse 経由で読み込み済み

show_contrast <- function(mat, title) {
  df <- as.data.frame(mat)
  df$Level <- rownames(df)
  long <- pivot_longer(df, -Level, names_to = "Contrast", values_to = "Weight")
  long$Level <- factor(long$Level, levels = rev(rownames(mat)))

  ggplot(long, aes(x = Contrast, y = Level, fill = Weight)) +
    geom_tile(color = "white", linewidth = 1) +
    geom_text(aes(label = sprintf("%.1f", Weight)), size = 5, fontface = "bold") +
    scale_fill_gradient2(low = "#2166AC", mid = "white", high = "#B2182B",
                         midpoint = 0, limits = c(-1.1, 1.1)) +
    labs(title = title) +
    theme_minimal(base_size = 12, base_family = base_font) +
    theme(legend.position = "none",
          axis.title = element_blank(),
          panel.grid = element_blank())
}

p1 <- show_contrast(contr.treatment(2), "Treatment\n(0 vs 1)")
p2 <- show_contrast(contr.sum(2), "Sum\n(-1 vs +1)")
p3 <- show_contrast(matrix(c(-0.5, 0.5), ncol = 1, dimnames = list(1:2, "1")),
                    "Sum (centered)\n(-0.5 vs +0.5)")

patchwork::wrap_plots(p1, p2, p3, nrow = 1) +
  patchwork::plot_annotation(
    title = "Contrast Matrices Compared",
    subtitle = "Color = weight direction; number = coefficient weight"
  )
```

**この図の読み方**: 各パネルが 1 つのコーディング方式。**赤 = 正の重み、青 = 負の重み、白 = ゼロ**。Treatment coding では参照水準が 0（白）で、他が 1（赤）。Sum coding では対称的に -1（青）と +1（赤）。Centered sum coding（-0.5/+0.5）は効果の大きさが直接解釈しやすい形です。

#### Step 3: 係数の比較（フォレストプロット）

```{r}
#| eval: true
#| label: contrast-forest-plot
#| fig-height: 5
#| fig-width: 9

# ── 3 つのコーディングで推定 ──
sim_data$utensil_f <- factor(sim_data$utensil)
sim_data$food_f    <- factor(sim_data$food)

# Treatment coding（R デフォルト）
m_treat <- lm(speed ~ utensil_f * food_f, data = sim_data)

# Sum coding
sim_data$utensil_sum <- sim_data$utensil_f
sim_data$food_sum    <- sim_data$food_f
contrasts(sim_data$utensil_sum) <- contr.sum(2)
contrasts(sim_data$food_sum)    <- contr.sum(2)
m_sum <- lm(speed ~ utensil_sum * food_sum, data = sim_data)

# Helmert coding
sim_data$utensil_helm <- sim_data$utensil_f
sim_data$food_helm    <- sim_data$food_f
contrasts(sim_data$utensil_helm) <- contr.helmert(2)
contrasts(sim_data$food_helm)    <- contr.helmert(2)
m_helm <- lm(speed ~ utensil_helm * food_helm, data = sim_data)

# ── フォレストプロット ──
tidy_and_label <- function(mod, label) {
  df <- broom::tidy(mod, conf.int = TRUE)
  df$model <- label
  df$term_clean <- c("Intercept", "Utensil", "Food", "Utensil × Food")
  df
}

all_coefs <- rbind(
  tidy_and_label(m_treat, "Treatment"),
  tidy_and_label(m_sum,   "Sum"),
  tidy_and_label(m_helm,  "Helmert")
)

# 切片を除外し、効果のみ比較
effects_only <- all_coefs[all_coefs$term_clean != "Intercept", ]

ggplot(effects_only, aes(x = estimate, y = term_clean, color = model)) +
  geom_vline(xintercept = 0, linetype = "dashed", color = "gray60") +
  geom_pointrange(aes(xmin = conf.low, xmax = conf.high),
                  position = position_dodge(width = 0.5), size = 0.8) +
  scale_color_manual(values = c(Treatment = "#D55E00", Sum = "#009E73", Helmert = "#0072B2")) +
  labs(title = "Forest Plot: Same Data, Different Coefficient Interpretations",
       subtitle = "Utensil & Food: true main effect = 0. Only the interaction is real.",
       x = "Coefficient Estimate (95% CI)", y = NULL, color = "Coding") +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "top")
```

**この図の読み方**:

- **Utensil と Food の行**: Treatment coding（赤）だけが 0 から大きく離れている = 有意に見える。Sum（緑）と Helmert（青）は 0 付近 = 正しく非有意。
- **Utensil × Food の行**: 3 方式とも同程度の大きな効果 = 交互作用はコーディングに依存しない（ただし大きさは異なる: Helmert は非スケール版のため値が大きい）。
- **重要**: Treatment coding の Utensil/Food の係数は「主効果」ではなく「参照水準における単純効果」。

#### Step 4: 係数の意味の図解

```{r}
#| eval: true
#| label: contrast-meaning-plot
#| fig-height: 5
#| fig-width: 10

# 係数が「何を比較しているか」を矢印で図示
# patchwork は setup で読み込み済み

base_plot <- ggplot(cell_means, aes(x = food, y = speed, color = utensil, group = utensil)) +
  geom_point(size = 4) +
  geom_line(linewidth = 1) +
  scale_color_manual(values = c(fork = "#E69F00", spoon = "#56B4E9")) +
  theme_bw(base_size = 11, base_family = base_font) +
  theme(legend.position = "none") +
  ylim(1, 5)

# Treatment coding: 係数は参照水準（fork, salad）からの差
p_treat <- base_plot +
  # Utensil 係数: spoon - fork, at salad (参照水準)
  annotate("segment", x = 0.85, xend = 0.85,
           y = cell_means$speed[cell_means$utensil=="fork" & cell_means$food=="salad"],
           yend = cell_means$speed[cell_means$utensil=="spoon" & cell_means$food=="salad"],
           arrow = arrow(length = unit(0.2, "cm")), color = "red", linewidth = 1.2) +
  annotate("text", x = 0.7, y = 3, label = "β[utensil]", color = "red",
           size = 4, fontface = "bold", parse = TRUE) +
  # Food 係数: soup - salad, at fork (参照水準)
  annotate("segment", x = 1.15, xend = 1.85,
           y = cell_means$speed[cell_means$utensil=="fork" & cell_means$food=="salad"],
           yend = cell_means$speed[cell_means$utensil=="fork" & cell_means$food=="soup"],
           arrow = arrow(length = unit(0.2, "cm")), color = "purple", linewidth = 1.2) +
  annotate("text", x = 1.5, y = 4.3, label = "β[food]", color = "purple",
           size = 4, fontface = "bold", parse = TRUE) +
  labs(title = "Treatment Coding", subtitle = "Coefficients = simple effects at reference level")

# Sum coding: 係数は全体平均からの偏差
grand_mean <- mean(sim_data$speed)
p_sum <- base_plot +
  geom_hline(yintercept = grand_mean, linetype = "dashed", color = "gray40") +
  annotate("text", x = 2.3, y = grand_mean + 0.15, label = "Grand Mean",
           color = "gray40", size = 3.5) +
  annotate("segment", x = 1.5, xend = 1.5, y = grand_mean,
           yend = mean(cell_means$speed[cell_means$utensil == "fork"]),
           arrow = arrow(length = unit(0.2, "cm")), color = "red", linewidth = 1.2) +
  annotate("text", x = 1.35, y = grand_mean + 0.4, label = "β[utensil] ≈ 0",
           color = "red", size = 4, fontface = "bold") +
  labs(title = "Sum Coding", subtitle = "Coefficients = deviation from grand mean")

p_treat + p_sum +
  plot_annotation(title = "What Each Coefficient Actually Measures",
                  theme = theme(plot.title = element_text(face = "bold", size = 14)))
```

**この図の読み方**:

- **左（Treatment）**: 赤矢印 = 「salad のときの fork→spoon の差」（単純効果）。紫矢印 = 「fork のときの salad→soup の差」（単純効果）。どちらも参照水準に限定した比較。
- **右（Sum）**: 赤矢印 = 「fork の平均と全体平均の差」≈ 0（正しい主効果 = ゼロ）。線が交差しているため、全体平均からの偏差はほぼゼロ。

### このシミュレーション全体が示すこと

1. **同じデータ・同じモデル適合**でも、コーディングで「主効果」の有意性が変わる（Step 3 のフォレストプロット）。
2. **Treatment coding の「主効果」は単純効果**であり、交互作用がある場合に誤解を招く（Step 4 の矢印図）。
3. **交互作用を含むモデルでは sum coding を使うべき**[@schad2020]。
4. ただし、**emmeans による下位検定はコーディングに依存しない**（次の Step 5 で実証）。

### `emmeans` はコーディングに依存しない（重要）

「コーディングで結論が変わるなら、どれを信用すればいいのか？」という疑問が生じます。ここで重要な事実があります：**`emmeans` による推定周辺平均とそのペアワイズ比較は、コーディング方式に関係なく完全に同一の結果を返します**。

```{r}
#| eval: true
#| label: contrast-emmeans-invariance

# 上のシミュレーションデータで emmeans を比較
# emmeans は setup で読み込み済み

# Treatment coding のモデルで emmeans
emm_treat <- emmeans(m_treat, ~ utensil_f | food_f)

# Sum coding のモデルで emmeans
emm_sum <- emmeans(m_sum, ~ utensil_sum | food_sum)

# 推定値を並べて比較
tibble::tibble(
  Condition = c("fork|salad", "spoon|salad", "fork|soup", "spoon|soup"),
  `Treatment coding` = sprintf("%.3f", summary(emm_treat)$emmean),
  `Sum coding`        = sprintf("%.3f", summary(emm_sum)$emmean),
  `一致?` = ifelse(
    abs(summary(emm_treat)$emmean - summary(emm_sum)$emmean) < 0.001,
    "\u2705", "\u274c")
) |>
  knitr::kable(
    caption = "emmeans はコーディングに依存しない: どちらのコーディングでも推定周辺平均は完全に一致"
  )
```

### なぜ emmeans は同じ結果になるのか

`emmeans` はモデルの**予測値**（各セルの期待値）から逆算して周辺平均を求めます。予測値はコーディングに依存しません（モデルの当てはまりが同一なので）。コーディングが変えるのは β の「意味」だけであり、モデルが表現する関数そのものは同一です。

**実務的含意**:

- **回帰表（β, SE, p）を直接報告する場合** → コーディングの選択が結論に直結。sum coding を推奨。
- **emmeans で下位検定して報告する場合** → コーディングは結論に影響しない。ただし、β を報告する場合の解釈のために、コーディング方式は必ず明記する。

### その他のコーディング方式 {.tabset}

#### Helmert — 順次比較

Helmert coding は「各水準をそれ以前の全水準の平均と比較する」直交コントラストです。例えば 3 群（統制群、介入A、介入B）のとき：

- コントラスト 1: 介入A vs 統制群
- コントラスト 2: 介入B vs (統制群 + 介入A) の平均

```{r}
#| eval: true
#| label: contrast-helmert-demo

# Helmert coding（R は非スケール版を返す）
contr.helmert(3)

# スケーリングすると解釈しやすい
# 列1: B vs A → -1/2, 1/2, 0
# 列2: C vs mean(A,B) → -1/3, -1/3, 2/3
```

::: {.callout-note}
**注意**: R の `contr.helmert()` は**非スケール版**を返します。p 値は正しいですが、係数の大きさを解釈する際には手動でスケーリングが必要です。実用的には `emmeans::contrast(emm, method = "consec")` で逐次比較を行うほうが解釈しやすいです。
:::

#### Polynomial — 順序変数のトレンド検出

R で `ordered()` を使うと、自動的に**多項式コントラスト**（`contr.poly()`）が適用され、線形トレンド、二次（U字型）トレンドなどを検出できます。

```{r}
#| eval: true
#| label: contrast-poly-demo

# 4水準の順序因子: 初級 < 中級 < 上級 < 超上級
prof_demo <- ordered(c("beginner", "intermediate", "advanced", "expert"),
                       levels = c("beginner", "intermediate", "advanced", "expert"))
contrasts(prof_demo)
```

| コントラスト | 検出するパターン | 英語教育での例 |
|:---:|:---|:---|
| `.L` (linear) | 水準が上がるほど一方向に変化 | 熟達度が上がるほど正答率が上がる |
| `.Q` (quadratic) | U字型またはΛ字型 | 中級者が最も流暢さに問題を感じる（中間層の落ち込み） |
| `.C` (cubic) | S字型 | 初級→中級で急伸、中級→上級で停滞、上級→超上級で再加速 |

::: {.callout-warning}
#### 多項式コントラストの前提と限界

1. **等間隔の前提**: `contr.poly()` は水準間の間隔が等しいと仮定します。「初級・中級・上級」の熟達度差が等間隔でない場合、結果は歪みます。
2. **高次項の過適合**: 3次以上のトレンドは解釈が困難で、サンプルサイズが小さいと過適合のリスクがあります。
3. **代替手段**: 等間隔でない場合は `MASS::contr.sdif()`（逐次差分コーディング）が有用です[@schad2020]。これは「隣接する水準間の差」を直接検定します。
:::

#### Successive Difference — 不等間隔の順序変数

等間隔でない順序変数には `MASS::contr.sdif()` が有用です。隣接する水準間の差を直接検定します[@schad2020]。

```{r}
#| eval: true
#| label: contrast-sdif-demo

# 逐次差分コーディング（隣接水準間の差を検定）
MASS::contr.sdif(4)
# 列1: intermediate - beginner
# 列2: advanced - intermediate
# 列3: expert - advanced
```

「初級→中級」の伸びと「上級→超上級」の伸びが異なるかを直接比較できるため、英語教育研究で順序付き熟達度を扱う際に特に有用です。

### 実務での選択ガイド

```{r}
#| eval: true
#| label: contrast-decision-guide
#| echo: false

tibble::tribble(
  ~研究の状況, ~推奨コーディング, ~理由,
  "2群比較（処理 vs 統制）", "Sum coding (-0.5, +0.5)", "切片 = 全体平均、係数 = 群間差の半分",
  "交互作用を含む要因計画", "Sum coding", "ANOVA 的な主効果の解釈が可能[@schad2020]",
  "特定の参照群との比較", "Treatment coding", "「統制群と比べて各群がどう異なるか」を直接検定",
  "順序付き水準（等間隔）", "Polynomial (contr.poly)", "線形・二次トレンドの検出",
  "順序付き水準（不等間隔）", "Successive diff (contr.sdif)", "隣接水準間の差を検定",
  "順次的な群の追加", "Helmert coding", "「新しい群は既存群の平均と異なるか」を検定"
) |>
  knitr::kable(caption = "コーディング方式の選択ガイド")
```

### 本章データへの適用

本データの `same_class` は 2 値（0/1）です。2 水準の場合、treatment coding（0, 1）と sum coding（-0.5, +0.5）の違いは以下の通りです：

- **Treatment**: 切片 = 異クラスペアの平均ログオッズ。`same_class` の係数 = 同クラスと異クラスの差。
- **Sum (-0.5, +0.5)**: 切片 = 全体平均のログオッズ。`same_class` の係数 = 同クラスと異クラスの差（同じ値）。

2 水準で交互作用がない場合、両者の `same_class` 係数は同一です。**ただし**、将来的に交互作用（例：`same_class × prof_gap`）を追加する予定がある場合は、最初から sum coding を使うことを推奨します。

```{r}
#| eval: true
#| label: contrast-apply-to-data

# 本データで sum coding を適用する例（実行のみ、以降の分析には影響しない）
dat_bin_sum <- dat_bin
# same_class は整数型（0/1）なので、まず因子に変換してからコントラストを設定
dat_bin_sum$same_class_sum <- factor(dat_bin_sum$same_class)
contrasts(dat_bin_sum$same_class_sum) <- contr.sum(2)

# Treatment vs Sum の係数比較
m_treat_demo <- glm(tie ~ same_class + prof_gap, family = binomial, data = dat_bin)
m_sum_demo   <- glm(tie ~ same_class_sum + prof_gap, family = binomial, data = dat_bin_sum)

tibble::tibble(
  Term = c("Intercept", "same_class / same_class_sum", "prof_gap"),
  Treatment = sprintf("%.3f (SE = %.3f)", coef(m_treat_demo), summary(m_treat_demo)$coefficients[,2]),
  Sum       = sprintf("%.3f (SE = %.3f)", coef(m_sum_demo), summary(m_sum_demo)$coefficients[,2])
) |>
  knitr::kable(
    caption = "Treatment vs Sum coding: 本データでの比較（2水準・交互作用なしでは same_class の係数は同一）"
  )
```

### コーディングの報告チェックリスト

論文では以下を必ず明記してください[@brehm2022]：

1. **使用したコーディング方式**（例："Sum coding was used for all categorical predictors"）
2. **参照水準**（treatment coding の場合）
3. **順序因子の扱い**（polynomial か successive difference か）
4. **理由**（例："to ensure that coefficients reflect main effects rather than simple effects"）

報告例:

> Categorical predictors were sum-coded (−0.5/+0.5) following recommendations by Schad et al. (2020), so that regression coefficients reflect main effects averaged across levels of other factors. Proficiency level was coded with successive difference contrasts (MASS::contr.sdif) to test differences between adjacent levels.

## ランダム効果構造の選択（理論と実践）

コーディングの選択を理解したところで、次にもう 1 つの重要な設計上の判断——**ランダム効果構造の選択**——を扱います。

### なぜランダム効果構造が重要か

ランダム効果構造の選択は、GLMM の推定結果に直接影響します。**ランダム傾きを省略すると、第一種の過誤率（偽陽性率）が深刻に上昇する**ことがシミュレーション研究で繰り返し示されています[@barr2013; @schielzeth2009]。一方で、データが支持しない過剰なランダム効果を含めると、収束失敗や検出力の低下を招きます[@bates2015parsimonious; @matuschek2017]。

### 基本原則: 実験デザインが構造を決める

ランダム効果構造を決める最も重要な原則は：

> **予測子 X がグルーピング変数 G の水準の「中で」変動する場合に限り、X の G によるランダム傾きを含めることができる**[@barr2013; @brauer2018]。

つまり、**within-participant（参加者内）か between-participant（参加者間）かが、ランダム傾きの可否を決定**します。

```{r}
#| eval: true
#| label: re-design-table
#| echo: false

tibble::tribble(
  ~デザイン, ~定義, ~`英語教育での例`, ~`ランダム傾き`,
  "Within-participant", "全参加者が全条件を経験", "全学習者が congruent/incongruent 両条件の文を読む", "✅ 含めるべき",
  "Between-participant", "各参加者が 1 条件のみ", "L1 話者 vs L2 話者（1人1群）", "❌ 含められない",
  "Within-item", "全項目が全条件に出現", "ラテン方格で全項目が全条件に割当", "✅ 含めるべき",
  "Between-item", "各項目が 1 条件のみ", "高頻度語リスト vs 低頻度語リスト（別項目）", "❌ 含められない"
) |>
  knitr::kable(
    caption = "デザインとランダム傾きの対応: 予測子がグルーピング変数の中で変動する場合のみランダム傾きが可能"
  )
```

#### なぜ between 変数にランダム傾きを入れられないのか

直感的に説明すると、ランダム傾きは「その個人が条件間の差にどう反応するか」を推定するものです。ある学習者が条件 A しか経験していない場合、その学習者の「A と B の差への反応」は推定しようがありません。情報がないのです。数学的には、デザイン行列のランクが不足し、パラメータが識別不能（unidentifiable）になります。

### 3 つの立場: maximal vs parsimonious vs compromise {.tabset}

ランダム効果構造の選択については、心理言語学で3つの主要な立場があります。

#### Barr et al. (2013): Maximal

@barr2013 は「**デザインが正当化する最大のランダム効果構造**」を推奨しました。

**主張の根拠**: シミュレーションにより、必要なランダム傾きを省略すると第一種の過誤率が名目水準（5%）を大幅に超えることを示しました。

**具体的な手順**:

1. デザインに基づいてランダム傾きを決定（上の表を参照）
2. 全てのランダム傾きとその相関を含む maximal モデルを推定
3. 収束しない場合の対処（優先順位）：
   - まず相関を除去（`||` 記法を使用）
   - 次に切片を除去（傾きは残す）
   - 最後に、分散推定値が最小の傾きを除去

```r
# Maximal model の例（2x2 参加者内計画）
# condition は within-participant → ランダム傾きを含める
maximal <- glmer(y ~ condition * group + (1 + condition | subject) + (1 | item),
                 family = binomial, data = dat)

# 収束しない場合 → 相関を除去
no_corr <- glmer(y ~ condition * group + (1 + condition || subject) + (1 | item),
                 family = binomial, data = dat)
```

**交互作用のランダム傾き**[@barr2013interaction]: 交互作用 A:B のランダム傾きは、A と B の**両方**がそのグルーピング変数の中で変動する（within）場合のみ含めることができます。

#### Bates et al. (2015): Parsimonious

@bates2015parsimonious は maximal アプローチに対して以下の批判を展開しました。

**主張の根拠**: Maximal モデルは頻繁に収束失敗または特異適合（singular fit）を起こし、データが支持しない過剰なパラメータを推定しようとしている。

**`rePCA` ワークフロー**:

1. Maximal モデルを推定
2. `rePCA()` でランダム効果の主成分分析を実行
3. 分散がゼロまたは近ゼロの成分を特定
4. 寄与の小さいランダム効果を段階的に除去
5. 各段階で尤度比検定で確認

```r
# rePCA による診断
maximal <- glmer(y ~ condition + (1 + condition | subject), family = binomial, data = dat)
summary(rePCA(maximal))
# → 1 成分で 99%+ なら、ランダム傾きは実質不要
```

**鍵となる主張**: 「過適合モデルは収束しないことでデータが構造を支持していないことを教えてくれている。無理に収束させるのではなく、その信号に従うべき。」

#### Matuschek et al. (2017): LRT ベースの妥協

@matuschek2017 は両立場の長短を定量的に評価し、妥協案を提示しました。

**シミュレーション結果**:

| アプローチ | 第一種の過誤率 | 検出力 |
|:---:|:---:|:---:|
| Maximal（Barr） | ✅ 適切 | ⚠️ 低い |
| 切片のみ | ❌ 膨張 | ✅ 高い |
| **LRT 選択（Matuschek）** | **✅ ほぼ適切** | **✅ 高い** |

**推奨手順**:

1. Maximal モデルから出発
2. 各ランダム効果成分を尤度比検定で評価
3. 有意でない成分を除去
4. モデル適合が有意に悪化しない限り簡略化を続ける

### SLA・英語教育の典型的デザインと lme4 式 {.tabset}

以下に、SLA・英語教育研究で頻出する 4 つのデザインについて、ランダム効果構造を具体的に示します。

#### ケース1: 語彙テスト（参加者×項目）

**場面**: 50 人の L2 学習者が 40 個の語彙項目（高頻度 20 + 低頻度 20）に正誤で回答。

- **参加者**: 全員が全項目に回答 → `frequency` は **within-participant**
- **項目**: 各項目は高頻度か低頻度の一方のみ → `frequency` は **between-item**

```r
# Maximal（Barr et al. 推奨）
glmer(correct ~ frequency * L1_group +
        (1 + frequency | participant) +  # 参加者: frequency は within → 傾き可
        (1 | item),                       # 項目: frequency は between → 切片のみ
      family = binomial, data = vocab_data)

# L1_group（L1 vs L2）は between-participant → 参加者の傾きに含めない
# frequency × L1_group: L1_group が between なので交互作用の傾きも不可
```

#### ケース2: 文処理実験（ラテン方格）

**場面**: 32 人の参加者が 24 文を読む。文は 2 条件（一致/不一致）でラテン方格により割当。

- **参加者**: 各参加者が両条件を経験 → `congruency` は **within-participant**
- **項目**: ラテン方格で各項目が両条件に出現 → `congruency` は **within-item**

```r
# Maximal（二重 maximal: 参加者と項目の両方にランダム傾き）
glmer(accuracy ~ congruency +
        (1 + congruency | participant) +  # within-participant → 傾き可
        (1 + congruency | item),          # within-item（ラテン方格）→ 傾き可
      family = binomial, data = reading_data)

# 収束しない場合: まず相関を除去
glmer(accuracy ~ congruency +
        (1 + congruency || participant) +
        (1 + congruency || item),
      family = binomial, data = reading_data)
```

#### ケース3: ライティング評価（MFRM 的構造）

**場面**: 20 人の学生のエッセイを 5 人の評価者が採点。各学生を全評価者が評価。

- **評価者**: 全評価者が全学生を評価 → `student_proficiency` は **within-rater**
- **学生**: 全学生が全評価者に評価される → `rater_severity` は **within-student**

```r
# Maximal
lmer(score ~ student_proficiency + rater_severity +
       (1 + student_proficiency | rater) +   # within-rater → 傾き可
       (1 + rater_severity | student),        # within-student → 傾き可
     data = essay_data)
```

#### ケース4: 介入研究（プレポスト）

**場面**: 60 人の学習者（介入群 30 + 統制群 30）がプレ・ポストテストを受験。

- **時点（time）**: 全参加者がプレ・ポスト両方 → **within-participant** → 傾き可
- **群（group）**: 各参加者が介入か統制の一方 → **between-participant** → 傾き不可
- **time × group**: group が between なので → **交互作用の傾きも不可**

```r
# Maximal
glmer(correct ~ time * group +
        (1 + time | participant) +  # time は within → 傾き可
        (1 | item),                 # 全参加者が同じ項目セット
      family = binomial, data = intervention_data)

# group, time×group は between-participant なので参加者の傾きに含めない
```

### モデル比較の判断基準

ランダム効果構造を比較する際の具体的な判断基準をまとめます。

```{r}
#| eval: true
#| label: re-comparison-criteria
#| echo: false

tibble::tribble(
  ~基準, ~計算方法, ~判断ルール, ~注意点,
  "ΔAIC", "AIC(簡略) - AIC(複雑)", "< -2: 複雑モデル優位\n-2〜2: 実質的差なし\n> 2: 簡略モデル優位", "小さいほど良い。ペナルティがBICより軽い",
  "ΔBIC", "BIC(簡略) - BIC(複雑)", "< -6: 複雑モデルを強く支持\n-2〜2: 差なし\n> 6: 簡略モデルを強く支持", "BIC はサンプルサイズに依存。大標本でより保守的",
  "LRT", "anova(簡略, 複雑)", "p < .05: 複雑モデルが有意に改善\np ≥ .05: 簡略化を支持", "境界問題: 分散=0 の帰無仮説では p が保守的[@self1987]",
  "isSingular()", "lme4::isSingular(model)", "TRUE: 過剰パラメータ化\nFALSE: 安全", "TRUE でも推定値は使えるが、過適合の強い示唆",
  "rePCA()", "summary(rePCA(model))", "累積分散 99%+ で 1 成分\n→ ランダム傾き不要", "各成分の寄与率を確認"
) |>
  gt::gt() |>
  gt::tab_header(
    title = "モデル比較の判断基準一覧",
    subtitle = "AIC/BIC は小さいほど良い。LRT は p < .05 で複雑モデルを支持"
  )
```

::: {.callout-tip}
#### 実務でのモデル比較の報告例

> Random effects structure was determined following a forward model-building approach (Matuschek et al., 2017). Starting from a random-intercepts-only model, we tested the addition of by-participant random slopes for each within-participant predictor using likelihood ratio tests. Adding a random slope for *congruency* significantly improved model fit (χ²(2) = 8.45, *p* = .015, ΔAIC = −4.5), and this slope was retained. Adding a random slope for *frequency* did not improve fit (χ²(2) = 1.23, *p* = .54), and the simpler model was retained. The final model included random intercepts for participants and items, and a by-participant random slope for congruency.
:::

### 本章データへの適用: 設計に基づくランダム効果の決定

本データでは `sender` と `receiver` が交差分類のグルーピング変数です。各予測子について within/between を確認します。

```{r}
#| eval: true
#| label: re-design-check
#| echo: false

tibble::tribble(
  ~予測子, ~`sender に対して`, ~`receiver に対して`, ~推奨,
  "same_class", "Within（各 sender は同クラス・異クラスの receiver 両方とペアを持つ）", "Within（同上）", "ランダム傾き可能",
  "prof_gap", "Within（各 sender の相手は異なる熟達度差）", "Within", "ランダム傾き可能",
  "score_sender_sc", "Between（各 sender のスコアは固定）", "Within（異なる sender のスコアを見る）", "sender 傾き ❌ / receiver 傾き ✅",
  "score_receiver_sc", "Within（各 sender は異なるスコアの receiver と対面）", "Between（各 receiver のスコアは固定）", "sender 傾き ✅ / receiver 傾き ❌"
) |>
  gt::gt() |>
  gt::tab_header(
    title = "本データの予測子と within/between の判定",
    subtitle = "ランダム傾きは within の場合のみ可能"
  )
```

::: {.callout-tip}
#### 本章での実践的判断

上記の分析から、本データの maximal モデルは非常に複雑になります（sender 側と receiver 側で異なるランダム傾き）。実際に本章の後半で、ランダム傾きモデル（`m_glmm_rs`）が**特異適合**を起こしたことを確認しています。これは @bates2015parsimonious の予測通りで、データがランダム傾きを支持していないことを示唆しています。

**本章の結論**: `(1 | sender) + (1 | receiver)`（ランダム切片のみの交差分類モデル）がこのデータに最も適切です。この判断は以下の 3 つの証拠に基づきます：

1. ランダム傾きモデルの特異適合（`isSingular() = TRUE`）
2. `rePCA()` で 1 成分が分散の 99%+ を説明
3. AIC/BIC でランダム切片モデルが優位

ただし、**データが大規模**で within 変数が明確な場合は、ランダム傾きを含めることが推奨されます[@barr2013]。
:::

### ランダム効果構造の選択フロー

```{r}
#| eval: true
#| label: re-decision-flowchart
#| echo: false

tibble::tribble(
  ~ステップ, ~操作, ~判断基準,
  "1", "デザインに基づき maximal モデルを特定", "Within 変数のみランダム傾きに含める",
  "2", "Maximal モデルを推定", "収束するか？",
  "3a", "収束した場合: isSingular() を確認", "FALSE → Step 4 へ。TRUE → Step 3b へ",
  "3b", "特異適合: rePCA() で分散ゼロの成分を特定", "ゼロ成分を除去して再推定",
  "4", "最終モデルと簡略化モデルを LRT で比較", "p < .05 ならランダム効果を保持",
  "5", "収束しない場合: 段階的に簡略化", "① 相関除去 (||) → ② 交互作用傾き除去 → ③ 主効果傾き除去",
  "6", "報告", "試みた maximal モデルと最終モデルの両方を明記"
) |>
  gt::gt() |>
  gt::tab_header(title = "ランダム効果構造の選択フロー")
```

::: {.callout-note}
#### 参考文献の整理

| 目的 | 推奨文献 |
|------|---------|
| 理論的背景 | @barr2013（maximal の根拠）、@bates2015parsimonious（parsimonious の根拠） |
| 実践的妥協 | @matuschek2017（LRT ベースの選択）、@meteyard2020（ベストプラクティス） |
| 交互作用の扱い | @barr2013interaction（交互作用のランダム傾き） |
| 参加者 × 項目の交差分類 | @baayen2008（crossed random effects の基礎） |
| 統一的フレームワーク | @brauer2018（within/between の統一的扱い） |
| 自動化ツール | `buildmer` パッケージ[@voeten2019]（自動的な構造選択） |
:::

## 階層性を理解する: なぜデータは独立でないのか

ランダム効果構造の理論を理解したところで、次にこのデータの依存構造を ICC で数値化し、GLMM が本当に必要かを確認します。

### 「階層」は分野によって見え方が異なる

混合効果モデルの教科書は教育学・社会学の文脈で書かれることが多く、「学校→クラス→生徒」のような **入れ子の階層** を出発点にします[@snijders2012; @hox2018]。しかし、英語教育・SLA・心理言語学のデータでは、階層の構造が大きく異なります。

```{r}
#| eval: true
#| label: hierarchy-comparison
#| echo: false

tibble::tribble(
  ~分野, ~典型的構造, ~グルーピング変数, ~`階層の型`, ~`混合モデルの式`,
  "教育学", "地域→学校→生徒", "学校, 地域", "入れ子（nested）", "(1 | school/region)",
  "臨床心理学", "セラピスト→患者→セッション", "セラピスト, 患者", "入れ子", "(1 | therapist/patient)",
  "心理言語学", "参加者 × テスト項目", "参加者, 項目", "交差（crossed）", "(1 | participant) + (1 | item)",
  "英語教育（テスト）", "学習者 × テスト項目", "学習者, 項目", "交差", "(1 | learner) + (1 | item)",
  "英語教育（評価）", "学習者 × 評価者", "学習者, 評価者", "交差", "(1 | learner) + (1 | rater)",
  "本章データ", "sender × receiver（助言ネットワーク）", "sender, receiver", "交差", "(1 | sender) + (1 | receiver)"
) |>
  knitr::kable(
    caption = "分野別の階層構造の比較: 英語教育・SLA では交差分類（crossed）が標準"
  )
```

### 英語教育データの階層性: 「参加者 × 項目」が基本

英語教育・SLA で最も頻繁に出会う階層は「**1 人の学習者が複数の項目に回答する**」構造です。このとき：

- **同じ学習者の複数の回答は独立でない**（能力の高い学習者は全体的に正答しやすい）
- **同じ項目への複数の回答は独立でない**（易しい項目は全員が正答しやすい）

GLM はこの依存構造を無視するため、標準誤差が過小になり、p 値が楽観的になります。GLMM はランダム効果で「学習者ごとの偏り」と「項目ごとの偏り」を同時にモデル化し、標準誤差を正直に推定します[@baayen2008]。

これはまさに **IRT（項目反応理論）が扱う構造と同じ**です（次のセクション参照）。

### なぜ F1・F2 分析ではなく mixed model なのか

心理言語学では長く、参加者を単位にした **F1 分析**と、項目を単位にした **F2 分析**を別々に走らせるやり方が使われてきました。これは「参加者にも項目にも一般化したい」という問題意識から出てきたもので、背景には Clark の *language-as-fixed-effect fallacy* があります[@clark1973]。

ただし、F1/F2 には限界があります。

- 同じ固定効果を **2 本の別モデル**で見ているだけなので、結論が割れると解釈が難しい
- `participant × item` の**交差分類**を 1 つの式で同時に扱えない
- random slope のような「効果の個人差 / 項目差」を自然に入れにくい
- binary outcome では、F1/F2 的な分割分析をすると、2 値 GLMM の利点である **正しい link と分布**を生かしにくい

LME / GLMM は、この問題を **1 本のモデル**で扱います[@baayen2008; @barr2013; @lme4_lmer; @lme4_glmer]。  
連続アウトカムなら `lmer()`、2 値アウトカムなら `glmer(..., family = binomial)` を使って、participant と item の両方をランダム効果として同時に入れます。

```{r}
#| eval: true
#| label: tbl-f1f2-vs-mixed
#| echo: false

tibble::tribble(
  ~Approach, ~Unit_of_analysis, ~What_it_generalizes_to, ~Main_limitation,
  "F1", "participants", "参加者への一般化", "項目差を同時に扱えない",
  "F2", "items", "項目への一般化", "参加者差を同時に扱えない",
  "LME / GLMM", "trial-level responses", "参加者と項目への同時一般化", "ランダム効果構造を考える必要がある"
) |>
  knitr::kable(
    caption = "F1・F2 分析と mixed model の違い"
  )
```

要するに、F1/F2 は「参加者にも項目にも一般化したい」という正しい問題意識を持っていましたが、**それを分割して近似的に処理する方法**でした。mixed model は、その問題を **participant と item のランダム効果を 1 本のモデルに同時に入れる**ことで、より直接的に扱います。

この違いは、連続アウトカムなら **LME が F1/F2 の自然な後継**であり、2 値アウトカムなら **2 値 GLMM が F1/F2 を logistic の枠組みで置き換える**、と理解すると分かりやすいです。

### 標準例: participant × item を 1 本通して見る

dyad データは GLMM の交差分類を説明するには有用ですが、英語教育・心理言語学で最も典型的なのはやはり **participant × item** です。ここでは「学習者が高頻度語と低頻度語の語彙項目に答える 2 値データ」を小さく再現して、**どこが within で、どこが between で、なぜ random slope が入るのか**を一度まとめて見ます。

- `frequency` は **within-participant**: どの学習者も高頻度語と低頻度語の両方に答える
- `frequency` は **between-item**: 各項目は高頻度語か低頻度語のどちらか一方にしか属さない
- したがって `(1 + frequency | learner_id)` は可能だが、`(1 + frequency | item_id)` は識別できない

```{r}
#| eval: true
#| label: participant-item-mini-case

set.seed(2026)

n_learner_pg <- 12
n_item_each_freq <- 12

pi_learners <- tibble(
  learner_id = factor(sprintf("L%02d", seq_len(n_learner_pg * 3))),
  proficiency = factor(rep(c("A2", "B1", "B2"), each = n_learner_pg),
                       levels = c("A2", "B1", "B2")),
  u_learner = rnorm(n_learner_pg * 3, 0, 0.9),
  u_freq    = rnorm(n_learner_pg * 3, 0, 0.45)
)

pi_items <- tibble(
  item_id = factor(sprintf("I%02d", seq_len(n_item_each_freq * 2))),
  frequency = factor(rep(c("Low", "High"), each = n_item_each_freq),
                     levels = c("Low", "High")),
  u_item = rnorm(n_item_each_freq * 2, 0, 0.55)
)

pi_demo_dat <- tidyr::crossing(pi_learners, pi_items) |>
  dplyr::mutate(
    x_high = as.numeric(frequency == "High"),
    eta = -0.6 +
      0.8 * x_high +
      0.55 * as.numeric(proficiency == "B1") +
      1.00 * as.numeric(proficiency == "B2") +
      0.15 * x_high * as.numeric(proficiency == "B1") +
      0.25 * x_high * as.numeric(proficiency == "B2") +
      u_learner + u_item + u_freq * x_high,
    prob = plogis(eta),
    correct = rbinom(dplyr::n(), 1, prob)
  )

m_pi_glm <- glm(
  correct ~ frequency * proficiency,
  family = binomial,
  data = pi_demo_dat
)

m_pi_glmm_ri <- glmer(
  correct ~ frequency * proficiency + (1 | learner_id) + (1 | item_id),
  family = binomial,
  data = pi_demo_dat,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)

m_pi_glmm_rs <- glmer(
  correct ~ frequency * proficiency + (1 + frequency | learner_id) + (1 | item_id),
  family = binomial,
  data = pi_demo_dat,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)
```

```{r}
#| eval: true
#| label: participant-item-mini-case-plot
#| fig-height: 4.8
#| fig-width: 10

pi_participant_means <- pi_demo_dat |>
  dplyr::group_by(learner_id, proficiency, frequency) |>
  dplyr::summarise(accuracy = mean(correct), .groups = "drop")

pi_group_means <- pi_participant_means |>
  dplyr::group_by(proficiency, frequency) |>
  dplyr::summarise(accuracy = mean(accuracy), .groups = "drop")

p_pi_learners <- ggplot(pi_participant_means,
                        aes(x = frequency, y = accuracy, group = learner_id)) +
  geom_line(color = "grey65", alpha = 0.45) +
  geom_point(color = "grey45", alpha = 0.65, size = 1.6) +
  geom_line(data = pi_group_means,
            aes(x = frequency, y = accuracy, color = proficiency, group = proficiency),
            linewidth = 1.1, inherit.aes = FALSE) +
  geom_point(data = pi_group_means,
             aes(x = frequency, y = accuracy, color = proficiency),
             size = 2.6, inherit.aes = FALSE) +
  scale_color_manual(values = c(A2 = "#E69F00", B1 = "#56B4E9", B2 = "#009E73")) +
  labs(
    title = "Participant-Level Accuracy by Item Frequency",
    subtitle = "Each gray line = one learner. Colored lines = group means.",
    x = "Item frequency",
    y = "Accuracy",
    color = "Proficiency"
  ) +
  theme_bw(base_size = 11, base_family = base_font)

p_pi_items <- pi_demo_dat |>
  dplyr::group_by(item_id, frequency) |>
  dplyr::summarise(difficulty = 1 - mean(correct), .groups = "drop") |>
  ggplot(aes(x = frequency, y = difficulty, fill = frequency)) +
  geom_boxplot(alpha = 0.8, width = 0.55, outlier.shape = NA) +
  geom_jitter(width = 0.12, alpha = 0.6, size = 1.7) +
  scale_fill_manual(values = c(Low = "#CC79A7", High = "#0072B2")) +
  labs(
    title = "Item Difficulty by Frequency Group",
    subtitle = "Each dot = one item. Frequency varies across items, not within an item.",
    x = "Item frequency",
    y = "Difficulty (1 - accuracy)",
    fill = NULL
  ) +
  theme_bw(base_size = 11, base_family = base_font) +
  theme(legend.position = "none")

p_pi_learners + p_pi_items
```

**この図の読み方**:

- 左図では、**同じ学習者が Low/High の 2 条件をまたいで繰り返し観測されている**ことが見えます。だから `frequency` は learner の中で変動し、learner ごとの random slope 候補になります。
- 右図では、各項目は Low か High のどちらか一方にしか属していません。だから `frequency` は item の中では変動せず、`(1 + frequency | item_id)` は識別できません。
- この 2 つを同時に見ると、**「同じ predictor でも、どの grouping factor の中で変動するかによって slope の可否が決まる」**ことが分かります。

```{r}
#| eval: true
#| label: participant-item-mini-case-table
#| echo: false

extract_frequency_row <- function(model, model_name) {
  coef_tbl <- broom.mixed::tidy(model, effects = "fixed")
  row <- coef_tbl[coef_tbl$term == "frequencyHigh", ]

  tibble::tibble(
    Model = model_name,
    Estimate = row$estimate,
    SE = row$std.error,
    OR = exp(row$estimate),
    AIC = AIC(model),
    Singular = if (inherits(model, "merMod")) as.character(lme4::isSingular(model)) else "NA"
  )
}

pi_model_compare_tbl <- dplyr::bind_rows(
  extract_frequency_row(m_pi_glm, "GLM"),
  extract_frequency_row(m_pi_glmm_ri, "GLMM-RI: (1 | learner) + (1 | item)"),
  extract_frequency_row(m_pi_glmm_rs, "GLMM-RS: (1 + frequency | learner) + (1 | item)")
) |>
  dplyr::mutate(
    dplyr::across(c(Estimate, SE, OR, AIC), ~ round(.x, 3))
  )

knitr::kable(
  pi_model_compare_tbl,
  caption = "標準的な participant × item デザインにおける frequency 効果の比較"
)
```

この小表では、**frequency の係数自体は大きく変わらなくても、SE と AIC が変わる**ことに注目してください。GLMM は「平均の向き」を大きく変えるというより、**依存構造を入れて精度の主張を正直にする**モデルです。さらに random slope を許すと、「frequency の効き方が learner ごとに違うか」という問いまで扱えます。

### 語彙研究の実務拡張: item-level pre-test をそのまま統制する

語彙介入研究では、**post-test の得点だけを見ると解釈を誤りやすい**ことがあります。理由は単純で、target word は「本来は pre-test で不正解になるはず」でも、実際には一部の参加者がすでに知っていることがあるからです。すると post-test の正答率には、

1. **本当に学習した効果**
2. **もともと知っていた語の ceiling**

が混ざります。

ここで重要なのは、pre-test を**参加者合計点**としてだけ入れるのではなく、`learner × item` ごとの **item-level 正誤**として入れることです。つまり「この学習者は、この語を pre-test で知っていたか」という情報を、そのまま観測単位の共変量として扱います。これは explanatory IRT / item-level GLMM の発想と同じで、participant と item をまたぐ 2 値反応をそのままモデル化する考え方です[@baayen2008; @deboeck2004; @wilson2008; @cunnings2012; @linck2015]。

```{r}
#| eval: true
#| label: vocab-pretest-itemlevel-data

set.seed(2027)

n_vocab_learners <- 48
n_vocab_items <- 30

vocab_learners <- tibble(
  learner_id = factor(sprintf("VL%02d", seq_len(n_vocab_learners))),
  treatment = factor(rep(c("Control", "Gloss"), each = n_vocab_learners / 2),
                     levels = c("Control", "Gloss")),
  proficiency = factor(rep(c("A2", "B1", "B2"), each = n_vocab_learners / 3),
                       levels = c("A2", "B1", "B2")),
  u_learner = rnorm(n_vocab_learners, 0, 0.75)
)

vocab_items <- tibble(
  item_id = factor(sprintf("VW%02d", seq_len(n_vocab_items))),
  frequency = factor(rep(c("Low", "High"), each = n_vocab_items / 2),
                     levels = c("Low", "High")),
  u_item = rnorm(n_vocab_items, 0, 0.65)
)

vocab_prepost_dat <- tidyr::crossing(vocab_learners, vocab_items) |>
  dplyr::mutate(
    high_freq = as.integer(frequency == "High"),
    b1 = as.integer(proficiency == "B1"),
    b2 = as.integer(proficiency == "B2"),
    eta_pre = -2.35 +
      0.45 * high_freq +
      0.50 * b1 +
      1.00 * b2 +
      u_learner + u_item,
    pretest_correct = rbinom(dplyr::n(), 1, plogis(eta_pre)),
    pretest_status = factor(
      pretest_correct,
      levels = c(0, 1),
      labels = c("Incorrect at pre-test", "Correct at pre-test")
    ),
    eta_post = -1.20 +
      0.40 * as.integer(treatment == "Gloss") +
      1.95 * pretest_correct +
      0.35 * high_freq +
      0.40 * b1 +
      0.80 * b2 +
      0.55 * as.integer(treatment == "Gloss") * as.integer(pretest_correct == 0) +
      u_learner + u_item,
    posttest_correct = rbinom(dplyr::n(), 1, plogis(eta_post))
  )

m_vocab_pretest <- glmer(
  posttest_correct ~ treatment * pretest_status + frequency + proficiency +
    (1 | learner_id) + (1 | item_id),
  family = binomial,
  data = vocab_prepost_dat,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)
```

このモデルが答える問いは、単なる「Gloss 群のほうが post-test で高いか」ではありません。より正確には、

- **同じ頻度帯・同じ proficiency で**
- **同じ learner / item のばらつきを考慮したうえで**
- **その語を pre-test で知っていたかどうかも統制して**

`Gloss` が post-test の正答確率をどれだけ押し上げるか、を問います。

ここでの実務ルールは重要です。

- **参加者 total pre-test score だけでは不十分**: それは「この学習者は全体として語彙力が高いか」を示すが、「この語をこの学習者が事前に知っていたか」は表せません。
- **item-level pre-test は observation-level covariate**: `learner × item` のセルにぶら下がる情報なので、GLMM の 1 行 1 反応の枠組みと非常に相性が良いです。
- **研究質問に応じて予測対象を変える**: 「post-test 全体の成績」を見たいのか、「pre-test で未知だった語の学習」を見たいのかで、描くべき予測プロットは変わります。

```{r}
#| eval: true
#| label: vocab-pretest-design-options
#| echo: false

tibble::tribble(
  ~Approach, ~What_it_controls, ~What_it_cannot_control, ~Best_use,
  "Participant total pre-test score", "General learner ability / prior vocabulary size", "Whether learner A already knew item X", "Learner-level adjustment when item-level pre-test is unavailable",
  "Item-level pre-test status", "Whether this learner knew this specific item before instruction", "Broader learner ability differences unless additional learner covariates are included", "Vocabulary learning studies targeting gain on specific words",
  "Gain score / residualized gain", "Overall pre-post change at the aggregated score level", "Item-level heterogeneity and learner × item dependence", "Coarse score-level summaries, not item-response modeling"
) |>
  knitr::kable(
    caption = "Pre-test をどうモデルに入れるかで、統制できるものが変わる"
  )
```

この表の要点は、**pre-test という言葉は同じでも、実際には別の統制をしている**ということです。語彙研究で「target words の学習」を主張したいなら、最も自然なのは `item-level pre-test status` です。逆に、総得点だけを入れて「事前知識は統制した」と書くと、査読者から「個別の語の既知・未知は統制できていない」と指摘される可能性があります。

### post-test だけを見るモデルと、pre/post を長データ化するモデルは何が違うか

語彙研究では、`pre-test` の扱いとして少なくとも 2 つの GLMM 的アプローチがあります。

```{r}
#| eval: true
#| label: vocab-pretest-two-strategies
#| echo: false

tibble::tribble(
  ~Strategy, ~Outcome, ~Main_question, ~Typical_formula,
  "Post-test only + item-level pre-test covariate", "posttest_correct", "未知語に対して介入がどれだけ学習を押し上げたか", "posttest_correct ~ treatment * pretest_status + ... + (1 | learner) + (1 | item)",
  "Pre/post long-data GLMM", "correct at each time point", "時間と介入の交互作用として、全体の変化パターンがどう違うか", "correct ~ time * treatment + ... + (1 + time | learner) + (1 | item)"
) |>
  knitr::kable(
    caption = "語彙研究でよく使う 2 つの GLMM 戦略"
  )
```

両者の違いは、**どの問いを主眼に置くか**です。

- **post-test only + item-level pre-test**:  
  「もともと知らなかった語に対して、学習後にどれだけ正答確率が上がったか」を聞きたいときに強い。
- **pre/post long-data GLMM**:  
  「pre から post への変化そのものが群間で違うか」を、`time × treatment` で直接聞きたいときに強い。

以下は後者の最小例です。ここでは `pretest_correct` と `posttest_correct` を縦に積み、`time` を within-learner かつ within-item の要因として扱います。

```{r}
#| eval: true
#| label: vocab-prepost-long-model

vocab_long_dat <- vocab_prepost_dat |>
  dplyr::transmute(
    learner_id, item_id, treatment, proficiency, frequency,
    pretest_correct, posttest_correct
  ) |>
  tidyr::pivot_longer(
    cols = c(pretest_correct, posttest_correct),
    names_to = "time",
    values_to = "correct"
  ) |>
  dplyr::mutate(
    time = factor(
      time,
      levels = c("pretest_correct", "posttest_correct"),
      labels = c("Pre", "Post")
    )
  )

m_vocab_long <- glmer(
  correct ~ time * treatment + frequency + proficiency +
    (1 + time | learner_id) + (1 | item_id),
  family = binomial,
  data = vocab_long_dat,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)
```

このモデルでは `timePost:treatmentGloss` が、「`Gloss` 群は `Control` 群より pre→post の伸びが大きいか」を表します。したがって、**全体の成長曲線**を見たいときは長データ GLMM が自然です。一方で、「pre-test で未知だった target word のみ」に焦点を当てたいときは、先ほどの `post-test only + item-level pre-test` の方が解釈しやすいことが多いです。

### GLMM と IRT の関係

2 値 GLMM と IRT は、実は**同じ数学的枠組みの異なるパラメータ化**です[@deboeck2004; @wilson2008]。

```{r}
#| eval: true
#| label: glmm-irt-connection
#| echo: false

tibble::tribble(
  ~側面, ~`GLMM（lme4）`, ~`IRT（Rasch/2PL）`,
  "モデル式", "logit(P) = β₀ + β₁X + b_participant + b_item", "logit(P) = θ_person − δ_item",
  "参加者の能力", "ランダム切片 b_participant", "能力パラメータ θ",
  "項目の困難度", "ランダム切片 b_item（または固定効果）", "困難度パラメータ δ",
  "予測子の効果", "固定効果 β（実験条件など）", "（通常は含まない）",
  "識別力", "項目のランダム傾き（あれば）", "識別力パラメータ a（2PL）",
  "推定法", "ML / Laplace 近似（lme4）", "ML / EM / MCMC（mirt, TAM）",
  "利点", "予測子を柔軟に追加できる", "項目パラメータの安定した推定、テスト情報関数"
) |>
  knitr::kable(
    caption = "GLMM と IRT の対応関係: 同じデータ構造を異なる視点からモデル化"
  )
```

#### いつ GLMM を使い、いつ IRT を使うか

- **GLMM**: 「実験条件や学習者属性が正答率にどう影響するか」を検証したいとき。予測子（固定効果）が主な関心。
- **IRT**: 「テスト項目の性質（困難度・識別力）を推定したい」「学習者の能力を推定したい」とき。項目パラメータや能力推定が主な関心。
- **両者を組み合わせる**: Explanatory IRT[@wilson2008]では、IRT に予測子（学習者属性や項目属性）を追加できる。`TAM::tam.mml.mfr()` や `lme4::glmer()` で実装可能。

**例**: 語彙テストで「高頻度語と低頻度語で正答率が異なるか」を検証する場合、GLMM で `frequency` を固定効果に入れ、`(1 | learner) + (1 | item)` をランダム効果に入れるのが自然です。一方、テストの項目分析や能力推定が目的なら IRT（本書 12 章参照）が適切です。

### 本章データの階層構造

このデータは「有向ペア」が観測単位ですが、観測は sender / receiver に跨って
繰り返し出現します。したがって独立性仮定は破れやすく、GLM 単独では
標準誤差が過小になる可能性があります[@snijders2012; @hox2018]。

**本章の交差分類データ構造**:

- Level 1: 有向ペア（sender → receiver）
- 高次構造: sender と receiver の**交差分類ランダム効果**
- 重要点: 厳密な単純階層（生徒がクラスにのみ入る形）ではないため、
  ICC/DEFF は「構造把握の近似指標」として使う

### ICC を 1 分で直感的に理解する

ICC は、ひとことで言えば**「同じクラスターを共有する 2 観測が、どれくらい似やすいか」**を表す指標です。2 値 GLMM では raw の 0/1 相関そのものというより、**logit の潜在尺度で共有される傾向の相関**として読むのが安全です[@snijders2012; @hox2018; @performance_icc]。

同じ考え方は、SLA・心理言語学・教育測定のさまざまな場面にそのまま現れます。

| データの場面 | 何が繰り返されるか | ICC が捉えるもの |
|---|---|---|
| 本章の助言 dyad | 同じ sender / receiver が複数の dyad に登場する | 「助言しやすい人」「助言を受けやすい人」という個人差の共有 |
| 語彙テストの項目反応 | 同じ学習者が複数項目に答える | 同じ学習者の反応どうしが似る程度 |
| スピーキング評価 | 同じ受験者が複数課題・複数評価者で評定される | 受験者差・課題差・評価者差がどれだけ得点を揺らすか |

たとえば本章の dyad データでは、同じ sender が別の peer に対して示す 2 つの助言行動は、完全に独立ではありません。どちらも同じ sender の「助言しやすさ」を共有するからです。語彙テストなら、同じ学習者が Item 3 と Item 9 に答える 2 つの反応は、その学習者の能力を共有します。**ICC は、この「共有される傾向」の強さを分散比として要約したもの**です。

sender を共有する 2 観測の潜在 logit 傾向を考えると、直感的には次の比で読めます。

$$
\mathrm{Corr}(\eta_{ij}, \eta_{ik})
=
\frac{\sigma^2_{\mathrm{sender}}}
{\sigma^2_{\mathrm{sender}} + \sigma^2_{\mathrm{receiver}} + \pi^2/3}
$$

ここで \(\eta\) は「まだ確率に戻す前の潜在 logit 傾向」であり、\(\pi^2/3\) はロジスティック分布に基づく観測レベルの残差分散です[@nakagawa2017; @performance_icc]。つまり **sender 分散が大きいほど、同じ sender を共有する観測は似やすくなります**。

::: {.callout-warning}
#### ネットワークデータ特有の依存構造: 双方向性（reciprocity）

本データは有向ネットワーク（A → B の助言関係）であり、`(1 | sender) + (1 | receiver)` の交差分類ランダム効果でモデル化しています。しかし、ネットワークデータには**双方向性（reciprocity）**という追加の依存構造があります。つまり「A が B に助言する場合、B も A に助言する確率が高い」可能性です。

この双方向依存は `(1 | sender) + (1 | receiver)` では捉えられません。より精密なモデル化には Social Relations Model（SRM）や p2 モデル[@kenny2006dyadic; @vanduijn2004]が必要ですが、これらは本章の範囲を超えます。

**本章での扱い**: 交差分類ランダム効果は双方向性を直接モデル化しないが、sender と receiver の個人差を考慮することで、依存構造の主要部分は吸収されます。双方向性が強いデータでは、標準誤差がなお過小評価される可能性がある点に留意してください。典型的な「学習者 × テスト項目」デザインではこの問題は生じません。
:::


### null GLMM で ICC を推定

ICC（級内相関係数）を推定するために、まず固定効果を含まない**空モデル（null model）**を当てはめます。

```{r}
#| eval: true
#| label: icc-null-glmm

# ── Step 1: 空モデルを当てはめる ──
# tie ~ 1 は「切片だけ」（固定効果なし）
# (1 | sender) + (1 | receiver) は交差分類ランダム切片
m_null <- glmer(
  tie ~ 1 + (1 | sender) + (1 | receiver),
  family = binomial(link = "logit"),
  data = dat_bin,
  control = glmerControl(
    optimizer = "bobyqa",           # 収束しやすい最適化手法
    optCtrl = list(maxfun = 2e5)    # 最大反復回数を増やす
  )
)
```

#### 方法 A: `performance::icc()` で一発計算（実務向け）

実務では `performance::icc()` を使うのが最もシンプルです。
内部でロジスティック分布の残差分散（$\pi^2/3$）を自動的に処理します。

```{r}
#| eval: true
#| label: icc-performance

performance::icc(m_null)
```

`performance::icc()` は adjusted ICC（条件付き）と unadjusted ICC（周辺）を返します[@performance_icc]。
null モデルでは固定効果がないため両者は実質同じ値です。

**出力の読み方**: Adjusted ICC は、「固定効果をまだ入れていない段階で、どれくらいの分散が sender / receiver 側に乗っているか」を示します。値が 0.05 を超えると依存構造が実務上無視しにくいことが多いですが、**反復測定や交差分類が研究設計に含まれるなら、ICC が小さくても GLMM を候補から外す根拠にはなりません**。ここでは GLMM の要否を機械的に決めるのではなく、次に DEFF とあわせて「依存構造の大きさ」を説明します。

#### 方法 B: 手計算で原理を理解する

`performance::icc()` の中身を理解するために、ICC を手計算で確認します。
ロジスティック GLMM では、Level 1 の残差分散を $\pi^2/3 \approx 3.29$ と近似する
「潜在変数法」が標準的です[@snijders2012; @hox2018]。

$$
\mathrm{ICC}_{\mathrm{sender}}
= \frac{\sigma^2_{\mathrm{sender}}}
       {\sigma^2_{\mathrm{sender}} + \sigma^2_{\mathrm{receiver}} + \pi^2/3}
$$

```{r}
#| eval: true
#| label: icc-manual

# ランダム効果の分散を取り出す
vc <- as.data.frame(VarCorr(m_null))
var_sender   <- vc |> dplyr::filter(grp == "sender")   |> pull(vcov)
var_receiver <- vc |> dplyr::filter(grp == "receiver") |> pull(vcov)
var_logistic <- (pi^2) / 3   # ≈ 3.29（ロジスティック分布の残差分散）

# ICC を手計算
icc_sender   <- var_sender   / (var_sender + var_receiver + var_logistic)
icc_receiver <- var_receiver / (var_sender + var_receiver + var_logistic)
icc_total    <- (var_sender + var_receiver) /
                (var_sender + var_receiver + var_logistic)

tibble(
  component = c("sender ICC", "receiver ICC", "total clustered share"),
  estimate  = c(icc_sender, icc_receiver, icc_total)
) |>
  gt() |>
  fmt_number(columns = estimate, decimals = 3) |>
  tab_header(title = "ICC（手計算：潜在変数近似）")
```

::: {.callout-tip}
#### ICC の読み方

| 値 | 意味 |
|----|------|
| **sender ICC** | 助言行動のばらつきのうち、「誰が送り手か」で説明される割合 |
| **receiver ICC** | 「誰が受け手か」で説明される割合 |
| **total** | 両方を合わせた群内相関。GLM → GLMM の必要性の主要な根拠 |

方法 A と方法 B の値がほぼ一致することを確認してください。一致していれば、`performance::icc()` が正しく潜在変数近似を使っていることがわかります。
:::

```{r}
#| eval: true
#| label: icc-variance-share
#| fig-height: 4
#| fig-width: 7

icc_share_df <- tibble(
  component = factor(
    c("Sender variance share", "Receiver variance share", "Observation-level logistic residual"),
    levels = c("Sender variance share", "Receiver variance share", "Observation-level logistic residual")
  ),
  share = c(icc_sender, icc_receiver, 1 - icc_total)
)

ggplot(icc_share_df, aes(x = component, y = share, fill = component)) +
  geom_col(width = 0.65, show.legend = FALSE) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +
  scale_fill_manual(values = c("#4E79A7", "#F28E2B", "#B0B0B0")) +
  labs(
    title = "How the Null-Model Variance Is Partitioned",
    subtitle = "ICC is the clustered share on the latent logit scale",
    x = NULL,
    y = "Share of latent-scale variance"
  ) +
  coord_flip() +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**: 青と橙の部分が sender / receiver に由来する**共有分散**で、灰色は観測レベルのロジスティック残差です。青 + 橙が `total clustered share`、つまり「GLM が独立と仮定すると取りこぼす依存構造」の大きさです。

### デザインエフェクト（DEFF）の近似

DEFF（デザインエフェクト）は、「独立性仮定が破れることで、実効サンプルサイズがどれだけ縮小するか」を示す指標です。DEFF = 2 なら、見かけのサンプルサイズの半分しか情報がないことを意味します。

::: {.callout-warning}
**交差分類での DEFF の限界**: DEFF の公式 $1 + (\bar{m} - 1) \times \text{ICC}$ は**入れ子構造**（生徒がクラスに所属する形）に対して導出されたものです[@kish1965]。本データのような**交差分類**（sender × receiver）では、この公式は**近似にすぎません**。交差分類では各観測が複数のクラスターに同時に所属するため、正確な DEFF の計算は困難です。ここでは「依存構造の深刻さの大まかな目安」として使います。最終的なモデル選択は AIC/BIC と診断に基づきます。
:::

```{r}
#| eval: true
#| label: design-effect-approx

# 各 sender / receiver が何ペアに登場するか（= 平均クラスターサイズ）
m_sender   <- dat_bin |> count(sender)   |> summarise(mean_n = mean(n)) |> pull()
m_receiver <- dat_bin |> count(receiver) |> summarise(mean_n = mean(n)) |> pull()

# DEFF = 1 + (クラスターサイズ - 1) × ICC
deff_sender   <- 1 + (m_sender - 1)   * icc_sender
deff_receiver <- 1 + (m_receiver - 1) * icc_receiver
deff_total    <- 1 + (mean(c(m_sender, m_receiver)) - 1) * icc_total

# 実効サンプルサイズ = 観測数 / DEFF
n_obs       <- nrow(dat_bin)
n_effective <- n_obs / deff_total

tibble(
  quantity = c(
    "観測数（ペア数）",
    "平均クラスターサイズ（sender）",
    "平均クラスターサイズ（receiver）",
    "DEFF sender", "DEFF receiver", "DEFF total",
    "実効サンプルサイズ ≈ 観測数 / DEFF total"
  ),
  value = c(n_obs, m_sender, m_receiver,
            deff_sender, deff_receiver, deff_total,
            n_effective)
) |>
  gt() |>
  fmt_number(columns = value, decimals = 1) |>
  tab_header(title = "Design Effect（DEFF）の近似計算")
```

**DEFF 表の読み方**：

| 指標 | 解釈 |
|------|------|
| 平均クラスターサイズ | 各 sender/receiver が関与するペア数の平均（n-1 に近い）|
| DEFF sender / receiver | 繰り返し構造による情報の目減り。**1 なら独立と同等**、大きいほど深刻 |
| DEFF total | 両方を合わせた概算 |
| 実効サンプルサイズ（参考値） | 観測数 / DEFF の概算。**あくまで参考値**であり、最終的なモデル選択の根拠にはしない |

単純階層データ（入れ子構造）では $\mathrm{DEFF}=1+(m-1)\mathrm{ICC}$ が標準式です[@kish1965]。**本章の交差分類（sender × receiver）にはこの公式の理論的根拠がありません**。ここで示す DEFF と実効サンプルサイズは「独立性違反がどの程度深刻かの大まかな参考値」にすぎず、モデル選択の根拠には使いません。最終的な GLM/GLMM の選択は、後のセクションで行う **AIC/BIC 比較と LRT** に基づきます。

::: {.callout-note}
#### 実務での読み方（ICC/DEFF）

- ICC が 0 に近い: 群内相関は小さく、GLM と GLMM の差は小さいことが多い。
- ICC が中程度以上: GLMM を優先し、標準誤差の過小評価を避ける。
- DEFF が 1 を大きく超える: 独立性仮定の破れが大きい可能性がある。
- 交差分類では ICC/DEFF は「採用判断の補助」であり、最終判断は
  `compare_performance()` と診断を併用する。

**報告テンプレート（ICC/DEFF）**:

> Intraclass correlation coefficients from a null GLMM indicated that ICC_sender = X.XX and ICC_receiver = X.XX, suggesting that [a substantial / a modest / a negligible] proportion of variance in advice ties was attributable to individual differences. The design effect was approximately X.X, reducing the effective sample size from N to approximately N_eff. These results confirmed the need for a mixed-effects approach.
:::

### ICC と DEFF の役割分担

ICC と DEFF はセットで出てくることが多いですが、答えている問いは違います。

| 指標 | 何を問うか | 直感的な読み方 |
|---|---|---|
| ICC | 同じクラスター内の観測はどれくらい似るか | 「似ている強さ」 |
| DEFF | その類似のせいで情報量はどれくらい減るか | 「有効サンプルサイズの目減り」 |

たとえば、同じ 30 人の学習者が 20 項目に答えるデータでは、ICC が大きいほど「同じ学習者の 20 反応は互いに似やすい」と言えます。DEFF は、その結果として「600 観測あるように見えても、独立観測 600 個ぶんの情報はない」と示す指標です。**ICC が“似ている強さ”、DEFF が“その帰結”**という関係で理解すると混乱しません。

## ステップ1: GLM（基準モデル）

まずランダム効果なしのロジスティック回帰で基準線を作ります。この GLM は「全ペアが独立」と仮定しており、sender/receiver の繰り返し構造を無視しています。**目的は GLMM との比較基準を作ること**であり、このモデル自体を最終モデルとして採用するわけではありません。

固定効果の意味：

| 予測子 | 意味 |
|--------|------|
| `same_class` | 同じクラスに所属するペアか（0/1）|
| `prof_gap` | 熟達度水準の差の絶対値（0, 1, 2）|
| `score_sender_sc` | 送り手のベースラインスコア（z 標準化）|
| `score_receiver_sc` | 受け手のベースラインスコア（z 標準化）|
| `score_gap` | スコア差の絶対値 |

```{r}
#| eval: true
#| label: fit-glm

# ランダム効果なしの標準ロジスティック回帰（比較基準）
m_glm <- glm(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap,
  family = binomial(link = "logit"),  # 2値 → ロジスティック
  data = dat_bin
)

broom::tidy(m_glm, conf.int = TRUE) |>
  gt() |>
  tab_header(title = "Binomial GLM: Logistic Regression") |>
  fmt_number(columns = where(is.numeric), decimals = 3)
```

**`summary(m_glm)` の読み方**：

| 出力項目 | 読み方 |
|---------|--------|
| `Estimate` | logit スケールでの係数。正なら助言関係の **オッズが増加** する方向 |
| `Std. Error` | 係数の標準誤差。**GLM はペアの独立性を仮定しているため、この SE は過小評価の可能性が高い** |
| `z value` | Estimate / SE。Wald 検定統計量 |
| `Pr(>\|z\|)` | 帰無仮説（係数 = 0）の p 値 |
| `AIC` | モデルの相対的な当てはまり。後で GLMM の AIC と比較する |

::: {.callout-warning}
### この GLM の SE をそのまま信用しない

ICC/DEFF の分析で見たとおり、このデータには sender/receiver による依存構造があります。GLM はこの構造を無視するため、標準誤差が過小に見積もられ、**p 値が本来より小さく出ている可能性**があります。次のステップで GLMM に移行した際に、SE がどのように変化するか（通常は大きくなる）を確認してください。
:::

## ステップ2: GLMM（ランダム切片）

次に sender / receiver のランダム切片を追加します。`(1 | sender) + (1 | receiver)` は「各送り手と各受け手が、全体平均からどれだけ偏るか」を推定する項です。

- `(1 | sender)`: 「助言をしやすい人/しにくい人」の個人差
- `(1 | receiver)`: 「助言を受けやすい人/受けにくい人」の個人差
- この 2 つは **交差分類**（同じ 30 人のプールから sender と receiver が出ている）

`control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))` は、デフォルトの最適化アルゴリズムでは収束しにくい場合の標準対処です。`bobyqa`（制約付き最適化）と最大反復回数の増加を組み合わせています。

```{r}
#| eval: true
#| label: fit-glmm-ri

# GLMM: 交差分類ランダム切片（sender + receiver）
m_glmm_ri <- glmer(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 | sender) +    # sender のランダム切片（助言をしやすい/しにくい個人差）
    (1 | receiver),   # receiver のランダム切片（助言を受けやすい/受けにくい個人差）
  family = binomial(link = "logit"),
  data = dat_bin,
  control = glmerControl(
    optimizer = "bobyqa",         # 収束しやすい最適化手法
    optCtrl = list(maxfun = 2e5)  # 最大反復回数
  )
)

broom.mixed::tidy(m_glmm_ri, conf.int = TRUE) |>
  gt() |>
  tab_header(title = "GLMM: Random Intercept Model") |>
  fmt_number(columns = where(is.numeric), decimals = 3)
```

**`summary(m_glmm_ri)` の読み方**：

GLMM の出力は GLM と構造が似ていますが、**ランダム効果の分散成分**が追加されています。

**ランダム効果セクション**：

| 出力項目 | 読み方 |
|---------|--------|
| `Groups` / `Name` | ランダム効果のグループ変数と項（ここでは sender と receiver の切片）|
| `Variance` | そのグループの分散。**大きいほど個人差が大きい** |
| `Std.Dev.` | 分散の平方根（logit スケール）。0.5 以上なら実質的な個人差がある |
| `Number of obs` / `groups` | 観測数とグループ数 |

**固定効果セクション**：GLM と同じ列構成ですが、**SE が GLM より大きくなっている**はずです。これが GLMM の本質的な効果——依存構造を考慮することで SE が「正直に」なるのです。

::: {.callout-important}
### GLM と GLMM の SE を比較する

`summary(m_glm)` と `summary(m_glmm_ri)` の SE を並べて見てください。GLMM の SE が GLM より大きくなっていれば、GLM が独立性仮定のもとで SE を過小評価していた証拠です。**推定値（Estimate）自体はそれほど変わらないが、SE が変わる**——これが GLMM の核心です。「GLMM は推定値を変えるのではなく、精度の主張を正直にする」と理解してください。
:::

分散成分の大きさを直感的に解釈するには、ロジスティック分布の残差分散 $\pi^2/3 \approx 3.29$ と比較します。sender 分散が 0.5、receiver 分散が 0.3 なら、残差（$\pi^2/3$）に対する比率はそれぞれ約 15%、9% であり、sender 側の個人差がより大きいことがわかります。

## ステップ3: ランダム傾き候補と特異適合

ランダム切片モデルは「全体平均からの偏り」だけを推定しますが、**ランダム傾きモデル**は「固定効果の強さ自体が個人ごとに異なるか」を検討します。

#### ランダム傾きは「線が平行でなくなる」ことを拾う

random intercept のみで適切なモデル化が可能な場合、各参加者の条件差はおおむね**平行移動**として表れます。つまり「全体的に正答しやすい人 / しにくい人」はいても、条件差の大きさ自体はおおむね共通です。これに対して random slope が必要なのは、**条件差そのものが参加者ごとに違う**ときです。

以下の図は participant × item の典型設計を使った純粋な模式例です。左は random intercept のみ、右は random intercept + random slope のデータです。

```{r}
#| eval: true
#| label: random-slope-intuition
#| fig-height: 5
#| fig-width: 10

make_slope_demo <- function(sd_slope, scenario_label) {
  participants <- tibble(
    participant = factor(sprintf("P%02d", seq_len(18))),
    u_intercept = rnorm(18, 0, 0.9),
    u_slope = rnorm(18, 0, sd_slope)
  )

  items <- tibble(
    item = factor(sprintf("I%02d", seq_len(24))),
    condition = factor(rep(c("Baseline", "Treatment"), each = 12),
                       levels = c("Baseline", "Treatment")),
    u_item = rnorm(24, 0, 0.35)
  )

  tidyr::crossing(participants, items) |>
    dplyr::mutate(
      x_treat = as.numeric(condition == "Treatment"),
      eta = -0.5 + 0.8 * x_treat + u_intercept + u_item + u_slope * x_treat,
      prob = plogis(eta),
      y = rbinom(dplyr::n(), 1, prob),
      scenario = scenario_label
    ) |>
    dplyr::group_by(scenario, participant, condition) |>
    dplyr::summarise(accuracy = mean(y), .groups = "drop")
}

rs_intuition_df <- dplyr::bind_rows(
  make_slope_demo(0.00, "Random intercept only"),
  make_slope_demo(0.75, "Random intercept + random slope")
)

rs_group_means <- rs_intuition_df |>
  dplyr::group_by(scenario, condition) |>
  dplyr::summarise(accuracy = mean(accuracy), .groups = "drop")

ggplot(rs_intuition_df,
       aes(x = condition, y = accuracy, group = participant, color = participant)) +
  geom_line(alpha = 0.45, linewidth = 0.6) +
  geom_point(alpha = 0.7, size = 1.8) +
  geom_line(
    data = rs_group_means,
    aes(x = condition, y = accuracy, group = 1),
    inherit.aes = FALSE,
    color = "black",
    linewidth = 1.2
  ) +
  geom_point(
    data = rs_group_means,
    aes(x = condition, y = accuracy),
    inherit.aes = FALSE,
    color = "black",
    size = 2.5
  ) +
  facet_wrap(~scenario) +
  labs(
    title = "Why Random Slopes Matter",
    subtitle = "Black line = grand mean. Colored lines = participant-specific condition effects.",
    x = NULL,
    y = "Accuracy"
  ) +
  theme_bw(base_size = 11, base_family = base_font) +
  theme(legend.position = "none")
```

**この図の読み方**:

- **左**: 線の上下位置は人によって違いますが、傾きはおおむね類似しています。これは「全体的な得意・不得意」は違っても、Treatment の効き方はほぼ共通という状況です。random intercept のみで十分なことが多いです。
- **右**: 線の傾きそのものがばらつきます。Treatment で大きく伸びる人、ほとんど変わらない人が混ざっています。これが **random slope が表現したい構造**です。
- random slope は、固定効果を否定するものではありません。**全体平均としての効果（黒線）を保ったまま、その効き方の個人差を追加で表現する**のが random slope です。

participant × item の標準例で言えば、先ほどの `frequency` は learner の中で変動していたので learner ごとの random slope 候補になりました。本章の dyad 例でも `same_class` は sender の中で変動するので、原理的には同じです。以下では、その random slope を実データに対して実際に試し、**データがその複雑さを支持するか**を `isSingular()` とモデル比較で判断します。

ここでは `(1 + same_class | sender)` を試します。これは「same_class の効果が sender ごとに異なるか」——つまり「同クラスを好む傾向に送り手間の個人差があるか」を意味します。ただし、ランダム構造が過剰だとモデルが **特異適合（singular fit）** を起こし、分散成分が 0 に張り付くことがあります。

```{r}
#| eval: true
#| label: fit-glmm-rs

# GLMM: ランダム傾き追加（same_class の効果が sender ごとに異なるか？）
m_glmm_rs <- glmer(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 + same_class | sender) +  # 切片 + same_class の傾きを sender ごとに推定
    (1 | receiver),
  family = binomial(link = "logit"),
  data = dat_bin,
  control = glmerControl(
    optimizer = "bobyqa",
    optCtrl = list(maxfun = 2e5)
  )
)

# 特異適合の確認：FALSE なら安全、TRUE なら過剰パラメータ化
isSingular(m_glmm_ri)  # ランダム切片モデル
isSingular(m_glmm_rs)  # ランダム傾き追加モデル
```

**`isSingular()` の読み方**：

- `FALSE` → 特異適合ではない。ランダム効果構造はデータに支持されている。
- `TRUE` → **特異適合が発生**。分散成分の一部が 0 に張り付いており、モデルが複雑すぎる。

**本データでの判断**: `m_glmm_ri` が FALSE（安全）、`m_glmm_rs` が TRUE（過剰）であれば、ランダム傾きモデルはこのデータには複雑すぎると判断し、ランダム切片モデル（`m_glmm_ri`）を採用します。

::: {.callout-warning}
### 特異適合が起きたときの対処法

1. **無視しない**: 特異適合はモデルの過剰パラメータ化の徴候であり、推定結果の信頼性に影響する。
2. **ランダム構造を簡素化**: ランダム傾きを落とし、ランダム切片モデルに戻す。
3. **試みたことを報告**: 論文では「ランダム傾きを試みたが特異適合が生じたため、ランダム切片モデルを採用した」と書くことが推奨される[@bates2015parsimonious; @matuschek2017]。SLA 分野での具体的な判断例は @linck2015 を参照。

本データでは、`m_glmm_ri`（ランダム切片のみ）が `FALSE` であれば安全に使用でき、`m_glmm_rs`（ランダム傾き追加）が `TRUE` であれば、ランダム切片モデルに留めるのが実務上の正解です。
:::


ステップ 1–3 でモデルの候補がそろいました。ここからはモデル比較に入ります。GLMM のモデル比較は **3 段階**で進めるのが定石です[@bolker2009; @barr2013; @bates2015parsimonious; @matuschek2017; @meteyard2020; @glmmfaq]。

モデル比較は 3 段階で進めます。第1段階では固定効果を据え置いたままランダム効果構造を比較し、第2段階では採用したランダム構造を固定してリンク関数（logit / probit / cloglog）を比較します。第3段階でランダム構造とリンク関数を固定したまま固定効果を比較し、最終モデルを確定して解釈と診断に進みます。

**ΔAIC の目安**（全ステップ共通）：

| ΔAIC | 解釈 |
|------|------|
| 0–2 | ほぼ同等。パラメータの少ないモデルを選ぶのが倹約的 |
| 4–7 | 中程度の証拠。ΔAIC が大きい側のモデルは劣る可能性 |
| 10+ | 強い証拠。ΔAIC が大きい側のモデルは実質的に支持されない |

### ① ランダム効果構造の比較

::: {.callout-important}
#### モデル比較の原則：一度に動かすのは一つだけ

ランダム効果構造と固定効果を同時に変えると、何が改善に寄与したか分からなくなります。手順は：

1. **固定効果を固定** → ランダム効果構造を比較（①）
2. **ランダム構造を固定** → リンク関数を比較（②）
3. **ランダム構造・リンクを固定** → 固定効果を比較（③）
:::

まず sender / receiver の組み合わせを変えた候補を推定し、GLM（ランダム効果なし）とステップ 2–3 で推定済みの `m_glmm_ri`・`m_glmm_rs` を含めて一括比較します。

```{r}
#| eval: true
#| label: fit-random-structure-candidates

# 候補1: sender のランダム切片のみ
m_glmm_sender <- glmer(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 | sender),
  family = binomial(link = "logit"),
  data = dat_bin,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)

# 候補2: receiver のランダム切片のみ
m_glmm_receiver <- glmer(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 | receiver),
  family = binomial(link = "logit"),
  data = dat_bin,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)
# 候補3: m_glmm_ri（sender + receiver）と候補4: m_glmm_rs（ランダム傾き追加）は既に推定済み

# ── 全候補の AIC/BIC/R² を一括比較（ΔAIC 付き、最良モデルをハイライト）──
compare_models_gt(
  m_glm, m_glmm_sender, m_glmm_receiver, m_glmm_ri, m_glmm_rs,
  title = "① ランダム効果構造の候補比較（AIC/BIC/R²）"
)
```

**この表の読み方**：

1. **AIC/BIC**: 小さいほど良い。GLM vs GLMM で ΔAIC > 10 なら、ランダム効果が大幅に改善。
2. **R² (conditional vs marginal)**: conditional R² が marginal を大きく上回るほど、ランダム効果（個人差）が予測に寄与している。
3. **ランダム傾き**: AIC が改善していても特異適合（`isSingular = TRUE`）ならば採用は避ける。

次に LRT（尤度比検定）でランダム効果構造を正式に比較します。LRT は **入れ子関係にあるモデル同士** でのみ有効です。

```{r}
#| eval: true
#| label: lrt-random-structure

# 同一固定効果でのランダム構造比較（LRT）
# lme4::anova.merMod() は自動で尤度比検定を実行する（test 引数は不要）
anova(m_glmm_sender, m_glmm_ri)
anova(m_glmm_receiver, m_glmm_ri)
anova(m_glmm_ri, m_glmm_rs)
```

**LRT 出力の読み方**：

| 出力項目 | 読み方 |
|---------|--------|
| `Chisq` | カイ二乗検定統計量。大きいほど 2 モデル間の適合差が大きい |
| `Df` | パラメータ数の差（追加されたランダム効果の数）|
| `Pr(>Chisq)` | p 値。有意なら、より複雑なモデルが有意に適合を改善 |

**3 つの LRT の結果をまとめると**: (1) sender のみ → sender + receiver で有意に改善 → receiver のランダム切片は必要。(2) receiver のみ → sender + receiver で有意に改善 → sender のランダム切片も必要。(3) sender + receiver → sender + receiver + ランダム傾き で改善しない → ランダム傾きは不要。この結論は `isSingular()` の結果とも整合します。

::: {.callout-note}
#### 境界問題に注意

ランダム効果の分散はゼロ以上の制約があるため、帰無仮説（分散 = 0）はパラメータ空間の境界に位置します。このため LRT の p 値は**保守的**（やや大きめ）になる傾向があります[@self1987; @stram1994]。実務的には、LRT が有意でなくても AIC/BIC が改善していれば、ランダム効果の追加を検討する余地があります。
:::

次に特異適合の正式チェックと、ランダム効果の主成分分析（`rePCA()`）を行います。`rePCA()` はランダム効果の分散共分散行列を主成分分解し、各成分がどれだけ情報を持つかを示します。

```{r}
#| eval: true
#| label: singularity-repca

# isSingular() は先のチャンクで確認済み。ここでは VarCorr と rePCA を確認

# ランダム効果の分散共分散構造を確認（分散が 0 に近い項があれば過剰）
VarCorr(m_glmm_rs)

# ランダム効果の主成分分析（各成分がどれだけ情報を持つか）
# → 1成分で 99%+ なら、ランダム傾きは実質不要
rePCA(m_glmm_rs)
```

`isSingular()` と `rePCA()` は、ランダム効果が過剰でないかを点検する定番です[@lme4issingular; @lme4repca]。

**出力の読み方**：

| 関数 | 結果 | 意味 |
|------|------|------|
| `isSingular()` | `FALSE` | 安全。ランダム効果構造は推定可能 |
| `isSingular()` | `TRUE` | 特異適合。分散成分の一部が 0 に収束 |
| `VarCorr()` | 分散 ≈ 0 の項あり | その項のランダム効果は過剰 |
| `rePCA()` | 1成分で 99%+ | ランダム傾きは実質的に不要 |

`m_glmm_rs` が特異適合かつ適合改善が小さい場合は、
ランダム切片モデルを採用するのが実務上の安全策です。

::: {.callout-note collapse="true"}
#### 連続変数のランダム傾き（発展）

本データでは `same_class`（2 値）のランダム傾きを試みましたが、実務では**連続変数のランダム傾き**がより頻繁に登場します。例えば「語彙テストの項目難易度の効果が学習者ごとに異なるか」を検討する場合です。

```r
# 連続変数のランダム傾き例（語彙テストを想定）
# item_difficulty は連続変数（標準化済み）
glmer(correct ~ item_difficulty * proficiency +
        (1 + item_difficulty | participant) +  # 連続変数のランダム傾き
        (1 | item),
      family = binomial, data = vocab_data)
```

**連続変数のランダム傾き特有の注意点**:

1. **収束失敗が起きやすい**: 連続変数は 2 値変数よりランダム傾きの推定が不安定。まず `||`（相関なし）で試す。
2. **中心化が重要**: 中心化されていない連続変数（例：年齢 20〜60）のランダム傾きは、切片との相関が人工的に高くなる。必ず z 標準化してからランダム傾きに含める。
3. **解釈**: 「SD_slope = 0.3」は「ある学習者の item_difficulty の効果は平均 ± 0.3 logit の範囲でばらつく」を意味する。分散が 0 に近ければ、全学習者で効果が均一（ランダム傾き不要）。
:::

#### ランダム効果の可視化

```{r}
#| eval: true
#| label: sjplot-random-effects

# ランダム効果の個人ごとの推定値を可視化
# → 各点が 1 人の sender（または receiver）
# plot_model(type = "re") は複数ランダム効果で ggplot のリストを返す
# → lapply で個別にテーマ適用し、patchwork で結合
re_plots <- sjPlot::plot_model(
  m_glmm_ri,
  type = "re",
  title = "Random effects by sender and receiver"
)
# リスト / 単一 ggplot 両対応
if (inherits(re_plots, "list")) {
  re_plots <- lapply(re_plots, function(p) p + theme_bw(base_size = 12, base_family = base_font))
  patchwork::wrap_plots(re_plots, ncol = 2)
} else {
  re_plots + theme_bw(base_size = 12, base_family = base_font)
}
```

`plot_model(type = "re")` で sender / receiver のランダム効果分布を可視化します。

**図の読み方**：

- 各点が 1 人の sender（または receiver）のランダム効果推定値。
- **信頼区間が 0 をまたぐ**個人は、全体平均と有意に異ならない。
- 信頼区間が 0 をまたがない個人は、特に助言をしやすい（正）または しにくい（負）。
- 例えば、sender のランダム効果が +0.8 の人は、全体平均よりもログオッズで 0.8 高い（＝助言をしやすい）個人です。確率で言えば、平均的な sender に比べてベースラインの助言確率が約 10-15 ポイント高くなります。
- sender と receiver のプロットを比べ、**どちらの個人差が大きいか**を視覚的に判断できる。散布の幅が大きいほど個人差が大きい。

#### 部分プーリング（shrinkage）: GLMM が個人差を推定する仕組み

GLMM のランダム効果推定値（BLUP）は、個人ごとに別々に推定した値（no-pooling）と全員を同一と見なした値（complete pooling）の**中間**に位置します。データが少ない個人ほど全体平均に引き寄せられます。これを**shrinkage（収縮）**または**部分プーリング**と呼びます[@gelman2006]。

```{r}
#| eval: true
#| label: shrinkage-visualization
#| fig-height: 5
#| fig-width: 8

# ── No-pooling: sender ごとに個別 GLM で切片を推定 ──
sender_ids <- levels(dat_bin$sender)

no_pool_est <- sapply(sender_ids, function(s) {
  d <- dat_bin[dat_bin$sender == s, ]
  if (length(unique(d$tie)) < 2) return(NA)  # 完全分離を回避
  m <- glm(tie ~ 1, family = binomial, data = d)
  coef(m)[1]
})

# ── Partial-pooling: GLMM のランダム切片 ──
partial_pool_est <- lme4::fixef(m_glmm_ri)["(Intercept)"] + lme4::ranef(m_glmm_ri)$sender[, 1]
names(partial_pool_est) <- rownames(lme4::ranef(m_glmm_ri)$sender)

# ── 比較プロット ──
shrink_df <- data.frame(
  sender = sender_ids,
  no_pool = no_pool_est[sender_ids],
  partial_pool = partial_pool_est[sender_ids]
) |> tidyr::drop_na()

grand_mean <- lme4::fixef(m_glmm_ri)["(Intercept)"]

ggplot(shrink_df, aes(x = no_pool, y = partial_pool)) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed", color = "grey50") +
  geom_hline(yintercept = grand_mean, linetype = "dotted", color = "#0072B2") +
  geom_segment(aes(xend = no_pool, yend = no_pool),
               arrow = arrow(length = unit(0.1, "cm")),
               color = "#D55E00", alpha = 0.5) +
  geom_point(size = 2.5, color = "#D55E00") +
  annotate("text", x = max(shrink_df$no_pool, na.rm = TRUE),
           y = grand_mean + 0.15, label = "Grand Mean",
           color = "#0072B2", hjust = 1, size = 3.5) +
  labs(title = "Shrinkage: No-Pooling vs. Partial-Pooling Estimates",
       subtitle = "Arrows show how GLMM pulls extreme estimates toward the grand mean",
       x = "No-pooling estimate (individual GLM intercept)",
       y = "Partial-pooling estimate (GLMM BLUP)") +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**:

- 各点は 1 人の sender。破線は「収縮なし」（no-pooling = partial-pooling）の対角線。
- **矢印**: no-pooling 推定値（矢印の先端）が GLMM 推定値（点）に引き寄せられる方向を示す。
- **極端な推定値ほど大きく収縮**する（対角線からの乖離が大きい）。
- 青点線は全体平均（grand mean）。GLMM は全ての推定値をここに向かって引き寄せる。

::: {.callout-note}
#### なぜ収縮は良いことなのか

- **少数データの過適合を防ぐ**: ペア数が少ない sender の推定値は不安定。GLMM はそれを全体平均で安定化させる。
- **統計学的には正則化（regularization）と等価**: Ridge 回帰のペナルティと同じ効果を自然に持つ。
- **予測精度の向上**: 新しいデータに対する予測は、no-pooling より partial-pooling のほうが平均的に正確。
:::

#### `sjPlot` による GLMM の可視化ギャラリー {.tabset}

`sjPlot::plot_model()` は `type` 引数を変えるだけで多様な可視化を生成します。以下に GLMM で有用な type を一覧します。

##### type = "est"（固定効果フォレストプロット）

```{r}
#| eval: true
#| label: sjplot-est
#| fig-height: 4
#| fig-width: 7

# 固定効果の係数を OR スケールでフォレストプロット表示
sjPlot::plot_model(
  m_glmm_ri,
  type = "est",
  show.values = TRUE,   # 係数の数値を表示
  show.p = TRUE,         # 有意性マークを表示
  sort.est = TRUE,       # 効果量順にソート
  title = "Fixed Effects (OR scale)",
  vline.color = "grey70"
) + theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**: 各点が固定効果の OR 推定値、水平バーが 95% CI。**縦の灰色線（OR = 1）を CI がまたがなければ有意**。`sort.est = TRUE` で効果量順にソートされているため、最も影響の大きい予測子が一目でわかります。論文の Figure として最も使われる形式です。

##### type = "std2"（標準化係数 — Gelman 法）

```{r}
#| eval: true
#| label: sjplot-std2
#| fig-height: 4
#| fig-width: 7

# Gelman (2008) の 2SD 標準化: 2値変数と連続変数の効果を直接比較可能
sjPlot::plot_model(
  m_glmm_ri,
  type = "std2",
  show.values = TRUE,
  title = "Standardized Coefficients (2 SD, Gelman 2008)"
) + theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**: 係数が「2 SD あたりの変化」に標準化されているため、2 値変数（same_class）と連続変数（score_sender_sc 等）の**効果の大きさを直接比較**できます[@gelman2008]。通常の標準化（`type = "std"`）は 1 SD 単位ですが、Gelman の 2 SD 法は 2 値変数のスケール（0→1 の変化 ≈ 2 SD）と自然に揃います。

##### type = "pred"（予測プロット）

```{r}
#| eval: true
#| label: sjplot-pred-gallery
#| fig-height: 4.5
#| fig-width: 9

# 2 つの予測子の効果を並べて表示
p_sender <- sjPlot::plot_model(
  m_glmm_ri, type = "pred",
  terms = "score_sender_sc",
  pred.type = "fe",
  title = "Effect of Sender Score"
) + theme_bw(base_size = 11, base_family = base_font)

p_class <- sjPlot::plot_model(
  m_glmm_ri, type = "pred",
  terms = "same_class",
  pred.type = "fe",
  title = "Effect of Same Class"
) + theme_bw(base_size = 11, base_family = base_font)

patchwork::wrap_plots(p_sender, p_class, nrow = 1)
```

**この図の読み方**: `pred.type = "fe"` は**固定効果のみの母平均的予測**（ランダム効果を平均化）。灰色帯は 95% CI。左パネルは sender のスコアが高いほど助言確率が上がるか。右パネルは同クラス vs 異クラスの予測確率差。

#### `sjPlot::tab_model()` によるモデル比較表（出版用）

```{r}
#| eval: true
#| label: sjplot-tab-compare

# GLM, GLMM-RI, GLMM-RS を 1 つの表で比較
sjPlot::tab_model(
  m_glm, m_glmm_ri, m_glmm_rs,
  show.icc = TRUE,        # ICC を表示
  show.re.var = TRUE,      # ランダム効果の分散を表示
  show.ngroups = TRUE,     # グループ数を表示
  show.aic = TRUE,         # AIC を表示
  p.style = "numeric_stars",  # p 値 + 星印の両方
  dv.labels = c("GLM", "GLMM (RI)", "GLMM (RS)"),
  title = "Model Comparison: GLM → GLMM"
)
```

**この表の読み方**:

- **3 列の比較**: 左から GLM（ランダム効果なし）、GLMM-RI（ランダム切片）、GLMM-RS（ランダム傾き）。
- **SE の変化に注目**: GLMM では SE が GLM より大きくなるのが通常（標準誤差が正直になった証拠）。
- **ICC**: ランダム効果が説明する分散の割合。高いほど GLMM が重要。
- **Random Effects**: 各グルーピング変数のランダム効果分散。0 に近い場合は過剰パラメータ化の可能性。
- **AIC**: 小さいほど良い。モデル間で比較してランダム効果の追加が正当化されるかを判断。

::: {.callout-tip}
#### `tab_model()` のカスタマイズの方法

- **`p.style = "stars"`**: 星印のみ（APA 向けにすっきり）
- **`collapse.ci = FALSE`**: CI を別列に分けて表示（Excel へのコピペがしやすい）
- **`show.se = TRUE`**: SE 列を追加
- **`pred.labels`**: 予測子名を読みやすくカスタマイズ: `pred.labels = c("Same class", "Proficiency gap", ...)`
- **`file = "table.html"`**: HTML ファイルに保存（Word で開ける）
- **`CSS`**: スタイルをカスタマイズ。例: `CSS = list(css.table = "border: 2px solid;")` で枠線追加
:::

### ② リンク関数の選択

ランダム効果構造が決まったら、次はリンク関数を選択します。5 章で学んだように、ロジスティック GLMM でも `logit`（標準）、`probit`（潜在正規尺度）、`cloglog`（非対称）の 3 つを選べます。

```{r}
#| eval: true
#| label: link-compare-glmm

# リンク関数だけ変えて比較（固定効果・ランダム構造は同一）
m_glmm_probit <- glmer(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 | sender) + (1 | receiver),
  family = binomial(link = "probit"),   # 潜在正規尺度
  data = dat_bin,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)

m_glmm_cloglog <- glmer(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 | sender) + (1 | receiver),
  family = binomial(link = "cloglog"),  # 非対称リンク
  data = dat_bin,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)

# 3 リンク関数を AIC/BIC で比較
compare_models_gt(
  m_glmm_ri, m_glmm_probit, m_glmm_cloglog,
  title = "② リンク関数の比較: logit vs probit vs cloglog"
)
```

**出力の読み方**:

| 状況 | 判断 |
|------|------|
| ΔAIC < 2（3 モデル間） | 実質的差なし → 理論的根拠で選択 |
| 1 つのリンクが ΔAIC > 4 で優位 | そのリンクの使用を検討（ただし理論的根拠も確認） |
| AIC がほぼ同等 | **logit を採用**（以下の理由） |

AIC がほぼ同等であれば、**logit を採用するのが実務上の推奨**です。理由は（1）$\exp(\beta)$ = OR（オッズ比）として直接解釈可能、（2）読者が最も慣れている、（3）先行研究との比較が容易、の 3 点です。

probit を選ぶべきなのは、SDT の $d'$ を直接推定したい場合（文法性判断・語彙判断など）に限られます[@decarlo1998]。cloglog は離散時間ハザード分析（学期ごとの脱落率など）で理論的に適切です[@singer2003]。

::: {.callout-note}
#### 係数のスケーリングに注意

logit と probit の係数は数値が約 1.7 倍異なります（$\beta_{\text{logit}} \approx 1.7 \times \beta_{\text{probit}}$）。モデルを比較する際に係数の大きさだけを見て「probit のほうが効果が小さい」と誤解しないでください。**予測確率は両リンクでほぼ同一**です。
:::

#### GLM vs GLMM の係数比較（中間確認）

ここまでの選択を確認するため、`sjPlot::tab_model()` で GLM と GLMM の係数を並べて表示します。**SE の変化**に注目してください。

```{r}
#| eval: true
#| label: sjplot-model-compare

# GLM と GLMM の係数を並べて比較する表
# → SE の変化と有意性の変化に注目
sjPlot::tab_model(
  m_glm, m_glmm_ri,
  show.aic = TRUE,   # AIC を表示
  show.se  = TRUE,   # 標準誤差を表示
  dv.labels = c("GLM（基準）", "GLMM（ランダム切片）")
)
```

**注目すべき点**：

- **SE の変化**: GLMM の SE が GLM より大きくなっていれば、GLM が SE を過小評価していた証拠。
- **有意性の変化**: GLM では有意だった予測子が GLMM では非有意になることがある。これは「見かけの有意性」が依存構造の無視によるものだったことを示す。
- **ICC / R²**: 表の下部に表示される。conditional R² が marginal R² より大きければ、ランダム効果が重要。

### ③ 固定効果の比較（ランダム構造は固定）

最後に、採用したランダム構造（ここでは `m_glmm_ri`）を固定したまま、固定効果を比較します[@lme4pvalues; @meteyard2020]。

以下では `update()` を使い、`m_glmm_ri` から各予測子を 1 つずつ落としたモデルを作成します。これにより「その予測子を入れることで適合がどれだけ改善するか」を個別に検証できます。

```{r}
#| eval: true
#| label: fit-fixed-effect-candidates

# update() で「フルモデルから 1 予測子だけ落としたモデル」を作成
# → 各予測子の寄与を個別に検証するため
# 注: score_gap は score_sender_sc と score_receiver_sc から算出した派生変数なので、
#     ここでは主要な 4 予測子のみを個別に検証する
m_glmm_drop_same     <- update(m_glmm_ri, . ~ . - same_class)
m_glmm_drop_prof     <- update(m_glmm_ri, . ~ . - prof_gap)
m_glmm_drop_sender   <- update(m_glmm_ri, . ~ . - score_sender_sc)
m_glmm_drop_receiver <- update(m_glmm_ri, . ~ . - score_receiver_sc)
```

次に LRT で各予測子の寄与を正式に検定します。`drop1()` は「フルモデルから各予測子を 1 つずつ落とし、適合が有意に悪化するかを一括検定する」便利関数です。

```{r}
#| eval: true
#| label: lrt-fixed-effects

# drop1(): 各予測子の LRT を一括実行
# → 「この予測子を落とすと適合がどれだけ悪化するか」を検定
drop1(m_glmm_ri, test = "Chisq")
```

**`drop1()` 出力の読み方**：

| 出力項目 | 読み方 |
|---------|--------|
| `Df` | その予測子のパラメータ数（通常 1）|
| `AIC` | その予測子を落とした場合の AIC。フルモデルより大きければ、落とすと悪化する |
| `LRT` | 尤度比統計量。大きいほど、その予測子の寄与が大きい |
| `Pr(Chi)` | p 値。p < .05 なら、その予測子はモデルの適合に有意に寄与 |

**出力の確認**: p < .05 の予測子はモデルの適合に有意に寄与しています。非有意な予測子でも理論的根拠があれば（統制変数など）残すのが一般的です。全予測子が非有意であっても、ランダム効果の追加は ICC/LRT で正当化されています。

::: {.callout-warning}
#### Wald 検定と LRT の p 値は異なることがある

`summary(model)` の z 値と p 値は **Wald 検定**に基づいており、`drop1()` の p 値は**尤度比検定（LRT）**に基づいています。小標本や推定が不安定な場合、両者の結論が異なることがあります。一般に **LRT のほうが信頼性が高い**ため、固定効果の有意性判断には `drop1()` や `anova()` を優先してください[@bolker2009]。`summary()` の p 値は効果の方向と大まかな有意性の確認に使い、最終的な報告には LRT を使うのが安全です。
:::

::: {.callout-tip collapse="true"}
#### 個別に `anova()` で確認したい場合（クリックで展開）

`drop1()` の結果をより詳細に確認したい場合は、個別の `anova()` で 2 モデル比較ができます：

```r
# 例：same_class の寄与を個別に検定
anova(m_glmm_drop_same, m_glmm_ri, test = "Chisq")
```
:::

```{r}
#| eval: true
#| label: compare-fixed-performance

compare_models_gt(
  m_glmm_drop_same, m_glmm_drop_prof,
  m_glmm_drop_sender, m_glmm_drop_receiver, m_glmm_ri,
  title = "③ 固定効果候補の比較"
)
```

**この表の読み方**: 各行はフルモデル（`m_glmm_ri`）から 1 つの予測子を除いたモデルです。AIC がフルモデルより**大きい**ほど、その予測子の除外がモデルを悪化させることを意味します。逆に AIC が同等以下なら、その予測子は省略可能です。`R2_conditional` が `R2_marginal` を大きく上回っていれば、ランダム効果（個人差）が予測に大きく寄与していることを示します。

::: {.callout-note}
#### ①②③ の結果を踏まえた最終判断

- **①** ランダム効果構造: AIC が改善し、かつ特異適合でないモデルを優先。本データでは `m_glmm_ri`（sender + receiver のランダム切片）を採用。
- **②** リンク関数: AIC が同等なら logit を採用（OR で解釈可能なため）。
- **③** 固定効果: LRT・AIC/BIC・理論整合性を総合的に判断。統制変数として投入した予測子は、非有意でも残すのが一般的。
- 境界問題を含む比較では p 値は近似なので、`compare_performance()` と診断を併用する。
:::

## 交互作用の検討（実践例）

ステップ 1-3 で最終的な加法モデルが決まりましたが、理論的に重要な交互作用がないかも確認する必要があります。

ここまでのモデルは全て**加法モデル**（予測子の効果が独立）でした。しかし SLA 研究では「**ある効果が別の条件によって異なるか**」が主要な研究の問いになることが多いです。例えば：

- 「同クラスの効果は、熟達度差が大きいペアほど強いのか？」（same_class × prof_gap）
- 「送り手のスコアの効果は、同クラスか異クラスかで異なるか？」（score_sender_sc × same_class）

### 加法モデル vs 交互作用モデルの比較

```{r}
#| eval: true
#| label: interaction-model

# ── 交互作用モデルの推定 ──
# same_class × prof_gap: 同クラスの効果が熟達度差によって変わるか
m_glmm_int <- glmer(
  tie ~ same_class * prof_gap + score_sender_sc + score_receiver_sc + score_gap +
    (1 | sender) + (1 | receiver),
  family = binomial(link = "logit"),
  data = dat_bin,
  control = glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
)

# ── 加法モデル vs 交互作用モデルの LRT ──
anova(m_glmm_ri, m_glmm_int)
```

**LRT の読み方**: p < .05 なら交互作用が有意 = 「same_class の効果は prof_gap によって異なる」。p ≥ .05 なら加法モデルで十分 = 「same_class の効果は prof_gap に依存しない」。

```{r}
#| eval: true
#| label: interaction-comparison-table

# AIC/BIC/R² で比較
compare_models_gt(
  m_glmm_ri, m_glmm_int,
  title = "Additive vs. Interaction Model"
)
```

### 交互作用の可視化

交互作用が有意かどうかにかかわらず、可視化は解釈に不可欠です。

```{r}
#| eval: true
#| label: interaction-plot
#| fig-height: 5
#| fig-width: 9

# ── sjPlot で交互作用を可視化 ──
# same_class × prof_gap の予測確率
sjPlot::plot_model(
  m_glmm_int, type = "pred",
  terms = c("prof_gap", "same_class"),
  pred.type = "fe",
  title = "Interaction: Same Class × Proficiency Gap"
) + theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**:

- 2 本の線（same_class = 0 と 1）が**平行に近い**なら交互作用は弱い。
- 線が**交差するか、間隔が変わる**なら交互作用がある。
- 例えば「同クラスの効果（2 線の差）が prof_gap = 0 では大きいが、prof_gap = 2 では小さい」なら、熟達度差が大きいと同クラスの利点が薄れることを意味する。

### 交互作用の emmeans による分解

交互作用が見つかった場合（あるいは理論的に重要な場合）、`emmeans` で**条件ごとの推定確率**と**単純効果**を求めます。ここでの役割分担は明確です。`pairs(emm_int)` が**どの simple effect が有意か**を検定し、`emmip()` は**その検定の土台になっている推定周辺平均の形**を応答スケールで見せます[@emmeans_emmip]。

```{r}
#| eval: true
#| label: interaction-emmeans
#| fig-height: 4.5
#| fig-width: 8

# ── 条件ごとの推定周辺平均 ──
# prof_gap の各水準ごとに same_class の効果を分解
emm_int <- emmeans(m_glmm_int, ~ same_class | prof_gap,
                   type = "response",
                   at = list(prof_gap = c(0, 1, 2)))

# 推定確率の表
as.data.frame(emm_int) |>
  dplyr::mutate(dplyr::across(where(is.numeric), \(x) round(x, 3))) |>
  gt::gt() |>
  gt::tab_header(
    title = "Estimated Probabilities: Same Class Effect at Each Proficiency Gap",
    subtitle = "type = 'response' for probability scale"
  )

# ── 単純効果: prof_gap の各水準での same_class の OR ──
pairs(emm_int, adjust = "none")  # 2群なので補正不要
```

**読み方**:

- **推定確率表**: prof_gap = 0, 1, 2 のそれぞれで、同クラス（1）と異クラス（0）の予測確率を比較。
- **`pairs()` 出力**: 各 prof_gap 水準での same_class の OR と p 値。OR が prof_gap によって異なれば、交互作用の実質的な意味が明確になる。

```{r}
#| eval: true
#| label: interaction-emmeans-plot
#| fig-height: 4.5
#| fig-width: 7

# ── 交互作用の emmeans プロット ──
emmeans::emmip(m_glmm_int, same_class ~ prof_gap,
               type = "response",
               at = list(prof_gap = c(0, 1, 2)),
               CIs = TRUE) +
  labs(title = "Interaction Plot: emmeans (probability scale)",
       subtitle = "Lines = same_class levels; Error bars = 95% CI",
       x = "Proficiency Gap", y = "Predicted Probability",
       color = "Same Class") +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**:

- `emmip()` は、上の `emm_int` と **同じ EMM グリッド**を描いています。したがって、この図は `pairs(emm_int)` の simple effect をそのまま絵にしたものです。
- 2 本の線の**縦の差**が、その `prof_gap` 水準における same_class の simple effect に対応します。
- もし `prof_gap = 0` で 2 線の差が大きく、`prof_gap = 2` で差が縮むなら、「同クラス効果は熟達度差が小さいときに強い」という解釈になります。
- つまり `emmip()` は「交互作用があるか」を眺める図ではなく、**交互作用を分解したあと、どの水準でどれだけ差があるか**を読む図です。

::: {.callout-tip}
#### 交互作用の報告テンプレート

**交互作用が有意な場合**:

> A significant interaction between same-class membership and proficiency gap was observed (OR = X.XX, 95% CI [LL, UU], *p* = .XXX). To decompose this interaction, simple effects of same-class membership were examined at each level of proficiency gap using `emmeans`. At prof_gap = 0, same-class pairs showed significantly higher advice probability (OR = X.XX, *p* = .XXX); at prof_gap = 2, this effect was [reduced / non-significant] (OR = X.XX, *p* = .XXX).

**交互作用が非有意な場合**:

> The interaction between same-class membership and proficiency gap was not significant (χ²(1) = X.XX, *p* = .XXX, ΔAIC = X.X). The additive model was retained, indicating that the effect of same-class membership did not vary as a function of proficiency gap.
:::

## 固定効果の解釈（OR）

logit スケールの係数（β）はそのままでは解釈しにくいため、**オッズ比（OR = exp(β)）** に変換して報告します。OR は「その予測子が 1 単位増加したとき、イベント（助言関係）のオッズが何倍になるか」を示します。

### 方法 A: `sjPlot::tab_model()` で OR を表示（実務向け）

`tab_model()` は係数を自動的に OR に変換し、95% CI 付きの表を生成します[@sjplot_tab_model]。
**論文やレポートに使う表はこちらで十分**です。

```{r}
#| eval: true
#| label: or-sjplot

sjPlot::tab_model(
  m_glmm_ri,
  show.se = TRUE,      # 標準誤差を表示
  show.stat = TRUE,    # z 値を表示
  dv.labels = "GLMM (Random Intercepts)",
  title = "Fixed Effects as Odds Ratios"
)
```

### 方法 B: 手計算で OR 表を作る（原理理解のため）

`sjPlot::tab_model()` の裏側では `exp(β)` の計算が行われています。
以下のコードでこれを手動で再現します。**OR = exp(logit 係数)**、**CI = exp(係数 ± 1.96 × SE)** です。

```{r}
#| eval: true
#| label: or-table

coef_mat <- summary(m_glmm_ri)$coefficients

or_tbl <- tibble(
  term           = rownames(coef_mat),
  estimate_logit = coef_mat[, "Estimate"],
  std_error      = coef_mat[, "Std. Error"],
  z_value        = coef_mat[, "z value"],
  p_value        = coef_mat[, "Pr(>|z|)"],
  OR      = exp(estimate_logit),                       # exp(β)
  OR_low  = exp(estimate_logit - 1.96 * std_error),    # 95% CI 下限
  OR_high = exp(estimate_logit + 1.96 * std_error)     # 95% CI 上限
)

or_tbl |>
  gt() |>
  tab_header(title = "Fixed Effects as Odds Ratios（手計算版）") |>
  fmt_number(columns = c(estimate_logit, std_error, z_value, OR, OR_low, OR_high), decimals = 3) |>
  fmt_scientific(columns = p_value, decimals = 2) |>
  cols_label(
    term = "Term", estimate_logit = "β (logit)", std_error = "SE",
    z_value = "z", p_value = "p", OR = "OR",
    OR_low = "OR 2.5%", OR_high = "OR 97.5%"
  )
```

方法 A と方法 B の OR 値が一致することを確認してください。

::: {.callout-note}
#### Wald CI vs Profile CI

上の 95% CI は **Wald 近似**（$\exp(\hat{\beta} \pm 1.96 \times \text{SE})$）で計算しています。これは最も一般的な方法ですが、サンプルサイズが小さい場合や推定値が極端な場合に精度が落ちることがあります。より正確な CI が必要な場合は**プロファイル尤度 CI** を使用してください：

```r
# プロファイル尤度 CI（計算に時間がかかるが、Wald より正確）
ci_profile <- confint(m_glmm_ri, method = "profile", parm = "beta_")
exp(ci_profile)  # OR スケールに変換
```

実務上、サンプルサイズが十分（各群 30+ 観測）であれば Wald CI で問題ありません。
:::

**OR 表の全予測子の解釈**（以下は出力例に基づく解釈パターンです。実際の数値で読み替えてください）：

| 予測子 | 解釈の読み方 |
|--------|-------------|
| `(Intercept)` | 基準条件（same_class=0, prof_gap=0, 全スコア=0）でのオッズ。OR < 1 ならベースラインで助言関係は少ない |
| `same_class` | **同クラスのペアは異クラスに比べて OR 倍のオッズで助言関係をもつ**。OR > 1 なら同クラスが助言を促進 |
| `prof_gap` | 熟達度差が 1 段階広がるごとに、助言のオッズが OR 倍に変化。OR < 1 なら差が大きいほど助言が減る |
| `score_sender_sc` | 送り手のスコアが 1 SD 高いごとに、助言のオッズが OR 倍に変化 |
| `score_receiver_sc` | 受け手のスコアが 1 SD 高いごとに、助言のオッズが OR 倍に変化 |
| `score_gap` | sender と receiver の z スコアの絶対差が 1 単位増えるごとに、助言のオッズが OR 倍に変化（注: これは元のスコアの 1 SD 差ではなく、z 変換後の絶対差 1 単位） |

::: {.callout-tip}
### OR 報告の 4 点セット

1. **方向**: OR > 1（促進）か OR < 1（抑制）か
2. **OR と 95% CI**: OR = X.XX, 95% CI [LL, UL]
3. **p 値**: 統計的有意性
4. **確率差への変換**（応答スケール）: 次のセクションで `emmeans` を使って報告
:::

#### OR と確率差は別物

OR は**確率差そのもの**ではありません。例えば OR = 2.0 は「オッズが 2 倍」であり、確率が 2 倍になるわけではありません。確率差を知るには、次のセクションの応答スケール予測が必要です。

### OR の大きさの目安（効果量ベンチマーク）

OR の大きさは Cohen's *d* に変換して解釈できます。変換式は $d = \ln(\text{OR}) / 1.81$ です[@chinn2000; @chen2010]。

| 効果の大きさ | Cohen's *d* | 対応する OR |
|:---:|:---:|:---:|
| 小 (small) | 0.2 | 1.68 |
| 中 (medium) | 0.5 | 3.47 |
| 大 (large) | 0.8 | 6.71 |

**注意**: この変換式 $d = \ln(\text{OR})/1.81$ は、潜在変数がロジスティック分布に従うという仮定のもとで導出されています[@chinn2000]。OR そのものはベースラインの確率に依存しない指標ですが、OR から**リスク比（RR）やリスク差**に換算する場合はベースラインの確率に強く依存します。OR を報告する際には、この表と比較して効果の実質的な大きさを論じると、査読者に伝わりやすくなります。

## 応答スケールでの報告（予測確率）

```{r}
#| eval: true
#| label: prediction-target-cheatsheet
#| echo: false

tibble::tribble(
  ~Target, ~What_is_averaged, ~Generalization_target, ~Typical_function,
  "Observed proportion", "実測 0/1 をそのまま平均", "このサンプルの記述統計", "mean(), summarise()",
  "EMM on response scale", "他の共変量を代表値に固定した周辺平均", "条件比較の平均的効果", "emmeans(..., type = \"response\")",
  "Marginal prediction", "ランダム効果を平均化した予測", "新しい learner / item への一般化", "predict(..., re.form = NA), ggpredict(type = \"fixed\")",
  "Conditional prediction", "観測された learner / item のランダム効果も含む予測", "このサンプル内での fitted probability", "predict(..., re.form = NULL)",
  "Unknown-at-pretest prediction", "pretest_status = incorrect に条件づけた平均", "未知語に対する学習効果", "emmeans(..., at = list(pretest_status = ...))"
) |>
  knitr::kable(
    caption = "この章で使う予測量の早見表: 同じ『確率』でも意味は同じではない"
  )
```

この表を先に頭に入れておくと、後の予測図がかなり読みやすくなります。特に混乱しやすいのは、**EMM**, **marginal prediction**, **conditional prediction** の違いです。どれも 0〜1 の確率を返しますが、「何を平均しているか」と「誰に一般化するか」が違います。

### 群比較（same class）

`emmeans()` は、他の予測子を平均的な値に固定したうえで、特定の条件における推定周辺平均（EMMs）を算出します。`type = "response"` を指定すると、logit スケールではなく**確率スケール**で結果を返します。

```{r}
#| eval: true
#| label: emmeans-class

# same_class の各水準での推定確率（他の予測子は平均値に固定）
# same_class は integer (0/1) なので、at で評価点を明示的に指定
emm_class <- emmeans(m_glmm_ri, ~ same_class, type = "response",
                     at = list(same_class = c(0, 1)))

# 確率スケールでの結果表
as.data.frame(emm_class) |>
  knitr::kable(caption = "Predicted Probability by Same-Class Condition")

# ペアワイズ比較（OR と p 値）
pairs(emm_class)
```

**EMMs 表の読み方**：

| 列名 | 意味 |
|------|------|
| `prob` | 各条件での推定確率（他の予測子は平均値に固定した周辺平均） |
| `SE` | 推定確率の標準誤差（デルタ法による近似） |
| `df` | 自由度（GLMM では `Inf` = 漸近正規近似） |
| `asymp.LCL` / `asymp.UCL` | 確率の 95% 信頼区間（漸近的） |

**確率差**: 2 つの `prob` の差が、same_class の実質的効果を示す。例えば 0.15 vs 0.25 なら、同クラスは約 10 ポイント高い。

**`pairs()` の読み方**：

- `odds.ratio`: 2 条件間の OR（OR 表の same_class の OR と一致するはず）。
- `p.value`: ペアワイズ比較の p 値。

#### 多重比較の補正

`pairs()` はデフォルトで **Tukey 法**による多重比較補正を行います。2 群比較の場合は補正の影響はありませんが、3 群以上（例：A2/B1/B2）の場合は重要です：

```r
# 3 群比較での emmeans
emm_prof <- emmeans(model, ~ proficiency, type = "response")
pairs(emm_prof)                          # デフォルト: Tukey 補正
pairs(emm_prof, adjust = "bonferroni")   # Bonferroni 補正
pairs(emm_prof, adjust = "none")         # 補正なし（非推奨）
```

**報告テンプレート（emmeans ペアワイズ比較）**:

> Estimated marginal means on the probability scale were computed using the `emmeans` package (Lenth, 2024). Same-class pairs showed a higher predicted probability of advice ties (*p* = XX.XX, 95% CI [LL, UU]) compared to different-class pairs (*p* = XX.XX, 95% CI [LL, UU]). Pairwise comparison yielded OR = X.XX, 95% CI [LL, UU], *p* = .XXX [Tukey-adjusted for K comparisons].

### 下位検定の可視化（Case: same class）

EMMs をプロットで可視化します。エラーバーが重ならないほど、条件間の差が明確です。

```{r}
#| eval: true
#| label: emmeans-class-plot

emm_class_df <- as.data.frame(emm_class) |>
  mutate(
    same_class = factor(
      same_class,
      levels = c(0, 1),
      labels = c("Different class", "Same class")
    )
  )

ggplot(emm_class_df, aes(x = same_class, y = prob, ymin = asymp.LCL, ymax = asymp.UCL)) +
  geom_pointrange(size = 0.8) +
  labs(
    x = "Dyad Class Relation",
    y = "Predicted Probability of Advice Tie",
    title = "Post-hoc EMMs with 95% CI"
  )
```

**図の読み方**: 点が推定確率、縦線が 95% CI。2 条件の CI が重なっていなければ、差は統計的に有意と視覚的に判断できます（ただし正式な検定は `pairs()` の p 値で行う）。

次に `sjPlot::plot_model()` で連続予測子の効果を可視化します。`pred.type = "fe"` は固定効果のみによる予測（母平均的予測）です。`plot_model()` は**まず全体像を素早くつかむ**のに向いています[@sjplot_plot_model]。

```{r}
#| eval: true
#| label: sjplot-glmm-pred

# score_sender_sc を連続的に動かし、same_class 別に予測確率をプロット
# pred.type = "fe" → 固定効果のみの母平均的予測
sjPlot::plot_model(
  m_glmm_ri,
  type = "pred",
  terms = c("score_sender_sc", "same_class [0,1]"),
  pred.type = "fe",
  show.data = FALSE
) +
  labs(
    x = "Sender score (z)",
    y = "Predicted probability of advice tie",
    color = "Same class",
    fill = "Same class"
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "bottom")
```

**図の読み方**: 線が予測確率、帯が 95% CI。2 つの線（same_class = 0 vs 1）の差が same_class の効果を確率スケールで示します。次のチャンクでは、この予測をより細かく制御する方法を示します。

### `ggeffects::ggpredict()` で予測グリッドを明示的に作る

`sjPlot::plot_model(type = "pred")` は内部で `ggeffects` を使って予測値を作っています。図をすぐ描くなら `plot_model()` が便利ですが、**どの x 値・どの条件の組み合わせで予測したかを明示的に確認したい**場合は、`ggeffects::ggpredict()` を直接使う方が透明です[@ggeffects_ggpredict]。

```{r}
#| eval: true
#| label: ggpredict-same-class
#| fig-height: 4.8
#| fig-width: 8.5

ggpred_sender <- ggeffects::ggpredict(
  m_glmm_ri,
  terms = c("score_sender_sc [-2:2 by=0.25]", "same_class [0,1]"),
  type = "fixed"
)

ggpred_sender |>
  dplyr::mutate(
    x = as.numeric(x),
    group = factor(group,
                   levels = c("0", "1"),
                   labels = c("Different class", "Same class"))
  ) |>
  ggplot(aes(x = x, y = predicted, color = group, fill = group)) +
  geom_ribbon(aes(ymin = conf.low, ymax = conf.high), alpha = 0.18, color = NA) +
  geom_line(linewidth = 1) +
  scale_color_manual(values = c("Different class" = "#7F7F7F", "Same class" = "#D55E00")) +
  scale_fill_manual(values = c("Different class" = "#BDBDBD", "Same class" = "#F4A582")) +
  labs(
    title = "Adjusted Predictions from ggpredict()",
    subtitle = "The prediction grid is explicit, so the x values and groups are easy to inspect.",
    x = "Sender score (z)",
    y = "Predicted probability of advice tie",
    color = NULL,
    fill = NULL
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "bottom")
```

**この図の読み方**:

- `ggpredict()` の利点は、**予測に使ったグリッド自体がデータフレームで残る**ことです。`score_sender_sc` を -2 から +2 まで 0.25 刻みで動かし、same_class を 0/1 に固定したことがコードから直接分かります。
- 線と帯の意味は `plot_model(type = "pred")` と同じですが、こちらは **予測値を別の図や表へ再利用しやすい** という実務上の利点があります。
- 章の中では、`sjPlot` を「素早い俯瞰」、`ggeffects` を「予測グリッドを明示する再利用可能な土台」として使い分けると理解してください。

### 語彙研究の応用: item-level pre-test を統制した予測プロット

ここからが、語彙研究では特に重要な応用です。`m_vocab_pretest` は **post-test の 0/1 正答**を目的変数にしつつ、`pretest_status` を observation-level covariate として入れています。したがって、同じ「予測プロット」でも、少なくとも次の 3 種類を区別する必要があります。

1. **raw post-test proportion**: 実測の post-test 正答率。pre-test の既知語が混ざる。
2. **adjusted prediction over the model**: pre-test, item, learner を統制した母平均的予測。
3. **counterfactual prediction for unknown words**: `pretest_status = "Incorrect at pre-test"` に固定した予測。  
   これは「最初は知らなかった語に限ると、学習後の正答確率はどう違うか」を見る図です。

```{r}
#| eval: true
#| label: vocab-pretest-raw-vs-adjusted
#| fig-height: 5
#| fig-width: 11

vocab_post_raw <- vocab_prepost_dat |>
  dplyr::group_by(treatment, proficiency) |>
  dplyr::summarise(
    posttest_prob = mean(posttest_correct),
    .groups = "drop"
  )

p_vocab_raw <- ggplot(
  vocab_post_raw,
  aes(x = proficiency, y = posttest_prob, color = treatment, group = treatment)
) +
  geom_point(size = 2.8) +
  geom_line(linewidth = 0.9) +
  scale_color_manual(values = c(Control = "#7F7F7F", Gloss = "#D55E00")) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1), limits = c(0, 1)) +
  labs(
    title = "Raw Post-test Accuracy",
    subtitle = "This mixes genuinely learned words with items already known at pre-test.",
    x = "Proficiency",
    y = "Observed post-test accuracy",
    color = NULL
  ) +
  theme_bw(base_size = 11, base_family = base_font) +
  theme(legend.position = "bottom")

vocab_adj_pred <- ggeffects::ggpredict(
  m_vocab_pretest,
  terms = c("proficiency", "treatment", "pretest_status [Incorrect at pre-test,Correct at pre-test]"),
  type = "fixed"
)

p_vocab_adjusted <- vocab_adj_pred |>
  dplyr::mutate(
    x = factor(x, levels = c("A2", "B1", "B2")),
    group = factor(group, levels = c("Control", "Gloss")),
    facet = factor(
      facet,
      levels = c("Incorrect at pre-test", "Correct at pre-test")
    )
  ) |>
  ggplot(aes(x = x, y = predicted, color = group, group = group)) +
  geom_point(size = 2.5) +
  geom_line(linewidth = 0.9) +
  geom_errorbar(aes(ymin = conf.low, ymax = conf.high), width = 0.08, alpha = 0.7) +
  facet_wrap(~ facet) +
  scale_color_manual(values = c(Control = "#7F7F7F", Gloss = "#D55E00")) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1), limits = c(0, 1)) +
  labs(
    title = "Adjusted Post-test Predictions Controlling Item-Level Pre-test Status",
    subtitle = "The left panel isolates genuinely unknown words; the right panel shows ceiling-prone items.",
    x = "Proficiency",
    y = "Predicted post-test accuracy",
    color = NULL
  ) +
  theme_bw(base_size = 11, base_family = base_font) +
  theme(legend.position = "bottom")

p_vocab_raw + p_vocab_adjusted
```

**この図の読み方**:

- 左の raw 図は、`Gloss` 群の post-test が高く見えても、**それが learning なのか、もともと知っていた語が多かっただけなのか**を区別できません。
- 右の adjusted 図では、facet が `pretest_status` を分けています。  
  左 facet `Incorrect at pre-test` が、**本当に学習対象だった語**に対する予測です。ここで `Gloss > Control` なら、「未知語に対して介入が効いた」と読みやすくなります。
- 右 facet `Correct at pre-test` は ceiling に近い領域なので、ここで群差が小さくても不思議ではありません。むしろ、**この facet の情報を混ぜたまま全体平均だけを見ると、介入効果の解釈が鈍る**ことがあります。

```{r}
#| eval: true
#| label: vocab-pretest-unknown-emmeans

emm_vocab_unknown <- emmeans(
  m_vocab_pretest,
  ~ treatment | proficiency,
  type = "response",
  at = list(pretest_status = "Incorrect at pre-test")
)

as.data.frame(emm_vocab_unknown) |>
  knitr::kable(
    caption = "Predicted post-test probability for words that were unknown at pre-test"
  )

pairs(emm_vocab_unknown)
```

この `emmeans` は、**pre-test で未知だった語だけ**に焦点を当てた post-test 比較です。語彙介入研究で最も答えたい問いが「未習得語がどれだけ学習されたか」なら、この表のほうが全体平均より直接的です。

```{r}
#| eval: true
#| label: vocab-pretest-unknown-emmip
#| fig-height: 4.8
#| fig-width: 7.5

emmeans::emmip(
  m_vocab_pretest,
  treatment ~ proficiency,
  type = "response",
  at = list(pretest_status = "Incorrect at pre-test"),
  CIs = TRUE
) +
  labs(
    title = "Learning Effect for Words Unknown at Pre-test",
    subtitle = "EMMs are computed after conditioning on item-level pre-test failure.",
    x = "Proficiency",
    y = "Predicted post-test accuracy",
    color = NULL
  ) +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**:

- この `emmip()` は、「各 proficiency で、unknown-at-pre-test 語に対する `Gloss` と `Control` の差」を見ています。
- したがって、ここで読むべき縦の差は **純粋な介入効果に近い差**です。少なくとも、pre-test で既知だった語を混ぜた差ではありません。
- もし A2 で差が大きく B2 で差が小さいなら、「介入は、もともと語を知らなかった初級者により効きやすい」という解釈になります。

```{r}
#| eval: true
#| label: vocab-pretest-three-targets
#| echo: false

vocab_compare_targets <- dplyr::bind_rows(
  vocab_prepost_dat |>
    dplyr::group_by(treatment) |>
    dplyr::summarise(prob = mean(posttest_correct), .groups = "drop") |>
    dplyr::mutate(target = "Observed raw post-test"),
  as.data.frame(emmeans(m_vocab_pretest, ~ treatment, type = "response")) |>
    dplyr::transmute(treatment, prob, target = "Adjusted average over pre-test mix"),
  as.data.frame(emmeans(
    m_vocab_pretest, ~ treatment, type = "response",
    at = list(pretest_status = "Incorrect at pre-test")
  )) |>
    dplyr::transmute(treatment, prob, target = "Adjusted for words unknown at pre-test")
) |>
  dplyr::mutate(prob = round(prob, 3))

knitr::kable(
  vocab_compare_targets,
  caption = "同じモデルでも、何を平均するかで報告値は変わる"
)
```

この小表の意図は、**「どの予測値を報告するかは研究質問の一部である」**ことを明示することです。

- `Observed raw post-test`: 記述統計として有用。ただし既知語を含む。
- `Adjusted average over pre-test mix`: モデルで調整した周辺平均。全体的な post-test 成績の比較に向く。
- `Adjusted for words unknown at pre-test`: 語彙学習の効果そのものに最も近い。  
  介入研究で「学習されたか」を主張するなら、この条件つき予測を本文で前面に出す価値があります。

### 同じ語彙モデルでも、予測が「誰に対するものか」は違う

ここでもう 1 つ重要なのは、**同じ `m_vocab_pretest` から出した予測でも、それが誰に対する確率かは 1 通りではない**という点です。

- **marginal / population-level prediction**: `re.form = NA`。  
  「新しい learner・新しい item に一般化した、平均的な予測」
- **conditional / subject-item-specific prediction**: `re.form = NULL`。  
  「この learner とこの item のランダム切片も使った、観測サンプル内での fitted probability」

語彙研究で本文の主要結果として出すのは、通常は前者です。後者は、**このデータの中で誰が特に高く／低く当たりやすかったか**を反映するため、一般化可能性よりも sample-specific fit の側面が強くなります。

```{r}
#| eval: true
#| label: vocab-pretest-marginal-conditional
#| fig-height: 4.9
#| fig-width: 8.8

vocab_unknown_only <- vocab_prepost_dat |>
  dplyr::filter(pretest_status == "Incorrect at pre-test")

vocab_unknown_only$pred_marginal <- predict(
  m_vocab_pretest,
  newdata = vocab_unknown_only,
  type = "response",
  re.form = NA
)

vocab_unknown_only$pred_conditional <- predict(
  m_vocab_pretest,
  newdata = vocab_unknown_only,
  type = "response",
  re.form = NULL
)

vocab_unknown_pred <- vocab_unknown_only |>
  dplyr::group_by(treatment, proficiency) |>
  dplyr::summarise(
    Observed = mean(posttest_correct),
    `Marginal prediction` = mean(pred_marginal),
    `Conditional prediction` = mean(pred_conditional),
    .groups = "drop"
  ) |>
  tidyr::pivot_longer(
    cols = c(Observed, `Marginal prediction`, `Conditional prediction`),
    names_to = "target",
    values_to = "prob"
  )

ggplot(
  vocab_unknown_pred,
  aes(x = proficiency, y = prob, color = target, group = target)
) +
  geom_point(size = 2.4) +
  geom_line(linewidth = 0.9) +
  facet_wrap(~ treatment) +
  scale_color_manual(
    values = c(
      "Observed" = "#4E79A7",
      "Marginal prediction" = "#D55E00",
      "Conditional prediction" = "#59A14F"
    )
  ) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1), limits = c(0, 1)) +
  labs(
    title = "Unknown-at-Pre-test Words: Observed vs Marginal vs Conditional Prediction",
    subtitle = "Marginal = new learner/item average; Conditional = fitted to the observed learners and items.",
    x = "Proficiency",
    y = "Post-test probability",
    color = NULL
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "bottom")
```

**この図の読み方**:

- 青が実測、橙が **marginal prediction**、緑が **conditional prediction** です。
- 橙は「同じ fixed effects なら、新しい learner・新しい item にも平均的にこれくらいの確率で一般化できそうだ」という線です。**論文本文で主報告に向くのは通常こちら**です。
- 緑は観測された learner / item のランダム切片を使うので、sample-specific な fitted value になります。適合は良く見えやすいですが、**新しいサンプルへの一般化可能性をそのまま示すわけではありません**。
- 青と緑が近く、橙が少し離れることがあります。これは「このサンプルの learner / item の癖を入れるとよく当たるが、それを平均化するともう少し保守的な予測になる」ことを意味します。

```{r}
#| eval: true
#| label: vocab-pretest-prediction-audience
#| echo: false

tibble::tribble(
  ~Prediction, ~Question_it_answers, ~Typical_use,
  "Observed proportion", "このサンプルで実際に何%正答だったか", "記述統計・データ把握",
  "Marginal / population-level prediction", "新しい learner / item に一般化した平均的効果はどれくらいか", "本文の主要結果・介入効果の説明",
  "Conditional / subject-item-specific prediction", "この learner とこの item を含めるとどれくらい当たりやすいか", "適合確認・個別予測・サンプル内説明"
) |>
  knitr::kable(
    caption = "同じ GLMM でも、予測の対象が違えば読み方も変わる"
  )
```

したがって、語彙介入研究で `pre-test` を統制した予測を報告するときは、少なくとも次の 2 点を本文で明示するとよいです。

1. **pre-test を何として入れたか**  
   参加者総得点なのか、item-level の既知/未知なのか。
2. **予測が誰に対する確率か**  
   population-level 予測なのか、sample-specific fitted probability なのか。

この 2 点が曖昧だと、同じ「予測確率 0.62」という数字でも、査読者にはまったく違う意味に読まれてしまいます。

### 語彙研究の予測確率は、どこまで当てになるか: calibration と Brier score

2 値モデルでは、群差が見えることと、**確率予測そのものが妥当であること**は別です。そこで `unknown-at-pretest` 語について、`marginal` と `conditional` の 2 種類の予測を calibration と Brier score で比べます。Brier score は、予測確率と実際の 0/1 結果の二乗誤差の平均で、**小さいほどよい**指標です[@brier1950]。

```{r}
#| eval: true
#| label: vocab-pretest-brier
#| echo: false

vocab_brier_tbl <- tibble::tibble(
  Prediction = c("Marginal", "Conditional"),
  Brier = c(
    mean((vocab_unknown_only$posttest_correct - vocab_unknown_only$pred_marginal)^2),
    mean((vocab_unknown_only$posttest_correct - vocab_unknown_only$pred_conditional)^2)
  )
) |>
  dplyr::mutate(Brier = round(Brier, 4))

knitr::kable(
  vocab_brier_tbl,
  caption = "Brier score for unknown-at-pretest words"
)
```

```{r}
#| eval: true
#| label: vocab-pretest-calibration
#| fig-height: 4.8
#| fig-width: 8.5

cal_marg <- vocab_unknown_only |>
  dplyr::mutate(bin = dplyr::ntile(pred_marginal, 10)) |>
  dplyr::group_by(bin) |>
  dplyr::summarise(
    predicted = mean(pred_marginal),
    observed = mean(posttest_correct),
    n = dplyr::n(),
    .groups = "drop"
  ) |>
  dplyr::mutate(target = "Marginal")

cal_cond <- vocab_unknown_only |>
  dplyr::mutate(bin = dplyr::ntile(pred_conditional, 10)) |>
  dplyr::group_by(bin) |>
  dplyr::summarise(
    predicted = mean(pred_conditional),
    observed = mean(posttest_correct),
    n = dplyr::n(),
    .groups = "drop"
  ) |>
  dplyr::mutate(target = "Conditional")

vocab_calibration <- dplyr::bind_rows(cal_marg, cal_cond)

ggplot(vocab_calibration, aes(x = predicted, y = observed, color = target)) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed", color = "grey60") +
  geom_point(aes(size = n), alpha = 0.85) +
  geom_line(linewidth = 0.9) +
  scale_color_manual(values = c(Marginal = "#D55E00", Conditional = "#59A14F")) +
  scale_x_continuous(limits = c(0, 1), labels = scales::percent_format(accuracy = 1)) +
  scale_y_continuous(limits = c(0, 1), labels = scales::percent_format(accuracy = 1)) +
  labs(
    title = "Calibration for Words Unknown at Pre-test",
    subtitle = "Points close to the diagonal indicate well-calibrated probabilities.",
    x = "Mean predicted probability",
    y = "Observed proportion correct",
    color = NULL,
    size = "Bin n"
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "bottom")
```

**この図の読み方**:

- 破線の対角線が「予測 40% なら実際にも約 40%正答している」という理想線です。
- 橙の `marginal` は一般化用の予測なので、やや保守的で、対角線の近くにいれば「未知語に対する平均的な学習確率」として安心して使えます。
- 緑の `conditional` は sample-specific な fitted value なので、しばしば対角線により近づきます。これは良いことでもありますが、**新しい learner / item への一般化可能性まで保証するものではありません**。
- したがって、本文では `marginal prediction` を主報告し、`conditional prediction` は「このサンプルでの当てはまり確認」として補助的に使うのが安全です。

### 連続予測子を動かした確率曲線

以下では、送り手のスコア（`score_sender_sc`）を -2 〜 +2 SD の範囲で変化させ、`same_class` ごとに予測確率をプロットします。`re.form = NA` を指定することで、**ランダム効果を含まない母平均的な予測**（population-level prediction）を算出しています。

```{r}
#| eval: true
#| label: prediction-curve
#| cache: true

# ── 予測用の新しいデータフレームを作成 ──
# score_sender_sc を -2 〜 +2 SD で動かし、他は代表値で固定
new_pred <- tidyr::expand_grid(
  same_class        = c(0L, 1L),                   # 異クラス vs 同クラス
  prof_gap          = median(dat_bin$prof_gap),     # 熟達度差は中央値で固定
  score_sender_sc   = seq(-2, 2, length.out = 120), # 送り手スコアを変化
  score_receiver_sc = 0                             # 受け手は平均（z = 0）
)

new_pred$score_gap <- abs(new_pred$score_sender_sc - new_pred$score_receiver_sc)

# re.form = NA → ランダム効果を含まない母平均的な予測
# bootMer で信頼帯を計算（パラメトリックブートストラップ）
pred_fun <- function(model) {
  predict(model, newdata = new_pred, type = "response", re.form = NA)
}
new_pred$pred_prob <- pred_fun(m_glmm_ri)

# ブートストラップ CI（nsim=100 はデモ用; 本番は 500+）
boot_pred <- lme4::bootMer(m_glmm_ri, FUN = pred_fun, nsim = 100,
                           re.form = NA, type = "parametric", seed = 2024)
new_pred$ci_lower <- apply(boot_pred$t, 2, quantile, probs = 0.025)
new_pred$ci_upper <- apply(boot_pred$t, 2, quantile, probs = 0.975)

plot_pred <- new_pred |>
  mutate(
    same_class = factor(
      same_class,
      levels = c(0, 1),
      labels = c("Different class", "Same class")
    )
  )

ggplot(plot_pred, aes(x = score_sender_sc, y = pred_prob,
                       color = same_class, fill = same_class)) +
  geom_ribbon(aes(ymin = ci_lower, ymax = ci_upper), alpha = 0.2, color = NA) +
  geom_line(linewidth = 1) +
  labs(
    x = "Sender Baseline Score (z)",
    y = "Predicted Probability of Advice Tie",
    title = "Response-Scale Prediction from GLMM",
    subtitle = "Shaded ribbon = 95% parametric bootstrap CI (re.form = NA)",
    color = "Condition", fill = "Condition"
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "top")
```

**図の読み方**：

- **横軸**: 送り手のベースラインスコア（z スコア）。0 が平均、+1 が 1 SD 上。
- **縦軸**: 助言関係の予測確率。
- **2 本の曲線**: Same class（同クラス）と Different class（異クラス）。
- **曲線の間隔**: same_class の効果の大きさを確率スケールで示す。間隔が広いほど効果が大きい。
- **曲線の傾き**: score_sender_sc の効果。右上がりなら、スコアが高い送り手ほど助言を出しやすい。
- ロジスティック曲線のため、確率の中央付近（≈0.5）で効果が最も大きく、端（≈0 や ≈1）では効果が小さくなる（S 字カーブの性質）。

::: {.callout-note}
#### `re.form = NA` vs `re.form = NULL`

- `re.form = NA`: ランダム効果を**含まない**予測。母平均的な効果（「典型的な」送り手・受け手）を表す。**論文の主要な結果図にはこちらを使用**。
- `re.form = NULL`: ランダム効果を**含む**予測。特定の個人の条件付き予測。個人レベルの予測が必要な場合に使用。
:::

## 診断（必須セット）

効果の解釈が終わったら、最終モデルの仮定が満たされているかを診断します。診断で問題が見つからなければ、このモデルの結論を信頼して報告に進めます。

### marginal R² と conditional R²（GLMMで必須）

混合効果モデルの $R^2$ は、単一の値ではなく
**marginal / conditional** を分けて報告するのが標準です
[@nakagawa2013; @johnson2014; @nakagawa2017]。

$$
R^2_{\mathrm{marginal}}
=
\frac{\mathrm{Var}(X\beta)}
{\mathrm{Var}(X\beta)+\mathrm{Var}(Zu)+\mathrm{Var}_{\mathrm{dist}}}
$$

$$
R^2_{\mathrm{conditional}}
=
\frac{\mathrm{Var}(X\beta)+\mathrm{Var}(Zu)}
{\mathrm{Var}(X\beta)+\mathrm{Var}(Zu)+\mathrm{Var}_{\mathrm{dist}}}
$$

- `marginal R²`: 固定効果だけで説明される割合
- `conditional R²`: 固定 + ランダム効果で説明される割合
- 差分 `conditional - marginal`: ランダム効果が担う説明分の目安

```{r}
#| eval: true
#| label: r2-glmm

# Nakagawa の R² を計算（GLMM 専用の分散分解）
r2_glmm <- performance::r2_nakagawa(m_glmm_ri)
r2_df   <- as.data.frame(r2_glmm)

# 見やすい表にまとめる
r2_tbl <- tibble(
  metric = c(
    "Marginal R²（固定効果のみ）",
    "Conditional R²（固定 + ランダム効果）",
    "ランダム効果の寄与（差分）"
  ),
  value = c(
    r2_df$R2_marginal[1],
    r2_df$R2_conditional[1],
    r2_df$R2_conditional[1] - r2_df$R2_marginal[1]  # この差がランダム効果の説明分
  )
)

r2_tbl |>
  gt() |>
  fmt_number(columns = value, decimals = 3) |>
  tab_header(title = "R² 分解（Nakagawa's R²）")
```

`performance::r2_nakagawa()` は、GLMM の分散分解に基づく
R²（Nakagawa系）を実装した関数です[@performance_r2; @performancepkg]。

::: {.callout-note}
#### 読み方（実務）

- marginal R²が小さく、conditional R²が大きい:
  個体差・項目差などのランダム効果が主要因になっている可能性。
- 両者が近い:
  固定効果だけで大部分が説明され、ランダム効果の追加利得は小さい。
- R²の大小だけで採否を決めず、`compare_performance()` と残差診断を併用する。
:::

### ICC と marginal / conditional R² はどう違うか

ここは混同しやすいところですが、**ICC と Nakagawa の R² は似た分散分解の言葉を使っていても、答えている問いが違います**[@nakagawa2017; @performance_icc; @performance_r2]。

```{r}
#| eval: true
#| label: icc-r2-bridge
#| echo: false

icc_r2_bridge_tbl <- tibble(
  指標 = c(
    "Null-model total ICC",
    "Marginal R²",
    "Conditional R²",
    "Conditional - Marginal"
  ),
  値 = c(
    icc_total,
    r2_df$R2_marginal[1],
    r2_df$R2_conditional[1],
    r2_df$R2_conditional[1] - r2_df$R2_marginal[1]
  ),
  問い = c(
    "予測子を入れる前に、どれだけクラスター依存があるか",
    "固定効果だけでどれだけ説明できるか",
    "固定 + ランダム効果でどれだけ説明できるか",
    "予測子を入れた後、ランダム効果がどれだけ上乗せで説明するか"
  )
)

knitr::kable(
  icc_r2_bridge_tbl,
  digits = 3,
  align = c("l", "r", "l"),
  caption = "ICC と Nakagawa の R² は、同じ分散分解ファミリーだが役割が異なる"
)
```

安全な読み分けは次の通りです。

- **Null-model ICC**: 予測子をまだ入れていない段階で、「もともとどれだけ clustered か」を見る。
- **Marginal R²**: fixed effects がどれだけ signal を拾えたかを見る。
- **Conditional R²**: fixed + random effects を合わせた説明力を見る。
- **Conditional - Marginal**: 予測子を入れた後も、どれだけ個人差・項目差が残っているかを見る。

したがって、`conditional - marginal` は**ランダム効果の寄与の目安**ですが、**null モデルの ICC と数値的に一致するとは限りません**。たとえば `same_class` のような固定効果が sender / receiver の系統差を部分的に吸収すれば、**null モデルでは大きかった clustered share が、最終モデルでは小さく見える**ことがあります。逆に固定効果で拾えない個人差が多ければ、差分は大きく残ります。

### 後続章への橋渡し: G 係数・Φ係数・Rasch 信頼性

ICC をこの章だけの指標として終わらせず、**測定論の言葉に接続しておくこと**が重要です。第10章の一般化可能性理論と第11章の Rasch モデルで出てくる信頼性指標は、どちらも「信号と誤差の比」を別の目的で表現したものです。

#### G 係数は「設計つき ICC」と考えると分かりやすい

一般化可能性理論では、1 回の観測の類似度だけでなく、「**項目を何問、評価者を何人、課題を何本平均したら、人物の順位づけはどれくらい安定するか**」を問います[@Cronbach1972; @Brennan2001; @ShavelsonWebb1991; @Jiang2018]。1 ファセットの単純な \(p \times i\) 設計では、相対的決定の G 係数は

$$
G
=
\frac{\sigma^2_p}
{\sigma^2_p + \sigma^2_{pi,e}/n_i}
$$

で与えられます。ここで大事なのは、**誤差分散が \(n_i\) で割られている**ことです。つまり G 係数は、「1 項目だけ見たときの人物差」ではなく、**複数項目を平均した後の人物差の安定性**を表します。

| 指標 | 単位 | 主な問い |
|---|---|---|
| ICC | 1 観測レベル | 同じクラスターを共有する 2 観測はどれくらい似るか |
| G 係数 | 平均得点レベル | その設計で人物の順位づけはどれくらい安定か |
| Φ 係数 | 判定レベル | 合否や到達判定のような絶対判断はどれくらい安定か |

したがって、**ICC は「依存構造の出発点」**、**G 係数は「その依存構造を踏まえて平均得点をどれだけ信頼してよいか」**を表します。第10章ではこれを、`lme4` の分散成分と G / Φ に直接つないで詳しく扱います。

#### 具体例: 項目数を増やすと ICC ではなく G / Φ が上がる

以下では、スピーキング評価のような 1 ファセット設計を単純化して、人物分散 \(\sigma^2_p = 0.40\)、課題分散 \(\sigma^2_i = 0.15\)、残差分散 \(\sigma^2_{pi,e} = 0.60\) という仮想例を考えます。ここで **項目数（または課題数）を増やすと何が起きるか** を見ると、ICC と G / Φ の違いがかなり直感的になります。

```{r}
#| eval: true
#| label: g-phi-vs-icc-plot
#| fig-height: 4.6
#| fig-width: 8.2

var_p_demo  <- 0.40
var_i_demo  <- 0.15
var_e_demo  <- 0.60

g_phi_demo <- tibble(
  n_items = 1:8,
  `ICC (single observation)` = var_p_demo / (var_p_demo + var_i_demo + var_e_demo),
  `G coefficient` = var_p_demo / (var_p_demo + var_e_demo / n_items),
  `Phi coefficient` = var_p_demo / (var_p_demo + var_i_demo / n_items + var_e_demo / n_items)
) |>
  pivot_longer(-n_items, names_to = "metric", values_to = "value")

ggplot(g_phi_demo, aes(x = n_items, y = value, color = metric)) +
  geom_line(linewidth = 1.05) +
  geom_point(size = 2) +
  scale_x_continuous(breaks = 1:8) +
  scale_y_continuous(labels = scales::number_format(accuracy = 0.01), limits = c(0, 1)) +
  scale_color_manual(values = c(
    "ICC (single observation)" = "#9C755F",
    "G coefficient" = "#4E79A7",
    "Phi coefficient" = "#F28E2B"
  )) +
  labs(
    title = "Why G / Phi Improve When More Tasks Are Averaged",
    subtitle = "ICC describes dependence at one observation; G / Phi describe reliability after averaging",
    x = "Number of tasks/items averaged",
    y = "Coefficient value",
    color = NULL
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "top")
```

**この図の読み方**:

- **ICC** は水平に近い線です。1 観測レベルの依存性そのものは、課題数を増やしても自動では変わりません。
- **G 係数** は右上がりです。複数課題を平均すると、相対誤差が \(n_i\) で割られるので、順位づけの安定性が上がります。
- **Φ 係数** も上がりますが、通常は G より低く出ます。これは、絶対判断では課題主効果 \(\sigma^2_i\) も誤差に数えるからです。

要するに、**ICC は 1 回の観測の依存性、G / Φ は設計を変えたあとに得点をどれだけ信頼できるか**を表しています。ここが混ざると、「ICC が高いから測定が信頼できる」「ICC が低いから信頼性も低い」といった誤読が起きやすくなります。

#### Rasch の separation reliability は「人物推定の信号対誤差」

Rasch モデルの `Separation Reliability` も、直感としては ICC に近い指標です。どちらも「ばらつきのうち、どれだけが実質的な個人差で、どれだけが誤差か」を見ています[@mair2007; @bondfox2007; @erm_seprel]。ただし、**どのレベルの量を分解しているか**が違います。

- **2 値 GLMM の ICC**: 反応レベルの潜在 logit 傾向を、person / item / residual に分ける。
- **Rasch の separation reliability**: 推定された人物能力 \(\hat{\theta}\) の広がりが、人物ごとの測定誤差より十分大きいかを見る。

`eRm::SepRel()` の考え方は

$$
\mathrm{SepRel}
=
\frac{\mathrm{SSD}-\mathrm{MSE}}{\mathrm{SSD}}
$$

で、SSD は人物推定値の広がり、MSE は人物推定の平均二乗誤差です[@erm_seprel]。ここから分かるように、Rasch の reliability は**人物パラメータ推定の精度**を見ており、GLMM の ICC のように response-level の分散分解を直接返しているわけではありません。

実務的には次のように理解すると混乱しません。

| 指標 | 高くなる条件 | 低くなる条件 |
|---|---|---|
| GLMM の ICC | person / item のランダム効果分散が大きい | 観測レベル残差が支配的 |
| Rasch の separation reliability | 人物能力の広がりが大きく、項目 targeting が良い | サンプルが均質、極端反応が多い、項目が mistargeted |

つまり、**両者は“信号対誤差”という点で親戚だが、数値的に同じものではありません**。GLMM の ICC は「反応データの clustered dependence」、Rasch の reliability は「推定した能力値をどれだけ安心して順位づけに使えるか」を問います。第11章では、この違いを Wright Map と `SepRel()` の出力を使ってさらに丁寧に扱います。GLMM / Rasch の数理的な橋渡し自体は、explanatory IRT の整理で詳しく解説します[@deboeck2004; @wilson2008; @dunn2024]。

#### 具体例: 同じ項目セットでもサンプルが均質だと Rasch reliability は下がる

Rasch の separation reliability が「テスト固有の固定値」ではないことも、図にすると分かりやすくなります。以下では、**同じ項目セット・同じ平均測定誤差**でも、受験者集団のばらつきが違う 2 つのコホートを比べます。

```{r}
#| eval: true
#| label: rasch-seprel-cohort-demo
#| fig-height: 4.2
#| fig-width: 7.4

sep_rel_demo <- tibble(
  cohort = c("Homogeneous placement class", "Mixed-proficiency cohort"),
  theta_variance = c(0.35, 0.95),
  mse = c(0.18, 0.18)
) |>
  mutate(sep_rel = (theta_variance - mse) / theta_variance)

ggplot(sep_rel_demo, aes(x = cohort, y = sep_rel, fill = cohort)) +
  geom_col(width = 0.62, show.legend = FALSE) +
  geom_text(aes(label = sprintf("%.2f", sep_rel)), vjust = -0.4, size = 4) +
  scale_y_continuous(limits = c(0, 1), labels = scales::number_format(accuracy = 0.01)) +
  scale_fill_manual(values = c("#59A14F", "#4E79A7")) +
  labs(
    title = "Rasch Separation Reliability Depends on Sample Spread",
    subtitle = "Same test, same average measurement error, different person variance",
    x = NULL,
    y = "Separation reliability"
  ) +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**:

- 左の均質なクラスでは、受験者どうしの能力差が小さいため、同じ測定誤差でも reliability は下がります。
- 右の mixed-proficiency cohort では、人物差が大きいので、同じ誤差でも reliability は高くなります。
- したがって Rasch の reliability は、「このテストが絶対的に良いか悪いか」だけでなく、**このサンプルに対して人をどれだけ区別できたか**にも依存します。

ここは GLMM の ICC と対照的です。ICC は response-level の clustered variance を見ていますが、Rasch reliability は**推定された人物能力の分散と、その推定誤差の比**を見ています。同じ “信号対誤差” でも、何を signal とし、何を error とするかが異なります。

::: {.panel-tabset}

#### DHARMa 残差診断

DHARMa はシミュレーションベースの残差を生成し、モデルの仮定が満たされているかを視覚的・統計的に検証します。`n = 250` はシミュレーション回数です（推奨は 250–1000）。

```{r}
#| eval: true
#| label: diag-dharma

# シミュレーション残差を生成（n = 250 回のシミュレーション）
res_glmm <- DHARMa::simulateResiduals(m_glmm_ri, n = 250)

plot(res_glmm)                      # QQ プロット + 残差 vs 予測値
DHARMa::testDispersion(res_glmm)    # 過分散テスト（p > .05 なら OK）
DHARMa::testUniformity(res_glmm)    # 残差分布テスト（p > .05 なら OK）
```

**読み方**：

- **QQ プロット（左）**: 点が対角線に沿っていれば残差分布は適切。大きく外れていればモデルのミスフィット。
- **残差 vs 予測値（右）**: パターン（U 字、扇形など）がなければ OK。系統的パターンがあればモデル構造の問題。
- `testDispersion`: **p > .05 なら過分散の問題なし**。有意なら過分散（観測データの分散がモデルの想定より大きい）。
- `testUniformity`: **p > .05 なら残差分布に大きな問題なし**。有意なら分布の仮定に懸念。

::: {.callout-important}
#### DHARMa 診断の合格基準

以下の 3 条件がすべて満たされていれば、モデルの当てはまりは実務上問題ない：

1. `testDispersion` が非有意（p > .05）
2. `testUniformity` が非有意（p > .05）
3. QQ プロットと残差プロットに系統的パターンがない
:::

#### 過分散チェック

```{r}
#| eval: true
#| label: diag-overdispersion

# 過分散チェック（dispersion ratio ≈ 1.0 なら問題なし）
performance::check_overdispersion(m_glmm_ri)
```

**読み方**：

- `Dispersion ratio` ≈ 1.0 なら過分散なし。1.0 から大きく離れるほど過分散の懸念。
- p > .05 なら過分散は統計的に有意ではない。

::: {.callout-important}
#### 2値 GLMM での過分散: この検定は参考情報

**個々の観測が 0/1 の2値データでは、過分散は理論的に定義されません**。各観測の分散は $p(1-p)$ で決まり、追加の分散パラメータは存在しないためです。上の `check_overdispersion()` の結果は**参考情報としてのみ**掲載しています。

過分散が実質的に問題になるのは以下のケースです：

- **集計データ**（n回中k回成功のような binomial(n, p) データ）
- **観測レベルのランダム効果がないモデル**に未モデル化の異質性がある場合

本章のモデルは個々の 0/1 観測にランダム効果を含んでいるため、過分散の懸念は低いです。**モデルの妥当性は DHARMa の残差診断と AUC で判断**してください。
:::

#### 共線性チェック

```{r}
#| eval: true
#| label: diag-collinearity

# VIF（分散拡大因子）: < 3 なら安全、> 5 なら要注意
performance::check_collinearity(m_glm)      # GLM 版
performance::check_collinearity(m_glmm_ri)  # GLMM 版（両方で確認）
```

**VIF（分散拡大因子）の判断基準**：

| VIF | 判断 |
|-----|------|
| < 3 | 問題なし |
| 3–5 | 注意。解釈に影響がないか確認 |
| > 5 | 共線性の懸念。予測子の除外や合成を検討 |
| > 10 | 深刻な共線性。推定が不安定な可能性が高い |

GLM と GLMM の両方で VIF を確認しているのは、ランダム効果の追加で共線性パターンが変化しないことを確認するためです。

**出力の確認**: すべての VIF が 3 未満であれば共線性の問題はなく、各予測子の推定が安定していると判断できます。VIF が 5 を超える予測子がある場合は、その予測子の除外やスコアの合成を検討してください。

#### logit スケールでの線形性チェック

ロジスティック回帰は「予測子と log-odds の関係が線形」を仮定しています。連続予測子について、この仮定が妥当かを視覚的に確認します。

```{r}
#| eval: true
#| label: diag-linearity-logit
#| fig-height: 4
#| fig-width: 9

# ── 連続予測子ごとに、経験的 log-odds を計算して線形性を確認 ──
cont_vars <- c("score_sender_sc", "score_receiver_sc", "prof_gap")

linearity_plots <- lapply(cont_vars, function(v) {
  dat_bin$xbin <- cut(dat_bin[[v]], breaks = 8)
  agg <- aggregate(tie ~ xbin, data = dat_bin, FUN = function(y) {
    p <- mean(y)
    p <- pmax(pmin(p, 0.999), 0.001)  # 0/1 を避ける
    log(p / (1 - p))
  })
  agg$x_mid <- seq_along(agg$xbin)

  ggplot(agg, aes(x = x_mid, y = tie)) +
    geom_point(size = 3) +
    geom_smooth(method = "lm", se = FALSE, linetype = "dashed", color = "red") +
    labs(title = v, x = paste(v, "(binned)"), y = "Empirical log-odds") +
    theme_bw(base_size = 10, base_family = base_font)
})

patchwork::wrap_plots(linearity_plots, nrow = 1)
```

**この図の読み方**: 各パネルで、点（経験的 log-odds）が赤の直線（線形トレンド）に沿っていれば、logit スケールでの線形性仮定は妥当です。U 字型や階段状のパターンが見られる場合は、二次項の追加やスプラインの使用を検討してください（→ GAMM への拡張、第 5 章参照）。

#### check_model（総合診断）

```{r}
#| eval: true
#| label: diag-checkmodel
#| fig-width: 10
#| fig-height: 4.8

# 総合的な視覚診断（複数パネルを一括表示）
plot_check_model_separate(m_glm, model_name = "m_glm")        # GLM 版
plot_check_model_separate(m_glmm_ri, model_name = "m_glmm_ri")  # GLMM 版
```

`check_model()` はモデル仮定の可視化を一括で確認できるため、
最終モデルの報告前チェックとして有用です[@performance_checkmodel]。

**パネルの読み方**：

- **Posterior predictive check**: 実データ（黒線）とモデルからのシミュレーション（青線）が近いか
- **Linearity**: 残差の平均が 0 付近を推移しているか
- **Homogeneity of variance**: 残差の散布が予測値に依存していないか
- **Influential observations**: Cook の距離が極端に大きい観測がないか
- **VIF**: 共線性の視覚的確認

:::

::: {.callout-tip}
#### 診断結果の 1 文報告テンプレート

すべての診断が合格した場合、論文には以下の 1 文を記載すれば十分です：

> Diagnostic checks indicated no major violations: DHARMa simulation-based tests for dispersion and uniformity were non-significant, VIF values were below 3, and visual inspection of residual plots revealed no systematic patterns.

**診断で問題が見つかった場合**の報告例：

> A DHARMa dispersion test indicated potential overdispersion (*p* = .03); however, visual inspection of residual plots showed no systematic patterns. Given the robustness of binary GLMMs to mild distributional violations, the model was retained. As a sensitivity check, an observation-level random effect was added, with substantive conclusions unchanged.
:::

### モデルの判別能: AUC / C 統計量（2 値 GLMM 特有）

2 値アウトカムのモデルでは、残差診断に加えて「**モデルがどの程度正確にイベント（tie = 1）と非イベント（tie = 0）を区別できるか**」を評価します。これを**判別能（discrimination）**と呼び、**AUC（Area Under the ROC Curve）** で定量化します。AUC は **C 統計量**（concordance statistic）とも呼ばれ、2 値ロジスティック回帰に特有の評価指標です。

```{r}
#| eval: true
#| label: diag-auc-roc
#| fig-height: 5
#| fig-width: 9

# pROC は setup で読み込み済み

# ── 条件付き予測（ランダム効果込み）と周辺予測（固定効果のみ）──
pred_cond <- predict(m_glmm_ri, type = "response")            # ランダム効果込み
pred_marg <- predict(m_glmm_ri, type = "response", re.form = NA)  # 固定効果のみ

# ROC 曲線の計算
roc_cond <- roc(dat_bin$tie, pred_cond, quiet = TRUE)
roc_marg <- roc(dat_bin$tie, pred_marg, quiet = TRUE)

# ── ggplot2 で 2 つの ROC 曲線を重ね描き ──
roc_df <- rbind(
  data.frame(sensitivity = roc_cond$sensitivities,
             specificity = 1 - roc_cond$specificities,
             Model = sprintf("Conditional (AUC = %.3f)", auc(roc_cond))),
  data.frame(sensitivity = roc_marg$sensitivities,
             specificity = 1 - roc_marg$specificities,
             Model = sprintf("Marginal (AUC = %.3f)", auc(roc_marg)))
)

ggplot(roc_df, aes(x = specificity, y = sensitivity, color = Model)) +
  geom_line(linewidth = 1.1) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed", color = "grey60") +
  scale_color_manual(values = c("#0072B2", "#D55E00")) +
  labs(title = "ROC Curves: Conditional vs. Marginal Predictions",
       subtitle = "Diagonal = chance (AUC = 0.5). Further from diagonal = better discrimination.",
       x = "1 - Specificity (False Positive Rate)",
       y = "Sensitivity (True Positive Rate)") +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "top")
```

```{r}
#| eval: true
#| label: diag-auc-table
#| echo: false

tibble::tibble(
  予測タイプ = c("Conditional（ランダム効果込み）", "Marginal（固定効果のみ）"),
  AUC = c(as.numeric(auc(roc_cond)), as.numeric(auc(roc_marg))),
  `95% CI` = c(
    sprintf("[%.3f, %.3f]", ci.auc(roc_cond)[1], ci.auc(roc_cond)[3]),
    sprintf("[%.3f, %.3f]", ci.auc(roc_marg)[1], ci.auc(roc_marg)[3])
  )
) |>
  gt::gt() |>
  gt::tab_header(title = "AUC / C-statistic: モデルの判別能")
```

**AUC の判断基準**[@hosmer2013]:

| AUC | 判別能 |
|:---:|:---|
| 0.5 | 偶然と同じ（判別不能） |
| 0.5–0.7 | Poor |
| 0.7–0.8 | Acceptable |
| 0.8–0.9 | Excellent |
| > 0.9 | Outstanding |

**2 つの AUC の違い**:

- **Conditional**: ランダム効果（個人差）を含めた予測。「この sender/receiver を知っている場合」の判別能。通常は高い。
- **Marginal**: 固定効果のみの予測。「新しい未知の sender/receiver に対する」判別能。研究の一般化可能性を示す。

::: {.callout-note}
#### AUC の報告テンプレート

> The model demonstrated [acceptable/excellent] discrimination, with a C-statistic of .XX, 95% CI [.XX, .XX], based on [conditional / marginal] predicted probabilities. [If both are reported: The conditional C-statistic (.XX) exceeded the marginal C-statistic (.XX), indicating that individual differences in sender/receiver tendencies contribute substantially to prediction accuracy.]
:::

## 最終モデルの確定と結果の要約

### 診断結果に基づく最終モデルの確定

```{r}
#| eval: true
#| label: final-model-declaration
#| echo: false

# 診断結果を集約して最終モデルを確定
dharma_disp <- DHARMa::testDispersion(res_glmm, plot = FALSE)
dharma_unif <- DHARMa::testUniformity(res_glmm, plot = FALSE)
vif_max     <- max(performance::check_collinearity(m_glmm_ri)$VIF)
auc_cond    <- as.numeric(pROC::auc(roc_cond))

diag_final_tbl <- tibble::tribble(
  ~診断, ~結果, ~判断,
  "DHARMa dispersion test", sprintf("p = %.3f", dharma_disp$p.value),
    ifelse(dharma_disp$p.value > 0.05, "問題なし", "要検討"),
  "DHARMa uniformity test", sprintf("p = %.3f", dharma_unif$p.value),
    ifelse(dharma_unif$p.value > 0.05, "問題なし", "要検討"),
  "VIF (最大値)", sprintf("%.2f", vif_max),
    ifelse(vif_max < 3, "共線性なし", "要検討"),
  "AUC (conditional)", sprintf("%.3f", auc_cond),
    ifelse(auc_cond >= 0.7, "acceptable 以上", "poor")
)

knitr::kable(
  diag_final_tbl,
  align = c("l", "l", "l"),
  caption = "診断結果の総括: 全診断が概ね良好だったため、m_glmm_ri を最終モデルとして採用"
)
```

全ての診断で大きな問題が見られなかったため、**`m_glmm_ri`（交差分類ランダム切片モデル、logit リンク、5 固定効果）を最終モデルとして確定**します。

### 結果を読む順番

この章では結果をかなり多面的に確認しましたが、**最終的な読解順は 3 段階**で十分です。

1. `sjPlot::tab_model()` で、どの固定効果が残り、ランダム効果がどれくらい重要かを俯瞰する。
2. `sjPlot::plot_model(type = "est")` で、OR と 95% CI を見ながら「どの効果が最も大きいか」を確認する。
3. `sjPlot::plot_model(type = "pred")` と `emmeans()` で、最終的に**確率で何ポイント違うのか**を読む。

### `sjPlot` による最終結果の俯瞰

::: {.panel-tabset}

#### Model Table

```{r}
#| eval: true
#| label: final-sjplot-tab-model
#| echo: false

sjPlot::tab_model(
  m_glmm_ri,
  show.se = TRUE,
  show.stat = TRUE,
  show.aic = TRUE,
  show.icc = TRUE,
  show.re.var = TRUE,
  show.ngroups = TRUE,
  dv.labels = "Final GLMM (RI)",
  title = "Final Model Summary"
)
```

**読み方**:

- 最初に `same_class` と `prof_gap` の行を見て、95% CI が 1 をまたぐかどうかを確認する。
- 次に表の下部で `ICC`、`Random Effects`、`N groups` を見て、個人差を入れたことが実質的に意味を持つかを判断する。
- `AIC` はこの表だけでは比較できないので、ここでは**最終モデルの記述**として読む。モデル選択そのものは前節の比較表で確認済み。

#### Effect Plot

```{r}
#| eval: true
#| label: final-sjplot-est
#| fig-height: 4.2
#| fig-width: 7.2
#| echo: false

sjPlot::plot_model(
  m_glmm_ri,
  type = "est",
  show.values = TRUE,
  show.p = TRUE,
  sort.est = TRUE,
  title = "Final Fixed Effects (OR scale)",
  vline.color = "grey70"
) +
  theme_bw(base_size = 12, base_family = base_font)
```

**読み方**:

- 点が OR、水平線が 95% CI。灰色の縦線 `OR = 1` をまたぐかどうかが基本判断。
- `sort.est = TRUE` なので、上にあるほど影響の大きい効果。`same_class` が最上位なら、「同クラスであること」が最も強い予測子だと読める。
- ただし OR は**確率差そのものではない**。大きさの比較には便利だが、実務的な説明は次の予測確率図で行う。

#### Probability Plot

```{r}
#| eval: true
#| label: final-sjplot-pred
#| fig-height: 4.8
#| fig-width: 8.5
#| echo: false

sjPlot::plot_model(
  m_glmm_ri,
  type = "pred",
  terms = c("score_sender_sc", "same_class [0,1]"),
  pred.type = "fe",
  show.data = FALSE,
  title = "Predicted Probability by Sender Score and Class Relation"
) +
  labs(
    x = "Sender score (z)",
    y = "Predicted probability of advice tie",
    color = "Same class",
    fill = "Same class"
  ) +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "bottom")
```

**読み方**:

- 2 本の線の縦方向の差が、`same_class` の効果を**確率スケール**で表す。
- 線の傾きが右上がりなら、送り手スコアが高いほど助言関係の確率が高い。
- 線がほぼ平行なら、`same_class` と `score_sender_sc` の間に強い交互作用はない。つまり「同クラス効果は送り手スコア全体にわたって概ね一定」と読める。

:::

### 結果の要約（Results at a Glance）

```{r}
#| eval: true
#| label: results-summary
#| echo: false

fe <- summary(m_glmm_ri)$coefficients
r2 <- performance::r2_nakagawa(m_glmm_ri)
vc <- as.data.frame(VarCorr(m_glmm_ri))

results_summary_tbl <- tibble::tribble(
  ~指標, ~値, ~解釈,
  "同クラス (same_class)", sprintf("OR = %.2f, p %s .001", exp(fe["same_class", "Estimate"]),
    ifelse(fe["same_class", "Pr(>|z|)"] < 0.001, "<", "=")),
    sprintf("同クラスのペアは助言関係を持つオッズが約%.1f倍高い", exp(fe["same_class", "Estimate"])),
  "熟達度差 (prof_gap)", sprintf("OR = %.2f, p = %.3f", exp(fe["prof_gap", "Estimate"]),
    fe["prof_gap", "Pr(>|z|)"]),
    sprintf("熟達度差が1段階広がるとオッズが約%.1f倍に変化", exp(fe["prof_gap", "Estimate"])),
  "送り手スコア (score_sender_sc)", sprintf("OR = %.2f, p = %.3f", exp(fe["score_sender_sc", "Estimate"]),
    fe["score_sender_sc", "Pr(>|z|)"]),
    "非有意: 送り手のスコアは助言関係を予測しない",
  "受け手スコア (score_receiver_sc)", sprintf("OR = %.2f, p = %.3f", exp(fe["score_receiver_sc", "Estimate"]),
    fe["score_receiver_sc", "Pr(>|z|)"]),
    "非有意: 受け手のスコアは助言関係を予測しない",
  "スコア差 (score_gap)", sprintf("OR = %.2f, p = %.3f", exp(fe["score_gap", "Estimate"]),
    fe["score_gap", "Pr(>|z|)"]),
    "非有意: スコア差は助言関係を予測しない",
  "R² (marginal / conditional)", sprintf("%.3f / %.3f", r2$R2_marginal, r2$R2_conditional),
    sprintf("固定効果で%.0f%%、固定+ランダム効果で%.0f%%の分散を説明",
            r2$R2_marginal * 100, r2$R2_conditional * 100),
  "AUC (conditional)", sprintf("%.3f", auc_cond),
    ifelse(auc_cond >= 0.8, "Excellent な判別能",
           ifelse(auc_cond >= 0.7, "Acceptable な判別能", "Poor な判別能")),
  "ランダム効果 SD (sender / receiver)", sprintf("%.3f / %.3f", vc$sdcor[1], vc$sdcor[2]),
    ifelse(vc$sdcor[2] > vc$sdcor[1],
           "receiver の個人差が sender より大きい",
           "sender の個人差が receiver より大きい")
)

knitr::kable(
  results_summary_tbl,
  align = c("l", "l", "l"),
  caption = "分析結果の要約: 最終モデル m_glmm_ri（交差分類ランダム切片、logit リンク）"
)
```

::: {.callout-important}
### この分析から何がわかったか

本データの 2 値ロジスティック GLMM 分析から、以下が明らかになりました：

1. **同クラスの学習者同士は助言関係を持ちやすい**（`r sprintf("OR = %.2f, p < .001", exp(lme4::fixef(m_glmm_ri)["same_class"]))`)。これは最も強い予測子であり、クラス編成がネットワーク構造に直接影響していることを示唆する。
2. **熟達度差が大きいペアほど助言関係が見られる**（`r sprintf("OR = %.2f, p = %.03f", exp(lme4::fixef(m_glmm_ri)["prof_gap"]), summary(m_glmm_ri)$coefficients["prof_gap", "Pr(>|z|)"])`）。異なる熟達度の学習者間で知識共有が生じている可能性がある。
3. **個々のスコアやスコア差は助言関係を予測しない**。テスト得点そのものよりも、クラス所属や熟達度ラベルのほうが社会的ネットワークの構造に影響している。
4. **receiver のランダム効果 SD（`r sprintf("%.3f", as.data.frame(lme4::VarCorr(m_glmm_ri))$sdcor[2])`）が sender（`r sprintf("%.3f", as.data.frame(lme4::VarCorr(m_glmm_ri))$sdcor[1])`）より大きい**。「助言を受けやすい人」の個人差のほうが、「助言をしやすい人」の個人差より大きい。
:::

## 検出力分析（研究計画段階で必須）

モデルの分析と診断が完了しました。ここからは、今後の研究計画に役立つ検出力分析の方法を紹介します。この節は分析後の振り返りとして、あるいは次回の研究設計のために読んでください。

GLMM の検出力は解析的に求まらないため、**シミュレーションベース**で推定します[@debruine2021; @green2016]。以下では @debruine2021 のアプローチに従い、データ生成過程を完全に明示した手動シミュレーションを実装します。

::: {.callout-important}
### なぜ参加者数と項目数の両方を変える必要があるのか

GLMM では「サンプルサイズ」が**2 つの次元**を持ちます。「30 人 × 10 項目」と「30 人 × 20 項目」では検出力が異なりますし、「30 人 × 20 項目」と「60 人 × 20 項目」でも異なります。研究計画では**両次元を同時に探索**して、予算・時間の制約下での最適な組み合わせを見つけます[@westfall2014; @brysbaert2018]。
:::

### シミュレーションの原理

検出力分析の核心は 4 ステップです[@debruine2021]：

1. **パラメータを設定**: 固定効果 β、ランダム効果の SD、参加者数、項目数
2. **データを生成**: ランダム効果を `rnorm()` で生成 → 線形予測子 η を計算 → `plogis(η)` で確率に変換 → `rbinom()` で 2 値データを生成
3. **モデルを推定**: `glmer()` を当てはめ、関心のある効果の p 値を取得
4. **繰り返す**: 1-3 を nsim 回繰り返し、p < α となった割合 = **検出力**

### Step 1: データ生成関数の定義

英語教育で典型的な「**参加者 × 項目**」の交差分類デザインを想定します。熟達度（A2/B1/B2）が参加者間要因として 3 水準あるケースです。

```{r}
#| eval: true
#| label: power-sim-functions

# ── データ生成関数 ──
# 全パラメータが明示的に見える（ブラックボックスなし）
simulate_glmm_data <- function(
  n_subj_per_group = 20,   # 各熟達度群の参加者数
  n_item           = 20,   # テスト項目数
  beta_0           = 0.0,  # A2 群のベースログオッズ（50% 正答率）
  beta_B1          = 0.5,  # B1 vs A2（OR = 1.65）
  beta_B2          = 1.0,  # B2 vs A2（OR = 2.72）
  sd_subj          = 1.0,  # 参加者ランダム切片の SD
  sd_item          = 0.5   # 項目ランダム切片の SD
) {
  n_total <- n_subj_per_group * 3

  # 参加者ランダム効果: 能力の高い学習者は全体的に正答しやすい
  subj <- data.frame(
    subj_id     = paste0("S", seq_len(n_total)),
    proficiency = factor(rep(c("A2", "B1", "B2"), each = n_subj_per_group)),
    u_subj      = rnorm(n_total, 0, sd_subj)
  )

  # 項目ランダム効果: 易しい項目は全員が正答しやすい
  items <- data.frame(
    item_id = paste0("I", seq_len(n_item)),
    u_item  = rnorm(n_item, 0, sd_item)
  )

  # 全参加者 × 全項目の交差（完全交差デザイン）
  dat <- tidyr::crossing(subj, items) |>
    dplyr::mutate(
      # Treatment coding: A2 が参照水準
      X_B1 = as.numeric(proficiency == "B1"),
      X_B2 = as.numeric(proficiency == "B2"),
      # 線形予測子 = 固定効果 + 参加者ランダム効果 + 項目ランダム効果
      eta  = beta_0 + beta_B1 * X_B1 + beta_B2 * X_B2 + u_subj + u_item,
      # 確率に変換（逆ロジット）
      prob = plogis(eta),
      # 2値アウトカムを生成
      y    = rbinom(dplyr::n(), 1, prob)
    )
  dat
}

# ── 1回のシミュレーション: データ生成 → 推定 → p値抽出 ──
run_single_sim <- function(...) {
  dat <- simulate_glmm_data(...)

  # フルモデル（proficiency あり）
  mod_full <- glmer(
    y ~ proficiency + (1 | subj_id) + (1 | item_id),
    family = binomial, data = dat,
    control = glmerControl(optimizer = "bobyqa")
  )

  # ヌルモデル（proficiency なし）→ オムニバス LRT 用
  mod_null <- glmer(
    y ~ 1 + (1 | subj_id) + (1 | item_id),
    family = binomial, data = dat,
    control = glmerControl(optimizer = "bobyqa")
  )

  # 個別の固定効果 p 値
  fe <- broom.mixed::tidy(mod_full, effects = "fixed")
  p_B1 <- fe$p.value[fe$term == "proficiencyB1"]
  p_B2 <- fe$p.value[fe$term == "proficiencyB2"]

  # オムニバス LRT（proficiency 全体の効果、2 df）
  lr <- anova(mod_null, mod_full)
  p_omnibus <- lr[["Pr(>Chisq)"]][2]

  data.frame(p_B1 = p_B1, p_B2 = p_B2, p_omnibus = p_omnibus)
}
```

ここまでで、`simulate_glmm_data()` は指定したパラメータから2値データを生成し、`run_single_sim()` はデータ生成から `glmer()` による推定、p値抽出までを1回で実行できる状態になっています。

### パラメータ設定が意味する確率を確認する

関数を定義したら、まず「設定したパラメータがどのようなデータを生成するか」を 1 回だけ生成して確認します。これは検出力分析の**最も重要なステップ**です。パラメータが非現実的なら、検出力の数値も無意味になります。

```{r}
#| eval: true
#| label: power-visualize-one-dataset
#| fig-height: 5
#| fig-width: 10

set.seed(42)

# ── 1 回だけデータを生成 ──
one_dataset <- simulate_glmm_data(
  n_subj_per_group = 20, n_item = 20,
  beta_0 = 0.0, beta_B1 = 0.5, beta_B2 = 1.0,
  sd_subj = 1.0, sd_item = 0.5
)

# ── パラメータが意味する確率を可視化 ──
# patchwork は setup で読み込み済み

# (a) 群別の正答率分布
p_rate <- one_dataset |>
  dplyr::group_by(subj_id, proficiency) |>
  dplyr::summarise(accuracy = mean(y), .groups = "drop") |>
  ggplot(aes(x = proficiency, y = accuracy, fill = proficiency)) +
  geom_boxplot(alpha = 0.7, outlier.shape = 21) +
  geom_jitter(width = 0.15, alpha = 0.4, size = 1.5) +
  scale_fill_manual(values = c(A2 = "#E69F00", B1 = "#56B4E9", B2 = "#009E73")) +
  labs(title = "(a) Accuracy by Proficiency Group",
       subtitle = "Each dot = one participant's mean accuracy",
       x = "Proficiency", y = "Accuracy") +
  theme_bw(base_size = 11, base_family = base_font) + theme(legend.position = "none")

# (b) 項目別の正答率分布
p_item <- one_dataset |>
  dplyr::group_by(item_id) |>
  dplyr::summarise(difficulty = 1 - mean(y), .groups = "drop") |>
  ggplot(aes(x = reorder(item_id, difficulty), y = difficulty)) +
  geom_col(fill = "#0072B2", alpha = 0.7) +
  coord_flip() +
  labs(title = "(b) Item Difficulty (1 - accuracy)",
       subtitle = "Higher = more difficult",
       x = NULL, y = "Difficulty") +
  theme_bw(base_size = 11, base_family = base_font)

# (c) ランダム効果の分布
p_re <- data.frame(
  effect = c(one_dataset$u_subj[!duplicated(one_dataset$subj_id)],
             one_dataset$u_item[!duplicated(one_dataset$item_id)]),
  type   = c(rep("Participant (SD=1.0)",
                  length(unique(one_dataset$subj_id))),
             rep("Item (SD=0.5)",
                  length(unique(one_dataset$item_id))))
) |>
  ggplot(aes(x = effect, fill = type)) +
  geom_histogram(bins = 15, alpha = 0.7, position = "identity") +
  scale_fill_manual(values = c("Participant (SD=1.0)" = "#D55E00",
                                "Item (SD=0.5)" = "#0072B2")) +
  labs(title = "(c) Random Effects Distribution",
       x = "Random Intercept (logit)", y = "Count", fill = NULL) +
  theme_bw(base_size = 11, base_family = base_font) + theme(legend.position = "top")

p_rate + p_item + p_re + plot_layout(widths = c(1, 1, 1))
```

**この図の読み方**:

- **(a)**: A2/B1/B2 の正答率の箱ひげ図。`β_B1 = 0.5, β_B2 = 1.0` が「現実的な群間差」かを確認。参加者間のばらつき（SD = 1.0）が大きいため、群内の分布が広い。
- **(b)**: 項目の難易度分布。SD = 0.5 のランダム効果により、易しい項目と難しい項目が自然に分布。
- **(c)**: ランダム効果の実際の分布。参加者（赤）は項目（青）より散布が大きい → 参加者数を増やすほうが検出力に寄与しやすい。

::: {.callout-tip}
#### パラメータの設定方法

- **パイロットデータがある場合**: パイロットで `glmer()` を推定し、`fixef()` と `VarCorr()` の値をそのまま使う。
- **先行研究に基づく場合**: 先行研究の報告値（OR, ランダム効果の SD）を使用。OR → β の変換は `log(OR)`。
- **目安がない場合**: Cohen の OR ベンチマーク（小 = 1.68, 中 = 3.47）と、ランダム効果 SD = 0.5〜1.5 の範囲で感度分析。
:::

### Step 2: 1 つの設定での検出力

まず「20 人/群 × 20 項目」での検出力を確認します。

```{r}
#| eval: true
#| label: power-single-setting
#| cache: true

set.seed(2024)
nsim <- 50  # デモ用。本番は 500-1000 に増やす

# nsim 回のシミュレーション
sim_results <- do.call(rbind, lapply(seq_len(nsim), function(i) {
  run_single_sim(n_subj_per_group = 20, n_item = 20,
                 beta_0 = 0.0, beta_B1 = 0.5, beta_B2 = 1.0,
                 sd_subj = 1.0, sd_item = 0.5)
}))

# 検出力 = p < .05 の割合
power_summary <- data.frame(
  Effect   = c("B1 vs A2 (OR=1.65)", "B2 vs A2 (OR=2.72)", "Omnibus (2 df)"),
  Power    = c(mean(sim_results$p_B1 < 0.05),
               mean(sim_results$p_B2 < 0.05),
               mean(sim_results$p_omnibus < 0.05)),
  CI_lower = NA, CI_upper = NA
)

# 二項分布の 95% CI
for (j in seq_len(nrow(power_summary))) {
  bt <- binom.test(round(power_summary$Power[j] * nsim), nsim)
  power_summary$CI_lower[j] <- bt$conf.int[1]
  power_summary$CI_upper[j] <- bt$conf.int[2]
}

power_summary |>
  dplyr::mutate(
    Power_pct = sprintf("%.0f%% [%.0f, %.0f]",
                        Power * 100, CI_lower * 100, CI_upper * 100)
  ) |>
  dplyr::select(Effect, Power_pct) |>
  gt::gt() |>
  gt::tab_header(
    title = "検出力: 20人/群 × 20項目",
    subtitle = sprintf("nsim = %d. β = (0, 0.5, 1.0), SD_subj = 1.0, SD_item = 0.5", nsim)
  )
```

**結果の読み方**: 小さい効果（B1 vs A2, OR = `r sprintf("%.2f", exp(0.5))`)は大きい効果（B2 vs A2, OR = `r sprintf("%.2f", exp(1.0))`)より検出力が低くなります。オムニバス検定は全水準の情報を合算するため、個別対比より検出力が高いことが多いです。

```{r}
#| eval: true
#| label: power-p-distribution
#| fig-height: 4
#| fig-width: 10

# ── p 値の分布を可視化（検出力の直感的理解）──
p_long <- tidyr::pivot_longer(
  sim_results, cols = everything(),
  names_to = "test", values_to = "p_value"
) |>
  dplyr::mutate(test = dplyr::case_when(
    test == "p_B1"      ~ "B1 vs A2 (OR=1.65)",
    test == "p_B2"      ~ "B2 vs A2 (OR=2.72)",
    test == "p_omnibus"  ~ "Omnibus (2 df)"
  ))

ggplot(p_long, aes(x = p_value, fill = test)) +
  geom_histogram(bins = 20, alpha = 0.8, boundary = 0) +
  geom_vline(xintercept = 0.05, linetype = "dashed", color = "red", linewidth = 0.8) +
  facet_wrap(~test, scales = "free_y") +
  scale_fill_manual(values = c("#E69F00", "#56B4E9", "#009E73")) +
  labs(title = "Distribution of p-values across simulations",
       subtitle = "Red line = α = 0.05. Bars LEFT of the line = significant = power.",
       x = "p-value", y = "Count") +
  theme_bw(base_size = 11, base_family = base_font) +
  theme(legend.position = "none")
```

**この図の読み方**: 各パネルの赤線（α = 0.05）の**左側に積み上がっている割合 = 検出力**です。効果が大きい（B2 vs A2）ほど p 値が左に偏り、検出力が高くなります。効果が小さい（B1 vs A2）場合、p 値が 0〜1 に広く分布し、有意にならないケースが多くなります。

### Step 3: 参加者数 × 項目数のグリッド探索

「**30 人/群 × 10 項目と 30 人/群 × 20 項目ではどちらが十分か？40 人にすればどうか？**」をシミュレーションで回答します。

```{r}
#| eval: true
#| label: power-grid-participant-item
#| cache: true
#| fig-height: 5.5
#| fig-width: 9

set.seed(2024)
nsim_grid <- 25  # デモ用。本番は 500+ に増やす

# ── 参加者数（群あたり）× 項目数のグリッド ──
grid <- expand.grid(
  n_subj_pg = c(15, 20, 30, 50),
  n_item    = c(10, 15, 20, 30)
)
grid$power_omnibus <- NA
grid$power_B1 <- NA

for (i in seq_len(nrow(grid))) {
  ns <- grid$n_subj_pg[i]
  ni <- grid$n_item[i]

  sims <- do.call(rbind, lapply(seq_len(nsim_grid), function(j) {
    run_single_sim(n_subj_per_group = ns, n_item = ni,
                   beta_0 = 0.0, beta_B1 = 0.5, beta_B2 = 1.0,
                   sd_subj = 1.0, sd_item = 0.5)
  }))

  grid$power_omnibus[i] <- mean(sims$p_omnibus < 0.05)
  grid$power_B1[i]      <- mean(sims$p_B1 < 0.05)
}

# ── ヒートマップ: オムニバス検出力 ──
grid$power_pct <- round(grid$power_omnibus * 100)

ggplot(grid, aes(x = factor(n_subj_pg), y = factor(n_item),
                  fill = power_omnibus)) +
  geom_tile(color = "white", linewidth = 1.5) +
  geom_text(aes(label = paste0(power_pct, "%")), size = 5, fontface = "bold") +
  scale_fill_gradient2(low = "#D73027", mid = "#FEE08B", high = "#1A9850",
                       midpoint = 0.8, limits = c(0, 1),
                       labels = scales::percent) +
  labs(title = "Power Grid: Participants per Group × Number of Items",
       subtitle = sprintf("Omnibus proficiency test (A2/B1/B2). nsim = %d per cell", nsim_grid),
       x = "Participants per Proficiency Group",
       y = "Number of Items",
       fill = "Power") +
  theme_bw(base_size = 12, base_family = base_font) +
  theme(legend.position = "right")
```

**この図の読み方**:

- 各セルは「群あたり N 人 × K 項目」でのオムニバス検出力。
- **緑（≥ 80%）**: 十分な検出力。**黄（60-80%）**: 要検討。**赤（< 60%）**: 不足。
- **横方向**（参加者を増やす）と**縦方向**（項目を増やす）のどちらが検出力を効率的に上げるかを読み取る。
- ランダム効果の分散が大きいほう（ここでは参加者: SD = 1.0 > 項目: SD = 0.5）のサンプルを増やすのが効率的[@westfall2014]。

```{r}
#| eval: true
#| label: power-grid-B1
#| fig-height: 5.5
#| fig-width: 9

# ── B1 vs A2（小さい効果）の検出力も可視化 ──
grid$power_B1_pct <- round(grid$power_B1 * 100)

p_omnibus <- ggplot(grid, aes(x = factor(n_subj_pg), y = factor(n_item),
                               fill = power_omnibus)) +
  geom_tile(color = "white", linewidth = 1.5) +
  geom_text(aes(label = paste0(power_pct, "%")), size = 4, fontface = "bold") +
  scale_fill_gradient2(low = "#D73027", mid = "#FEE08B", high = "#1A9850",
                       midpoint = 0.8, limits = c(0, 1)) +
  labs(title = "Omnibus (proficiency overall)",
       x = "Participants/group", y = "Items", fill = "Power") +
  theme_bw(base_size = 10, base_family = base_font) + theme(legend.position = "none")

p_b1 <- ggplot(grid, aes(x = factor(n_subj_pg), y = factor(n_item),
                           fill = power_B1)) +
  geom_tile(color = "white", linewidth = 1.5) +
  geom_text(aes(label = paste0(power_B1_pct, "%")), size = 4, fontface = "bold") +
  scale_fill_gradient2(low = "#D73027", mid = "#FEE08B", high = "#1A9850",
                       midpoint = 0.8, limits = c(0, 1)) +
  labs(title = "B1 vs A2 (small effect, OR=1.65)",
       x = "Participants/group", y = "Items", fill = "Power") +
  theme_bw(base_size = 10, base_family = base_font) + theme(legend.position = "right")

p_omnibus + p_b1 +
  plot_annotation(
    title = "Power Comparison: Omnibus vs. Small Pairwise Effect",
    subtitle = "Small effects require substantially more participants and/or items"
  )
```

**2 つのヒートマップの比較**:

- **左（オムニバス）**: 全体的な proficiency の効果を検出する力。B1 と B2 の情報を合算するため、比較的高い検出力。
- **右（B1 vs A2）**: 小さい効果（OR = `r sprintf("%.2f", exp(0.5))`）だけを検出する力。オムニバスより大幅に低い。
- **実務的含意**: 「全体として差がある」ことと「特定の 2 群間で差がある」ことは検出力が異なる。研究の問いが**特定の対比**に焦点を当てている場合は、その対比に基づいて検出力を計画する必要がある。

### 語彙介入研究では `pre-test` 既知率も検出力を左右する

語彙研究では、項目数を 30 語用意しても、そのすべてが「学習対象」として残るわけではありません。`pre-test` で既に知っていた語が多いほど、**unknown-at-pretest 語の数が減り、介入効果を検出する情報量が落ちます**。つまり、participant 数と item 数だけでなく、**pre-known rate** も power の設計要因です。

```{r}
#| eval: true
#| label: power-vocab-preknown-functions

simulate_vocab_learning_data <- function(
  n_subj_per_group = 20,
  n_item = 20,
  known_rate = 0.30,
  beta_treatment_unknown = 0.55,
  beta_preknown = 1.80,
  sd_subj = 0.9,
  sd_item = 0.6
) {
  n_total <- n_subj_per_group * 2

  learners <- tibble::tibble(
    learner_id = factor(sprintf("P%03d", seq_len(n_total))),
    treatment = factor(rep(c("Control", "Gloss"), each = n_subj_per_group),
                       levels = c("Control", "Gloss")),
    u_subj = rnorm(n_total, 0, sd_subj)
  )

  items <- tibble::tibble(
    item_id = factor(sprintf("V%03d", seq_len(n_item))),
    u_item = rnorm(n_item, 0, sd_item)
  )

  tidyr::crossing(learners, items) |>
    dplyr::mutate(
      pretest_correct = rbinom(dplyr::n(), 1, known_rate),
      pretest_status = factor(
        pretest_correct,
        levels = c(0, 1),
        labels = c("Incorrect at pre-test", "Correct at pre-test")
      ),
      eta_post = -1.25 +
        beta_preknown * pretest_correct +
        beta_treatment_unknown * as.integer(treatment == "Gloss") * as.integer(pretest_correct == 0) +
        u_subj + u_item,
      posttest_correct = rbinom(dplyr::n(), 1, plogis(eta_post))
    )
}

run_vocab_learning_sim <- function(...) {
  dat <- simulate_vocab_learning_data(...)

  mod <- glmer(
    posttest_correct ~ treatment * pretest_status + (1 | learner_id) + (1 | item_id),
    family = binomial,
    data = dat,
    control = glmerControl(optimizer = "bobyqa")
  )

  fe <- broom.mixed::tidy(mod, effects = "fixed")
  p_unknown <- fe$p.value[fe$term == "treatmentGloss"]
  data.frame(p_unknown = p_unknown)
}
```

```{r}
#| eval: true
#| label: power-vocab-preknown-grid
#| cache: true
#| fig-height: 6.2
#| fig-width: 10

set.seed(2026)
nsim_known <- 20  # デモ用。本番は 500+ に増やす

grid_known <- expand.grid(
  n_subj_pg = c(15, 20, 30, 40),
  n_item = c(15, 20, 30),
  known_rate = c(0.2, 0.4, 0.6)
)
grid_known$power_unknown <- NA_real_

for (i in seq_len(nrow(grid_known))) {
  ns <- grid_known$n_subj_pg[i]
  ni <- grid_known$n_item[i]
  kr <- grid_known$known_rate[i]

  sims <- do.call(rbind, lapply(seq_len(nsim_known), function(j) {
    run_vocab_learning_sim(
      n_subj_per_group = ns,
      n_item = ni,
      known_rate = kr,
      beta_treatment_unknown = 0.55,
      beta_preknown = 1.80,
      sd_subj = 0.9,
      sd_item = 0.6
    )
  }))

  grid_known$power_unknown[i] <- mean(sims$p_unknown < 0.05, na.rm = TRUE)
}

grid_known$known_label <- factor(
  paste0("Pre-known rate = ", round(grid_known$known_rate * 100), "%"),
  levels = c("Pre-known rate = 20%", "Pre-known rate = 40%", "Pre-known rate = 60%")
)
grid_known$power_pct <- round(grid_known$power_unknown * 100)

ggplot(
  grid_known,
  aes(x = factor(n_subj_pg), y = factor(n_item), fill = power_unknown)
) +
  geom_tile(color = "white", linewidth = 1.2) +
  geom_text(aes(label = paste0(power_pct, "%")), size = 4.3, fontface = "bold") +
  facet_wrap(~ known_label) +
  scale_fill_gradient2(
    low = "#D73027", mid = "#FEE08B", high = "#1A9850",
    midpoint = 0.8, limits = c(0, 1), labels = scales::percent
  ) +
  labs(
    title = "Power for the Treatment Effect on Words Unknown at Pre-test",
    subtitle = sprintf("Higher pre-known rates leave fewer analyzable learning targets. nsim = %d per cell", nsim_known),
    x = "Participants per Group",
    y = "Number of Items",
    fill = "Power"
  ) +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**:

- facet は `pre-test` で既知だった語の割合です。**同じ参加者数・同じ項目数でも、known rate が高いほど power が落ちる**ことが分かります。
- `Pre-known rate = 60%` では、30 語を用意しても unknown-at-pretest 語はかなり減るため、学習効果の検出は難しくなります。
- したがって語彙研究では、「30 項目ある」ではなく、**「そのうち何語が未知語として残る見込みか」**まで含めて設計する必要があります。

要するに、語彙介入研究の power は `participants × items × unknown-word yield` の 3 つで決まります。pilot で target words の既知率が高いと分かった場合は、参加者数か項目数を増やすか、より難しい target words を選び直すのが実務的です。

::: {.callout-tip}
#### 検出力分析の実務ガイドライン

- **目標検出力**: 80%（慣例）。査読で求められることが増えています。
- **グループ数の目安**: ランダム切片のみなら各群 20+、ランダム傾きも含むなら 30+ が望ましい[@maas2005; @brysbaert2018]。
- **nsim**: 上記はデモ用に 50-100 回。最終報告には **500-1000 回**に増やしてください。
- **効果量の設定**: パイロットデータの推定値か、先行研究のメタ分析値を使用。
- **パラメータの感度分析**: ランダム効果の SD を ±50% 変えてみて、検出力がどう変わるかを確認するのも重要です。
:::

::: {.callout-note}
#### 報告テンプレート（検出力分析）

> A simulation-based power analysis was conducted following DeBruine and Barr (2021). We assumed effect sizes of β_B1 = 0.50 (OR = 1.65) and β_B2 = 1.00 (OR = 2.72) for the proficiency effect, with random intercept SDs of 1.0 (participants) and 0.5 (items), based on [pilot data / prior literature]. Binary response data were generated from the specified GLMM, fitted with `glmer()`, and the omnibus likelihood ratio test for the proficiency factor was evaluated. Monte Carlo simulations (nsim = 1000) across a grid of participant counts (15–50 per group) and item counts (10–30) indicated that XX participants per group with YY items provided ZZ% power (95% CI [LL, UU]) for the omnibus proficiency effect at α = .05 (Figure X).
:::

## 技術的補足（中級者向け）

::: {.callout-note collapse="true"}
#### `nAGQ` による推定精度の向上

`glmer()` はデフォルトで Laplace 近似（`nAGQ = 1`）を使います。`nAGQ` を増やすことで数値積分の精度が向上し、推定が改善する場合があります[@bates2015]。

```r
# nAGQ = 10 で精度向上
# 重要: nAGQ > 1 は「単一のランダム切片のみ」のモデルでしか使えない
# 本章のような交差分類 (1|sender) + (1|receiver) では使用不可
m_nAGQ <- glmer(correct ~ condition + (1 | participant),
                family = binomial, data = dat, nAGQ = 10)
```

**制約（重要）**: `nAGQ > 1` は以下の場合に**使用不可**です（lme4 の制約）：

- ランダム傾きを含むモデル（例: `(1 + condition | participant)`）
- **複数のグルーピング変数**を含むモデル（例: `(1 | participant) + (1 | item)`）

つまり、英語教育で典型的な「参加者 × 項目」の交差分類モデルでは `nAGQ > 1` は使えません。`nAGQ` が有効なのは `(1 | participant)` のみの単純な 1 グループモデルに限られます。`nAGQ` を増やしても推定値がほとんど変わらなければ、デフォルトの Laplace 近似で十分です。
:::

::: {.callout-note collapse="true"}
#### `allFit()` による収束の頑健性確認

収束警告が出た場合、`lme4::allFit()` で複数のオプティマイザを一括比較できます。全オプティマイザで推定値が近ければ、警告は無視して構いません。

```r
# 全利用可能オプティマイザで再推定
all_fits <- lme4::allFit(m_glmm_ri)
summary(all_fits)$fixef  # 固定効果の比較表

# 結果が一致していれば安全
ss <- summary(all_fits)
print(ss$which.OK)  # どのオプティマイザが収束したか
```
:::

::: {.callout-note collapse="true"}
#### ランダム効果の分散成分に対する信頼区間

ランダム効果の分散はモデル出力では点推定のみですが、`confint()` でプロファイル尤度ベースの信頼区間を計算できます。

```r
# プロファイル尤度ベースの CI（計算に時間がかかることがある）
ci <- confint(m_glmm_ri, method = "profile", oldNames = FALSE)
print(ci)
# .sig01 = sender SD, .sig02 = receiver SD, (Intercept) 〜 score_gap = 固定効果
```
:::

::: {.callout-note collapse="true"}
#### REML vs ML（GLMM では問題にならない理由）

線形混合モデル（`lmer()`）では REML と ML の選択が重要ですが、GLMM（`glmer()`）では Laplace 近似や AGQ による周辺尤度推定が自動で行われ、REML/ML の区別は存在しません。したがって、**`glmer()` で推定したモデル同士は AIC/LRT で自由に比較できます**（固定効果のみの変更でも、ランダム構造の変更でも）。
:::

## 非有意な結果の解釈と感度分析

検出力分析の考え方を踏まえたうえで、実際に非有意な結果が得られた場合にどう解釈すべきかを確認します。

::: {.callout-important}
### 「有意でない」は「効果がない」と同じではない

GLMM で p ≥ .05 を得た場合、以下の 2 つの可能性を区別する必要があります：

1. **真に効果がない**（真のゼロ効果）
2. **効果はあるが検出力が足りなかった**（偽陰性 / Type II error）

この区別には**信頼区間の幅**と**検出力の事後確認**が有用です。
:::

### 信頼区間による判断

```{r}
#| eval: true
#| label: sensitivity-ci-interpretation
#| echo: false

tibble::tribble(
  ~状況, ~`95% CI の特徴`, ~解釈,
  "p < .05, CI が狭い", "CI が 0（OR=1）をまたがない。幅が狭い", "効果あり。推定精度も高い。自信を持って報告可能。",
  "p ≥ .05, CI が狭い", "CI が 0（OR=1）をまたぐが、幅が狭い", "効果がない可能性が高い。実質的にゼロと判断してよい。",
  "p ≥ .05, CI が広い", "CI が 0（OR=1）をまたぎ、幅が非常に広い", "判断不能。検出力不足の可能性。サンプルサイズを増やして再検討。",
  "p < .05, CI が広い", "CI が 0 をまたがないが、OR の範囲が 1.2〜8.5 など", "効果はあるが推定が不安定。サンプルサイズに注意。"
) |>
  gt::gt() |>
  gt::tab_header(
    title = "p 値と信頼区間の組み合わせによる解釈ガイド",
    subtitle = "CI の幅が結論の強さを決める"
  )
```

### 感度分析: 「この設計で検出できた最小の効果量は？」

事前検出力分析（本章の検出力分析セクション参照）は研究計画前に行うものですが、**分析後**にも「このデータサイズで 80% の検出力があったのは OR ≥ いくつか？」を確認することで、非有意な結果の意味づけが可能になります。

```{r}
#| eval: true
#| label: sensitivity-analysis
#| cache: true
#| fig-height: 4
#| fig-width: 7

# ── 感度分析: 効果量を変えた検出力の探索 ──
# 「現在のサンプルサイズで OR = X を検出する力はどのくらいか？」
or_values <- seq(1.2, 4.0, by = 0.4)
nsim_sensitivity <- 25
sensitivity_power <- sapply(or_values, function(or_val) {
  sims <- do.call(rbind, lapply(seq_len(nsim_sensitivity), function(i) {
    run_single_sim(
      n_subj_per_group = 10,  # 本データの群あたり人数に近似
      n_item = 29,            # 本データのペア相手数
      beta_0 = -1.0,          # ベースレートが低い場合
      beta_B1 = log(or_val),  # 検出したい効果量
      beta_B2 = log(or_val * 1.5),
      sd_subj = 1.0, sd_item = 0.5
    )
  }))
  mean(sims$p_omnibus < 0.05)
})

sensitivity_df <- data.frame(OR = or_values, Power = sensitivity_power)

ggplot(sensitivity_df, aes(x = OR, y = Power)) +
  geom_line(linewidth = 1.2, color = "#0072B2") +
  geom_point(size = 2) +
  geom_hline(yintercept = 0.8, linetype = "dashed", color = "red") +
  geom_vline(xintercept = sensitivity_df$OR[which.min(abs(sensitivity_df$Power - 0.8))],
             linetype = "dotted", color = "gray50") +
  annotate("text", x = 3.5, y = 0.85, label = "80% power target",
           color = "red", size = 3.5) +
  labs(title = "Sensitivity Analysis: Detectable Effect Size",
       subtitle = "What is the smallest OR this study could detect at 80% power?",
       x = "Effect Size (Odds Ratio)", y = "Power") +
  theme_bw(base_size = 12, base_family = base_font)
```

**この図の読み方**: 赤線（80%）と曲線が交わる点が「この設計で信頼性をもって検出できる最小の効果量」です。それより小さい効果は、たとえ真に存在しても、このデータでは見逃す可能性が高いです。

#### 非有意結果の報告テンプレート

> The effect of [predictor] was not statistically significant (OR = X.XX, 95% CI [LL, UU], *p* = .XXX). A sensitivity analysis indicated that the current sample size provided 80% power to detect effects of OR ≥ X.XX. The observed confidence interval was [narrow enough to rule out practically meaningful effects / too wide to draw firm conclusions], suggesting that [the effect is likely negligible / additional data are needed to resolve this question].

## ベースレートとクラス不均衡

```{r}
#| eval: true
#| label: base-rate-check
#| fig-height: 3.5
#| fig-width: 7

# ── ベースレート（tie = 1 の割合）を確認 ──
tie_rate <- mean(dat_bin$tie)

ggplot(dat_bin, aes(x = factor(tie, labels = c("No tie (0)", "Tie (1)")))) +
  geom_bar(aes(fill = factor(tie)), show.legend = FALSE) +
  geom_text(stat = "count", aes(label = after_stat(count)), vjust = -0.5) +
  scale_fill_manual(values = c("0" = "#999999", "1" = "#E69F00")) +
  labs(title = sprintf("Outcome Distribution: Base Rate = %.1f%%", tie_rate * 100),
       x = "Outcome", y = "Count") +
  theme_bw(base_size = 12, base_family = base_font)
```

#### ベースレートが低い場合の注意点

ベースレートが低い（< 10-15%）場合：

1. **完全分離のリスク**: 一部の予測子の組み合わせで tie = 1 が 0 件になり、推定が発散する。`blme::bglmer()` やベイズ推定（`brms`）が有効。
2. **OR の解釈に注意**: ベースレートが低い場合、OR ≈ リスク比（RR）の近似が成り立つ。ベースレートが高い場合は OR > RR となり、OR は効果を過大評価する。
3. **検出力への影響**: イベントが少ないとパラメータ推定の精度が低下する。検出力分析のパラメータ設定に `beta_0`（切片）を反映させることが重要。

ICC に基づく GLM/GLMM の判断基準は「階層性の確認」セクションで詳述しました。安全策として、**データが階層的（繰り返し構造がある）なら常に GLMM を使うのが推奨**です。

#### Bayesian GLMM への発展

頻度主義の GLMM で収束問題が解決しない場合、または事前情報を活用したい場合は、**Bayesian GLMM**（`brms` パッケージ）が有力な代替手段です。

```r
# brms による Bayesian GLMM（最小例）
m_bayes <- brms::brm(
  tie ~ same_class + prof_gap + score_sender_sc + score_receiver_sc +
    (1 | sender) + (1 | receiver),
  family = bernoulli(link = "logit"),
  data = dat_bin,
  chains = 4, iter = 2000, warmup = 1000
)
```

Bayesian GLMM の利点: (1) 収束が安定、(2) 事前分布で完全分離を回避、(3) 事後分布から直接確率的な推論が可能。詳細は @buerkner2017 を参照。

## 第5章からの接続: GLMM と GAMM の使い分け

- **GLMM**: 非線形よりも、階層・反復構造の扱いが主課題のとき。
- **GAMM**: 階層構造に加えて連続予測子の非線形も重要なとき[@wood2017; @wood2020; @pedersen2019]。

次の判断が有効です。

1. まず GLMM（線形効果）
2. 残差/理論から非線形が必要なら GAMM へ拡張
3. 複雑化で解釈が落ちる場合は GLMM に戻して報告

### 発展: LME / Poisson / ordinal / multinomial への橋渡し

本章は 2 値（binomial）の GLMM ですが、**考え方の骨格**は他の混合モデルでもほぼ同じです。  
変わるのは主に次の 3 点です。

1. **アウトカムの分布**
2. **リンク関数**
3. **係数をどのスケールで解釈するか**

逆に、以下はかなり共通です。

- ランダム効果構造の考え方  
  どの予測子が `within-participant` / `within-item` かを見る
- `marginal` vs `conditional` 予測の区別
- `sjPlot` / `ggeffects` / `emmeans` を使った予測可視化
- 研究質問に合わせて「どの確率・どの期待値を報告するか」を明示すること

```{r}
#| eval: true
#| label: mixed-models-extension-table
#| echo: false

tibble::tribble(
  ~Model_family, ~Typical_outcome, ~Main_question, ~Typical_function, ~Coefficient_scale, ~What_to_plot,
  "LME (Gaussian)", "連続得点, z得点, 正規近似できる尺度", "平均がどれだけ違うか", "lmer()", "平均差 (b)", "adjusted means / fitted lines",
  "Poisson / NB GLMM", "回数, エラー数, 発話数", "期待回数がどれだけ変わるか", "glmer(..., family = poisson), glmmTMB()", "IRR = exp(beta)", "predicted counts / rates",
  "Ordinal mixed model", "Likert 1-5, CEFR rating", "上位カテゴリに入る odds がどう変わるか", "ordinal::clmm(), brms()", "cumulative OR", "category probabilities / Pr(Y >= k)",
  "Multinomial mixed model", "方略選択, 3択以上の反応", "各カテゴリの選択確率がどう配分されるか", "brms(), mclogit()", "relative risk ratio / category log-odds", "predicted category shares",
  "Gamma / lognormal mixed model", "反応時間, 所要時間, 正の連続量", "期待時間が何倍になるか", "glmmTMB(), brms()", "multiplicative effect", "predicted mean time"
) |>
  knitr::kable(
    caption = "2値 GLMM から他の混合モデルへ: 何が同じで、何が変わるか"
  )
```

#### 1. LME: 連続アウトカムなら、まず `lmer()`

アウトカムがほぼ正規的で、**平均差そのもの**を読みたいなら、最初に考えるべきは LME（線形混合モデル）です[@winter2013; @brown2021]。  
たとえば、語彙サイズ得点、作文総得点、理解度の合成尺度などです。

```r
lmer(score ~ time * group + (1 + time | learner_id) + (1 | item_id), data = dat)
```

ここでの係数は log-odds ではなく、**得点差そのもの**です。したがって、

- `b = 2.4` なら「平均で 2.4 点高い」
- 予測図は **adjusted mean の差** として読む

という、より直感的な解釈になります。  
ただし反応時間のように歪みが強い正の連続量では、LME より Gamma 系混合モデルのほうが自然なことがあります[@lo2015]。

#### 2. Poisson / Negative Binomial: カウントデータなら IRR で読む

誤用数、修正回数、発話イベント数、相談回数などは、**回数**なので Gaussian より Poisson / NB が自然です。  
Poisson は平均 = 分散を仮定し、過分散が強ければ Negative Binomial へ進みます。

```r
glmer(error_count ~ treatment + (1 | learner_id) + (1 | item_id),
      family = poisson, data = dat)
```

このとき `exp(beta)` は **IRR (incidence-rate ratio)** です。

- `IRR = 1.30` なら「期待回数が 1.30 倍」
- `IRR = 0.75` なら「期待回数が 25% 減る」

2値 GLMM で OR を確率差に戻したのと同じで、ここでも **IRR だけで終わらず、予測回数の図を併記**すると伝わりやすくなります。  
Poisson / NB では、`predicted count` や `predicted rate` の plot が binomial の予測確率図に対応します。

#### 3. Ordinal: Likert や CEFR のような順序尺度

5 件法 self-efficacy、CEFR 評定、作文 rubric などは、カテゴリに順序があるため ordinal mixed model が自然です[@mccullagh1989; @ordinalpkg]。

```r
ordinal::clmm(rating ~ treatment + time + (1 | learner_id) + (1 | rater_id), data = dat)
```

ここでの係数は、「1 点上がる効果」ではなく、**より高いカテゴリ側へ入る cumulative odds** の変化です。

- `OR = 1.80` なら「より高いカテゴリに入る odds が 1.8 倍」

ただし、読者に伝わるのは OR よりも

- `Pr(rating >= 4)`
- 各カテゴリの予測確率

です。  
したがって ordinal では、**threshold の説明より category probability の図**を主に読むのが実務的です。これは第5章で見た ordinal regression の読み方と同じです。

#### 4. Multinomial: 3 択以上の選択データ

未知語処理方略（skip / infer / lookup）、課題選好、反応タイプ分類のように、**順序のない 3 水準以上**なら multinomial です[@gudmestad2025; @agresti2013cda; @venables2002modern; @mclogitpkg]。

ここでは係数が参照カテゴリとの比較になるため、log-odds や RRR だけでは読みづらくなります。  
したがって、binomial 以上に

- **predicted category probabilities**
- **stacked area / stacked bar**

が重要です。  
読み方は「どの条件で、各カテゴリのシェアがどう再配分されるか」です。

#### 5. 何が変わっても、予測図の原理は同じ

この章で徹底した原理は、発展先でもそのまま使えます。

- **binomial**: 確率
- **Poisson / NB**: 期待回数
- **ordinal**: 上位カテゴリ確率 or カテゴリ確率
- **multinomial**: 各カテゴリの選択確率
- **LME**: 調整済み平均値

要するに、係数表は出発点ですが、**読者に伝える最終形は応答スケールの予測図**です。  
`sjPlot::plot_model()`、`ggeffects::ggpredict()`、`emmeans()` はこの意味で family が変わっても発想を保ちやすい道具です。違うのは、縦軸が確率なのか回数なのか平均値なのか、という点だけです。

#### 研究質問からの最短マッピング

```{r}
#| eval: true
#| label: advanced-model-routing
#| echo: false

tibble::tribble(
  ~Question, ~Recommended_model, ~Primary_interpretation,
  "平均点がどれだけ違うか", "LME (`lmer`)", "adjusted mean difference",
  "正答する確率がどれだけ違うか", "binomial GLMM (`glmer`)", "odds ratio + predicted probability",
  "エラー数・発話数がどれだけ違うか", "Poisson/NB GLMM", "IRR + predicted count",
  "より高い評定カテゴリに入る傾向が違うか", "ordinal mixed model", "cumulative OR + category probability",
  "3 つ以上の反応タイプの配分がどう変わるか", "multinomial mixed model", "category probability profile"
) |>
  knitr::kable(
    caption = "研究質問から混合モデル族を選ぶための最短ガイド"
  )
```

本章を読んだあとで最も自然な発展順は、  
**LME → Poisson/NB → ordinal → multinomial** です。  
理由は、ランダム効果構造の考え方を保ったまま、応答分布と解釈だけを 1 つずつ増やせるからです。

Poisson と ordinal の本格的な worked example はベイズ章で詳しく扱います。  
ここでは、第 6 章の読者がすぐ応用しやすい **LME** と **multinomial mixed model** を、SLA・心理言語学・英語教育の具体例で最後まで通します。

### Worked Example 1: LME で英語ライティングの改稿得点を分析する

#### 研究プロンプト

英語ライティング授業で、**AI フィードバック群は統制群より第 2 稿で大きく伸びるか**。  
学習者は複数の writing prompt に応答しており、各 prompt の難しさも異なる。このとき、単純な `lm()` ではなく `lmer()` を使うと、**学習者内の繰り返し**と**prompt 差**を同時に統制したうえで、調整済み平均差を報告できます。

#### 分析ステップ

1. `learner × prompt × draft` のロングデータを作る。
2. まず naive な `lm()` を当て、次に `lmer()` で `learner_id` と `prompt_id` のランダム切片を入れる。
3. `ggpredict()` で調整済み平均を描き、`emmip()` で下位比較を視覚化する。
4. 係数は平均差として、最終図は **adjusted mean score** として読む。

#### `lmer()` は何をしている関数か

`lmer()` は、**連続アウトカムの混合モデルを REML または ML で推定する関数**です[@lme4_lmer]。  
この worked example の式

```r
lmer(score ~ draft * feedback + proficiency_sc +
       (1 | learner_id) + (1 | prompt_id), data = dat_lme_worked)
```

は、次の 3 層に分けて読むと分かりやすいです。

1. `score`  
   連続の目的変数です。ここでは作文得点なので、係数はそのまま**得点差**として解釈できます。
2. `draft * feedback + proficiency_sc`  
   固定効果です。`draft * feedback` は `draft + feedback + draft:feedback` を意味します。  
   したがって、このモデルは「改稿の主効果」「群差」「AI 群だけ追加で伸びるか」を同時に見ています。
3. `(1 | learner_id) + (1 | prompt_id)`  
   ランダム切片です。同じ learner が複数 prompt に答え、同じ prompt に複数 learner が答えるため、各 learner と各 prompt に固有の baseline を持たせています。

`lmer()` のデフォルトは `REML = TRUE` です[@lme4_lmer]。  
したがって、**最終モデルの係数推定**は REML のままでよい一方、**固定効果構造を比較するとき**は ML に切り替える必要があります。これは `anova.merMod()` の公式ヘルプでも明示されており、`lmerMod` 同士を比較するとき、固定効果が違う REML モデルをそのまま比べるのは不適切なので、既定で `refit = TRUE` にして ML へ refit します[@lme4_anova]。

```{r}
#| eval: true
#| label: lme-worked-data

set.seed(6201)

n_learners_lme <- 48
n_prompts_lme  <- 8

learner_tbl_lme <- tibble(
  learner_id = factor(sprintf("L%02d", 1:n_learners_lme)),
  feedback = factor(
    rep(c("Control", "AI"), each = n_learners_lme / 2),
    levels = c("Control", "AI")
  ),
  proficiency_sc = scale(rnorm(n_learners_lme, mean = 0, sd = 1))[, 1],
  u0 = rnorm(n_learners_lme, mean = 0, sd = 2.4)
)

prompt_tbl_lme <- tibble(
  prompt_id = factor(sprintf("P%02d", 1:n_prompts_lme)),
  prompt_diff_sc = scale(seq(-1.2, 1.2, length.out = n_prompts_lme))[, 1],
  v0 = rnorm(n_prompts_lme, mean = 0, sd = 1.0)
)

dat_lme_worked <- tidyr::crossing(
  learner_id = learner_tbl_lme$learner_id,
  prompt_id  = prompt_tbl_lme$prompt_id,
  draft      = factor(c("Draft1", "Draft2"), levels = c("Draft1", "Draft2"))
) |>
  left_join(learner_tbl_lme, by = "learner_id") |>
  left_join(prompt_tbl_lme, by = "prompt_id") |>
  mutate(
    draft_num = ifelse(draft == "Draft2", 1, 0),
    mu = 72 +
      2.4 * proficiency_sc -
      1.3 * prompt_diff_sc +
      2.8 * draft_num +
      1.2 * (feedback == "AI") +
      2.1 * draft_num * (feedback == "AI") +
      u0 + v0,
    score = rnorm(n(), mean = mu, sd = 2.8)
  )

m_lme_naive <- lm(
  score ~ draft * feedback + proficiency_sc,
  data = dat_lme_worked
)

m_lme_worked <- lmer(
  score ~ draft * feedback + proficiency_sc +
    (1 | learner_id) + (1 | prompt_id),
  data = dat_lme_worked
)

r2_lme_worked <- performance::r2_nakagawa(m_lme_worked)
```

#### LME のモデル比較: REML と ML をどう使い分けるか

最も混乱しやすいのはここです。ルールはかなり単純です。

- **最終係数を報告するモデル**: `REML = TRUE` の `lmer()` でよい
- **固定効果を増減したモデル比較**: `REML = FALSE` に refit してから比べる
- **同じ固定効果でランダム効果だけ比べる**: REML 比較でもよいが、実務上は ML に統一してもよい

この worked example では、`draft:feedback` が本当に必要かを ML で確認します。

```{r}
#| eval: true
#| label: tbl-lme-worked-model-selection
#| echo: false

m_lme_main_ml <- update(
  m_lme_worked,
  . ~ . - draft:feedback,
  REML = FALSE
)

m_lme_full_ml <- update(
  m_lme_worked,
  REML = FALSE
)

tbl_lme_ml_compare <- anova(m_lme_main_ml, m_lme_full_ml) |>
  as.data.frame() |>
  tibble::rownames_to_column("Model") |>
  mutate(
    Model = c("No interaction (ML)", "Interaction model (ML)"),
    AIC = round(AIC, 1),
    BIC = round(BIC, 1),
    logLik = round(logLik, 1),
    Chisq = round(Chisq, 2),
    `Pr(>Chisq)` = format.pval(`Pr(>Chisq)`, digits = 3, eps = .001)
  ) |>
  dplyr::select(Model, npar, AIC, BIC, logLik, Chisq, Df, `Pr(>Chisq)`)

tbl_lme_ml_compare |>
  knitr::kable(
    caption = "Worked example (LME): fixed-effect comparison は ML で行う"
  )
```

この表では、`No interaction` から `Interaction model` へ行ったときに、AIC/BIC と LRT をまとめて見られます。  
ポイントは、**交互作用の有無を比べるときは ML 比較で判断し、採択後の係数や分散成分は REML で報告する**ことです。  
つまり、一文でまとめると:

> `lmer()` はまず REML で推定してよいが、固定効果を削る・足す比較だけは ML に切り替える。

#### 生データをまず見る

`lm()` と `lmer()` の違いは、**同じ学習者が複数 prompt に出てくる**ことと、**同じ prompt に複数の学習者が答える**ことをどこまで正面から扱うかにあります。  
まずは raw data の形を見ておくと、なぜ mixed model が必要かが直感的に分かります。

```{r}
#| eval: true
#| label: fig-lme-worked-raw
#| fig-cap: "Worked example (LME): raw mean scores for a subset of learners across two drafts"

set.seed(6202)
learners_to_show_lme <- sample(levels(dat_lme_worked$learner_id), 12)

dat_lme_raw_plot <- dat_lme_worked |>
  dplyr::filter(learner_id %in% learners_to_show_lme) |>
  group_by(learner_id, feedback, draft) |>
  summarise(score = mean(score), .groups = "drop")

ggplot(
  dat_lme_raw_plot,
  aes(x = draft, y = score, group = learner_id, color = feedback)
) +
  geom_line(alpha = 0.25, linewidth = 0.6) +
  geom_point(alpha = 0.35, size = 1.7) +
  stat_summary(
    aes(group = feedback),
    fun = mean,
    geom = "line",
    linewidth = 1.2
  ) +
  stat_summary(
    aes(group = feedback),
    fun = mean,
    geom = "point",
    size = 3
  ) +
  facet_wrap(~ feedback) +
  labs(
    x = "Draft",
    y = "Writing Score",
    color = "Feedback"
  )
```

この raw plot では、どちらの群も第 2 稿で上がっています。  
ただし、この図だけでは **同じ learner の複数観測**と**同じ prompt の共有**を分離できません。そこで次に `lm()` と `lmer()` を並べ、標準誤差の違いを確認します。

```{r}
#| eval: true
#| label: tbl-lme-worked-model-compare
#| echo: false

lme_term_labels <- c(
  "draftDraft2" = "Draft2 vs Draft1",
  "feedbackAI" = "AI vs Control at Draft1",
  "proficiency_sc" = "Proficiency (z)",
  "draftDraft2:feedbackAI" = "Extra AI gain at Draft2"
)

tbl_lme_worked_compare <- bind_rows(
  as_tibble(summary(m_lme_naive)$coefficients, rownames = "term") |>
    dplyr::filter(term %in% names(lme_term_labels)) |>
    transmute(
      Model = "Naive LM",
      Term = unname(lme_term_labels[term]),
      Estimate = round(Estimate, 2),
      SE = round(`Std. Error`, 2),
      Statistic = round(`t value`, 2)
    ),
  as_tibble(summary(m_lme_worked)$coefficients, rownames = "term") |>
    dplyr::filter(term %in% names(lme_term_labels)) |>
    transmute(
      Model = "LME",
      Term = unname(lme_term_labels[term]),
      Estimate = round(Estimate, 2),
      SE = round(`Std. Error`, 2),
      Statistic = round(`t value`, 2)
    )
)

tbl_lme_worked_compare |>
  knitr::kable(
    caption = "Worked example (LME): naive LM と LME の固定効果推定の比較"
  )
```

この比較では、**係数の方向そのものは大きく変わらない**一方で、SE は mixed model 側の方が「正直」になります。  
つまり LME の価値は、平均差を奇妙に変えることではなく、**不確かさを設計に見合った形で評価すること**にあります。

```{r}
#| eval: true
#| label: fig-lme-worked-est
#| fig-cap: "Worked example (LME): coefficient plot on the original score scale"

sjPlot::plot_model(
  m_lme_worked,
  type = "est",
  transform = NULL,
  show.values = TRUE,
  value.offset = 0.25,
  title = "LME coefficients (score scale)"
)
```

`LME` では係数がそのまま**得点差**です。  
たとえば `draftDraft2` が正なら、第 2 稿の平均得点が高いことを意味します。  
`draftDraft2:feedbackAI` が正なら、**AI 群の改稿 gain が統制群より大きい**と読みます。

#### 調整済み平均を予測図で読む

読者に伝わりやすい最終図は、log-odds ではなく **adjusted mean score** です。  
`ggpredict()` では「他の変数を固定したときの予測平均」を取り出せるので、`draft × feedback` の主たる研究質問をそのまま図にできます。

```{r}
#| eval: true
#| label: fig-lme-worked-pred
#| fig-cap: "Worked example (LME): adjusted mean writing scores from the mixed model"

pred_lme_worked <- ggeffects::ggpredict(
  m_lme_worked,
  terms = c("draft", "feedback")
) |>
  as_tibble()

ggplot(
  pred_lme_worked,
  aes(x = x, y = predicted, color = group, group = group)
) +
  geom_line(linewidth = 1.1) +
  geom_point(size = 2.5) +
  geom_errorbar(
    aes(ymin = conf.low, ymax = conf.high),
    width = 0.08,
    linewidth = 0.6
  ) +
  labs(
    x = "Draft",
    y = "Adjusted Mean Writing Score",
    color = "Feedback"
  )
```

この図は、「prompt の違いと learner の個人差を平均化したうえで、**平均的な learner がどれくらい伸びるか**」を表しています。  
本例では、Control も AI も第 2 稿で上がりますが、**AI 群の上昇幅の方が大きい**ため、交互作用が主要な読みどころになります。

#### 下位比較は `emmeans` と `emmip()` で可視化する

LME の結果を論文で書くときは、交互作用が見えたあとに「では各 draft で群差はどの程度か」を示すことが多いです。  
ここでは `emmeans()` で simple effect を計算し、`emmip()` で同じ grid を可視化します。

```{r}
#| eval: true
#| label: tbl-lme-worked-simple-effects
#| echo: false

emm_lme_worked <- emmeans(m_lme_worked, ~ feedback | draft)

tbl_lme_simple <- summary(pairs(emm_lme_worked), infer = TRUE) |>
  as.data.frame() |>
  transmute(
    Draft = draft,
    Contrast = contrast,
    Difference = round(estimate, 2),
    SE = round(SE, 2),
    Lower = round(lower.CL, 2),
    Upper = round(upper.CL, 2),
    p = format.pval(p.value, digits = 3, eps = .001)
  )

tbl_lme_simple |>
  knitr::kable(
    caption = "Worked example (LME): 各 draft における AI vs Control の simple effect"
  )
```

```{r}
#| eval: true
#| label: fig-lme-worked-emmip
#| fig-cap: "Worked example (LME): emmip plot for simple effects across drafts"

emmip(
  m_lme_worked,
  feedback ~ draft,
  CIs = TRUE
) +
  labs(
    x = "Draft",
    y = "Estimated Marginal Mean Score",
    color = "Feedback"
  )
```

`emmip()` の利点は、**simple effect の検定と同じ EMM grid をそのまま図にしている**ことです。  
この worked example では、第 1 稿では群差が比較的小さく、第 2 稿では AI 群がさらに上がるため、`draftDraft2:feedbackAI` の正の交互作用が視覚的にも確認できます。

LME の報告では、次の 3 点を確認すると十分です。

1. 係数は **得点差**として読む。
2. 読者向けの主図は **adjusted mean score** にする。
3. 交互作用があるなら、`emmeans` と `emmip()` で **どこに差があるか**を分解する。

補足として、`m_lme_worked` の Nakagawa 型 $R^2$ は `marginal = `r round(r2_lme_worked$R2_marginal, 3)`、`conditional = `r round(r2_lme_worked$R2_conditional, 3)` です。  
つまり、この例では固定効果だけでもかなりの説明力がありますが、learner と prompt のランダム効果を加えると説明力はさらに伸びる、という読みになります。

#### 2値ロジスティック GLMM と multinomial mixed model は何が違うか

worked example 2 に入る前に、**2 値**と**多値**の違いを 1 回だけ整理しておくと混乱しにくくなります。

- 2 値ロジスティック GLMM は、`P(Y = 1)` を 1 本の logit で表します[@lme4_glmer]。
- multinomial mixed model は、カテゴリが $K$ 個あるとき、**基準カテゴリに対して $K-1$ 本**の logit を立てます[@mclogit_mblogit]。

2 値のときは、基準を入れ替えても実質的には同じです。  
`success vs failure` を `failure vs success` に変えると、係数の符号が反転し OR が逆数になるだけで、**予測確率は同じ**です。

一方、3 カテゴリ以上では事情が変わります。`A / B / C` の 3 択で `A` を reference にすると、出てくるのは

- `B vs A`
- `C vs A`

です。  
`B vs C` が「消える」わけではなく、**別の基準化に隠れている**だけです。だから多値モデルでは、binary よりもさらに

- 参照カテゴリを明記する
- 係数表だけで終わらず、カテゴリ確率の図を出す

ことが重要になります。

```{r}
#| eval: true
#| label: tbl-binary-vs-multinomial
#| echo: false

tibble::tribble(
  ~Aspect, ~`2値ロジスティック GLMM`, ~`Multinomial mixed model`,
  "方程式の本数", "1 本", "カテゴリ数 - 1 本",
  "係数の読み", "1 単位増加で成功 odds が何倍か", "基準カテゴリに対して各カテゴリへどれだけ寄るか",
  "参照カテゴリ変更の影響", "符号反転・OR が逆数になるだけ", "表示される比較式そのものが変わる",
  "主たる予測図", "Pr(Y = 1)", "各カテゴリの選択確率の配分",
  "本文での主報告", "OR + predicted probability", "RRR は補助、主報告は category shares"
) |>
  knitr::kable(
    caption = "2値ロジスティック GLMM と multinomial mixed model の違い"
  )
```

要するに、

- **binary** では「1 つの確率」を追う
- **multinomial** では「複数カテゴリの配分」を追う

という違いがあります。  
このため、2 値 GLMM の最終図が 1 本の予測確率線になるのに対して、multinomial mixed model の最終図は **カテゴリシェアがどう入れ替わるか**を見せる図になります。

### 実践例 2: Multinomial mixed model で未知語処理方略の配分をみる

#### 研究プロンプト

英語読解中に未知語に出会ったとき、学習者は **Infer / Lookup / Skip** のどの方略を選ぶのか。  
L1 gloss の有無と proficiency が、この 3 択の**配分そのもの**をどう変えるかを見たい。  
同じ learner が複数 item に答え、同じ item に複数 learner が遭遇するので、`learner_id × item_id` の交差分類を入れた multinomial mixed model が必要になります[@gudmestad2025; @mclogitpkg]。

#### 分析ステップ

1. `learner × item` のロングデータを作り、各観測の strategy を `Infer / Lookup / Skip` として保持する。
2. `mclogit::mblogit()` で learner と item のランダム切片を入れた baseline-category logit を推定する。
3. 係数は `Lookup vs Infer`、`Skip vs Infer` の **RRR** として読む。
4. ただし本文の主図は RRR ではなく、**調整済みカテゴリ確率の再配分**にする。

#### `mblogit()` は何をしている関数か

`mblogit()` は、**baseline-category logit model** を推定する関数です[@mclogit_mblogit]。  
目的変数が 3 カテゴリ `A / B / C` のとき、1 つを基準カテゴリに決め、残りのカテゴリをその基準と比較する形で複数の logit 方程式を同時に推定します。

たとえば `A` を基準にすると、

$$
\log \frac{P(B)}{P(A)} = \eta_B, \qquad
\log \frac{P(C)}{P(A)} = \eta_C
$$

の 2 本が推定されます。  
本例では response の factor levels を `c("Infer", "Lookup", "Skip")` にしているので、**`Infer` が reference category** です。したがって `summary(m_multinom_worked)` には

- `Lookup vs Infer`
- `Skip vs Infer`

だけが表示されます。

ここでよくある疑問は、**「では `Lookup vs Skip` はどこに行ったのか」**です。  
結論から言うと、**消えていません**。baseline-category logit では、`Lookup vs Skip` は基準カテゴリを介して表現されているだけです。

$$
\log \frac{P(\text{Skip})}{P(\text{Lookup})}
=
\log \frac{P(\text{Skip})}{P(\text{Infer})}
\log \frac{P(\text{Lookup})}{P(\text{Infer})}
$$

つまり、

- `A-B`
- `A-C`

が分かれば、`B-C` はそこから復元できます。  
ただし実務では、**係数の差を手で計算するより、基準カテゴリを入れ替えるか、予測カテゴリ確率を図で読む方が安全**です。

もう 1 つ重要なのは、mixed multinomial の `mblogit()` では random effects 推定に **PQL または MQL の近似**を使うことです[@mclogit_mblogit]。  
この worked example では `method = "PQL"` を明示しており、`predict(..., type = "response", conditional = FALSE)` を使って **population-level category probabilities** を描きます[@mclogit_predict]。  
したがって multinomial mixed model では、2 値 GLMM 以上に

- 係数表は「補助」
- 主たる結論は **predicted category shares**

という読み方が重要になります。
```{r}
#| eval: true
#| label: multinom-worked-data
#| message: false
#| warning: false

set.seed(6203)

n_learners_multi <- 42
n_items_multi    <- 24

learner_tbl_multi <- tibble(
  learner_id = factor(sprintf("L%02d", 1:n_learners_multi)),
  gloss = factor(
    rep(c("NoGloss", "L1Gloss"), each = n_learners_multi / 2),
    levels = c("NoGloss", "L1Gloss")
  ),
  proficiency = factor(
    sample(c("A2", "B1", "B2"), n_learners_multi, replace = TRUE, prob = c(0.35, 0.40, 0.25)),
    levels = c("A2", "B1", "B2")
  ),
  prof_sc = scale(c(A2 = -0.8, B1 = 0, B2 = 0.9)[as.character(proficiency)])[, 1],
  u_lookup = rnorm(n_learners_multi, mean = 0, sd = 0.35),
  u_skip   = rnorm(n_learners_multi, mean = 0, sd = 0.30)
)

item_tbl_multi <- tibble(
  item_id = factor(sprintf("W%02d", 1:n_items_multi)),
  frequency_sc = scale(runif(n_items_multi, -1.2, 1.2))[, 1],
  cognate = factor(
    sample(c("NonCognate", "Cognate"), n_items_multi, replace = TRUE),
    levels = c("NonCognate", "Cognate")
  ),
  v_lookup = rnorm(n_items_multi, mean = 0, sd = 0.20),
  v_skip   = rnorm(n_items_multi, mean = 0, sd = 0.20)
)

dat_multinom_worked <- tidyr::crossing(
  learner_id = learner_tbl_multi$learner_id,
  item_id    = item_tbl_multi$item_id
) |>
  left_join(learner_tbl_multi, by = "learner_id") |>
  left_join(item_tbl_multi, by = "item_id") |>
  mutate(
    eta_lookup = -0.1 +
      0.75 * (gloss == "L1Gloss") -
      0.80 * prof_sc -
      0.45 * frequency_sc +
      0.25 * (cognate == "Cognate") +
      u_lookup + v_lookup,
    eta_skip = 0.5 -
      0.75 * (gloss == "L1Gloss") -
      1.00 * prof_sc -
      0.55 * frequency_sc -
      0.15 * (cognate == "Cognate") +
      u_skip + v_skip,
    denom = 1 + exp(eta_lookup) + exp(eta_skip),
    p_infer  = 1 / denom,
    p_lookup = exp(eta_lookup) / denom,
    p_skip   = exp(eta_skip) / denom,
    strategy = purrr::pmap_chr(
      list(p_infer, p_lookup, p_skip),
      function(a, b, c) sample(c("Infer", "Lookup", "Skip"), 1, prob = c(a, b, c))
    ),
    strategy = factor(strategy, levels = c("Infer", "Lookup", "Skip"))
  )

# mclogit::mblogit() は PQL の反復過程を詳細に表示するため、
# 出力は隠しても、収束そのものは次の表で明示的に確認する。
m_multinom_worked <- mclogit::mblogit(
  strategy ~ gloss + proficiency + frequency_sc + cognate,
  random = list(~1 | learner_id, ~1 | item_id),
  data = dat_multinom_worked,
  method = "PQL"
)
```

```{r}
#| eval: true
#| label: tbl-multinom-worked-meta
#| echo: false

tbl_multinom_meta <- tibble(
  Metric = c(
    "Outcome",
    "Baseline category",
    "Learner groups",
    "Item groups",
    "Observations",
    "Converged",
    "Approx. residual deviance"
  ),
  Value = c(
    "Infer / Lookup / Skip",
    "Infer",
    n_distinct(dat_multinom_worked$learner_id),
    n_distinct(dat_multinom_worked$item_id),
    nrow(dat_multinom_worked),
    ifelse(isTRUE(m_multinom_worked$converged), "Yes", "No"),
    round(m_multinom_worked$deviance, 1)
  )
)

tbl_multinom_meta |>
  knitr::kable(
    caption = "Worked example (multinomial mixed model): モデル構造と収束確認"
  )
```

#### `Lookup vs Infer` と `Skip vs Infer` しか出ないのはなぜか

`mblogit()` のヘルプは、戻り値の `coefficients` や `fitted.values` が**カテゴリ数 - 1 本の行列**になると説明しています[@mclogit_mblogit]。  
要するに、3 カテゴリなら「3 本」ではなく「2 本」の比較式が出るのが仕様です。

この点を実データで確認するために、基準カテゴリを `Infer` から `Lookup` に入れ替えてみます。  
すると先ほどは表示されなかった `Skip vs Lookup` が出てきます。

```{r}
#| eval: true
#| label: tbl-multinom-worked-relevel
#| echo: false
#| message: false
#| warning: false

dat_multinom_lookup_ref <- dat_multinom_worked |>
  mutate(strategy_lookup_ref = relevel(strategy, ref = "Lookup"))

m_multinom_lookup_ref <- mclogit::mblogit(
  strategy_lookup_ref ~ gloss + proficiency + frequency_sc + cognate,
  random = list(~1 | learner_id, ~1 | item_id),
  data = dat_multinom_lookup_ref,
  method = "PQL"
)

coef_base_infer <- summary(m_multinom_worked)$coefficients
coef_base_lookup <- summary(m_multinom_lookup_ref)$coefficients

tbl_multinom_relevel <- tibble(
  Displayed_comparison = c(
    "Lookup vs Infer",
    "Skip vs Infer",
    "Infer vs Lookup",
    "Skip vs Lookup"
  ),
  `Gloss coefficient` = c(
    coef_base_infer["Lookup~glossL1Gloss", "Estimate"],
    coef_base_infer["Skip~glossL1Gloss", "Estimate"],
    coef_base_lookup["Infer~glossL1Gloss", "Estimate"],
    coef_base_lookup["Skip~glossL1Gloss", "Estimate"]
  ),
  RRR = exp(`Gloss coefficient`)
) |>
  mutate(
    `Gloss coefficient` = round(`Gloss coefficient`, 3),
    RRR = round(RRR, 2)
  )

tbl_multinom_relevel |>
  knitr::kable(
    caption = "Worked example (multinomial mixed model): 基準カテゴリを変えると表示される比較式も変わる"
  )
```

この表の読み方は次のとおりです。

- `Lookup vs Infer` の係数が正  
  `L1Gloss` だと `Infer` より `Lookup` が出やすい
- `Infer vs Lookup` の係数が負  
  これは同じ内容を**逆向き**に言っているだけ
- `Skip vs Lookup` が負  
  `L1Gloss` だと `Skip` より `Lookup` に寄りやすい

したがって、**B-C 比較は存在しないのではなく、どのカテゴリを分母に置くかで「表示のされ方」が変わる**と理解するのが正確です。  
そして最終的には、係数を一つずつ追うより、次節のように **カテゴリ確率の再配分図**で全体像を読む方が誤解が少なくなります。

#### まずは「何と何を比較している係数か」を明示する

multinomial mixed model の係数は、2 値 GLMM のように 1 本ではありません。  
この例では `Infer` を基準カテゴリにしているので、

- `Lookup vs Infer`
- `Skip vs Infer`

の 2 本の logit 方程式が同時に推定されます。  
したがって、係数表を見るときは必ず **どの比較式の係数か** を先に確認する必要があります。

```{r}
#| eval: true
#| label: tbl-multinom-worked-coefs
#| echo: false

tbl_multinom_coef <- as_tibble(
  summary(m_multinom_worked)$coefficients,
  rownames = "comparison_term"
) |>
  tidyr::separate(
    comparison_term,
    into = c("Comparison", "Term"),
    sep = "~",
    extra = "merge"
  ) |>
  mutate(
    Comparison = recode(
      Comparison,
      "Lookup" = "Lookup vs Infer",
      "Skip"   = "Skip vs Infer"
    ),
    Term = recode(
      Term,
      "(Intercept)" = "Intercept",
      "glossL1Gloss" = "L1 Gloss vs No Gloss",
      "proficiencyB1" = "B1 vs A2",
      "proficiencyB2" = "B2 vs A2",
      "frequency_sc" = "Word frequency (z)",
      "cognateCognate" = "Cognate vs Non-cognate"
    ),
    RRR = exp(Estimate),
    Lower = exp(Estimate - 1.96 * `Std. Error`),
    Upper = exp(Estimate + 1.96 * `Std. Error`)
  ) |>
  transmute(
    Comparison,
    Term,
    Estimate = round(Estimate, 2),
    SE = round(`Std. Error`, 2),
    RRR = round(RRR, 2),
    Lower = round(Lower, 2),
    Upper = round(Upper, 2),
    p = format.pval(`Pr(>|z|)`, digits = 3, eps = .001)
  )

tbl_multinom_coef |>
  knitr::kable(
    caption = "Worked example (multinomial mixed model): RRR を併記した fixed-effect table"
  )
```

```{r}
#| eval: true
#| label: fig-multinom-worked-rrr
#| fig-cap: "Worked example (multinomial mixed model): relative risk ratios by comparison equation"

plot_multinom_rrr <- as_tibble(
  summary(m_multinom_worked)$coefficients,
  rownames = "comparison_term"
) |>
  tidyr::separate(
    comparison_term,
    into = c("Comparison", "Term"),
    sep = "~",
    extra = "merge"
  ) |>
  dplyr::filter(Term != "(Intercept)") |>
  mutate(
    Comparison = recode(
      Comparison,
      "Lookup" = "Lookup vs Infer",
      "Skip"   = "Skip vs Infer"
    ),
    Term = recode(
      Term,
      "glossL1Gloss" = "L1 Gloss vs No Gloss",
      "proficiencyB1" = "B1 vs A2",
      "proficiencyB2" = "B2 vs A2",
      "frequency_sc" = "Word frequency (z)",
      "cognateCognate" = "Cognate vs Non-cognate"
    ),
    RRR = exp(Estimate),
    Lower = exp(Estimate - 1.96 * `Std. Error`),
    Upper = exp(Estimate + 1.96 * `Std. Error`)
  )

ggplot(
  plot_multinom_rrr,
  aes(x = RRR, y = forcats::fct_rev(Term))
) +
  geom_vline(xintercept = 1, linetype = 2, color = "gray50") +
  geom_errorbarh(aes(xmin = Lower, xmax = Upper), height = 0.15) +
  geom_point(size = 2.2) +
  facet_wrap(~ Comparison, nrow = 1) +
  scale_x_log10() +
  labs(
    x = "Relative Risk Ratio (log scale)",
    y = NULL
  )
```

ここでの `RRR > 1` は、その比較式で**分子側のカテゴリが出やすくなる**ことを意味します。  
たとえば `Lookup vs Infer` で `L1 Gloss vs No Gloss` の `RRR > 1` なら、L1 gloss があると `Infer` より `Lookup` に寄りやすい、という意味です。  
一方、本文の最終メッセージとしては、RRR よりも「3 つのカテゴリのシェアがどう再配分されるか」を示す方が直感的です。  
特に multinomial では、

- `RRR > 1` でも、そのカテゴリの**最終確率**が最も大きいとは限らない
- あるカテゴリが増えると、残り 2 カテゴリのシェアも同時に変わる

ため、**係数表だけでは全体像を取り違えやすい**点に注意が必要です。

#### 主図はカテゴリ確率の再配分にする

`mblogit()` は `predict(..., type = "response")` でカテゴリ確率を返します。  
ここでは `conditional = FALSE` にしてランダム効果を 0 に固定し、**population-level prediction** を作ります。  
さらに、item の頻度や cognate 性は元データの分布をそのまま残したまま平均化し、**実際の item mix を織り込んだ調整済みカテゴリ配分**を描きます。

```{r}
#| eval: true
#| label: fig-multinom-worked-shares
#| fig-cap: "Worked example (multinomial mixed model): observed vs adjusted category shares"

obs_multinom_worked <- dat_multinom_worked |>
  count(gloss, proficiency, strategy, name = "n") |>
  group_by(gloss, proficiency) |>
  mutate(prob = n / sum(n), Source = "Observed share") |>
  ungroup() |>
  dplyr::select(gloss, proficiency, strategy, prob, Source)

pred_multinom_worked <- as_tibble(
  predict(m_multinom_worked, type = "response", conditional = FALSE)
) |>
  bind_cols(dat_multinom_worked |> dplyr::select(gloss, proficiency)) |>
  pivot_longer(
    cols = c(Infer, Lookup, Skip),
    names_to = "strategy",
    values_to = "prob"
  ) |>
  group_by(gloss, proficiency, strategy) |>
  summarise(prob = mean(prob), .groups = "drop") |>
  mutate(Source = "Adjusted prediction")

bind_rows(obs_multinom_worked, pred_multinom_worked) |>
  ggplot(aes(x = proficiency, y = prob, fill = strategy)) +
  geom_col(width = 0.72) +
  facet_grid(Source ~ gloss) +
  scale_y_continuous(labels = scales::label_percent()) +
  labs(
    x = "Proficiency",
    y = "Strategy Share",
    fill = "Strategy"
  )
```

この図では、**raw の観測比率**と**モデルで調整したカテゴリ配分**を分けて見られます。  
この例では次の読みが重要です。

1. `NoGloss` では A2 学習者の `Skip` が大きい。
2. `L1Gloss` を与えると、A2/B1 では `Skip` の一部が `Lookup` に移る。
3. B2 では `Infer` が優勢になり、gloss の有無で配分は少し変わっても、`Skip` はかなり減る。

要するに、multinomial mixed model の結論は「どの係数が有意か」より、**どの条件で 3 つの選択肢のシェアがどう入れ替わるか**として述べるのが自然です。

#### `mblogit()` のモデル比較はどう考えるか

ここは `lmer()` より慎重に扱うべきです。  
`lmer()` では ML へ refit して LRT/AIC を比較的容易に使用できますが、mixed multinomial の `mblogit()` は `PQL` / `MQL` の**準尤度近似**に基づいています[@mclogit_mblogit]。そのため、`anova()` で deviance difference を機械的に検定すると、「quasi-likelihood の deviance は直接比較できないので結果は不安定」という警告が出ます。

したがって、この章の実務ルールは次の通りです。

1. `mblogit()` の固定効果は、まず係数方向と RRR で把握する
2. 最終的な主張は **predicted category shares** の安定性で行う
3. AIC は候補モデルのスクリーニングには使えるが、LME のような「厳密な LRT」と同じ強さでは扱わない
4. 厳密な比較や完全な事後分布が必要なら、後続のベイズ章で扱う multinomial multilevel model へ進む

この点は、multinomial mixed model が「使ってはいけない」という意味ではありません。  
むしろ、**係数の有意差だけで押し切らず、カテゴリ確率の再配分図を主たるエビデンスにする**という、より丁寧な報告を促すものと理解するとよいです。

```{r}
#| eval: true
#| label: fig-multinom-worked-lookup
#| fig-cap: "Worked example (multinomial mixed model): adjusted probability of Lookup"

pred_multinom_worked |>
  dplyr::filter(strategy == "Lookup") |>
  ggplot(aes(x = proficiency, y = prob, color = gloss, group = gloss)) +
  geom_line(linewidth = 1) +
  geom_point(size = 2.5) +
  scale_y_continuous(labels = scales::label_percent()) +
  labs(
    x = "Proficiency",
    y = "Adjusted Pr(Lookup)",
    color = "Gloss"
  )
```

この補助図は、`Lookup` だけに焦点を当てた読み方です。  
たとえば英語教育の授業設計では、「gloss を付けると**どの proficiency 帯で**辞書的参照が増えるのか」を直接知りたいことがあります。その場合、3 択全体の stacked bar に加えて、**関心カテゴリ 1 本の確率図**を出すと解釈しやすくなります。

multinomial mixed model の報告では、次の原則が有効です。

1. 係数表では `Lookup vs Infer`、`Skip vs Infer` の**比較式**を明記する。
2. 本文の主たる解釈は **category probability profile** に置く。
3. `conditional = FALSE` の予測は「新しい learner・新しい item への平均的予測」であると明記する。
4. 必要なら、関心カテゴリだけを取り出した確率図も補助的に示す。

## 報告テンプレート（実践向け）

::: {.panel-tabset}

### Method

以下は本データの変数名を使った worked example です。自身の分析に合わせて変数名と数値を置き換えてください。

### Method 欄での引用の方法

SLA 分野の論文では、`lme4` の技術論文[@bates2015]だけでなく、
**分野内のチュートリアル論文**[@cunnings2012; @linck2015]も併せて引用すると、
「なぜこの手法を選んだか」の正当化が査読者に伝わりやすくなります。

```text
We analyzed directed advice ties among 30 EFL learners using a
binomial generalized linear mixed-effects model (GLMM; Bates et al.,
2015) implemented in R (lme4 package, version 1.1-x). The binary
outcome was whether a given sender–receiver pair had an advice tie
(1) or not (0). Fixed effects included same-class membership
(binary), proficiency gap (ordinal), standardized sender baseline
score, standardized receiver baseline score, and absolute score gap.
To account for the non-independence of dyadic observations, we
specified crossed random intercepts for sender and receiver.

Model selection proceeded in two stages: (a) random-effect structure
was compared via AIC, BIC, and likelihood-ratio tests, with
singularity checks (isSingular; Bates et al., 2015); (b) fixed
effects were evaluated via backward elimination (drop1) with the
random structure held constant. The optimizer was set to bobyqa
(maxfun = 200,000). Diagnostics included DHARMa simulation-based
residual tests (Hartig, 2022), overdispersion checks, and VIF-based
collinearity assessment (performance package; Lüdecke et al., 2021).
```

### Results（本データの実際の値を使った完成形）

以下は本章の分析結果を inline R で自動挿入した**完成形の報告例**です。自分のデータでは変数名と数値を置き換えてください。

**固定効果**:

Same-class membership positively predicted advice tie formation, β = `r sprintf("%.2f", lme4::fixef(m_glmm_ri)["same_class"])`, SE = `r sprintf("%.2f", summary(m_glmm_ri)$coefficients["same_class", "Std. Error"])`, *z* = `r sprintf("%.2f", summary(m_glmm_ri)$coefficients["same_class", "z value"])`, *p* < .001, OR = `r sprintf("%.2f", exp(lme4::fixef(m_glmm_ri)["same_class"]))`, 95% CI [`r sprintf("%.2f", exp(lme4::fixef(m_glmm_ri)["same_class"] - 1.96 * summary(m_glmm_ri)$coefficients["same_class", "Std. Error"]))`, `r sprintf("%.2f", exp(lme4::fixef(m_glmm_ri)["same_class"] + 1.96 * summary(m_glmm_ri)$coefficients["same_class", "Std. Error"]))`]. Learners in the same class were approximately `r sprintf("%.1f", exp(lme4::fixef(m_glmm_ri)["same_class"]))` times more likely to form an advice tie than those in different classes.

Proficiency gap also positively predicted advice tie formation, β = `r sprintf("%.2f", lme4::fixef(m_glmm_ri)["prof_gap"])`, SE = `r sprintf("%.2f", summary(m_glmm_ri)$coefficients["prof_gap", "Std. Error"])`, *z* = `r sprintf("%.2f", summary(m_glmm_ri)$coefficients["prof_gap", "z value"])`, *p* = `r sprintf("%.3f", summary(m_glmm_ri)$coefficients["prof_gap", "Pr(>|z|)"])`, OR = `r sprintf("%.2f", exp(lme4::fixef(m_glmm_ri)["prof_gap"]))`. Sender baseline score, receiver baseline score, and score gap were not significant predictors (*p*s > .05).

**ランダム効果・分散説明**:

Random intercept variance was estimated for both sender (σ² = `r sprintf("%.3f", as.data.frame(lme4::VarCorr(m_glmm_ri))$vcov[1])`) and receiver (σ² = `r sprintf("%.3f", as.data.frame(lme4::VarCorr(m_glmm_ri))$vcov[2])`), confirming clustered dependence in dyadic observations. Model-level explained variance was R²~marginal~ = `r sprintf("%.3f", performance::r2_nakagawa(m_glmm_ri)$R2_marginal)` (fixed effects only) and R²~conditional~ = `r sprintf("%.3f", performance::r2_nakagawa(m_glmm_ri)$R2_conditional)` (fixed + random effects). The model demonstrated acceptable discrimination (AUC = `r sprintf("%.3f", as.numeric(pROC::auc(roc_cond)))`).

**モデル比較**:

The GLMM with crossed random intercepts (sender and receiver) showed substantially improved fit over the fixed-effects-only GLM (ΔAIC = `r sprintf("%.1f", AIC(m_glm) - AIC(m_glmm_ri))`). A random-slope model for same_class was also tested but exhibited singular fit, so the random-intercept model was retained as the final model.

**診断**:

```text
Diagnostic checks indicated no major violations: DHARMa
simulation-based tests for dispersion (p = .XX) and uniformity
(p = .XX) were non-significant, VIF values were below X.X for
all predictors, and visual inspection of residual plots
revealed no systematic patterns.
```

### 査読者チェックリスト

論文投稿前に以下の 7 項目を確認してください：

1. **GLM vs GLMM の根拠**: ICC/DEFF またはモデル比較（ΔAIC）で GLMM の必要性を示したか
2. **ランダム効果構造の選択**: ランダム傾きを試み、特異適合が生じたためランダム切片に留めた、等の経緯を記載したか
3. **特異適合チェック**: `isSingular()` の結果を報告したか（特に RS モデルを試みた場合）
4. **OR + 確率スケール**: 固定効果を OR（95% CI 付き）と応答スケール予測の両方で報告したか
5. **R² の併記**: marginal R² と conditional R² の両方を報告したか
6. **診断結果**: DHARMa（dispersion + uniformity）、VIF、残差プロットの結果を記載したか
7. **オプティマイザ設定**: `bobyqa`、`maxfun` の値を記載したか（再現性のため）

:::

### APA 7 最低限の表記ルール

- 統計記号はイタリック: *β*, *SE*, *z*, *p*, OR, *R*^2^
- *p* 値は `p = .032`、非常に小さい場合は `p < .001`
- 区間推定は `95% CI [LL, UL]`
- GLMM では ICC と *R*^2^ (marginal / conditional) を併記

### コピペ用 1 文テンプレート

**Method（最短版）**: "We fitted a binomial GLMM with crossed random intercepts for sender and receiver (lme4; Bates et al., 2015), using bobyqa optimization."

**固定効果 1 予測子**: "[Predictor] significantly predicted [outcome], *β* = X.XX, *SE* = X.XX, *z* = X.XX, *p* < .001, OR = X.XX, 95% CI [X.XX, X.XX]."

**ランダム効果**: "Random intercept variances were σ²~sender~ = X.XX and σ²~receiver~ = X.XX; *R*^2^~marginal~ = .XXX, *R*^2^~conditional~ = .XXX."

**診断**: "DHARMa residual tests and VIF diagnostics indicated no major model violations."

## 自分のデータに適用するには

本章は助言ネットワークデータ（sender × receiver の交差分類）を使いましたが、英語教育の研究データは多くの場合「**学習者 × テスト項目**」の構造です。以下に、典型的なデータ形式から GLMM に到達するまでの手順を示します。

### ワイド形式からロング形式への変換

多くのテストデータは「1行 = 1学習者、列 = 各項目の正誤」のワイド形式で記録されています。`glmer()` はロング形式（1行 = 1観測）を要求するため、変換が必要です。

```r
# ── ワイド形式（よくある形式）──
# learner_id, item1, item2, item3, ..., group
# L01,        1,     0,     1,    ..., intervention

# ── ロング形式に変換 ──
dat_long <- dat_wide |>
  tidyr::pivot_longer(
    cols = starts_with("item"),  # 項目列を指定
    names_to = "item",           # 列名 → item 変数
    values_to = "correct"        # 値 → correct (0/1)
  ) |>
  dplyr::mutate(
    learner_id = factor(learner_id),
    item       = factor(item)
  )

# ── GLMM の推定 ──
m <- lme4::glmer(correct ~ group + (1 | learner_id) + (1 | item),
           family = binomial, data = dat_long)
```

### プレ・ポストデザインの場合

```r
# time (pre=0, post=1) は within-participant → ランダム傾きが可能
m <- glmer(correct ~ time * group + (1 + time | learner_id) + (1 | item),
           family = binomial, data = dat_long,
           control = glmerControl(optimizer = "bobyqa"))

# 収束しない場合は相関を除去
m <- glmer(correct ~ time * group + (1 + time || learner_id) + (1 | item),
           family = binomial, data = dat_long,
           control = glmerControl(optimizer = "bobyqa"))
```

### 本章との対応

| 本章のデータ | 実際の研究データ | 対応 |
|---|---|---|
| sender | learner_id | ランダム切片のグルーピング変数 |
| receiver | item | ランダム切片のグルーピング変数 |
| tie (0/1) | correct (0/1) | 2値アウトカム |
| same_class | group (intervention/control) | 主要な固定効果 |
| prof_gap | proficiency level | 統制変数（固定効果） |

本章で学んだ手順（ICC → GLM → GLMM-RI → モデル比較 → 交互作用 → OR → emmeans → 診断 → 報告）は、上の対応関係を使ってそのまま適用できます。

## トラブルシューティング

### 収束警告（convergence warning）が出る

**症状**: `Model failed to converge` や `max|grad| = ...` といった警告メッセージ。

**原因**: 最適化アルゴリズムが最大反復回数内に収束しなかった。変数のスケールが大きく異なる場合や、ランダム効果構造が複雑すぎる場合に起こりやすい。

**段階的な修正手順**:

1. **変数の標準化**: 連続予測子を z スコア化する（`scale()`）。これが最も効果的。
2. **オプティマイザ変更**: `glmerControl(optimizer = "bobyqa")` に変更。
3. **反復回数増加**: `optCtrl = list(maxfun = 2e5)` または `5e5`。
4. **別のオプティマイザ**: `nloptwrap` や `optimx` パッケージの `nlminb` を試す。
5. **ランダム構造簡素化**: ランダム傾きを落とす。

```r
# 段階的対処の実装例
control_step1 <- glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 2e5))
control_step2 <- glmerControl(optimizer = "bobyqa", optCtrl = list(maxfun = 5e5))
```

### 特異適合（singular fit）

**症状**: `isSingular(model)` が `TRUE` を返す。`boundary (singular) fit` という警告。

**原因**: ランダム効果の分散成分が 0（またはその境界）に推定された。モデルの複雑さに対してデータが不足している。

**修正手順**:

1. `VarCorr(model)` で分散が 0 に近い項を特定。
2. その項を含むランダム効果を除外してモデルを再推定。
3. 論文には「ランダム傾きを試みたが特異適合が生じたため、ランダム切片モデルを採用した」と記載。

### OR の誤解

**症状**: OR = 2.0 を「確率が 2 倍」と誤って解釈。

**原因**: OR（オッズ比）と確率比（リスク比）の混同。

**正しい理解**: OR = 2.0 は「オッズが 2 倍」。ベースライン確率が低い場合（例：p = .05）は確率もほぼ 2 倍に近いが、ベースラインが高い場合（例：p = .50）はオッズ 2 倍でも確率は .50 → .67 程度の変化にとどまる。

**対処**: OR だけでなく、`emmeans()` による応答スケール予測（確率差）を必ず併記する。

### `re.form` の混同

**症状**: 予測値が観測値と大きくずれる、または個人ごとの予測が全員同じになる。

**原因**: `predict()` の `re.form` 引数の誤用。

**正しい使い分け**:

| 引数 | 意味 | 用途 |
|------|------|------|
| `re.form = NA` | ランダム効果を含まない（母平均的予測）| 論文の主要結果図、一般的な効果の報告 |
| `re.form = NULL` | ランダム効果を含む（条件付き予測）| 個人レベルの予測、モデルの適合度確認 |
| `re.form = ~0` | `NA` と同じ | 同上 |

### DHARMa の警告が出た

**症状**: `testDispersion` や `testUniformity` が有意（p < .05）。

**原因と対処**:

- **testDispersion が有意**: 過分散の可能性。2値データでは観測レベルのランダム効果（`(1 | obs_id)`）を追加するか、`MASS::glmmPQL` を検討。ただし、2値単独観測では過分散は定義上起こらないため、群レベルの集計をしていないか確認。
- **testUniformity が有意**: モデル構造のミスフィット。非線形効果の追加（GAMM への拡張）や、外れ値の確認を検討。
- **有意だが視覚的に問題がない場合**: サンプルサイズが大きいと微小な逸脱でも有意になることがあります。QQ プロットと残差プロットを視覚的に確認し、系統的パターンがなければ実務上は問題ないと判断できます。混合モデルは軽度の分布仮定の逸脱に対して頑健であることがシミュレーションで確認されているため[@schielzeth2020]、検定が有意でも**視覚的に問題がなければ過度に心配する必要はありません**。

### 完全分離（complete separation）

**症状**: `glmer` が収束しない、または係数が極端に大きい（|β| > 10）。

**原因**: ある予測子の値によってアウトカムが完全に予測される（例：same_class = 1 のとき tie が常に 1）。

**修正手順**:

1. クロス表で完全分離の有無を確認: `table(dat_bin$same_class, dat_bin$tie)`
2. 完全分離がある場合、`blme::bglmer()` で弱い事前分布を追加するか、Firth 補正を検討。
3. カテゴリの統合や、問題のある予測子の除外も選択肢。

### グループ数が少ない

**症状**: ランダム効果の分散推定が不安定。推定値が極端に大きいまたは小さい。

**原因**: ランダム効果のグループ数（例：sender 数）が少ない（目安として < 10）と、分散の推定に十分な情報がない。

**対処**:

- グループ数が 5 未満の場合、ランダム効果ではなく固定効果として投入することを検討。
- グループ数が 10–20 の場合、ランダム切片のみに留め、ランダム傾きは避ける。
- ベイズ推定（`brms` パッケージ）は少グループでもより安定した推定を行える。

## 発展学習

本章の内容をさらに深めたい場合、**次の段階の目標**に合わせて文献を選んでください。

::: {.panel-tabset}

### 基礎を固めたい

本章を読んで「ランダム効果って何をしているの？」がまだ曖昧な場合は、以下の順で読み進めると効率的です。

1. @winter2013 — 言語学の例題で、線形モデルから混合モデルへの拡張を段階的に解説。数式よりコードで理解したい人向け。
2. @brown2021 — `lme4` で線形混合モデルを一から実装する R チュートリアル。心理学データを使用。
3. @debruine2021 — 「自分でデータを生成して、ランダム効果が何をしているか体感する」アプローチ。式の意味が腑に落ちます。

### SLA の文脈で学びたい

SLA・応用言語学に特化した GLMM ガイドです。論文の方法欄を書くときにも引用として役立ちます。

- @cunnings2012 — SLA 研究者を対象に、混合モデルの基礎と利点を体系的に解説した先駆的論文。「なぜ ANOVA では不十分か」の議論も含む。
- @linck2015 — ランダム効果構造の選択やモデル比較の手順を SLA データで実演。本章のステップ 2-3 と直接対応する内容。
- @gudmestad2025 — 3 値以上のカテゴリカル目的変数（例：正用・誤用・無反応）を扱う多項ロジスティック混合モデルの実装ガイド。

### アウトカムが 2 値以外のとき

本章は 2 値（binomial）でしたが、同じ GLMM の枠組みを他のデータ型にも拡張できます。

- @jaeger2008; @quene2008 — 2 値データでの GLMM の基礎。本章の理論的背景。
- @lo2015 — **反応時間データ**に対する「変換してから LMM」vs「GLMM（Gamma/逆ガウス）で直接扱う」を比較。変換の問題点を具体的に示した重要論文。
- @brysbaert2018 — 混合モデルでのエフェクトサイズとパワー分析。サンプルサイズ計画に不可欠。

### ランダム効果構造で悩んだら

「最大構造にすべき」vs「節約的にすべき」という論争は、GLMM の最も実務的な難所です。

- @barr2013 — 「Keep it maximal」：確認的仮説検定ではランダム効果構造を最大にすべきという主張。
- @bates2015parsimonious; @matuschek2017 — 「節約的モデリング」：過剰パラメータ化を避け、データが支持する範囲に留めるべきという反論。
- @schielzeth2020 — 混合モデルが分布仮定の逸脱にどこまで耐えるかを大規模シミュレーションで検証。「多少の違反は大丈夫」の根拠。

実務的な判断基準は @meteyard2020 が最もバランスよくまとめています。

### 参考文献ガイド

- **GLMM FAQ**: @glmmfaq — 実務で遭遇するほぼすべての疑問に回答。ブックマーク推奨。
- **p 値の取得**: @lme4pvalues — `lme4` で p 値を得る複数の方法を比較。
- **特異適合の対処**: @lme4issingular; @lme4repca — `isSingular()` と `rePCA()` の公式ドキュメント。
- **モデル診断ツール**: @performance_compare; @performance_checkmodel; @performancepkg — `performance` パッケージの各関数リファレンス。
- **混合モデルの教科書**: @baayen2008; @bates2015; @snijders2012; @hox2018; @bolker2009

### 第5章との接続（GAM/GAMM）

連続予測子に非線形関係が疑われる場合は、GLMM から GAMM へ拡張できます。

- @wood2017; @wood2020; @pedersen2019

:::

## セッション情報（再現性のため）

```{r}
#| eval: true
#| label: session-info

sessionInfo()
```

## 参考文献

::: {#refs}
:::
